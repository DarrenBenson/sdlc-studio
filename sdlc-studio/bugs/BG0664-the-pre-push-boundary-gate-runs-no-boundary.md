# BG0664: the pre-push boundary gate runs NO boundary-only test, so the marker's own promise is false at the boundary it names

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 3; plan rows 11; executed 11; killed 11; survived 0; lines ruled 4; not-run 0; entry point 2 of 3 criteria through the shipped CLI, 1 in-process | fp 6f73dedfbd48 ]]
> **Severity:** Medium
> **Points:** 5
> **Affects:** .githooks/pre-push, .claude/skills/sdlc-studio/scripts/tests/boundary.py, .claude/skills/sdlc-studio/scripts/gate.py, tools/skill-tests.sh, .githooks/commit-msg, tools/tests/test_pre_push_hook.py, tools/tests/test_boundary_marker.py
> **Evidence:** Push of 1a9f948d on 2026-09-10: `gate.py --boundary push` reported 23 lanes, all green, none of them a suite lane (conformance, reconcile, index-derived, validate, constitution, integrity, duplicate-id, provenance, doc-coverage, doc-surface, engagement-floor, disclosure, doc-freshness, mutation, window, hook-enabled, batch-size, changelog-fragments, derived-depth, evidence-drift, release-rehearsal, revert-check, module-alone). CI run 34539944618 on the same commit then failed on a `@boundary_only` test. `SDLC_STUDIO_BOUNDARY_SUITE` is exported at tools/run-suite.sh:192 and .github/workflows/lint.yml:65-80, and nowhere on the push path.
> **Created:** 2026-09-11
> **Created-by:** sdlc-studio file
> **Raised-by:** Claude Opus 5; human; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`boundary.py`'s docstring states that a marked test runs in FULL at every boundary, that `tools/run-suite.sh` sets the marker so every push, release, close and CI run executes it, and that it is deferred only in the per-commit selected run. Measured against what the push actually does, the first half is false. `.githooks/pre-push` runs `gate.py --boundary push`, and that gate runs NO test-suite lane at all: its 23 lanes are the artefact and doc checks plus release-rehearsal, revert-check and module-alone. `module-alone` does run every module, but under the plain unittest runner without `SDLC_STUDIO_BOUNDARY_SUITE`, so every `@boundary_only` test SKIPS there.

So a boundary-only test is executed by CI and by `tools/run-suite.sh`, and by nothing on the push path. The deferral `boundary_only` sells - `it reaches push before it reaches anyone else` - does not happen: it reaches main, and CI is the first thing to run it. Measured on the push of 1a9f948d, whose gate passed every lane while a boundary-only test was red on the very tree being pushed.

## Steps to Reproduce

1. Read the lane list of any `gate.py --boundary push` run: no suite lane appears.
2. `grep -rn SDLC_STUDIO_BOUNDARY_SUITE tools/ .github/` - it is exported by `tools/run-suite.sh` and by the CI workflow, and by nothing the pre-push hook invokes.
3. Mark any cheap test `@boundary_only`, make it fail, and push: the gate passes.
4. Measured instance: the push of 1a9f948d passed the boundary gate and CI then failed on `test_cli_grammar`'s boundary-only control (BG0663).

## Proposed Fix

Decide which is true and make the tree say it. Either the push boundary runs the full suite with the marker set - the reading `boundary.py` already publishes, at the cost of the suite's wall clock on every push - or the docstring is corrected to say that a marked test runs at CI and at `run-suite.sh` only, and the deferral is priced honestly. Whichever is chosen, a test must pin it, because this is a claim about which command runs which tests and nothing reads it today.

## Acceptance Criteria

- [ ] **AC1** Given a throwaway clone carrying the tracked `.githooks/pre-push` and an `sdlc-studio/` project directory (so the tracked gate does not refuse first on `scope: no SDLC project`), a bare remote, a stub `gh` answering "no completed run", and at the hook's `gate.py` path a forwarding shim that execs the TRACKED `gate.py` with the argv the hook handed it plus only `--root <clone> --only module-alone` (and records that argv), whose skill tests directory holds a copy of the tracked `boundary.py` and one `@boundary_only` test that fails with a sentinel message, and with `SDLC_STUDIO_BOUNDARY_SUITE` REMOVED from the environment every fixture push runs under (as AC3 does - CI's lint.yml and `tools/run-suite.sh` export it, and an inherited marker would let the unfixed hook pass): a branch push and a tag push are each REFUSED, the remote receives nothing, the shim recorded `--boundary push` and `--boundary release` from the hook, and the refusal carries `[FAIL] module-alone` with the sentinel message, which only a marked test that EXECUTED can print. The positive control on the same fixture: with the marked test green and writing a sentinel file under the clone, both pushes are accepted and the sentinel file exists, so the marked test ran rather than skipped. Every fixture subprocess reaches `env` as `/usr/bin/env`, never through PATH. Priced, not hidden: the two marked tests in the tree (test_gate's ReleaseRehearsalLaneTests, 228 s; test_cli_grammar's, 83 s) then run inside module-alone, whose slowest module, test_gate, is already about four minutes, so the push boundary grows by up to about four minutes; module-alone reads only the skill tests tree, which is the only tree the marker is used in
  - **Verify:** pytest tools/tests/test_pre_push_hook.py::BoundaryMarkerReachesThePushTests::test_a_red_boundary_only_test_refuses_the_push
  - **Verified:** yes (2026-09-15)
- [ ] **AC2** The runs `boundary.py` claims execute a marked test - in its docstring and in the `boundary_only` skip reason's `(runs at ...)` list - are exactly the runs backed by a live, non-comment command that sets `SDLC_STUDIO_BOUNDARY_SUITE=1`: the hook's `gate.py --boundary push` invocation, its `gate.py --boundary release` invocation, and the CI workflow's suite commands. A run named with no such command behind it (`close` today, which nothing on a close sets) fails, and so does a backed run the claim omits - the paired control that keeps CI in the claim. Read from the hook, the workflow and the marker together, never from two documents agreeing with each other
  - **Verify:** pytest tools/tests/test_boundary_marker.py::MarkerPromiseTests::test_the_docstring_claim_matches_the_paths_that_set_the_marker
  - **Verified:** yes (2026-09-15)
- [ ] **AC3** The per-commit path still defers a marked test. Driving `bash tools/skill-tests.sh <fixture skill dir>` with the marker removed from the environment, over a fixture tests directory holding a copy of the tracked `boundary.py` and one red `@boundary_only` test, exits 0 and reports `OK (skipped=1)` (measured: the plain runner prints the count, not the reason); the positive control, the same fixture with `SDLC_STUDIO_BOUNDARY_SUITE=1`, exits non-zero with the test's sentinel message. And no live, non-comment line of `.githooks/pre-commit`, `.githooks/commit-msg` or `tools/skill-tests.sh` names the marker, so no per-commit invoker can set it. A repair that runs every marked test everywhere removes the deferral rather than fixing the claim, and the marker exists because that cost was measured at 24% of the suite
  - **Verify:** pytest tools/tests/test_boundary_marker.py::MarkerPromiseTests::test_a_marked_test_is_still_deferred_in_the_per_commit_run
  - **Verified:** yes (2026-09-15)

## Impact

The whole point of the pre-push gate is that a red main is prevented rather than discovered. A class of test exists that the gate cannot see, and its own documentation says otherwise - so an author reading `boundary_only` believes deferring a test costs nothing before the push, and it costs a red main. This repository's recorded failure mode is a rule stated in one place and not applied to the thing that exercises it; this is an instance in the gate itself.

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .githooks/pre-push, set `SDLC_STUDIO_BOUNDARY_SUITE=1` only on the release-boundary `gate.py --boundary release` invocation, leaving the branch-ref `gate.py --boundary push` run without the marker | Given a throwaway clone carrying the tracked `.githooks/pre-push` and an `sdlc-studio/` project directory (so the tracked gate does not refuse first on `scope: no SDLC project`), a bare remote, a stub `gh` answering "no completed run", and at the hook's `gate.py` path a forwarding shim that execs the TRACKED `gate.py` with the argv the hook handed it plus only `--root <clone> --only module-alone` (and records that argv), whose skill tests directory holds a copy of the tracked `boundary.py` and one `@boundary_only` test that fails with a sentinel message, and with `SDLC_STUDIO_BOUNDARY_SUITE` REMOVED from the environment every fixture push runs under (as AC3 does - CI's lint.yml and `tools/run-suite.sh` export it, and an inherited marker would let the unfixed hook pass): a branch push and a tag push are each REFUSED, the remote receives nothing, the shim recorded `--boundary push` and `--boundary release` from the hook, and the refusal carries `[FAIL] module-alone` with the sentinel message, which only a marked test that EXECUTED can print. The positive control on the same fixture: with the marked test green and writing a sentinel file under the clone, both pushes are accepted and the sentinel file exists, so the marked test ran rather than skipped. Every fixture subprocess reaches `env` as `/usr/bin/env`, never through PATH. Priced, not hidden: the two marked tests in the tree (test_gate's ReleaseRehearsalLaneTests, 228 s; test_cli_grammar's, 83 s) then run inside module-alone, whose slowest module, test_gate, is already about four minutes, so the push boundary grows by up to about four minutes; module-alone reads only the skill tests tree, which is the only tree the marker is used in |
| AC1 | in .githooks/pre-push, set `SDLC_STUDIO_BOUNDARY_SUITE=1` only on the branch-ref `gate.py --boundary push` invocation, leaving a tag push's `--boundary release` run without it | Given a throwaway clone carrying the tracked `.githooks/pre-push` and an `sdlc-studio/` project directory (so the tracked gate does not refuse first on `scope: no SDLC project`), a bare remote, a stub `gh` answering "no completed run", and at the hook's `gate.py` path a forwarding shim that execs the TRACKED `gate.py` with the argv the hook handed it plus only `--root <clone> --only module-alone` (and records that argv), whose skill tests directory holds a copy of the tracked `boundary.py` and one `@boundary_only` test that fails with a sentinel message, and with `SDLC_STUDIO_BOUNDARY_SUITE` REMOVED from the environment every fixture push runs under (as AC3 does - CI's lint.yml and `tools/run-suite.sh` export it, and an inherited marker would let the unfixed hook pass): a branch push and a tag push are each REFUSED, the remote receives nothing, the shim recorded `--boundary push` and `--boundary release` from the hook, and the refusal carries `[FAIL] module-alone` with the sentinel message, which only a marked test that EXECUTED can print. The positive control on the same fixture: with the marked test green and writing a sentinel file under the clone, both pushes are accepted and the sentinel file exists, so the marked test ran rather than skipped. Every fixture subprocess reaches `env` as `/usr/bin/env`, never through PATH. Priced, not hidden: the two marked tests in the tree (test_gate's ReleaseRehearsalLaneTests, 228 s; test_cli_grammar's, 83 s) then run inside module-alone, whose slowest module, test_gate, is already about four minutes, so the push boundary grows by up to about four minutes; module-alone reads only the skill tests tree, which is the only tree the marker is used in |
| AC1 | in .githooks/pre-push, add `SDLC_STUDIO_BOUNDARY_SUITE=true` to both `/usr/bin/env` invocations instead of `=1`, a value `at_boundary()` does not accept | Given a throwaway clone carrying the tracked `.githooks/pre-push` and an `sdlc-studio/` project directory (so the tracked gate does not refuse first on `scope: no SDLC project`), a bare remote, a stub `gh` answering "no completed run", and at the hook's `gate.py` path a forwarding shim that execs the TRACKED `gate.py` with the argv the hook handed it plus only `--root <clone> --only module-alone` (and records that argv), whose skill tests directory holds a copy of the tracked `boundary.py` and one `@boundary_only` test that fails with a sentinel message, and with `SDLC_STUDIO_BOUNDARY_SUITE` REMOVED from the environment every fixture push runs under (as AC3 does - CI's lint.yml and `tools/run-suite.sh` export it, and an inherited marker would let the unfixed hook pass): a branch push and a tag push are each REFUSED, the remote receives nothing, the shim recorded `--boundary push` and `--boundary release` from the hook, and the refusal carries `[FAIL] module-alone` with the sentinel message, which only a marked test that EXECUTED can print. The positive control on the same fixture: with the marked test green and writing a sentinel file under the clone, both pushes are accepted and the sentinel file exists, so the marked test ran rather than skipped. Every fixture subprocess reaches `env` as `/usr/bin/env`, never through PATH. Priced, not hidden: the two marked tests in the tree (test_gate's ReleaseRehearsalLaneTests, 228 s; test_cli_grammar's, 83 s) then run inside module-alone, whose slowest module, test_gate, is already about four minutes, so the push boundary grows by up to about four minutes; module-alone reads only the skill tests tree, which is the only tree the marker is used in |
| AC1 | in .githooks/pre-push, add the misspelt `SDLC_STUDIO_BOUNDARY_SUITES=1` to both `/usr/bin/env` invocations | Given a throwaway clone carrying the tracked `.githooks/pre-push` and an `sdlc-studio/` project directory (so the tracked gate does not refuse first on `scope: no SDLC project`), a bare remote, a stub `gh` answering "no completed run", and at the hook's `gate.py` path a forwarding shim that execs the TRACKED `gate.py` with the argv the hook handed it plus only `--root <clone> --only module-alone` (and records that argv), whose skill tests directory holds a copy of the tracked `boundary.py` and one `@boundary_only` test that fails with a sentinel message, and with `SDLC_STUDIO_BOUNDARY_SUITE` REMOVED from the environment every fixture push runs under (as AC3 does - CI's lint.yml and `tools/run-suite.sh` export it, and an inherited marker would let the unfixed hook pass): a branch push and a tag push are each REFUSED, the remote receives nothing, the shim recorded `--boundary push` and `--boundary release` from the hook, and the refusal carries `[FAIL] module-alone` with the sentinel message, which only a marked test that EXECUTED can print. The positive control on the same fixture: with the marked test green and writing a sentinel file under the clone, both pushes are accepted and the sentinel file exists, so the marked test ran rather than skipped. Every fixture subprocess reaches `env` as `/usr/bin/env`, never through PATH. Priced, not hidden: the two marked tests in the tree (test_gate's ReleaseRehearsalLaneTests, 228 s; test_cli_grammar's, 83 s) then run inside module-alone, whose slowest module, test_gate, is already about four minutes, so the push boundary grows by up to about four minutes; module-alone reads only the skill tests tree, which is the only tree the marker is used in |
| AC1 | in .claude/skills/sdlc-studio/scripts/gate.py `_module_alone`, add `env.pop("SDLC_STUDIO_BOUNDARY_SUITE", None)` beside the `PYTEST_CURRENT_TEST` pop, so each per-module interpreter loses the marker the hook exported | Given a throwaway clone carrying the tracked `.githooks/pre-push` and an `sdlc-studio/` project directory (so the tracked gate does not refuse first on `scope: no SDLC project`), a bare remote, a stub `gh` answering "no completed run", and at the hook's `gate.py` path a forwarding shim that execs the TRACKED `gate.py` with the argv the hook handed it plus only `--root <clone> --only module-alone` (and records that argv), whose skill tests directory holds a copy of the tracked `boundary.py` and one `@boundary_only` test that fails with a sentinel message, and with `SDLC_STUDIO_BOUNDARY_SUITE` REMOVED from the environment every fixture push runs under (as AC3 does - CI's lint.yml and `tools/run-suite.sh` export it, and an inherited marker would let the unfixed hook pass): a branch push and a tag push are each REFUSED, the remote receives nothing, the shim recorded `--boundary push` and `--boundary release` from the hook, and the refusal carries `[FAIL] module-alone` with the sentinel message, which only a marked test that EXECUTED can print. The positive control on the same fixture: with the marked test green and writing a sentinel file under the clone, both pushes are accepted and the sentinel file exists, so the marked test ran rather than skipped. Every fixture subprocess reaches `env` as `/usr/bin/env`, never through PATH. Priced, not hidden: the two marked tests in the tree (test_gate's ReleaseRehearsalLaneTests, 228 s; test_cli_grammar's, 83 s) then run inside module-alone, whose slowest module, test_gate, is already about four minutes, so the push boundary grows by up to about four minutes; module-alone reads only the skill tests tree, which is the only tree the marker is used in |
| AC2 | in .claude/skills/sdlc-studio/scripts/tests/boundary.py, delete "and CI" from the module docstring's sentence listing where a deferred test executes, leaving push and release only | The runs `boundary.py` claims execute a marked test - in its docstring and in the `boundary_only` skip reason's `(runs at ...)` list - are exactly the runs backed by a live, non-comment command that sets `SDLC_STUDIO_BOUNDARY_SUITE=1`: the hook's `gate.py --boundary push` invocation, its `gate.py --boundary release` invocation, and the CI workflow's suite commands. A run named with no such command behind it (`close` today, which nothing on a close sets) fails, and so does a backed run the claim omits - the paired control that keeps CI in the claim. Read from the hook, the workflow and the marker together, never from two documents agreeing with each other |
| AC2 | in .githooks/pre-push, remove `SDLC_STUDIO_BOUNDARY_SUITE=1` from the `gate.py --boundary release` invocation, leaving boundary.py's text naming release unchanged | The runs `boundary.py` claims execute a marked test - in its docstring and in the `boundary_only` skip reason's `(runs at ...)` list - are exactly the runs backed by a live, non-comment command that sets `SDLC_STUDIO_BOUNDARY_SUITE=1`: the hook's `gate.py --boundary push` invocation, its `gate.py --boundary release` invocation, and the CI workflow's suite commands. A run named with no such command behind it (`close` today, which nothing on a close sets) fails, and so does a backed run the claim omits - the paired control that keeps CI in the claim. Read from the hook, the workflow and the marker together, never from two documents agreeing with each other |
| AC2 | in .claude/skills/sdlc-studio/scripts/tests/boundary.py, edit only the module docstring and keep the `skipUnless` message string untouched, so its word `close` survives | The runs `boundary.py` claims execute a marked test - in its docstring and in the `boundary_only` skip reason's `(runs at ...)` list - are exactly the runs backed by a live, non-comment command that sets `SDLC_STUDIO_BOUNDARY_SUITE=1`: the hook's `gate.py --boundary push` invocation, its `gate.py --boundary release` invocation, and the CI workflow's suite commands. A run named with no such command behind it (`close` today, which nothing on a close sets) fails, and so does a backed run the claim omits - the paired control that keeps CI in the claim. Read from the hook, the workflow and the marker together, never from two documents agreeing with each other |
| AC3 | in .claude/skills/sdlc-studio/scripts/tests/boundary.py, change `at_boundary()` to test `!= "0"` instead of `== "1"`, so an unset marker counts as a boundary run and every marked test runs in the per-commit selection | The per-commit path still defers a marked test. Driving `bash tools/skill-tests.sh <fixture skill dir>` with the marker removed from the environment, over a fixture tests directory holding a copy of the tracked `boundary.py` and one red `@boundary_only` test, exits 0 and reports `OK (skipped=1)` (measured: the plain runner prints the count, not the reason); the positive control, the same fixture with `SDLC_STUDIO_BOUNDARY_SUITE=1`, exits non-zero with the test's sentinel message. And no live, non-comment line of `.githooks/pre-commit`, `.githooks/commit-msg` or `tools/skill-tests.sh` names the marker, so no per-commit invoker can set it. A repair that runs every marked test everywhere removes the deferral rather than fixing the claim, and the marker exists because that cost was measured at 24% of the suite |
| AC3 | in tools/skill-tests.sh, add `export SDLC_STUDIO_BOUNDARY_SUITE=1` before the `python3 -m unittest` calls, so the per-commit suite runner executes every marked test | The per-commit path still defers a marked test. Driving `bash tools/skill-tests.sh <fixture skill dir>` with the marker removed from the environment, over a fixture tests directory holding a copy of the tracked `boundary.py` and one red `@boundary_only` test, exits 0 and reports `OK (skipped=1)` (measured: the plain runner prints the count, not the reason); the positive control, the same fixture with `SDLC_STUDIO_BOUNDARY_SUITE=1`, exits non-zero with the test's sentinel message. And no live, non-comment line of `.githooks/pre-commit`, `.githooks/commit-msg` or `tools/skill-tests.sh` names the marker, so no per-commit invoker can set it. A repair that runs every marked test everywhere removes the deferral rather than fixing the claim, and the marker exists because that cost was measured at 24% of the suite |
| AC3 | in .githooks/commit-msg, prefix the `bash tools/skill-tests.sh "$skill" $skill_sel` lane command with `SDLC_STUDIO_BOUNDARY_SUITE=1`, so the per-commit gate sets the marker for its selected run | The per-commit path still defers a marked test. Driving `bash tools/skill-tests.sh <fixture skill dir>` with the marker removed from the environment, over a fixture tests directory holding a copy of the tracked `boundary.py` and one red `@boundary_only` test, exits 0 and reports `OK (skipped=1)` (measured: the plain runner prints the count, not the reason); the positive control, the same fixture with `SDLC_STUDIO_BOUNDARY_SUITE=1`, exits non-zero with the test's sentinel message. And no live, non-comment line of `.githooks/pre-commit`, `.githooks/commit-msg` or `tools/skill-tests.sh` names the marker, so no per-commit invoker can set it. A repair that runs every marked test everywhere removes the deferral rather than fixing the claim, and the marker exists because that cost was measured at 24% of the suite |

## Coverage Rulings

| File | Line | Hash | Reason | Author | Date |
| --- | --- | --- | --- | --- | --- |
| tools/tests/test_boundary_marker.py | 141 | e97ea59d3080d112 | Defensive: flushes a backslash continuation left open at end of file. Neither .githooks/pre-push nor .github/workflows/lint.yml ends on a dangling backslash, so no live input reaches it. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| tools/tests/test_boundary_marker.py | 150 | e97ea59d3080d112 | Defensive: shlex.split raising ValueError (an unbalanced quote). Every live pre-push and workflow command tokenises, so the except clause is unreachable on the real files. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| tools/tests/test_boundary_marker.py | 151 | e97ea59d3080d112 | Defensive: the return inside the shlex ValueError handler. Every live pre-push and workflow command tokenises, so this refusal is unreachable on the real files. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| tools/tests/test_boundary_marker.py | 154 | e97ea59d3080d112 | Defensive: no token matches the head pattern. Hook commands reach this only after a regex naming gate.py --boundary matched them, and every live CI suite command carries a bash, python or coverage token. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| tools/tests/test_pre_push_hook.py | 163 | 3d93f41d2ab197d5 | test scaffolding: the direct-hook helper is exercised by the tag-first-order test in this module, which sits outside BG0664's own Verify selectors | sdlc-studio | 2026-09-15 |
| tools/tests/test_pre_push_hook.py | 163 | 22a433ae90a866b0 | test scaffolding: the direct-hook helper is exercised by the tag-first-order test in this module, which sits outside BG0664's own Verify selectors | sdlc-studio | 2026-09-15 |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-11 | Claude Opus 5 | Filed |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r1 REJECT answered. AC1 now runs the REAL chain: a clone with the tracked pre-push hook, a stub gh, and a forwarding shim that execs the tracked gate.py with the hook's own argv plus only --root and --only module-alone, over a fixture tests dir holding the tracked boundary.py and one red marked test; both a branch push and a tag push must be refused with the sentinel in the module-alone FAIL line, and the positive control (marked test green, writing a sentinel file) must push and prove the test executed. Re-probed at HEAD: unset and =true pass, =1 fails, at both push and release. New AC1 rows: marker only on the push invocation, =true, a misspelt name, and _module_alone popping the marker (gate.py added to Affects). AC2 now requires the runs boundary.py claims (docstring and the skip reason's list) to equal the runs a live command backs with the marker, and gains a hook-side row removing it from the release invocation and a row leaving close in the skip reason. AC3 drives tools/skill-tests.sh on the fixture with the marker unset (exit 0, OK skipped=1, measured) beside a =1 control, plus a scan of the per-commit invokers; rows added for the marker exported in skill-tests.sh and in commit-msg (both added to Affects). The push cost is priced in AC1: the two marked tests (228 s and 83 s) then run inside module-alone. Not taken: run-suite.sh:188's comment claiming close (pre-existing, outside Affects). Judged 5 points, up from 3. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r2 REJECT answered (one BLOCKING). AC1 now states that every fixture push runs with SDLC_STUDIO_BOUNDARY_SUITE removed from its environment, as AC3 does, because lint.yml and tools/run-suite.sh export it and an inherited marker let the unfixed hook pass rows 1, 2 and 4. Cheap issue taken: AC1's Given now carries an sdlc-studio/ project directory so the tracked gate does not refuse first on scope: no SDLC project. Not taken (advice): boundary.py's BOUNDARY_ENV comment, run-suite.sh:192 in AC2's list, and pricing revert-check's re-run under the marker. |
