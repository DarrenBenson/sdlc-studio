# BG0659: a code span whose value ends in a space cannot be recorded in any review ledger - markdownlint MD038 refuses the row

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 5; plan rows 12; executed 12; killed 12; survived 0; lines ruled 3; not-run 0; entry point 5 of 5 criteria through the shipped CLI, 0 in-process | fp c2946eba1922 ]]
> **Severity:** Medium
> **Points:** 5
> **Affects:** .claude/skills/sdlc-studio/scripts/critic.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py
> **Evidence:** BG0637 revision history, reproductions four to seven (2026-09-09). Delivery review of BG0637, product seat, tagged [pre-existing]: `_clean` returns a span with a trailing space verbatim and .markdownlint.json sets default true.
> **Created:** 2026-09-09
> **Created-by:** sdlc-studio file
> **Raised-by:** Claude Opus 5; human; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`critic._clean` neutralises pipes and newlines and refuses odd backtick parity, but it has nothing to say about a code span whose interior begins or ends with a space. Markdownlint's MD038 refuses exactly that, and `.markdownlint.json` sets default true, so the row reaches the ledger and the NEXT commit that touches the file is blocked - by a rule about a row somebody else wrote. Four of BG0637's seven recorded reproductions are this class, not the parity class BG0637 fixed: a reviewer quoting a bare word with a trailing space, the withdrawal sentinel literal whose trailing space is part of the value, an http verifier with a trailing ellipsis, and a level-two heading marker whose trailing space is the whole point of the quote. There is no way to write any of those in a ledger row today, so the writer refuses to record facts about its own siblings' contracts. BG0634 is Won't Fix on a different premise (a fixed-width truncation that does not exist) and does not discharge this.

## Steps to Reproduce

1. Record a verdict whose finding quotes a literal ending in a space, inside a code span - the withdrawal sentinel is the live example.
2. The row is written: no refusal, no rewrite.
3. Run markdownlint over the ledger. MD038 fires on that row.
4. The next commit touching that file is blocked, naming a row the committer did not write.

## Proposed Fix

Refuse at the write, where the author can still act, the way parity was decided in BG0637: a finding carrying a code span whose interior begins or ends with a space is refused, naming the span and which edge carries the space, and nothing is written. The other candidate - padding the span to CommonMark's form - is REFUTED by execution (2026-09-15, the repository's own markdownlint 0.49.1 with `.markdownlint.json`): `` `budget: ` ``, `` ` budget:  ` ``, `` `  x` `` and `` `  x ` `` each raise MD038. A span padded with ONE space on each side passes, but CommonMark strips that padding, so it renders WITHOUT the edge space: no code span can carry a value whose first or last character is a space, and padding cannot represent one. Padding would have shipped a fix that still blocks the next commit. The refusal's remedy names the two ways that do lint: quote the value without the space and state the space in prose, or move the literal out of the span.

## Acceptance Criteria

The rule the criteria share: a span's interior is judged after CommonMark's padding rule (one space removed from each end when both ends are a space and the interior is not all spaces); what remains beginning or ending in a whitespace character (`str.isspace`, so a tab counts) is refused inside the shared `_clean`, so every ledger writer inherits it. An all-space interior is left undecided (AC3 says why). CLI fixtures stand brief provenance down in the fixture's `.config.yaml` rather than pass `--brief`, so no fingerprint matcher runs and BG0672's marker cannot touch these rows.

- [ ] **AC1** `critic.py record` given an origin-tagged finding holding `` `budget: ` `` exits 2, its stderr quotes that span with its backticks and says `trailing edge`, the word `leading` appears nowhere in the message, and a ledger seeded with its header and one row is byte-identical afterwards
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::CodeSpanEdgeSpaceTests::test_a_trailing_space_span_is_refused_by_the_cli
  - **Verified:** yes (2026-09-15)
- [ ] **AC2** `critic.py record` given an origin-tagged finding holding `` ` budget:` `` exits 2, its stderr quotes that span with its backticks and says `leading edge`, the word `trailing` appears nowhere in the message, and a seeded ledger is byte-identical afterwards
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::CodeSpanEdgeSpaceTests::test_a_leading_space_span_is_refused_by_the_cli
  - **Verified:** yes (2026-09-15)
- [ ] **AC3** A finding whose every span MD038 admits - one with no edge whitespace, ` x ` padded with exactly one space each side, and the double-backtick span ``` `` a `` ``` (the letter a padded one space each side: four backticks, an even count, so BG0637's parity rule admits it at HEAD) - records through `critic.py record` with exit 0 and an Issues cell equal to a pinned literal of what HEAD writes; all-space spans are held out, because MD038 admits them and CommonMark keeps their spaces, so either answer is lint-safe and none is pinned
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::CodeSpanEdgeSpaceTests::test_a_clean_span_records_unchanged
  - **Verified:** yes (2026-09-15)
- [ ] **AC4** Recorded one by one through `record_verdict` into a single ledger, a corpus of `` `budget: ` ``, `` ` budget:` ``, a two-space-padded span, both asymmetric paddings, both tab-edged spans, a double-backtick span with an edge space, ` x ` and ``` `` a `` ```, linted by `node_modules/.bin/markdownlint` under the repository's `.markdownlint.json`, yields no MD038; the ` x ` and ``` `` a `` ``` rows are present, so a writer refusing everything fails; and the refused members written raw, bypassing the writer, each raise MD038, so the harness can see what it judges. Absent markdownlint the test skips naming it, as the repository's other lint-measured tests do. MD010 on any tab is pre-existing and out of scope: the test reads MD038 alone, and the corpus holds no interior tab
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::CodeSpanEdgeSpaceTests::test_every_admitted_span_passes_md038
  - **Verified:** yes (2026-09-15)
- [ ] **AC5** `critic.py evidence` given findings holding `` `budget: ` `` exits 2 with stderr saying `trailing edge` and leaves the evidence ledger as it was, because the refusal lives in the shared `_clean`; the same command with the value trimmed to `budget:` records, the paired control
  - **Verify:** manual - retired by US0918: `critic.py evidence` is retired and its ledger is read by nothing
  - **Verified:** manual (2026-09-26) - retired, superseded by US0918

## Impact

Blocks commits, at a distance from whoever caused it, and silently narrows what a review ledger can say: a reviewer cannot quote a literal whose trailing space is the fact being reported. Four of BG0637's seven blocked commits this week were this class.

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .claude/skills/sdlc-studio/scripts/critic.py `_clean`, replace the refusal of a trailing-edge space with padding the span to CommonMark's form (insert a matching leading space) and write the row - the candidate the bug refuted by execution | `critic.py record` given an origin-tagged finding holding `` `budget: ` `` exits 2, its stderr quotes that span with its backticks and says `trailing edge`, the word `leading` appears nowhere in the message, and a ledger seeded with its header and one row is byte-identical afterwards |
| AC1 | in .claude/skills/sdlc-studio/scripts/critic.py `_clean`, drop the offending span from the refusal message so it names the edge alone and never shows the reviewer which span to fix | `critic.py record` given an origin-tagged finding holding `` `budget: ` `` exits 2, its stderr quotes that span with its backticks and says `trailing edge`, the word `leading` appears nowhere in the message, and a ledger seeded with its header and one row is byte-identical afterwards |
| AC1 | in .claude/skills/sdlc-studio/scripts/critic.py `_clean`, hard-code the edge word the refusal names to `leading`, whichever edge of the span carries the whitespace | `critic.py record` given an origin-tagged finding holding `` `budget: ` `` exits 2, its stderr quotes that span with its backticks and says `trailing edge`, the word `leading` appears nowhere in the message, and a ledger seeded with its header and one row is byte-identical afterwards |
| AC1 | in .claude/skills/sdlc-studio/scripts/critic.py `record_verdict`, call `_write_verdict_row` with the row built from the raw issues first and only then call `_clean(issues)`, so the refusal is raised after the row lands | `critic.py record` given an origin-tagged finding holding `` `budget: ` `` exits 2, its stderr quotes that span with its backticks and says `trailing edge`, the word `leading` appears nowhere in the message, and a ledger seeded with its header and one row is byte-identical afterwards |
| AC2 | in .claude/skills/sdlc-studio/scripts/critic.py `_clean`, test only the LAST character of each span interior for whitespace and drop the first-character check, so a leading-edge space is written | `critic.py record` given an origin-tagged finding holding `` ` budget:` `` exits 2, its stderr quotes that span with its backticks and says `leading edge`, the word `trailing` appears nowhere in the message, and a seeded ledger is byte-identical afterwards |
| AC2 | in .claude/skills/sdlc-studio/scripts/critic.py `_clean`, change every edge refusal to say `leading and trailing edge`, whichever edge carries the whitespace | `critic.py record` given an origin-tagged finding holding `` ` budget:` `` exits 2, its stderr quotes that span with its backticks and says `leading edge`, the word `trailing` appears nowhere in the message, and a seeded ledger is byte-identical afterwards |
| AC3 | in .claude/skills/sdlc-studio/scripts/critic.py _clean, replace the per-span-interior edge check with a regex over the whole value (a backtick beside a space), so ordinary prose round a span is refused | A finding whose every span MD038 admits - one with no edge whitespace, ` x ` padded with exactly one space each side, and the double-backtick span ``` `` a `` ``` (the letter a padded one space each side: four backticks, an even count, so BG0637's parity rule admits it at HEAD) - records through `critic.py record` with exit 0 and an Issues cell equal to a pinned literal of what HEAD writes; all-space spans are held out, because MD038 admits them and CommonMark keeps their spaces, so either answer is lint-safe and none is pinned |
| AC3 | in .claude/skills/sdlc-studio/scripts/critic.py _clean, change the edge check to refuse any span whose interior begins with a space, so a symmetrically padded span that lints clean is refused | A finding whose every span MD038 admits - one with no edge whitespace, ` x ` padded with exactly one space each side, and the double-backtick span ``` `` a `` ``` (the letter a padded one space each side: four backticks, an even count, so BG0637's parity rule admits it at HEAD) - records through `critic.py record` with exit 0 and an Issues cell equal to a pinned literal of what HEAD writes; all-space spans are held out, because MD038 admits them and CommonMark keeps their spaces, so either answer is lint-safe and none is pinned |
| AC3 | in .claude/skills/sdlc-studio/scripts/critic.py _clean, strip CommonMark's one-space padding only from single-backtick spans, so the padded double-backtick span around the letter a is judged raw, its leading space seen, and refused | A finding whose every span MD038 admits - one with no edge whitespace, ` x ` padded with exactly one space each side, and the double-backtick span ``` `` a `` ``` (the letter a padded one space each side: four backticks, an even count, so BG0637's parity rule admits it at HEAD) - records through `critic.py record` with exit 0 and an Issues cell equal to a pinned literal of what HEAD writes; all-space spans are held out, because MD038 admits them and CommonMark keeps their spaces, so either answer is lint-safe and none is pinned |
| AC4 | in .claude/skills/sdlc-studio/scripts/critic.py `_clean`, exempt every span whose interior both begins and ends with a space rather than exactly one space each side of a non-space interior, so two-space and asymmetric paddings are written | Recorded one by one through `record_verdict` into a single ledger, a corpus of `` `budget: ` ``, `` ` budget:` ``, a two-space-padded span, both asymmetric paddings, both tab-edged spans, a double-backtick span with an edge space, ` x ` and ``` `` a `` ```, linted by `node_modules/.bin/markdownlint` under the repository's `.markdownlint.json`, yields no MD038; the ` x ` and ``` `` a `` ``` rows are present, so a writer refusing everything fails; and the refused members written raw, bypassing the writer, each raise MD038, so the harness can see what it judges. Absent markdownlint the test skips naming it, as the repository's other lint-measured tests do. MD010 on any tab is pre-existing and out of scope: the test reads MD038 alone, and the corpus holds no interior tab |
| AC4 | in .claude/skills/sdlc-studio/scripts/critic.py `_clean`, compare each span edge with the space character U+0020 instead of calling `str.isspace`, so a tab-edged span is written | Recorded one by one through `record_verdict` into a single ledger, a corpus of `` `budget: ` ``, `` ` budget:` ``, a two-space-padded span, both asymmetric paddings, both tab-edged spans, a double-backtick span with an edge space, ` x ` and ``` `` a `` ```, linted by `node_modules/.bin/markdownlint` under the repository's `.markdownlint.json`, yields no MD038; the ` x ` and ``` `` a `` ``` rows are present, so a writer refusing everything fails; and the refused members written raw, bypassing the writer, each raise MD038, so the harness can see what it judges. Absent markdownlint the test skips naming it, as the repository's other lint-measured tests do. MD010 on any tab is pre-existing and out of scope: the test reads MD038 alone, and the corpus holds no interior tab |
| AC5 | in .claude/skills/sdlc-studio/scripts/critic.py, move the edge check out of `_clean` into `record_verdict`, applied to its issues argument only, so `record_evidence` writes the span | `critic.py evidence` given findings holding `` `budget: ` `` exits 2 with stderr saying `trailing edge` and leaves the evidence ledger as it was, because the refusal lives in the shared `_clean`; the same command with the value trimmed to `budget:` records, the paired control |

## Coverage Rulings

| File | Line | Hash | Reason | Author | Date |
| --- | --- | --- | --- | --- | --- |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6444 | 9710ddd81b1c0402 | withdrawn 2026-09-15: Withdrawn by its author to correct a factual slip in the reason text (the binary or directory it named was misstated); the same line is re-ruled at once with an accurate reason. (was: Defensive: markdownlint printing output that is not JSON. The installed markdownlint-cli2 is invoked with --json and emits JSON, so the handler is not reached on a working install.) | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6445 | 9710ddd81b1c0402 | Defensive: the failure message inside the JSONDecodeError handler for markdownlint output that is not JSON; the installed binary emits JSON under --json, so no run reaches it. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6520 | 9710ddd81b1c0402 | A skip: fires only when node_modules markdownlint or node is absent. Both are installed in this clone, so the MD038 corpus check ran rather than skipping. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6444 | 9710ddd81b1c0402 | Defensive: markdownlint printing output that is not JSON. node_modules/.bin/markdownlint (markdownlint-cli) is invoked with --json and emits JSON, so the handler is not reached on a working install. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/critic.py | 289 | 96238b789f9a9553 | an all-space span has no edge to judge and is admitted; the spec holds all-space spans out of every fixture and out of scope, so no criterion reaches it | sdlc-studio | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/critic.py | 289 | dcca627ee2a2afd5 | an all-space span has no edge to judge and is admitted; the spec holds all-space spans out of every fixture and out of scope, so no criterion reaches it | sdlc-studio | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6751 | d9a28d09aaa9ed32 | test harness: the handler for markdownlint returning output that is not JSON; the installed markdownlint always returns JSON | sdlc-studio | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6752 | d9a28d09aaa9ed32 | the failure message of the same non-JSON handler | sdlc-studio | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6827 | d9a28d09aaa9ed32 | skip when markdownlint is not installed; it is installed in this repository | sdlc-studio | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/critic.py | 289 | 4b1aeb1ba7d53f2d | an all-space span has no edge to judge and is admitted; the spec holds all-space spans out of every fixture and out of scope, so no criterion reaches it. Re-ruled at its new line: another batch unit's edit to the file voided the earlier ruling by whole-file hash (BG0706). | Claude Opus 5 (authoring session) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6827 | 5057f80808e19d85 | test harness: the handler for markdownlint returning output that is not JSON; the installed markdownlint-cli is invoked with --json and always returns JSON, so no run reaches it. Re-ruled at its new line: another batch unit's edit to the file voided the earlier ruling by whole-file hash (BG0706). | Claude Opus 5 (authoring session) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6828 | 5057f80808e19d85 | the failure message of the same non-JSON handler as the line above; unreachable on a working install for the same reason. Re-ruled at its new line: another batch unit's edit to the file voided the earlier ruling by whole-file hash (BG0706). | Claude Opus 5 (authoring session) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6903 | 5057f80808e19d85 | skip when node_modules markdownlint or node is absent; both are installed in this clone, so the corpus check runs rather than skipping. Re-ruled at its new line: another batch unit's edit to the file voided the earlier ruling by whole-file hash (BG0706). | Claude Opus 5 (authoring session) | 2026-09-15 |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-09 | Claude Opus 5 | Filed |
| 2026-09-09 | Claude Opus 5 | Reproduced on the day it was filed, and it blocked this run's commit. Recording BG0608's three delivery verdicts wrote four rows quoting the budget command's own prefix - a code span whose value ends in a space, which is the whole of this bug - and markdownlint refused both ledgers. Repaired by hand to make the tree committable: the span was rewritten to name the dash in prose. That is a fifth reproduction of the class on top of BG0637's four, and the second time this week that a reviewer could not quote a literal the tool itself prints |
| 2026-09-10 | Claude Opus 5 | Ruled OPEN for v5.1 on 2026-09-10, under D0186's disclosure half. It is a defect in what a reviewer can RECORD, not in what the tool decides: a value whose code span ends in a space is refused by markdownlint after `_clean` writes it, and the reviewer's remedy - trim the span or restate the value - costs one edit and loses nothing. Three points against a run already over its appetite, in the same function BG0637 has just been through twice; changing `_clean` again in the same week is how the third round of a repair gets shipped untested. It ships disclosed, and it is the first Medium on the next batch. |
| 2026-09-15 | sprint planning 2026-09-15 | Groomed for the next batch: the design choice the bug left open is decided by execution. Padding a code span to CommonMark's form still raises MD038 under the repository's markdownlint 0.49.1, so the fix is to refuse at the write, naming the span and the edge, as parity is refused. Four real criteria replace the two tool-derived ones. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r1 REJECT answered. The edge rule is stated once for all criteria: a span interior is judged after CommonMark's one-space padding rule and what remains beginning or ending in whitespace (str.isspace, tabs included) is refused inside the shared _clean. AC1/AC2 now pin the edge word (the refusal says trailing edge or leading edge and the other edge word appears nowhere), with rows for a hard-coded edge word and a both-edges message, and pin writes-nothing as a byte-identical SEEDED ledger (record_verdict writes the header before _clean runs - pre-existing, so the fixture seeds rather than relying on an absent file), with a write-before-clean row. AC4 now lints a corpus holding two-space, both asymmetric and both tab-edged spans through node_modules/.bin/markdownlint, with the loose symmetric-exemption mutant and a U+0020-only mutant replacing the equivalent newline one, a positive control that the lint-clean spans are admitted, and a raw-corpus control that each refused member raises MD038. AC3 gains a double-backtick padding row; all-space spans are held out of every fixture because MD038 admits them. New AC5 pins a second writer (evidence) inheriting the refusal from _clean. CLI fixtures stand brief provenance down in config rather than pass --brief, so BG0672's marker cannot reach these rows. MD010 on any tab is pre-existing and out of scope. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r2 REJECT answered. BLOCKING (AC3/AC4): the positive control that was a double-backtick span around one lone backtick is refused at HEAD by BG0637's parity rule (five backticks, odd), so a correct build failed both criteria. Replaced by a double-backtick span around the letter a padded one space each side (four backticks, even): measured at HEAD, critic.py record exits 0 on a finding holding it beside the no-edge and one-space-padded spans, and the written ledger lints clean under the repository's markdownlint. AC3's single-backtick-only padding mutant now discriminates: the correct build strips the padding and admits the span, the mutant judges its interior raw, sees the leading space and refuses it. ISSUE taken: a new AC1 row for a refusal that names the edge but drops the offending span, which the 'quotes that span with its backticks' clause kills. The pre-existing nested-backtick parity defect and the unpinned repair/signoff/sprint-review/supersede writers stay out of scope. |
| 2026-09-26 | US0918 | AC5 retired in the D0259 pattern: `critic.py evidence` is retired and its ledger is read by nothing |
