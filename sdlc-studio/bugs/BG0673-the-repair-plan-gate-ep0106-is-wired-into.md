# BG0673: the repair-plan gate (EP0106) is wired into nothing: no command records a plan or verdict, and turning review.repair_plan_gate on refuses nothing a delivery command runs

> **Status:** In Progress
> **Verification depth:** functional [[derived: criteria 7; plan rows 16; executed 16; killed 16; survived 0; not-run 0; entry point 7 of 7 criteria through the shipped CLI, 0 in-process | fp cac488b1cd0f ]]
> **Severity:** Medium
> **Points:** 8
> **Affects:** .claude/skills/sdlc-studio/scripts/repair_plan.py, .claude/skills/sdlc-studio/scripts/transition.py, .claude/skills/sdlc-studio/scripts/critic.py, .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py, .claude/skills/sdlc-studio/scripts/tests/test_transition.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py, .claude/skills/sdlc-studio/reference-scripts.md, .claude/skills/sdlc-studio/reference-scripts-review.md, .claude/skills/sdlc-studio/reference-scripts-surface.md
> **Created:** 2026-09-15
> **Created-by:** sdlc-studio file
> **Raised-by:** backlog sweep 2026-09-15; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`repair_plan.py` (EP0106, RFC0053, D0054) ships `record_repair_plan` (line 158), `review_repair_plan` (233) and `repair_gate` (298), and a `review.repair_plan_gate` setting (default off). None of the three has a caller outside `repair_plan.py` itself; its CLI offers only `brief` and `gate`, so neither a plan nor its verdict can be recorded by any command; `critic.repair_provenance` (critic.py:2957) is defined and never called. A project that turns the setting on gets no refusal from any command it actually runs - the shipped-but-unwired gate LL0027 names. CR0544 asks for a repair-approach review as if none existed; this is its prior art. Reproduced at 51f264db by grep and `repair_plan.py --help`; found by the 2026-09-15 backlog sweep.

## Steps to Reproduce

1. `python3 .claude/skills/sdlc-studio/scripts/repair_plan.py --help` - verbs are only {brief, gate}.
2. `grep -rn 'repair_gate\|record_repair_plan\|review_repair_plan' .claude/skills/sdlc-studio/scripts --include=*.py | grep -v /tests/` - every hit is inside `repair_plan.py.`
3. `grep -rn 'repair_provenance(' ... | grep -v 'def '` - no caller.

## Proposed Fix

Wire it (operator ruling, 2026-09-15 sprint planning): add record and review verbs to repair_plan.py that key a plan to the unit it repairs (so a plan is found by the unit's id, not by a free plan id the transition cannot know), and call `repair_gate` from the transition that terminal-closes a repair unit when the setting is on. CR0544 asks for exactly this capability, so this builds on RFC0053's shipped half rather than replacing it. Rounds, brief provenance, the approval pin and plan-author independence moved to BG0678.

## Acceptance Criteria

- [ ] **AC1** With `review.repair_plan_gate: on`, `transition.py set` taking a bug that has no repair plan to Fixed exits non-zero, its refusal names `review.repair_plan_gate` (other gates also refuse a bug going to Fixed, so the assertion is on this gate's own key, not on a refusal), and the bug file still reads its prior status
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_the_gate_refuses_at_the_terminal_transition
  - **Verified:** yes (2026-09-15)
- [ ] **AC2** A repair plan keyed to the unit it repairs, and its verdict, record through `repair_plan.py record` and `repair_plan.py review`; `review` run with the plan's recorded author as reviewer exits non-zero naming independence and writes no plan-review row, beside the positive case where a different reviewer's verdict is written
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_a_plan_and_verdict_record_through_the_cli
  - **Verified:** yes (2026-09-15)
- [ ] **AC3** With the setting off, the AC1 fixture (a bug with no repair plan) reaches Fixed and no output names `review.repair_plan_gate` - the paired control, beside AC1's refusal of the same fixture with the setting on
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_the_gate_off_changes_nothing
  - **Verified:** yes (2026-09-15)
- [ ] **AC4** A repair-plan verdict written by `repair_plan.py review` carries the kind `repair-plan`, a member of `critic.PLAN_REVIEW_KINDS`, and is invisible to the spec and test-plan verdict lookups: `critic.verdict_for(root, unit, phase="plan-review", kind=...)` returns None for `spec` and for `test-plan`, beside the positive control that `kind="repair-plan"` returns the APPROVE (the repair ledger's reading of the same row is AC6's)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_a_repair_plan_approval_is_not_a_spec_plan_review
  - **Verified:** yes (2026-09-15)
- [ ] **AC5** With `review.repair_plan_gate: on` and no plan recorded, a story whose `Parent` names a BG id and one whose `Delivers` names an RV id are each refused at Done naming the gate, while a bug set to `Won't Fix` and a story whose Parent names an epic produce no output naming `review.repair_plan_gate`. Each story fixture otherwise passes every other Done gate (its executable criteria stamped passed, below `review.two_role_after`, `review.test_plan_after` unset), shown in the same test by the same two repair stories reaching Done with the setting off, so a correct build cannot be refused for a reason other than this gate
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_the_gate_holds_repair_stories_and_spares_decision_terminals
  - **Verified:** yes (2026-09-15)
- [ ] **AC6** A repair-plan REJECT holds nothing in the plan-review repair ledger, whatever `review.repair_plan_gate` reads: a unit whose `critic.py record --kind test-plan` REJECT was answered by a complete `critic.py repair --phase plan-review` reads `critic.plan_review_repair_clears` as (True, "") before and after `repair_plan.py review --verdict REJECT` records a repair-plan REJECT on the same unit id; and on a unit whose only plan-review REJECT is a repair-plan one, `critic.py repair --phase plan-review` exits non-zero naming no live REJECT and appends no repair row, beside the same command on a unit carrying a test-plan REJECT, which exits 0
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_a_repair_plan_reject_does_not_hold_the_test_plan_repair_clause
  - **Verified:** yes (2026-09-15)
- [ ] **AC7** With `review.repair_plan_gate: on` and `review.test_plan_after` unset, AC3's bug gets a repair plan recorded by `repair_plan.py record` under the bug's own id by one author, and a DIFFERENT reviewer APPROVEs it with `repair_plan.py review`; `transition.py set` taking the bug to Fixed then exits 0, the bug file reads Fixed, and no output names `review.repair_plan_gate`. The gate-on positive control beside AC1's refusal: without it a transition that asks `repair_gate` about the wrong plan id, or a `plan_reviewed` that reads a kind `review` never writes, passes AC1-AC6 while no gated repair unit can ever close. This fixture needs nothing from BG0678: one round, one plan, one approval
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_the_gate_on_lets_an_independently_approved_plan_reach_fixed
  - **Verified:** yes (2026-09-15)

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .claude/skills/sdlc-studio/scripts/transition.py, catch the ValueError repair_plan.repair_gate raises in _pre_write_gates and return it as the advisory gate_warn instead of appending it to blocks, so the write proceeds | With `review.repair_plan_gate: on`, `transition.py set` taking a bug that has no repair plan to Fixed exits non-zero, its refusal names `review.repair_plan_gate` (other gates also refuse a bug going to Fixed, so the assertion is on this gate's own key, not on a refusal), and the bug file still reads its prior status |
| AC1 | in .claude/skills/sdlc-studio/scripts/transition.py, call repair_plan.repair_gate only when target_canon is Done, so a bug reaching Fixed is never checked | With `review.repair_plan_gate: on`, `transition.py set` taking a bug that has no repair plan to Fixed exits non-zero, its refusal names `review.repair_plan_gate` (other gates also refuse a bug going to Fixed, so the assertion is on this gate's own key, not on a refusal), and the bug file still reads its prior status |
| AC2 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, add only a `record` subparser to build_parser and no verb that calls review_repair_plan, so the plan's verdict still has no command | A repair plan keyed to the unit it repairs, and its verdict, record through `repair_plan.py record` and `repair_plan.py review`; `review` run with the plan's recorded author as reviewer exits non-zero naming independence and writes no plan-review row, beside the positive case where a different reviewer's verdict is written |
| AC2 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, have the new review verb call critic.record_verdict directly instead of review_repair_plan, skipping the independence check against the plan's recorded author | A repair plan keyed to the unit it repairs, and its verdict, record through `repair_plan.py record` and `repair_plan.py review`; `review` run with the plan's recorded author as reviewer exits non-zero naming independence and writes no plan-review row, beside the positive case where a different reviewer's verdict is written |
| AC3 | in .claude/skills/sdlc-studio/scripts/transition.py, call repair_plan.plan_reviewed directly from _pre_write_gates and block when it is not ok, bypassing the gate_enabled check repair_gate makes | With the setting off, the AC1 fixture (a bug with no repair plan) reaches Fixed and no output names `review.repair_plan_gate` - the paired control, beside AC1's refusal of the same fixture with the setting on |
| AC4 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, drop the kind argument from review_repair_plan's critic.record_verdict call, so the approval is written under the default spec kind | A repair-plan verdict written by `repair_plan.py review` carries the kind `repair-plan`, a member of `critic.PLAN_REVIEW_KINDS`, and is invisible to the spec and test-plan verdict lookups: `critic.verdict_for(root, unit, phase="plan-review", kind=...)` returns None for `spec` and for `test-plan`, beside the positive control that `kind="repair-plan"` returns the APPROVE (the repair ledger's reading of the same row is AC6's) |
| AC4 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, call critic.record_verdict twice in review_repair_plan, once with kind repair-plan and once with the default kind | A repair-plan verdict written by `repair_plan.py review` carries the kind `repair-plan`, a member of `critic.PLAN_REVIEW_KINDS`, and is invisible to the spec and test-plan verdict lookups: `critic.verdict_for(root, unit, phase="plan-review", kind=...)` returns None for `spec` and for `test-plan`, beside the positive control that `kind="repair-plan"` returns the APPROVE (the repair ledger's reading of the same row is AC6's) |
| AC5 | in .claude/skills/sdlc-studio/scripts/transition.py, call `repair_plan.repair_gate` only when type_ is bug instead of reading `is_repair_unit`, so a story delivering a finding reaches Done unplanned | With `review.repair_plan_gate: on` and no plan recorded, a story whose `Parent` names a BG id and one whose `Delivers` names an RV id are each refused at Done naming the gate, while a bug set to `Won't Fix` and a story whose Parent names an epic produce no output naming `review.repair_plan_gate`. Each story fixture otherwise passes every other Done gate (its executable criteria stamped passed, below `review.two_role_after`, `review.test_plan_after` unset), shown in the same test by the same two repair stories reaching Done with the setting off, so a correct build cannot be refused for a reason other than this gate |
| AC5 | in .claude/skills/sdlc-studio/scripts/transition.py, call repair_plan.repair_gate for every terminal status via sdlc_md.is_terminal_status instead of only Done and Fixed | With `review.repair_plan_gate: on` and no plan recorded, a story whose `Parent` names a BG id and one whose `Delivers` names an RV id are each refused at Done naming the gate, while a bug set to `Won't Fix` and a story whose Parent names an epic produce no output naming `review.repair_plan_gate`. Each story fixture otherwise passes every other Done gate (its executable criteria stamped passed, below `review.two_role_after`, `review.test_plan_after` unset), shown in the same test by the same two repair stories reaching Done with the setting off, so a correct build cannot be refused for a reason other than this gate |
| AC5 | in .claude/skills/sdlc-studio/scripts/transition.py, call repair_plan.repair_gate for every unit reaching Done without asking is_repair_unit | With `review.repair_plan_gate: on` and no plan recorded, a story whose `Parent` names a BG id and one whose `Delivers` names an RV id are each refused at Done naming the gate, while a bug set to `Won't Fix` and a story whose Parent names an epic produce no output naming `review.repair_plan_gate`. Each story fixture otherwise passes every other Done gate (its executable criteria stamped passed, below `review.two_role_after`, `review.test_plan_after` unset), shown in the same test by the same two repair stories reaching Done with the setting off, so a correct build cannot be refused for a reason other than this gate |
| AC5 | in .claude/skills/sdlc-studio/scripts/transition.py, test only the story's Parent field for a BG or RV prefix before calling repair_plan.repair_gate, ignoring Delivers | With `review.repair_plan_gate: on` and no plan recorded, a story whose `Parent` names a BG id and one whose `Delivers` names an RV id are each refused at Done naming the gate, while a bug set to `Won't Fix` and a story whose Parent names an epic produce no output naming `review.repair_plan_gate`. Each story fixture otherwise passes every other Done gate (its executable criteria stamped passed, below `review.two_role_after`, `review.test_plan_after` unset), shown in the same test by the same two repair stories reaching Done with the setting off, so a correct build cannot be refused for a reason other than this gate |
| AC6 | in .claude/skills/sdlc-studio/scripts/critic.py, remove the kind filter from repair_state, so it reads every plan-review verdict row and the repair-plan row's `plan=` and `findings-hash=` cells count as outstanding findings | A repair-plan REJECT holds nothing in the plan-review repair ledger, whatever `review.repair_plan_gate` reads: a unit whose `critic.py record --kind test-plan` REJECT was answered by a complete `critic.py repair --phase plan-review` reads `critic.plan_review_repair_clears` as (True, "") before and after `repair_plan.py review --verdict REJECT` records a repair-plan REJECT on the same unit id; and on a unit whose only plan-review REJECT is a repair-plan one, `critic.py repair --phase plan-review` exits non-zero naming no live REJECT and appends no repair row, beside the same command on a unit carrying a test-plan REJECT, which exits 0 |
| AC6 | in .claude/skills/sdlc-studio/scripts/critic.py, drop the kind argument from record_repair's verdict_for call, so a plan-review repair accepts closures against a repair-plan rejection | A repair-plan REJECT holds nothing in the plan-review repair ledger, whatever `review.repair_plan_gate` reads: a unit whose `critic.py record --kind test-plan` REJECT was answered by a complete `critic.py repair --phase plan-review` reads `critic.plan_review_repair_clears` as (True, "") before and after `repair_plan.py review --verdict REJECT` records a repair-plan REJECT on the same unit id; and on a unit whose only plan-review REJECT is a repair-plan one, `critic.py repair --phase plan-review` exits non-zero naming no live REJECT and appends no repair row, beside the same command on a unit carrying a test-plan REJECT, which exits 0 |
| AC7 | in .claude/skills/sdlc-studio/scripts/transition.py, replace the unit id handed to repair_plan.repair_gate with None | With `review.repair_plan_gate: on` and `review.test_plan_after` unset, AC3's bug gets a repair plan recorded by `repair_plan.py record` under the bug's own id by one author, and a DIFFERENT reviewer APPROVEs it with `repair_plan.py review`; `transition.py set` taking the bug to Fixed then exits 0, the bug file reads Fixed, and no output names `review.repair_plan_gate`. The gate-on positive control beside AC1's refusal: without it a transition that asks `repair_gate` about the wrong plan id, or a `plan_reviewed` that reads a kind `review` never writes, passes AC1-AC6 while no gated repair unit can ever close. This fixture needs nothing from BG0678: one round, one plan, one approval |
| AC7 | in .claude/skills/sdlc-studio/scripts/transition.py, hard-code the id handed to repair_plan.repair_gate as `RP-` prefixed to the unit id | With `review.repair_plan_gate: on` and `review.test_plan_after` unset, AC3's bug gets a repair plan recorded by `repair_plan.py record` under the bug's own id by one author, and a DIFFERENT reviewer APPROVEs it with `repair_plan.py review`; `transition.py set` taking the bug to Fixed then exits 0, the bug file reads Fixed, and no output names `review.repair_plan_gate`. The gate-on positive control beside AC1's refusal: without it a transition that asks `repair_gate` about the wrong plan id, or a `plan_reviewed` that reads a kind `review` never writes, passes AC1-AC6 while no gated repair unit can ever close. This fixture needs nothing from BG0678: one round, one plan, one approval |
| AC7 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, change plan_reviewed's verdict_for kind argument from `repair-plan` to `spec` | With `review.repair_plan_gate: on` and `review.test_plan_after` unset, AC3's bug gets a repair plan recorded by `repair_plan.py record` under the bug's own id by one author, and a DIFFERENT reviewer APPROVEs it with `repair_plan.py review`; `transition.py set` taking the bug to Fixed then exits 0, the bug file reads Fixed, and no output names `review.repair_plan_gate`. The gate-on positive control beside AC1's refusal: without it a transition that asks `repair_gate` about the wrong plan id, or a `plan_reviewed` that reads a kind `review` never writes, passes AC1-AC6 while no gated repair unit can ever close. This fixture needs nothing from BG0678: one round, one plan, one approval |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-15 | backlog sweep 2026-09-15 | Filed |
| 2026-09-15 | sprint planning 2026-09-15 | Operator ruling at sprint planning: WIRE the gate, do not retire it. The retire alternatives are removed from the fix and the criteria. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan r1 REJECT answered under the operator's WIRE ruling. A repair-plan verdict is to be recorded under a distinct plan-review kind, repair-plan, added to critic.PLAN_REVIEW_KINDS and read by plan_reviewed with that kind, so an earlier test-plan REJECT on the unit id cannot hide the approval and it cannot count as a spec or test-plan review (new AC5 positive control, new AC6). AC4 now pins the lookup by unit id (an approved plan for another unit is refused as no plan) and adds the self-reviewed plan; AC2 adds the independence refusal of the plan's author and its positive case; AC1 asserts the gate's own key and AC3 that the bug reaches Fixed with the setting off; new AC7 covers stories whose Parent or Delivers names a BG/RV and spares Won't Fix. Left open: the review-after-repair ordering (US0312 AC4) - the transition has no repair timestamp and critic rows carry a date only. Size now judged 8 points. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan r2 REJECT answered under the author's four design rulings. Storage: every round of a repair plan is kept under the unit id, never overwritten, so repeat_rounds counts rounds on one unit and US0343's threshold binds (new AC9); the verdict pin covers the whole plan (findings and entries), so a post-approval re-record loses the approval (new AC10). Brief provenance: repair_plan.py review writes the fingerprint of the finding set the plan answers into the Brief cell - the key every round of one unit's plan shares, and the only one under which a revised round's APPROVE can retire the round-one REJECT through _unanswered_rejects; a whole-plan hash there would never retire it, and AC5 carries that as a mutant (AC5 gains the reject-revise-approve fixture and leaves review.test_plan_after unset). Independence: plan_reviewed also compares the reviewer with the plan's recorded author; AC4 now pins both self-review shapes through critic.py record --kind repair-plan, beside a third-identity control. Kind-aware repair ledger: repair_state and record_repair skip repair-plan rows (new AC8); AC6's words narrowed to the verdict_for lookups its fixture covers. Still open and untracked: the review-after-repair ordering (US0312 AC4); project_upgrade.py:678's kind-blind plan-review read is pre-existing. Honest size is now 13 points against the 8 in the header; recommended split: AC1-3, AC6-8 (wiring, kind, spared test-plan ledger, 8) and AC4, AC5, AC9, AC10 (rounds, brief, pin, plan-author independence, 5, depends on the first). Implementer note: RepairPlanPinTests and DesignDecisionTests re-record under one id and must stay green under round storage. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Split at 13 points: this unit keeps the wiring, the repair-plan kind and the spared test-plan ledger (former AC1-3 and AC6-8, now AC1-AC6, 8 points); rounds, brief provenance, the approval pin and plan-author independence (former AC4, AC5, AC9, AC10) moved to BG0678, which depends on this unit. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r3 REJECT answered (one BLOCKING). New AC7 is the gate-on positive control in this unit alone: AC3's bug with review.repair_plan_gate on and review.test_plan_after unset, a plan recorded by repair_plan.py record under the bug's id, a different reviewer's APPROVE through repair_plan.py review, and transition.py set then reaches Fixed with the file reading Fixed and no output naming the gate. Rows: transition.py hands repair_gate None, or an RP- id derived from the unit id; plan_reviewed reads verdict_for with kind spec instead of repair-plan. Issue taken: AC5's story fixtures pass every other Done gate, shown by the same stories reaching Done with the setting off. Left open: US0312 AC4 ordering still has no id (filing it is outside this file); project_upgrade.py:678 is pre-existing. Points unchanged at 8. |
