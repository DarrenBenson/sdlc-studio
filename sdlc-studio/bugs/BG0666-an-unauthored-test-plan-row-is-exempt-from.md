# BG0666: an UNAUTHORED Test Plan row is exempt from the quality guard an authored one must pass, so leaving the placeholder buys a clean derive

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 3; plan rows 15; executed 15; killed 15; survived 0; not-run 0; entry point 3 of 3 criteria through the shipped CLI, 0 in-process | fp 67b7ebe3c0f1 ]]
> **Severity:** Medium
> **Points:** 3
> **Affects:** .claude/skills/sdlc-studio/scripts/verify_ac.py, .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py, .claude/skills/sdlc-studio/scripts/critic.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py
> **Evidence:** verify_ac.py:3711 `if mutant != _TESTPLAN_PLACEHOLDER:` guards the `testplan_row_faults` call. Measured on BG0663, 2026-09-11: the table held the placeholder three times, `derive --dry-run` reported `unchanged - 3 row(s) already match its 3 criteria, and 0 authored mutant(s) were kept`, and the reviewer's brief rendered the placeholders back. Writing the ledger's three real mutants in produced exit 2 with five faults.
> **Created:** 2026-09-11
> **Created-by:** sdlc-studio file
> **Raised-by:** Claude Opus 5; human; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`verify_ac.py testplan derive` refuses a mutant cell that names no path from the unit's `Affects`, carries no edit verb, or restates more than 60% of its own criterion. It skips all of those checks for a row still equal to `_TESTPLAN_PLACEHOLDER`: the loop reads `if mutant != _TESTPLAN_PLACEHOLDER:` before calling `testplan_row_faults`.

So a unit whose author ran `derive` and never wrote the mutants passes every check, reports `N row(s) for N criteria`, and hands a reviewer a plan that names nothing. Authoring the real mutants is what risks a refusal. The incentive points the wrong way, and the state is invisible from the command's own output.

Found by an independent test-plan reviewer on BG0663, whose table held the placeholder three times while the real mutants existed only in the gitignored ledger - in no clone, no diff and no brief. The seat's own words: the placeholder is why nobody saw this.

## Steps to Reproduce

1. Run `verify_ac.py testplan derive --unit <id>` on a unit with no authored mutants: it writes `{{name the production change this test must fail on}}` per criterion and exits 0.
2. Run it again: `unchanged - N row(s) already match its N criteria, and 0 authored mutant(s) were kept`. Nothing says the rows name nothing.
3. Now author a real mutant that names no `Affects` path: it is REFUSED at exit 2.
4. So the unauthored row passes a check the authored one fails.

## Proposed Fix

Count the unauthored criteria and REPORT them - by criterion - on the two surfaces where a plan is read before review: `derive`'s own summary and the plan-review brief `critic.py brief --phase plan-review` renders. One helper in `verify_ac.py` decides which criteria are unauthored and renders one shared note; both surfaces call it, so they cannot disagree about a plan. A criterion is unauthored when ANY of its rows is still `_TESTPLAN_PLACEHOLDER` - one authored row beside a placeholder does not make it authored, because the placeholder row is a claim nobody has made. A row declared `unnameable: <reason>` is authored. A placeholder is a legitimate intermediate state while a unit is being written, so this is a report at exit 0 rather than a refusal at `derive`, and the `if mutant != _TESTPLAN_PLACEHOLDER` exemption stays. The terminal transition's plan gate is OUT OF SCOPE for this bug: no criterion here covers it, and it is a separate surface to file on its own if wanted.

## Acceptance Criteria

- [ ] **AC1** Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints
  - **Verify:** manual - retired by US0912: `verify_ac.py testplan derive` and the plan-row readers were deleted; a unit's criteria and their Verify selectors are its test plan
  - **Verified:** manual (2026-09-25) - retired, superseded by US0912
- [ ] **AC2** Given the same mixed plan as AC1's first derive leaves it, when `critic.py brief --unit <id> --seat qa --phase plan-review` runs through the CLI, then the brief carries the same shared note, naming AC2, AC3 and AC4 and not AC1. The brief is what a reviewer judges from, and one that renders a placeholder as though it were a plan is what produced the rejection this bug was filed from; today's brief already renders the placeholder text inside the table, so only the note's own sentence can satisfy this
  - **Verify:** manual - retired by US0915: the plan-review brief was deleted with plan review, so no command renders it
  - **Verified:** manual (2026-09-25) - retired, superseded by US0915
- [ ] **AC3** Given a plan whose every row is authored, one of them declared `unnameable: <reason>`, when `testplan derive` runs through the CLI, then it exits 0 on it and its output does not carry the shared note's sentence - a derive refusing the plan at exit 2 prints no note either, and would pass a silence-only check. Positive control beside it: the same command on AC1's mixed plan DOES carry it, so the silence is not a surface that never prints. A checker that always warns is one nobody reads, and it would fire on every unit mid-authoring
  - **Verify:** manual - retired by US0912: `verify_ac.py testplan derive` and the plan-row readers were deleted; a unit's criteria and their Verify selectors are its test plan
  - **Verified:** manual (2026-09-25) - retired, superseded by US0912

## Impact

A test plan is the load-bearing half of a criterion - it is what says the criterion can fail. A plan of placeholders reads as complete to `derive`, to the reviewer's brief, and to the test-plan gate, so the review that exists to check the mutants is handed nothing to check. It cost a REJECT and a re-review on BG0663.

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, print the note on the `->` write branch of `cmd_testplan` only and leave the `unchanged - N row(s) already match` return as it is today, so the re-run BG0663's author read still names nothing | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, move the note's `print` inside the `if res.get("unchanged")` block of `cmd_testplan`, leaving the `->` path bare | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, change the unauthored helper's `any(...)` over a criterion's rows to `all(...)`, so a criterion holding one authored row beside a placeholder is counted as authored | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, change the note to list every criterion id whenever any row is `_TESTPLAN_PLACEHOLDER`, so an authored criterion is named too | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, remove the `if mutant != _TESTPLAN_PLACEHOLDER` exemption in `testplan_derive`, so each placeholder is judged by `testplan_row_faults` and refused at exit 2 with its criterion named on stderr | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, print the note with `file=sys.stderr` instead of to stdout | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, call the unauthored helper on the `text` `testplan_derive` read at its start rather than on the rows it derived, so a criterion whose placeholder this same run writes goes unnamed until a later run | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, set the note's count to the number of rows equal to `_TESTPLAN_PLACEHOLDER` rather than the number of criteria it names | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC1 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, hard-code the note's count from `res['authored']` in `cmd_testplan` instead of the length of the helper's criterion list | Given a MIXED plan of four criteria - AC1 three authored rows, AC2 two placeholder rows, AC3 one authored row plus one placeholder row, AC4 NO row at all - with stale titles, when `verify_ac.py testplan derive --unit` runs twice through the CLI (the first run writes AC4's placeholder itself and re-syncs the titles on the `->` write branch, the second takes the `unchanged` branch), then BOTH runs exit 0 and print on STDOUT the shared unauthored note naming AC2, AC3 and AC4, the count 3, and not AC1, while STDERR names no criterion. The first run must name AC4, whose row did not exist until that run wrote it. The fixture keeps the figures apart - 4 authored rows, 4 placeholder rows once derived, 4 criteria, 3 unauthored - so only a count of unauthored criteria prints 3. The test asserts the note's own sentence, imported from `verify_ac.py`, never the placeholder string or the existing `authored mutant(s)` count, which today's output already prints |
| AC2 | in .claude/skills/sdlc-studio/scripts/critic.py, drop the unauthored-note line from `_plan_review_brief`, so `critic.py brief --phase plan-review` renders the placeholder rows with no note | Given the same mixed plan as AC1's first derive leaves it, when `critic.py brief --unit <id> --seat qa --phase plan-review` runs through the CLI, then the brief carries the same shared note, naming AC2, AC3 and AC4 and not AC1. The brief is what a reviewer judges from, and one that renders a placeholder as though it were a plan is what produced the rejection this bug was filed from; today's brief already renders the placeholder text inside the table, so only the note's own sentence can satisfy this |
| AC2 | in .claude/skills/sdlc-studio/scripts/critic.py, replace the call to verify_ac's shared unauthored helper in `_plan_review_brief` with a local count of cells equal to the placeholder that prints its own wording, so the brief and derive disagree | Given the same mixed plan as AC1's first derive leaves it, when `critic.py brief --unit <id> --seat qa --phase plan-review` runs through the CLI, then the brief carries the same shared note, naming AC2, AC3 and AC4 and not AC1. The brief is what a reviewer judges from, and one that renders a placeholder as though it were a plan is what produced the rejection this bug was filed from; today's brief already renders the placeholder text inside the table, so only the note's own sentence can satisfy this |
| AC2 | in .claude/skills/sdlc-studio/scripts/critic.py, wrap the note in `_plan_review_brief` in an `all(...)` test over every table row, so a partly authored plan gets none | Given the same mixed plan as AC1's first derive leaves it, when `critic.py brief --unit <id> --seat qa --phase plan-review` runs through the CLI, then the brief carries the same shared note, naming AC2, AC3 and AC4 and not AC1. The brief is what a reviewer judges from, and one that renders a placeholder as though it were a plan is what produced the rejection this bug was filed from; today's brief already renders the placeholder text inside the table, so only the note's own sentence can satisfy this |
| AC3 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, drop the empty-list guard on the note, so both surfaces print it with an empty criterion list for a fully authored plan | Given a plan whose every row is authored, one of them declared `unnameable: <reason>`, when `testplan derive` runs through the CLI, then it exits 0 on it and its output does not carry the shared note's sentence - a derive refusing the plan at exit 2 prints no note either, and would pass a silence-only check. Positive control beside it: the same command on AC1's mixed plan DOES carry it, so the silence is not a surface that never prints. A checker that always warns is one nobody reads, and it would fire on every unit mid-authoring |
| AC3 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, change the unauthored helper to count a row starting `unnameable` as well as one equal to `_TESTPLAN_PLACEHOLDER`, so a declared-unnameable criterion is reported | Given a plan whose every row is authored, one of them declared `unnameable: <reason>`, when `testplan derive` runs through the CLI, then it exits 0 on it and its output does not carry the shared note's sentence - a derive refusing the plan at exit 2 prints no note either, and would pass a silence-only check. Positive control beside it: the same command on AC1's mixed plan DOES carry it, so the silence is not a surface that never prints. A checker that always warns is one nobody reads, and it would fire on every unit mid-authoring |
| AC3 | in .claude/skills/sdlc-studio/scripts/verify_ac.py, change `testplan_row_faults` to return a fault for every row starting `unnameable`, so `derive` refuses that plan at exit 2 and prints nothing to stdout | Given a plan whose every row is authored, one of them declared `unnameable: <reason>`, when `testplan derive` runs through the CLI, then it exits 0 on it and its output does not carry the shared note's sentence - a derive refusing the plan at exit 2 prints no note either, and would pass a silence-only check. Positive control beside it: the same command on AC1's mixed plan DOES carry it, so the silence is not a surface that never prints. A checker that always warns is one nobody reads, and it would fire on every unit mid-authoring |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-11 | Claude Opus 5 | Filed |
| 2026-09-11 | Claude Opus 5 | Duplicate warning against BG0597 answered: they are distinct. BG0597 was derive DESTROYING an authored row when a criterion carried more than one, and it is Fixed. This is the opposite direction - an UNAUTHORED row is exempt from the quality guard an authored one must pass, so leaving the placeholder is the cheaper path. They share two files and a vocabulary, which is what the 43% similarity measured |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r1 REJECT answered. BLOCKING: AC1 and AC2 now run on a MIXED plan (AC1 authored, AC2 placeholder only, AC3 one authored row beside a placeholder) and assert only AC2 and AC3 are named with count 2, killing a build that names every criterion whenever any row is a placeholder; AC2 is driven through critic.py brief --phase plan-review and moved to test_critic.py, with critic.py mutants (note dropped from `_plan_review_brief`, a local count with its own wording, an all-placeholder-only condition); AC1 carries the exemption-removal mutant, pinned by exit 0 and stdout-only naming. Issues: AC1 covers both the first-derive write branch and the unchanged branch; every surface asserts one shared note sentence imported from verify_ac.py, not the placeholder string or today's count; AC3 checks both surfaces with that sentence, carries an unnameable row, and has the mixed plan as its positive control. Proposed Fix states a criterion is unauthored if ANY of its rows is the placeholder, and strikes the terminal plan gate as out of scope. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r2 REJECT answered. BLOCKING: AC1's fixture now has four criteria and AC4 carries NO row before the first run, so derive writes its placeholder itself and the first `->` run must name it; a new AC1 row mutates the helper to read the `text` testplan_derive started from instead of the rows it derived, which leaves AC4 unnamed on that run. Issues: AC1 has three authored rows and AC2 two placeholder rows, keeping the figures apart (4 authored, 4 placeholder rows once derived, 4 criteria, 3 unauthored), with rows for a count of placeholder rows and a count taken from `res['authored']`; AC1 needed three authored rows rather than the two proposed, because with AC4 added two would make authored equal the unauthored count of 3. AC2 now runs on the plan as AC1's first derive leaves it and names AC4 too. AC3 asserts derive exits 0 on the fully authored plan, with a row that makes `testplan_row_faults` refuse every `unnameable` row. |
| 2026-09-25 | Claude Opus 5.5 | AC2 retired by US0915 (D0259 pattern): the plan-review brief was deleted |
| 2026-09-25 | Claude Opus 5.5 | AC3 narrowed by US0915 (D0259 pattern): `critic.py brief --phase plan-review` was deleted with plan review, so `derive` is the surface the criterion and its test still cover |
| 2026-09-25 | Claude Opus 5.5 | AC1, AC3 retired by US0912 (D0259 pattern): `testplan derive` and its unauthored-row note were deleted |
