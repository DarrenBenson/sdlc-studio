# BG0645: critic.py brief --rejoinder ignores --phase plan-review and renders the delivery brief, so a re-review of a rejected test plan is briefed with a diff scope that does not exist

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 2; plan rows 17; executed 17; killed 17; survived 0; not-run 0; entry point 2 of 2 criteria through the shipped CLI, 0 in-process | fp 39eaccfa70bc ]]
> **Severity:** Medium
> **Points:** 2
> **Affects:** .claude/skills/sdlc-studio/scripts/critic.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py, .claude/skills/sdlc-studio/reference-scripts-review.md
> **Created:** 2026-09-04
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

critic.py brief --unit X --seat Y --phase plan-review --rejoinder <verdict file> emits the DELIVERY re-review brief: 'You did NOT author this diff', a 'Diff scope (the unit's declared Affects - inspect with git diff/status)' block, and no test-plan section or plan-review return contract. The plan-review phase, which the same command renders correctly without --rejoinder, is dropped on the rejoinder path. A round-two plan review is therefore briefed to look for a diff before any code exists, which is the exact failure the plan-review brief's own text warns against. Hit on 2026-09-04 briefing round two of a six-unit plan review after three seats rejected in round one.

## Steps to Reproduce

1. critic.py brief --unit BG0641 --seat qa --phase plan-review > a.md (correct: test-plan section, no diff scope). 2. critic.py brief --unit BG0641 --seat qa --phase plan-review --rejoinder <a VERDICT/ISSUES/BLOCKING file> > b.md. 3. b.md carries 'Diff scope' and 'this diff' and no test plan; the phase flag had no effect.

## Proposed Fix

Route the rejoinder through the same phase switch as the first-round brief: a plan-review rejoinder quotes the prior verdict verbatim, restates the criteria and the CURRENT test plan (the artefact has been re-authored since the rejection - that is what a re-review judges), demands the probes be re-executed, and returns the plan-review contract. Pin it with a test that renders a plan-review rejoinder and asserts the absence of a diff scope and the presence of the test plan, beside the delivery-rejoinder control.

## Acceptance Criteria

- [ ] **AC1** Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::PlanReviewBriefTests::test_a_plan_review_rejoinder_keeps_the_plan_review_shape_through_the_cli
  - **Verified:** yes (2026-09-07)
- [ ] **AC2** Given a prior verdict file holding the block of a delivery REJECT recorded for the unit, when the same command runs with the default phase, then stdout carries the `Diff scope (` block header as today and the prior verdict under RE-REVIEW, and stderr carries the same fingerprint footer, recognised by `record --brief` and carrying `--tier-explicit` when the tier was chosen - the control, and the footer the delivery rejoinder had also lacked - no record held that defect until this unit
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::PlanReviewBriefTests::test_a_delivery_rejoinder_still_carries_the_diff_scope_and_a_footer
  - **Verified:** yes (2026-09-07)

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `critic.py` `cmd_brief`, resolve the phase from the rejoinder flag (delivery whenever a rejoinder is given) - today's code | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, render the plan-review charter but keep the `Diff scope (` block, so the shape is half plan-review | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, render the plan-review rejoinder without the Test Plan table | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, print no fingerprint footer on the rejoinder path - today's code | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, copy the delivery footer onto the plan-review rejoinder unchanged, so it tells the reviewer to record into the delivery ledger | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py` `rejoinder_brief`, keep appending the delivery return block on a plan-review rejoinder - today's code | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, accept `--tier` on the plan-review rejoinder instead of refusing it as the plain plan brief does (delivery, from the diff's own branch) | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, print the plan footer's record command without `--reviewer` and `--author`, so it exits 2 as printed (delivery review r1, product seat) | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, leave the `--rejoinder` help string describing the delivery shape only (delivery review r1, product seat) | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, print a constant in the plan rejoinder's footer instead of the fingerprint (delivery review r1, qa seat) | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, have the matcher compare first-round fingerprints only, so a rejoinder's is never recognised (delivery review r1, qa seat) | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, quote only the prior verdict's ISSUES line (delivery review r1, qa seat) | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, keep the delivery RE-EXECUTE demand on the plan rejoinder (delivery review r1, qa seat) | Given a prior verdict file holding the block of a plan-review REJECT recorded for the unit (the rejoinder reads the file, never the ledger), when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim - all three lines - under the RE-REVIEW section with an instruction to rule each finding CLOSED, OVER-CLAIMED or MOVED against the table rather than to re-execute mutants that do not exist, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes and RECOGNISES - the fingerprint identifies the rejoinder's base brief and phase, never the quoted prior, so the matcher reproduces it and no unrecognised-brief note is printed on an honest re-review - worded as the PLAN-REVIEW return contract (`record --phase plan-review`, with `--reviewer` and `--author` so it runs as printed), never the delivery block's; `--tier` is refused on the plan-review rejoinder as on the plain plan brief, and the flag's own `--help` describes the plan-review shape - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC2 | in `critic.py`, drop `--tier-explicit` from the delivery rejoinder's footer (delivery review r1, qa seat) | Given a prior verdict file holding the block of a delivery REJECT recorded for the unit, when the same command runs with the default phase, then stdout carries the `Diff scope (` block header as today and the prior verdict under RE-REVIEW, and stderr carries the same fingerprint footer, recognised by `record --brief` and carrying `--tier-explicit` when the tier was chosen - the control, and the footer the delivery rejoinder had also lacked - no record held that defect until this unit |
| AC2 | in `critic.py`, fingerprint the delivery rejoinder over its whole text, prior included, so the matcher cannot reproduce it (delivery review r1, qa seat) | Given a prior verdict file holding the block of a delivery REJECT recorded for the unit, when the same command runs with the default phase, then stdout carries the `Diff scope (` block header as today and the prior verdict under RE-REVIEW, and stderr carries the same fingerprint footer, recognised by `record --brief` and carrying `--tier-explicit` when the tier was chosen - the control, and the footer the delivery rejoinder had also lacked - no record held that defect until this unit |
| AC2 | in `critic.py`, drop the `Diff scope (` block from every rejoinder whatever the phase | Given a prior verdict file holding the block of a delivery REJECT recorded for the unit, when the same command runs with the default phase, then stdout carries the `Diff scope (` block header as today and the prior verdict under RE-REVIEW, and stderr carries the same fingerprint footer, recognised by `record --brief` and carrying `--tier-explicit` when the tier was chosen - the control, and the footer the delivery rejoinder had also lacked - no record held that defect until this unit |
| AC2 | in `critic.py`, print the footer on the plan-review rejoinder only | Given a prior verdict file holding the block of a delivery REJECT recorded for the unit, when the same command runs with the default phase, then stdout carries the `Diff scope (` block header as today and the prior verdict under RE-REVIEW, and stderr carries the same fingerprint footer, recognised by `record --brief` and carrying `--tier-explicit` when the tier was chosen - the control, and the footer the delivery rejoinder had also lacked - no record held that defect until this unit |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-04 | sdlc-studio | Filed |
| 2026-09-07 | sdlc-studio | Plan review, four rounds (2026-09-04 to 2026-09-07, 18 seat verdicts): AC1 re-authored per the seats - the Test Plan table named as the object under re-review, assertions anchored on the block header and the charter line rather than bare words, a prior verdict FILE as the Given, the plan-review footer worded for the plan ledger; AC2 added as the delivery control. Delivered 2026-09-07: rejoinder_brief takes its phase, cmd_brief refuses --tier on a plan rejoinder and prints a footer in both phases; run through the shipped command on this repository in both phases before briefing; a ninth row added for the tier refusal from the diff's own branch |
| 2026-09-07 | sdlc-studio | Delivery review r1 (product REJECT, qa REJECT, engineering APPROVE): the --rejoinder help string and reference-scripts-review.md described the delivery shape only, now both describe both; the rejoinder's fingerprint identifies its base brief and phase and the matcher reproduces it, so record --brief recognises it without the unrecognised-brief note (the item filed into CR0511 for it is closed by this unit); the plan footer is one function for both plan paths and its record command runs as printed; the prior verdict's three lines, the plan re-review's ruling instruction and --tier-explicit are pinned; AC1 and AC2 say what shipped and the CR0329 citation is dropped (that record never held the defect); eight plan rows added |
| 2026-09-07 | sdlc-studio | Delivery review r2: product APPROVE, qa APPROVE. Residuals: the reference sentence is terminated; the --tier refusal is one constant for both plan paths; the fragment states what the rejoinder fingerprint does not bind - a re-review of an unchanged artefact shares its predecessor's fingerprint, and a tier chosen against the derived one still misses as at HEAD; the matcher's derived-tier arm is correct and unpinned (a light-band fixture is CR0511's rejoinder item's territory) |
