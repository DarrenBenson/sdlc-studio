# BG0645: critic.py brief --rejoinder ignores --phase plan-review and renders the delivery brief, so a re-review of a rejected test plan is briefed with a diff scope that does not exist

> **Status:** Open
> **Verification depth:** functional [[derived: criteria 2; plan rows 8; EVIDENCE ABSENT - the mutation ledger holds no entry for this unit, which is not the same fact as nought killed; NOT RUN 8 (AC1 row 0, AC1 row 1, AC1 row 2, AC1 row 3, AC1 row 4, AC1 row 5, AC2 row 0, AC2 row 1); entry point 0 of 2 criteria through the shipped CLI, 0 in-process; 2 undetermined (the named node could not be isolated) | fp 1e6181b9f638 ]]
> **Severity:** Medium
> **Points:** 2
> **Affects:** .claude/skills/sdlc-studio/scripts/critic.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py
> **Created:** 2026-09-04
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

critic.py brief --unit X --seat Y --phase plan-review --rejoinder <verdict file> emits the DELIVERY re-review brief: 'You did NOT author this diff', a 'Diff scope (the unit's declared Affects - inspect with git diff/status)' block, and no test-plan section or plan-review return contract. The plan-review phase, which the same command renders correctly without --rejoinder, is dropped on the rejoinder path. A round-two plan review is therefore briefed to look for a diff before any code exists, which is the exact failure the plan-review brief's own text warns against. Hit on 2026-09-04 briefing round two of a six-unit plan review after three seats rejected in round one.

## Steps to Reproduce

1. critic.py brief --unit BG0641 --seat qa --phase plan-review > a.md (correct: test-plan section, no diff scope). 2. critic.py brief --unit BG0641 --seat qa --phase plan-review --rejoinder <a VERDICT/ISSUES/BLOCKING file> > b.md. 3. b.md carries 'Diff scope' and 'this diff' and no test plan; the phase flag had no effect.

## Proposed Fix

Route the rejoinder through the same phase switch as the first-round brief: a plan-review rejoinder quotes the prior verdict verbatim, restates the criteria and the CURRENT test plan (the artefact has been re-authored since the rejection - that is what a re-review judges), demands the probes be re-executed, and returns the plan-review contract. Pin it with a test that renders a plan-review rejoinder and asserts the absence of a diff scope and the presence of the test plan, beside the delivery-rejoinder control.

## Acceptance Criteria

- [ ] **AC1** Given a unit with a plan-review REJECT recorded, when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim under the RE-REVIEW section, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes, worded as the PLAN-REVIEW return contract (`record --phase plan-review`), never the delivery block's - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::PlanReviewBriefTests::test_a_plan_review_rejoinder_keeps_the_plan_review_shape_through_the_cli
- [ ] **AC2** Given a unit with a delivery REJECT recorded, when the same command runs with the default phase, then stdout carries the `Diff scope (` block header as today and the prior verdict under RE-REVIEW, and stderr carries the same fingerprint footer - the control, and the footer the delivery rejoinder has also lacked (CR0329)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::PlanReviewBriefTests::test_a_delivery_rejoinder_still_carries_the_diff_scope_and_a_footer

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `critic.py` `cmd_brief`, resolve the phase from the rejoinder flag (delivery whenever a rejoinder is given) - today's code | Given a unit with a plan-review REJECT recorded, when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim under the RE-REVIEW section, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes, worded as the PLAN-REVIEW return contract (`record --phase plan-review`), never the delivery block's - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, render the plan-review charter but keep the `Diff scope (` block, so the shape is half plan-review | Given a unit with a plan-review REJECT recorded, when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim under the RE-REVIEW section, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes, worded as the PLAN-REVIEW return contract (`record --phase plan-review`), never the delivery block's - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, render the plan-review rejoinder without the Test Plan table | Given a unit with a plan-review REJECT recorded, when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim under the RE-REVIEW section, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes, worded as the PLAN-REVIEW return contract (`record --phase plan-review`), never the delivery block's - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, print no fingerprint footer on the rejoinder path - today's code | Given a unit with a plan-review REJECT recorded, when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim under the RE-REVIEW section, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes, worded as the PLAN-REVIEW return contract (`record --phase plan-review`), never the delivery block's - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py`, copy the delivery footer onto the plan-review rejoinder unchanged, so it tells the reviewer to record into the delivery ledger | Given a unit with a plan-review REJECT recorded, when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim under the RE-REVIEW section, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes, worded as the PLAN-REVIEW return contract (`record --phase plan-review`), never the delivery block's - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC1 | in `critic.py` `rejoinder_brief`, keep appending the delivery return block on a plan-review rejoinder - today's code | Given a unit with a plan-review REJECT recorded, when the SHIPPED command `critic.py brief --unit <id> --seat <seat> --phase plan-review --rejoinder <verdict file>` runs as a subprocess, then its stdout carries the plan-review charter (`There is NO diff scope`), NO `Diff scope (` block header at the start of any line (asserted on the header, because this unit's own criteria quote the words), the criteria as law, the unit's CURRENT Test Plan table (the object a plan re-review judges, which the bug's Summary names as half the defect), and the prior verdict verbatim under the RE-REVIEW section, closing with the PLAN-REVIEW return contract - the stdout `Return EXACTLY` block that names a plan verdict's shape (critic.py holds two such blocks, delivery and plan-review, and `rejoinder_brief` appends the delivery one today), never the delivery block with its origin tags and `git log -S`; and its stderr carries the fingerprint footer that `critic.py record --brief` consumes, worded as the PLAN-REVIEW return contract (`record --phase plan-review`), never the delivery block's - a phase-blind footer copied from delivery would send a plan verdict into the delivery ledger, which `record` accepts with a note and conformance then reads as a diff review |
| AC2 | in `critic.py`, drop the `Diff scope (` block from every rejoinder whatever the phase | Given a unit with a delivery REJECT recorded, when the same command runs with the default phase, then stdout carries the `Diff scope (` block header as today and the prior verdict under RE-REVIEW, and stderr carries the same fingerprint footer - the control, and the footer the delivery rejoinder has also lacked (CR0329) |
| AC2 | in `critic.py`, print the footer on the plan-review rejoinder only | Given a unit with a delivery REJECT recorded, when the same command runs with the default phase, then stdout carries the `Diff scope (` block header as today and the prior verdict under RE-REVIEW, and stderr carries the same fingerprint footer - the control, and the footer the delivery rejoinder has also lacked (CR0329) |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-04 | sdlc-studio | Filed |
