# BG0646: status.py takes 113 seconds on this corpus, so the command every session is ordered to run first times out under a two-minute tool default

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 3; plan rows 11; executed 11; killed 10; survived 0; ruled equivalent 1; not-run 0; retracted 1; entry point 1 of 2 criteria through the shipped CLI, 0 in-process; 1 undetermined (the named node could not be isolated) | fp d7e78f5d5b25 ]]
> **Severity:** Medium
> **Points:** 8
> **Affects:** .claude/skills/sdlc-studio/scripts/status.py, .claude/skills/sdlc-studio/scripts/close_owed.py, .claude/skills/sdlc-studio/scripts/lib/sdlc_md.py, .claude/skills/sdlc-studio/scripts/tests/test_status.py, .claude/skills/sdlc-studio/scripts/tests/test_close_owed.py, .claude/skills/sdlc-studio/scripts/tests/test_sdlc_md.py, .claude/skills/sdlc-studio/scripts/handoff.py, .claude/skills/sdlc-studio/scripts/tests/test_handoff.py
> **Created:** 2026-09-04
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

AGENTS.md orders every session to run status second, before acting. Measured on 2026-09-04 over 814 stories and 644 bugs: 113 s wall clock (rc 0), and an earlier invocation in the same session was killed by a 120 s tool timeout with the output lost. A read-only dashboard that costs two minutes is one an agent learns to skip, which is the failure mode AGENTS.md names for this repository. The cost is not stated by the command and nothing prints before the census completes.

## Steps to Reproduce

1. time python3 .claude/skills/sdlc-studio/scripts/status.py on this tree. 2. Observe about 113 s with no output until the end. 3. Run it under a 120 s tool timeout and observe the kill.

## Proposed Fix

Profile the census: the likely cost is per-artefact parsing repeated across the requirements, bugs, reviews and backlog passes plus the already-delivered advisory's pairwise title comparison over 256 open artefacts. Parse each artefact once and share the census across passes, cache the parse keyed on file mtime under sdlc-studio/.local, print the headline lines before the advisories, and record the measured duration in gate-timings so a regression is visible. Target: under 15 s on this corpus.

## Acceptance Criteria

- [x] **AC1** Given a fixture corpus the test generates in THIS corpus's shape - 2,340 artefacts across stories, bugs, epics, CRs and 26 reviews, of which 190 are TERMINAL epics with children and NO retro covering them (so the close-owed advisory enters `_breakdown_child_ids` about 190 times, the 189 measured here being that count on this corpus) and 45 are open discovery items, with a close-owed baseline stamped so the advisory passes its unbaselined return and reads `retros/VELOCITY.md` (a 200-entry `sdlc-studio/retros/evidence/actuals-<date>.jsonl` sits beside it as corpus shape - the advisory does not read it), and a RUN OPEN whose batch exercises EVERY branch of handoff's predicate - two delivered units, one terminal unit whose verify report is red, one open unit, one closed without delivery, one id with no file on disk, and a unit the loop quarantined outside the batch, four remaining by hand - when `status.py` runs as a SUBPROCESS over it, then the first stdout line is the `Run:` headline, flushed and printed from the run record BEFORE the census and carrying `remaining=4)` - the count `handoff.build` derives for the same batch, asserted on the CLOSED token so a count that grew a digit cannot pass as a prefix (pinned IN-PROCESS as well as through the pipe: a test patches `gather` to assert the `Run:` line is already in the stdout buffer AND `sys.stdout.flush` has been called when `gather` is entered, and that `handoff.build` was never called - the flush spied, since a StringIO cannot see a missing flush and a cached gather finishes inside any clock bound; through the pipe the `Run:` line precedes every census-derived line and reaches it within 3 s), and the process exits 0 within 15 s - the shipped command measured end to end, never this repository (BG0647 removed exactly that); today's code over this fixture measured 45 s at plan review (engineering seat, 45 gather and 189 advisory walks exact) and the sweep-absence mutant (the ledger's AC1 row 1) re-measured by hand at delivery runs it in 23 s, above the bound, the figure recorded in the revision row, because `sdlc_md.children_of` walks and reads the whole corpus on each of its 45 gather calls and 189 advisory calls
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_status.py::GatherPerformanceTests
  - **Verified:** yes (2026-09-07)
- [x] **AC2** Given the same fixture, when `gather` and then `close_owed_advisory` run in ONE process inside one `corpus_cache()` sweep, then across both each ledger and artefact is read at most once - counted by one spy on `pathlib.Path.read_text` spanning both calls, since half the reads bypass `read_text_safe` - because the advisory reuses the sweep the gather opened: the walk hands each file's text to `read_text_safe` for the rest of that sweep and `children_of` answers from the sweep's child index, a memo that is the SWEEP's - opened and closed by the `with`, never a cache that outlives it, the placement `corpus_cache()`'s docstring refuses - so `children_of` called with no sweep open still walks and reads, the control, asserted by a second no-sweep call re-reading the corpus; and the SHIPPED command enters the advisory inside the same sweep it took the census in, asserted by identity, never after it closed or in a sweep of its own
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_close_owed.py::AdvisoryCostTests::test_gather_and_advisory_read_each_file_at_most_once_between_them
  - **Verified:** yes (2026-09-07)
- [x] **AC3** Given this corpus, when `python3 status.py` runs after delivery, then it completes in under 15 s with no run open and under 20 s with one open - which requires the `Run:` line's remaining count to come from a cheap entry point IN `handoff.py` that reuses handoff's own terminal-versus-dropped predicate (quarantined loop units and missing files counted as handoff counts them, the reason status.py says the dashboard must never own that predicate) rather than through `handoff.build` and its conformance detection (62.7 s on this corpus with a run open, a path the fixture cannot see) - recorded by hand at delivery as the reproduction of the 53 to 115 s figures, not a suite test
  - **Verify:** manual - time `python3 .claude/skills/sdlc-studio/scripts/status.py` on this tree at delivery, with and without a run open, and record both figures in the revision row

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `status.py`, print the `Run:` headline only after the full gather returns, so it is not the first line to reach the pipe (goal review, product seat) | Given a fixture corpus the test generates in THIS corpus's shape - 2,340 artefacts across stories, bugs, epics, CRs and 26 reviews, of which 190 are TERMINAL epics with children and NO retro covering them (so the close-owed advisory enters `_breakdown_child_ids` about 190 times, the 189 measured here being that count on this corpus) and 45 are open discovery items, with a close-owed baseline stamped so the advisory passes its unbaselined return and reads `retros/VELOCITY.md` (a 200-entry `sdlc-studio/retros/evidence/actuals-<date>.jsonl` sits beside it as corpus shape - the advisory does not read it), and a RUN OPEN whose batch exercises EVERY branch of handoff's predicate - two delivered units, one terminal unit whose verify report is red, one open unit, one closed without delivery, one id with no file on disk, and a unit the loop quarantined outside the batch, four remaining by hand - when `status.py` runs as a SUBPROCESS over it, then the first stdout line is the `Run:` headline, flushed and printed from the run record BEFORE the census and carrying `remaining=4)` - the count `handoff.build` derives for the same batch, asserted on the CLOSED token so a count that grew a digit cannot pass as a prefix (pinned IN-PROCESS as well as through the pipe: a test patches `gather` to assert the `Run:` line is already in the stdout buffer AND `sys.stdout.flush` has been called when `gather` is entered, and that `handoff.build` was never called - the flush spied, since a StringIO cannot see a missing flush and a cached gather finishes inside any clock bound; through the pipe the `Run:` line precedes every census-derived line and reaches it within 3 s), and the process exits 0 within 15 s - the shipped command measured end to end, never this repository (BG0647 removed exactly that); today's code over this fixture measured 45 s at plan review (engineering seat, 45 gather and 189 advisory walks exact) and the sweep-absence mutant (the ledger's AC1 row 1) re-measured by hand at delivery runs it in 23 s, above the bound, the figure recorded in the revision row, because `sdlc_md.children_of` walks and reads the whole corpus on each of its 45 gather calls and 189 advisory calls |
| AC1 | in `status.py`, open no corpus sweep around the gather and the advisories - today's code - so `children_of` walks and reads the corpus on every call and the fixture command exceeds 15 s (goal review, engineering seat; re-targeted at delivery: with the sweep open, disabling the child index alone runs the fixture in 8 s, inside the bound) | Given a fixture corpus the test generates in THIS corpus's shape - 2,340 artefacts across stories, bugs, epics, CRs and 26 reviews, of which 190 are TERMINAL epics with children and NO retro covering them (so the close-owed advisory enters `_breakdown_child_ids` about 190 times, the 189 measured here being that count on this corpus) and 45 are open discovery items, with a close-owed baseline stamped so the advisory passes its unbaselined return and reads `retros/VELOCITY.md` (a 200-entry `sdlc-studio/retros/evidence/actuals-<date>.jsonl` sits beside it as corpus shape - the advisory does not read it), and a RUN OPEN whose batch exercises EVERY branch of handoff's predicate - two delivered units, one terminal unit whose verify report is red, one open unit, one closed without delivery, one id with no file on disk, and a unit the loop quarantined outside the batch, four remaining by hand - when `status.py` runs as a SUBPROCESS over it, then the first stdout line is the `Run:` headline, flushed and printed from the run record BEFORE the census and carrying `remaining=4)` - the count `handoff.build` derives for the same batch, asserted on the CLOSED token so a count that grew a digit cannot pass as a prefix (pinned IN-PROCESS as well as through the pipe: a test patches `gather` to assert the `Run:` line is already in the stdout buffer AND `sys.stdout.flush` has been called when `gather` is entered, and that `handoff.build` was never called - the flush spied, since a StringIO cannot see a missing flush and a cached gather finishes inside any clock bound; through the pipe the `Run:` line precedes every census-derived line and reaches it within 3 s), and the process exits 0 within 15 s - the shipped command measured end to end, never this repository (BG0647 removed exactly that); today's code over this fixture measured 45 s at plan review (engineering seat, 45 gather and 189 advisory walks exact) and the sweep-absence mutant (the ledger's AC1 row 1) re-measured by hand at delivery runs it in 23 s, above the bound, the figure recorded in the revision row, because `sdlc_md.children_of` walks and reads the whole corpus on each of its 45 gather calls and 189 advisory calls |
| AC1 | in `status.py`, print the advisories before the headline | Given a fixture corpus the test generates in THIS corpus's shape - 2,340 artefacts across stories, bugs, epics, CRs and 26 reviews, of which 190 are TERMINAL epics with children and NO retro covering them (so the close-owed advisory enters `_breakdown_child_ids` about 190 times, the 189 measured here being that count on this corpus) and 45 are open discovery items, with a close-owed baseline stamped so the advisory passes its unbaselined return and reads `retros/VELOCITY.md` (a 200-entry `sdlc-studio/retros/evidence/actuals-<date>.jsonl` sits beside it as corpus shape - the advisory does not read it), and a RUN OPEN whose batch exercises EVERY branch of handoff's predicate - two delivered units, one terminal unit whose verify report is red, one open unit, one closed without delivery, one id with no file on disk, and a unit the loop quarantined outside the batch, four remaining by hand - when `status.py` runs as a SUBPROCESS over it, then the first stdout line is the `Run:` headline, flushed and printed from the run record BEFORE the census and carrying `remaining=4)` - the count `handoff.build` derives for the same batch, asserted on the CLOSED token so a count that grew a digit cannot pass as a prefix (pinned IN-PROCESS as well as through the pipe: a test patches `gather` to assert the `Run:` line is already in the stdout buffer AND `sys.stdout.flush` has been called when `gather` is entered, and that `handoff.build` was never called - the flush spied, since a StringIO cannot see a missing flush and a cached gather finishes inside any clock bound; through the pipe the `Run:` line precedes every census-derived line and reaches it within 3 s), and the process exits 0 within 15 s - the shipped command measured end to end, never this repository (BG0647 removed exactly that); today's code over this fixture measured 45 s at plan review (engineering seat, 45 gather and 189 advisory walks exact) and the sweep-absence mutant (the ledger's AC1 row 1) re-measured by hand at delivery runs it in 23 s, above the bound, the figure recorded in the revision row, because `sdlc_md.children_of` walks and reads the whole corpus on each of its 45 gather calls and 189 advisory calls |
| AC1 | in `status.py`, print the headline without flushing, so the flush spy has not fired when `gather` is entered | Given a fixture corpus the test generates in THIS corpus's shape - 2,340 artefacts across stories, bugs, epics, CRs and 26 reviews, of which 190 are TERMINAL epics with children and NO retro covering them (so the close-owed advisory enters `_breakdown_child_ids` about 190 times, the 189 measured here being that count on this corpus) and 45 are open discovery items, with a close-owed baseline stamped so the advisory passes its unbaselined return and reads `retros/VELOCITY.md` (a 200-entry `sdlc-studio/retros/evidence/actuals-<date>.jsonl` sits beside it as corpus shape - the advisory does not read it), and a RUN OPEN whose batch exercises EVERY branch of handoff's predicate - two delivered units, one terminal unit whose verify report is red, one open unit, one closed without delivery, one id with no file on disk, and a unit the loop quarantined outside the batch, four remaining by hand - when `status.py` runs as a SUBPROCESS over it, then the first stdout line is the `Run:` headline, flushed and printed from the run record BEFORE the census and carrying `remaining=4)` - the count `handoff.build` derives for the same batch, asserted on the CLOSED token so a count that grew a digit cannot pass as a prefix (pinned IN-PROCESS as well as through the pipe: a test patches `gather` to assert the `Run:` line is already in the stdout buffer AND `sys.stdout.flush` has been called when `gather` is entered, and that `handoff.build` was never called - the flush spied, since a StringIO cannot see a missing flush and a cached gather finishes inside any clock bound; through the pipe the `Run:` line precedes every census-derived line and reaches it within 3 s), and the process exits 0 within 15 s - the shipped command measured end to end, never this repository (BG0647 removed exactly that); today's code over this fixture measured 45 s at plan review (engineering seat, 45 gather and 189 advisory walks exact) and the sweep-absence mutant (the ledger's AC1 row 1) re-measured by hand at delivery runs it in 23 s, above the bound, the figure recorded in the revision row, because `sdlc_md.children_of` walks and reads the whole corpus on each of its 45 gather calls and 189 advisory calls |
| AC1 | in `status.py`, keep deriving the run line's remaining count through `handoff.build`, so a run open costs a conformance pass before the headline | Given a fixture corpus the test generates in THIS corpus's shape - 2,340 artefacts across stories, bugs, epics, CRs and 26 reviews, of which 190 are TERMINAL epics with children and NO retro covering them (so the close-owed advisory enters `_breakdown_child_ids` about 190 times, the 189 measured here being that count on this corpus) and 45 are open discovery items, with a close-owed baseline stamped so the advisory passes its unbaselined return and reads `retros/VELOCITY.md` (a 200-entry `sdlc-studio/retros/evidence/actuals-<date>.jsonl` sits beside it as corpus shape - the advisory does not read it), and a RUN OPEN whose batch exercises EVERY branch of handoff's predicate - two delivered units, one terminal unit whose verify report is red, one open unit, one closed without delivery, one id with no file on disk, and a unit the loop quarantined outside the batch, four remaining by hand - when `status.py` runs as a SUBPROCESS over it, then the first stdout line is the `Run:` headline, flushed and printed from the run record BEFORE the census and carrying `remaining=4)` - the count `handoff.build` derives for the same batch, asserted on the CLOSED token so a count that grew a digit cannot pass as a prefix (pinned IN-PROCESS as well as through the pipe: a test patches `gather` to assert the `Run:` line is already in the stdout buffer AND `sys.stdout.flush` has been called when `gather` is entered, and that `handoff.build` was never called - the flush spied, since a StringIO cannot see a missing flush and a cached gather finishes inside any clock bound; through the pipe the `Run:` line precedes every census-derived line and reaches it within 3 s), and the process exits 0 within 15 s - the shipped command measured end to end, never this repository (BG0647 removed exactly that); today's code over this fixture measured 45 s at plan review (engineering seat, 45 gather and 189 advisory walks exact) and the sweep-absence mutant (the ledger's AC1 row 1) re-measured by hand at delivery runs it in 23 s, above the bound, the figure recorded in the revision row, because `sdlc_md.children_of` walks and reads the whole corpus on each of its 45 gather calls and 189 advisory calls |
| AC1 | in `handoff.py`, count no unit the loop quarantined outside the batch in `remaining_count`, so the run line under-reports against `build` (delivery review r1, engineering and qa seats) | Given a fixture corpus the test generates in THIS corpus's shape - 2,340 artefacts across stories, bugs, epics, CRs and 26 reviews, of which 190 are TERMINAL epics with children and NO retro covering them (so the close-owed advisory enters `_breakdown_child_ids` about 190 times, the 189 measured here being that count on this corpus) and 45 are open discovery items, with a close-owed baseline stamped so the advisory passes its unbaselined return and reads `retros/VELOCITY.md` (a 200-entry `sdlc-studio/retros/evidence/actuals-<date>.jsonl` sits beside it as corpus shape - the advisory does not read it), and a RUN OPEN whose batch exercises EVERY branch of handoff's predicate - two delivered units, one terminal unit whose verify report is red, one open unit, one closed without delivery, one id with no file on disk, and a unit the loop quarantined outside the batch, four remaining by hand - when `status.py` runs as a SUBPROCESS over it, then the first stdout line is the `Run:` headline, flushed and printed from the run record BEFORE the census and carrying `remaining=4)` - the count `handoff.build` derives for the same batch, asserted on the CLOSED token so a count that grew a digit cannot pass as a prefix (pinned IN-PROCESS as well as through the pipe: a test patches `gather` to assert the `Run:` line is already in the stdout buffer AND `sys.stdout.flush` has been called when `gather` is entered, and that `handoff.build` was never called - the flush spied, since a StringIO cannot see a missing flush and a cached gather finishes inside any clock bound; through the pipe the `Run:` line precedes every census-derived line and reaches it within 3 s), and the process exits 0 within 15 s - the shipped command measured end to end, never this repository (BG0647 removed exactly that); today's code over this fixture measured 45 s at plan review (engineering seat, 45 gather and 189 advisory walks exact) and the sweep-absence mutant (the ledger's AC1 row 1) re-measured by hand at delivery runs it in 23 s, above the bound, the figure recorded in the revision row, because `sdlc_md.children_of` walks and reads the whole corpus on each of its 45 gather calls and 189 advisory calls |
| AC1 | in `handoff.py`, read a batch id with no file on disk as terminal in `_classify`, so a missing unit is neither remaining nor named (delivery review r1, engineering and qa seats) | Given a fixture corpus the test generates in THIS corpus's shape - 2,340 artefacts across stories, bugs, epics, CRs and 26 reviews, of which 190 are TERMINAL epics with children and NO retro covering them (so the close-owed advisory enters `_breakdown_child_ids` about 190 times, the 189 measured here being that count on this corpus) and 45 are open discovery items, with a close-owed baseline stamped so the advisory passes its unbaselined return and reads `retros/VELOCITY.md` (a 200-entry `sdlc-studio/retros/evidence/actuals-<date>.jsonl` sits beside it as corpus shape - the advisory does not read it), and a RUN OPEN whose batch exercises EVERY branch of handoff's predicate - two delivered units, one terminal unit whose verify report is red, one open unit, one closed without delivery, one id with no file on disk, and a unit the loop quarantined outside the batch, four remaining by hand - when `status.py` runs as a SUBPROCESS over it, then the first stdout line is the `Run:` headline, flushed and printed from the run record BEFORE the census and carrying `remaining=4)` - the count `handoff.build` derives for the same batch, asserted on the CLOSED token so a count that grew a digit cannot pass as a prefix (pinned IN-PROCESS as well as through the pipe: a test patches `gather` to assert the `Run:` line is already in the stdout buffer AND `sys.stdout.flush` has been called when `gather` is entered, and that `handoff.build` was never called - the flush spied, since a StringIO cannot see a missing flush and a cached gather finishes inside any clock bound; through the pipe the `Run:` line precedes every census-derived line and reaches it within 3 s), and the process exits 0 within 15 s - the shipped command measured end to end, never this repository (BG0647 removed exactly that); today's code over this fixture measured 45 s at plan review (engineering seat, 45 gather and 189 advisory walks exact) and the sweep-absence mutant (the ledger's AC1 row 1) re-measured by hand at delivery runs it in 23 s, above the bound, the figure recorded in the revision row, because `sdlc_md.children_of` walks and reads the whole corpus on each of its 45 gather calls and 189 advisory calls |
| AC2 | in `close_owed.py`, walk the corpus again for the advisory instead of reading through the gather's sweep, so the spy counts every file twice | Given the same fixture, when `gather` and then `close_owed_advisory` run in ONE process inside one `corpus_cache()` sweep, then across both each ledger and artefact is read at most once - counted by one spy on `pathlib.Path.read_text` spanning both calls, since half the reads bypass `read_text_safe` - because the advisory reuses the sweep the gather opened: the walk hands each file's text to `read_text_safe` for the rest of that sweep and `children_of` answers from the sweep's child index, a memo that is the SWEEP's - opened and closed by the `with`, never a cache that outlives it, the placement `corpus_cache()`'s docstring refuses - so `children_of` called with no sweep open still walks and reads, the control, asserted by a second no-sweep call re-reading the corpus; and the SHIPPED command enters the advisory inside the same sweep it took the census in, asserted by identity, never after it closed or in a sweep of its own |
| AC2 | in `lib/sdlc_md.py`, memoise `children_of` at module level so the advisory passes with no sweep open - the placement this criterion refuses | Given the same fixture, when `gather` and then `close_owed_advisory` run in ONE process inside one `corpus_cache()` sweep, then across both each ledger and artefact is read at most once - counted by one spy on `pathlib.Path.read_text` spanning both calls, since half the reads bypass `read_text_safe` - because the advisory reuses the sweep the gather opened: the walk hands each file's text to `read_text_safe` for the rest of that sweep and `children_of` answers from the sweep's child index, a memo that is the SWEEP's - opened and closed by the `with`, never a cache that outlives it, the placement `corpus_cache()`'s docstring refuses - so `children_of` called with no sweep open still walks and reads, the control, asserted by a second no-sweep call re-reading the corpus; and the SHIPPED command enters the advisory inside the same sweep it took the census in, asserted by identity, never after it closed or in a sweep of its own |
| AC2 | in `status.py`, run the advisory after the sweep closed so it takes its own walk | Given the same fixture, when `gather` and then `close_owed_advisory` run in ONE process inside one `corpus_cache()` sweep, then across both each ledger and artefact is read at most once - counted by one spy on `pathlib.Path.read_text` spanning both calls, since half the reads bypass `read_text_safe` - because the advisory reuses the sweep the gather opened: the walk hands each file's text to `read_text_safe` for the rest of that sweep and `children_of` answers from the sweep's child index, a memo that is the SWEEP's - opened and closed by the `with`, never a cache that outlives it, the placement `corpus_cache()`'s docstring refuses - so `children_of` called with no sweep open still walks and reads, the control, asserted by a second no-sweep call re-reading the corpus; and the SHIPPED command enters the advisory inside the same sweep it took the census in, asserted by identity, never after it closed or in a sweep of its own |
| AC3 | manual: no test dies on a production change - the row is this corpus's figure, timed by hand at delivery with and without a run open and recorded in the revision row | Given this corpus, when `python3 status.py` runs after delivery, then it completes in under 15 s with no run open and under 20 s with one open - which requires the `Run:` line's remaining count to come from a cheap entry point IN `handoff.py` that reuses handoff's own terminal-versus-dropped predicate (quarantined loop units and missing files counted as handoff counts them, the reason status.py says the dashboard must never own that predicate) rather than through `handoff.build` and its conformance detection (62.7 s on this corpus with a run open, a path the fixture cannot see) - recorded by hand at delivery as the reproduction of the 53 to 115 s figures, not a suite test |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-04 | sdlc-studio | Filed |
| 2026-09-07 | sdlc-studio | Delivered: headline printed and flushed from the run record before the census; one corpus sweep for gather and advisories; run line count via handoff.remaining_count. AC3 timed by hand on this tree at delivery: 0.87 s with RUN-01M1WPNV open, 0.62 s with the run record moved aside (was 53 to 115 s). AC1 verify widened to the class, since the flush pin is in-process; AC1 row 2 re-targeted to dropping the sweep - with the sweep open, disabling the child index alone runs the fixture in 8 s |
| 2026-09-07 | sdlc-studio | Delivery review r1 (engineering REJECT, qa REJECT, product APPROVE): handoff's terminal-versus-dropped predicate is ONE function, _classify, read by build and remaining_count; the fixture's run exercises every branch (delivered, red evidence, open, dropped, missing, quarantined outside the batch) and the run line is asserted on remaining=4) and against build's count; AC1 and AC2 re-worded to what shipped (the sweep-scoped text memo, the ledger row that carries the pre-fix figure, 23 s at delivery); the fixture gains 26 review files and a baseline stamp so the velocity record is read; open_run computed once per invocation; the repair-record warnings build printed on stderr are gone from the dashboard, disclosed in the fragment; AC1's test class is the one Verify line, since the census refuses a second - the earlier row's wording (AC1 row 2) meant the ledger's row 1 |
| 2026-09-07 | sdlc-studio | Delivery review r2: three APPROVEs, no blocking finding. Folded in the residuals: open_run pinned to one call per invocation; AC1 attributes the 23 s pre-fix figure to this row rather than to a ledger field that holds no number, and states the actuals file is corpus shape the advisory does not read; AC2 and the sdlc_md comment say never a cache that outlives the sweep; the write-inside-a-sweep hazard is stated at the memo and filed as a Low in CR0511; the disclosed read_text_safe behaviour change in _classify is pinned by ClassifyUnreadableTests in test_handoff.py (added to Affects) |
