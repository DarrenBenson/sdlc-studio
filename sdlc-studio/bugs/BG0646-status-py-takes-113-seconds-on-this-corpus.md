# BG0646: status.py takes 113 seconds on this corpus, so the command every session is ordered to run first times out under a two-minute tool default

> **Status:** Open
> **Verification depth:** functional [[derived: criteria 4; plan rows 9; EVIDENCE ABSENT - the mutation ledger holds no entry for this unit, which is not the same fact as nought killed; NOT RUN 9 (AC1 row 0, AC1 row 1, AC1 row 2, AC1 row 3, AC2 row 0, AC2 row 1, AC3 row 0, AC3 row 1, AC4 row 0); entry point 0 of 3 criteria through the shipped CLI, 0 in-process; 3 undetermined (the named node could not be isolated) | fp b9fe8458b5a7 ]]
> **Severity:** Medium
> **Points:** 5
> **Affects:** .claude/skills/sdlc-studio/scripts/status.py, .claude/skills/sdlc-studio/scripts/close_owed.py, .claude/skills/sdlc-studio/scripts/lib/sdlc_md.py, .claude/skills/sdlc-studio/scripts/tests/test_status.py, .claude/skills/sdlc-studio/scripts/tests/test_close_owed.py, .claude/skills/sdlc-studio/scripts/tests/test_sdlc_md.py
> **Created:** 2026-09-04
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

AGENTS.md orders every session to run status second, before acting. Measured on 2026-09-04 over 814 stories and 644 bugs: 113 s wall clock (rc 0), and an earlier invocation in the same session was killed by a 120 s tool timeout with the output lost. A read-only dashboard that costs two minutes is one an agent learns to skip, which is the failure mode AGENTS.md names for this repository. The cost is not stated by the command and nothing prints before the census completes.

## Steps to Reproduce

1. time python3 .claude/skills/sdlc-studio/scripts/status.py on this tree. 2. Observe about 113 s with no output until the end. 3. Run it under a 120 s tool timeout and observe the kill.

## Proposed Fix

Profile the census: the likely cost is per-artefact parsing repeated across the requirements, bugs, reviews and backlog passes plus the already-delivered advisory's pairwise title comparison over 256 open artefacts. Parse each artefact once and share the census across passes, cache the parse keyed on file mtime under sdlc-studio/.local, print the headline lines before the advisories, and record the measured duration in gate-timings so a regression is visible. Target: under 15 s on this corpus.

## Acceptance Criteria

- [ ] **AC1** Given a fixture corpus the test generates (2,000 artefacts across stories, bugs, epics and reviews, of which 40 are TERMINAL epics with children and NO retro covering them, so the close-owed advisory enters `_breakdown_child_ids`, the branch that costs 42 of today's 53 seconds, plus a close-owed history of 200 entries), when `status.py` runs as a SUBPROCESS over it, then the headline lines reach stdout within 3 s of start and the process exits 0 within 15 s - the shipped command measured end to end, on a fixture the machine can reproduce, never this repository (BG0647 removed exactly that). The headline (run of record, pipeline state, next step) is printed from the run record and the review anchor BEFORE the census, flushed, so it does not wait on the gather; the gather itself and the close-owed advisory stop re-walking the corpus per call (`sdlc_md.children_of` today walks and reads all 2,340 files on each of its 45 gather calls and 189 advisory calls) by reading from one census-backed map
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_status.py::GatherPerformanceTests::test_the_shipped_command_answers_a_fixture_corpus_with_the_headline_first
- [ ] **AC2** Given the same fixture, when `gather` and then `close_owed_advisory` run in ONE process, then across both each ledger and artefact is read at most once - counted by one spy on `pathlib.Path.read_text` spanning both calls, since half the reads bypass `read_text_safe` - and the advisory is bounded by the census the gather already took rather than a second walk: on HEAD the whole command is 53 s of which this advisory is 41.9 s and the gather 9.9 s
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_close_owed.py::AdvisoryCostTests::test_the_advisory_reads_each_file_at_most_once_and_reuses_the_census
- [ ] **AC3** Given the same fixture, when `gather` runs twice in one process with no file changed, then the second call reads no artefact file at all and returns the same census - the cache is keyed on file mtime and size, and one changed file re-reads only itself
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_status.py::GatherPerformanceTests::test_a_second_gather_reads_nothing_and_a_changed_file_re_reads_only_itself
- [ ] **AC4** Given this corpus, when `python3 status.py` runs after delivery, then it completes in under 15 s with no run open and under 20 s with one open - recorded by hand at delivery as the reproduction of the 53-112 s figures, not a suite test
  - **Verify:** manual - time `python3 .claude/skills/sdlc-studio/scripts/status.py` on this tree at delivery, with and without a run open, and record both figures in the revision row

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `status.py`, print the advisories before the headline | Given a fixture corpus the test generates (2,000 artefacts across stories, bugs, epics and reviews, of which 40 are TERMINAL epics with children and NO retro covering them, so the close-owed advisory enters `_breakdown_child_ids`, the branch that costs 42 of today's 53 seconds, plus a close-owed history of 200 entries), when `status.py` runs as a SUBPROCESS over it, then the headline lines reach stdout within 3 s of start and the process exits 0 within 15 s - the shipped command measured end to end, on a fixture the machine can reproduce, never this repository (BG0647 removed exactly that). The headline (run of record, pipeline state, next step) is printed from the run record and the review anchor BEFORE the census, flushed, so it does not wait on the gather; the gather itself and the close-owed advisory stop re-walking the corpus per call (`sdlc_md.children_of` today walks and reads all 2,340 files on each of its 45 gather calls and 189 advisory calls) by reading from one census-backed map |
| AC1 | in `close_owed.py`, walk the corpus a second time for the advisory - today's code - so the fixture command exceeds 15 s | Given a fixture corpus the test generates (2,000 artefacts across stories, bugs, epics and reviews, of which 40 are TERMINAL epics with children and NO retro covering them, so the close-owed advisory enters `_breakdown_child_ids`, the branch that costs 42 of today's 53 seconds, plus a close-owed history of 200 entries), when `status.py` runs as a SUBPROCESS over it, then the headline lines reach stdout within 3 s of start and the process exits 0 within 15 s - the shipped command measured end to end, on a fixture the machine can reproduce, never this repository (BG0647 removed exactly that). The headline (run of record, pipeline state, next step) is printed from the run record and the review anchor BEFORE the census, flushed, so it does not wait on the gather; the gather itself and the close-owed advisory stop re-walking the corpus per call (`sdlc_md.children_of` today walks and reads all 2,340 files on each of its 45 gather calls and 189 advisory calls) by reading from one census-backed map |
| AC1 | in `status.py`, keep printing the headline only after the full gather returns, so the 3 s first line is missed on the fixture (goal review, product seat) | Given a fixture corpus the test generates (2,000 artefacts across stories, bugs, epics and reviews, of which 40 are TERMINAL epics with children and NO retro covering them, so the close-owed advisory enters `_breakdown_child_ids`, the branch that costs 42 of today's 53 seconds, plus a close-owed history of 200 entries), when `status.py` runs as a SUBPROCESS over it, then the headline lines reach stdout within 3 s of start and the process exits 0 within 15 s - the shipped command measured end to end, on a fixture the machine can reproduce, never this repository (BG0647 removed exactly that). The headline (run of record, pipeline state, next step) is printed from the run record and the review anchor BEFORE the census, flushed, so it does not wait on the gather; the gather itself and the close-owed advisory stop re-walking the corpus per call (`sdlc_md.children_of` today walks and reads all 2,340 files on each of its 45 gather calls and 189 advisory calls) by reading from one census-backed map |
| AC1 | in `lib/sdlc_md.py`, keep `children_of` walking the corpus on every call - today's code - so the fixture command exceeds 15 s (goal review, engineering seat) | Given a fixture corpus the test generates (2,000 artefacts across stories, bugs, epics and reviews, of which 40 are TERMINAL epics with children and NO retro covering them, so the close-owed advisory enters `_breakdown_child_ids`, the branch that costs 42 of today's 53 seconds, plus a close-owed history of 200 entries), when `status.py` runs as a SUBPROCESS over it, then the headline lines reach stdout within 3 s of start and the process exits 0 within 15 s - the shipped command measured end to end, on a fixture the machine can reproduce, never this repository (BG0647 removed exactly that). The headline (run of record, pipeline state, next step) is printed from the run record and the review anchor BEFORE the census, flushed, so it does not wait on the gather; the gather itself and the close-owed advisory stop re-walking the corpus per call (`sdlc_md.children_of` today walks and reads all 2,340 files on each of its 45 gather calls and 189 advisory calls) by reading from one census-backed map |
| AC2 | in `close_owed.py`, read each artefact twice, once for the ledger join and once for the status, so the spy counts two per file | Given the same fixture, when `gather` and then `close_owed_advisory` run in ONE process, then across both each ledger and artefact is read at most once - counted by one spy on `pathlib.Path.read_text` spanning both calls, since half the reads bypass `read_text_safe` - and the advisory is bounded by the census the gather already took rather than a second walk: on HEAD the whole command is 53 s of which this advisory is 41.9 s and the gather 9.9 s |
| AC2 | in `status.py`, hand the advisory no census so it takes its own walk | Given the same fixture, when `gather` and then `close_owed_advisory` run in ONE process, then across both each ledger and artefact is read at most once - counted by one spy on `pathlib.Path.read_text` spanning both calls, since half the reads bypass `read_text_safe` - and the advisory is bounded by the census the gather already took rather than a second walk: on HEAD the whole command is 53 s of which this advisory is 41.9 s and the gather 9.9 s |
| AC3 | in `status.py`, key the cache on the path alone so a changed file is served stale | Given the same fixture, when `gather` runs twice in one process with no file changed, then the second call reads no artefact file at all and returns the same census - the cache is keyed on file mtime and size, and one changed file re-reads only itself |
| AC3 | in `status.py`, drop the cache so the second gather re-reads every file | Given the same fixture, when `gather` runs twice in one process with no file changed, then the second call reads no artefact file at all and returns the same census - the cache is keyed on file mtime and size, and one changed file re-reads only itself |
| AC4 | manual: no test dies on a production change - the row is this corpus's figure, timed by hand at delivery with and without a run open and recorded in the revision row | Given this corpus, when `python3 status.py` runs after delivery, then it completes in under 15 s with no run open and under 20 s with one open - recorded by hand at delivery as the reproduction of the 53-112 s figures, not a suite test |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-04 | sdlc-studio | Filed |
