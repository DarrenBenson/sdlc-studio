# BG0642: the required status check never reports at push time, so every push to main bypasses branch protection and CI failures go unread

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 5; plan rows 22; executed 22; killed 21; survived 0; ruled equivalent 1; not-run 0; entry point 1 of 4 criteria through the shipped CLI, 3 in-process | fp 68fc82bde842 ]]
> **Severity:** Medium
> **Points:** 3
> **Affects:** .githooks/pre-push, tools/tests/test_pre_push_hook.py, AGENTS.md, sdlc-studio/decisions.md
> **Depends on:** BG0641
> **Created:** 2026-09-03
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

Branch protection on main requires a status check named `ci`. Every push reports `Bypassed rule violations for refs/heads/main: Required status check "ci" is expected`, because the check has not run at the moment the push is evaluated - a direct push to main is always evaluated before its own CI. The bypass is therefore routine rather than exceptional, and nothing local compensates: there is no pre-push hook either. The consequence is not theoretical: the run for c458afec FAILED on 2026-09-01 and main stayed red for two days with nobody reading it, because the push had already been reported as successful.

## Steps to Reproduce

1. `git push origin main` on a clean fast-forward.
2. The remote prints `Bypassed rule violations ... Required status check "ci" is expected` and accepts the push.
3. `gh run list` shows the workflow starting AFTER the push.
4. `gh run view <id>` for the previous push shows conclusion `failure`, unread.

## Proposed Fix

Decide which the working practice is, then make one thing true. If direct pushes to main are intended - and trunk-based development is this project's recorded practice - then a required status check that can never be satisfied by that flow is the wrong control, and the honest fix is a post-push signal somebody reads: a failing run on main should reach the operator rather than sit in a tab nobody opens. If pushes are meant to wait for CI, the flow needs a PR or a pre-push gate. What must not persist is a rule that is bypassed every time and a red main that nobody notices.

Groomed 2026-09-04 under the recorded practice: trunk-based, direct pushes to main. Measured at HEAD, three of the last four pushes failed CI and the protection carries `enforce_admins: false`, so the required `ci` context holds nobody. The signal somebody reads is the one in the command people run (LL0027): the pre-push hook BG0641 adds reads the latest completed run on main through `gh` and refuses the next push over a red one until the run id is acknowledged. The never-satisfiable required check is removed under a decision. The alternative - a PR flow that waits for CI - is the operator's call and is put to sign-off with this plan.

Plan review round 1 (2026-09-04): "the most recent completed run on main" was underdetermined and it mattered - on 2026-09-01 two green Dependabot runs sat between two red Lint runs, and the scheduled corpus lint (kept OFF the commit path deliberately) would have refused every push after its weekly failure. The hook reads the push-triggered `Lint` workflow only. Acknowledgements are remembered per run id because CI here takes up to an hour and a fix push does not make the red run stop being the latest completed one. The CI read precedes BG0641's gate so a red main costs a second, not the gate's minutes. Points 2 to 3.

Plan review round 2 (2026-09-04): the PATH as written could not exec the hook (bash and python3 were excluded), so the stub directory's contents are named. The remembered acknowledgement moved the stale-id hole into the file, so AC3 exercises the file with the variable unset and carries the blanket-file mutant. The stub's JSON fields are named so the refusal's run id, conclusion and URL come from the same answer the filters were asserted on.

## Acceptance Criteria

- [x] **AC1** Given a stub `gh` in a directory holding ONLY that stub plus symlinks to the real `git`, `bash` and `python3` (and nothing else, so the live forge is unreachable and the hook can still exec), placed as the whole PATH, answering `gh run list --workflow Lint --branch main --event push --status completed --limit 1 --json databaseId,conclusion,url` with a `failure` run while a NEWER green run of another workflow (Dependabot) would be returned without the workflow filter, when `git push` runs from a fixture clone, then the hook refuses BEFORE invoking the gate (the gate stub records no call), names the run id, conclusion and URL from those fields, and prints the acknowledgement command verbatim: `SDLC_PUSH_ACK_RED=<run id> git push`. The hook is written against bash builtins, git, python3 and gh ONLY (`.local/` is created through python3, and there is no `mkdir`, `cat`, `date`, `dirname`, `sed` or `jq` anywhere in it), because the PATH holds nothing else. The stub records its argv and the test asserts all four filters. The read is of `main` whatever ref is pushed - trunk-based practice makes main's state the state that matters
  - **Verify:** pytest tools/tests/test_pre_push_hook.py::ARedMainIsReadBeforeTheNextPushTests::test_a_failed_run_on_main_refuses_the_push_and_names_it
  - **Verified:** yes (2026-09-04)
- [x] **AC2** Given the latest such run concluded anything other than `success` - `failure`, `cancelled`, `timed_out`, `startup_failure` - when the hook runs, then it refuses; the rule is not-success, never an enumerated failure word, and the row is exercised with `timed_out`
  - **Verify:** pytest tools/tests/test_pre_push_hook.py::ARedMainIsReadBeforeTheNextPushTests::test_a_non_success_conclusion_refuses_not_only_failure
  - **Verified:** yes (2026-09-04)
- [x] **AC3** Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify`
  - **Verify:** pytest tools/tests/test_pre_push_hook.py::ARedMainIsReadBeforeTheNextPushTests::test_an_acknowledged_red_is_remembered_a_stale_ack_refuses_and_a_green_run_proceeds
  - **Verified:** yes (2026-09-04)
- [x] **AC4** Given the same PATH with the stub exiting non-zero (absent, unauthenticated or unreachable), or answering an EMPTY list (no completed Lint run on main to read), when the hook runs, then it prints exactly `CI state UNREAD: could not read the latest Lint run on main` for the first and `no completed Lint run on main to read` for the second, and the push COMPLETES (the gate stub exits 0 in these two steps and the test asserts the remote advanced) - an unreadable or empty state is never rendered as green and never blocks an offline push. This test also runs the red case and asserts neither sentence is a substring of that refusal
  - **Verify:** pytest tools/tests/test_pre_push_hook.py::ARedMainIsReadBeforeTheNextPushTests::test_an_unreadable_ci_state_is_named_unread_not_green
  - **Verified:** yes (2026-09-04)
- [ ] **AC5** Given the ruling is the OPERATOR'S and is taken AT PLAN SIGN-OFF, BEFORE CODE (a PR-flow ruling makes AC1-AC4 the wrong work): they choose between direct pushes to main (this plan's assumption - the `ci` required context is removed, since `enforce_admins: false` means it holds nobody, and AGENTS.md states the practice: direct pushes, gated at pre-push, a red main acknowledged by run id) and a PR flow that waits for the `ci` job (which WOULD satisfy the check). When the decision is recorded, then it names the alternative and why it was declined, the branch-protection edit happens AFTER the ruling and never as part of delivery, and whoever read the protection back is named; the delivery review reads AGENTS.md's acknowledgement paragraph back
  - **Verify:** manual - after the ruling, `gh api repos/DarrenBenson/sdlc-studio/branches/main/protection --jq .required_status_checks` prints `null` (direct-push branch) or the PR flow is documented instead, and `decisions.py list` shows the ruling with the alternative named

## Impact

Two days of red main is the measured cost, and the failure was real - the conformance lane naming seven non-conformant units. A control that is bypassed on every use trains everyone to read the bypass line as noise, so the one time it matters it reads the same as the times it did not.

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `.githooks/pre-push`, drop the comparison on the run's `conclusion` field so every completed run reads as passed | Given a stub `gh` in a directory holding ONLY that stub plus symlinks to the real `git`, `bash` and `python3` (and nothing else, so the live forge is unreachable and the hook can still exec), placed as the whole PATH, answering `gh run list --workflow Lint --branch main --event push --status completed --limit 1 --json databaseId,conclusion,url` with a `failure` run while a NEWER green run of another workflow (Dependabot) would be returned without the workflow filter, when `git push` runs from a fixture clone, then the hook refuses BEFORE invoking the gate (the gate stub records no call), names the run id, conclusion and URL from those fields, and prints the acknowledgement command verbatim: `SDLC_PUSH_ACK_RED=<run id> git push`. The hook is written against bash builtins, git, python3 and gh ONLY (`.local/` is created through python3, and there is no `mkdir`, `cat`, `date`, `dirname`, `sed` or `jq` anywhere in it), because the PATH holds nothing else. The stub records its argv and the test asserts all four filters. The read is of `main` whatever ref is pushed - trunk-based practice makes main's state the state that matters |
| AC1 | in `.githooks/pre-push`, drop the `--workflow Lint` filter so the newer green Dependabot run is the one read | Given a stub `gh` in a directory holding ONLY that stub plus symlinks to the real `git`, `bash` and `python3` (and nothing else, so the live forge is unreachable and the hook can still exec), placed as the whole PATH, answering `gh run list --workflow Lint --branch main --event push --status completed --limit 1 --json databaseId,conclusion,url` with a `failure` run while a NEWER green run of another workflow (Dependabot) would be returned without the workflow filter, when `git push` runs from a fixture clone, then the hook refuses BEFORE invoking the gate (the gate stub records no call), names the run id, conclusion and URL from those fields, and prints the acknowledgement command verbatim: `SDLC_PUSH_ACK_RED=<run id> git push`. The hook is written against bash builtins, git, python3 and gh ONLY (`.local/` is created through python3, and there is no `mkdir`, `cat`, `date`, `dirname`, `sed` or `jq` anywhere in it), because the PATH holds nothing else. The stub records its argv and the test asserts all four filters. The read is of `main` whatever ref is pushed - trunk-based practice makes main's state the state that matters |
| AC1 | in `.githooks/pre-push`, invoke the gate before reading CI | Given a stub `gh` in a directory holding ONLY that stub plus symlinks to the real `git`, `bash` and `python3` (and nothing else, so the live forge is unreachable and the hook can still exec), placed as the whole PATH, answering `gh run list --workflow Lint --branch main --event push --status completed --limit 1 --json databaseId,conclusion,url` with a `failure` run while a NEWER green run of another workflow (Dependabot) would be returned without the workflow filter, when `git push` runs from a fixture clone, then the hook refuses BEFORE invoking the gate (the gate stub records no call), names the run id, conclusion and URL from those fields, and prints the acknowledgement command verbatim: `SDLC_PUSH_ACK_RED=<run id> git push`. The hook is written against bash builtins, git, python3 and gh ONLY (`.local/` is created through python3, and there is no `mkdir`, `cat`, `date`, `dirname`, `sed` or `jq` anywhere in it), because the PATH holds nothing else. The stub records its argv and the test asserts all four filters. The read is of `main` whatever ref is pushed - trunk-based practice makes main's state the state that matters |
| AC1 | in `.githooks/pre-push`, read `--branch "$(git rev-parse --abbrev-ref HEAD)"` instead of main, so a tag push reads the wrong branch (delivery review, qa seat) | Given a stub `gh` in a directory holding ONLY that stub plus symlinks to the real `git`, `bash` and `python3` (and nothing else, so the live forge is unreachable and the hook can still exec), placed as the whole PATH, answering `gh run list --workflow Lint --branch main --event push --status completed --limit 1 --json databaseId,conclusion,url` with a `failure` run while a NEWER green run of another workflow (Dependabot) would be returned without the workflow filter, when `git push` runs from a fixture clone, then the hook refuses BEFORE invoking the gate (the gate stub records no call), names the run id, conclusion and URL from those fields, and prints the acknowledgement command verbatim: `SDLC_PUSH_ACK_RED=<run id> git push`. The hook is written against bash builtins, git, python3 and gh ONLY (`.local/` is created through python3, and there is no `mkdir`, `cat`, `date`, `dirname`, `sed` or `jq` anywhere in it), because the PATH holds nothing else. The stub records its argv and the test asserts all four filters. The read is of `main` whatever ref is pushed - trunk-based practice makes main's state the state that matters |
| AC2 | in `.githooks/pre-push`, compare `conclusion == "failure"` so `timed_out` reads as green | Given the latest such run concluded anything other than `success` - `failure`, `cancelled`, `timed_out`, `startup_failure` - when the hook runs, then it refuses; the rule is not-success, never an enumerated failure word, and the row is exercised with `timed_out` |
| AC2 | in `.githooks/pre-push`, refuse on an enumerated `failure \|\| timed_out` list, so `cancelled` reads as green (delivery review, engineering seat) | Given the latest such run concluded anything other than `success` - `failure`, `cancelled`, `timed_out`, `startup_failure` - when the hook runs, then it refuses; the rule is not-success, never an enumerated failure word, and the row is exercised with `timed_out` |
| AC3 | in `.githooks/pre-push`, test `[ -n "$SDLC_PUSH_ACK_RED" ]` instead of comparing it to the run id | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, proceed whenever `push-ack.json` exists, without comparing its id - the stale-file half survives | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, honour the variable and record nothing - the later push over the same run is refused | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, invert the conclusion test so `success` refuses | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, append a `git push --no-verify` bypass line to the red-CI refusal (delivery review, qa seat) | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, move the CI read below the cost announcement, whose line names the bypass (delivery review, qa seat) | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, drop the `os.makedirs` so a clone with no `.local/` cannot record its acknowledgement (delivery review, qa seat) | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, honour `SDLC_PUSH_ACK_RED=1` as well as the run id (delivery review, qa seat) | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, treat a run with no `databaseId` as already acknowledged, an empty id matching an empty record (delivery review, engineering seat) | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC3 | in `.githooks/pre-push`, print `remembered` whatever the record write returned (delivery review, engineering seat) | Given the red run's id acknowledged through `SDLC_PUSH_ACK_RED=<run id>`, when the hook runs, then the push proceeds and the id is recorded in `sdlc-studio/.local/push-ack.json` (the hook creates `.local/` if absent; the file is gitignored, so an acknowledgement is per clone), and a LATER push over the SAME red run - variable UNSET, file holding that id - proceeds without re-typing it (CI here takes 23 to 69 minutes); a stale acknowledgement - variable unset, file holding ANOTHER run's id - refuses; a WRONG acknowledgement - the variable set to another run's id with no `push-ack.json` present - refuses and records nothing, so a profile-exported `SDLC_PUSH_ACK_RED=1` cannot bypass every red main; and a latest run that concluded `success` proceeds. The acknowledgement is the only route the hook offers; this refusal never names `git push --no-verify` |
| AC4 | in `.githooks/pre-push`, treat a failed `gh` invocation as an empty run list and print nothing - the push proceeds but the sentence is absent | Given the same PATH with the stub exiting non-zero (absent, unauthenticated or unreachable), or answering an EMPTY list (no completed Lint run on main to read), when the hook runs, then it prints exactly `CI state UNREAD: could not read the latest Lint run on main` for the first and `no completed Lint run on main to read` for the second, and the push COMPLETES (the gate stub exits 0 in these two steps and the test asserts the remote advanced) - an unreadable or empty state is never rendered as green and never blocks an offline push. This test also runs the red case and asserts neither sentence is a substring of that refusal |
| AC4 | in `.githooks/pre-push`, refuse when `gh` fails - the offline push the test makes is blocked | Given the same PATH with the stub exiting non-zero (absent, unauthenticated or unreachable), or answering an EMPTY list (no completed Lint run on main to read), when the hook runs, then it prints exactly `CI state UNREAD: could not read the latest Lint run on main` for the first and `no completed Lint run on main to read` for the second, and the push COMPLETES (the gate stub exits 0 in these two steps and the test asserts the remote advanced) - an unreadable or empty state is never rendered as green and never blocks an offline push. This test also runs the red case and asserts neither sentence is a substring of that refusal |
| AC4 | in `.githooks/pre-push`, proceed silently on an empty list | Given the same PATH with the stub exiting non-zero (absent, unauthenticated or unreachable), or answering an EMPTY list (no completed Lint run on main to read), when the hook runs, then it prints exactly `CI state UNREAD: could not read the latest Lint run on main` for the first and `no completed Lint run on main to read` for the second, and the push COMPLETES (the gate stub exits 0 in these two steps and the test asserts the remote advanced) - an unreadable or empty state is never rendered as green and never blocks an offline push. This test also runs the red case and asserts neither sentence is a substring of that refusal |
| AC4 | in `.githooks/pre-push`, read a non-JSON answer at rc 0 as an empty list instead of UNREAD, in the parser's own fallback (delivery review, engineering seat) | Given the same PATH with the stub exiting non-zero (absent, unauthenticated or unreachable), or answering an EMPTY list (no completed Lint run on main to read), when the hook runs, then it prints exactly `CI state UNREAD: could not read the latest Lint run on main` for the first and `no completed Lint run on main to read` for the second, and the push COMPLETES (the gate stub exits 0 in these two steps and the test asserts the remote advanced) - an unreadable or empty state is never rendered as green and never blocks an offline push. This test also runs the red case and asserts neither sentence is a substring of that refusal |
| AC4 | in `.githooks/pre-push`, drop the list-shape guard and the shell fallback together, so a JSON number at rc 0 is waved through (delivery review r2, engineering seat) | Given the same PATH with the stub exiting non-zero (absent, unauthenticated or unreachable), or answering an EMPTY list (no completed Lint run on main to read), when the hook runs, then it prints exactly `CI state UNREAD: could not read the latest Lint run on main` for the first and `no completed Lint run on main to read` for the second, and the push COMPLETES (the gate stub exits 0 in these two steps and the test asserts the remote advanced) - an unreadable or empty state is never rendered as green and never blocks an offline push. This test also runs the red case and asserts neither sentence is a substring of that refusal |
| AC5 | manual: no test dies on a production change - the row is the operator's ruling and the forge edit, judged by reading both back | Given the ruling is the OPERATOR'S and is taken AT PLAN SIGN-OFF, BEFORE CODE (a PR-flow ruling makes AC1-AC4 the wrong work): they choose between direct pushes to main (this plan's assumption - the `ci` required context is removed, since `enforce_admins: false` means it holds nobody, and AGENTS.md states the practice: direct pushes, gated at pre-push, a red main acknowledged by run id) and a PR flow that waits for the `ci` job (which WOULD satisfy the check). When the decision is recorded, then it names the alternative and why it was declined, the branch-protection edit happens AFTER the ruling and never as part of delivery, and whoever read the protection back is named; the delivery review reads AGENTS.md's acknowledgement paragraph back |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-03 | sdlc-studio | Filed |
| 2026-09-04 | sdlc-studio | Groomed for the next sprint: executable criteria with a paired control each, Affects narrowed to where the fix lands, premise re-measured at HEAD |
| 2026-09-04 | sdlc-studio | Plan review round 1 (three seats): criteria and test plan re-authored to the findings; see the Proposed Fix note dated 2026-09-04 |
| 2026-09-04 | sdlc-studio | Plan review round 2 (three seats): fixtures that could not reach their branch repaired; see the Proposed Fix note |
| 2026-09-04 | sdlc-studio | Plan review round 3 (three seats): delivery notes folded into the criteria; BG0642 AC3 gains the wrong-acknowledgement step |
| 2026-09-04 | sdlc | Delivery review, two rounds (2026-09-04): r1 product APPROVE, engineering and qa REJECT (the direct-stdin helper ran under the caller's PATH so one fixture reached the real gh; AGENTS.md stated the forge edit as done; the red-CI refusal's 'never names the bypass' clause had no assertion); r2 both APPROVE. Eleven plan rows added from the seats' probes (no-id runs, unwritable records, cancelled, non-JSON and numeric answers, a tag pushed from a topic checkout, the profile-exported 1), 21 executed and killed, AC5 ruled equivalent (manual). The forge edit removing the ci check stays the operator's, owed until the read-back shows none; the unbounded gh read is filed into CR0511 |
