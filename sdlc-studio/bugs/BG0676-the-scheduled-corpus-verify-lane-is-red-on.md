# BG0676: the scheduled corpus-verify lane is red on main - 40 red criteria against a baseline of 20 - and every one of the 21 new ones passes locally

> **Status:** In Progress
> **Severity:** Medium
> **Points:** 8
> **Depends on:** BG0659, BG0661, BG0662, BG0664, BG0665, BG0666, BG0667, BG0668, BG0669, BG0670, BG0671, BG0672, BG0673, BG0674, BG0675, US0625, US0626, US0627, US0628, US0823
> **Affects:** .github/workflows/lint.yml, tools/verify-corpus.sh, tools/verify-corpus-baseline.txt, .claude/skills/sdlc-studio/scripts/gate.py, .claude/skills/sdlc-studio/scripts/tests/test_gate.py, tools/tests/test_lint_workflow_coverage.py, sdlc-studio/bugs/BG0676-the-scheduled-corpus-verify-lane-is-red-on.md
> **Created:** 2026-09-15
> **Created-by:** sdlc-studio file
> **Raised-by:** sprint planning 2026-09-15; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

Lint run 34830172971 (schedule, 2026-09-14, main at 28682ce2) failed `corpus-verify`: 'red-criteria: 40, baseline 20 - the SET moved', naming 21 NEW red criteria: US0031 AC3, US0220 AC2, US0284 AC1, US0597 AC1-AC3, US0663 AC2, US0674 AC1, US0815 AC1-AC6, US0816 AC1-AC4 and AC7-AC9 (and US0666 AC2 went green). Run locally at 51f264db with raw pytest, every one of the 21 passes: the seven non-coverage selectors individually, and all 82 tests behind US0815, US0816 and US0674 together (coverage 7.15 installed). So the lane is reporting an environment difference, not a regression (LL0011), and a scheduled red nobody reads is the BG0653 shape. Three candidate causes, unproven: (1) the corpus job installs only `pyyaml pytest`, not `coverage` - the package US0815/US0816's criteria need; (2) its checkout is shallow (actions/checkout default depth 1), which history-replaying criteria (US0597's claim-drift replays) may need; (3) the red pass runs `gate.py --release`, whose verifiers take the 120 s default timeout, and GateRealWrapperTests (US0031, US0284) take ~136 s locally. The push hook reads push-triggered runs only, so nothing blocks on it - which is how it stays unread.

## Steps to Reproduce

1. `gh run view 34830172971 --json jobs` - corpus-verify failed at 'Corpus verification (dead stamps + red criteria, against the baseline)'.
2. Its log: 'red-criteria: 40, baseline 20 - the SET moved' and the NEW list.
3. Locally, run each NEW criterion's Verify selector with raw pytest - all pass.
4. Compare the corpus-verify job in .github/workflows/lint.yml with the skill-tests job: no coverage install, default shallow checkout.

## Proposed Fix

Settle the cause by experiment, not by reading: dispatch the lane with each candidate corrected in turn (coverage>=7.10 installed as the skill-tests job does; fetch-depth 0; a verifier timeout above the slowest criterion), and keep only what moves the count. Then pin the job's environment in a test the way `test_lint_workflow_coverage` pins the skill-tests job, so the two jobs cannot drift apart again.

## Acceptance Criteria

- [ ] **AC1** Inside the corpus-verify job's own block of `.github/workflows/lint.yml`, a live `pip install` command - never a comment, never another job's step - installs coverage with the `>=7.10` floor, pinned by a test that reads that job's commands only
  - **Verify:** pytest tools/tests/test_lint_workflow_coverage.py::CorpusJobEnvironmentTests::test_the_corpus_job_installs_coverage
- [ ] **AC2** The release verify lane reads a per-verifier timeout override from `SDLC_VERIFY_TIMEOUT` when it RUNS and hands it to every verifier it executes, observed through the shipped entry point: `gate.py --root <fixture> --release --only verify,review-legs,versions,changelog-fragments` over a one-story fixture at Done whose only criterion is `shell sleep 3 && test -d sdlc-studio` prints a verify lane line naming `US9300::AC1` as failing with `SDLC_VERIFY_TIMEOUT=1`, and a `[PASS] verify` line with `SDLC_VERIFY_TIMEOUT=10`; the paired control, the variable unset, passes on the 120 s default. Measured at HEAD: that fixture's verify lane passes in 3.7 s today, so the lane line is readable though the gate's other bound lanes fail on a bare fixture
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_gate.py::VerifyTimeoutOverrideTests::test_the_release_lane_hands_the_override_to_a_verifier
- [ ] **AC3** The corpus-verify job sets `SDLC_VERIFY_TIMEOUT` - the exact name AC2's lane reads, which the test reads from gate.py's source rather than restating - on that job or its verification step, to an integer strictly above 136, the slowest criterion measured (GateRealWrapperTests, about 136 s locally, against today's 120 s default)
  - **Verify:** pytest tools/tests/test_lint_workflow_coverage.py::CorpusJobEnvironmentTests::test_the_corpus_job_gives_verifiers_time
- [ ] **AC4** The red-criteria baseline is re-measured from a CI corpus-verify run taken with AC3's override in force, never a local one. `tools/verify-corpus-baseline.txt` carries a comment line `# re-measured from CI run <databaseId>` naming that run; its red-criteria row's count equals the ids in its field 4; and every id in field 4 that is not among the 20 the row holds at 3f73ab64 (held in the test as a literal, so 'added' is judged against a fixed prior set) is named on a comment line of its own - never in field 4, which the lane splits on whitespace - beside either a bug id whose file exists under `sdlc-studio/bugs/` or `cause:` followed by a non-empty proven environment cause (coverage, history depth, ripgrep, time or another), so the re-measure cannot bank whatever CI happens to read. A comment line that merely mentions an id in prose does not justify it: lines 51, 95 and 102 of the baseline at 3f73ab64 already name US0031::AC3 and US0220::AC2 with neither a bug id nor `cause:`, and neither may pass on those lines alone. Ids that went green, US0666 AC2 among them, leave the row, which AC5's run judges: the lane fails on a move in either direction
  - **Verify:** pytest tools/tests/test_lint_workflow_coverage.py::CorpusJobEnvironmentTests::test_the_baseline_names_its_ci_run
- [ ] **AC5** A completed workflow_dispatch Lint run on the sha BG0676 records in its own header as `> **Verified-on:** <40-hex sha>` concluded success in its corpus-verify JOB (the workflow's other jobs also run on a dispatch and must not stand in for it), and that sha is on HEAD's history. Step one of the work is an uncorrected workflow_dispatch of Lint on main (none exists yet), so a red dispatch run is on record. The delivery writes the field only after pushing BG0676's closing commit, confirming main's HEAD is that commit, and dispatching Lint on main - `gh workflow run` dispatches a ref, not a sha - so the recorded sha is the one CI read and not whatever HEAD the close paperwork later moves to. BG0676 depends on every other batch unit, so it is delivered last and that sha carries the whole batch; `--status completed` keeps an in-progress rerun from reading as a failure
  - **Verify:** shell sha=$(grep -m1 -oE '^> .{2}Verified-on:.{2} [0-9a-f]{40}$' sdlc-studio/bugs/BG0676-*.md | grep -oE '[0-9a-f]{40}$') && git merge-base --is-ancestor "$sha" HEAD && id=$(gh run list --workflow Lint --event workflow_dispatch --commit "$sha" --status completed --limit 1 --json databaseId --jq '.[0].databaseId') && [ -n "$id" ] && gh run view "$id" --json jobs --jq '.jobs[] | select(.name=="corpus-verify") | .conclusion' | grep -qx success

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .github/workflows/lint.yml, drop `'coverage>=7.10'` from the corpus-verify job's install step while the ci job's install line keeps it, so a whole-file search still finds the floor | Inside the corpus-verify job's own block of `.github/workflows/lint.yml`, a live `pip install` command - never a comment, never another job's step - installs coverage with the `>=7.10` floor, pinned by a test that reads that job's commands only |
| AC1 | in .github/workflows/lint.yml, insert plain `coverage`, unversioned, after `pyyaml pytest` | Inside the corpus-verify job's own block of `.github/workflows/lint.yml`, a live `pip install` command - never a comment, never another job's step - installs coverage with the `>=7.10` floor, pinned by a test that reads that job's commands only |
| AC1 | in .github/workflows/lint.yml, insert the line `# pip install 'coverage>=7.10'` under corpus-verify, leaving its real install `pyyaml pytest` | Inside the corpus-verify job's own block of `.github/workflows/lint.yml`, a live `pip install` command - never a comment, never another job's step - installs coverage with the `>=7.10` floor, pinned by a test that reads that job's commands only |
| AC2 | in .claude/skills/sdlc-studio/scripts/gate.py, read `SDLC_VERIFY_TIMEOUT` into a local in `main()` that the release registry's `_verify_acs(r, allow_external=_x, batch=True)` lambda never passes, so the lane keeps the definition-time 120 s default | The release verify lane reads a per-verifier timeout override from `SDLC_VERIFY_TIMEOUT` when it RUNS and hands it to every verifier it executes, observed through the shipped entry point: `gate.py --root <fixture> --release --only verify,review-legs,versions,changelog-fragments` over a one-story fixture at Done whose only criterion is `shell sleep 3 && test -d sdlc-studio` prints a verify lane line naming `US9300::AC1` as failing with `SDLC_VERIFY_TIMEOUT=1`, and a `[PASS] verify` line with `SDLC_VERIFY_TIMEOUT=10`; the paired control, the variable unset, passes on the 120 s default. Measured at HEAD: that fixture's verify lane passes in 3.7 s today, so the lane line is readable though the gate's other bound lanes fail on a bare fixture |
| AC2 | in .claude/skills/sdlc-studio/scripts/gate.py, read the override from `SDLC_VERIFY_TIMEOUT_S`, a name the fixture and the job never set | The release verify lane reads a per-verifier timeout override from `SDLC_VERIFY_TIMEOUT` when it RUNS and hands it to every verifier it executes, observed through the shipped entry point: `gate.py --root <fixture> --release --only verify,review-legs,versions,changelog-fragments` over a one-story fixture at Done whose only criterion is `shell sleep 3 && test -d sdlc-studio` prints a verify lane line naming `US9300::AC1` as failing with `SDLC_VERIFY_TIMEOUT=1`, and a `[PASS] verify` line with `SDLC_VERIFY_TIMEOUT=10`; the paired control, the variable unset, passes on the 120 s default. Measured at HEAD: that fixture's verify lane passes in 3.7 s today, so the lane line is readable though the gate's other bound lanes fail on a bare fixture |
| AC2 | in .claude/skills/sdlc-studio/scripts/gate.py `_verify_acs`, hand the override to `pytest_batch_cache` and `jest_batch_cache` only, leaving each per-criterion `verify_story` call on `VERIFY_TIMEOUT` | The release verify lane reads a per-verifier timeout override from `SDLC_VERIFY_TIMEOUT` when it RUNS and hands it to every verifier it executes, observed through the shipped entry point: `gate.py --root <fixture> --release --only verify,review-legs,versions,changelog-fragments` over a one-story fixture at Done whose only criterion is `shell sleep 3 && test -d sdlc-studio` prints a verify lane line naming `US9300::AC1` as failing with `SDLC_VERIFY_TIMEOUT=1`, and a `[PASS] verify` line with `SDLC_VERIFY_TIMEOUT=10`; the paired control, the variable unset, passes on the 120 s default. Measured at HEAD: that fixture's verify lane passes in 3.7 s today, so the lane line is readable though the gate's other bound lanes fail on a bare fixture |
| AC3 | in .github/workflows/lint.yml, set the override to 130 seconds for the corpus lane - above the 120 s default gate.py already carries, yet short of the 136 s GateRealWrapperTests needs | The corpus-verify job sets `SDLC_VERIFY_TIMEOUT` - the exact name AC2's lane reads, which the test reads from gate.py's source rather than restating - on that job or its verification step, to an integer strictly above 136, the slowest criterion measured (GateRealWrapperTests, about 136 s locally, against today's 120 s default) |
| AC3 | in .github/workflows/lint.yml, set `SDLC_VERIFY_TIMEOUT: 600` on the ci job's suite step instead of the corpus-verify job | The corpus-verify job sets `SDLC_VERIFY_TIMEOUT` - the exact name AC2's lane reads, which the test reads from gate.py's source rather than restating - on that job or its verification step, to an integer strictly above 136, the slowest criterion measured (GateRealWrapperTests, about 136 s locally, against today's 120 s default) |
| AC3 | in .github/workflows/lint.yml, change the corpus-verify job's variable name to `SDLC_VERIFY_TIMEOUT_S`, which gate.py never reads | The corpus-verify job sets `SDLC_VERIFY_TIMEOUT` - the exact name AC2's lane reads, which the test reads from gate.py's source rather than restating - on that job or its verification step, to an integer strictly above 136, the slowest criterion measured (GateRealWrapperTests, about 136 s locally, against today's 120 s default) |
| AC4 | in tools/verify-corpus-baseline.txt, append US0031::AC3 to field 4 and bump the count, keeping the run line and adding no new comment line, so only the existing prose on line 51 mentions it | The red-criteria baseline is re-measured from a CI corpus-verify run taken with AC3's override in force, never a local one. `tools/verify-corpus-baseline.txt` carries a comment line `# re-measured from CI run <databaseId>` naming that run; its red-criteria row's count equals the ids in its field 4; and every id in field 4 that is not among the 20 the row holds at 3f73ab64 (held in the test as a literal, so 'added' is judged against a fixed prior set) is named on a comment line of its own - never in field 4, which the lane splits on whitespace - beside either a bug id whose file exists under `sdlc-studio/bugs/` or `cause:` followed by a non-empty proven environment cause (coverage, history depth, ripgrep, time or another), so the re-measure cannot bank whatever CI happens to read. A comment line that merely mentions an id in prose does not justify it: lines 51, 95 and 102 of the baseline at 3f73ab64 already name US0031::AC3 and US0220::AC2 with neither a bug id nor `cause:`, and neither may pass on those lines alone. Ids that went green, US0666 AC2 among them, leave the row, which AC5's run judges: the lane fails on a move in either direction |
| AC4 | in tools/verify-corpus-baseline.txt, justify each appended id yet write no run line at all, only `local 3f73ab64` | The red-criteria baseline is re-measured from a CI corpus-verify run taken with AC3's override in force, never a local one. `tools/verify-corpus-baseline.txt` carries a comment line `# re-measured from CI run <databaseId>` naming that run; its red-criteria row's count equals the ids in its field 4; and every id in field 4 that is not among the 20 the row holds at 3f73ab64 (held in the test as a literal, so 'added' is judged against a fixed prior set) is named on a comment line of its own - never in field 4, which the lane splits on whitespace - beside either a bug id whose file exists under `sdlc-studio/bugs/` or `cause:` followed by a non-empty proven environment cause (coverage, history depth, ripgrep, time or another), so the re-measure cannot bank whatever CI happens to read. A comment line that merely mentions an id in prose does not justify it: lines 51, 95 and 102 of the baseline at 3f73ab64 already name US0031::AC3 and US0220::AC2 with neither a bug id nor `cause:`, and neither may pass on those lines alone. Ids that went green, US0666 AC2 among them, leave the row, which AC5's run judges: the lane fails on a move in either direction |
| AC4 | in tools/verify-corpus-baseline.txt, justify one appended id with BG9999, a number no file carries | The red-criteria baseline is re-measured from a CI corpus-verify run taken with AC3's override in force, never a local one. `tools/verify-corpus-baseline.txt` carries a comment line `# re-measured from CI run <databaseId>` naming that run; its red-criteria row's count equals the ids in its field 4; and every id in field 4 that is not among the 20 the row holds at 3f73ab64 (held in the test as a literal, so 'added' is judged against a fixed prior set) is named on a comment line of its own - never in field 4, which the lane splits on whitespace - beside either a bug id whose file exists under `sdlc-studio/bugs/` or `cause:` followed by a non-empty proven environment cause (coverage, history depth, ripgrep, time or another), so the re-measure cannot bank whatever CI happens to read. A comment line that merely mentions an id in prose does not justify it: lines 51, 95 and 102 of the baseline at 3f73ab64 already name US0031::AC3 and US0220::AC2 with neither a bug id nor `cause:`, and neither may pass on those lines alone. Ids that went green, US0666 AC2 among them, leave the row, which AC5's run judges: the lane fails on a move in either direction |
| AC4 | in tools/verify-corpus-baseline.txt, justify one appended id with a comment line ending in `cause:` and nothing after it | The red-criteria baseline is re-measured from a CI corpus-verify run taken with AC3's override in force, never a local one. `tools/verify-corpus-baseline.txt` carries a comment line `# re-measured from CI run <databaseId>` naming that run; its red-criteria row's count equals the ids in its field 4; and every id in field 4 that is not among the 20 the row holds at 3f73ab64 (held in the test as a literal, so 'added' is judged against a fixed prior set) is named on a comment line of its own - never in field 4, which the lane splits on whitespace - beside either a bug id whose file exists under `sdlc-studio/bugs/` or `cause:` followed by a non-empty proven environment cause (coverage, history depth, ripgrep, time or another), so the re-measure cannot bank whatever CI happens to read. A comment line that merely mentions an id in prose does not justify it: lines 51, 95 and 102 of the baseline at 3f73ab64 already name US0031::AC3 and US0220::AC2 with neither a bug id nor `cause:`, and neither may pass on those lines alone. Ids that went green, US0666 AC2 among them, leave the row, which AC5's run judges: the lane fails on a move in either direction |
| AC5 | in sdlc-studio/bugs/BG0676-the-scheduled-corpus-verify-lane-is-red-on.md, set the Verified-on field to the sha of the delivery's first diagnostic dispatch - main HEAD before any candidate cause is corrected, whose corpus-verify job concluded failure. Executed by writing that sha, running the Verify (it must exit non-zero), then restoring the closing sha | A completed workflow_dispatch Lint run on the sha BG0676 records in its own header as `> **Verified-on:** <40-hex sha>` concluded success in its corpus-verify JOB (the workflow's other jobs also run on a dispatch and must not stand in for it), and that sha is on HEAD's history. Step one of the work is an uncorrected workflow_dispatch of Lint on main (none exists yet), so a red dispatch run is on record. The delivery writes the field only after pushing BG0676's closing commit, confirming main's HEAD is that commit, and dispatching Lint on main - `gh workflow run` dispatches a ref, not a sha - so the recorded sha is the one CI read and not whatever HEAD the close paperwork later moves to. BG0676 depends on every other batch unit, so it is delivered last and that sha carries the whole batch; `--status completed` keeps an in-progress rerun from reading as a failure |
| AC5 | in sdlc-studio/bugs/BG0676-the-scheduled-corpus-verify-lane-is-red-on.md, set the Verified-on field to the full sha of f7c120ef, a main commit that was pushed but never dispatched. Executed the same way: write it, run the Verify (it must exit non-zero, finding no dispatch run), restore | A completed workflow_dispatch Lint run on the sha BG0676 records in its own header as `> **Verified-on:** <40-hex sha>` concluded success in its corpus-verify JOB (the workflow's other jobs also run on a dispatch and must not stand in for it), and that sha is on HEAD's history. Step one of the work is an uncorrected workflow_dispatch of Lint on main (none exists yet), so a red dispatch run is on record. The delivery writes the field only after pushing BG0676's closing commit, confirming main's HEAD is that commit, and dispatching Lint on main - `gh workflow run` dispatches a ref, not a sha - so the recorded sha is the one CI read and not whatever HEAD the close paperwork later moves to. BG0676 depends on every other batch unit, so it is delivered last and that sha carries the whole batch; `--status completed` keeps an in-progress rerun from reading as a failure |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-15 | sprint planning 2026-09-15 | Filed |
| 2026-09-15 | sprint planning 2026-09-15 | Distinct from BG0657 (Fixed), which made the corpus lane name WHICH criteria are new; this is the lane red again, at 40 against 20, for what the local runs show is an environment difference. |
| 2026-09-15 | sprint planning 2026-09-15 | Re-groomed at the sprint goal review (round 1): the CI check is bound to the CLOSING sha; the baseline file and gate.py's bare VERIFY_TIMEOUT join Affects, since the fix cannot pass without them; the baseline is re-measured from CI. The lane has failed all 8 scheduled runs since 2026-07-27 (dead-stamps before 09-14), so the goal says 'passes', not 'green again'. 3 -> 5 points. |
| 2026-09-15 | sprint planning 2026-09-15 | Goal review round 2 repairs: depends on every other batch unit so it is delivered last and AC4's dispatch runs on the commit that carries the whole batch; each id added to the re-measured baseline must name its bug or proven cause, so the baseline cannot bank whatever CI reads. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r1 REJECT answered. AC5 (was AC4) no longer finds the commit with a moving git log --grep: the delivery records the sha in this bug's header as a Verified-on field after pushing the closing commit, confirming it is main's HEAD and dispatching Lint on main; the Verify reads that field, requires the sha on HEAD's history, reads the dispatch run with --status completed and judges the corpus-verify job only. Its two mutants are executed by writing a sha into that field (the first diagnostic dispatch, red before any cause is corrected; a pushed commit never dispatched), running the Verify, expecting non-zero, and restoring; the Verify's parsing was exercised both ways at HEAD. The old AC2 is split: new AC2 drives the shipped gate.py --release --only verify (plus the bound lanes) on a one-story fixture whose shell verifier sleeps 3 s, observing the timeout the lane hands a verifier (1 s red, 10 s green, unset green on the default; measured feasible at HEAD in 3.7 s), with rows for an env read the release lambda never passes, a misspelt name, and an override reaching only the batch caches; new AC3 pins the job's value strictly above the 136 s slowest criterion, with rows for 100 s, the wrong job and a misspelt name. AC4 (was AC3) splits the compound mutant into a missing cause, a missing CI run id and a cause naming no bug file, judges 'added' against the 20 ids the row holds at 3f73ab64 as a literal, and puts causes on comment lines, not field 4. AC1 gains rows for an unversioned install and a floor only in a comment. Not taken: requiring fetch-depth 0, which the Proposed Fix keeps only if the experiment moves the count; AC4 now names history depth and ripgrep among the causes an added id may cite. Judged 8 points, up from 5. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r2 REJECT answered (two BLOCKING). AC4: the missing-cause row now appends US0031::AC3, which baseline line 51 already names in prose, with no new comment line; the criterion now says a prose mention justifies nothing and names lines 51, 95 and 102, and requires a non-empty cause after cause:. New AC4 row: a comment line ending in cause: with nothing after it. AC3: the short-override row is now 130 s, above the 120 s default yet short of the 136 s floor, so it separates the two. Cheap issue taken: AC5 now makes an uncorrected workflow_dispatch of Lint on main step one of the work. Not taken (advice): count-mismatch row, ancestor-clause row, non-verification-step row, empty-override row. |
