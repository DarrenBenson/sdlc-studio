# BG0603: Stacked Verify lines are refused at Draft and Ready but not on an Open bug

> **Status:** In Progress
> **Verification depth:** functional
> **Severity:** Medium
> **Points:** 2
> **Affects:** .claude/skills/sdlc-studio/scripts/verify_ac.py, .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py
> **Created:** 2026-08-21
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`lint_stacked_verifiers` refuses a criterion carrying more than one `Verify:` line, but `verify_ac.py` applies it only at Draft and Ready. A bug sits at Open for its whole delivery, so the shipped command never refuses the shape while it is being authored - the only thing that catches it is a repo-only census in `tools/tests/test_known_issues.py`, which does not ship and does not run in a consuming project. A stacked criterion cannot report which of its claims failed, which is why the rule exists.

## Steps to Reproduce

Author an Open bug with two `Verify:` lines under one criterion. Run `verify_ac.py lint --unit <id>`: it passes. Move the same artefact to Ready and it refuses. The repo census catches it here only because this repo happens to ship one.

## Proposed Fix

Apply `lint_stacked_verifiers` at Open as well. The status gate was presumably meant to spare half-written drafts, but Open is the status a bug occupies while its criteria are final, so it is the wrong status to exempt. Pin the new status set in a test - LL0027, gate the rule in the command people actually run.

Plan review round 1 (2026-09-04): the filed summary's "repo-only census in `tools/tests/test_known_issues.py`" is wrong - the file exists and holds no stacked-verifier census; the census is `StackedVerifierTests::test_no_ac_block_in_the_workspace_stacks_verifiers` in the shipped suite. Measured at HEAD: stacked lines pass `lint` at Open AND at In Progress and are refused at Ready, so the set is defined as non-terminal rather than widened by one name. The tests drive the pre-commit invocation (`lint --ratchet --bugs`) as a subprocess, because plain `lint` walks stories only and would never read the fixture bug.

Plan review round 2 (2026-09-04): both rejecting seats EXECUTED the control and found it exits 1 on today's code and on the fix alike, because `--ratchet` refuses a tree with no `.verify-lint-baseline.json` before any criterion is read. The fixture seeds an empty baseline, keeps every selector distinct, and AC1 asserts the REFUSED line rather than the exit code. The terminal set is read through `is_terminal_status`, and the widening reaches non-terminal stories as well as bugs, which the criterion now says.

## Acceptance Criteria

- [ ] **AC1** Given a fixture tree that carries `sdlc-studio/.verify-lint-baseline.json` as `{"groups": {}}` (the criteria are linted and their REFUSED lines printed regardless; without a baseline the ratchet then returns 1, so the exit-0 control of AC3 needs it - measured), an OPEN bug and an IN PROGRESS bug each with two `Verify:` lines under one criterion, and every selector in the tree distinct (a shared selector is a duplicate group the ratchet refuses for its own reason), when `verify_ac.py lint --ratchet --bugs --root <fixture>` runs as a subprocess - the invocation `.githooks/pre-commit` makes - then it exits non-zero AND prints the per-criterion line beginning `<file> AC1: REFUSED: 1 further` for each (the lint backticks the word `Verify:` in that line, so the test asserts the prefix), which today's code does not print at Open. The linted set is every NON-TERMINAL status, read through `is_terminal_status(type_, status)` (in `lib/sdlc_md.py`) over `TERMINAL_STATUS`, passed the UN-LOWERED status because the lint loop lowercases for its own set, with the type taken from the id prefix - never enumerated - so it covers stories at Proposed, Planned, In Progress, Review, Blocked and Deferred too (measured at HEAD over every non-terminal story and bug: zero stacked and zero markdown-only verifiers, so nothing lands refused)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py::StackedVerifierTests::test_stacked_verifiers_are_refused_at_open
  - **Verified:** yes (2026-09-04)
- [ ] **AC2** Given two STORIES at Ready with the same stacked shape (Ready is not a bug status - a bug at Ready would reach the unrecognised-status branch, not the window), when the same invocation runs, then each is refused with the same line exactly as today - the control that shows the rule was widened rather than moved
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py::StackedVerifierTests::test_ready_still_refuses
  - **Verified:** yes (2026-09-04)
- [ ] **AC3** Given the same fixture tree (baseline seeded) holding an Open bug with ONE `Verify:` line per criterion, one of them a `grep` over a `.py` file, and a FIXED bug carrying two `Verify:` lines under one criterion, when the identical invocation runs, then it exits 0 and prints no REFUSED line for the Fixed bug - the paired control, reachable only because the baseline is seeded, and the terminal edge of "non-terminal", without which linting every status passes all three tests; and it exercises `lint_markdown_evidence`, which shares the `authoring` flag and is widened with this change (intended, and measured zero-impact above)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py::StackedVerifierTests::test_a_single_verifier_at_open_passes
  - **Verified:** yes (2026-09-04)
- [ ] **AC4** Given an Open bug whose only verifier is a `grep` over a markdown file, when the identical invocation runs, then it is refused with the markdown-only line (`-> REFUSED: ... prose`) - the markdown-evidence rule shares the window and the widening is pinned, not only stated. Added at delivery on 2026-09-04: the review found no test that fails when the widening is absent
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py::StackedVerifierTests::test_the_markdown_only_refusal_shares_the_window
  - **Verified:** yes (2026-09-04)
- [ ] **AC5** Given a story whose Status line is decorated (`Done (v5.0.1)`) and carries stacked lines, when linted through `--story`, then it is NOT refused - the decorated status canonicalises to a terminal one; and given an artefact with an unrecognised status or an unknown id prefix, then it IS refused - the window fails closed. Added at delivery on 2026-09-04: both branches were promised by the docstring and pinned by nothing
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py::StackedVerifierTests::test_a_decorated_terminal_status_is_exempt_and_an_unreadable_one_fails_closed
  - **Verified:** yes (2026-09-04)

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py`, reintroduce the enumerated `{"draft", "ready"}` set in place of the terminal check - today's code - so an Open bug is never linted | Given a fixture tree that carries `sdlc-studio/.verify-lint-baseline.json` as `{"groups": {}}` (the criteria are linted and their REFUSED lines printed regardless; without a baseline the ratchet then returns 1, so the exit-0 control of AC3 needs it - measured), an OPEN bug and an IN PROGRESS bug each with two `Verify:` lines under one criterion, and every selector in the tree distinct (a shared selector is a duplicate group the ratchet refuses for its own reason), when `verify_ac.py lint --ratchet --bugs --root <fixture>` runs as a subprocess - the invocation `.githooks/pre-commit` makes - then it exits non-zero AND prints the per-criterion line beginning `<file> AC1: REFUSED: 1 further` for each (the lint backticks the word `Verify:` in that line, so the test asserts the prefix), which today's code does not print at Open. The linted set is every NON-TERMINAL status, read through `is_terminal_status(type_, status)` (in `lib/sdlc_md.py`) over `TERMINAL_STATUS`, passed the UN-LOWERED status because the lint loop lowercases for its own set, with the type taken from the id prefix - never enumerated - so it covers stories at Proposed, Planned, In Progress, Review, Blocked and Deferred too (measured at HEAD over every non-terminal story and bug: zero stacked and zero markdown-only verifiers, so nothing lands refused) |
| AC1 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py`, reintroduce an enumerated set in `_still_authoring` that names `Open` capitalised while comparing the lowercased status, so it never matches | Given a fixture tree that carries `sdlc-studio/.verify-lint-baseline.json` as `{"groups": {}}` (the criteria are linted and their REFUSED lines printed regardless; without a baseline the ratchet then returns 1, so the exit-0 control of AC3 needs it - measured), an OPEN bug and an IN PROGRESS bug each with two `Verify:` lines under one criterion, and every selector in the tree distinct (a shared selector is a duplicate group the ratchet refuses for its own reason), when `verify_ac.py lint --ratchet --bugs --root <fixture>` runs as a subprocess - the invocation `.githooks/pre-commit` makes - then it exits non-zero AND prints the per-criterion line beginning `<file> AC1: REFUSED: 1 further` for each (the lint backticks the word `Verify:` in that line, so the test asserts the prefix), which today's code does not print at Open. The linted set is every NON-TERMINAL status, read through `is_terminal_status(type_, status)` (in `lib/sdlc_md.py`) over `TERMINAL_STATUS`, passed the UN-LOWERED status because the lint loop lowercases for its own set, with the type taken from the id prefix - never enumerated - so it covers stories at Proposed, Planned, In Progress, Review, Blocked and Deferred too (measured at HEAD over every non-terminal story and bug: zero stacked and zero markdown-only verifiers, so nothing lands refused) |
| AC1 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py`, enumerate `open` into a set and leave `in progress` out | Given a fixture tree that carries `sdlc-studio/.verify-lint-baseline.json` as `{"groups": {}}` (the criteria are linted and their REFUSED lines printed regardless; without a baseline the ratchet then returns 1, so the exit-0 control of AC3 needs it - measured), an OPEN bug and an IN PROGRESS bug each with two `Verify:` lines under one criterion, and every selector in the tree distinct (a shared selector is a duplicate group the ratchet refuses for its own reason), when `verify_ac.py lint --ratchet --bugs --root <fixture>` runs as a subprocess - the invocation `.githooks/pre-commit` makes - then it exits non-zero AND prints the per-criterion line beginning `<file> AC1: REFUSED: 1 further` for each (the lint backticks the word `Verify:` in that line, so the test asserts the prefix), which today's code does not print at Open. The linted set is every NON-TERMINAL status, read through `is_terminal_status(type_, status)` (in `lib/sdlc_md.py`) over `TERMINAL_STATUS`, passed the UN-LOWERED status because the lint loop lowercases for its own set, with the type taken from the id prefix - never enumerated - so it covers stories at Proposed, Planned, In Progress, Review, Blocked and Deferred too (measured at HEAD over every non-terminal story and bug: zero stacked and zero markdown-only verifiers, so nothing lands refused) |
| AC2 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py`, make the window `{"open"}` alone, so widening to Open moved the rule off Ready | Given two STORIES at Ready with the same stacked shape (Ready is not a bug status - a bug at Ready would reach the unrecognised-status branch, not the window), when the same invocation runs, then each is refused with the same line exactly as today - the control that shows the rule was widened rather than moved |
| AC3 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py` `lint_stacked_verifiers`, return a refusal for every block whatever it carries | Given the same fixture tree (baseline seeded) holding an Open bug with ONE `Verify:` line per criterion, one of them a `grep` over a `.py` file, and a FIXED bug carrying two `Verify:` lines under one criterion, when the identical invocation runs, then it exits 0 and prints no REFUSED line for the Fixed bug - the paired control, reachable only because the baseline is seeded, and the terminal edge of "non-terminal", without which linting every status passes all three tests; and it exercises `lint_markdown_evidence`, which shares the `authoring` flag and is widened with this change (intended, and measured zero-impact above) |
| AC3 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py` `lint_markdown_evidence`, refuse every `grep` verifier regardless of its target | Given the same fixture tree (baseline seeded) holding an Open bug with ONE `Verify:` line per criterion, one of them a `grep` over a `.py` file, and a FIXED bug carrying two `Verify:` lines under one criterion, when the identical invocation runs, then it exits 0 and prints no REFUSED line for the Fixed bug - the paired control, reachable only because the baseline is seeded, and the terminal edge of "non-terminal", without which linting every status passes all three tests; and it exercises `lint_markdown_evidence`, which shares the `authoring` flag and is widened with this change (intended, and measured zero-impact above) |
| AC3 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py`, make `_still_authoring` return True unconditionally so terminal artefacts are linted too | Given the same fixture tree (baseline seeded) holding an Open bug with ONE `Verify:` line per criterion, one of them a `grep` over a `.py` file, and a FIXED bug carrying two `Verify:` lines under one criterion, when the identical invocation runs, then it exits 0 and prints no REFUSED line for the Fixed bug - the paired control, reachable only because the baseline is seeded, and the terminal edge of "non-terminal", without which linting every status passes all three tests; and it exercises `lint_markdown_evidence`, which shares the `authoring` flag and is widened with this change (intended, and measured zero-impact above) |
| AC4 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py`, gate `lint_markdown_evidence` on the old `{"draft", "ready"}` window while the stacked lint uses the new one | Given an Open bug whose only verifier is a `grep` over a markdown file, when the identical invocation runs, then it is refused with the markdown-only line (`-> REFUSED: ... prose`) - the markdown-evidence rule shares the window and the widening is pinned, not only stated. Added at delivery on 2026-09-04: the review found no test that fails when the widening is absent |
| AC5 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py` `_still_authoring`, pass the raw status to `is_terminal_status` without canonicalising it | Given a story whose Status line is decorated (`Done (v5.0.1)`) and carries stacked lines, when linted through `--story`, then it is NOT refused - the decorated status canonicalises to a terminal one; and given an artefact with an unrecognised status or an unknown id prefix, then it IS refused - the window fails closed. Added at delivery on 2026-09-04: both branches were promised by the docstring and pinned by nothing |
| AC5 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py` `_still_authoring`, return False for an unknown id prefix | Given a story whose Status line is decorated (`Done (v5.0.1)`) and carries stacked lines, when linted through `--story`, then it is NOT refused - the decorated status canonicalises to a terminal one; and given an artefact with an unrecognised status or an unknown id prefix, then it IS refused - the window fails closed. Added at delivery on 2026-09-04: both branches were promised by the docstring and pinned by nothing |
| AC5 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py` `_still_authoring`, return False for an unrecognised status | Given a story whose Status line is decorated (`Done (v5.0.1)`) and carries stacked lines, when linted through `--story`, then it is NOT refused - the decorated status canonicalises to a terminal one; and given an artefact with an unrecognised status or an unknown id prefix, then it IS refused - the window fails closed. Added at delivery on 2026-09-04: both branches were promised by the docstring and pinned by nothing |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-08-21 | sdlc-studio | Filed |
| 2026-09-04 | sdlc-studio | Plan review round 1 (three seats): criteria and test plan re-authored to the findings; see the Proposed Fix note dated 2026-09-04 |
| 2026-09-04 | sdlc-studio | Plan review round 2 (three seats): fixtures that could not reach their branch repaired; see the Proposed Fix note |
| 2026-09-04 | sdlc-studio | Plan review round 3 (three seats): delivery notes folded into the criteria; BG0642 AC3 gains the wrong-acknowledgement step |
| 2026-09-04 | sdlc-studio | Delivery review r1: the not-baselined claim corrected to what executes; AC2 fixture uses two stories; AC4 pins the markdown-only widening, AC5 the derived window's edges; mutant rows re-worded to the new code |
