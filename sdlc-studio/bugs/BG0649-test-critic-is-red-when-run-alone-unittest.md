# BG0649: test_critic is red when run alone: unittest.mock is used at line 5131 without an import

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 3; plan rows 26; executed 26; killed 26; survived 0; not-run 0; entry point 1 of 2 criteria through the shipped CLI, 1 in-process; 1 with no locatable test file | fp 856318d90f18 ]]
> **Severity:** Medium
> **Points:** 3
> **Affects:** .claude/skills/sdlc-studio/scripts/tests/test_critic.py, .claude/skills/sdlc-studio/scripts/gate.py, .claude/skills/sdlc-studio/scripts/tests/test_gate.py, AGENTS.md, tools/tests/test_check_spec_claims.py, .githooks/pre-push, tools/tests/test_pre_push_hook.py
> **Evidence:** Found by the BG0644 engineering delivery review (round 2, 2026-09-04) measuring modules alone; reproduced at ba715bd2. Present since 46cdf08b (2026-08-03).
> **Created:** 2026-09-04
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`.claude/skills/sdlc-studio/scripts/tests/test_critic.py` references `unittest.mock.patch.object` at line 5131 (and 5536) but never imports `unittest.mock`; the name resolves only when another test module has imported it first. Discovery order hides it, and a commit whose selection holds `test_critic` alone is refused by a test failure that is not the commit's.

## Steps to Reproduce

1. `cd .claude/skills/sdlc-studio/scripts/tests && python3 -B -m unittest test_critic`
2. Observe `ERROR: test_a_repair_does_not_answer_a_LATER_rejection` with `AttributeError: module 'unittest' has no attribute 'mock'` (rc=1).
3. Run the discovery suite or `python3 -B -m unittest test_config test_critic`: the same test passes, because a module loaded earlier imported `unittest.mock`.

## Proposed Fix

Import `unittest.mock` (or `from unittest import mock`) at the top of `test_critic.py` and reference it consistently; add a tools/ check or a test that runs each skill test module alone is out of scope here and is the subject of the selected-run lanes.

## Acceptance Criteria

- [x] **AC1** Given `test_critic`, the one skill test module red when run alone from the REPOSITORY ROOT on HEAD (`unittest.mock` used without a head import; `test_file_finding` and `test_sprint` are red only from the tests directory, because each passes `.` as its root, which is a working-directory dependence filed into CR0511 on 2026-09-07 and out of this unit's scope), when it is run as the only module under the UNITTEST runner - `PYTHONPATH=<tests dir> python3 -B -m unittest test_critic` from the repository root in a fresh interpreter, the hook's own invocation, never pytest, which imports the missing name itself and passes on the broken module - then it exits 0 with no ERROR
  - **Verify:** shell PYTHONPATH=.claude/skills/sdlc-studio/scripts/tests python3 -B -m unittest test_critic
  - **Verified:** yes (2026-09-07)
- [x] **AC2** Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_gate.py::ModuleAloneLaneTests::test_the_push_boundary_runs_every_module_alone_and_names_the_one_that_fails
  - **Verified:** yes (2026-09-07)
- [x] **AC3** Given the AGENTS.md lane roster, when `tools/tests/test_check_spec_claims.py` runs, then `module-alone` is named in the roster as bound at the push and release boundaries only, with its cost stated in minutes beside D0180's and the slowest module named - a pin that fails when the cost sentence is deleted or its figure is not stated in minutes, so a boundary-only lane the hook-derived sweep cannot see is not one nobody wrote down (LL0013)
  - **Verify:** pytest tools/tests/test_check_spec_claims.py::GateLaneTests::test_the_lane_roster_names_module_alone_as_boundary_bound
  - **Verified:** yes (2026-09-07)

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `test_critic.py`, drop the head import of `unittest.mock` again and leave the function-local one at line 1377, so the name resolves only where that test ran first - killed by the unittest runner from the root, not by pytest | Given `test_critic`, the one skill test module red when run alone from the REPOSITORY ROOT on HEAD (`unittest.mock` used without a head import; `test_file_finding` and `test_sprint` are red only from the tests directory, because each passes `.` as its root, which is a working-directory dependence filed into CR0511 on 2026-09-07 and out of this unit's scope), when it is run as the only module under the UNITTEST runner - `PYTHONPATH=<tests dir> python3 -B -m unittest test_critic` from the repository root in a fresh interpreter, the hook's own invocation, never pytest, which imports the missing name itself and passes on the broken module - then it exits 0 with no ERROR |
| AC2 | in `gate.py`, run the modules under one interpreter in discovery order instead of one fresh interpreter each, so an import-order dependency passes | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, read the module list from a hard-coded tuple instead of the tests directory under --root, so a new module is never run alone | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, bind the lane per commit as well, so the per-commit gate pays the whole suite twice - the test runs the gate without --boundary and asserts the lane absent | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, name the failing module without its failure line | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, run each module from the tests directory instead of `--root`, so two modules that read config under the working directory are red and the lane refuses a clean push | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, run the modules serially, so the four one-second fixture modules take four seconds | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, ignore the `serial_only` marker and run every module in the parallel phase, so the marked fixture module runs beside the others | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, read the `serial_only` marker by a source scan for its name, so the decoy module runs in the serial phase (delivery review r1, engineering seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, drop the no-output fallback, so a module that dies silently is not named (delivery review r1, engineering seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, name the LAST failure header instead of the first (delivery review r1, engineering seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, read a failed `pytest --collect-only` as an empty partition and run on (delivery review r1, engineering and qa seats) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, export the tests directory as an ABSOLUTE `PYTHONPATH`, so `test_repo_hygiene` is red under the lane and green under the hook (delivery review r1, all three seats) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, name the failure header without its exception line (delivery review r1, product seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, size the parallel pool at two workers instead of the machine's cores (delivery review r1, qa seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, run a module with no timeout, so a hung module hangs the push (delivery review r1, qa seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, narrow the module glob back to `test_*.py`, so a module unittest discover finds is never run alone (delivery review r2, qa seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, print the machine's core count while sizing the pool smaller, so the detail's worker figure is decoupled from the pool (delivery review r2, qa seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, label the serial phase's module as parallel (delivery review r2, engineering seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, print the module total for the parallel count (delivery review r2, engineering seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC3 | in `AGENTS.md`, delete the cost sentence and leave D0180 in the paragraph (delivery review r2, qa seat) | Given the AGENTS.md lane roster, when `tools/tests/test_check_spec_claims.py` runs, then `module-alone` is named in the roster as bound at the push and release boundaries only, with its cost stated in minutes beside D0180's and the slowest module named - a pin that fails when the cost sentence is deleted or its figure is not stated in minutes, so a boundary-only lane the hook-derived sweep cannot see is not one nobody wrote down (LL0013) |
| AC2 | in `gate.py`, clip the refusal's reason mid-path at 300 characters (delivery review r3, qa seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC2 | in `gate.py`, size the pool at half the cores while printing the core count, so the sleepers run in two waves (delivery review r3, qa seat) | Given the push boundary gate (`gate.py --boundary push`), when its `module-alone` lane runs, then every module under `<root>/.claude/skills/sdlc-studio/scripts/tests` (derived from `--root`, and reported N/A when that directory is absent) is run alone under the unittest runner in a fresh interpreter FROM THE REPOSITORY ROOT (`--root`, the hook's working directory, which alone decides which modules are red) with the hook's own RELATIVE tests path on `PYTHONPATH` (one module is green under that form and red under an absolute one), in PARALLEL across the machine's cores (the `serial_only` marker read the runner's way, `pytest --collect-only -q -m serial_only`, never a source scan - pinned by a decoy module carrying the marker's name in a comment) so the wall-clock is bounded by the slowest module (`test_gate`, about four minutes) rather than the serial sum - 519 s at filing, about 1,000 s measured at delivery (pinned by a fixture of eight modules that each sleep one second, a lane that finishes in under three, a detail line naming a parallel phase on the pool's own worker count, read from the pool, and the sleepers' own start and end stamps showing eight asleep at once on a machine with eight cores or more), and any failure names the module and carries its FIRST failure header with its exception line, a module that dies with no output by a fallback naming that, a module that hangs by its timeout (`SDLC_MODULE_ALONE_TIMEOUT`, half an hour by default), and a partition read that fails is refused rather than read as empty, its reason clipped at a word never mid-path by a helper the test exercises; the modules run are the ones `unittest discover` finds (`test*.py`), pinned by a module the narrower `test_*.py` glob would miss; the phase label and the parallel count in the detail are pinned by a marked module that is red; modules carrying the suite's `serial_only` marker (today `test_rehearse_release`, which snapshots the real tree's `git status --ignored` while siblings write bytecode) run in a SERIAL phase after the parallel phase, as the suite's own runner partitions them, and the lane records which phase each ran in - pinned by a fixture module carrying the marker that the test asserts ran after every other; the lane binds at the push and release boundaries only, on `release-rehearsal`'s precedent, and is absent from the per-commit gate; pinned in a fixture tests directory of two modules where one passes only after the other imported a name |
| AC3 | in `AGENTS.md`, restore the false figure - `Its cost is about 45 s, the gate's own budget` - with the word minutes later in the sentence (delivery review r3, qa seat) | Given the AGENTS.md lane roster, when `tools/tests/test_check_spec_claims.py` runs, then `module-alone` is named in the roster as bound at the push and release boundaries only, with its cost stated in minutes beside D0180's and the slowest module named - a pin that fails when the cost sentence is deleted or its figure is not stated in minutes, so a boundary-only lane the hook-derived sweep cannot see is not one nobody wrote down (LL0013) |
| AC3 | in `AGENTS.md`, leave the roster without the lane | Given the AGENTS.md lane roster, when `tools/tests/test_check_spec_claims.py` runs, then `module-alone` is named in the roster as bound at the push and release boundaries only, with its cost stated in minutes beside D0180's and the slowest module named - a pin that fails when the cost sentence is deleted or its figure is not stated in minutes, so a boundary-only lane the hook-derived sweep cannot see is not one nobody wrote down (LL0013) |
| AC3 | in `AGENTS.md`, name the lane in the roster without saying it binds at the boundaries only | Given the AGENTS.md lane roster, when `tools/tests/test_check_spec_claims.py` runs, then `module-alone` is named in the roster as bound at the push and release boundaries only, with its cost stated in minutes beside D0180's and the slowest module named - a pin that fails when the cost sentence is deleted or its figure is not stated in minutes, so a boundary-only lane the hook-derived sweep cannot see is not one nobody wrote down (LL0013) |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-04 | sdlc-studio | Filed |
| 2026-09-07 | sdlc-studio | Delivered, then delivery review r1 (three REJECTs, by execution): the lane was red on this repository because it exported an absolute PYTHONPATH where the hook uses a relative one; the roster's 45 s cost was the gate's budget constant, not a measurement; three AC2 clauses had no fixture case. Repaired: relative tests path, a refused partition read, a per-module timeout (SDLC_MODULE_ALONE_TIMEOUT), the exception line beside the failure header, unittest discover's own pattern, the pool's worker count in the detail, a thirteen-module fixture, honest figures, the hook and roster wording; AC2 re-worded to what shipped and eight Test Plan rows added |
| 2026-09-07 | sdlc-studio | Delivery review r2 (engineering REJECT, qa REJECT, product APPROVE): the hook wording change broke a pin in tools/tests/test_pre_push_hook.py, now updated and the hook and its test added to Affects; the AC3 pin now fails when the cost sentence goes and demands both boundaries named; the cost is stated as the lane's wall clock (four to five minutes measured, test_gate near four) against a serial sum of fifteen to seventeen; the worker figure is read from the pool; the serial label and parallel count are pinned by a red marked module; a module the narrower glob would miss is in the fixture; a clipped reason breaks at a word; help/gate.md names the three boundary lanes and the knob; five Test Plan rows added; three pre-existing findings filed into CR0511 |
| 2026-09-07 | sdlc-studio | Before delivery review r3: the pool-size mutant (AC2 row 16) survived the worker-count string pin, so the fixture's sleepers now stamp their start and end and the test asserts eight were asleep at once on a machine with eight cores or more; AC2 says so; plan-row titles re-synced to the criteria; five tooling CRs filed from the failure analysis (CR0564 to CR0568) |
| 2026-09-07 | sdlc-studio | Delivery review r3: engineering APPROVE, qa APPROVE. Residues folded in: the AC3 pin demands the wall-clock figure in minutes immediately after 'Its cost is', so a restored 'about 45 s' fails it (AC3 row 3); the reason clip is a helper the AC2 test exercises at a word boundary (AC2 row 19); the sleepers are one per core, sixteen at most, so a half-size pool runs them in two waves and dies on the overlap (AC2 row 20); the roster states that the lane reads the marker through pytest and refuses a clone without it; BG0641 carries a revision row noting its AC4 count of two lanes is stale by one. Lane measured on this repository by the seats: 248.8 to 293.0 s wall, test_gate 220.7 to 237.6 s alone |
