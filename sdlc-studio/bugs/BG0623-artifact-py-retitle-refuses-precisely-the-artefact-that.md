# BG0623: artifact.py retitle refuses precisely the artefact that needs it, because a malformed H1 is both the defect and the thing the tool requires to work

> **Status:** Fixed
> **Severity:** Medium
> **Points:** 3
> **Affects:** .claude/skills/sdlc-studio/scripts/artifact.py, .claude/skills/sdlc-studio/scripts/tests/test_artifact.py
> **Evidence:** Found during RUN-01M0YXN3 wave 1, 2026-08-26. BG0621's hardened bar reported BG0131 unparseable - the first live instance the new guard surfaced - and `retitle` then refused to repair it. The H1 was corrected by hand, which is the outcome this bug is about.
> **Verification depth:** functional [[derived: criteria 5; plan rows 15; executed 15; killed 15; survived 0; not-run 0; entry point 5 of 5 criteria through the shipped CLI, 0 in-process | fp 2d5b6fc8444c ]] (four criteria, five mutants - AC1 carries two, because its criterion names two distinct wrongs and one mutant cannot stand for both. Every one applied to the real file with bytecode purged and the tree restored. Entry point reads 4 of 4: every criterion drives `artifact.py retitle` as a subprocess, which is where the bug's own evidence lives. AC1's fixtures invoke with a NON-canonical spelling on purpose - `norm_id` folds case and the dash, so an implementation stamping the caller's spelling passes any test that asks canonically.)
> **Created:** 2026-08-26
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`retitle` exists because a title lives in the H1, the filename slug and the index row at once, so correcting one by hand means correcting all three plus every inbound link. But it resolves the artefact through `find_by_id`, which needs the H1 to parse - so the one case where the H1 is WRONG is the one case the tool cannot help with. Hit on 2026-08-26 on BG0131, whose H1 read `# BG0131 (CORRECTED - the original claim was WRONG): ...`, putting a parenthetical between the id and the colon. `retitle --id BG0131` answered "no `# <ID>: <title>` H1 ... to rewrite". The refusal message is honest and names the remedy - add the heading, then retitle - but that remedy is the hand-edit the tool exists to replace, and it is performed on the file least likely to be noticed, since a malformed heading is exactly what makes an artefact invisible to the readers that would otherwise flag it.

## Steps to Reproduce

**The live instance is GONE and the steps must not be read as reproducing against HEAD.** BG0131 was
the finding whose H1 carried a parenthetical between the id and the colon; it was repaired BY HAND
during RUN-01M0YXN3 - which is the outcome this bug is about - so `find_by_id` resolves it now and
no artefact in the corpus still shows the symptom.

Reproduce in a FIXTURE: mint an artefact, rewrite its H1 to any form `# <ID>: <title>` does not
match, then run `artifact.py retitle --id <id> --title '<new>' --dry-run`. It refuses at the `h1`
surface and writes nothing, leaving the hand-edit as the only route - and the hand-edit is performed
on the file least likely to be noticed, because a malformed heading is exactly what makes an
artefact invisible to the id-addressed readers that would otherwise flag it.

## Proposed Fix

Let `retitle` REPAIR an unparseable H1 rather than require a parseable one. The unit is already identified by `--id` and by the filename, so the H1 does not have to be the lookup key for a verb whose whole job is to rewrite it. A narrower option is a `--repair-heading` mode that accepts a file path instead of an id. Either way the tool should be able to fix the state it currently refuses, because that state is the one its own callers cannot fix any other way.

## Acceptance Criteria

- [x] **AC1** Given an artefact whose H1 does not parse, when `artifact.py retitle` is invoked as a SUBPROCESS with the id SPELLED DIFFERENTLY from the filename - `bg0623` or `BG-0623` against `BG0623-...md` - then it exits 0 and the repaired heading carries the FILE'S canonical id. The spelling mismatch is required by the criterion rather than left to the fixture: `norm_id` folds case and the dash, so an implementation writing `# bg0623:` resolves and passes unless the test asks non-canonically - not the whole stem, and not the id as the caller spelled it. `artifact.py`:1906 reads `extract_record_id(path.stem) or path.stem`, so the obvious repair composes the heading from the entire filename
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_artifact.py::RetitleRepairsTests::test_the_repaired_heading_carries_the_files_canonical_id
  - **Verified:** yes (2026-08-28)
- [x] **AC2** Given an artefact whose H1 parses but spells its id in a DIFFERENT FORM from the stem - the dashed form, or lowercase - when `retitle` runs, then that form is preserved. The form mismatch is required: `_H1_RETITLE_RE` keeps its first group verbatim and substitutes only the tail, so for an ordinary matching heading an unconditional rewrite is byte-identical and a control built that way pins nothing
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_artifact.py::RetitleRepairsTests::test_a_differently_formed_heading_is_preserved
  - **Verified:** yes (2026-08-28)
- [x] **AC3** Given an artefact whose H1 needs repair AND whose index row is missing, when `retitle` runs, then NOTHING is written - the repair is not an escape hatch from the all-or-nothing validation the other surfaces are held to
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_artifact.py::RetitleRepairsTests::test_a_repair_still_respects_the_all_or_nothing_validation
  - **Verified:** yes (2026-08-28)
- [x] **AC4** Given an artefact whose H1 needs repair and whose other surfaces are all valid, when `retitle` runs, then the file IS written and every surface carries the new title. AC3's silence assertion is satisfied by a command that refuses outright, so without this row the repair need never happen
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_artifact.py::RetitleRepairsTests::test_a_valid_repair_actually_writes_every_surface
  - **Verified:** yes (2026-08-28)

- [x] **AC5** Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_artifact.py::RetitleRepairsTests::test_no_shape_loses_content_and_only_a_level_one_heading_is_overwritten
  - **Verified:** yes (2026-08-28)

## Impact

The doctrine's rule is that mechanical work goes through a tool and hand-authoring is an error. Here the tool declines exactly when hand-authoring is most dangerous: a malformed H1 makes an artefact invisible to the id-addressed readers, so the artefact that most needs a deterministic repair is the one that gets a manual one. BG0619 records the sibling shape, where a retro and a handoff cannot be addressed at all.

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, compose the repaired heading from `path.stem` rather than from the extracted record id, which is the fallback already on that line, so the H1 carries the whole filename | Given an artefact whose H1 does not parse, when `artifact.py retitle` is invoked as a SUBPROCESS with the id SPELLED DIFFERENTLY from the filename - `bg0623` or `BG-0623` against `BG0623-...md` - then it exits 0 and the repaired heading carries the FILE'S canonical id. The spelling mismatch is required by the criterion rather than left to the fixture: `norm_id` folds case and the dash, so an implementation writing `# bg0623:` resolves and passes unless the test asks non-canonically - not the whole stem, and not the id as the caller spelled it. `artifact.py`:1906 reads `extract_record_id(path.stem) or path.stem`, so the obvious repair composes the heading from the entire filename |
| AC1 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, compose the repaired heading from `artifact_id` - the id as the CALLER spelled it, in scope three lines above `file_id` - rather than from the file's own. The criterion names two wrongs and one mutant cannot stand for both | Given an artefact whose H1 does not parse, when `artifact.py retitle` is invoked as a SUBPROCESS with the id SPELLED DIFFERENTLY from the filename - `bg0623` or `BG-0623` against `BG0623-...md` - then it exits 0 and the repaired heading carries the FILE'S canonical id. The spelling mismatch is required by the criterion rather than left to the fixture: `norm_id` folds case and the dash, so an implementation writing `# bg0623:` resolves and passes unless the test asks non-canonically - not the whole stem, and not the id as the caller spelled it. `artifact.py`:1906 reads `extract_record_id(path.stem) or path.stem`, so the obvious repair composes the heading from the entire filename |
| AC2 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, replace the parse check with an unconditional rewrite of the H1 from the id and title, normalising a differently-formed heading behind the author's back | Given an artefact whose H1 parses but spells its id in a DIFFERENT FORM from the stem - the dashed form, or lowercase - when `retitle` runs, then that form is preserved. The form mismatch is required: `_H1_RETITLE_RE` keeps its first group verbatim and substitutes only the tail, so for an ordinary matching heading an unconditional rewrite is byte-identical and a control built that way pins nothing |
| AC3 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, skip the all-or-nothing validation when the H1 needs repair and write immediately | Given an artefact whose H1 needs repair AND whose index row is missing, when `retitle` runs, then NOTHING is written - the repair is not an escape hatch from the all-or-nothing validation the other surfaces are held to |
| AC4 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, detect the unparseable H1 and refuse with a clearer message instead of repairing it, which reads as the safer change and leaves the artefact untouchable by the tool that exists to touch it | Given an artefact whose H1 needs repair and whose other surfaces are all valid, when `retitle` runs, then the file IS written and every surface carries the new title. AC3's silence assertion is satisfied by a command that refuses outright, so without this row the repair need never happen |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, widen `_H1_LINE_RE` from one hash to `#{1,6}`, admitting `##` through `######` as replacement candidates | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, replace `_H1_LINE_RE` with `^\s*#`, readmitting a `#hashtag`, a four-space-indented code line and a seven-hash line as headings | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, remove the tilde alternative from `_FENCE_RE`, so a `~~~` block stops suspending markdown and its `#` comment is taken for the heading | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, narrow both runs in `_FENCE_RE` to exactly three characters, so the outer marker of a nested pair no longer matches and state flips on the inner one | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, replace the fence-closing test with a bare `if m:`, dropping the character comparison, the run-length comparison and the info-string check together | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, return the LAST matching heading instead of the first - which every earlier fixture survived, because each held exactly one heading | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, replace the `_front_matter_end(lines)` start index with a literal `0` | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, remove the HTML-comment tracking, so a `#` line inside a multi-line comment is taken for the heading - the shape `personas.md` and `RETRO0007` both open with | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, overwrite line 0 rather than inserting when no heading is found, which is the line-1 assumption this bug's first repair shipped | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |
| AC5 | in `.claude/skills/sdlc-studio/scripts/artifact.py`, set the insertion index to a literal `0` rather than `_front_matter_end(lines)` | Given ANY document shape, when `artifact.py retitle` repairs a broken H1, then NO line of the original is lost: the canonical H1 is present, and the count of a distinctive marker in the body is unchanged. The ONLY line that may be overwritten is an unambiguous LEVEL-ONE ATX heading outside every container that suspends markdown - a fenced block (matched on the fence character AND run length, so a four-backtick fence wrapping a three-backtick one stays closed), an HTML comment, and YAML front matter; every other shape, INCLUDING a `##` in a document carrying no H1, gets an INSERT placed after any front matter. This row states the INVARIANT, not a list of shapes, because enumerating shapes is precisely what failed three times: each repair fixed the shapes its criterion named and destroyed the next one along - a fenced `#` comment, a `#hashtag`, an indented code line, `## Summary` (which every bug artefact `_render` writes), a `#` inside an HTML comment, a `#` inside front matter. The asymmetry is the design: a shape the finder does not understand must cost a duplicate heading a human can see, never a deleted line the command reports success over |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-08-26 | sdlc-studio | Filed |
