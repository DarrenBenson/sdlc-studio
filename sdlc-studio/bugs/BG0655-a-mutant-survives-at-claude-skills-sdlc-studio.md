# BG0655: the survivor filer reads a WITHDRAWN row as a live survivor, so a close files a High bug for a mutant the ledger says is dead

> **Status:** Fixed
> **Mutation-survivor-run:** RUN-01M1YK70
> **Mutation-survivor:** US0818-4124f2e32130f7e4
> **Severity:** Medium
> **Points:** 2
> **Verification depth:** functional (authored at plan time as the tier this unit is driven to; the derived half is written by `verify_ac.py depth --write` at delivery, never by hand)
> **Affects:** .claude/skills/sdlc-studio/scripts/transition.py, .claude/skills/sdlc-studio/scripts/tests/test_transition.py
> **Created:** 2026-09-08
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`transition.py` `_survivor_records` selects a ledger row on its verdict alone - `mu.get("verdict") == "survived"` - with no test for the `withdrawn` mark every other reader applies. A row that was recorded survived, withdrawn with a reason and then re-registered killed is therefore read as a live survivor, and the close files a High bug naming a mutant the ledger says is dead.

This artefact is that false positive, kept and retitled rather than deleted: the run's close filed it against US0818 AC2 r4 at `mutation.py:2173`, where the ledger holds the survived row withdrawn and the killed row live. A false High is not harmless - the v5.1 bar is zero open High, so an over-reporting filer holds a release on evidence that does not exist.

## Steps to Reproduce

1. Take a unit whose ledger row was recorded `survived`, withdrawn with a reason, and re-registered `killed` on the same key - US0818 AC2 r4 on `mutation.py` is one on this tree today.
2. Drive that unit to a terminal status: `transition.py set US0818 Done`.
3. Read the bug the transition files: it names the withdrawn row as a surviving mutant, at High severity.
4. Read `_survivor_records` in `transition.py`: it selects on `mu.get("verdict") == "survived"` with no test for the `withdrawn` mark that `gate.py` and four other readers honour.

## Proposed Fix

Add the `withdrawn` test to the row predicate in `_survivor_records`, so a retracted reading is not offered as evidence. The mark is already written by `mutation.py retract` and already honoured everywhere else that reads a row; this is the one reader that does not.

## Acceptance Criteria

- [x] **AC1** Given a ledger row recorded `survived` and then WITHDRAWN with a reason, when the survivor filer runs for that unit at a terminal transition, then the row is not offered as a survivor and no bug is filed for it - a withdrawal is the ledger's record that the reading was retracted, and every other reader already honours it. The paired control: a row recorded `survived` and NOT withdrawn is still offered, so the filter narrows the selection rather than emptying it.
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_transition.py::SurvivorFilerTests::test_a_withdrawn_row_is_not_offered_as_a_survivor
  - **Verified:** yes (2026-09-08)
- [x] **AC2** Given a unit whose only surviving row is withdrawn and whose live row for the same key is `killed`, when the unit is driven to its terminal status THROUGH `transition.py set` as a subprocess, then no `Mutation-survivor-run` bug artefact is written to the fixture's bugs directory and the command says nothing survived - the wiring is the half a library test cannot see, and a filer that over-reports holds a release on evidence the ledger says does not exist.
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_transition.py::SurvivorFilingCLITests::test_no_bug_is_filed_when_the_live_row_for_the_key_is_killed
  - **Verified:** yes (2026-09-08)

- [x] **AC3** Given a unit whose ONLY recorded row is a withdrawn `survived` row, when the mutation-evidence GATE counts, then it reports that NO mutant was applied and names no survivor. The observable is the APPLIED count, not the survivor count: measured, a fix that only narrows the survivor predicate leaves this row counted as applied, so a unit with no live evidence at all satisfies the vacuous-zero check out of a reading the ledger has retracted
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_transition.py::SurvivorFilerTests::test_a_withdrawn_row_is_not_counted_by_the_evidence_gate
  - **Verified:** yes (2026-09-08)

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .claude/skills/sdlc-studio/scripts/transition.py, delete the `withdrawn` test from `_survivor_records`'s row predicate so the selection returns to verdict alone | Given a ledger row recorded `survived` and then WITHDRAWN with a reason, when the survivor filer runs for that unit at a terminal transition, then the row is not offered as a survivor and no bug is filed for it - a withdrawal is the ledger's record that the reading was retracted, and every other reader already honours it. The paired control: a row recorded `survived` and NOT withdrawn is still offered, so the filter narrows the selection rather than emptying it. |
| AC2 | in .claude/skills/sdlc-studio/scripts/transition.py, replace the filtered helper on the CLI's survivor-filing path with a direct unfiltered `ledger_entries` read, so the library filter stands while the shipped command still files | Given a unit whose only surviving row is withdrawn and whose live row for the same key is `killed`, when the unit is driven to its terminal status THROUGH `transition.py set` as a subprocess, then no `Mutation-survivor-run` bug artefact is written to the fixture's bugs directory and the command says nothing survived - the wiring is the half a library test cannot see, and a filer that over-reports holds a release on evidence the ledger says does not exist. |
| AC3 | in .claude/skills/sdlc-studio/scripts/transition.py, delete the withdrawn skip from `repair_mutation_gate`'s counting loop | Given a unit whose ONLY recorded row is a withdrawn `survived` row, when the mutation-evidence GATE counts, then it reports that NO mutant was applied and names no survivor. The observable is the APPLIED count, not the survivor count: measured, a fix that only narrows the survivor predicate leaves this row counted as applied, so a unit with no live evidence at all satisfies the vacuous-zero check out of a reading the ledger has retracted |

## Coverage Rulings

| File | Line | Hash | Reason | Author | Date |
| --- | --- | --- | --- | --- | --- |
| .claude/skills/sdlc-studio/scripts/transition.py | 713 | 4c14fce6d599cc39 | BG0633's annotate-vocabulary block, in a file both units changed against the same base ref. It is executed by BG0633's own criteria, which are green; BG0655 is about the withdrawn-survivor predicate and has no honest reason to drive the annotate verb | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/transition.py | 714 | 4c14fce6d599cc39 | BG0633's annotate-vocabulary block, in a file both units changed against the same base ref. It is executed by BG0633's own criteria, which are green; BG0655 is about the withdrawn-survivor predicate and has no honest reason to drive the annotate verb | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/transition.py | 718 | 4c14fce6d599cc39 | BG0633's annotate-vocabulary block, in a file both units changed against the same base ref. It is executed by BG0633's own criteria, which are green; BG0655 is about the withdrawn-survivor predicate and has no honest reason to drive the annotate verb | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/transition.py | 719 | 4c14fce6d599cc39 | BG0633's annotate-vocabulary block, in a file both units changed against the same base ref. It is executed by BG0633's own criteria, which are green; BG0655 is about the withdrawn-survivor predicate and has no honest reason to drive the annotate verb | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6253 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6254 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6255 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6256 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6258 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6266 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6267 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6268 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6269 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6270 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6272 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6273 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6275 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6285 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6286 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6287 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6288 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6289 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6291 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6292 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6293 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6294 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6303 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6304 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6305 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6306 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6308 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6309 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6310 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6312 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6325 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6326 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6327 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6328 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6329 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6331 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6332 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6333 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6334 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6335 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6336 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6338 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6339 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6340 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6341 | 94538dd441d6448a | BG0633's `AnnotateSeverityTests` class, in a test module both units changed against the same base ref, so the diff attributes its added lines to each of them. It is executed by BG0633's own criteria, which are green. BG0655 is about the withdrawn-survivor predicate and reaching these lines would be BG0633's tests written a second time | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/transition.py | 713 | d0e40839ed71a83b | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/transition.py | 714 | d0e40839ed71a83b | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/transition.py | 718 | d0e40839ed71a83b | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/transition.py | 719 | d0e40839ed71a83b | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/transition.py | 1294 | d0e40839ed71a83b | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 1957 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 1958 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 1959 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 1960 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 1961 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6267 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6268 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6269 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6270 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6272 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6280 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6281 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6282 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6283 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6284 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6286 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6287 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6289 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6299 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6300 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6301 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6302 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6303 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6305 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6306 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6307 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6308 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6317 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6318 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6319 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6320 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6322 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6323 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6324 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6326 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6339 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6340 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6341 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6342 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6343 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6345 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6346 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6347 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6348 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6349 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6350 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6352 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6353 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6354 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |
| .claude/skills/sdlc-studio/scripts/tests/test_transition.py | 6355 | 7589b2f50cd266f5 | BG0630, BG0633 and BG0655 changed this module against the same base ref alongside US0822's one remedy string, so the diff attributes their lines to it too | Claude Opus 5 | 2026-09-10 |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-08 | sdlc-studio | Filed |
| 2026-09-08 | Claude Fable 5.1 | Delivered, and the fix reached a SECOND reader the plan did not name. `repair_mutation_gate` counts a unit's rows on the verdict alone in its own loop, so a withdrawn row was still a survivor there and still counted as applied. Fixing only the filer would have left the gate refusing the transition for evidence the ledger says does not exist. One predicate now serves both, and AC3 was added with its own mutant rather than the extra work shipping unpinned |
| 2026-09-08 | Claude Fable 5.1 | Delivery review, round one: all three seats APPROVE. Five non-blocking findings are answered on the repair record rather than in code. Two are corrected here: the claim that the original filing was a HIGH is not settleable from the committed record, since this artefact's first committed version already reads Medium, so it is withdrawn from the changelog and the docstring rather than defended; and the docstring named the release-notes gate where the per-commit lane is the disclosure test module. The engineering seat also measured that the predicate's withdrawn half is unreachable at the gate loop, which skips first - kept as one predicate so a later reader cannot restore the verdict-only reading, and recorded as defensive rather than claimed as load-bearing |
