# BG0675: an author-declared Points value sets a unit's review tier: route.estimate's spec subscore reads Points, which D0150 rules out of review depth

> **Status:** In Progress
> **Verification depth:** functional [[derived: criteria 4; plan rows 13; executed 13; killed 13; survived 0; lines ruled 2; not-run 0; entry point 0 of 4 criteria through the shipped CLI, 4 in-process | fp 6c9ba025a2f5 ]]
> **Severity:** Medium
> **Points:** 3
> **Affects:** .claude/skills/sdlc-studio/scripts/route.py, .claude/skills/sdlc-studio/scripts/tests/test_route.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py, tools/tests/test_tier_shift_record.py, sdlc-studio/bugs/BG0675-an-author-declared-points-value-sets-a-unit.md
> **Created:** 2026-09-15
> **Created-by:** sdlc-studio file
> **Raised-by:** backlog sweep 2026-09-15; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

route.estimate weights `spec` at 0.25 (route.py:54) and derives it from the unit's `Points` (route.py:103-147). The band feeds `critic.tier_for` via `plan_review._difficulty_band`, which sets the review depth a brief asks for, and `plan_review`'s difficulty trigger. D0150: 'No author-declared field may gate review depth ... Points, Severity, Size, or any field like them'. Measured on one throwaway unit (two Affects files, one AC), changing ONLY Points from 1 to 8 moves the score 30 -> 43, the band low -> medium and `critic.tier_for` light -> full. CR0549, which diagnosed the estimator, is Superseded; the Blocked US0677/US0678/US0684 carry diff-basis residue but none removes Points from the band in force. Found by the 2026-09-15 backlog sweep.

## Steps to Reproduce

1. In a throwaway workspace, one story with Affects of two files and one AC, Points 1: `route.estimate` gives 30/low and `critic.tier_for` gives light.
2. Change only the Points field to 8: 43/medium and full.

## Proposed Fix

Remove the author-declared input from the subscore wherever the band decides review depth, deriving `spec` from a signal the author does not declare (or from the diff once a diff basis exists). Fixed in code, not ruled (operator ruling, 2026-09-15 sprint planning). Review tiers will shift for existing units: the delivery reports how many units change tier, measured over the corpus before and after.

## Acceptance Criteria

- [ ] **AC1** A unit whose Affects are two existing non-code files and which carries one AC reads `light` through `critic.tier_for` at Points 1 and at Points 8 - each tier asserted equal to `light`, not merely equal to each other; and the same unit with six ACs at Points 1 reads `full`, the in-test control that the fixture reaches the deeper tier through the criterion count the fix keeps
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_critic.py::TierIgnoresDeclaredFieldsTests::test_points_alone_does_not_move_the_tier
  - **Verified:** yes (2026-09-15)
- [ ] **AC2** route.estimate's difficulty score for one unit is identical at Points 1, at Points 8 and with no size field at all, under BOTH the canonical `Points` spelling and the legacy `Story Points` spelling (the fixture is written per spelling, not through `_story`, which writes only the legacy one); the same holds for a zero-AC variant of the unit, carrying no criterion at all, whose score and band are identical at Points 1, at Points 8 and with no size field - the case where no AC is counted and a build that falls back to Points would still read it; and moving the same unit from one AC to six does change the score, the control that the fixture's score can move
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_route.py::DeclaredFieldsDoNotScoreTests::test_points_alone_does_not_move_the_score
  - **Verified:** yes (2026-09-15)
- [ ] **AC3** A unit that declares Points 3 reads a strictly higher route.estimate `spec` subscore with six ACs than with one - the paired control for the signal the fix keeps, run on a Points-bearing fixture because every real unit carries Points (Affects breadth is author-declared too, and is US0679's question, not this bug's)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_route.py::DeclaredFieldsDoNotScoreTests::test_ac_count_still_moves_the_spec_subscore
  - **Verified:** yes (2026-09-15)
- [ ] **AC4** This bug carries a `## Tier Shift` section with one line `- **Tier shift:** K of M units change tier (A light to full, B full to light); before L0 light / F0 full, after L1 light / F1 full; measured at <sha>`, the delivery's measurement of `critic.tier_for`'s tier (the `route.estimate` band through `critic.BAND_TIER`) for every `US*` in `sdlc-studio/stories` and `BG*` in `sdlc-studio/bugs` at `<sha>`, before the fix and after it. The test holds the frame, never the live corpus (BG0234: a figure recomputed against a growing corpus un-verifies itself): `<sha>` is an ancestor of HEAD whose `route.py` blob still carries the size-to-spec expression `story_points / max_points` (the fix removes it, so a post-fix commit fails); M equals the count of `sdlc-studio/stories/US*.md` and `sdlc-studio/bugs/BG*.md` in `git ls-tree -r` at `<sha>` (1,499 at 3f73ab64, archive and `_index.md` excluded); A + B = K; K is at least 1, since a zero is what two passes over one estimator record; L0 + F0 = L1 + F1 = M; L1 = L0 - A + B. K itself is not recomputed - a corpus pass takes about nine minutes (1,499 units, 518 s measured) - and the reviewer reads it against the recorded counts. The test's own checker is shown refusing a sample line whose split does not sum, the control that the arithmetic check can fail. In a shallow clone (`git rev-parse --is-shallow-repository` prints true, as the Lint job's default checkout does) the test skips only its sha-bound checks - ancestry, the blob expression and the ls-tree count - naming that reason, while the section's presence, the K floor and the split arithmetic run in every clone; in a full clone an unreachable `<sha>` fails, so it binds at the pre-push boundary
  - **Verify:** pytest tools/tests/test_tier_shift_record.py::TierShiftRecordTests::test_the_bug_records_the_corpus_tier_shift_at_a_named_commit
  - **Verified:** yes (2026-09-15)

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .claude/skills/sdlc-studio/scripts/route.py `estimate`, gate the Points removal behind a new keyword that defaults to still counting Points, so `plan_review._difficulty_band`'s unchanged call - and so `critic.tier_for` - still scores Points | A unit whose Affects are two existing non-code files and which carries one AC reads `light` through `critic.tier_for` at Points 1 and at Points 8 - each tier asserted equal to `light`, not merely equal to each other; and the same unit with six ACs at Points 1 reads `full`, the in-test control that the fixture reaches the deeper tier through the criterion count the fix keeps |
| AC1 | in .claude/skills/sdlc-studio/scripts/route.py `estimate`, when a size value was parsed call `sub("spec", None)` instead of reading `ac_count`, so the 0.5 missing default lifts the score to 40 and the band to medium | A unit whose Affects are two existing non-code files and which carries one AC reads `light` through `critic.tier_for` at Points 1 and at Points 8 - each tier asserted equal to `light`, not merely equal to each other; and the same unit with six ACs at Points 1 reads `full`, the in-test control that the fixture reaches the deeper tier through the criterion count the fix keeps |
| AC1 | in .claude/skills/sdlc-studio/scripts/route.py `estimate`, set `sub("spec", 0.0)` whenever a size value was parsed, so the criterion count is silenced for every sized unit and six criteria score 28 | A unit whose Affects are two existing non-code files and which carries one AC reads `light` through `critic.tier_for` at Points 1 and at Points 8 - each tier asserted equal to `light`, not merely equal to each other; and the same unit with six ACs at Points 1 reads `full`, the in-test control that the fixture reaches the deeper tier through the criterion count the fix keeps |
| AC2 | in .claude/skills/sdlc-studio/scripts/route.py `estimate`, drop only the `sdlc_md.POINTS_FIELD` read and keep the `Story Points` fallback feeding `sub("spec", ...)`, so a unit on the legacy spelling still moves the score | route.estimate's difficulty score for one unit is identical at Points 1, at Points 8 and with no size field at all, under BOTH the canonical `Points` spelling and the legacy `Story Points` spelling (the fixture is written per spelling, not through `_story`, which writes only the legacy one); the same holds for a zero-AC variant of the unit, carrying no criterion at all, whose score and band are identical at Points 1, at Points 8 and with no size field - the case where no AC is counted and a build that falls back to Points would still read it; and moving the same unit from one AC to six does change the score, the control that the fixture's score can move |
| AC2 | in .claude/skills/sdlc-studio/scripts/route.py `estimate`, remove the `"Story Points"` fallback from `points_raw` but keep `extract_field(text, sdlc_md.POINTS_FIELD)` feeding `story_points` and so `sub("spec", ...)` | route.estimate's difficulty score for one unit is identical at Points 1, at Points 8 and with no size field at all, under BOTH the canonical `Points` spelling and the legacy `Story Points` spelling (the fixture is written per spelling, not through `_story`, which writes only the legacy one); the same holds for a zero-AC variant of the unit, carrying no criterion at all, whose score and band are identical at Points 1, at Points 8 and with no size field - the case where no AC is counted and a build that falls back to Points would still read it; and moving the same unit from one AC to six does change the score, the control that the fixture's score can move |
| AC2 | in .claude/skills/sdlc-studio/scripts/route.py `estimate`, swap the branch order so `ac_count` is read first and `story_points / max_points` is the fallback when no AC is counted | route.estimate's difficulty score for one unit is identical at Points 1, at Points 8 and with no size field at all, under BOTH the canonical `Points` spelling and the legacy `Story Points` spelling (the fixture is written per spelling, not through `_story`, which writes only the legacy one); the same holds for a zero-AC variant of the unit, carrying no criterion at all, whose score and band are identical at Points 1, at Points 8 and with no size field - the case where no AC is counted and a build that falls back to Points would still read it; and moving the same unit from one AC to six does change the score, the control that the fixture's score can move |
| AC3 | in .claude/skills/sdlc-studio/scripts/route.py estimate, drop the ac_count read from the spec subscore along with Points, so spec scores nothing | A unit that declares Points 3 reads a strictly higher route.estimate `spec` subscore with six ACs than with one - the paired control for the signal the fix keeps, run on a Points-bearing fixture because every real unit carries Points (Affects breadth is author-declared too, and is US0679's question, not this bug's) |
| AC3 | in .claude/skills/sdlc-studio/scripts/route.py `estimate`, replace the `story_points / max_points` branch with `sub("spec", 0.0)`, so a sized unit reads zero whatever its `ac_count` | A unit that declares Points 3 reads a strictly higher route.estimate `spec` subscore with six ACs than with one - the paired control for the signal the fix keeps, run on a Points-bearing fixture because every real unit carries Points (Affects breadth is author-declared too, and is US0679's question, not this bug's) |
| AC4 | in sdlc-studio/bugs/BG0675-an-author-declared-points-value-sets-a-unit.md, count only the story directory for the population figure, leaving every bug file out of it | This bug carries a `## Tier Shift` section with one line `- **Tier shift:** K of M units change tier (A light to full, B full to light); before L0 light / F0 full, after L1 light / F1 full; measured at <sha>`, the delivery's measurement of `critic.tier_for`'s tier (the `route.estimate` band through `critic.BAND_TIER`) for every `US*` in `sdlc-studio/stories` and `BG*` in `sdlc-studio/bugs` at `<sha>`, before the fix and after it. The test holds the frame, never the live corpus (BG0234: a figure recomputed against a growing corpus un-verifies itself): `<sha>` is an ancestor of HEAD whose `route.py` blob still carries the size-to-spec expression `story_points / max_points` (the fix removes it, so a post-fix commit fails); M equals the count of `sdlc-studio/stories/US*.md` and `sdlc-studio/bugs/BG*.md` in `git ls-tree -r` at `<sha>` (1,499 at 3f73ab64, archive and `_index.md` excluded); A + B = K; K is at least 1, since a zero is what two passes over one estimator record; L0 + F0 = L1 + F1 = M; L1 = L0 - A + B. K itself is not recomputed - a corpus pass takes about nine minutes (1,499 units, 518 s measured) - and the reviewer reads it against the recorded counts. The test's own checker is shown refusing a sample line whose split does not sum, the control that the arithmetic check can fail. In a shallow clone (`git rev-parse --is-shallow-repository` prints true, as the Lint job's default checkout does) the test skips only its sha-bound checks - ancestry, the blob expression and the ls-tree count - naming that reason, while the section's presence, the K floor and the split arithmetic run in every clone; in a full clone an unreachable `<sha>` fails, so it binds at the pre-push boundary |
| AC4 | in sdlc-studio/bugs/BG0675-an-author-declared-points-value-sets-a-unit.md, name a revision taken after the fix, whose estimator no longer parses the size field, so both passes measure one estimator and report nothing moved | This bug carries a `## Tier Shift` section with one line `- **Tier shift:** K of M units change tier (A light to full, B full to light); before L0 light / F0 full, after L1 light / F1 full; measured at <sha>`, the delivery's measurement of `critic.tier_for`'s tier (the `route.estimate` band through `critic.BAND_TIER`) for every `US*` in `sdlc-studio/stories` and `BG*` in `sdlc-studio/bugs` at `<sha>`, before the fix and after it. The test holds the frame, never the live corpus (BG0234: a figure recomputed against a growing corpus un-verifies itself): `<sha>` is an ancestor of HEAD whose `route.py` blob still carries the size-to-spec expression `story_points / max_points` (the fix removes it, so a post-fix commit fails); M equals the count of `sdlc-studio/stories/US*.md` and `sdlc-studio/bugs/BG*.md` in `git ls-tree -r` at `<sha>` (1,499 at 3f73ab64, archive and `_index.md` excluded); A + B = K; K is at least 1, since a zero is what two passes over one estimator record; L0 + F0 = L1 + F1 = M; L1 = L0 - A + B. K itself is not recomputed - a corpus pass takes about nine minutes (1,499 units, 518 s measured) - and the reviewer reads it against the recorded counts. The test's own checker is shown refusing a sample line whose split does not sum, the control that the arithmetic check can fail. In a shallow clone (`git rev-parse --is-shallow-repository` prints true, as the Lint job's default checkout does) the test skips only its sha-bound checks - ancestry, the blob expression and the ls-tree count - naming that reason, while the section's presence, the K floor and the split arithmetic run in every clone; in a full clone an unreachable `<sha>` fails, so it binds at the pre-push boundary |
| AC4 | in sdlc-studio/bugs/BG0675-an-author-declared-points-value-sets-a-unit.md, record 0 moved units, taken by running the second pass too early, on the old estimator | This bug carries a `## Tier Shift` section with one line `- **Tier shift:** K of M units change tier (A light to full, B full to light); before L0 light / F0 full, after L1 light / F1 full; measured at <sha>`, the delivery's measurement of `critic.tier_for`'s tier (the `route.estimate` band through `critic.BAND_TIER`) for every `US*` in `sdlc-studio/stories` and `BG*` in `sdlc-studio/bugs` at `<sha>`, before the fix and after it. The test holds the frame, never the live corpus (BG0234: a figure recomputed against a growing corpus un-verifies itself): `<sha>` is an ancestor of HEAD whose `route.py` blob still carries the size-to-spec expression `story_points / max_points` (the fix removes it, so a post-fix commit fails); M equals the count of `sdlc-studio/stories/US*.md` and `sdlc-studio/bugs/BG*.md` in `git ls-tree -r` at `<sha>` (1,499 at 3f73ab64, archive and `_index.md` excluded); A + B = K; K is at least 1, since a zero is what two passes over one estimator record; L0 + F0 = L1 + F1 = M; L1 = L0 - A + B. K itself is not recomputed - a corpus pass takes about nine minutes (1,499 units, 518 s measured) - and the reviewer reads it against the recorded counts. The test's own checker is shown refusing a sample line whose split does not sum, the control that the arithmetic check can fail. In a shallow clone (`git rev-parse --is-shallow-repository` prints true, as the Lint job's default checkout does) the test skips only its sha-bound checks - ancestry, the blob expression and the ls-tree count - naming that reason, while the section's presence, the K floor and the split arithmetic run in every clone; in a full clone an unreachable `<sha>` fails, so it binds at the pre-push boundary |
| AC4 | in sdlc-studio/bugs/BG0675-an-author-declared-points-value-sets-a-unit.md, change the after-fix light figure by hand without adjusting either direction count, so the before and after totals no longer reconcile | This bug carries a `## Tier Shift` section with one line `- **Tier shift:** K of M units change tier (A light to full, B full to light); before L0 light / F0 full, after L1 light / F1 full; measured at <sha>`, the delivery's measurement of `critic.tier_for`'s tier (the `route.estimate` band through `critic.BAND_TIER`) for every `US*` in `sdlc-studio/stories` and `BG*` in `sdlc-studio/bugs` at `<sha>`, before the fix and after it. The test holds the frame, never the live corpus (BG0234: a figure recomputed against a growing corpus un-verifies itself): `<sha>` is an ancestor of HEAD whose `route.py` blob still carries the size-to-spec expression `story_points / max_points` (the fix removes it, so a post-fix commit fails); M equals the count of `sdlc-studio/stories/US*.md` and `sdlc-studio/bugs/BG*.md` in `git ls-tree -r` at `<sha>` (1,499 at 3f73ab64, archive and `_index.md` excluded); A + B = K; K is at least 1, since a zero is what two passes over one estimator record; L0 + F0 = L1 + F1 = M; L1 = L0 - A + B. K itself is not recomputed - a corpus pass takes about nine minutes (1,499 units, 518 s measured) - and the reviewer reads it against the recorded counts. The test's own checker is shown refusing a sample line whose split does not sum, the control that the arithmetic check can fail. In a shallow clone (`git rev-parse --is-shallow-repository` prints true, as the Lint job's default checkout does) the test skips only its sha-bound checks - ancestry, the blob expression and the ls-tree count - naming that reason, while the section's presence, the K floor and the split arithmetic run in every clone; in a full clone an unreachable `<sha>` fails, so it binds at the pre-push boundary |
| AC4 | in sdlc-studio/bugs/BG0675-an-author-declared-points-value-sets-a-unit.md, delete the whole recorded section and leave the figure only in changelog.d/BG0675.md | This bug carries a `## Tier Shift` section with one line `- **Tier shift:** K of M units change tier (A light to full, B full to light); before L0 light / F0 full, after L1 light / F1 full; measured at <sha>`, the delivery's measurement of `critic.tier_for`'s tier (the `route.estimate` band through `critic.BAND_TIER`) for every `US*` in `sdlc-studio/stories` and `BG*` in `sdlc-studio/bugs` at `<sha>`, before the fix and after it. The test holds the frame, never the live corpus (BG0234: a figure recomputed against a growing corpus un-verifies itself): `<sha>` is an ancestor of HEAD whose `route.py` blob still carries the size-to-spec expression `story_points / max_points` (the fix removes it, so a post-fix commit fails); M equals the count of `sdlc-studio/stories/US*.md` and `sdlc-studio/bugs/BG*.md` in `git ls-tree -r` at `<sha>` (1,499 at 3f73ab64, archive and `_index.md` excluded); A + B = K; K is at least 1, since a zero is what two passes over one estimator record; L0 + F0 = L1 + F1 = M; L1 = L0 - A + B. K itself is not recomputed - a corpus pass takes about nine minutes (1,499 units, 518 s measured) - and the reviewer reads it against the recorded counts. The test's own checker is shown refusing a sample line whose split does not sum, the control that the arithmetic check can fail. In a shallow clone (`git rev-parse --is-shallow-repository` prints true, as the Lint job's default checkout does) the test skips only its sha-bound checks - ancestry, the blob expression and the ls-tree count - naming that reason, while the section's presence, the K floor and the split arithmetic run in every clone; in a full clone an unreachable `<sha>` fails, so it binds at the pre-push boundary |

## Tier Shift

- **Tier shift:** 37 of 1501 units change tier (33 light to full, 4 full to light); before 193 light / 1308 full, after 164 light / 1337 full; measured at 6fd766cc

Method: `critic.tier_for`'s chain (`plan_review._difficulty_band`, then `critic.BAND_TIER`) over every `sdlc-studio/stories/US*.md` and `sdlc-studio/bugs/BG*.md` in `git ls-tree -r 6fd766cc` (823 stories, 678 bugs), read from a `git archive` export of that commit with churn taken from `git log 6fd766cc`. The before-pass imported 6fd766cc's own `route.py`; the after-pass imported the fixed `route.py` with every other script and the corpus tree byte-identical. 205 units changed band and 37 of those crossed the light/full line. A 41-unit stride of the before-pass matched `critic.tier_for` run live in a checkout at 6fd766cc, 41 of 41.

## Coverage Rulings

| File | Line | Hash | Reason | Author | Date |
| --- | --- | --- | --- | --- | --- |
| tools/tests/test_tier_shift_record.py | 144 | 42ba2f649f5fa391 | A skip: the shallow-clone branch runs only where git rev-parse --is-shallow-repository prints true (the Lint job's default checkout). This clone is full, so the sha-bound checks ran instead. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| tools/tests/test_tier_shift_record.py | 190 | 42ba2f649f5fa391 | Script entry point under `if __name__ == '__main__'`; pytest imports the module so it never runs there. Exercised by hand: python3 tools/tests/test_tier_shift_record.py ran 1 test, OK, exit 0 on 2026-09-15. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-15 | backlog sweep 2026-09-15 | Filed |
| 2026-09-15 | sprint planning 2026-09-15 | Operator ruling at sprint planning: FIX in code, not by ruling D0150 narrower - a ruling would leave the goal's 'every defect fixed' false. The delivery reports the tier shift over the corpus. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan r1 REJECT repaired. AC1 now asserts light at Points 1 and 8 (not only equality) on a two-markdown-Affects one-AC fixture, with the six-AC control reading full; rows added for the spec-missing-when-sized mutant (40/full at both) and the spec-0.0-when-sized mutant (control reads 28/light). AC2 varies both the Points and the Story Points spelling and compares against no size field, with an AC-count control; mirror row added (keep POINTS_FIELD, drop the Story Points fallback). AC3 fixture now carries Points 3 (34 at both AC counts on HEAD, so red now), selector renamed to test_ac_count_still_moves_the_spec_subscore, and the spec-0.0-when-sized row added. AC4 added for the corpus tier-shift report: a Tier shift line in this bug checked at a named commit (population by git ls-tree, pre-fix estimator by blob, split arithmetic), K not recomputed because a corpus pass measured 518 s over 1,499 units; skips only in a shallow clone. Affects gains tools/tests/test_tier_shift_record.py and this bug file. Every fixture figure measured against HEAD and each mutant in a scratch copy of route.py. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r2 REJECT answered (one BLOCKING). AC2 gains a zero-AC variant of the unit whose score and band must be identical at Points 1, Points 8 and no size field, and a new row swapping estimate's branch order so ac_count is read first and Points is the fallback when no AC is counted - the minimal-diff build that matched the fix on every earlier fixture. Cheap issues taken in AC4: K must be at least 1 (new row: 0 moved units recorded by running the second pass on the old estimator), and a shallow clone now skips only the sha-bound checks (ancestry, blob expression, ls-tree count) while the section, the K floor and the split arithmetic run everywhere. |
