# BG0672: critic record accepts a --brief fingerprint no brief produced, recording the row as briefed with only a stderr note

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 5; plan rows 12; executed 12; killed 12; survived 0; lines ruled 1; not-run 0; entry point 5 of 5 criteria through the shipped CLI, 0 in-process | fp 2e834ee654f5 ]]
> **Severity:** Medium
> **Points:** 5
> **Affects:** .claude/skills/sdlc-studio/scripts/critic.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py
> **Created:** 2026-09-15
> **Created-by:** sdlc-studio file
> **Raised-by:** backlog sweep 2026-09-15; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`critic.py record --brief <12 hex>` refuses an ABSENT fingerprint, but accepts any 12-hex string: a value no brief rendered prints a stderr NOTE (critic.py ~4220-4226, 'matches no brief this repo can currently produce') and the verdict row is written with that fingerprint in its Brief column, indistinguishable from a briefed one. AGENTS.md's refusal table says `critic record` refuses a verdict carrying no brief provenance; that holds for an absent value and not for an invented one. The note exists rather than a refusal because the matcher has false negatives (CR0511 findings #14 and #46: a tier-mismatched or first-round light brief matches no re-render), so refusing today would refuse honest verdicts. Reproduced in a throwaway fixture by the 2026-09-15 backlog sweep.

## Steps to Reproduce

1. In a throwaway workspace with one story, run `critic.py record --unit <id> --verdict APPROVE --reviewer r --author a --tier full --brief 0123456789ab`.
2. Observe exit 0, a stderr note that the fingerprint matches no brief, and a ledger row carrying `0123456789ab` in its Brief column with nothing marking it unmatched.

## Proposed Fix

Record the match result on the row itself (mark an unmatched fingerprint in the ledger) so every reader that counts a verdict as briefed can tell the difference, and have those readers not count an unmatched row as briefed. Once CR0511 #14/#46 close the matcher's false negatives, turn the note into a refusal.

## Acceptance Criteria

What the marker means: the fingerprint matches no brief this repo can CURRENTLY produce for that unit and phase. It does not claim fabrication - a fingerprint gone stale because the unit changed after briefing is marked like an invented one, and re-briefing clears it. It is written by `critic.py record`, the one writer that takes a fingerprint from its caller, as the word `unmatched` beside the fingerprint in the row's Brief cell; an unmarked cell holds the fingerprint alone. Where the question cannot be asked - no seat cards - the matcher answers unknown and nothing is marked. The two honest paths that match nothing at HEAD (probed 2026-09-15: an explicit `--tier light` on a unit deriving full, and `--brief-file` holding a saved rejoinder brief, delivery or plan-review) are made to match - the matcher tries an explicit `--tier`, and a `--brief-file` carrying the rejoinder mark is hashed the way `rejoinder_fingerprint` hashes it for the recorded phase - so neither is marked. A `--brief-file` WITHOUT the rejoinder mark stays hashed with `brief_fingerprint`: a saved first-round brief already records its footer's value at HEAD (probed 2026-09-15), and the rejoinder branch must not reach it. Fixtures are workspaces carrying the shipped seat cards unless a criterion says otherwise.

- [ ] **AC1** `critic.py record --brief 0123456789ab`, a value no brief rendered, writes a delivery row whose Brief cell holds `0123456789ab` and `unmatched`
  - **Verify:** manual - retired by US0923: `critic.py record` stores a `--brief` value as given and no longer matches it against a brief or writes the `unmatched` mark; `_brief_key` still reads a historical marked row as unbriefed
  - **Verified:** manual (2026-09-25) - retired, superseded by US0923
- [ ] **AC2** In one `critic.py record --unit A --unit B` invocation carrying the fingerprint `critic.py brief` printed for A, B's row is marked and A's is not - the marker is decided per unit
  - **Verify:** manual - retired by US0923: `critic.py record` stores a `--brief` value as given and no longer matches it against a brief or writes the `unmatched` mark; `_brief_key` still reads a historical marked row as unbriefed
  - **Verified:** manual (2026-09-25) - retired, superseded by US0923
- [ ] **AC3** Neither reader that counts a verdict as briefed counts a marked row: a panel sign-off refuses a unit whose governing verdict is marked, naming the missing brief provenance, and signs its twin whose verdict matched; and `verdict_for` returns a marked REJECT followed by a marked APPROVE carrying the same fingerprint, where the same pair with a matched fingerprint returns the APPROVE (the `_brief_key` observable)
  - **Verify:** manual - retired by US0923: `critic.py record` stores a `--brief` value as given and no longer matches it against a brief or writes the `unmatched` mark; `_brief_key` still reads a historical marked row as unbriefed
  - **Verified:** manual (2026-09-25) - retired, superseded by US0923
- [ ] **AC4** Each honest path writes a Brief cell equal byte for byte to the fingerprint the brief verb printed, unmarked: no `--tier` on a unit deriving light; `brief --tier light` then `record --tier light --tier-explicit` on a unit deriving full; `record --brief-file` of a saved first-round delivery `brief`; and `record --brief-file` of a saved delivery `brief --rejoinder`. Beside them an invented value recorded in the same workspace is marked, the positive control
  - **Verify:** manual - retired by US0923: `critic.py record` stores a `--brief` value as given and no longer matches it against a brief or writes the `unmatched` mark; `_brief_key` still reads a historical marked row as unbriefed
  - **Verified:** manual (2026-09-25) - retired, superseded by US0923
- [ ] **AC5** In a workspace with no seat cards, `critic.py record --brief 0123456789ab` writes its row unmarked - the matcher cannot ask, and unknown is not unmatched; the same value is marked in AC1's seat-carded workspace, the positive control
  - **Verify:** manual - retired by US0923: `critic.py record` stores a `--brief` value as given and no longer matches it against a brief or writes the `unmatched` mark; `_brief_key` still reads a historical marked row as unbriefed
  - **Verified:** manual (2026-09-25) - retired, superseded by US0923

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`'s write(), replace the `seats is not None and not seats` test guarding the marker with `seats is None`, so an invented fingerprint in a workspace that has seat cards is written unmarked | `critic.py record --brief 0123456789ab`, a value no brief rendered, writes a delivery row whose Brief cell holds `0123456789ab` and `unmatched` |
| AC1 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`'s write(), skip the marker when the phase is plan-review, so the plan ledger's row is written unmarked | `critic.py record --brief 0123456789ab`, a value no brief rendered, writes a delivery row whose Brief cell holds `0123456789ab` and `unmatched` |
| AC2 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`, call `_seats_whose_brief_matches` once, for the first unit, before the write loop and apply its answer to every row | In one `critic.py record --unit A --unit B` invocation carrying the fingerprint `critic.py brief` printed for A, B's row is marked and A's is not - the marker is decided per unit |
| AC3 | in .claude/skills/sdlc-studio/scripts/critic.py record_signoff's panel interlock, keep the bare .strip(' -') test on the Brief cell, so a marked row still ratifies | Neither reader that counts a verdict as briefed counts a marked row: a panel sign-off refuses a unit whose governing verdict is marked, naming the missing brief provenance, and signs its twin whose verdict matched; and `verdict_for` returns a marked REJECT followed by a marked APPROVE carrying the same fingerprint, where the same pair with a matched fingerprint returns the APPROVE (the `_brief_key` observable) |
| AC3 | in .claude/skills/sdlc-studio/scripts/critic.py _brief_key, strip the unmatched marker before comparing, so a marked row still keys as briefed | Neither reader that counts a verdict as briefed counts a marked row: a panel sign-off refuses a unit whose governing verdict is marked, naming the missing brief provenance, and signs its twin whose verdict matched; and `verdict_for` returns a marked REJECT followed by a marked APPROVE carrying the same fingerprint, where the same pair with a matched fingerprint returns the APPROVE (the `_brief_key` observable) |
| AC4 | in .claude/skills/sdlc-studio/scripts/critic.py, decide the marker by re-rendering `brief()` at its default tier full for each seat instead of calling `_seats_whose_brief_matches`, so a fingerprint the brief verb printed at the unit's derived light tier is marked | Each honest path writes a Brief cell equal byte for byte to the fingerprint the brief verb printed, unmarked: no `--tier` on a unit deriving light; `brief --tier light` then `record --tier light --tier-explicit` on a unit deriving full; `record --brief-file` of a saved first-round delivery `brief`; and `record --brief-file` of a saved delivery `brief --rejoinder`. Beside them an invented value recorded in the same workspace is marked, the positive control |
| AC4 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`, keep passing no tier to `_seats_whose_brief_matches`, which goes on trying only full and the derived tier, so an explicit light brief on a full-deriving unit is marked | Each honest path writes a Brief cell equal byte for byte to the fingerprint the brief verb printed, unmarked: no `--tier` on a unit deriving light; `brief --tier light` then `record --tier light --tier-explicit` on a unit deriving full; `record --brief-file` of a saved first-round delivery `brief`; and `record --brief-file` of a saved delivery `brief --rejoinder`. Beside them an invented value recorded in the same workspace is marked, the positive control |
| AC4 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`, change the rejoinder-mark test to read the --brief value instead of the --brief-file text, so a saved rejoinder is hashed whole and marked | Each honest path writes a Brief cell equal byte for byte to the fingerprint the brief verb printed, unmarked: no `--tier` on a unit deriving light; `brief --tier light` then `record --tier light --tier-explicit` on a unit deriving full; `record --brief-file` of a saved first-round delivery `brief`; and `record --brief-file` of a saved delivery `brief --rejoinder`. Beside them an invented value recorded in the same workspace is marked, the positive control |
| AC4 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`, split a --brief-file at the rejoinder mark and hash the base with `brief_fingerprint`, dropping the phase tag, so the row carries the first-round value | Each honest path writes a Brief cell equal byte for byte to the fingerprint the brief verb printed, unmarked: no `--tier` on a unit deriving light; `brief --tier light` then `record --tier light --tier-explicit` on a unit deriving full; `record --brief-file` of a saved first-round delivery `brief`; and `record --brief-file` of a saved delivery `brief --rejoinder`. Beside them an invented value recorded in the same workspace is marked, the positive control |
| AC4 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`, call `rejoinder_fingerprint` on a --brief-file at its default delivery phase whatever --phase says, so a plan-review rejoinder is marked | Each honest path writes a Brief cell equal byte for byte to the fingerprint the brief verb printed, unmarked: no `--tier` on a unit deriving light; `brief --tier light` then `record --tier light --tier-explicit` on a unit deriving full; `record --brief-file` of a saved first-round delivery `brief`; and `record --brief-file` of a saved delivery `brief --rejoinder`. Beside them an invented value recorded in the same workspace is marked, the positive control |
| AC4 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`, hash every --brief-file with `rejoinder_fingerprint` for the recorded phase whether or not it carries the rejoinder mark, so a saved first-round brief records a value no footer printed and the matcher, re-rendering the same text, still leaves it unmarked | Each honest path writes a Brief cell equal byte for byte to the fingerprint the brief verb printed, unmarked: no `--tier` on a unit deriving light; `brief --tier light` then `record --tier light --tier-explicit` on a unit deriving full; `record --brief-file` of a saved first-round delivery `brief`; and `record --brief-file` of a saved delivery `brief --rejoinder`. Beside them an invented value recorded in the same workspace is marked, the positive control |
| AC5 | in .claude/skills/sdlc-studio/scripts/critic.py `cmd_record`'s write(), change the marker test to `not seats`, so the None a workspace without seat cards returns is marked | In a workspace with no seat cards, `critic.py record --brief 0123456789ab` writes its row unmarked - the matcher cannot ask, and unknown is not unmatched; the same value is marked in AC1's seat-carded workspace, the positive control |

## Coverage Rulings

| File | Line | Hash | Reason | Author | Date |
| --- | --- | --- | --- | --- | --- |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6599 | 9710ddd81b1c0402 | withdrawn 2026-09-15: Withdrawn by its author to correct a factual slip in the reason text (the binary or directory it named was misstated); the same line is re-ruled at once with an accurate reason. (was: A skip: fires only when the shipped personas/seats directory holds no seat cards. The skill ships three seat cards, so the test always finds them and runs.) | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6599 | 9710ddd81b1c0402 | A skip: fires only when sdlc-studio/personas/seats holds no seat cards. This workspace carries three (engineering, product, qa), so the test finds them and runs. | Claude Opus 5 (mutation-registration subagent) | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6906 | d9a28d09aaa9ed32 | skip when the shipped seat cards are absent; they ship with the skill | sdlc-studio | 2026-09-15 |
| .claude/skills/sdlc-studio/scripts/tests/test_critic.py | 6982 | 5057f80808e19d85 | skip when the shipped seat cards are absent; the three seat cards ship with the skill in this workspace, so the test finds them and runs. Re-ruled at its new line: another batch unit's edit to the file voided the earlier ruling by whole-file hash (BG0706). | Claude Opus 5 (authoring session) | 2026-09-15 |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-15 | backlog sweep 2026-09-15 | Filed |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r1 REJECT answered. The marker is defined: it means the fingerprint matches no brief this repo can currently produce for the unit and phase - a stale fingerprint is marked like an invented one, re-briefing clears it - written by critic.py record (the only writer taking a caller fingerprint) as the word unmatched beside the fingerprint in the Brief cell; the no-seat-cards None branch is decided unmarked (unknown is not unmatched) and pinned as new AC5. Both honest paths the reviewer probed, plus a third found while re-probing (--brief-file of a saved PLAN-REVIEW rejoinder), matched nothing at HEAD (reproduced 2026-09-15); the design now makes them match - the matcher tries an explicit --tier, and a --brief-file carrying the rejoinder mark is hashed as rejoinder_fingerprint hashes it for the recorded phase - and AC4 pins each as a Brief cell byte-equal to the brief verb's footer, unmarked, with rows for ignoring --tier, hashing the whole rejoinder, hashing the base without the phase tag, and defaulting the phase, beside an invented-value positive control. AC1 adds the plan-review ledger; new AC2 pins the per-unit decision in a batch; AC3 names the `_brief_key` observable (`verdict_for` returns a marked REJECT after a marked APPROVE with the same fingerprint, while a matched pair returns the APPROVE) and the panel interlock's matched twin as control. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r2 REJECT answered. BLOCKING (AC4): record --brief-file of a saved FIRST-ROUND delivery brief is added as an honest path - its Brief cell must equal the footer byte for byte, unmarked - with the row 'hash every --brief-file with rejoinder_fingerprint whether or not it carries the rejoinder mark', which the reviewer showed records a value no footer printed while the matcher leaves it unmarked. Measured at HEAD in a seat-carded fixture: the first-round --brief-file already records the footer's fingerprint (221de5bfb2a6 both), so the preamble now states that a --brief-file without the rejoinder mark stays hashed with brief_fingerprint and the rejoinder branch must not reach it. The stale-fingerprint panel cost the reviewer noted is already stated and accepted in the preamble; unchanged. |
| 2026-09-25 | Claude Opus 5.5 | AC1 and AC4 narrowed by US0915 (D0259 pattern): `record --phase plan-review` and the plan-review rejoinder were deleted with plan review, so the delivery paths are what each criterion and its test still cover |
| 2026-09-25 | Claude Opus 5.5 | AC1-AC5 retired by US0923 (D0259 pattern): `critic.py record` no longer matches a `--brief` value or writes the `unmatched` mark; the mark's reader stays for historical rows |
