# BG0643: the --verify flag BG0636 shipped is refused for the one case it exists for: a criterion whose test is not written yet

> **Status:** Fixed
> **Verification depth:** functional
> **Severity:** Medium
> **Points:** 5
> **Affects:** .claude/skills/sdlc-studio/scripts/file_finding.py, .claude/skills/sdlc-studio/scripts/verify_ac.py, .claude/skills/sdlc-studio/scripts/tests/test_file_finding.py, .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py, sdlc-studio/bugs/BG0570-the-write-time-verify-guard-cannot-tell-a.md, sdlc-studio/stories/US0667-every-writer-refuses-a-verify-selector-that-resolves.md
> **Created:** 2026-09-03
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

BG0636 added `--verify` so a finding can be filed with an executable criterion. The write guard then refuses any selector naming a test that does not exist. For a bug filed BEFORE its fix - which is every bug - the test never exists yet, so the flag is refused in the ordinary case and accepted only when the work is already done. Found by using it three times in the run that shipped it: BG0640, BG0641 and this artefact all had to be filed WITHOUT the flag and then hand-edited to add the Verify lines, which is precisely the hand-rolling the flag was added to end.

## Steps to Reproduce

1. `file_finding.py file --type bug --ac '<criterion>' --verify 'pytest tests/test_x.py::NewTests::test_new'` where the file exists and the class does not.
2. Refused: `a Verify: selector names no test that exists`.
3. The same criterion, hand-written into the artefact afterwards, is accepted by `verify_ac run`, which reports it RED - the correct state for a criterion whose fix is not built.

## Proposed Fix

Separate a MISTYPED selector from a NOT-YET-WRITTEN one at the filing boundary, the way `selector_resolves` already separates a missing tree from a broken file. A selector whose test FILE resolves and whose node does not is not obviously a typo when the artefact is a bug in Open status: the node is what the fix will add. The guard's own suggestion mechanism proves the distinction is available - it offers `did you mean` from the file's real nodes - so a near-miss can be refused while a genuinely new node name is accepted and reported RED. What must not happen is the flag being usable only after the work it is meant to precede.

Groomed 2026-09-04 against `_classify_selector` at HEAD, which already splits four cases and refuses the "file listed its nodes, this node is absent" one outright. The fix splits THAT case on what `selector_near_miss` already computes: a method that exists under another class, or a close match at the same depth of an existing class, is a typo and stays refused with the near miss named; a class the file does not collect, or a method no collected method in that class is close to, is the not-yet-written test and is accepted. The file cases (missing, mistyped path, will-not-collect) are untouched.

**This narrows BG0570 AC1.** That criterion pinned "a node absent from a file that exists and collects is refused", and its test `VerifySelectorWriteGuardTests::test_a_node_absent_from_an_existing_file_is_still_refused` files `ThisClassIsNotInThatFile::test_x` and expects a refusal - the exact shape this bug accepts. The test is RE-AUTHORED in place, under its own name so BG0570's stamp is not orphaned, to the shapes that stay refused (a method that exists under another class; a near miss on an existing class), and BG0570 carries a revision row naming the narrowing. A criterion changed by a later unit is recorded, never quietly contradicted. Both writers share `_classify_selector`, so `artifact.py new --verify` follows.

Plan review round 1 (2026-09-04) changed the plan in three places. (a) `selector_near_miss` gathers its difflib candidates FILE-WIDE at the node's depth, not from the named class: measured over `test_verify_ac.py`'s 360 nodes, two of BG0603's own selectors and this unit's AC1 selector are 0.6-or-better matches to methods in other classes, so an implementer reusing it unchanged would refuse the batch's own paperwork. The matcher is scoped to the named class, which is why `verify_ac.py` carries a row. (b) Precedence: a typo shape wins over the not-yet-written shape. (c) FOUR existing tests file the shape this accepts, not one, and US0667 AC1 pins the same rule as BG0570 AC1; both are re-worded, not merely noted. Pre-existing and out of scope: `verify_ac.py run` renders an unknown node as `FAIL` with a vacuity note that says the run "exited 0" for an exit-4 run and tells the author to re-point the selector - filed into CR0511, the consolidated low-severity set, by the filer's own routing.

Plan review round 2 (2026-09-04): AC1's second row was unkillable by its own fixture (a method matching nothing in the file gives a file-wide consult nothing to find), so AC1 now files a second selector whose method resembles one in another class, and states that shape's verdict. `test_one_reader_answers_which_file_a_selector_targets` is dropped from the re-author list: it monkeypatches the mistyped-path branch and never reaches the node. Both writers share the seam (`artifact.py` calls `check_verify_selectors`); the delivery adds one control call on `artifact.py new --verify --dry-run` under AC1's test, without a criterion of its own. `test_verify_ac.py` is in Affects because the delivery adds a direct in-class scoping test for `selector_near_miss` there, beside the existing near-miss tests; the criterion that pins the scoping is AC3, through the filer.

## Acceptance Criteria

- [ ] **AC1** Given `file_finding.py file --type bug --root <fixture> --ac <criterion> --verify 'pytest <existing test file>::<class the file does not collect>::<method>'` driven as a subprocess (with the filer's required `--severity`, `--steps` and `--fix`), for TWO selectors - one whose method matches nothing in the file, and one whose method is a 0.6-or-better difflib match to a method in some EXISTING class of that file - when filed, then both exit 0, both selectors reach the artefact byte-exact, and `verify_ac.py run --root <fixture> --dir sdlc-studio/bugs --id <id>` driven as a subprocess prints (plain `run` walks stories only and exits 2 over a bugs-only tree) `FAIL AC1` and `FAIL AC2` for them - RED, the criterion-before-its-test ordering the flag exists for, with every write under the fixture root so this repository's `.local` is never touched. Precedence: a same-leaf match (AC2) or an in-class near miss (AC3) outranks this and refuses; a class the file does not collect has NO in-class candidates, so a resemblance to a method elsewhere in the file never refuses it. The accepted trade: `WrongClass::test_methd` (class AND method mistyped) is refused with a hint today and files RED tomorrow, and the RED first run is the net
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_file_finding.py::NotYetWrittenSelectorTests::test_a_new_class_in_an_existing_file_is_filed_and_runs_red
  - **Verified:** yes (2026-09-04)
- [ ] **AC2** Given a selector whose method EXISTS in that file under a different class, when filed, then it is refused and the refusal names the CLASS the author meant - the test asserts the class name, not only `did you mean`, which the path case also prints
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_file_finding.py::NotYetWrittenSelectorTests::test_a_method_that_exists_under_another_class_is_still_refused
  - **Verified:** yes (2026-09-04)
- [ ] **AC3** Given a selector naming an EXISTING class and a method absent from it, when filed, then it is accepted where no method collected IN THAT CLASS is a close match (difflib at the 0.6 cutoff `selector_near_miss` already uses) and refused naming the near miss where one is - and a 0.6-or-better match that lives in ANOTHER class of the same file does not refuse. One test asserts all three. `selector_near_miss` is scoped to the named class for the close-match branch (file_finding.py is its only caller, measured); its same-leaf branch is unchanged, so `VerifySelectorWriteGuardTests::test_the_refusal_names_the_near_miss`, which uses the exact-leaf shape, survives and is not re-authored
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_file_finding.py::NotYetWrittenSelectorTests::test_a_new_method_on_an_existing_class_files_and_a_near_miss_does_not
  - **Verified:** yes (2026-09-04)
- [ ] **AC4** Given a selector whose file does not exist but whose basename exists at another path, when filed, then it is refused naming that path exactly as today - the file cases keep their verdicts, so the licence to accept was narrowed to the node case and not widened to the file case. This test exists and passes today; it is the control whose mutant sits in the node-versus-file split
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_file_finding.py::VerifySelectorWriteGuardTests::test_a_misspelled_test_filename_is_still_refused
  - **Verified:** yes (2026-09-04)
- [ ] **AC5** Given the THREE tests in `VerifySelectorWriteGuardTests` whose fixtures reach the node branch with the shape AC1 now accepts - `test_a_verify_selector_naming_no_test_is_refused_at_write`, `test_a_node_absent_from_an_existing_file_is_still_refused`, `test_a_judgeable_unresolvable_selector_is_still_refused` - when the split lands, then each is re-authored under its own name to a shape that stays refused (the same-leaf typo), and the class passes; `test_the_refusal_names_the_near_miss` reddens on this row as well, so five reds are expected on it, not three. `test_one_reader_answers_which_file_a_selector_targets` keeps its shape and purpose (the guard follows the helper, never its own parser): its FIRST half forces the mistyped-path branch untouched, and its SECOND half - which forces `selector_collected` True and expects a refusal - now uses a same-leaf selector, because under the split a collected file with no near miss files (amended at delivery on 2026-09-04: the plan said untouched, and the second half was reached). BG0570 AC1 and US0667 AC1 are re-worded to the narrowed rule with a revision row each in the same commit - documentary, checked by the delivery review reading both rows back, because no executable verifier can see an artefact row
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_file_finding.py::VerifySelectorWriteGuardTests
  - **Verified:** yes (2026-09-04)
- [ ] **AC6** Given a collected test file and a selector that names NO node - a `-k` pattern that selects nothing, or a bare file that collects nothing - or a node hanging BELOW a collected test (`file::Class::test_x::extra`), when filed, then it is refused exactly as at the base ref: the licence to accept covers the node case only, and a pattern names no test to write later; a `-k` pattern that does select something files - the control. Added at delivery on 2026-09-04: the delivery review found the split accepted every target without `::` because the near-miss reader answers None for them
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_file_finding.py::NotYetWrittenSelectorTests::test_a_pattern_a_bare_file_and_a_node_below_a_leaf_are_still_refused
  - **Verified:** yes (2026-09-04)

## Impact

The flag is the whole of BG0636's authoring half, and the grooming gate BG0636 also shipped now REFUSES a batch holding a unit whose criteria carry no verifier. So a filer hits a refusal at filing, hand-edits the artefact to get past it, and the doctrine's no-hand-rolling rule is broken by the tool that exists to uphold it. Three of the four findings filed after BG0636 landed took exactly that route.

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `_classify_selector`, keep `return (True, "")` for the `selector_collected` case - today's code - so a class the file does not collect is refused as a typo | Given `file_finding.py file --type bug --root <fixture> --ac <criterion> --verify 'pytest <existing test file>::<class the file does not collect>::<method>'` driven as a subprocess (with the filer's required `--severity`, `--steps` and `--fix`), for TWO selectors - one whose method matches nothing in the file, and one whose method is a 0.6-or-better difflib match to a method in some EXISTING class of that file - when filed, then both exit 0, both selectors reach the artefact byte-exact, and `verify_ac.py run --root <fixture> --dir sdlc-studio/bugs --id <id>` driven as a subprocess prints (plain `run` walks stories only and exits 2 over a bugs-only tree) `FAIL AC1` and `FAIL AC2` for them - RED, the criterion-before-its-test ordering the flag exists for, with every write under the fixture root so this repository's `.local` is never touched. Precedence: a same-leaf match (AC2) or an in-class near miss (AC3) outranks this and refuses; a class the file does not collect has NO in-class candidates, so a resemblance to a method elsewhere in the file never refuses it. The accepted trade: `WrongClass::test_methd` (class AND method mistyped) is refused with a hint today and files RED tomorrow, and the RED first run is the net |
| AC1 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `_classify_selector`, restore the file-wide candidate gather in `verify_ac.py` `selector_near_miss` AND consult it before the class-collected check - a two-file edit, applied as declared - so the second selector, whose method resembles one in another class, is refused | Given `file_finding.py file --type bug --root <fixture> --ac <criterion> --verify 'pytest <existing test file>::<class the file does not collect>::<method>'` driven as a subprocess (with the filer's required `--severity`, `--steps` and `--fix`), for TWO selectors - one whose method matches nothing in the file, and one whose method is a 0.6-or-better difflib match to a method in some EXISTING class of that file - when filed, then both exit 0, both selectors reach the artefact byte-exact, and `verify_ac.py run --root <fixture> --dir sdlc-studio/bugs --id <id>` driven as a subprocess prints (plain `run` walks stories only and exits 2 over a bugs-only tree) `FAIL AC1` and `FAIL AC2` for them - RED, the criterion-before-its-test ordering the flag exists for, with every write under the fixture root so this repository's `.local` is never touched. Precedence: a same-leaf match (AC2) or an in-class near miss (AC3) outranks this and refuses; a class the file does not collect has NO in-class candidates, so a resemblance to a method elsewhere in the file never refuses it. The accepted trade: `WrongClass::test_methd` (class AND method mistyped) is refused with a hint today and files RED tomorrow, and the RED first run is the net |
| AC1 | in `.claude/skills/sdlc-studio/scripts/file_finding.py`, route the accepted selector through `_md_safe` so it is backtick-wrapped in the artefact rather than byte-exact | Given `file_finding.py file --type bug --root <fixture> --ac <criterion> --verify 'pytest <existing test file>::<class the file does not collect>::<method>'` driven as a subprocess (with the filer's required `--severity`, `--steps` and `--fix`), for TWO selectors - one whose method matches nothing in the file, and one whose method is a 0.6-or-better difflib match to a method in some EXISTING class of that file - when filed, then both exit 0, both selectors reach the artefact byte-exact, and `verify_ac.py run --root <fixture> --dir sdlc-studio/bugs --id <id>` driven as a subprocess prints (plain `run` walks stories only and exits 2 over a bugs-only tree) `FAIL AC1` and `FAIL AC2` for them - RED, the criterion-before-its-test ordering the flag exists for, with every write under the fixture root so this repository's `.local` is never touched. Precedence: a same-leaf match (AC2) or an in-class near miss (AC3) outranks this and refuses; a class the file does not collect has NO in-class candidates, so a resemblance to a method elsewhere in the file never refuses it. The accepted trade: `WrongClass::test_methd` (class AND method mistyped) is refused with a hint today and files RED tomorrow, and the RED first run is the net |
| AC2 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `_classify_selector`, return `(False, "")` for every collected-file case without consulting `verify_ac.selector_near_miss` | Given a selector whose method EXISTS in that file under a different class, when filed, then it is refused and the refusal names the CLASS the author meant - the test asserts the class name, not only `did you mean`, which the path case also prints |
| AC2 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `check_verify_selectors`, refuse but skip the near-miss hint on a refusal, so the class is never named | Given a selector whose method EXISTS in that file under a different class, when filed, then it is refused and the refusal names the CLASS the author meant - the test asserts the class name, not only `did you mean`, which the path case also prints |
| AC3 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `_classify_selector`, accept whenever the selector's class is among the collected nodes, skipping the method match - the in-class near-miss half survives | Given a selector naming an EXISTING class and a method absent from it, when filed, then it is accepted where no method collected IN THAT CLASS is a close match (difflib at the 0.6 cutoff `selector_near_miss` already uses) and refused naming the near miss where one is - and a 0.6-or-better match that lives in ANOTHER class of the same file does not refuse. One test asserts all three. `selector_near_miss` is scoped to the named class for the close-match branch (file_finding.py is its only caller, measured); its same-leaf branch is unchanged, so `VerifySelectorWriteGuardTests::test_the_refusal_names_the_near_miss`, which uses the exact-leaf shape, survives and is not re-authored |
| AC3 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `_classify_selector`, refuse whenever the class exists and the method is absent, whatever the match says - the new-method half survives | Given a selector naming an EXISTING class and a method absent from it, when filed, then it is accepted where no method collected IN THAT CLASS is a close match (difflib at the 0.6 cutoff `selector_near_miss` already uses) and refused naming the near miss where one is - and a 0.6-or-better match that lives in ANOTHER class of the same file does not refuse. One test asserts all three. `selector_near_miss` is scoped to the named class for the close-match branch (file_finding.py is its only caller, measured); its same-leaf branch is unchanged, so `VerifySelectorWriteGuardTests::test_the_refusal_names_the_near_miss`, which uses the exact-leaf shape, survives and is not re-authored |
| AC3 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py` `selector_near_miss`, gather close-match candidates from every class at that depth rather than from the named class - today's code - so the cross-class half survives | Given a selector naming an EXISTING class and a method absent from it, when filed, then it is accepted where no method collected IN THAT CLASS is a close match (difflib at the 0.6 cutoff `selector_near_miss` already uses) and refused naming the near miss where one is - and a 0.6-or-better match that lives in ANOTHER class of the same file does not refuse. One test asserts all three. `selector_near_miss` is scoped to the named class for the close-match branch (file_finding.py is its only caller, measured); its same-leaf branch is unchanged, so `VerifySelectorWriteGuardTests::test_the_refusal_names_the_near_miss`, which uses the exact-leaf shape, survives and is not re-authored |
| AC4 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `_classify_selector`, return `(False, "")` for the `basename_matches` case, so a mistyped path is accepted as an ordering | Given a selector whose file does not exist but whose basename exists at another path, when filed, then it is refused naming that path exactly as today - the file cases keep their verdicts, so the licence to accept was narrowed to the node case and not widened to the file case. This test exists and passes today; it is the control whose mutant sits in the node-versus-file split |
| AC5 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `_classify_selector`, accept the same-leaf case too (a method that exists under another class), so the three re-authored tests redden together | Given the THREE tests in `VerifySelectorWriteGuardTests` whose fixtures reach the node branch with the shape AC1 now accepts - `test_a_verify_selector_naming_no_test_is_refused_at_write`, `test_a_node_absent_from_an_existing_file_is_still_refused`, `test_a_judgeable_unresolvable_selector_is_still_refused` - when the split lands, then each is re-authored under its own name to a shape that stays refused (the same-leaf typo), and the class passes; `test_the_refusal_names_the_near_miss` reddens on this row as well, so five reds are expected on it, not three. `test_one_reader_answers_which_file_a_selector_targets` keeps its shape and purpose (the guard follows the helper, never its own parser): its FIRST half forces the mistyped-path branch untouched, and its SECOND half - which forces `selector_collected` True and expects a refusal - now uses a same-leaf selector, because under the split a collected file with no near miss files (amended at delivery on 2026-09-04: the plan said untouched, and the second half was reached). BG0570 AC1 and US0667 AC1 are re-worded to the narrowed rule with a revision row each in the same commit - documentary, checked by the delivery review reading both rows back, because no executable verifier can see an artefact row |
| AC6 | in `.claude/skills/sdlc-studio/scripts/file_finding.py` `_classify_selector`, drop the `"::" not in target` guard so a pattern or a bare file that names no node files silently | Given a collected test file and a selector that names NO node - a `-k` pattern that selects nothing, or a bare file that collects nothing - or a node hanging BELOW a collected test (`file::Class::test_x::extra`), when filed, then it is refused exactly as at the base ref: the licence to accept covers the node case only, and a pattern names no test to write later; a `-k` pattern that does select something files - the control. Added at delivery on 2026-09-04: the delivery review found the split accepted every target without `::` because the near-miss reader answers None for them |
| AC6 | in `.claude/skills/sdlc-studio/scripts/verify_ac.py` `selector_near_miss`, drop the below-a-leaf branch so `ExistingA::test_alpha_thing::extra` files | Given a collected test file and a selector that names NO node - a `-k` pattern that selects nothing, or a bare file that collects nothing - or a node hanging BELOW a collected test (`file::Class::test_x::extra`), when filed, then it is refused exactly as at the base ref: the licence to accept covers the node case only, and a pattern names no test to write later; a `-k` pattern that does select something files - the control. Added at delivery on 2026-09-04: the delivery review found the split accepted every target without `::` because the near-miss reader answers None for them |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-03 | sdlc-studio | Filed |
| 2026-09-04 | sdlc-studio | Groomed for the next sprint: executable criteria with a paired control each, Affects narrowed to where the fix lands, premise re-measured at HEAD |
| 2026-09-04 | sdlc-studio | Plan review round 1 (three seats): criteria and test plan re-authored to the findings; see the Proposed Fix note dated 2026-09-04 |
| 2026-09-04 | sdlc-studio | Plan review round 2 (three seats): fixtures that could not reach their branch repaired; see the Proposed Fix note |
| 2026-09-04 | sdlc-studio | Plan review round 3 (three seats): delivery notes folded into the criteria; BG0642 AC3 gains the wrong-acknowledgement step |
| 2026-09-04 | sdlc-studio | Delivery amendment to AC5: the one-reader test's second half forces a collected file and is reached by the split, so it uses a same-leaf selector; plan said untouched |
| 2026-09-04 | sdlc-studio | Delivery review r1: AC6 added for the -k, bare-file and below-a-leaf shapes the split had widened; near-miss assertions made backticked; second-writer control added; count corrected |
| 2026-09-04 | sdlc-studio | Delivery review r2: AC6 moved from under Impact into the Acceptance Criteria section, where the brief renderer and the section readers see it |
