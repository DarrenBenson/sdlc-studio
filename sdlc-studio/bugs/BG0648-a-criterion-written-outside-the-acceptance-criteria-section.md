# BG0648: a criterion written outside the Acceptance Criteria section is executed by verify_ac and invisible to the brief, the transition gate, validate and the sprint report

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 4; plan rows 19; executed 19; killed 19; survived 0; not-run 0; retracted 3; entry point 3 of 4 criteria through the shipped CLI, 0 in-process; 1 undetermined (the named node could not be isolated) | fp 7e8490d20ace ]]
> **Severity:** Medium
> **Points:** 5
> **Affects:** .claude/skills/sdlc-studio/scripts/verify_ac.py, .claude/skills/sdlc-studio/scripts/critic.py, .claude/skills/sdlc-studio/scripts/validate.py, .claude/skills/sdlc-studio/scripts/lib/sdlc_md.py, .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py, .claude/skills/sdlc-studio/scripts/tests/test_validate.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py, .claude/skills/sdlc-studio/scripts/tests/test_transition.py, .claude/skills/sdlc-studio/scripts/tests/test_sdlc_md.py, sdlc-studio/stories/US0597-the-premise-is-replayed-and-measured-the-blocking.md, sdlc-studio/bugs/BG0270-the-goal-review-gate-refuses-a-plan-when.md, .claude/skills/sdlc-studio/scripts/tests/test_artifact.py
> **Created:** 2026-09-04
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`verify_ac.parse_story` collects every ACn block wherever it sits in the artefact, so `verify_ac` run and the AC fingerprint counted six criteria on BG0643 while critic.py brief (its own regex over the Acceptance Criteria section) rendered five, and `sdlc_md.criteria_section`'s consumers - transition.py's ticked-or-executable gate, `sprint_report.py`, validate.py - skipped the sixth. An AC6 appended under Impact at delivery on 2026-09-04 therefore passed conformance and validate, was executed and mutation-checked, and was absent from the brief every seat and the reviewer of record was handed. Two readers of one document disagree about what it contains, and nothing refuses the shape. Found by the product seat in BG0643's delivery review round two.

## Steps to Reproduce

1. Append a well-formed - [ ] **AC6** block with a Verify line under ## Impact of any bug. 2. `verify_ac.py` run --id <bug> --dir sdlc-studio/bugs prints ac=6. 3. critic.py brief --unit <bug> --seat product renders AC1 to AC5. 4. validate.py check and gate.py pass.

## Proposed Fix

One reader: `parse_story` and the brief renderer read the criteria through `sdlc_md.criteria_section`, and validate refuses an ACn block that sits outside the Acceptance Criteria section, naming the heading it was found under. Pin with a fixture carrying a criterion under Impact and assert every consumer agrees on the count.

## Acceptance Criteria

- [x] **AC1** Given a bug or story whose criteria sit outside `## Acceptance Criteria` in EITHER shape the corpus holds - the `**ACn**` bullet under another heading (the fixture appends one under `## Impact`) and the `### ACn:` heading under another section (the shape all corpus offenders use) - when `validate.py check` runs, then, for bugs and stories only (a retro or handoff that happens to carry an `ACn` shape is out of scope and stated so in code), it reports an ERROR tagged `[criterion-outside-section]` per misplaced criterion naming the artefact, the criterion id and the heading it sits under, an ERROR for an artefact that has criteria but NO `## Acceptance Criteria` section at all, and an artefact whose criteria all sit in their section reports none - the control - the section heading read as every reader reads it, in any case and with more words after it
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_validate.py::BugCriteriaTests::test_a_criterion_outside_its_section_in_either_shape_is_an_error_naming_the_heading
  - **Verified:** yes (2026-09-07)
- [x] **AC2** Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py::CriteriaSectionTests::test_verify_ac_and_the_brief_count_the_same_criteria
  - **Verified:** yes (2026-09-07)
- [x] **AC3** Given the same fixture, when `verify_ac.py lint --bugs` runs, then each misplaced criterion is named as unreviewable with the section it belongs in, so the author is told where to move it rather than left with a green run and a silent brief; and the same lint over an artefact whose criteria all sit in their section names nothing - the positive control
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_verify_ac.py::CriteriaSectionTests::test_lint_names_a_misplaced_criterion_with_its_section
  - **Verified:** yes (2026-09-07)
- [x] **AC4** Given this corpus, when the new ERROR lands, then the two artefacts verify_ac's own parser finds outside their section - US0597-the-premise-is-replayed-and-measured-the-blocking.md (criteria with no section: a `## Acceptance Criteria` section is created and they move into it) and BG0270-the-goal-review-gate-refuses-a-plan-when.md (AC4 under Revision History moves into the section) - are corrected with their derived depth fields regenerated in the SAME commit as the lane, so `validate.py check` over this tree exits 0 with no criterion error at the commit that ships it; US0629's `### AC1 -` headings under Test Plan are a hand-authored plan the grammar never matched and are left alone
  - **Verify:** shell python3 .claude/skills/sdlc-studio/scripts/validate.py check && ! python3 .claude/skills/sdlc-studio/scripts/validate.py check 2>&1 | grep -q 'criterion-outside-section'
  - **Verified:** yes (2026-09-07)

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `validate.py`, detect the `**ACn**` bullet shape only, so the `### ACn:` heading offenders pass | Given a bug or story whose criteria sit outside `## Acceptance Criteria` in EITHER shape the corpus holds - the `**ACn**` bullet under another heading (the fixture appends one under `## Impact`) and the `### ACn:` heading under another section (the shape all corpus offenders use) - when `validate.py check` runs, then, for bugs and stories only (a retro or handoff that happens to carry an `ACn` shape is out of scope and stated so in code), it reports an ERROR tagged `[criterion-outside-section]` per misplaced criterion naming the artefact, the criterion id and the heading it sits under, an ERROR for an artefact that has criteria but NO `## Acceptance Criteria` section at all, and an artefact whose criteria all sit in their section reports none - the control - the section heading read as every reader reads it, in any case and with more words after it |
| AC1 | in `validate.py`, report the misplaced block as a WARNING instead of an ERROR | Given a bug or story whose criteria sit outside `## Acceptance Criteria` in EITHER shape the corpus holds - the `**ACn**` bullet under another heading (the fixture appends one under `## Impact`) and the `### ACn:` heading under another section (the shape all corpus offenders use) - when `validate.py check` runs, then, for bugs and stories only (a retro or handoff that happens to carry an `ACn` shape is out of scope and stated so in code), it reports an ERROR tagged `[criterion-outside-section]` per misplaced criterion naming the artefact, the criterion id and the heading it sits under, an ERROR for an artefact that has criteria but NO `## Acceptance Criteria` section at all, and an artefact whose criteria all sit in their section reports none - the control - the section heading read as every reader reads it, in any case and with more words after it |
| AC1 | in `validate.py`, name the artefact and the id but not the heading | Given a bug or story whose criteria sit outside `## Acceptance Criteria` in EITHER shape the corpus holds - the `**ACn**` bullet under another heading (the fixture appends one under `## Impact`) and the `### ACn:` heading under another section (the shape all corpus offenders use) - when `validate.py check` runs, then, for bugs and stories only (a retro or handoff that happens to carry an `ACn` shape is out of scope and stated so in code), it reports an ERROR tagged `[criterion-outside-section]` per misplaced criterion naming the artefact, the criterion id and the heading it sits under, an ERROR for an artefact that has criteria but NO `## Acceptance Criteria` section at all, and an artefact whose criteria all sit in their section reports none - the control - the section heading read as every reader reads it, in any case and with more words after it |
| AC1 | in `validate.py`, treat an artefact with criteria and no section as clean | Given a bug or story whose criteria sit outside `## Acceptance Criteria` in EITHER shape the corpus holds - the `**ACn**` bullet under another heading (the fixture appends one under `## Impact`) and the `### ACn:` heading under another section (the shape all corpus offenders use) - when `validate.py check` runs, then, for bugs and stories only (a retro or handoff that happens to carry an `ACn` shape is out of scope and stated so in code), it reports an ERROR tagged `[criterion-outside-section]` per misplaced criterion naming the artefact, the criterion id and the heading it sits under, an ERROR for an artefact that has criteria but NO `## Acceptance Criteria` section at all, and an artefact whose criteria all sit in their section reports none - the control - the section heading read as every reader reads it, in any case and with more words after it |
| AC2 | in `verify_ac.py`, keep executing every `**ACn**` block wherever it sits - today's code | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC2 | in `critic.py` `brief`, read every `**ACn**` block in the file so the brief agrees with today's verify_ac by widening rather than narrowing | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC2 | in `verify_ac.py`, narrow the no-section artefact to zero criteria instead of the whole-file fallback | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC2 | in `critic.py` `brief`, keep rendering zero criteria for an artefact with no section, so the two readers disagree on the fallback | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC1 | in `verify_ac.py`, anchor the section heading on the whole line, case-sensitive, so `## acceptance criteria (revised)` reads as no section (delivery review r1, engineering seat) | Given a bug or story whose criteria sit outside `## Acceptance Criteria` in EITHER shape the corpus holds - the `**ACn**` bullet under another heading (the fixture appends one under `## Impact`) and the `### ACn:` heading under another section (the shape all corpus offenders use) - when `validate.py check` runs, then, for bugs and stories only (a retro or handoff that happens to carry an `ACn` shape is out of scope and stated so in code), it reports an ERROR tagged `[criterion-outside-section]` per misplaced criterion naming the artefact, the criterion id and the heading it sits under, an ERROR for an artefact that has criteria but NO `## Acceptance Criteria` section at all, and an artefact whose criteria all sit in their section reports none - the control - the section heading read as every reader reads it, in any case and with more words after it |
| AC2 | in `transition.py`, drop the misplaced-criteria refusal from the Done gate, so a red criterion outside the section passes by being ignored (delivery review r1, engineering seat) | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC2 | in `critic.py`, keep the brief's section regex case-sensitive and whole-line, so the two readers disagree on a heading variant (delivery review r1, engineering seat) | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC2 | in `verify_ac.py`, keep `ac_fingerprint` hashing every block, so editing a misplaced criterion changes the fingerprint of what the run judged (delivery review r1, engineering seat) | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC2 | in `verify_ac.py`, revert `_entry_point_split` to `parse_story`, so the depth deriver counts the misplaced criterion (delivery review r1, engineering seat) | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC2 | in `verify_ac.py`, read the `##` section marker before the fence guard, so a heading inside a fence moves a later criterion out of the section (delivery review r1, qa seat) | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC1 | in `validate.py`, widen the type guard to every artefact type, so an epic carrying the shape draws the error (delivery review r1, qa seat) | Given a bug or story whose criteria sit outside `## Acceptance Criteria` in EITHER shape the corpus holds - the `**ACn**` bullet under another heading (the fixture appends one under `## Impact`) and the `### ACn:` heading under another section (the shape all corpus offenders use) - when `validate.py check` runs, then, for bugs and stories only (a retro or handoff that happens to carry an `ACn` shape is out of scope and stated so in code), it reports an ERROR tagged `[criterion-outside-section]` per misplaced criterion naming the artefact, the criterion id and the heading it sits under, an ERROR for an artefact that has criteria but NO `## Acceptance Criteria` section at all, and an artefact whose criteria all sit in their section reports none - the control - the section heading read as every reader reads it, in any case and with more words after it |
| AC2 | in `transition.py`, refuse only misplaced criteria that carry a Verify line, so a bare stray criterion passes by omission (delivery review r2, engineering seat) | Given the same two-shape fixture, when `verify_ac.py run` and `critic.py brief` read it, then both report the same criterion count, the count of the section's criteria, and a misplaced block is neither executed nor briefed - pinned by giving the misplaced block a verifier that FAILS (`shell false`) and asserting the run stays green and the report names no such criterion, so a count equality alone cannot pass it; an artefact with NO section keeps today's whole-file read in BOTH readers - which changes the brief, since today it renders zero criteria for such an artefact - so the two readers agree, and the `Done` gate refuses an artefact with a misplaced criterion BY NAME rather than ignoring it - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; the depth deriver and the AC fingerprint read the section's blocks too (the fallback rule: narrowing verify_ac instead turns fifteen tests red in test_verify_ac alone, so the no-section case is refused by validate's error rather than narrowed) |
| AC3 | in `verify_ac.py` `lint`, name misplaced criteria on bugs only, so a story's is skipped (delivery review r2, engineering seat) | Given the same fixture, when `verify_ac.py lint --bugs` runs, then each misplaced criterion is named as unreviewable with the section it belongs in, so the author is told where to move it rather than left with a green run and a silent brief; and the same lint over an artefact whose criteria all sit in their section names nothing - the positive control |
| AC3 | in `verify_ac.py` `lint`, skip blocks outside the section silently | Given the same fixture, when `verify_ac.py lint --bugs` runs, then each misplaced criterion is named as unreviewable with the section it belongs in, so the author is told where to move it rather than left with a green run and a silent brief; and the same lint over an artefact whose criteria all sit in their section names nothing - the positive control |
| AC4 | leave US0597-the-premise-is-replayed-and-measured-the-blocking.md without a criteria section, so the lane refuses this tree at its own commit | Given this corpus, when the new ERROR lands, then the two artefacts verify_ac's own parser finds outside their section - US0597-the-premise-is-replayed-and-measured-the-blocking.md (criteria with no section: a `## Acceptance Criteria` section is created and they move into it) and BG0270-the-goal-review-gate-refuses-a-plan-when.md (AC4 under Revision History moves into the section) - are corrected with their derived depth fields regenerated in the SAME commit as the lane, so `validate.py check` over this tree exits 0 with no criterion error at the commit that ships it; US0629's `### AC1 -` headings under Test Plan are a hand-authored plan the grammar never matched and are left alone |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-04 | sdlc-studio | Filed |
| 2026-09-07 | sdlc-studio | Delivered 2026-09-07: every parsed block carries its section and criteria_blocks is the one reader for the runner, lint, plan deriver, brief and the census (twelve call sites); validate reports criterion-outside-section per misplaced criterion and for criteria with no section; lint names each misplaced criterion; the brief renders a no-section artefact's whole-file criteria. Corpus: US0597 gains its section (it carries no depth field to regenerate) and BG0270's AC4 moves into its own (no test plan, nothing to regenerate); validate check over this tree exits 0 with no criterion error, checked=2346. Two suite fixtures (GOOD_STORY, _BAD_STATUS_STORY) gain the section the rule now demands, and the deriver's stray-criterion case moves from a count refusal to a section read |
| 2026-09-07 | sdlc-studio | Delivery review r1 (engineering REJECT, product APPROVE, qa APPROVE): the Done gate refuses a misplaced criterion by name and reads the section's blocks - the runner no longer executes a misplaced red verifier, so the gate must not let it pass by omission; one heading rule for every reader (case-insensitive, a suffix allowed, as count_acs reads it); the fingerprint, the depth deriver and the brief's caller check read the section's blocks; the validate and lint messages say what the runner does; the new error carries a criteria-baseline key like no-ac; BG0270's revision row returned to its table and its depth field restored (nothing to regenerate), both corpus artefacts record their edit; a fence-safety pin and an epic in the validate fixture pin the section tracker and the bugs-and-stories guard; seven plan rows added. Unpinned and disclosed: revert-check and the epic AC matrix read the section's blocks with no test of their own |
| 2026-09-07 | sdlc-studio | Delivery review r2: engineering APPROVE. Residuals folded in: the misplaced-criterion refusal has its own channel in the Done gate rather than the evidence gate's broken-tooling slot, and names a bare stray criterion too (AC2 row 9); lint names a story's misplaced criterion (AC3 row 1); the fragment says neither corpus artefact carried a depth field to regenerate and that a retro, not an epic, also holds the shape under this detector; the section regex comment states where it and count_acs differ. Nine AC2 rows measured against a red base test were withdrawn and re-measured; the runner now refuses a red base. Unpinned and disclosed: revert-check and the epic AC matrix read the section's blocks |
