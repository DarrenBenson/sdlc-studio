# BG0678: A wired repair-plan gate keeps no rounds, no brief and no approval pin, so a rejected plan can never be retired and a re-recorded plan keeps its approval

> **Status:** Open
> **Severity:** Medium
> **Points:** 5
> **Depends on:** BG0673
> **Affects:** .claude/skills/sdlc-studio/scripts/repair_plan.py, .claude/skills/sdlc-studio/scripts/transition.py, .claude/skills/sdlc-studio/scripts/critic.py, .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py, .claude/skills/sdlc-studio/scripts/tests/test_critic.py
> **Evidence:** BG0673 round-2 plan review verdict, 2026-09-15 (sdlc-studio/reviews/plan-review-verdicts.md).
> **Created:** 2026-09-15
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

Split from BG0673 at its second plan review (RUN-01M2JA6J, 2026-09-15), which judged the whole at 13 points. BG0673 wires the gate: the record and review verbs, the transition call, the repair-plan kind and a clear test-plan ledger. This unit carries what the wired gate needs to be usable and honest: every round of a plan kept under the unit id (so the design threshold counts rounds on one unit), the reviewed findings' fingerprint written as the verdict's brief provenance (so an independent APPROVE of a revised round retires the earlier REJECT), a whole-plan pin that a post-approval re-record invalidates, and plan-author independence (a reviewer who is the plan's recorded author never satisfies the gate). Without it, a unit whose repair plan was rejected once can never close, and an approved plan can be rewritten and keep its approval.

## Steps to Reproduce

Measured by the round-2 QA seat on the planned BG0673 build: record a plan, review REJECT (bob), re-record, review APPROVE (bob) -> `plan_reviewed` ok=False and `verdict_for(kind=repair-plan)` = REJECT, because `review_repair_plan` writes no brief. Three rounds on one unit id are accepted where RP1..RP3 is refused, because each round overwrites `<unit>.json`. An approved plan re-recorded with an unreviewed approach still passes, because the pin hashes findings only.

## Proposed Fix

Carry BG0673's former AC4, AC5, AC9 and AC10 (now AC1-AC4) with their Test Plan rows: rounds stored per unit, never overwritten; the findings fingerprint in the Brief cell retiring the REJECT; the whole-plan hash in the approval pin; the reviewer compared with the plan's recorded author as well as the row's. Depends on BG0673.

Implementer notes: `RepairPlanPinTests` and `DesignDecisionTests` record plans under one id and must stay green once rounds are stored separately. The findings fingerprint goes in the verdict's Brief cell - the key every round of one unit's plan shares, and so the only one under which a revised round's APPROVE retires the round-one REJECT - and the whole-plan hash (findings and entries) goes in the approval pin; swapping them is AC2's fourth mutant.

## Acceptance Criteria

- [ ] **AC1** With `review.repair_plan_gate: on`, five repair units are each refused at Fixed naming which state holds them, the plan found by the unit's id: one with no plan recorded for it; one holding only an approved plan recorded for a DIFFERENT unit (refused as no plan); one whose plan has no verdict; one whose only verdict, written through `critic.py record --phase plan-review --kind repair-plan`, names the row's own Author as reviewer; and one whose only verdict, written the same way, names the plan's RECORDED author as reviewer while the row's Author cell names someone else. Both self-reviews carry the brief and pin `repair_plan.py review` would write and are refused naming independence, beside the same `critic.py record` naming a third identity as reviewer, for which `repair_plan.plan_reviewed` reads ok (depends on BG0673: the refusal is raised at the Fixed transition BG0673 wires, the plan is found by the unit id BG0673's `repair_plan.py record` keys it to, and `--kind repair-plan` is the kind BG0673 adds)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_each_missing_plan_state_is_refused_by_unit_id
- [ ] **AC2** With `review.repair_plan_gate: on` and `review.test_plan_after` left unset (so `_test_plan_gate` does not judge the fixture), two bugs reach Fixed through `repair_plan.py record` and `review`: one whose single plan round was independently APPROVEd although an earlier `critic.py record --phase plan-review --kind test-plan` REJECT stands on the same unit id; and one whose first round was REJECTed through `repair_plan.py review` and whose revised round (declaring its design, as a repeat requires) the same independent reviewer then APPROVEd. Each repair-plan verdict's Brief cell carries the fingerprint of the finding set the plan answers, which every round of that unit's plan shares, so the later APPROVE retires the round-one REJECT. Beside the first fixture with no plan, refused naming the gate (depends on BG0673: `repair_plan.py record` and `review` are its verbs, Fixed is reached through the transition it wires, and `repair-plan` is the kind it adds)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_an_approved_plan_passes_despite_an_earlier_test_plan_reject
- [ ] **AC3** Every round of a unit's repair plan is kept under the unit id, never overwritten: three `repair_plan.py record` rounds on one unit id answering one finding class are accepted for round one and for round two declaring `design: retain`, and the third declaring `design: retain` exits non-zero naming its 2 failed rounds and writes nothing, beside the same third round declaring `design: change`, which is accepted (depends on BG0673's `repair_plan.py record` verb)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_every_round_is_kept_so_the_design_threshold_binds_on_one_unit
- [ ] **AC4** A plan re-recorded after its approval loses that approval: with `review.repair_plan_gate: on`, a unit whose plan round was independently APPROVEd through `repair_plan.py review` passes `repair_plan.py gate` for its id; after `repair_plan.py record` re-records it with the same findings and a different approach (declaring its design), the same gate exits non-zero naming that the approval answered a different plan; and a fresh independent `review` APPROVE of the new round passes it again (depends on BG0673's `repair_plan.py record` and `review` verbs and on its keying of a plan to the unit id `repair_plan.py gate` is given)
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_repair_plan.py::RepairGateIsReachableTests::test_a_re_record_after_approval_invalidates_the_approval

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, return the same 'no reviewed plan' refusal for a unit with no plan as for one with an unreviewed plan, so the two causes are not named apart | With `review.repair_plan_gate: on`, five repair units are each refused at Fixed naming which state holds them, the plan found by the unit's id: one with no plan recorded for it; one holding only an approved plan recorded for a DIFFERENT unit (refused as no plan); one whose plan has no verdict; one whose only verdict, written through `critic.py record --phase plan-review --kind repair-plan`, names the row's own Author as reviewer; and one whose only verdict, written the same way, names the plan's RECORDED author as reviewer while the row's Author cell names someone else. Both self-reviews carry the brief and pin `repair_plan.py review` would write and are refused naming independence, beside the same `critic.py record` naming a third identity as reviewer, for which `repair_plan.plan_reviewed` reads ok (depends on BG0673: the refusal is raised at the Fixed transition BG0673 wires, the plan is found by the unit id BG0673's `repair_plan.py record` keys it to, and `--kind repair-plan` is the kind BG0673 adds) |
| AC1 | in .claude/skills/sdlc-studio/scripts/transition.py, replace the unit-id plan lookup with the most recently written plan's id when calling repair_plan.repair_gate | With `review.repair_plan_gate: on`, five repair units are each refused at Fixed naming which state holds them, the plan found by the unit's id: one with no plan recorded for it; one holding only an approved plan recorded for a DIFFERENT unit (refused as no plan); one whose plan has no verdict; one whose only verdict, written through `critic.py record --phase plan-review --kind repair-plan`, names the row's own Author as reviewer; and one whose only verdict, written the same way, names the plan's RECORDED author as reviewer while the row's Author cell names someone else. Both self-reviews carry the brief and pin `repair_plan.py review` would write and are refused naming independence, beside the same `critic.py record` naming a third identity as reviewer, for which `repair_plan.plan_reviewed` reads ok (depends on BG0673: the refusal is raised at the Fixed transition BG0673 wires, the plan is found by the unit id BG0673's `repair_plan.py record` keys it to, and `--kind repair-plan` is the kind BG0673 adds) |
| AC1 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, remove the critic.is_independent check from plan_reviewed so an APPROVE whose reviewer is the row's own Author counts as a review | With `review.repair_plan_gate: on`, five repair units are each refused at Fixed naming which state holds them, the plan found by the unit's id: one with no plan recorded for it; one holding only an approved plan recorded for a DIFFERENT unit (refused as no plan); one whose plan has no verdict; one whose only verdict, written through `critic.py record --phase plan-review --kind repair-plan`, names the row's own Author as reviewer; and one whose only verdict, written the same way, names the plan's RECORDED author as reviewer while the row's Author cell names someone else. Both self-reviews carry the brief and pin `repair_plan.py review` would write and are refused naming independence, beside the same `critic.py record` naming a third identity as reviewer, for which `repair_plan.plan_reviewed` reads ok (depends on BG0673: the refusal is raised at the Fixed transition BG0673 wires, the plan is found by the unit id BG0673's `repair_plan.py record` keys it to, and `--kind repair-plan` is the kind BG0673 adds) |
| AC1 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, drop the plan file's stored author from plan_reviewed's independence check, so the reviewer is set against the row's Author cell alone (HEAD's reading) | With `review.repair_plan_gate: on`, five repair units are each refused at Fixed naming which state holds them, the plan found by the unit's id: one with no plan recorded for it; one holding only an approved plan recorded for a DIFFERENT unit (refused as no plan); one whose plan has no verdict; one whose only verdict, written through `critic.py record --phase plan-review --kind repair-plan`, names the row's own Author as reviewer; and one whose only verdict, written the same way, names the plan's RECORDED author as reviewer while the row's Author cell names someone else. Both self-reviews carry the brief and pin `repair_plan.py review` would write and are refused naming independence, beside the same `critic.py record` naming a third identity as reviewer, for which `repair_plan.plan_reviewed` reads ok (depends on BG0673: the refusal is raised at the Fixed transition BG0673 wires, the plan is found by the unit id BG0673's `repair_plan.py record` keys it to, and `--kind repair-plan` is the kind BG0673 adds) |
| AC2 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, drop the kind argument from plan_reviewed's critic.verdict_for call, so it reads every plan-review row on the unit id and returns the earlier test-plan REJECT | With `review.repair_plan_gate: on` and `review.test_plan_after` left unset (so `_test_plan_gate` does not judge the fixture), two bugs reach Fixed through `repair_plan.py record` and `review`: one whose single plan round was independently APPROVEd although an earlier `critic.py record --phase plan-review --kind test-plan` REJECT stands on the same unit id; and one whose first round was REJECTed through `repair_plan.py review` and whose revised round (declaring its design, as a repeat requires) the same independent reviewer then APPROVEd. Each repair-plan verdict's Brief cell carries the fingerprint of the finding set the plan answers, which every round of that unit's plan shares, so the later APPROVE retires the round-one REJECT. Beside the first fixture with no plan, refused naming the gate (depends on BG0673: `repair_plan.py record` and `review` are its verbs, Fixed is reached through the transition it wires, and `repair-plan` is the kind it adds) |
| AC2 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, hard-code kind test-plan in both review_repair_plan and plan_reviewed, so the repair approval shares a kind with the test-plan ledger | With `review.repair_plan_gate: on` and `review.test_plan_after` left unset (so `_test_plan_gate` does not judge the fixture), two bugs reach Fixed through `repair_plan.py record` and `review`: one whose single plan round was independently APPROVEd although an earlier `critic.py record --phase plan-review --kind test-plan` REJECT stands on the same unit id; and one whose first round was REJECTed through `repair_plan.py review` and whose revised round (declaring its design, as a repeat requires) the same independent reviewer then APPROVEd. Each repair-plan verdict's Brief cell carries the fingerprint of the finding set the plan answers, which every round of that unit's plan shares, so the later APPROVE retires the round-one REJECT. Beside the first fixture with no plan, refused naming the gate (depends on BG0673: `repair_plan.py record` and `review` are its verbs, Fixed is reached through the transition it wires, and `repair-plan` is the kind it adds) |
| AC2 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, drop the brief argument from review_repair_plan's critic.record_verdict call, so the ledger writes `-` and _unanswered_rejects keeps the round-one REJECT standing | With `review.repair_plan_gate: on` and `review.test_plan_after` left unset (so `_test_plan_gate` does not judge the fixture), two bugs reach Fixed through `repair_plan.py record` and `review`: one whose single plan round was independently APPROVEd although an earlier `critic.py record --phase plan-review --kind test-plan` REJECT stands on the same unit id; and one whose first round was REJECTed through `repair_plan.py review` and whose revised round (declaring its design, as a repeat requires) the same independent reviewer then APPROVEd. Each repair-plan verdict's Brief cell carries the fingerprint of the finding set the plan answers, which every round of that unit's plan shares, so the later APPROVE retires the round-one REJECT. Beside the first fixture with no plan, refused naming the gate (depends on BG0673: `repair_plan.py record` and `review` are its verbs, Fixed is reached through the transition it wires, and `repair-plan` is the kind it adds) |
| AC2 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, replace review_repair_plan's brief with the whole-plan hash (findings and entries), so the revised round's APPROVE carries a different key and never retires the REJECT | With `review.repair_plan_gate: on` and `review.test_plan_after` left unset (so `_test_plan_gate` does not judge the fixture), two bugs reach Fixed through `repair_plan.py record` and `review`: one whose single plan round was independently APPROVEd although an earlier `critic.py record --phase plan-review --kind test-plan` REJECT stands on the same unit id; and one whose first round was REJECTed through `repair_plan.py review` and whose revised round (declaring its design, as a repeat requires) the same independent reviewer then APPROVEd. Each repair-plan verdict's Brief cell carries the fingerprint of the finding set the plan answers, which every round of that unit's plan shares, so the later APPROVE retires the round-one REJECT. Beside the first fixture with no plan, refused naming the gate (depends on BG0673: `repair_plan.py record` and `review` are its verbs, Fixed is reached through the transition it wires, and `repair-plan` is the kind it adds) |
| AC3 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, have record_repair_plan write the plan to `<unit>.json` in place, so each round replaces the last and repeat_rounds never counts past one | Every round of a unit's repair plan is kept under the unit id, never overwritten: three `repair_plan.py record` rounds on one unit id answering one finding class are accepted for round one and for round two declaring `design: retain`, and the third declaring `design: retain` exits non-zero naming its 2 failed rounds and writes nothing, beside the same third round declaring `design: change`, which is accepted (depends on BG0673's `repair_plan.py record` verb) |
| AC3 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, change repeat_rounds to count the distinct unit ids carrying the class instead of the recorded rounds | Every round of a unit's repair plan is kept under the unit id, never overwritten: three `repair_plan.py record` rounds on one unit id answering one finding class are accepted for round one and for round two declaring `design: retain`, and the third declaring `design: retain` exits non-zero naming its 2 failed rounds and writes nothing, beside the same third round declaring `design: change`, which is accepted (depends on BG0673's `repair_plan.py record` verb) |
| AC4 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, replace the whole-plan pin with findings_fingerprint alone, so a round with a new approach and the same findings inherits the earlier APPROVE | A plan re-recorded after its approval loses that approval: with `review.repair_plan_gate: on`, a unit whose plan round was independently APPROVEd through `repair_plan.py review` passes `repair_plan.py gate` for its id; after `repair_plan.py record` re-records it with the same findings and a different approach (declaring its design), the same gate exits non-zero naming that the approval answered a different plan; and a fresh independent `review` APPROVE of the new round passes it again (depends on BG0673's `repair_plan.py record` and `review` verbs and on its keying of a plan to the unit id `repair_plan.py gate` is given) |
| AC4 | in .claude/skills/sdlc-studio/scripts/repair_plan.py, have load_plan return the unit's first recorded round rather than its latest, so plan_reviewed judges the round that was approved | A plan re-recorded after its approval loses that approval: with `review.repair_plan_gate: on`, a unit whose plan round was independently APPROVEd through `repair_plan.py review` passes `repair_plan.py gate` for its id; after `repair_plan.py record` re-records it with the same findings and a different approach (declaring its design), the same gate exits non-zero naming that the approval answered a different plan; and a fresh independent `review` APPROVE of the new round passes it again (depends on BG0673's `repair_plan.py record` and `review` verbs and on its keying of a plan to the unit id `repair_plan.py gate` is given) |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-15 | sdlc-studio | Filed |
| 2026-09-15 | sprint plan repair 2026-09-15 | Took BG0673's former AC4, AC5, AC9 and AC10 (now AC1-AC4) with their Verify lines and 12 Test Plan rows, replacing the scaffolded criteria; each criterion now names the BG0673 verbs it depends on, and transition.py joins Affects for AC1's unit-id lookup mutant. |
