# BG0651: A later commit that changes a target file empties an earlier unit's mutation evidence silently, and nothing refuses until the close dry-run

> **Status:** Fixed
> **Verification depth:** functional [[derived: criteria 4; plan rows 22; executed 22; killed 22; survived 0; not-run 0; entry point 1 of 4 criteria through the shipped CLI, 3 in-process | fp e6a9c7e3070f ]]
> **Severity:** Medium
> **Points:** 5
> **Affects:** .claude/skills/sdlc-studio/scripts/gate.py, .claude/skills/sdlc-studio/scripts/mutation.py, .claude/skills/sdlc-studio/scripts/tests/test_gate.py, .claude/skills/sdlc-studio/scripts/tests/test_mutation.py, tools/tests/test_check_spec_claims.py, AGENTS.md
> **Evidence:** RUN-01M1NS3C close dry-run 2026-09-04: `done-gate: BG0641 -> Fixed blocked: 19 planned mutant(s) unaccounted for` and `BG0643: 2 planned mutant(s) unaccounted for`, both units Fixed with 30/30 and 12/12 killed at transition. Ruled a Medium bug by the operator on 2026-09-06.
> **Created:** 2026-09-06
> **Created-by:** sdlc-studio file
> **Raised-by:** sdlc-studio; agent; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`mutation.py register` hashes the target's bytes, and any later edit to that file drops every registered row for it (LL0053). In RUN-01M1NS3C this happened four times in one day: BG0642's edit to `.githooks/pre-push` emptied BG0641's thirty rows, BG0603's edit to `verify_ac.py` emptied two of BG0643's, and the round-two repairs emptied BG0644's and BG0647's own rows. Each unit was Fixed with `from-plan` reading every row killed; the drop surfaced only when `sprint close --dry-run` re-checked the done-gate hours later. No commit-time lane compares the ledger's live rows against the files a commit changes.

## Steps to Reproduce

1. Register a unit's mutants against a file and transition it to Fixed.
2. In a later unit, edit that file and commit.
3. `mutation.py run --story <first unit> --from-plan`: every row reads not-run; `transition set <first unit> Fixed --dry-run` is blocked; nothing at the second commit said so.

## Proposed Fix

A pre-commit lane (in `gate.py`'s block, beside derived-depth) that lists every Fixed or Done unit whose live ledger rows target a file the commit changes, and refuses with the units and their re-measure command; or re-measures automatically when the unit's runner is recorded. Decide at grooming which; refusing is the smaller change.

## Acceptance Criteria

- [x] **AC1** Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_gate.py::EvidenceDriftTests::test_a_commit_that_rewrites_a_registered_target_is_refused_with_the_unit_named
  - **Verified:** yes (2026-09-07)
- [x] **AC2** Given the same file, when every row on it has been re-registered against the staged bytes so no recorded hash differs from the staged blob's, then the lane passes; and a unit at a status that is NOT terminal for its type (Open, In Progress, Ready, Review) whose rows differ is REPORTED with the same names and never refused - its evidence is still being made
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_gate.py::EvidenceDriftTests::test_re_registered_rows_pass_and_a_non_terminal_unit_is_reported_not_refused
  - **Verified:** yes (2026-09-07)
- [x] **AC3** Given the AGENTS.md lane roster, when `tools/tests/test_check_spec_claims.py` runs, then `evidence-drift` is named in the roster with its blocking status, so the lane is not one nobody wrote down (LL0013)
  - **Verify:** pytest tools/tests/test_check_spec_claims.py::GateLaneTests::test_the_lane_roster_names_evidence_drift_and_its_blocking_status
  - **Verified:** yes (2026-09-07)
- [x] **AC4** Given a delivered-terminal unit with registered rows on a file, when a LATER unit runs `mutation.py register` on that file with bytes that differ from the earlier unit's recorded hash, then the earlier rows are KEPT and marked stale with the later unit named, never deleted (today `register` deletes them before any commit, so a commit-time lane alone finds nothing - the path every unit in this batch takes over critic.py and gate.py), the register output names the earlier unit and the same remedy, and the commit lane then sees the stale rows; a STALE row means the same thing to every reader of the ledger - its recorded hash matches neither HEAD's blob nor the working file, the condition AC1 names - and `--from-plan` and the done-gate read a stale row as NOT-RUN, never as killed, so a unit with any stale row cannot reach Fixed until it re-registers, whether or not the commit lane ran (hooks off, `--no-verify`, a drift that landed before the lane); a register by the SAME unit on its own rows replaces them as today
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_mutation.py::RegisterKeepsOtherUnitsRowsTests::test_a_later_unit_register_marks_the_earlier_rows_stale_and_names_it
  - **Verified:** yes (2026-09-07)

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in `gate.py`, compare the ledger's rows against the WORKING tree instead of the staged blob, so an unstaged edit refuses and a staged one passes | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC1 | in `gate.py`, name the unit but not the rows or the re-register remedy | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC1 | in `gate.py`, refuse whenever any unit has rows on the file, ignoring whether the staged hash differs | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC1 | in `gate.py`, decide terminal by the literal `Fixed` alone, so the fixture's Done story goes unguarded | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC1 | in `gate.py`, refuse rows that were already stale at HEAD as if the commit had drifted them, so the lane refuses its own first commit | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC1 | in `gate.py`, decide terminal by `terminal_statuses`, so a Won't Fix bug's rows refuse a commit | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC1 | in `gate.py`, crash on a registered target that exists neither at HEAD nor on disk instead of reporting it stale | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC1 | in `gate.py`, name the unit and the rows but not the FILE, so the author cannot tell which staged path to re-register against | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC2 | in `gate.py`, name a WITHDRAWN row beside the live ones (delivery review, qa seat) | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC2 | in `gate.py`, crash on a unit id with no artefact on disk instead of reporting it (delivery review, qa seat) | Given a unit at a status that is DELIVERED-TERMINAL for its type (a bug at Fixed, Verified or Closed; a story at Done - decided by `sdlc_md.is_delivered_terminal`, the predicate the done-gate itself uses, never a literal and never `terminal_statuses`, which also holds Won't Fix and Superseded; the fixture holds a Fixed bug, a Done story AND a Won't Fix bug with rows on the same file, whose rows are REPORTED and never refused, which is the outcome that separates the two predicates - the test asserts the Won't Fix id is absent from the refusal's unit list and present in the report's, so the output separates refused from reported) whose registered mutant rows target a file, when a commit stages a change to that file and the staged blob's hash differs from the hash the row recorded, and HEAD's blob for that file still matched the recorded hash (the drift is the COMMIT's own), then the pre-commit gate's `evidence-drift` lane refuses, naming the unit, the file, the rows, and the remedy that clears it: re-apply each row's mutant and `mutation.py register` it with the file's working copy equal to what is staged, since `register` hashes the WORKING file and the lane compares the staged blob, and the lane says so when the two differ (the `--from-plan` join reads the ledger and never compares a hash, so it is named as the CHECK after re-registering, never as the remedy); and a commit staging no registered target passes the lane, including one whose WORKING tree holds an unstaged edit to a registered target - the control that separates staged from working; rows registered against a WORKING copy that differs from the staged blob are evidence about bytes the commit will not carry, so for a delivered unit they REFUSE with that reason and for any other unit they are reported; a withdrawn row is never named; a unit with no artefact on disk is reported, never a crash; and rows that were ALREADY stale at HEAD (the recorded hash matches neither HEAD's blob nor the staged one - 17 of the 63 entries on this tree, three of them on files this unit stages) are REPORTED by unit with the same remedy and never refused, so the lane does not refuse the commit that ships it and the earlier drift is named rather than hidden; a row whose registered target exists neither at HEAD nor on disk (BG0607 left one on `tools/tests/check_backfill_evidence.py`) is treated as already stale, reported, never a crash; and the 11 LIVE rows on `gate.py` and `mutation.py` that this unit's own commit drifts (BG0640's six on gate.py, BG0596's four and BG0597's one on mutation.py, by the ledger) - the rule's home is the one join at `mutation.py` that `--from-plan`, the done-gate and the depth deriver all consume are re-measured and re-registered by THIS unit at its delivery, priced into its points |
| AC2 | in `gate.py`, refuse an In Progress unit's differing rows as if it were terminal | Given the same file, when every row on it has been re-registered against the staged bytes so no recorded hash differs from the staged blob's, then the lane passes; and a unit at a status that is NOT terminal for its type (Open, In Progress, Ready, Review) whose rows differ is REPORTED with the same names and never refused - its evidence is still being made |
| AC2 | in `gate.py`, read the rows' recorded hash but never re-hash the staged blob, so re-registered rows still refuse | Given the same file, when every row on it has been re-registered against the staged bytes so no recorded hash differs from the staged blob's, then the lane passes; and a unit at a status that is NOT terminal for its type (Open, In Progress, Ready, Review) whose rows differ is REPORTED with the same names and never refused - its evidence is still being made |
| AC2 | in `gate.py`, skip every non-terminal unit's differing rows silently instead of reporting them | Given the same file, when every row on it has been re-registered against the staged bytes so no recorded hash differs from the staged blob's, then the lane passes; and a unit at a status that is NOT terminal for its type (Open, In Progress, Ready, Review) whose rows differ is REPORTED with the same names and never refused - its evidence is still being made |
| AC2 | in `gate.py`, stay silent when rows were registered against a working copy that differs from the staged blob (delivery review, product seat) | Given the same file, when every row on it has been re-registered against the staged bytes so no recorded hash differs from the staged blob's, then the lane passes; and a unit at a status that is NOT terminal for its type (Open, In Progress, Ready, Review) whose rows differ is REPORTED with the same names and never refused - its evidence is still being made |
| AC3 | in `AGENTS.md`, leave the roster without the lane | Given the AGENTS.md lane roster, when `tools/tests/test_check_spec_claims.py` runs, then `evidence-drift` is named in the roster with its blocking status, so the lane is not one nobody wrote down (LL0013) |
| AC3 | in `AGENTS.md`, name the lane in the roster without its blocking status | Given the AGENTS.md lane roster, when `tools/tests/test_check_spec_claims.py` runs, then `evidence-drift` is named in the roster with its blocking status, so the lane is not one nobody wrote down (LL0013) |
| AC4 | in `mutation.py` `register`, keep deleting another unit's rows on a changed file - today's code - so the commit lane finds nothing | Given a delivered-terminal unit with registered rows on a file, when a LATER unit runs `mutation.py register` on that file with bytes that differ from the earlier unit's recorded hash, then the earlier rows are KEPT and marked stale with the later unit named, never deleted (today `register` deletes them before any commit, so a commit-time lane alone finds nothing - the path every unit in this batc |
| AC4 | in `mutation.py` `register`, keep the rows but name no unit, so the reader cannot tell whose evidence went stale | Given a delivered-terminal unit with registered rows on a file, when a LATER unit runs `mutation.py register` on that file with bytes that differ from the earlier unit's recorded hash, then the earlier rows are KEPT and marked stale with the later unit named, never deleted (today `register` deletes them before any commit, so a commit-time lane alone finds nothing - the path every unit in this batc |
| AC4 | in `mutation.py` `register`, keep another unit's rows even when the SAME unit re-registers its own, so a unit can never replace its evidence | Given a delivered-terminal unit with registered rows on a file, when a LATER unit runs `mutation.py register` on that file with bytes that differ from the earlier unit's recorded hash, then the earlier rows are KEPT and marked stale with the later unit named, never deleted (today `register` deletes them before any commit, so a commit-time lane alone finds nothing - the path every unit in this batc |
| AC4 | in `mutation.py`, keep the `--from-plan` join hash-blind so a stale row still reads killed and the done-gate passes a partially drifted unit | Given a delivered-terminal unit with registered rows on a file, when a LATER unit runs `mutation.py register` on that file with bytes that differ from the earlier unit's recorded hash, then the earlier rows are KEPT and marked stale with the later unit named, never deleted (today `register` deletes them before any commit, so a commit-time lane alone finds nothing - the path every unit in this batc |
| AC4 | in `mutation.py`, let the stale mark decide the join beside the hash comparison, so a file restored to its recorded bytes still reads stale (delivery review, qa seat) | Given a delivered-terminal unit with registered rows on a file, when a LATER unit runs `mutation.py register` on that file with bytes that differ from the earlier unit's recorded hash, then the earlier rows are KEPT and marked stale with the later unit named, never deleted (today `register` deletes them before any commit, so a commit-time lane alone finds nothing - the path every unit in this batc |
| AC4 | in `mutation.py`, read a row whose hash matches HEAD but not the working file as stale, so an unstaged edit blocks the join (delivery review, qa seat) | Given a delivered-terminal unit with registered rows on a file, when a LATER unit runs `mutation.py register` on that file with bytes that differ from the earlier unit's recorded hash, then the earlier rows are KEPT and marked stale with the later unit named, never deleted (today `register` deletes them before any commit, so a commit-time lane alone finds nothing - the path every unit in this batc |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-06 | sdlc-studio | Filed |
| 2026-09-06 | sdlc | Shape ruled by the operator on 2026-09-06: refuse the commit and name the unit - a pre-commit lane that lists every Fixed unit whose registered mutant rows target a file the commit changes and refuses with the unit and its re-measure command; automatic re-measure and advisory-first were declined. Groomed alongside the D0182 batch |
| 2026-09-07 | sdlc | Delivery review r1 (2026-09-07): three REJECTs. Repaired: rows registered against a working copy that differs from the staged blob now REFUSE for a delivered unit and are reported otherwise (the AC1 clause that was written and not built); the stale mark is a record of who and when, never a rule beside the computed hash comparison; withdrawn rows are never named and an unfindable unit is reported; the TRD's default-sweep list names the lane; the test helper scrubs through gitutil.git_env; the done-gate itself is pinned reading a stale row as not-run; the row inventory corrected to BG0640's six, BG0596's four and BG0597's one. Five plan rows added, 22 executed and killed after the last edit; BG0640's, BG0596's and BG0597's rows re-measured against the final bytes - the first pass had re-measured them against an intermediate file, exactly the drop this unit exists to stop, caught by the product seat |
