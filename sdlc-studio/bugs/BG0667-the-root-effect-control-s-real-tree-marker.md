# BG0667: the root-effect control's real-tree marker is bound to an id range this project has already outgrown, so its evidence window closes as ids advance

> **Status:** Open
> **Severity:** Medium
> **Points:** 3
> **Affects:** .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py
> **Evidence:** Introduced by commit 0987a620 (BG0555/BG0556) and untouched since. Measured by an independent test-plan reviewer on 2026-09-11 in an isolated copy: BG0663's AC1 mutant survives with `changelog.d/US0674.md` present (`1 passed, 10 subtests passed in 41.07s`) and dies with the directory emptied.
> **Created:** 2026-09-11
> **Created-by:** sdlc-studio file
> **Raised-by:** Claude Opus 5; human; v1
> **Raised-in-batch:** none open - raised outside a delivery batch

## Summary

`test_cli_grammar._REAL_TREE_MARKER` is `\\b(?:BG05\\d\\d|US06\\d\\d|RUN-01K\\w+)`. It is what proves a verb READ the real tree rather than printing something either way, and it recognises only three id shapes fixed at the time it was written. This project now mints BG06xx and US08xx, and run ids have moved past `RUN-01K`.

The control therefore weakens silently as the corpus advances: a verb whose output names only recent ids reads as naming nothing. An independent seat measured the consequence on BG0663 - with one realistic pending fragment present (`changelog.d/US0674.md`, a live story id), the marker matched and a mutant that should have died SURVIVED; with only BG06xx fragments present, outside the marker, the same mutant died. The verdict oscillates with which ids happen to be in the tree.

A guard whose evidence window is an id range is a guard with an expiry date nobody wrote down.

## Steps to Reproduce

1. Read `_REAL_TREE_MARKER`: it matches `BG05xx`, `US06xx` and `RUN-01K...` only.
2. `ls sdlc-studio/bugs | tail -1` - this project mints BG06xx, outside it.
3. Put a `US06xx`-named pending fragment in `changelog.d/` and reinstate `changelog.py check` in the inventory: the control PASSES, because the marker matched.
4. Empty `changelog.d/` and repeat: it fails. Same code, opposite verdict, decided by which ids are in the tree.

## Proposed Fix

Replace the frozen alternation with a RESOLVING marker - one function, `_real_tree_ids(text, root)`, that the fixture guard and the boundary control both call. It extracts the id SHAPES `\b(?:BG|US|CR|RFC|EP)\d{4,}\b` and keeps only those `sdlc_md.find_by_id(root, id)` resolves to an artefact, so it cannot go stale as ids advance and an id-shaped string naming nothing is not evidence. Artefact homes are the ones `find_by_id` reads: a `changelog.d/` fragment or a file under `.claude/worktrees/` named for an id does not make that id resolve. RUN- ids stay in the marker on purpose - they are how a verb reading the ledger shows it read this tree - with the shape `\bRUN-[0-9A-Z]{8,}\b`, resolving when a file in the root's `sdlc-studio/retros/` names the run (every closed run's retro carries its id, and the retros are tracked, so a clean clone resolves them too).

Both call sites resolve against the REAL repository root (`RootIsReadNotJustParsed.REPO`), never the fixture. The guard points a verb at an EMPTY fixture and asks whether the answer names this repository: resolved against the fixture, nothing ever resolves and the guard passes every leak (measured by the plan reviewer: 1 of 9 verbs caught against the fixture, 9 of 9 against the real tree).

## Acceptance Criteria

- [ ] **AC1** Given a fixture tree the test populates with one artefact per family, each numbered beyond EVERY range this corpus holds - `BG9001`, `US9001`, `CR9001`, `RFC9001`, `EP9001`, each a minimal file with its header in its type's home - and a retro naming `RUN-01ZZZZZZ`, when `_real_tree_ids` is asked about text naming all six with that fixture as its root, then it returns all six. One per family, because the inventory's verbs do not share one: `flow.py compute` names only US ids and `ac_scope.py check` only US and EP
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py::RealTreeMarkerTests::test_an_id_beyond_every_range_the_corpus_holds_is_recognised
- [ ] **AC2** Given AC1's populated fixture, when `_real_tree_ids` is asked about near misses - `BG9002` and `RUN-01YYYYYY`, id-shaped and in no artefact home or retro; `BG9003`, named only by a `changelog.d/BG9003.md` fragment; and `BG9004`, present only as `.claude/worktrees/w/sdlc-studio/bugs/BG9004-x.md` - then it returns none of them, while in the same call it still returns `BG9001`, the positive control that the empty answer is not a marker that recognises nothing. Widening the pattern until everything matches passes AC1 and destroys the thing the marker is for, which is telling a real-tree answer from any answer
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py::RealTreeMarkerTests::test_an_id_shaped_string_that_resolves_to_nothing_is_refused
- [ ] **AC3** Given the fixture guard (`test_no_verb_answers_about_the_real_tree_when_pointed_at_a_fixture`) run with its verb runner (`_run`) replaced by one returning a real-tree answer - text naming one artefact id and nothing else, no repository path: the id of the highest-numbered file in this repository's own `sdlc-studio/bugs/`, which the test asserts does NOT match `BG05\d\d|US06\d\d|RUN-01K\w+`, so the leak lies outside every frozen range and an in-range id cannot let a guard still on the frozen marker pass - when the guard runs, then it FAILS and its message names that id; and with the runner returning text that names only `BG9999`, which resolves nowhere, the guard passes. The guard is the thing the marker serves: it points a verb at an EMPTY fixture, so it must resolve what leaked against the REAL tree, where a leaked id exists. The guard is run on a separate test-case instance through its own `TestResult`, never on `self`, because pytest records subtests natively and its subtest failures would otherwise land on the outer test
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py::RealTreeMarkerTests::test_the_fixture_guard_resolves_leaked_ids_against_the_real_tree
- [ ] **AC4** Given a COPY of this repository (the working tree is never touched), when the boundary control runs every listed verb against that copy twice - once with `changelog.d/` emptied, once holding a single fragment the test mints and names for an id that resolves in the copy - then the control is GREEN in both states: every listed verb names at least one id `_real_tree_ids` resolves in the copy. Not merely equal - two red verdicts are equal too. The test is `@boundary_only`, because it costs at least twice the 83s control plus the copy; until BG0664 lands the pre-push gate runs no boundary-only test, which is pre-existing and filed there
  - **Verify:** pytest .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py::RealTreeMarkerTests::test_the_control_is_green_in_either_fragment_state

## Impact

The control exists so that a clean run of the fixture guard means the `--root` flag was obeyed, rather than that the verb prints nothing either way. As ids advance it quietly stops being able to tell those apart, and it takes the fixture guard's meaning with it - while continuing to pass.

## Test Plan

| Criterion | Mutant - the production change this test must fail on | Title |
| --- | --- | --- |
| AC1 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, keep the frozen `BG05\d\d`, `US06\d\d`, `RUN-01K\w+` alternation as `_real_tree_ids`' candidate pattern and add the `find_by_id` step after it, so a `BG9001` is never extracted to be resolved | Given a fixture tree the test populates with one artefact per family, each numbered beyond EVERY range this corpus holds - `BG9001`, `US9001`, `CR9001`, `RFC9001`, `EP9001`, each a minimal file with its header in its type's home - and a retro naming `RUN-01ZZZZZZ`, when `_real_tree_ids` is asked about text naming all six with that fixture as its root, then it returns all six. One per family, because the inventory's verbs do not share one: `flow.py compute` names only US ids and `ac_scope.py check` only US and EP |
| AC1 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, narrow the candidate shape to `(?:BG\|US)\d{4}`, the two families the frozen marker knew, so a CR, RFC or EP id is never extracted | Given a fixture tree the test populates with one artefact per family, each numbered beyond EVERY range this corpus holds - `BG9001`, `US9001`, `CR9001`, `RFC9001`, `EP9001`, each a minimal file with its header in its type's home - and a retro naming `RUN-01ZZZZZZ`, when `_real_tree_ids` is asked about text naming all six with that fixture as its root, then it returns all six. One per family, because the inventory's verbs do not share one: `flow.py compute` names only US ids and `ac_scope.py check` only US and EP |
| AC1 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, drop the `RUN-` alternative from `_real_tree_ids`, so a ledger-reading verb that names only run ids reads as naming nothing | Given a fixture tree the test populates with one artefact per family, each numbered beyond EVERY range this corpus holds - `BG9001`, `US9001`, `CR9001`, `RFC9001`, `EP9001`, each a minimal file with its header in its type's home - and a retro naming `RUN-01ZZZZZZ`, when `_real_tree_ids` is asked about text naming all six with that fixture as its root, then it returns all six. One per family, because the inventory's verbs do not share one: `flow.py compute` names only US ids and `ac_scope.py check` only US and EP |
| AC2 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, replace the `find_by_id` step in `_real_tree_ids` with a bare shape match, so any id-shaped string is accepted as real | Given AC1's populated fixture, when `_real_tree_ids` is asked about near misses - `BG9002` and `RUN-01YYYYYY`, id-shaped and in no artefact home or retro; `BG9003`, named only by a `changelog.d/BG9003.md` fragment; and `BG9004`, present only as `.claude/worktrees/w/sdlc-studio/bugs/BG9004-x.md` - then it returns none of them, while in the same call it still returns `BG9001`, the positive control that the empty answer is not a marker that recognises nothing. Widening the pattern until everything matches passes AC1 and destroys the thing the marker is for, which is telling a real-tree answer from any answer |
| AC2 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, replace the `find_by_id` step with `any(root.rglob(f"{id}*"))`, so a changelog fragment or a file under `.claude/worktrees/` makes an id resolve | Given AC1's populated fixture, when `_real_tree_ids` is asked about near misses - `BG9002` and `RUN-01YYYYYY`, id-shaped and in no artefact home or retro; `BG9003`, named only by a `changelog.d/BG9003.md` fragment; and `BG9004`, present only as `.claude/worktrees/w/sdlc-studio/bugs/BG9004-x.md` - then it returns none of them, while in the same call it still returns `BG9001`, the positive control that the empty answer is not a marker that recognises nothing. Widening the pattern until everything matches passes AC1 and destroys the thing the marker is for, which is telling a real-tree answer from any answer |
| AC2 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, change the `RUN-` rule to accept any string of that shape without reading `sdlc-studio/retros/`, so an unrecorded run id counts | Given AC1's populated fixture, when `_real_tree_ids` is asked about near misses - `BG9002` and `RUN-01YYYYYY`, id-shaped and in no artefact home or retro; `BG9003`, named only by a `changelog.d/BG9003.md` fragment; and `BG9004`, present only as `.claude/worktrees/w/sdlc-studio/bugs/BG9004-x.md` - then it returns none of them, while in the same call it still returns `BG9001`, the positive control that the empty answer is not a marker that recognises nothing. Widening the pattern until everything matches passes AC1 and destroys the thing the marker is for, which is telling a real-tree answer from any answer |
| AC3 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, change the root handed to `_real_tree_ids` in `test_no_verb_answers_about_the_real_tree_when_pointed_at_a_fixture` from `self.REPO` to the fixture root `d`, so nothing a verb leaks can resolve | Given the fixture guard (`test_no_verb_answers_about_the_real_tree_when_pointed_at_a_fixture`) run with its verb runner (`_run`) replaced by one returning a real-tree answer - text naming one artefact id and nothing else, no repository path: the id of the highest-numbered file in this repository's own `sdlc-studio/bugs/`, which the test asserts does NOT match `BG05\d\d\|US06\d\d\|RUN-01K\w+`, so the leak lies outside every frozen range and an in-range id cannot let a guard still on the frozen marker pass - when the guard runs, then it FAILS and its message names that id; and with the runner returning text that names only `BG9999`, which resolves nowhere, the guard passes. The guard is the thing the marker serves: it points a verb at an EMPTY fixture, so it must resolve what leaked against the REAL tree, where a leaked id exists. The guard is run on a separate test-case instance through its own `TestResult`, never on `self`, because pytest records subtests natively and its subtest failures would otherwise land on the outer test |
| AC3 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, drop the `_real_tree_ids` call from the fixture guard and keep only its `str(self.REPO) in out` check, so an answer naming an artefact id with no path passes | Given the fixture guard (`test_no_verb_answers_about_the_real_tree_when_pointed_at_a_fixture`) run with its verb runner (`_run`) replaced by one returning a real-tree answer - text naming one artefact id and nothing else, no repository path: the id of the highest-numbered file in this repository's own `sdlc-studio/bugs/`, which the test asserts does NOT match `BG05\d\d\|US06\d\d\|RUN-01K\w+`, so the leak lies outside every frozen range and an in-range id cannot let a guard still on the frozen marker pass - when the guard runs, then it FAILS and its message names that id; and with the runner returning text that names only `BG9999`, which resolves nowhere, the guard passes. The guard is the thing the marker serves: it points a verb at an EMPTY fixture, so it must resolve what leaked against the REAL tree, where a leaked id exists. The guard is run on a separate test-case instance through its own `TestResult`, never on `self`, because pytest records subtests natively and its subtest failures would otherwise land on the outer test |
| AC3 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, leave the fixture guard calling `_REAL_TREE_MARKER.findall` instead of `_real_tree_ids`, while `_real_tree_ids` and the boundary control are fixed | Given the fixture guard (`test_no_verb_answers_about_the_real_tree_when_pointed_at_a_fixture`) run with its verb runner (`_run`) replaced by one returning a real-tree answer - text naming one artefact id and nothing else, no repository path: the id of the highest-numbered file in this repository's own `sdlc-studio/bugs/`, which the test asserts does NOT match `BG05\d\d\|US06\d\d\|RUN-01K\w+`, so the leak lies outside every frozen range and an in-range id cannot let a guard still on the frozen marker pass - when the guard runs, then it FAILS and its message names that id; and with the runner returning text that names only `BG9999`, which resolves nowhere, the guard passes. The guard is the thing the marker serves: it points a verb at an EMPTY fixture, so it must resolve what leaked against the REAL tree, where a leaked id exists. The guard is run on a separate test-case instance through its own `TestResult`, never on `self`, because pytest records subtests natively and its subtest failures would otherwise land on the outer test |
| AC4 | in .claude/skills/sdlc-studio/scripts/tests/test_cli_grammar.py, move `("changelog.py", ("check",))` out of `WITHDRAWN_ROOT_EFFECT_VERBS` and back into `ROOT_EFFECT_VERBS`, on the reading that the resolving marker cured it - it names a fragment only while `changelog.d/` holds one | Given a COPY of this repository (the working tree is never touched), when the boundary control runs every listed verb against that copy twice - once with `changelog.d/` emptied, once holding a single fragment the test mints and names for an id that resolves in the copy - then the control is GREEN in both states: every listed verb names at least one id `_real_tree_ids` resolves in the copy. Not merely equal - two red verdicts are equal too. The test is `@boundary_only`, because it costs at least twice the 83s control plus the copy; until BG0664 lands the pre-push gate runs no boundary-only test, which is pre-existing and filed there |

## Revision History

| Date | Author | Change |
| --- | --- | --- |
| 2026-09-11 | Claude Opus 5 | Filed |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r1 REJECT answered. BLOCKING: new AC3 pins that the fixture guard resolves leaked ids against the REAL repository root - the guard is run with its verb runner returning a real-tree answer (an id from this repo's own bugs, no path) and must fail naming it, and with an id resolving nowhere must pass - with the mutant handing the fixture root to the resolving marker in the guard, plus one that keeps only the repo-path check. Issues: AC1 mints one id per family (BG, US, CR, RFC, EP beyond every range) plus a retro-recorded RUN- id and its test name says so; AC2 uses AC1's populated tree for near misses and states changelog.d/ and .claude/worktrees/ are not artefact homes, with BG9001 as its positive control; AC4 (was AC3) mints its own resolving fragment, asserts the control GREEN in both states rather than equal, works in a copy of the tree and is boundary_only, its mutant phrased as a move out of WITHDRAWN_ROOT_EFFECT_VERBS. RUN- ids kept in the marker on purpose, resolving through the tracked retros. BG0664 dependency for boundary-only tests noted as pre-existing. |
| 2026-09-15 | sprint plan repair 2026-09-15 | Plan review r2 REJECT answered (one BLOCKING). AC3 now takes the leaked id from the highest-numbered file in sdlc-studio/bugs/ and asserts it does not match the frozen BG05/US06/RUN-01K alternation, so an in-range id cannot let a guard still on the frozen marker pass. New AC3 row: the fixture guard left calling `_REAL_TREE_MARKER.findall` instead of `_real_tree_ids`. AC3 also states the guard runs on a separate instance through its own TestResult, because pytest records subtests natively. test_cli_grammar.py was already the only Affects entry. |
