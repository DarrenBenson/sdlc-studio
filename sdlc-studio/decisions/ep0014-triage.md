# Decisions Ledger - ep0014-triage

> Append-only. One row per ruling; earlier rulings are never rewritten.

| At | Decision | Rationale |
| --- | --- | --- |
| 2026-07-08T22:55:47Z | Declared inter-story Depends-on edges from the groomed design notes (D0015): US0066 -> US0065; US0065/US0067/US0068 independent. Waves now W1{US0065,US0067,US0068} parallel, W2{US0066}. | Planner collapsed to a flat single wave because no Depends-on was declared. Design notes: US0066 needs US0065's triaged state; US0067 is creation-time (independent); US0068 fully independent. This is the --goal design grooming the sprint reference prescribes for a collapsed wave. Finer than LATEST.md's coarse 'US0065 then the other three'. |
| 2026-07-08T22:55:47Z | Dismissed tranche-audit cross-epic-ac flag on US0065 as a benign false positive. | ac_scope heuristic matched the word 'structured' in 'structured triaged_by' against EP0013's title 'Structured authorship...'. The AC means a typed/validated triaged_by field, not the EP0013 authorship capability. Advisory heuristic, shared domain vocabulary; AC left unchanged. |
| 2026-07-08T23:01:44Z | US0065 vocab interpretation: under schema_version:3, prepend 'inbox' to bug/cr/rfc STATUS_VOCAB. The 'triaged' transition target is type-specific per D0015: bug->Open, cr->Approved, rfc->In Review. Proposed(cr)/Draft(rfc) remain for human-authored artefacts; agent-filed FINDINGS go inbox->triaged-target, skipping the human proposal state (findings-only scope). | D0015 phrase 'first workflow state (Open/Approved/In Review)' conflicts literally with cr first-state=Proposed, rfc first-state=Draft. Resolved by the operator's model: workflow proper begins at the accepted state (Approved/In Review/Open); Proposed/Draft are pre-workflow proposal states an agent finding never occupies. Safe reversible default grounded in D0015's explicit target list; era-gated dormant (repo is v2), no release. |
| 2026-07-08T23:21:12Z | Critic REJECT on US0065 resolved: (#1) file_finding.py now files findings into inbox under v3 (was the escape - it is the primary agent finding path, artifact.py alone was insufficient); (#2) the triage gate now fires on ANY exit from inbox, not just the canonical accept target, so an agent cannot sidestep triage via inbox->In Progress/terminal; (#3) the gate is honest under --dry-run. Tests added for all three. | Adversarial full-diff critic (independent instance) reproduced #1 and #2 as MAJOR escapes of AC1's separation-of-duties invariant. Fixed and re-verified. |
| 2026-07-08T23:21:12Z | Deferred (out of US0065 scope): critic note #4 - tool-filed findings carry no structured Raised-by, so the transition SoD check is vacuous for them until an authorship-stamping story (EP0013 territory) lands. US0065's SoD is correct and fires whenever Raised-by is present; validate.py already flags a missing v3 Raised-by separately. | Stamping Raised-by on creation is the EP0013 authorship layer, not the triage vocab/gate US0065 owns. Recorded so it is not lost. |
