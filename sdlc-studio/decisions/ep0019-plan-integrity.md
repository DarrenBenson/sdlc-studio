# Decisions Ledger - ep0019-plan-integrity

> Append-only. One row per ruling; earlier rulings are never rewritten.

| At | Decision | Rationale |
| --- | --- | --- |
| 2026-07-09T08:34:50Z | Sprint batch = CR0194 (High) + CR0195 (Med) + CR0196 (Med), decomposed to US0090 (trigger+gate), US0091 (charter+verdict+telemetry, depends US0090), US0092 (spec-edit block), US0093 (bench phase). CR0197 deferred - it depends on CR0194's gate existing. Waves: W1{US0090, US0093}, W2{US0091, US0092}, then a closing full-diff pass. | Operator picked the plan-integrity theme plus the cheap independent bench fix. CR0194 splits into gate vs charter because both touch shared files (critic.py, plan_review.py) and the charter records into the gate. US0093 is repo-only (tools/bench), no shared skill files, so it runs truly parallel in W1. Shared-file collisions on config.py / critic prompt template / reference docs force US0090/US0091/US0092 to serialise as orchestrator, closing pass per the RETRO0012 lesson. |
| 2026-07-09T08:34:50Z | D1 (CR0194 open decision - seat): the plan-review is a distinct PHASE, not a new seat type. It is rendered under the existing QA/tester seat (a separate instance from the plan's author) and recorded via `critic.py record --phase plan-review`; the mechanical author != reviewer gate (CR0117) is reused unchanged. | Reuses the RFC0021 role-based seat model and the existing independence gate rather than inventing a parallel actor. Minimal new surface; the QA seat is already the AC-integrity owner. Blind re-derivation stays available as an escalation, not a new seat. |
| 2026-07-09T08:34:50Z | D2 (CR0194 open decision - trigger): fire on ANY of three deterministic signals (OR): an AC/Affects citing a configured spec path, `affects_files >= plan_review.affects_files_threshold` (default 5), or routed difficulty band >= `plan_review.min_difficulty` (default medium). No model judgement in fire/skip (TRD ADR-006). | The benchmark's headline finding is that judgement-scaled hygiene is skipped 10/10 under pressure; a dodgeable trigger is the same failure at one remove. OR-of-signals is the hardest to dodge. Thresholds live in config so a project can tune cost. Era-gated v3 (dormant on v2, freeze holds). |
| 2026-07-09T08:34:50Z | D3 (CR0194 open decision - reviewer mode): default is challenge-the-written-ACs against the cited source section (cheaper, ADR-006-friendly), with the charter REQUIRING the reviewer to re-read the cited section and flag any AC that contradicts/inverts it. Blind re-derivation is an escalation for difficulty >= high. | Captures the inversion-catch value of blind re-derivation (the R5 failure was an inverted rule) without paying its full cost on every triggered unit. The mandatory re-read of the source is what turns anchoring risk into a real check. |
| 2026-07-09T08:34:50Z | Tranche audit: 3/3 ready, 0 not. Reconcile drift 0 at plan time. | Recorded at the Triage STOP; operator approved execution with no answers to D1-D3, so they were resolved here with recorded rationale as the design rung prescribes. |
| 2026-07-09T09:10:37Z | US0093 (bench phase, W1) delivered + independent critic (qa-seat) APPROVE. Calibration-isolation invariant verified on the real runs.jsonl (n=5 cells, not n=6). 3 findings fixed: MEDIUM missing CHANGELOG entry; LOW --include-phase now validates values (a typo failed silently to measured-only); LOW record() no longer mutates the caller's dict. | Repo-only tooling; critic re-checked the exclusion holds under adversarial outliers. |
| 2026-07-09T09:10:37Z | US0090 (plan-review gate, W1) delivered + independent critic REJECT->fix->APPROVE (same critic re-ran its repros). Escapes the author's tests missed: (MAJOR) the gate fired only on entry to In Progress, so a spec-derived v3 story could be closed straight Ready->Done unreviewed - fixed to fire on entry to any of {In Progress, Review, Done}; (MEDIUM) stale-approval TOCTOU - the verdict bound to story id not AC content, so approve-benign-then-invert rode the stale APPROVE - fixed with an ac_fingerprint pin (record_review/`plan_review record` stamps ac-hash; a later AC edit invalidates the verdict); (MEDIUM) cites_spec under-fired on extension-less spec-dir refs (requirements/r5) - _PATH_RE broadened; (LOW) `-` sentinel counted as an override; (LOW) delivery critic-verdicts header prose drifted - restored byte-identical. | The independence gate earned its keep again: an instance that did not write the code found a complete bypass of the gate's PURPOSE (Ready->Done) that all author tests passed over. Re-verified by the same critic re-running its own reproductions. |
| 2026-07-09T09:10:37Z | Residual from US0090 re-review (MEDIUM, closed in US0090): the pinned record_review path was unreachable from shipped docs - reference-config.md/reference-scripts.md routed to the unpinned `critic record --phase plan-review`. Fixed: added `plan_review record` CLI (pins the AC fingerprint), pointed both docs at it, noted the bare critic form is unpinned back-compat. Loop-doctrine wiring (reference-sprint.md instructs the loop to use it) belongs to US0091. | The gate's anti-tampering value is only real if the sanctioned recording path is the one operators are told to use. |
| 2026-07-09T09:10:37Z | reference-scripts.md deliberately allowlisted at 633 (+plan_review.py entry). The catalogue has now grown three sprints running past its 600 ceiling; a split is overdue and should be its own follow-up CR, not smuggled into a feature sprint. | Scope discipline: the split is a distinct refactor. Recorded so it is not lost (carried forward from RETRO0012). |
| 2026-07-09T09:40:21Z | US0091 (charter+slot+telemetry, W2) delivered + independent critic REJECT->fix->APPROVE (3 rounds). Findings: MEDIUM plan-review telemetry events polluted the unit-close summarise aggregate (phantom 'unknown' type) - fixed by excluding event-bearing records and adding a dedicated plan_review summary block; MEDIUM missing CHANGELOG; LOW independence flag diverged from critic.is_independent; LOW literal phase field. The critic's re-review then caught a REGRESSION my fix introduced: telemetry.py lacked sys.path.insert and imported critic OUTSIDE the try/except, so record_plan_review raised on a clean interpreter (test_telemetry green only via cross-test path pollution) - fixed (module-top sys.path.insert + import-inside-try/except with a semantics-matching fallback). | A fix can introduce its own escape; the same critic re-running its repro in ISOLATION caught what the order-dependent full-suite green hid. |
| 2026-07-09T09:40:21Z | US0092 (spec-edit guard, W2) delivered + independent critic REJECT->fix->APPROVE. MAJOR under-flag: traceability was story-wide, so an untraced edit to spec file B read as traced whenever the story mentioned ANY spec path (a Verify grep line or Affects header naming a different spec) - the exact source-of-truth-falsification direction CR0195 forbids. Fixed to PER-edited-file matching; a referenced same-file edit is surfaced (not silently cleared) with 'a reference is not a change-request' messaging. MEDIUM docs overstated precision + MEDIUM under-flag untested (both fixed); LOW root SPEC.md now detected (*spec*.md synced across code + config). Residual by-design: basename-collision leniency (surfaced, not silently cleared) - noted for a future path-aware tightening. | Per-unit critic caught a defeatable gate through ordinary story content; the dangerous direction (under-flag) was entirely untested until the critic forced it. |
