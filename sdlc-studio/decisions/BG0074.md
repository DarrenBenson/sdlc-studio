# Decisions Ledger - BG0074

> Append-only. One row per ruling; earlier rulings are never rewritten.

| At | Decision | Rationale |
| --- | --- | --- |
| 2026-07-09T22:57:28Z | Alias-scan belt in allocate_number not implemented | After the stamp fix the numeric allocator is unreachable on a migrated project; the belt would put an O(corpus) full-file read on every allocation (the BG0070 cost class). Stamp is the fix. |
