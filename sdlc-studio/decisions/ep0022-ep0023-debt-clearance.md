# Decisions Ledger - ep0022-ep0023-debt-clearance

> Append-only. One row per ruling; earlier rulings are never rewritten.

| At | Decision | Rationale |
| --- | --- | --- |
| 2026-07-09 | Sprint = the held RV0006 debt + context tiering (operator picked "clear the remaining backlog"). CR0186 -> US0100 (supply-chain: SHA pins + installer checksum) + US0101 (sync/state/verifier hardening); CR0187 -> US0102 (shared-layer + CLI JSON parity) + US0103 (hotspot decomposition + cleanups, depends US0102); CR0179 -> US0104 (context tiering). Epics: EP0022 (debt) + EP0023 (feature). | Freeze-consistent: completing the backlog is the precondition for cutting v4.0 (CR0198). The two RV0006 buckets are diffuse but their items are decomposable into checkable stories; grouped by theme (2 stories each) rather than one story per item. |
| 2026-07-09 | Housekeeping: CR0172 + CR0173 transitioned Proposed -> Complete. They were delivered by EP0014 (US0065-US0068, the triage vocab / noise controls / record-only tranche / sampling per RETRO0012) but their status was never flipped - semantic drift the mechanical reconcile does not catch. | Files-are-truth: a delivered CR must not linger Proposed and mislead the backlog. Closed, not re-planned. |
| 2026-07-09 | US0103 depends on US0102 (both refactor the reconcile.py / shared-layer surface; the hotspot decomposition builds on the consolidated lookups). US0102/US0103/US0104 all touch reconcile.py (docstring, hotspots, a digest drift-check hook respectively), so they serialise as orchestrator even where no hard Depends-on is declared. Waves: W1{US0100, US0101, US0102, US0104}, W2{US0103}, then a closing pass. | US0100 (workflows/install) and US0101 (github_sync/verify_ac/mutation) touch disjoint files - genuinely parallel. The reconcile.py collision is handled by sequential orchestration + a closing full-diff pass, per the established pattern. |
| 2026-07-09 | Weakly-testable ACs acknowledged up front: CR0186 AC1 ("CI still green") and CR0187 AC1 ("no behaviour change") are structural/regression checks, not feature ACs. Each story's Verify is a grep/structural check or the existing suite staying green; the adversarial critic judges behaviour-preservation. | These are debt/remediation items, not new behaviour; the honest Verify is "the guard fails on a regression" and "the existing tests still pass", not a new feature test. |
| 2026-07-09 | Dismissed the tranche-audit `cross-epic-ac` flag on US0101 as a benign false positive; `US0103 sequenced-in-batch` is the W1->W2 wave. | `ac_scope` is a repo-wide heuristic; US0101's security ACs share generic vocabulary (mutation, state, http) with other epic titles. The story is correctly scoped to CR0186 (github_sync/verify_ac/mutation). Advisory only - same class as the dismissals in EP0014/EP0020. Aside: the planner emitted the `\|` SyntaxWarning that US0103 AC2 exists to fix - the debt is real and on the ledger. |
