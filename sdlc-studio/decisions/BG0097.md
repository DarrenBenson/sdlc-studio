# Decisions Ledger - BG0097

> Append-only. One row per ruling; earlier rulings are never rewritten.

| At | Decision | Rationale |
| --- | --- | --- |
| 2026-07-10T00:47:57Z | Session incident: critic contamination + broken env shim | Unit critic's section-D repro wrote core.hooksPath into the real XDG global git config (its HOME-only sandbox missed XDG_CONFIG_HOME); observed as a false 'pre-existing operator config' finding; remediated (--global --unset), critic confirmed and fixed its tooling. Separate operator-machine hazard surfaced: ~/.local/bin/env exits 0 without exec'ing its arguments, no-op'ing 'env -u VAR cmd' - flag to operator. |
