# Decisions Ledger - US0194

> Append-only. One row per ruling; earlier rulings are never rewritten.

| At | Decision | Rationale |
| --- | --- | --- |
| 2026-07-16T23:13:32Z | Two-role gate semantics: Done means signed off. Units hold at Review mid-run (verdict + evidence recorded, code committed green); the reviewer-of-record sign-off at the close records per-unit signoff rows and only then do units transition Done. conformance critiqued (two-role active) = evidence + independent sign-off, activated forward-only via review.two_role_after. | CR0323 AC1 binds sign-off into conformance critiqued, but a per-unit operator sign-off mid-run would stall the autonomous loop and fail pre-commit gates; RFC0044 chose close-level sign-off (seams sprint precedent). Forward-only cutoff per L-0043 (a new gate must not break existing projects). |
