# Decisions Ledger - ep0020-upgrade-rebaseline

> Append-only. One row per ruling; earlier rulings are never rewritten.

| At | Decision | Rationale |
| --- | --- | --- |
| 2026-07-09 | Sprint batch = CR0197 (Med) + CR0200 (Low, the reference-scripts split filed in RETRO0012/0013) + BG0068 (Med bug). CR0197 -> US0094 (census+report) + US0095 (backfill+policy, depends US0094); CR0200 -> US0096 (catalogue split); BG0068 is a standalone bug unit. Waves: W1{US0094, US0096, BG0068}, W2{US0095}, then a closing pass. | Operator picked the plan-integrity continuation. CR0197 is now ripe - it references CR0194's plan-review gate (delivered) for its re-review bucket. US0094/US0095 both touch project_upgrade.py so they serialise; US0096 (docs) and BG0068 (decisions.py) are independent. |
| 2026-07-09 | D1 (CR0197 open decision - home): a reusable `rebaseline()` helper in `project_upgrade.py`, invoked by `project upgrade`. A `reconcile detect --era` lens (the recurring cadence) is a NOTED FOLLOW-UP, not this sprint. | Upgrade is the one-shot moment the capability delta is known; scoping to it keeps the sprint bounded while factoring the helper so reconcile can adopt it later. |
| 2026-07-09 | D2 (CR0197 open decision - re-review marker): report-only this sprint - the re-review queue lives in the report/index, no per-artifact marker written. | The gate already enforces at the next transition by reading the LIVE artifact, so a persisted marker is redundant for enforcement and would mutate in-flight artifacts (churn). A per-artifact marker is a follow-up if a project wants the flag to survive in-file. |
| 2026-07-09 | D3 (CR0197 open decision - adopt_after): ONE era watermark this sprint; per-capability watermarks are a follow-up. | Simplest correct default: the plan-review gate and routing both key off `schema_version: 3` today, so one watermark suffices. Staggered per-capability adoption (routing at v3.5, plan-review at v4.0) is a real but separate design. |
| 2026-07-09 | CR0200 (catalogue split) scoped as documentation-only: reference-scripts.md becomes a lean index + grouped detail pages, the 643 budget allowlist is removed, doc-coverage stays at 0 undocumented, no script behaviour changes. | Third sprint running past the 600 ceiling; the split is now due. Kept a pure doc reorganisation so it carries no code risk. |
| 2026-07-09 | Dismissed the tranche-audit `cross-epic-ac` flag on US0095 as a benign false positive; `unmet-deps: US0094` is `sequenced-in-batch` (the W1->W2 wave). | `ac_scope` is a repo-wide heuristic; US0095's ACs share generic SDLC vocabulary (schema, policy, artifacts, change, scripts) with several epic titles. The story is correctly scoped to CR0197/EP0020 (project_upgrade.py); no ACs are misfiled. Advisory only - same class as US0065's 'structured' match last sprint. |
| 2026-07-09 | Replan at operator request: fold ALL open bugs into the tranche. Added BG0069 (install-path gate real-wrapper tests fail from an install - the failure seen on the EP0019 forward-port). Batch now US0094, US0095, US0096, BG0068, BG0069. Waves: W1{US0094, US0096, BG0068, BG0069}, W2{US0095}. | The two open bugs are BG0068 (already in) + BG0069; both now ride the tranche. BG0069 is independent (tools-side test hardening), a clean W1 unit. Tranche audit 4/5 ready (the 1 not-ready is the already-dismissed US0095 cross-epic-ac false positive). |
| 2026-07-09 | Operator folded in the 3 bounded EP0018 debt CRs: CR0181 -> US0097 (route github_sync/verify_ac through the shared discovery layer), CR0182 -> US0098 (consolidate the two archive impls on iter_tables), CR0188 -> US0099 (origin-drift pre-flight for sprint plan + id allocation). New epic EP0021 (Tooling and shared-layer hardening) houses them; the sprint now spans EP0020 + EP0021 + BG0068/BG0069 (8 units). Held back CR0179 (context-tiering feature), CR0186/CR0187 (diffuse RV0006 remediation grab-bags, weakly-testable ACs) for a dedicated debt pass. Waves: W1{US0094, US0096, US0097, US0098, US0099, BG0068, BG0069} - all touch disjoint files, genuinely parallel - W2{US0095}. | Operator wants a wider clear-the-decks sprint. The three folded CRs are bounded with testable ACs and share the tooling-hardening flavour; the three held back are either a feature or diffuse buckets ill-suited to the TDD+critic loop. Tranche audit 7/8 ready (the 1 not-ready is the already-dismissed US0095 cross-epic-ac false positive). |
| 2026-07-09T15:21:59Z | All 8 units delivered + committed, each independent-critic REJECT/APPROVE re-verified. Escapes the author's tests missed, by unit: BG0068 (over-lenient id parse - stray-digit typo silently flipped a row); BG0069 (the NEW locking test itself failed from an install, recreating the misleading FAILED the bug kills); US0094 (Verify count was file-wide not AC-section-scoped, masking a real gap; a lint-blocking (CR0197) tag); US0097 (verify_ac discovery was only HALF done - the alias without the case-insensitive walk; a false-green Verify covering only github_sync); US0099 (remote id allocation silently no-op'd on non-main default branches - next_id.remote_ids hardcoded origin/main, the exact collision CR0188 prevents); US0095 (AC4 grep vs uppercase doc phrase; CRLF normalised on the real read path); US0098 (the shipped regression test passed on buggy code - view header lacked an ID column). | The independence gate earned its keep 8/8: every unit had a real defect the author's own green suite passed over, several in the dangerous under-report/under-fire direction. US0099's was a complete bypass of the anti-collision feature on any master/develop repo. |
