# Repair Records

> Append-only. What was DONE about a REJECT, recorded beside the verdict rather than
> replacing it: what the reviewer found stays true, and the disposition becomes visible.
> A repair closing fewer findings than the verdict raised is PARTIAL and names the rest.
> A finding closed by FILING an artefact is recorded as filed, with the id - 'fixed' and
> 'filed as a known issue' are both legitimate, and telling them apart afterwards is not
> optional.

| Unit | Verdict date | Author | Date | Closed | Outstanding | Phase | Rejection |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BG0492 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | the digest mixed two representations of identical bytes -> replaced with a git tree object built in a throwaway index, so staging cannot move it, pinned by test\_staging\_an\_unchanged\_tree\_does\_not\_invalidate\_the\_verdict which fails on the old shape; test\_the\_verdicts\_own\_output\_does\_not\_expire\_it did not kill the mutant its own docstring named -> the fixture no longer writes a .gitignore masking the pathspec, and the -f mutant is now killed by test\_an\_ignored\_file\_does\_not\_invalidate\_the\_verdict; --check <unknown-suite> was silently accepted -> refused now, on the same terms the run path already refuses an unknown suite, pinned by test\_an\_unknown\_suite\_is\_refused\_rather\_than\_defaulted | - | | |
| BG0499 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | critic.py sprint-review is a THIRD command that records a round -> wired to escalation\_notice, pinned by test\_two\_rejects\_through\_critic\_sprint\_review\_escalate which fails when the loop is dropped; the new reader's unit-matching predicate has ZERO cover -> pinned by test\_a\_batch\_row\_escalates\_only\_the\_units\_it\_names, which fails on the if-True mutant the reviewer used; the phase guard is also unpinned -> pinned by test\_a\_plan\_review\_round\_does\_not\_inherit\_delivery\_batch\_rounds; a REFUSED critic record still prints the ESCALATED line -> the loop now runs only when a round was written, pinned by test\_a\_refused\_record\_escalates\_nothing; sprint-review rows enter the round count with no supersession filter -> filed as a gap in the batch ledger's erratum story rather than widened into this unit; the split-panel branch is unreachable from a review-batch-only history -> pre-existing, recorded and not fixed here; sprint.panel\_escalation now has no production caller -> the docstring now says so plainly rather than claiming callers it does not have | - | | |
| BG0502 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | understated footprint - sprint\_report.py holds the whole DEFERRED section -> declared in the unit's Affects, so critic.py brief now hands a reviewer the file implementing the criterion; AC1 says the section NAMES each filed artefact and the test asserted only the heading -> the test reads the filed ids off disk and asserts each appears in the DEFERRED block, killing both mutants that survived; prose - the changelog quoted the marker as filed-not-waived -> the fragment now quotes both markers accurately; the sibling imports sit outside the report emitter's try -> filed as BG0508, pre-existing and outside this unit's criteria | - | | |
| US0616 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #1 -> fixed: change enumeration rebuilt on git plumbing - diff HEAD --name-only plus ls-files --others - so a rename is reported as the names it is, pinned by test\_a\_renamed\_batch\_file\_is\_caught; #2 -> fixed: ls-files --others lists untracked FILES, expanding the new-directory case by construction, pinned by test\_a\_new\_module\_inside\_a\_new\_directory\_is\_caught; #3 -> fixed: Affects normalised through resolve\_affects with directory and glob matching, and the lstrip charset bug that disarmed the whole guard removed, pinned by test\_a\_directory\_declaration\_covers\_what\_sits\_under\_it; #4 -> fixed: a lane test drives a real close through main and asserts close\_tree reaches run-state, killing the reviewer's own mutant of all three call sites; #5 -> filed: BG0509; #6 -> fixed: recorded as a process note rather than code, the base ref for a mid-run review is the run's plan commit; #7 -> filed: BG0509 | - | | |
| US0617 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #1 -> fixed: change enumeration rebuilt on git plumbing - diff HEAD --name-only plus ls-files --others - so a rename is reported as the names it is, pinned by test\_a\_renamed\_batch\_file\_is\_caught; #2 -> fixed: ls-files --others lists untracked FILES, expanding the new-directory case by construction, pinned by test\_a\_new\_module\_inside\_a\_new\_directory\_is\_caught; #3 -> fixed: Affects normalised through resolve\_affects with directory and glob matching, and the lstrip charset bug that disarmed the whole guard removed, pinned by test\_a\_directory\_declaration\_covers\_what\_sits\_under\_it; #4 -> fixed: a lane test drives a real close through main and asserts close\_tree reaches run-state, killing the reviewer's own mutant of all three call sites; #5 -> filed: BG0509; #6 -> fixed: recorded as a process note rather than code, the base ref for a mid-run review is the run's plan commit; #7 -> filed: BG0509 | - | | |
| US0618 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #1 -> fixed: change enumeration rebuilt on git plumbing - diff HEAD --name-only plus ls-files --others - so a rename is reported as the names it is, pinned by test\_a\_renamed\_batch\_file\_is\_caught; #2 -> fixed: ls-files --others lists untracked FILES, expanding the new-directory case by construction, pinned by test\_a\_new\_module\_inside\_a\_new\_directory\_is\_caught; #3 -> fixed: Affects normalised through resolve\_affects with directory and glob matching, and the lstrip charset bug that disarmed the whole guard removed, pinned by test\_a\_directory\_declaration\_covers\_what\_sits\_under\_it; #4 -> fixed: a lane test drives a real close through main and asserts close\_tree reaches run-state, killing the reviewer's own mutant of all three call sites; #5 -> filed: BG0509; #6 -> fixed: recorded as a process note rather than code, the base ref for a mid-run review is the run's plan commit; #7 -> filed: BG0509 | - | | |
| US0619 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #1 -> fixed: change enumeration rebuilt on git plumbing - diff HEAD --name-only plus ls-files --others - so a rename is reported as the names it is, pinned by test\_a\_renamed\_batch\_file\_is\_caught; #2 -> fixed: ls-files --others lists untracked FILES, expanding the new-directory case by construction, pinned by test\_a\_new\_module\_inside\_a\_new\_directory\_is\_caught; #3 -> fixed: Affects normalised through resolve\_affects with directory and glob matching, and the lstrip charset bug that disarmed the whole guard removed, pinned by test\_a\_directory\_declaration\_covers\_what\_sits\_under\_it; #4 -> fixed: a lane test drives a real close through main and asserts close\_tree reaches run-state, killing the reviewer's own mutant of all three call sites; #5 -> filed: BG0509; #6 -> fixed: recorded as a process note rather than code, the base ref for a mid-run review is the run's plan commit; #7 -> filed: BG0509 | - | | |
| US0620 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #4 -> fixed: closure resolution now requires exactly one raised finding by ordinal, exact text or a 24-character prefix, refusing ambiguity, pinned by test\_a\_short\_closure\_cannot\_close\_every\_finding driven through the CLI; #5 -> fixed: the same resolver serves repair\_outstanding, so closing a short finding no longer closes a longer one, pinned by test\_closing\_a\_short\_finding\_does\_not\_close\_a\_longer\_one\_containing\_it; #6 -> fixed: four ClosureResolutionTests now pin the matching rule that had no cover at all; #7 -> fixed: the shipped preflight path honours a complete repair, pinned by test\_the\_shipped\_preflight\_stops\_calling\_a\_repaired\_unit\_uncovered which drives uncovered\_units rather than the private resolver; #8 -> fixed: US0624 Affects now declares sprint\_report.py, and its AC1 verifier drives the shipped path; #9 -> fixed: coverage\_state mirrors critiqued\_unmet exactly, honouring the pre-gate sentinel, so one question has one authority again; #10 -> fixed: coverage\_state consults sprint\_review\_for when no per-unit verdict exists, so a batch-reviewed unit is no longer named as the gap; #1 -> fixed: disposition is now stated with an explicit fixed or filed token, and the resolvable-id check runs over EVERY id the evidence names whatever the disposition; #2 -> fixed: a repair binds to the verdict date it answered, pinned by test\_a\_repair\_does\_not\_answer\_a\_LATER\_rejection; #3 -> fixed: the verb is documented in the script catalogue and the sprint toolchain runbook | - | | |
| US0621 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #4 -> fixed: closure resolution now requires exactly one raised finding by ordinal, exact text or a 24-character prefix, refusing ambiguity, pinned by test\_a\_short\_closure\_cannot\_close\_every\_finding driven through the CLI; #5 -> fixed: the same resolver serves repair\_outstanding, so closing a short finding no longer closes a longer one, pinned by test\_closing\_a\_short\_finding\_does\_not\_close\_a\_longer\_one\_containing\_it; #6 -> fixed: four ClosureResolutionTests now pin the matching rule that had no cover at all; #7 -> fixed: the shipped preflight path honours a complete repair, pinned by test\_the\_shipped\_preflight\_stops\_calling\_a\_repaired\_unit\_uncovered which drives uncovered\_units rather than the private resolver; #8 -> fixed: US0624 Affects now declares sprint\_report.py, and its AC1 verifier drives the shipped path; #9 -> fixed: coverage\_state mirrors critiqued\_unmet exactly, honouring the pre-gate sentinel, so one question has one authority again; #10 -> fixed: coverage\_state consults sprint\_review\_for when no per-unit verdict exists, so a batch-reviewed unit is no longer named as the gap; #1 -> fixed: disposition is now stated with an explicit fixed or filed token, and the resolvable-id check runs over EVERY id the evidence names whatever the disposition; #2 -> fixed: a repair binds to the verdict date it answered, pinned by test\_a\_repair\_does\_not\_answer\_a\_LATER\_rejection; #3 -> fixed: the verb is documented in the script catalogue and the sprint toolchain runbook | - | | |
| US0622 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #4 -> fixed: closure resolution now requires exactly one raised finding by ordinal, exact text or a 24-character prefix, refusing ambiguity, pinned by test\_a\_short\_closure\_cannot\_close\_every\_finding driven through the CLI; #5 -> fixed: the same resolver serves repair\_outstanding, so closing a short finding no longer closes a longer one, pinned by test\_closing\_a\_short\_finding\_does\_not\_close\_a\_longer\_one\_containing\_it; #6 -> fixed: four ClosureResolutionTests now pin the matching rule that had no cover at all; #7 -> fixed: the shipped preflight path honours a complete repair, pinned by test\_the\_shipped\_preflight\_stops\_calling\_a\_repaired\_unit\_uncovered which drives uncovered\_units rather than the private resolver; #8 -> fixed: US0624 Affects now declares sprint\_report.py, and its AC1 verifier drives the shipped path; #9 -> fixed: coverage\_state mirrors critiqued\_unmet exactly, honouring the pre-gate sentinel, so one question has one authority again; #10 -> fixed: coverage\_state consults sprint\_review\_for when no per-unit verdict exists, so a batch-reviewed unit is no longer named as the gap; #1 -> fixed: disposition is now stated with an explicit fixed or filed token, and the resolvable-id check runs over EVERY id the evidence names whatever the disposition; #2 -> fixed: a repair binds to the verdict date it answered, pinned by test\_a\_repair\_does\_not\_answer\_a\_LATER\_rejection; #3 -> fixed: the verb is documented in the script catalogue and the sprint toolchain runbook | - | | |
| US0623 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #4 -> fixed: closure resolution now requires exactly one raised finding by ordinal, exact text or a 24-character prefix, refusing ambiguity, pinned by test\_a\_short\_closure\_cannot\_close\_every\_finding driven through the CLI; #5 -> fixed: the same resolver serves repair\_outstanding, so closing a short finding no longer closes a longer one, pinned by test\_closing\_a\_short\_finding\_does\_not\_close\_a\_longer\_one\_containing\_it; #6 -> fixed: four ClosureResolutionTests now pin the matching rule that had no cover at all; #7 -> fixed: the shipped preflight path honours a complete repair, pinned by test\_the\_shipped\_preflight\_stops\_calling\_a\_repaired\_unit\_uncovered which drives uncovered\_units rather than the private resolver; #8 -> fixed: US0624 Affects now declares sprint\_report.py, and its AC1 verifier drives the shipped path; #9 -> fixed: coverage\_state mirrors critiqued\_unmet exactly, honouring the pre-gate sentinel, so one question has one authority again; #10 -> fixed: coverage\_state consults sprint\_review\_for when no per-unit verdict exists, so a batch-reviewed unit is no longer named as the gap; #1 -> fixed: disposition is now stated with an explicit fixed or filed token, and the resolvable-id check runs over EVERY id the evidence names whatever the disposition; #2 -> fixed: a repair binds to the verdict date it answered, pinned by test\_a\_repair\_does\_not\_answer\_a\_LATER\_rejection; #3 -> fixed: the verb is documented in the script catalogue and the sprint toolchain runbook | - | | |
| US0624 | 2026-08-03 | claude-opus-5-author | 2026-08-03 | #4 -> fixed: closure resolution now requires exactly one raised finding by ordinal, exact text or a 24-character prefix, refusing ambiguity, pinned by test\_a\_short\_closure\_cannot\_close\_every\_finding driven through the CLI; #5 -> fixed: the same resolver serves repair\_outstanding, so closing a short finding no longer closes a longer one, pinned by test\_closing\_a\_short\_finding\_does\_not\_close\_a\_longer\_one\_containing\_it; #6 -> fixed: four ClosureResolutionTests now pin the matching rule that had no cover at all; #7 -> fixed: the shipped preflight path honours a complete repair, pinned by test\_the\_shipped\_preflight\_stops\_calling\_a\_repaired\_unit\_uncovered which drives uncovered\_units rather than the private resolver; #8 -> fixed: US0624 Affects now declares sprint\_report.py, and its AC1 verifier drives the shipped path; #9 -> fixed: coverage\_state mirrors critiqued\_unmet exactly, honouring the pre-gate sentinel, so one question has one authority again; #10 -> fixed: coverage\_state consults sprint\_review\_for when no per-unit verdict exists, so a batch-reviewed unit is no longer named as the gap; #1 -> fixed: disposition is now stated with an explicit fixed or filed token, and the resolvable-id check runs over EVERY id the evidence names whatever the disposition; #2 -> fixed: a repair binds to the verdict date it answered, pinned by test\_a\_repair\_does\_not\_answer\_a\_LATER\_rejection; #3 -> fixed: the verb is documented in the script catalogue and the sprint toolchain runbook | - | | |
| BG0511 | 2026-08-04 | claude-opus-5-author | 2026-08-04 | #1 -> BLOCKING leg: scoped the criteria demand with sdlc\_md.executes\_verifiers so the census asks exactly the types transition asks, pinned in both polarities by test\_a\_type\_the\_deliverer\_never\_asks\_is\_not\_refused\_for\_criteria - cr, rfc and epic unrefused, story and bug refused. Re-checked on the live tree: breakdown --crs Proposed reports 0 ungroomed where the first draft refused all 12, and --stories Ready --bugs Open still reports 16 of 48. Non-blocking legs, all six closed: prefix matching replaced with whole-form regexes built from \_CRITERION\_FORM itself, pinned by test\_a\_fallback\_shaped\_authored\_criterion\_is\_not\_called\_derived which asserts an authored criterion opening The recorded outage is NOT derived while the real fallback form still is. The reason-code comment now states the actual coupling - the code decides WHETHER to filter, a prose match on sprint.\_AC\_MISS decides WHICH, and rewording it breaks 44 tests loudly rather than silently. The fail-open comment now says what the code does and why it matches transition's own construct. Affects corrected in both directions - file\_finding.py and the seven touched test modules added, test\_conformance.py removed as untouched. check\_groomed and grooming\_gaps docstrings rewritten to the narrowed contract they implement. Suite re-run after the repair: run-suite.sh all exit 0, 5897 passed | - | | |
| BG0558 | 2026-08-09 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> the test asserts breakdown\_mode is enforce and the DoR is non-empty before anything else; mutant re-applied and KILLED; #2 -> one tree holds a typo unit and a creation unit and the test asserts opposite CLI exit codes; #3 -> Ruling 1 names the signal: a basename existing elsewhere is a typo, one existing nowhere is a creation; #4 -> replaced with the over-widening mutant, fictional\_affects returning nothing; re-applied and KILLED; #5 -> the criterion names the surviving typo fixture; the message-shortening mutant SURVIVED and exposed a real gap, so the refusal now names the basename match; #6 -> both clauses carry a mutant, the verb one and the dropped-suggestion one, each re-applied and KILLED; #7 -> replaced with the breakdown-detail verb mutant; re-applied and KILLED; #8 -> the seam is fictional\_affects, not resolvability; stated in the criterion and in Ruling 1; #9 -> the mutant is a DIVERGENT case-sensitive inline copy; re-applied and KILLED; #10 -> WITHDRAWN as unbuildable and stated so on the artefact; the advisory lane computes no such verdict and is advisory by construction; #11 -> AC5 added for the writer check, driven through refine, with a mutant proving it is not a side effect of AC1; #12 -> as #1; #13 -> as #2 and #3; #14 -> as #4; #15 -> as #5 and #6; #16 -> as #8, #9 and #10; #17 -> the invariant HOLDS and passes; its helper was re-deriving the gate reason and now reads the recorded verdict | - | | |
| BG0559 | 2026-08-09 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria run gate.py as a subprocess and assert on the printed lane line; mutants re-applied and KILLED; #2 -> re-specified as a DIVERGENT copy testing the skill directory rather than SKILL.md inside it, with a boundary fixture; KILLED; #3 -> the fixture is skill-shaped with lib surface.py absent, so the fault is an import fault where the measurement is defined; #4 -> Affects widened and AC5 added; the close report row reports not-applicable rather than unreadable; KILLED; #5 -> Ruling 1: the predicate is asked at the LANE, and AC2 asserts a NON-ZERO count from command\_audit coverage on a bare tree; #6 -> the fixture is built by init run, the shape a real adopter has; the in-project-install case is claimed by no criterion; #7 -> AC3 runs the same fixture with and without the injected fault; #8 -> as #1; #9 -> as #2; #10 -> as #3; #11 -> as #4 | - | | |
| BG0558 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> BG0558 AC3 no longer asserts an absent literal: it reads the ungroomed line, asserts the verb the render CHOSE is `typo:` and that no `lacks` claim is made of a present field; the declared mutant re-applied and KILLED; #2 -> US0663 AC3 scans the WHOLE file in both documents through one predicate, and the positive control runs that same predicate over the REAL documents with the key appended at the end and again as prose; re-applied at the end of the plan\_review block and KILLED; #3 -> US0662 AC4 was the wrong half and is RETRACTED on the artefact: git cannot track an empty directory, so an absent retros/ is the normal state of a fresh clone and arming on it would mean the concession never applied at all; the criterion now covers the unreadable case, the docstring is corrected to match the code, and absent and no-retro are asserted SOFTENED so the three cases are distinguished; #4 -> both criteria now capture a real baseline: the same fixture run against a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr, with the normalised fields named in a constant the test asserts; #5 -> BG0560 AC3 covers plan\_review.enabled, and every row that resolves unset must say Dormant and must not describe itself as firing; the page's row was rekeyed to the real config name; #6 -> the 41 files under .rehearsal-scratch/ are removed from the index and the tree, and US0664 AC3 now also asserts that nothing shaped like rehearsal residue is TRACKED - a git-status check cannot see residue that has already been committed; #7 -> the write-nothing test now checks the harness exit status, so it can no longer pass on a rehearsal that failed outright and therefore wrote nothing | all 30 Verify filters collect exactly one test each and 17 of 20 spot-checked mutants were killed; BG0558 AC3 declared mutant survives; US0663 AC3 declared mutant survives; US0662 AC4 false for the absent and non-retro-file cases; US0662 AC3 and US0663 AC2 ship no captured baseline; BG0560 AC3 omits plan\\_review.enabled and does not redden on a dormant gate described as firing; .rehearsal-scratch mutation residue committed to main | | |
| BG0559 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> BG0558 AC3 no longer asserts an absent literal: it reads the ungroomed line, asserts the verb the render CHOSE is `typo:` and that no `lacks` claim is made of a present field; the declared mutant re-applied and KILLED; #2 -> US0663 AC3 scans the WHOLE file in both documents through one predicate, and the positive control runs that same predicate over the REAL documents with the key appended at the end and again as prose; re-applied at the end of the plan\_review block and KILLED; #3 -> US0662 AC4 was the wrong half and is RETRACTED on the artefact: git cannot track an empty directory, so an absent retros/ is the normal state of a fresh clone and arming on it would mean the concession never applied at all; the criterion now covers the unreadable case, the docstring is corrected to match the code, and absent and no-retro are asserted SOFTENED so the three cases are distinguished; #4 -> both criteria now capture a real baseline: the same fixture run against a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr, with the normalised fields named in a constant the test asserts; #5 -> BG0560 AC3 covers plan\_review.enabled, and every row that resolves unset must say Dormant and must not describe itself as firing; the page's row was rekeyed to the real config name; #6 -> the 41 files under .rehearsal-scratch/ are removed from the index and the tree, and US0664 AC3 now also asserts that nothing shaped like rehearsal residue is TRACKED - a git-status check cannot see residue that has already been committed; #7 -> the write-nothing test now checks the harness exit status, so it can no longer pass on a rehearsal that failed outright and therefore wrote nothing | docs/existing-users.md claims setting adopt\\_after and running reconcile makes the gate green, and nothing executes that claim; a legitimate creation whose basename is a common one - \\_\\_init\\_\\_.py, README.md - is still classified as a typo and refused, refused at the base ref too, warrants a CR; has\\_run\\_history is non-recursive, so retros archived into a subdirectory read as no history; the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| BG0560 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> BG0558 AC3 no longer asserts an absent literal: it reads the ungroomed line, asserts the verb the render CHOSE is `typo:` and that no `lacks` claim is made of a present field; the declared mutant re-applied and KILLED; #2 -> US0663 AC3 scans the WHOLE file in both documents through one predicate, and the positive control runs that same predicate over the REAL documents with the key appended at the end and again as prose; re-applied at the end of the plan\_review block and KILLED; #3 -> US0662 AC4 was the wrong half and is RETRACTED on the artefact: git cannot track an empty directory, so an absent retros/ is the normal state of a fresh clone and arming on it would mean the concession never applied at all; the criterion now covers the unreadable case, the docstring is corrected to match the code, and absent and no-retro are asserted SOFTENED so the three cases are distinguished; #4 -> both criteria now capture a real baseline: the same fixture run against a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr, with the normalised fields named in a constant the test asserts; #5 -> BG0560 AC3 covers plan\_review.enabled, and every row that resolves unset must say Dormant and must not describe itself as firing; the page's row was rekeyed to the real config name; #6 -> the 41 files under .rehearsal-scratch/ are removed from the index and the tree, and US0664 AC3 now also asserts that nothing shaped like rehearsal residue is TRACKED - a git-status check cannot see residue that has already been committed; #7 -> the write-nothing test now checks the harness exit status, so it can no longer pass on a rehearsal that failed outright and therefore wrote nothing | docs/existing-users.md claims setting adopt\\_after and running reconcile makes the gate green, and nothing executes that claim; a legitimate creation whose basename is a common one - \\_\\_init\\_\\_.py, README.md - is still classified as a typo and refused, refused at the base ref too, warrants a CR; has\\_run\\_history is non-recursive, so retros archived into a subdirectory read as no history; the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0662 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> BG0558 AC3 no longer asserts an absent literal: it reads the ungroomed line, asserts the verb the render CHOSE is `typo:` and that no `lacks` claim is made of a present field; the declared mutant re-applied and KILLED; #2 -> US0663 AC3 scans the WHOLE file in both documents through one predicate, and the positive control runs that same predicate over the REAL documents with the key appended at the end and again as prose; re-applied at the end of the plan\_review block and KILLED; #3 -> US0662 AC4 was the wrong half and is RETRACTED on the artefact: git cannot track an empty directory, so an absent retros/ is the normal state of a fresh clone and arming on it would mean the concession never applied at all; the criterion now covers the unreadable case, the docstring is corrected to match the code, and absent and no-retro are asserted SOFTENED so the three cases are distinguished; #4 -> both criteria now capture a real baseline: the same fixture run against a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr, with the normalised fields named in a constant the test asserts; #5 -> BG0560 AC3 covers plan\_review.enabled, and every row that resolves unset must say Dormant and must not describe itself as firing; the page's row was rekeyed to the real config name; #6 -> the 41 files under .rehearsal-scratch/ are removed from the index and the tree, and US0664 AC3 now also asserts that nothing shaped like rehearsal residue is TRACKED - a git-status check cannot see residue that has already been committed; #7 -> the write-nothing test now checks the harness exit status, so it can no longer pass on a rehearsal that failed outright and therefore wrote nothing | docs/existing-users.md claims setting adopt\\_after and running reconcile makes the gate green, and nothing executes that claim; a legitimate creation whose basename is a common one - \\_\\_init\\_\\_.py, README.md - is still classified as a typo and refused, refused at the base ref too, warrants a CR; has\\_run\\_history is non-recursive, so retros archived into a subdirectory read as no history; the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0663 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> BG0558 AC3 no longer asserts an absent literal: it reads the ungroomed line, asserts the verb the render CHOSE is `typo:` and that no `lacks` claim is made of a present field; the declared mutant re-applied and KILLED; #2 -> US0663 AC3 scans the WHOLE file in both documents through one predicate, and the positive control runs that same predicate over the REAL documents with the key appended at the end and again as prose; re-applied at the end of the plan\_review block and KILLED; #3 -> US0662 AC4 was the wrong half and is RETRACTED on the artefact: git cannot track an empty directory, so an absent retros/ is the normal state of a fresh clone and arming on it would mean the concession never applied at all; the criterion now covers the unreadable case, the docstring is corrected to match the code, and absent and no-retro are asserted SOFTENED so the three cases are distinguished; #4 -> both criteria now capture a real baseline: the same fixture run against a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr, with the normalised fields named in a constant the test asserts; #5 -> BG0560 AC3 covers plan\_review.enabled, and every row that resolves unset must say Dormant and must not describe itself as firing; the page's row was rekeyed to the real config name; #6 -> the 41 files under .rehearsal-scratch/ are removed from the index and the tree, and US0664 AC3 now also asserts that nothing shaped like rehearsal residue is TRACKED - a git-status check cannot see residue that has already been committed; #7 -> the write-nothing test now checks the harness exit status, so it can no longer pass on a rehearsal that failed outright and therefore wrote nothing | docs/existing-users.md claims setting adopt\\_after and running reconcile makes the gate green, and nothing executes that claim; a legitimate creation whose basename is a common one - \\_\\_init\\_\\_.py, README.md - is still classified as a typo and refused, refused at the base ref too, warrants a CR; has\\_run\\_history is non-recursive, so retros archived into a subdirectory read as no history; the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0664 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> BG0558 AC3 no longer asserts an absent literal: it reads the ungroomed line, asserts the verb the render CHOSE is `typo:` and that no `lacks` claim is made of a present field; the declared mutant re-applied and KILLED; #2 -> US0663 AC3 scans the WHOLE file in both documents through one predicate, and the positive control runs that same predicate over the REAL documents with the key appended at the end and again as prose; re-applied at the end of the plan\_review block and KILLED; #3 -> US0662 AC4 was the wrong half and is RETRACTED on the artefact: git cannot track an empty directory, so an absent retros/ is the normal state of a fresh clone and arming on it would mean the concession never applied at all; the criterion now covers the unreadable case, the docstring is corrected to match the code, and absent and no-retro are asserted SOFTENED so the three cases are distinguished; #4 -> both criteria now capture a real baseline: the same fixture run against a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr, with the normalised fields named in a constant the test asserts; #5 -> BG0560 AC3 covers plan\_review.enabled, and every row that resolves unset must say Dormant and must not describe itself as firing; the page's row was rekeyed to the real config name; #6 -> the 41 files under .rehearsal-scratch/ are removed from the index and the tree, and US0664 AC3 now also asserts that nothing shaped like rehearsal residue is TRACKED - a git-status check cannot see residue that has already been committed; #7 -> the write-nothing test now checks the harness exit status, so it can no longer pass on a rehearsal that failed outright and therefore wrote nothing | docs/existing-users.md claims setting adopt\\_after and running reconcile makes the gate green, and nothing executes that claim; a legitimate creation whose basename is a common one - \\_\\_init\\_\\_.py, README.md - is still classified as a typo and refused, refused at the base ref too, warrants a CR; has\\_run\\_history is non-recursive, so retros archived into a subdirectory read as no history; the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0665 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> BG0558 AC3 no longer asserts an absent literal: it reads the ungroomed line, asserts the verb the render CHOSE is `typo:` and that no `lacks` claim is made of a present field; the declared mutant re-applied and KILLED; #2 -> US0663 AC3 scans the WHOLE file in both documents through one predicate, and the positive control runs that same predicate over the REAL documents with the key appended at the end and again as prose; re-applied at the end of the plan\_review block and KILLED; #3 -> US0662 AC4 was the wrong half and is RETRACTED on the artefact: git cannot track an empty directory, so an absent retros/ is the normal state of a fresh clone and arming on it would mean the concession never applied at all; the criterion now covers the unreadable case, the docstring is corrected to match the code, and absent and no-retro are asserted SOFTENED so the three cases are distinguished; #4 -> both criteria now capture a real baseline: the same fixture run against a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr, with the normalised fields named in a constant the test asserts; #5 -> BG0560 AC3 covers plan\_review.enabled, and every row that resolves unset must say Dormant and must not describe itself as firing; the page's row was rekeyed to the real config name; #6 -> the 41 files under .rehearsal-scratch/ are removed from the index and the tree, and US0664 AC3 now also asserts that nothing shaped like rehearsal residue is TRACKED - a git-status check cannot see residue that has already been committed; #7 -> the write-nothing test now checks the harness exit status, so it can no longer pass on a rehearsal that failed outright and therefore wrote nothing | docs/existing-users.md claims setting adopt\\_after and running reconcile makes the gate green, and nothing executes that claim; a legitimate creation whose basename is a common one - \\_\\_init\\_\\_.py, README.md - is still classified as a typo and refused, refused at the base ref too, warrants a CR; has\\_run\\_history is non-recursive, so retros archived into a subdirectory read as no history; the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0666 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> BG0558 AC3 no longer asserts an absent literal: it reads the ungroomed line, asserts the verb the render CHOSE is `typo:` and that no `lacks` claim is made of a present field; the declared mutant re-applied and KILLED; #2 -> US0663 AC3 scans the WHOLE file in both documents through one predicate, and the positive control runs that same predicate over the REAL documents with the key appended at the end and again as prose; re-applied at the end of the plan\_review block and KILLED; #3 -> US0662 AC4 was the wrong half and is RETRACTED on the artefact: git cannot track an empty directory, so an absent retros/ is the normal state of a fresh clone and arming on it would mean the concession never applied at all; the criterion now covers the unreadable case, the docstring is corrected to match the code, and absent and no-retro are asserted SOFTENED so the three cases are distinguished; #4 -> both criteria now capture a real baseline: the same fixture run against a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr, with the normalised fields named in a constant the test asserts; #5 -> BG0560 AC3 covers plan\_review.enabled, and every row that resolves unset must say Dormant and must not describe itself as firing; the page's row was rekeyed to the real config name; #6 -> the 41 files under .rehearsal-scratch/ are removed from the index and the tree, and US0664 AC3 now also asserts that nothing shaped like rehearsal residue is TRACKED - a git-status check cannot see residue that has already been committed; #7 -> the write-nothing test now checks the harness exit status, so it can no longer pass on a rehearsal that failed outright and therefore wrote nothing | docs/existing-users.md claims setting adopt\\_after and running reconcile makes the gate green, and nothing executes that claim; a legitimate creation whose basename is a common one - \\_\\_init\\_\\_.py, README.md - is still classified as a typo and refused, refused at the base ref too, warrants a CR; has\\_run\\_history is non-recursive, so retros archived into a subdirectory read as no history; the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| BG0558 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria now capture a real baseline from a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr with the normalised fields named; #2 -> US0662 AC4 RETRACTED and narrowed on the artefact, docstring corrected to match the code, and the absent and no-retro cases asserted SOFTENED; #3 -> the rehearsal lane is REGISTERED at the boundary and no longer BOUND, so a scoped boundary run works as it did at the base ref; verified by execution: gate.py --boundary push --only conformance now PASSes; #4 -> removed from the index and the tree, with a tracked-residue assertion added to US0664 AC3; #5 -> the advisory is keyed on a `softened` flag the gate sets, never re-derived from fired-and-not-override, so a unit with an independent APPROVE no longer carries it; #6 -> BG0558 AC4's mutant re-worded: the divergent copy is the one the test actually applies, and the criterion no longer describes case-sensitivity it does not have; #7 -> US0665's title corrected on the artefact to say the gate is compared against a recorded baseline rather than green; #8 -> FILED rather than fixed: the page's adopt\_after claim is the CR0497 remedy and is out of this run's scope, recorded in the baseline file against CR0497; #9 -> REPORTED pre-existing, refused at the base ref too, and filed for a CR; #10 -> REPORTED pre-existing, non-recursive retro lookup, filed for a CR | US0662 AC4 false for the absent and non-retro-file cases; US0662 AC3 and US0663 AC2 ship no captured baseline; BG0560 AC3 omits plan\\_review.enabled and does not redden on a dormant gate described as firing; .rehearsal-scratch mutation residue committed to main | | |
| BG0559 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria now capture a real baseline from a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr with the normalised fields named; #2 -> US0662 AC4 RETRACTED and narrowed on the artefact, docstring corrected to match the code, and the absent and no-retro cases asserted SOFTENED; #3 -> the rehearsal lane is REGISTERED at the boundary and no longer BOUND, so a scoped boundary run works as it did at the base ref; verified by execution: gate.py --boundary push --only conformance now PASSes; #4 -> removed from the index and the tree, with a tracked-residue assertion added to US0664 AC3; #5 -> the advisory is keyed on a `softened` flag the gate sets, never re-derived from fired-and-not-override, so a unit with an independent APPROVE no longer carries it; #6 -> BG0558 AC4's mutant re-worded: the divergent copy is the one the test actually applies, and the criterion no longer describes case-sensitivity it does not have; #7 -> US0665's title corrected on the artefact to say the gate is compared against a recorded baseline rather than green; #8 -> FILED rather than fixed: the page's adopt\_after claim is the CR0497 remedy and is out of this run's scope, recorded in the baseline file against CR0497; #9 -> REPORTED pre-existing, refused at the base ref too, and filed for a CR; #10 -> REPORTED pre-existing, non-recursive retro lookup, filed for a CR | the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| BG0560 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria now capture a real baseline from a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr with the normalised fields named; #2 -> US0662 AC4 RETRACTED and narrowed on the artefact, docstring corrected to match the code, and the absent and no-retro cases asserted SOFTENED; #3 -> the rehearsal lane is REGISTERED at the boundary and no longer BOUND, so a scoped boundary run works as it did at the base ref; verified by execution: gate.py --boundary push --only conformance now PASSes; #4 -> removed from the index and the tree, with a tracked-residue assertion added to US0664 AC3; #5 -> the advisory is keyed on a `softened` flag the gate sets, never re-derived from fired-and-not-override, so a unit with an independent APPROVE no longer carries it; #6 -> BG0558 AC4's mutant re-worded: the divergent copy is the one the test actually applies, and the criterion no longer describes case-sensitivity it does not have; #7 -> US0665's title corrected on the artefact to say the gate is compared against a recorded baseline rather than green; #8 -> FILED rather than fixed: the page's adopt\_after claim is the CR0497 remedy and is out of this run's scope, recorded in the baseline file against CR0497; #9 -> REPORTED pre-existing, refused at the base ref too, and filed for a CR; #10 -> REPORTED pre-existing, non-recursive retro lookup, filed for a CR | the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0662 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria now capture a real baseline from a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr with the normalised fields named; #2 -> US0662 AC4 RETRACTED and narrowed on the artefact, docstring corrected to match the code, and the absent and no-retro cases asserted SOFTENED; #3 -> the rehearsal lane is REGISTERED at the boundary and no longer BOUND, so a scoped boundary run works as it did at the base ref; verified by execution: gate.py --boundary push --only conformance now PASSes; #4 -> removed from the index and the tree, with a tracked-residue assertion added to US0664 AC3; #5 -> the advisory is keyed on a `softened` flag the gate sets, never re-derived from fired-and-not-override, so a unit with an independent APPROVE no longer carries it; #6 -> BG0558 AC4's mutant re-worded: the divergent copy is the one the test actually applies, and the criterion no longer describes case-sensitivity it does not have; #7 -> US0665's title corrected on the artefact to say the gate is compared against a recorded baseline rather than green; #8 -> FILED rather than fixed: the page's adopt\_after claim is the CR0497 remedy and is out of this run's scope, recorded in the baseline file against CR0497; #9 -> REPORTED pre-existing, refused at the base ref too, and filed for a CR; #10 -> REPORTED pre-existing, non-recursive retro lookup, filed for a CR | the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0663 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria now capture a real baseline from a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr with the normalised fields named; #2 -> US0662 AC4 RETRACTED and narrowed on the artefact, docstring corrected to match the code, and the absent and no-retro cases asserted SOFTENED; #3 -> the rehearsal lane is REGISTERED at the boundary and no longer BOUND, so a scoped boundary run works as it did at the base ref; verified by execution: gate.py --boundary push --only conformance now PASSes; #4 -> removed from the index and the tree, with a tracked-residue assertion added to US0664 AC3; #5 -> the advisory is keyed on a `softened` flag the gate sets, never re-derived from fired-and-not-override, so a unit with an independent APPROVE no longer carries it; #6 -> BG0558 AC4's mutant re-worded: the divergent copy is the one the test actually applies, and the criterion no longer describes case-sensitivity it does not have; #7 -> US0665's title corrected on the artefact to say the gate is compared against a recorded baseline rather than green; #8 -> FILED rather than fixed: the page's adopt\_after claim is the CR0497 remedy and is out of this run's scope, recorded in the baseline file against CR0497; #9 -> REPORTED pre-existing, refused at the base ref too, and filed for a CR; #10 -> REPORTED pre-existing, non-recursive retro lookup, filed for a CR | the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0664 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria now capture a real baseline from a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr with the normalised fields named; #2 -> US0662 AC4 RETRACTED and narrowed on the artefact, docstring corrected to match the code, and the absent and no-retro cases asserted SOFTENED; #3 -> the rehearsal lane is REGISTERED at the boundary and no longer BOUND, so a scoped boundary run works as it did at the base ref; verified by execution: gate.py --boundary push --only conformance now PASSes; #4 -> removed from the index and the tree, with a tracked-residue assertion added to US0664 AC3; #5 -> the advisory is keyed on a `softened` flag the gate sets, never re-derived from fired-and-not-override, so a unit with an independent APPROVE no longer carries it; #6 -> BG0558 AC4's mutant re-worded: the divergent copy is the one the test actually applies, and the criterion no longer describes case-sensitivity it does not have; #7 -> US0665's title corrected on the artefact to say the gate is compared against a recorded baseline rather than green; #8 -> FILED rather than fixed: the page's adopt\_after claim is the CR0497 remedy and is out of this run's scope, recorded in the baseline file against CR0497; #9 -> REPORTED pre-existing, refused at the base ref too, and filed for a CR; #10 -> REPORTED pre-existing, non-recursive retro lookup, filed for a CR | the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0665 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria now capture a real baseline from a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr with the normalised fields named; #2 -> US0662 AC4 RETRACTED and narrowed on the artefact, docstring corrected to match the code, and the absent and no-retro cases asserted SOFTENED; #3 -> the rehearsal lane is REGISTERED at the boundary and no longer BOUND, so a scoped boundary run works as it did at the base ref; verified by execution: gate.py --boundary push --only conformance now PASSes; #4 -> removed from the index and the tree, with a tracked-residue assertion added to US0664 AC3; #5 -> the advisory is keyed on a `softened` flag the gate sets, never re-derived from fired-and-not-override, so a unit with an independent APPROVE no longer carries it; #6 -> BG0558 AC4's mutant re-worded: the divergent copy is the one the test actually applies, and the criterion no longer describes case-sensitivity it does not have; #7 -> US0665's title corrected on the artefact to say the gate is compared against a recorded baseline rather than green; #8 -> FILED rather than fixed: the page's adopt\_after claim is the CR0497 remedy and is out of this run's scope, recorded in the baseline file against CR0497; #9 -> REPORTED pre-existing, refused at the base ref too, and filed for a CR; #10 -> REPORTED pre-existing, non-recursive retro lookup, filed for a CR | the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| US0666 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> both criteria now capture a real baseline from a clone of the skill tree with the softening branch disabled, comparing exit status, stdout and stderr with the normalised fields named; #2 -> US0662 AC4 RETRACTED and narrowed on the artefact, docstring corrected to match the code, and the absent and no-retro cases asserted SOFTENED; #3 -> the rehearsal lane is REGISTERED at the boundary and no longer BOUND, so a scoped boundary run works as it did at the base ref; verified by execution: gate.py --boundary push --only conformance now PASSes; #4 -> removed from the index and the tree, with a tracked-residue assertion added to US0664 AC3; #5 -> the advisory is keyed on a `softened` flag the gate sets, never re-derived from fired-and-not-override, so a unit with an independent APPROVE no longer carries it; #6 -> BG0558 AC4's mutant re-worded: the divergent copy is the one the test actually applies, and the criterion no longer describes case-sensitivity it does not have; #7 -> US0665's title corrected on the artefact to say the gate is compared against a recorded baseline rather than green; #8 -> FILED rather than fixed: the page's adopt\_after claim is the CR0497 remedy and is out of this run's scope, recorded in the baseline file against CR0497; #9 -> REPORTED pre-existing, refused at the base ref too, and filed for a CR; #10 -> REPORTED pre-existing, non-recursive retro lookup, filed for a CR | the six changed shipped fixtures are honest corrections with assertion strength preserved, the sixteen armed fixtures are made stricter not weaker, doc\\_coverage.is\\_skill\\_repo is genuinely one reader for its three callers, and the full suite is GREEN at 6389 as claimed; US0662 AC3 and US0663 AC2 assert a restatement of the new code where both criteria demand a captured baseline; US0662 AC4 unmet for the absent and non-retro-file cases; gate.py --boundary with --only or --skip is refused where the base ref ran it; 41 files of stale rehearsal fixture output committed under .rehearsal-scratch/ | | |
| BG0558 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> \_git\_status now passes --ignored=matching, so residue at the hardened path is visible again; #2 -> test\_a\_scoped\_boundary\_run\_is\_not\_refused drives gate.py --boundary push --only duplicate-id through the CLI; the revert-mutant re-adding bound.append was applied and KILLED; #3 -> CLOSED, no action; #4 -> CLOSED, no action; #5 -> US0663 AC2 NARROWED on the artefact to state what the test builds, and the stronger base-ref-worktree form FILED as BG0567 rather than claimed; #6 -> changelog.d/US0662.md corrected: the entry now says an unreadable directory arms and an absent one does not, and says an earlier draft claimed otherwise; #7 -> the mutant is restated to name .rehearsal-scratch rather than the repository root, AND the harness now refuses a work root inside the repository before the cleanup trap is armed, so the class is removed rather than the wording patched; #8 -> test\_an\_approved\_unit\_carries\_no\_first\_run\_advisory records an independent APPROVE through critic and asserts no advisory; the revert-mutant was applied and KILLED; #9 -> REPORTED pre-existing, unchanged by this batch; #10 -> REPORTED pre-existing, BG0558 AC4's mutant wording, carried | the boundary regression is ungated; US0663 AC2 over-claims its baseline; the changelog still asserts the retracted claim; US0664 AC3's declared mutant is data-destroying; the advisory repair has no test; git status is blinded by the new ignore rule | | |
| US0662 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> \_git\_status now passes --ignored=matching, so residue at the hardened path is visible again; #2 -> test\_a\_scoped\_boundary\_run\_is\_not\_refused drives gate.py --boundary push --only duplicate-id through the CLI; the revert-mutant re-adding bound.append was applied and KILLED; #3 -> CLOSED, no action; #4 -> CLOSED, no action; #5 -> US0663 AC2 NARROWED on the artefact to state what the test builds, and the stronger base-ref-worktree form FILED as BG0567 rather than claimed; #6 -> changelog.d/US0662.md corrected: the entry now says an unreadable directory arms and an absent one does not, and says an earlier draft claimed otherwise; #7 -> the mutant is restated to name .rehearsal-scratch rather than the repository root, AND the harness now refuses a work root inside the repository before the cleanup trap is armed, so the class is removed rather than the wording patched; #8 -> test\_an\_approved\_unit\_carries\_no\_first\_run\_advisory records an independent APPROVE through critic and asserts no advisory; the revert-mutant was applied and KILLED; #9 -> REPORTED pre-existing, unchanged by this batch; #10 -> REPORTED pre-existing, BG0558 AC4's mutant wording, carried | the boundary regression is ungated; US0663 AC2 over-claims its baseline; the changelog still asserts the retracted claim; US0664 AC3's declared mutant is data-destroying; the advisory repair has no test; git status is blinded by the new ignore rule | | |
| US0664 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> \_git\_status now passes --ignored=matching, so residue at the hardened path is visible again; #2 -> test\_a\_scoped\_boundary\_run\_is\_not\_refused drives gate.py --boundary push --only duplicate-id through the CLI; the revert-mutant re-adding bound.append was applied and KILLED; #3 -> CLOSED, no action; #4 -> CLOSED, no action; #5 -> US0663 AC2 NARROWED on the artefact to state what the test builds, and the stronger base-ref-worktree form FILED as BG0567 rather than claimed; #6 -> changelog.d/US0662.md corrected: the entry now says an unreadable directory arms and an absent one does not, and says an earlier draft claimed otherwise; #7 -> the mutant is restated to name .rehearsal-scratch rather than the repository root, AND the harness now refuses a work root inside the repository before the cleanup trap is armed, so the class is removed rather than the wording patched; #8 -> test\_an\_approved\_unit\_carries\_no\_first\_run\_advisory records an independent APPROVE through critic and asserts no advisory; the revert-mutant was applied and KILLED; #9 -> REPORTED pre-existing, unchanged by this batch; #10 -> REPORTED pre-existing, BG0558 AC4's mutant wording, carried | the boundary regression is ungated; US0663 AC2 over-claims its baseline; the changelog still asserts the retracted claim; US0664 AC3's declared mutant is data-destroying; the advisory repair has no test; git status is blinded by the new ignore rule | | |
| US0665 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> \_git\_status now passes --ignored=matching, so residue at the hardened path is visible again; #2 -> test\_a\_scoped\_boundary\_run\_is\_not\_refused drives gate.py --boundary push --only duplicate-id through the CLI; the revert-mutant re-adding bound.append was applied and KILLED; #3 -> CLOSED, no action; #4 -> CLOSED, no action; #5 -> US0663 AC2 NARROWED on the artefact to state what the test builds, and the stronger base-ref-worktree form FILED as BG0567 rather than claimed; #6 -> changelog.d/US0662.md corrected: the entry now says an unreadable directory arms and an absent one does not, and says an earlier draft claimed otherwise; #7 -> the mutant is restated to name .rehearsal-scratch rather than the repository root, AND the harness now refuses a work root inside the repository before the cleanup trap is armed, so the class is removed rather than the wording patched; #8 -> test\_an\_approved\_unit\_carries\_no\_first\_run\_advisory records an independent APPROVE through critic and asserts no advisory; the revert-mutant was applied and KILLED; #9 -> REPORTED pre-existing, unchanged by this batch; #10 -> REPORTED pre-existing, BG0558 AC4's mutant wording, carried | the boundary regression is ungated; US0663 AC2 over-claims its baseline; the changelog still asserts the retracted claim; US0664 AC3's declared mutant is data-destroying; the advisory repair has no test; git status is blinded by the new ignore rule | | |
| BG0568 | 2026-08-10 | Claude Opus 5 session 6ee2f0cb | 2026-08-10 | #1 -> AC2 ADDS the breakdown gate the ladder never had, refusing over an unfinished child and naming it; the type-scope mutant re-applied and KILLED; #2 -> every mutant re-specified against the shipped code and all seven applied and KILLED; #3 -> AC3 now names In Progress deliberately and says why a bug routed to Fixed never reaches this gate; #4 -> the old deriver criterion is DELETED with the reason recorded, since transition.py names the deriver only inside a refusal string; #5 -> the old reconcile criterion is DELETED, since the drift detector never consults the index row; #6 -> the change is scoped to epic ALONE as `type\_ != "epic"`, and AC4 pins cr, plan, test-spec and workflow as still held - the first form of that criterion could not distinguish the two implementations and mutation found it; #7 -> the new gate is NOT entry-triggered and AC2 asserts refusal from both Draft and In Progress; #8 -> the reader is named: reconcile.declared\_breakdown\_ids, what the drift detector reads, and AC5 pins it as a CLI outcome rather than a claim about which function is called; #9 -> AC3 states the rule for all three states, mirroring the detector's silences with one deliberate inversion, and the gate is forceable so no epic is ever permanently stuck; #10 -> AC1 pins the from-status, and after round 3 it also pins that the cutoff is SET and the epic carries no Test Plan, without which the mutant survived on a fixture that never armed the gate; #11 -> AC5 rewritten to a CLI outcome; #12 -> AC3 gained the unresolvable-id mutant, the implementation error Ruling 5's wording most invites; #13 -> AC6 ADDED: force clears the unfinished-child refusal and the bypass is recorded, without which an implementation omitting the guard would pass every criterion and reproduce this bug's headline symptom in a brand-new gate | round 3 - NO criterion asserted that --force clears the new gate's principal refusal, so an implementation omitting the guard would pass every criterion and reproduce this bug's headline symptom in a brand-new gate; AC1 must pin the cutoff config and the absent test plan; AC5 must pin an observable exit rather than which function is called; AC3 needs the unresolvable-id mutant; a sixth criterion is missing - force must clear the unfinished-child refusal and the bypass must be recorded | | |
| BG0406 | 2026-08-11 | sdlc-studio; agent; v1 | 2026-08-11 | #1 -> the reader now takes the longest unclaimed prefix and files one key per cell, and two mutants are killed including the break variant the first replacement test could not see #2 -> the header prose now reads an empty bound as UNRECORDED, matching the diff's own reader comment, with the four such rows stated as recomputing to lower and deliberately not back-filled #3 -> pre-existing, established by execution against the base ref, reported rather than repaired here | - | | |
| BG0497 | 2026-08-11 | sdlc-studio; agent; v1 | 2026-08-11 | #1 -> both surfaces are matched anchored at line start with continuations joined first, the skill-only flag exemption is scoped to the slash surface so a script-form autonomous is refused, and four mutants are killed #2 -> the clause is corrected, and a fifth mutant proves the class gate is not decorative by deleting the script row and being killed by the per-surface emptiness assertion | - | | |
| BG0573 | 2026-08-11 | sdlc-studio; agent; v1 | 2026-08-11 | #1 -> the containment relation is added, so a directory inside the checkout is refused alongside the repository itself and its parents, probed over the scripts directory, sdlc-studio, tools and the home directory with a fresh tempdir still accepted #2 -> AC5 now pins the containment arm and its mutant is killed #3 -> the false sentence is corrected in the artefact, and the true half about a suite invoked outside the hook is kept and named | - | | |
| BG0574 | 2026-08-11 | sdlc-studio; agent; v1 | 2026-08-11 | #1 -> all three lock call sites are now conditional on the preview flag, probed through the CLI for the retro and review types and for the batch path #2 -> the verifier compares file sizes and modification times rather than a set of paths, and its fixture pre-creates the lock so it starts in the state every real project is in | - | | |
| BG0536 | 2026-08-11 | sdlc-studio; agent; v1 | 2026-08-11 | #1 -> the file is deleted, and nothing references it outside temp-root fixtures #2 -> the fixture guard is rewritten to ask whether the root is the repository under test rather than whether it sits under the temp directory, so the declared mutant no longer runs git against the reviewer's tree, and BG0573 carries that repair with its own criteria #3 -> reported and carried, since correcting it now would rewrite a Done unit's declared surface after its review | - | | |
| BG0543 | 2026-08-11 | sdlc-studio; agent; v1 | 2026-08-11 | #1 -> carried to v5.1 as a filed finding rather than repaired inside a release cut, because widening the verifier to all four refusing states is test work on a unit already Fixed and its code is correct on every state | - | | |
| BG0593 | 2026-08-21 | authoring session | 2026-08-21 | #1 -> the fragment now describes the delivered mechanism, a pure copy with no links out of it plus a separate read root reaching only the steps whose signature accepts one. The reviewer's own mutant - extending the copy with links to the sibling directories, which is what the fragment claimed - is KILLED by test\_the\_scratch\_reaches\_nothing\_outside\_sdlc\_studio at exit 1, hash before 93f09dc6a0c27b95, mutated ffde756aaed424c6, restored 93f09dc6a0c27b95; #7 -> the same repair, this finding being the BLOCKING restatement of #1; #2 -> the Revision History row now reads five of the six, naming AC1 as the exception with its reason, which matches the Verification depth field at line 5 of the same artefact | BG0598 AC6 - the docstring of test\\_an\\_unreadable\\_ledger\\_fails\\_closed declared a mutant against an explicit ledger-path probe that \\_rejected\\_unanswered no longer contains, where its Test Plan row is the correct one; BG0595 AC5 - the named verifier is a static census rather than the twice-run exit-code comparison AC5 states, disclosed as unnameable in the plan and with the substantive property pinned by AC4; BG0594 - tools/gate\\_timing.py carried four consecutive blank lines above budget\\_config, residue of a deleted edit; BG0601 the DryRunScratchParityTests class sweep truncates each probe result to its first two elements, so a divergence past the second is invisible | | |
| BG0594 | 2026-08-21 | authoring session | 2026-08-21 | #5 -> collapsed to the two blank lines PEP 8 requires, with tools/tests/test\_gate\_timing.py green at 55 passed | BG0593 AC3 - changelog.d/BG0593.md stated "The scratch now links those surfaces", naming the symlink design AC3 forbids and which rounds 2-3 removed, while the delivered scratch holds only sdlc-studio/ and zero links; BG0593 - the artefact's Revision History row claimed "every verifier drives close\\_dry\\_run", which line 5 of the same file corrects to "five of the six, AC1 does not"; BG0598 AC6 - the docstring of test\\_an\\_unreadable\\_ledger\\_fails\\_closed declared a mutant against an explicit ledger-path probe that \\_rejected\\_unanswered no longer contains, where its Test Plan row is the correct one; BG0595 AC5 - the named verifier is a static census rather than the twice-run exit-code comparison AC5 states, disclosed as unnameable in the plan and with the substantive property pinned by AC4; BG0601 the DryRunScratchParityTests class sweep truncates each probe result to its first two elements, so a divergence past the second is invisible; BG0593 AC3 - changelog.d/BG0593.md describes the rejected symlink design as delivered | | |
| BG0595 | 2026-08-21 | authoring session | 2026-08-21 | #4 -> reported, no change made and none owed. The row is declared unnameable in the Test Plan and disclosed as such, the property being harness hermeticity, which no production edit can falsify. The substantive claim is pinned by AC4, whose mutant is executed and killed | BG0593 AC3 - changelog.d/BG0593.md stated "The scratch now links those surfaces", naming the symlink design AC3 forbids and which rounds 2-3 removed, while the delivered scratch holds only sdlc-studio/ and zero links; BG0593 - the artefact's Revision History row claimed "every verifier drives close\\_dry\\_run", which line 5 of the same file corrects to "five of the six, AC1 does not"; BG0598 AC6 - the docstring of test\\_an\\_unreadable\\_ledger\\_fails\\_closed declared a mutant against an explicit ledger-path probe that \\_rejected\\_unanswered no longer contains, where its Test Plan row is the correct one; BG0594 - tools/gate\\_timing.py carried four consecutive blank lines above budget\\_config, residue of a deleted edit; BG0601 the DryRunScratchParityTests class sweep truncates each probe result to its first two elements, so a divergence past the second is invisible; BG0593 AC3 - changelog.d/BG0593.md describes the rejected symlink design as delivered | | |
| BG0598 | 2026-08-21 | authoring session | 2026-08-21 | #3 -> the docstring now declares the mutant its Test Plan row names, remove the except handler around the ledger read. The test itself was correct and unchanged, and passes on the corrected docstring | BG0593 AC3 - changelog.d/BG0593.md stated "The scratch now links those surfaces", naming the symlink design AC3 forbids and which rounds 2-3 removed, while the delivered scratch holds only sdlc-studio/ and zero links; BG0593 - the artefact's Revision History row claimed "every verifier drives close\\_dry\\_run", which line 5 of the same file corrects to "five of the six, AC1 does not"; BG0595 AC5 - the named verifier is a static census rather than the twice-run exit-code comparison AC5 states, disclosed as unnameable in the plan and with the substantive property pinned by AC4; BG0594 - tools/gate\\_timing.py carried four consecutive blank lines above budget\\_config, residue of a deleted edit; BG0601 the DryRunScratchParityTests class sweep truncates each probe result to its first two elements, so a divergence past the second is invisible; BG0593 AC3 - changelog.d/BG0593.md describes the rejected symlink design as delivered | | |
| BG0593 | 2026-08-21 | authoring session | 2026-08-21 | #1 -> the fragment now describes the delivered mechanism, a pure copy with no links out of it plus a separate read root reaching only the steps whose signature accepts one. The reviewer's own mutant - extending the copy with links to the sibling directories, which is what the fragment claimed - is KILLED by test\_the\_scratch\_reaches\_nothing\_outside\_sdlc\_studio at exit 1, hash before 93f09dc6a0c27b95, mutated ffde756aaed424c6, restored 93f09dc6a0c27b95; #7 -> the same repair, this finding being the BLOCKING restatement of #1; #2 -> the Revision History row now reads five of the six, naming AC1 as the exception with its reason, which matches the Verification depth field at line 5 of the same artefact | BG0598 AC6 - the docstring of test\\_an\\_unreadable\\_ledger\\_fails\\_closed declared a mutant against an explicit ledger-path probe that \\_rejected\\_unanswered no longer contains, where its Test Plan row is the correct one; BG0595 AC5 - the named verifier is a static census rather than the twice-run exit-code comparison AC5 states, disclosed as unnameable in the plan and with the substantive property pinned by AC4; BG0594 - tools/gate\\_timing.py carried four consecutive blank lines above budget\\_config, residue of a deleted edit; BG0601 the DryRunScratchParityTests class sweep truncates each probe result to its first two elements, so a divergence past the second is invisible | | |
| BG0593 | 2026-08-21 | authoring session | 2026-08-21 | #6 -> FILED as BG0601 rather than fixed in this batch. The truncation is real and was demonstrated rather than hypothesised, but the sweep is BG0593's own test scaffolding rather than a criterion's subject, and widening it inside a closing batch would change the evidence under six units already reviewed. BG0601 carries the reproduction and the fix | BG0593 AC3 - changelog.d/BG0593.md stated "The scratch now links those surfaces", naming the symlink design AC3 forbids and which rounds 2-3 removed, while the delivered scratch holds only sdlc-studio/ and zero links; BG0593 - the artefact's Revision History row claimed "every verifier drives close\\_dry\\_run", which line 5 of the same file corrects to "five of the six, AC1 does not"; BG0598 AC6 - the docstring of test\\_an\\_unreadable\\_ledger\\_fails\\_closed declared a mutant against an explicit ledger-path probe that \\_rejected\\_unanswered no longer contains, where its Test Plan row is the correct one; BG0595 AC5 - the named verifier is a static census rather than the twice-run exit-code comparison AC5 states, disclosed as unnameable in the plan and with the substantive property pinned by AC4; BG0594 - tools/gate\\_timing.py carried four consecutive blank lines above budget\\_config, residue of a deleted edit; BG0593 AC3 - changelog.d/BG0593.md describes the rejected symlink design as delivered | | |
| BG0593 | 2026-08-21 | authoring session | 2026-08-21 | #1 -> FIXED in BG0593. changelog.d/BG0593.md now describes the delivered mechanism, a pure copy with no links out of it plus a separate read root reaching only the steps whose signature accepts one. The reviewer's mutant - extending the copy with links to the sibling directories, which is what the fragment claimed - is KILLED by test\_the\_scratch\_reaches\_nothing\_outside\_sdlc\_studio at exit 1, hash before 93f09dc6a0c27b95, mutated ffde756aaed424c6, restored 93f09dc6a0c27b95; #7 -> FIXED by the same repair, this finding being the BLOCKING restatement of #1; #2 -> FIXED in BG0593. The Revision History row now reads five of the six, naming AC1 as the exception with its reason, matching the Verification depth field at line 5 of the same artefact; #3 -> FIXED in BG0598. The docstring of test\_an\_unreadable\_ledger\_fails\_closed now declares the mutant its Test Plan row names, remove the except handler around the ledger read. The test itself was correct and unchanged, and passes on the corrected docstring; #4 -> NOT OWED, in BG0595. The row is declared unnameable in the Test Plan and disclosed as such, the property being harness hermeticity, which no production edit can falsify. The substantive claim is pinned by AC4, whose mutant is executed and killed; #5 -> FIXED in BG0594. The four blank lines above budget\_config are collapsed to the two PEP 8 requires, with tools/tests/test\_gate\_timing.py green at 55 passed; #6 -> FILED as BG0601 rather than fixed. The truncation is real and was demonstrated rather than hypothesised, but the sweep is BG0593's test scaffolding rather than a criterion's subject, and widening it inside a closing batch would change the evidence under six units already reviewed | - | | |
| BG0594 | 2026-08-21 | authoring session | 2026-08-21 | #1 -> FIXED in BG0593. changelog.d/BG0593.md now describes the delivered mechanism, a pure copy with no links out of it plus a separate read root reaching only the steps whose signature accepts one. The reviewer's mutant - extending the copy with links to the sibling directories, which is what the fragment claimed - is KILLED by test\_the\_scratch\_reaches\_nothing\_outside\_sdlc\_studio at exit 1, hash before 93f09dc6a0c27b95, mutated ffde756aaed424c6, restored 93f09dc6a0c27b95; #7 -> FIXED by the same repair, this finding being the BLOCKING restatement of #1; #2 -> FIXED in BG0593. The Revision History row now reads five of the six, naming AC1 as the exception with its reason, matching the Verification depth field at line 5 of the same artefact; #3 -> FIXED in BG0598. The docstring of test\_an\_unreadable\_ledger\_fails\_closed now declares the mutant its Test Plan row names, remove the except handler around the ledger read. The test itself was correct and unchanged, and passes on the corrected docstring; #4 -> NOT OWED, in BG0595. The row is declared unnameable in the Test Plan and disclosed as such, the property being harness hermeticity, which no production edit can falsify. The substantive claim is pinned by AC4, whose mutant is executed and killed; #5 -> FIXED in BG0594. The four blank lines above budget\_config are collapsed to the two PEP 8 requires, with tools/tests/test\_gate\_timing.py green at 55 passed; #6 -> FILED as BG0601 rather than fixed. The truncation is real and was demonstrated rather than hypothesised, but the sweep is BG0593's test scaffolding rather than a criterion's subject, and widening it inside a closing batch would change the evidence under six units already reviewed | - | | |
| BG0595 | 2026-08-21 | authoring session | 2026-08-21 | #1 -> FIXED in BG0593. changelog.d/BG0593.md now describes the delivered mechanism, a pure copy with no links out of it plus a separate read root reaching only the steps whose signature accepts one. The reviewer's mutant - extending the copy with links to the sibling directories, which is what the fragment claimed - is KILLED by test\_the\_scratch\_reaches\_nothing\_outside\_sdlc\_studio at exit 1, hash before 93f09dc6a0c27b95, mutated ffde756aaed424c6, restored 93f09dc6a0c27b95; #7 -> FIXED by the same repair, this finding being the BLOCKING restatement of #1; #2 -> FIXED in BG0593. The Revision History row now reads five of the six, naming AC1 as the exception with its reason, matching the Verification depth field at line 5 of the same artefact; #3 -> FIXED in BG0598. The docstring of test\_an\_unreadable\_ledger\_fails\_closed now declares the mutant its Test Plan row names, remove the except handler around the ledger read. The test itself was correct and unchanged, and passes on the corrected docstring; #4 -> NOT OWED, in BG0595. The row is declared unnameable in the Test Plan and disclosed as such, the property being harness hermeticity, which no production edit can falsify. The substantive claim is pinned by AC4, whose mutant is executed and killed; #5 -> FIXED in BG0594. The four blank lines above budget\_config are collapsed to the two PEP 8 requires, with tools/tests/test\_gate\_timing.py green at 55 passed; #6 -> FILED as BG0601 rather than fixed. The truncation is real and was demonstrated rather than hypothesised, but the sweep is BG0593's test scaffolding rather than a criterion's subject, and widening it inside a closing batch would change the evidence under six units already reviewed | - | | |
| BG0596 | 2026-08-21 | authoring session | 2026-08-21 | #1 -> FIXED in BG0593. changelog.d/BG0593.md now describes the delivered mechanism, a pure copy with no links out of it plus a separate read root reaching only the steps whose signature accepts one. The reviewer's mutant - extending the copy with links to the sibling directories, which is what the fragment claimed - is KILLED by test\_the\_scratch\_reaches\_nothing\_outside\_sdlc\_studio at exit 1, hash before 93f09dc6a0c27b95, mutated ffde756aaed424c6, restored 93f09dc6a0c27b95; #7 -> FIXED by the same repair, this finding being the BLOCKING restatement of #1; #2 -> FIXED in BG0593. The Revision History row now reads five of the six, naming AC1 as the exception with its reason, matching the Verification depth field at line 5 of the same artefact; #3 -> FIXED in BG0598. The docstring of test\_an\_unreadable\_ledger\_fails\_closed now declares the mutant its Test Plan row names, remove the except handler around the ledger read. The test itself was correct and unchanged, and passes on the corrected docstring; #4 -> NOT OWED, in BG0595. The row is declared unnameable in the Test Plan and disclosed as such, the property being harness hermeticity, which no production edit can falsify. The substantive claim is pinned by AC4, whose mutant is executed and killed; #5 -> FIXED in BG0594. The four blank lines above budget\_config are collapsed to the two PEP 8 requires, with tools/tests/test\_gate\_timing.py green at 55 passed; #6 -> FILED as BG0601 rather than fixed. The truncation is real and was demonstrated rather than hypothesised, but the sweep is BG0593's test scaffolding rather than a criterion's subject, and widening it inside a closing batch would change the evidence under six units already reviewed | - | | |
| BG0597 | 2026-08-21 | authoring session | 2026-08-21 | #1 -> FIXED in BG0593. changelog.d/BG0593.md now describes the delivered mechanism, a pure copy with no links out of it plus a separate read root reaching only the steps whose signature accepts one. The reviewer's mutant - extending the copy with links to the sibling directories, which is what the fragment claimed - is KILLED by test\_the\_scratch\_reaches\_nothing\_outside\_sdlc\_studio at exit 1, hash before 93f09dc6a0c27b95, mutated ffde756aaed424c6, restored 93f09dc6a0c27b95; #7 -> FIXED by the same repair, this finding being the BLOCKING restatement of #1; #2 -> FIXED in BG0593. The Revision History row now reads five of the six, naming AC1 as the exception with its reason, matching the Verification depth field at line 5 of the same artefact; #3 -> FIXED in BG0598. The docstring of test\_an\_unreadable\_ledger\_fails\_closed now declares the mutant its Test Plan row names, remove the except handler around the ledger read. The test itself was correct and unchanged, and passes on the corrected docstring; #4 -> NOT OWED, in BG0595. The row is declared unnameable in the Test Plan and disclosed as such, the property being harness hermeticity, which no production edit can falsify. The substantive claim is pinned by AC4, whose mutant is executed and killed; #5 -> FIXED in BG0594. The four blank lines above budget\_config are collapsed to the two PEP 8 requires, with tools/tests/test\_gate\_timing.py green at 55 passed; #6 -> FILED as BG0601 rather than fixed. The truncation is real and was demonstrated rather than hypothesised, but the sweep is BG0593's test scaffolding rather than a criterion's subject, and widening it inside a closing batch would change the evidence under six units already reviewed | - | | |
| BG0598 | 2026-08-21 | authoring session | 2026-08-21 | #1 -> FIXED in BG0593. changelog.d/BG0593.md now describes the delivered mechanism, a pure copy with no links out of it plus a separate read root reaching only the steps whose signature accepts one. The reviewer's mutant - extending the copy with links to the sibling directories, which is what the fragment claimed - is KILLED by test\_the\_scratch\_reaches\_nothing\_outside\_sdlc\_studio at exit 1, hash before 93f09dc6a0c27b95, mutated ffde756aaed424c6, restored 93f09dc6a0c27b95; #7 -> FIXED by the same repair, this finding being the BLOCKING restatement of #1; #2 -> FIXED in BG0593. The Revision History row now reads five of the six, naming AC1 as the exception with its reason, matching the Verification depth field at line 5 of the same artefact; #3 -> FIXED in BG0598. The docstring of test\_an\_unreadable\_ledger\_fails\_closed now declares the mutant its Test Plan row names, remove the except handler around the ledger read. The test itself was correct and unchanged, and passes on the corrected docstring; #4 -> NOT OWED, in BG0595. The row is declared unnameable in the Test Plan and disclosed as such, the property being harness hermeticity, which no production edit can falsify. The substantive claim is pinned by AC4, whose mutant is executed and killed; #5 -> FIXED in BG0594. The four blank lines above budget\_config are collapsed to the two PEP 8 requires, with tools/tests/test\_gate\_timing.py green at 55 passed; #6 -> FILED as BG0601 rather than fixed. The truncation is real and was demonstrated rather than hypothesised, but the sweep is BG0593's test scaffolding rather than a criterion's subject, and widening it inside a closing batch would change the evidence under six units already reviewed | - | | |
| US0673 | 2026-08-24 | authoring session | 2026-08-24 | #1 -> `revert\_exemptions()` now requires EVERY row on the criterion to be `unnameable` before the exemption is granted #2 -> `\_MUTANT\_PATH\_RE` rewritten to match dotted and hyphenated path segments across nine language extensions; re-measured over the corpus rather than over the fixture | verify\\_ac.py `\\_MUTANT\\_PATH\\_RE` - 272 of 459 corpus plan rows could not be seen by the exemption's path pattern, so the exemption was unreachable for the majority of the corpus | | |
| US0671 | 2026-08-24 | authoring session | 2026-08-24 | #1 -> `\_revertible()` refuses anything absolute, outside `root`, a symlink, a non-file, or lacking R\_OK/W\_OK. The pinning test asserts the REASON ("not a readable file here", assertNotIn "could not read"), because the git-failure guard masked the outside-root guard and a test asserting only exit 3 passed with the repair removed #2 -> `\_base\_blob` is tri-state - `ls-tree` non-zero raises `\_BaseUnreadable`, an empty listing returns None, only a successful `show` returns bytes. The mutant was executed against a decoy repository with DIFFERENT base content, because a byte-identical decoy minted the same commit SHA and the mutant survived #3 -> the classifier maps `res.kind in ("invalid","blocked") or res.vacuous` to `unmeasured` rather than `green`, and a unit with nothing measurable exits 3 as REPORTED instead of passing | verify\\_ac.py `\\_base\\_blob` - ANY git failure read as 'absent at base', so the check deleted the production file and manufactured the red it was looking for - absent and could-not-ask were the same answer; verify\\_ac.py - a verifier that never RAN was counted as evidence that the tests reached the change, so an unmeasured selector passed the unit | | |
| US0672 | 2026-08-24 | authoring session | 2026-08-24 | #1 -> the snapshot stores `(bytes, S\_IMODE(st.st\_mode))` and restore re-chmods, pinned by a fixture whose `hashes()` records content AND mode AND link target via `lstat` #2 -> `\_revertible()` refuses `path.is\_symlink()`, so a symlink is never a revert target, and the same `lstat`-based fixture asserts the link survives byte-exact #3 -> `\_guard\_signals(restore)` installs SIGTERM, SIGINT and SIGHUP handlers that restore, reinstate the previous handler and re-raise. The mutant SURVIVED at first because the existing interrupted-run test patched `run\_verifier` to raise and the `finally` handled it, so `test\_a\_sigterm\_mid\_check\_still\_restores\_the\_tree` was added driving a real subprocess and a real SIGTERM | verify\\_ac.py - a symlink in the declared set was destroyed and replaced by a regular file; verify\\_ac.py - SIGTERM left the change reverted on disk where SIGINT did not, so an interrupted check silently shipped the base-ref content | | |
| US0673 | 2026-08-24 | authoring session | 2026-08-24 | #2 -> `\_MUTANT\_PATH\_RE` rewritten to match dotted and hyphenated path segments across nine language extensions, then RE-MEASURED over the corpus rather than over the fixture | verify\\_ac.py `revert\\_exemptions` - an `unnameable` row exempted a criterion that ALSO carried a nameable mutant, so a measurable criterion bought a blanket exemption | | |
| US0675 | 2026-08-24 | authoring session | 2026-08-24 | #1 -> the fixture is rebuilt so NO TWO COUNTS ARE EQUAL - 3 criteria, 4 plan rows, 3 executed (1 killed, 1 survived, 1 equivalent), 1 not-run - and the row arithmetic closes #2 -> `depth\_facts()` separates `plan\_absent` from `ledger\_absent` as independent facts. The conflated form was false on 484 of 561 units and hid survivors #3 -> `\_entry\_point\_split()` is node-scoped only, returning located/through\_cli/in\_process/undetermined/unlocatable and never falling back to the whole file. The first mutant pointed at the wrong test and SURVIVED, so it was repointed at `test\_a\_criterion\_whose\_node\_cannot\_be\_isolated\_is\_undetermined` | verify\\_ac.py `depth\\_facts` - EVIDENCE ABSENT was asserted about the LEDGER from the absence of a PLAN, false on 484 of 561 units and hiding survivors; verify\\_ac.py `\\_entry\\_point\\_split` - the entry-point count over-claimed through a whole-file fallback that never read the criterion's own node | | |
| US0671 | 2026-08-24 | authoring session | 2026-08-24 | #2 -> `\_base\_blob` is tri-state - `ls-tree` non-zero raises `\_BaseUnreadable`, an empty listing returns None, only a successful `show` returns bytes. The mutant was executed against a decoy repository with DIFFERENT base content, because a byte-identical decoy minted the same commit SHA and the mutant survived; #3 -> the classifier maps an invalid, blocked or vacuous verifier result to `unmeasured` rather than `green`, and a unit with nothing measurable exits 3 as REPORTED instead of passing | verify\\_ac.py `revert\\_targets` - a path outside the repository was classified as production, DELETED and recreated at the process umask, so a private 0600 file returned world-readable while the unit reported PASSING, and the docstring promised a confinement the code did not have | | |
| US0672 | 2026-08-24 | authoring session | 2026-08-24 | #2 -> `\_revertible()` refuses `path.is\_symlink()`, so a symlink is never a revert target, and the `lstat`-based fixture asserts the link survives byte-exact; #3 -> `\_guard\_signals(restore)` installs SIGTERM, SIGINT and SIGHUP handlers that restore, reinstate the previous handler and re-raise. The mutant SURVIVED at first because the existing interrupted-run test patched `run\_verifier` to raise and the `finally` handled it, so `test\_a\_sigterm\_mid\_check\_still\_restores\_the\_tree` was added driving a real subprocess and a real SIGTERM | verify\\_ac.py `revert\\_check` restore - file MODE was lost on the unlink/recreate path: a tracked 100755 script came back 100644 after a SUCCESSFUL run | | |
| US0673 | 2026-08-24 | authoring session | 2026-08-24 | #1 -> `revert\_exemptions()` now requires EVERY row on the criterion to be `unnameable` before the exemption is granted, so a criterion carrying any nameable mutant is measured | verify\\_ac.py `\\_MUTANT\\_PATH\\_RE` - 272 of 459 corpus plan rows could not be seen by the exemption's path pattern, so the exemption was unreachable for the majority of the corpus | | |
| US0675 | 2026-08-24 | authoring session | 2026-08-24 | #2 -> `depth\_facts()` separates `plan\_absent` from `ledger\_absent` as independent facts. The conflated form was false on 484 of 561 units and hid survivors; #3 -> `\_entry\_point\_split()` is node-scoped only, returning located/through\_cli/in\_process/undetermined/unlocatable and never falling back to the whole file. The first mutant pointed at the wrong test and SURVIVED, so it was repointed at `test\_a\_criterion\_whose\_node\_cannot\_be\_isolated\_is\_undetermined` | test\\_verify\\_ac.py DerivedDepthTests - the fixture used 2 criteria, 2 rows and 2 executions, so every figure was 2 and FOUR of the five counts survived being swapped for each other | | |
| US0674 | 2026-08-24 | authoring session | 2026-08-24 | #1 -> corrected in docs/release-notes-v5.0.1.md - the two paragraphs now agree and both are re-derived from the corpus rather than retyped: five High are open (BG0586, BG0588, BG0592, BG0604, BG0606), `known\_issues.py --write` regenerated the disclosure page and `--check` agrees at 14 findings; #2 -> `\_first\_three()` appends a `(+N more)` remainder exactly as the sibling `\_derived\_depth` already did, so a batch of four or more refused units no longer under-delivers AC1's naming promise; #3 -> a per-unit crash is collected in `crashed` rather than swallowed into `named`, and when nothing was refused the detail LEADS with the failure instead of appending it after a reassuring clause. Pinned by `test\_a\_unit\_the\_lane\_cannot\_examine\_is\_not\_reported\_as\_a\_clean\_pass`, whose mutant (swallow the crash into `named`) was executed and KILLED | the AC4 roster test pins the word ADVISORY and the lane name but never ties either to the lane's actual blocking flag, so a flip to blocking would leave the roster false with that test still green; CR0552 | | |
| US0674 | 2026-08-24 | authoring session | 2026-08-24 | #4 -> the roster test now reads the lane's actual `blocking` value on BOTH return paths - the one that found nothing and the one that found units - rather than only pinning AGENTS.md's prose and the lane's name. A lane that is advisory only while it is silent is not an advisory lane. The mutant (return `blocking: True` from the refused path) was executed and KILLED | docs/release-notes-v5.0.1.md:97 still reads `The six High findings above are listed separately` while the paragraph this commit rewrote at line 77 now says four are open, and `tools/known\\_issues.py --bar` exits 1 reporting exactly 4. At the base ref the two paragraphs AGREED at six and six, so this commit made a true sentence false in the project's own disclosure page - the one surface that exists to be read rather than trusted. The file is in no unit's Affects and carries no changelog fragment, so it rides the batch commit unowned; the lane truncates its findings to the first three with NO +N more indicator while the sibling `\\_derived\\_depth` in the same diff does print one, so on a batch of four or more refused units AC1's naming-each is silently not delivered; with every unit reported-not-examined the lane prints 0 examined and PASSES, an absence reading as a pass; CR0552 | | |
| US0674 | 2026-08-24 | authoring session | 2026-08-24 | #1 -> corrected in docs/release-notes-v5.0.1.md; both paragraphs are re-derived from the corpus and agree, and `known\_issues.py --check` agrees at 15 disclosed findings; #2 -> `\_first\_three()` appends a `(+N more)` remainder, and the claim is now driven through the LANE over four refused units rather than through the helper in isolation - the call-site bypass mutant was executed and KILLED; #3 -> a per-unit crash is collected in `crashed` and the detail LEADS with it instead of appending it after a clean-sweep clause; the swallow mutant was executed and KILLED; #4 -> the roster test reads the lane's actual `blocking` value on BOTH return paths, so a flip to blocking fails it; the flip mutant was executed and KILLED; #5 -> CR0552 is [pre-existing] and filed; it does not hold this gate | - | | |
| BG0607 | 2026-08-25 | authoring session | 2026-08-25 | #1 -> the claim is WITHDRAWN and the measurement re-run independently: keying on the Brief fingerprint takes the unanswered set from 81 units to 49, recovering 32 and losing none. BG0607's re-opening note, its Proposed Fix, the release notes, LATEST.md and RETRO0109 are all rewritten to the fingerprint-keyed scope plus an evidence backfill of the residue #2 -> RETRO0109's lesson is replaced with the true one: state what would have to be true for two rules to disagree before treating their agreement as evidence, because the repair-keyed unanswered set contains the reviewer-keyed one by construction #3 -> forward-ported at the close of this run, with `forward-port.sh --check` run afterwards to confirm the mirror matches #4 -> reported, pre-existing, and not repaired here | the two rules said to corroborate each other are NESTED by construction - the repair-keyed unanswered set contains the reviewer-keyed one - so their agreement was guaranteed before either ran, and that agreement is what licensed the schema conclusion. BLOCKING; the withdrawn fix is still LIVE in the installed copy, byte-identical to the pre-revert commit, so every project on this machine loads the roll-up measured at 579/690. BLOCKING; known\\_issues.py:177 asserts the release notes claim zero without reading them | | |
| BG0581 | 2026-08-25 | authoring session | 2026-08-25 | #1 -> the two-role cap is now computed ONLY on a COMPLETING rung and applied through `\_terminal\_in\_type\_vocab` rather than as a raw `RUNG\_TERMINALS` token. Story batches on triage, plan and design now report Ready or `no status change`, never Triaged or Review. AC4's test was extended to the capped path and the mutant "drop the COMPLETING\_RUNGS gate from the cap loop" executed and KILLED as AC4 row 1. AC1's test was corrected too, because it asserted Triaged of a STORY batch and so encoded the same defect #2 -> changelog.d/BG0581.md rewritten to state what the code does, re-checked through `sprint.py plan --format json` after the fix #3 -> the resolver now consults `sdlc\_md.triage\_target` for a triage rung, gated on `is\_schema\_v3(root)` so the answer is derived from the project's schema rather than assumed. Under v2 there is no inbox lane and the rung genuinely moves nothing #4 -> STATE\_UNCHANGED is now the noun phrase `no status change`, the basis names the rung and its per-type terminal, and the renderer no longer appends a fixed shortfall to a per-type state list #5 -> the resolver takes `root` and passes it to `status\_vocab` and the vocabulary check, matching sprint.py:6739 #6 -> `rung\_key` is resolved to a known key ONCE, before both the terminal lookup and the COMPLETING\_RUNGS membership test, so the two halves cannot disagree | changelog.d/BG0581.md claimed the state is said in the batch's OWN vocabulary, which the same command falsifies. BLOCKING; `\\_terminal\\_in\\_type\\_vocab` reported a finding as unmoved by a `triage` rung, contradicted by `sdlc\\_md.triage\\_target` under schema v3 which is the current seed; the `basis` string read `no gate in this project caps this batch below its status unchanged` and reached the seat brief verbatim, and the renderer appended a hardcoded `, NOT Done` to a per-type state list; `status\\_vocab(type\\_)` was called without repo\\_root though root was in scope, unlike sprint.py:6739; the unknown-rung fallback resolved the terminal to `done` but tested the unrecognised key for COMPLETING\\_RUNGS membership, latent behind argparse choices | | |
| BG0592 | 2026-08-25 | authoring session | 2026-08-25 | #1 -> the row now reads "extend \_claims\_completion's status\_vocab call with a project root", which is both an edit verb and the direction actually executed. `verify\_ac.py testplan derive --unit BG0592 --dry-run` is back to exit 0 reporting 18 rows for 15 criteria, and AC11's mutant was re-executed against the corrected wording and KILLED #2 -> the prose half now states 18 executed with 17 killed, one equivalent with its reason named, and one equivalent RETRACTED on the record, which is what the derived half says #3 -> test\_gate.py's MUTANT docstring rewritten to the row's wording verbatim | the Verification depth line read `all 18 KILLED` beside a derived half saying killed 17 with one ruled equivalent, contradicting itself within one line; the test docstring and the plan row described one edit in two different wordings | | |
| BG0606 | 2026-08-25 | authoring session | 2026-08-25 | #1 -> the check is now made OFF THE FILE and the derived-shape call was removed from AC2's test, so the two criteria ask different questions of different sources. The checkbox pattern accepts a capitalised box as `parse\_story` does, killing the false positive. Both properties were probed directly afterwards, a real title defect FAILS the check on its own and a capitalised checkbox no longer does #2 -> the field now states that the ledger evicted the US0671 and US0676 rows when a later registration landed against an edited verify\_ac.py, that `verify\_ac.py depth` reports EVIDENCE ABSENT for both, and that the eviction predates this unit's base ref #3 -> rewritten to what the shipped entry point reports today, which is exit 0 with the plan at line 40, and it names both of its own earlier errors #4 -> FILED as BG0614 after a full-ledger audit found nine such keys, four of them naming different tests #5 -> FILED as CR0558 | the depth field claimed not-run 0 for US0671, US0674 and US0676 while the ledger had evicted 23 of their 33 rows, an eviction predating this unit's base ref; the `Close blocked by` field stated a block the dry-run does not report; three LIVE duplicate ledger rows on BG0606 AC1 row 0, two naming a test node that no longer exists - filed as BG0614; the `derived-depth` lane checks each span against its own seal rather than re-deriving it, so it passed 1303 units including two whose evidence was gone - filed as CR0558 | | |
| BG0607 | 2026-08-25 | authoring session | 2026-08-25 | #1 -> the claim is WITHDRAWN and the measurement re-run independently. Keying on the Brief fingerprint takes the unanswered set from 81 units to 49, recovering 32 and losing none. BG0607's re-opening note, its Proposed Fix, the release notes, LATEST.md and RETRO0109 are all rewritten to the fingerprint-keyed scope plus an evidence backfill of the residue #2 -> RETRO0109's lesson is replaced with the true one. State what would have to be true for two rules to disagree before treating their agreement as evidence, because the repair-keyed unanswered set contains the reviewer-keyed one by construction #3 -> forward-ported at the close of this run, with `forward-port.sh --check` run afterwards to confirm the mirror matches #4 -> reported, pre-existing, and not repaired here | the two rules said to corroborate each other are NESTED by construction - the repair-keyed unanswered set contains the reviewer-keyed one - so their agreement was guaranteed before either ran, and that agreement is what licensed the schema conclusion. BLOCKING; the withdrawn fix is still LIVE in the installed copy, byte-identical to the pre-revert commit, so every project on this machine loads the roll-up measured at 579/690. BLOCKING; known\\_issues.py:177 asserts the release notes claim zero without reading them | | |
| BG0592 | 2026-08-25 | authoring session | 2026-08-25 | #1 -> the row now reads "extend \_claims\_completion's status\_vocab call with a project root", which is both an edit verb and the direction actually executed. `verify\_ac.py testplan derive --unit BG0592 --dry-run` is back to exit 0 reporting 18 rows for 15 criteria, and AC11's mutant was re-executed against the corrected wording and KILLED #2 -> the prose half now states 18 executed with 17 killed, one equivalent with its reason named, and one equivalent RETRACTED on the record, which is what the derived half says #3 -> test\_gate.py's MUTANT docstring rewritten to the row's wording verbatim | the Verification depth line read `all 18 KILLED` beside a derived half saying killed 17 with one ruled equivalent, contradicting itself within one line; the test docstring and the plan row described one edit in two different wordings | | |
| BG0581 | 2026-08-25 | authoring session | 2026-08-25 | changelog.d/BG0581.md claimed the state is said in the batch's OWN voc -> changelog.d/BG0581.md rewritten to state what the code does, re-checked through `sprint.py plan --format json` after the fix | `\\_state\\_for\\_batch` wrote the two-role cap over the vocabulary-resolved story entry, so a story batch on the `triage` rung reported `Triaged` - not a story status - falsifying AC4's own general clause through the shipped CLI (sprint.py:3186-3187), and AC4's test asserted a strictly weaker claim than AC4 so it could not see it. BLOCKING; `\\_terminal\\_in\\_type\\_vocab` reported a finding as unmoved by a `triage` rung, contradicted by `sdlc\\_md.triage\\_target` under schema v3 which is the current seed; the `basis` string read `no gate in this project caps this batch below its status unchanged` and reached the seat brief verbatim, and the renderer appended a hardcoded `, NOT Done` to a per-type state list; `status\\_vocab(type\\_)` was called without repo\\_root though root was in scope, unlike sprint.py:6739; the unknown-rung fallback resolved the terminal to `done` but tested the unrecognised key for COMPLETING\\_RUNGS membership, latent behind argparse choices | | |
| BG0581 | 2026-08-25 | authoring session | 2026-08-25 | `\_terminal\_in\_type\_vocab` reported a finding as unmoved by a tria -> the resolver now consults `sdlc\_md.triage\_target` for a triage rung, gated on `is\_schema\_v3(root)`, so the answer is derived from the project's schema rather than assumed. Under v2 there is no inbox lane and the rung genuinely moves nothing | `\\_state\\_for\\_batch` wrote the two-role cap over the vocabulary-resolved story entry, so a story batch on the `triage` rung reported `Triaged` - not a story status - falsifying AC4's own general clause through the shipped CLI (sprint.py:3186-3187), and AC4's test asserted a strictly weaker claim than AC4 so it could not see it. BLOCKING; changelog.d/BG0581.md claimed the state is said in the batch's OWN vocabulary, which the same command falsifies. BLOCKING; the `basis` string read `no gate in this project caps this batch below its status unchanged` and reached the seat brief verbatim, and the renderer appended a hardcoded `, NOT Done` to a per-type state list; `status\\_vocab(type\\_)` was called without repo\\_root though root was in scope, unlike sprint.py:6739; the unknown-rung fallback resolved the terminal to `done` but tested the unrecognised key for COMPLETING\\_RUNGS membership, latent behind argparse choices | | |
| BG0581 | 2026-08-25 | authoring session | 2026-08-25 | the `basis` string read no gate in this project caps this batch below -> STATE\_UNCHANGED is now the noun phrase `no status change`, the basis names the rung and its per-type terminal, and the renderer no longer appends a fixed shortfall to a per-type state list | `\\_state\\_for\\_batch` wrote the two-role cap over the vocabulary-resolved story entry, so a story batch on the `triage` rung reported `Triaged` - not a story status - falsifying AC4's own general clause through the shipped CLI (sprint.py:3186-3187), and AC4's test asserted a strictly weaker claim than AC4 so it could not see it. BLOCKING; changelog.d/BG0581.md claimed the state is said in the batch's OWN vocabulary, which the same command falsifies. BLOCKING; `\\_terminal\\_in\\_type\\_vocab` reported a finding as unmoved by a `triage` rung, contradicted by `sdlc\\_md.triage\\_target` under schema v3 which is the current seed; `status\\_vocab(type\\_)` was called without repo\\_root though root was in scope, unlike sprint.py:6739; the unknown-rung fallback resolved the terminal to `done` but tested the unrecognised key for COMPLETING\\_RUNGS membership, latent behind argparse choices | | |
| BG0581 | 2026-08-25 | authoring session | 2026-08-25 | `status\_vocab(type\_)` was called without repo\_root though root was -> the resolver takes `root` and passes it to `status\_vocab` and the vocabulary check, matching sprint.py:6739 | `\\_state\\_for\\_batch` wrote the two-role cap over the vocabulary-resolved story entry, so a story batch on the `triage` rung reported `Triaged` - not a story status - falsifying AC4's own general clause through the shipped CLI (sprint.py:3186-3187), and AC4's test asserted a strictly weaker claim than AC4 so it could not see it. BLOCKING; changelog.d/BG0581.md claimed the state is said in the batch's OWN vocabulary, which the same command falsifies. BLOCKING; `\\_terminal\\_in\\_type\\_vocab` reported a finding as unmoved by a `triage` rung, contradicted by `sdlc\\_md.triage\\_target` under schema v3 which is the current seed; the `basis` string read `no gate in this project caps this batch below its status unchanged` and reached the seat brief verbatim, and the renderer appended a hardcoded `, NOT Done` to a per-type state list; the unknown-rung fallback resolved the terminal to `done` but tested the unrecognised key for COMPLETING\\_RUNGS membership, latent behind argparse choices | | |
| BG0581 | 2026-08-25 | authoring session | 2026-08-25 | the unknown-rung fallback resolved the terminal to `done` but tested t -> `rung\_key` is resolved to a known key ONCE, before both the terminal lookup and the COMPLETING\_RUNGS membership test, so the two halves cannot disagree | `\\_state\\_for\\_batch` wrote the two-role cap over the vocabulary-resolved story entry, so a story batch on the `triage` rung reported `Triaged` - not a story status - falsifying AC4's own general clause through the shipped CLI (sprint.py:3186-3187), and AC4's test asserted a strictly weaker claim than AC4 so it could not see it. BLOCKING; changelog.d/BG0581.md claimed the state is said in the batch's OWN vocabulary, which the same command falsifies. BLOCKING; `\\_terminal\\_in\\_type\\_vocab` reported a finding as unmoved by a `triage` rung, contradicted by `sdlc\\_md.triage\\_target` under schema v3 which is the current seed; the `basis` string read `no gate in this project caps this batch below its status unchanged` and reached the seat brief verbatim, and the renderer appended a hardcoded `, NOT Done` to a per-type state list; `status\\_vocab(type\\_)` was called without repo\\_root though root was in scope, unlike sprint.py:6739 | | |
| BG0592 | 2026-08-25 | authoring session | 2026-08-25 | the Verification depth line read `all 18 KILLED` beside a derived half -> the prose half now states 18 executed with 17 killed, one equivalent with its reason named, and one equivalent RETRACTED on the record, which is what the derived half says | the re-aimed AC11 row carried no edit verb, so `verify\\_ac.py testplan derive --unit BG0592` went from exit 0 at the base ref to exit 2 and the shape checker listed BG0592 among the off-shape units, shipped in the same commit as the checker that reports them, BLOCKING; the test docstring and the plan row described one edit in two different wordings | | |
| BG0592 | 2026-08-25 | authoring session | 2026-08-25 | the test docstring and the plan row described one edit in two differen -> test\_gate.py's MUTANT docstring rewritten to the row's wording verbatim | the re-aimed AC11 row carried no edit verb, so `verify\\_ac.py testplan derive --unit BG0592` went from exit 0 at the base ref to exit 2 and the shape checker listed BG0592 among the off-shape units, shipped in the same commit as the checker that reports them, BLOCKING; the Verification depth line read `all 18 KILLED` beside a derived half saying killed 17 with one ruled equivalent, contradicting itself within one line | | |
| BG0606 | 2026-08-25 | authoring session | 2026-08-25 | the depth field claimed not-run 0 for US0671, US0674 and US0676 while -> the field now states that a later registration against an edited verify\_ac.py evicted the US0671 and US0676 rows, that `verify\_ac.py depth` reports EVIDENCE ABSENT for both, and that the eviction predates this unit's base ref | `\\_titles\\_match\\_criteria` asserted a property the code did not have: it ran after the derived-shape check which regenerates the Title cell, so it could not fail on any real title defect, and its one independent failure mode was a FALSE POSITIVE on a `- [X]` checkbox its regex rejected and `parse\\_story` accepts. BLOCKING; the `Close blocked by` field stated a block the dry-run does not report; three LIVE duplicate ledger rows on BG0606 AC1 row 0, two naming a test node that no longer exists - filed as BG0614; the `derived-depth` lane checks each span against its own seal rather than re-deriving it, so it passed 1303 units including two whose evidence was gone - filed as CR0558 | | |
| BG0606 | 2026-08-25 | authoring session | 2026-08-25 | the `Close blocked by` field stated a block the dry-run does not repor -> rewritten to what the shipped entry point reports today, which is exit 0 with the plan at line 40, and it names both of its own earlier errors | `\\_titles\\_match\\_criteria` asserted a property the code did not have: it ran after the derived-shape check which regenerates the Title cell, so it could not fail on any real title defect, and its one independent failure mode was a FALSE POSITIVE on a `- [X]` checkbox its regex rejected and `parse\\_story` accepts. BLOCKING; the depth field claimed not-run 0 for US0671, US0674 and US0676 while the ledger had evicted 23 of their 33 rows, an eviction predating this unit's base ref; three LIVE duplicate ledger rows on BG0606 AC1 row 0, two naming a test node that no longer exists - filed as BG0614; the `derived-depth` lane checks each span against its own seal rather than re-deriving it, so it passed 1303 units including two whose evidence was gone - filed as CR0558 | | |
| BG0606 | 2026-08-25 | authoring session | 2026-08-25 | three LIVE duplicate ledger rows on BG0606 AC1 row 0, two naming a tes -> FILED as BG0614 after a full-ledger audit found nine such keys, four of them naming different tests | `\\_titles\\_match\\_criteria` asserted a property the code did not have: it ran after the derived-shape check which regenerates the Title cell, so it could not fail on any real title defect, and its one independent failure mode was a FALSE POSITIVE on a `- [X]` checkbox its regex rejected and `parse\\_story` accepts. BLOCKING; the depth field claimed not-run 0 for US0671, US0674 and US0676 while the ledger had evicted 23 of their 33 rows, an eviction predating this unit's base ref; the `Close blocked by` field stated a block the dry-run does not report; the `derived-depth` lane checks each span against its own seal rather than re-deriving it, so it passed 1303 units including two whose evidence was gone - filed as CR0558 | | |
| BG0606 | 2026-08-25 | authoring session | 2026-08-25 | the `derived-depth` lane checks each span against its own seal rather -> FILED as CR0558 | `\\_titles\\_match\\_criteria` asserted a property the code did not have: it ran after the derived-shape check which regenerates the Title cell, so it could not fail on any real title defect, and its one independent failure mode was a FALSE POSITIVE on a `- [X]` checkbox its regex rejected and `parse\\_story` accepts. BLOCKING; the depth field claimed not-run 0 for US0671, US0674 and US0676 while the ledger had evicted 23 of their 33 rows, an eviction predating this unit's base ref; the `Close blocked by` field stated a block the dry-run does not report; three LIVE duplicate ledger rows on BG0606 AC1 row 0, two naming a test node that no longer exists - filed as BG0614 | | |
| BG0607 | 2026-08-25 | authoring session | 2026-08-25 | the two rules said to corroborate each other are NESTED by constructio -> RETRO0109's lesson is replaced with the true one. State what would have to be true for two rules to disagree before treating their agreement as evidence, because the repair-keyed unanswered set contains the reviewer-keyed one by construction | the claim that nothing computed from stored data can separate the two cases was REFUTED from the ledger's own `Brief` column: keying the retraction on a matching fingerprint recovers a strict superset of the reviewer string, 81 units with an unanswered REJECT falling to 49, 32 recovered and none lost. BLOCKING; the withdrawn fix is still LIVE in the installed copy, byte-identical to the pre-revert commit, so every project on this machine loads the roll-up measured at 579/690. BLOCKING; known\\_issues.py:177 asserts the release notes claim zero without reading them | | |
| BG0607 | 2026-08-25 | authoring session | 2026-08-25 | the withdrawn fix is still LIVE in the installed copy, byte-identical -> forward-ported at the close of this run, with `forward-port.sh --check` run afterwards to confirm the mirror matches | the claim that nothing computed from stored data can separate the two cases was REFUTED from the ledger's own `Brief` column: keying the retraction on a matching fingerprint recovers a strict superset of the reviewer string, 81 units with an unanswered REJECT falling to 49, 32 recovered and none lost. BLOCKING; the two rules said to corroborate each other are NESTED by construction - the repair-keyed unanswered set contains the reviewer-keyed one - so their agreement was guaranteed before either ran, and that agreement is what licensed the schema conclusion. BLOCKING; known\\_issues.py:177 asserts the release notes claim zero without reading them | | |
| BG0607 | 2026-08-25 | authoring session | 2026-08-25 | known\_issues.py:177 asserts the release notes claim zero without read -> reported, pre-existing, and not repaired here | the claim that nothing computed from stored data can separate the two cases was REFUTED from the ledger's own `Brief` column: keying the retraction on a matching fingerprint recovers a strict superset of the reviewer string, 81 units with an unanswered REJECT falling to 49, 32 recovered and none lost. BLOCKING; the two rules said to corroborate each other are NESTED by construction - the repair-keyed unanswered set contains the reviewer-keyed one - so their agreement was guaranteed before either ran, and that agreement is what licensed the schema conclusion. BLOCKING; the withdrawn fix is still LIVE in the installed copy, byte-identical to the pre-revert commit, so every project on this machine loads the roll-up measured at 579/690. BLOCKING | | |
| BG0587 | 2026-08-25 | authoring session | 2026-08-25 | the `\_rung\_grades` guard `continue`s before BG0588's terminal check at sprint.py -> the guard no longer `continue`s. A type the rung cannot grade is treated as having nothing outstanding and FALLS THROUGH to the terminal question, which has a real answer for an epic because an epic holds `Ready`. Pinned by a new criterion AC4 and its test `test\_an\_epic\_short\_of\_the\_rungs\_terminal\_still\_blocks`, which uses an EPIC at Draft rather than a story, and by the mutant "continue for a type the rung cannot grade" executed and KILLED | `sprint.breakdown` at sprint.py:2131 still grades every type and names an epic the close's two surfaces skip, present at 5d4cc5a0 and untouched by this diff, filed as BG0613 | | |
| BG0587 | 2026-08-25 | authoring session | 2026-08-25 | `sprint.breakdown` at sprint.py:2131 still grades every type and names an epic -> FILED as BG0613, pre-existing at the base ref and untouched by this diff | the `\\_rung\\_grades` guard `continue`s before BG0588's terminal check at sprint.py:6725-6726, so a design-rung epic left at Draft raises no blocker - base 5d4cc5a0 returns 2 blockers on the fixture and HEAD returns 1, every test of that check uses a STORY so the suite stays green through the reversion, and an epic HOLDS `Ready` which is the design rung's terminal, so only the criteria question lacks an answer for it | | |
| BG0621 | 2026-08-26 | authoring session | 2026-08-26 | #1 -> the docstring now names the HEADING mutant, which the test does fail on - re-executed and killed. AC7's Test Plan row was re-declared to the same mutant and given its own criterion's title | `\\_is\\_open`'s casefold was unpinned - dropping it left all 28 tests green, and the corpus carries no lowercase terminal status to catch it, BLOCKING; AC7's Test Plan row duplicated AC3's mutant and carried a title that was not its own criterion; `TERMINAL` hand-copies the shipped bug vocabulary with nothing pinning the two together; AC2's criterion named `Blocked` as a bug status when it is not in the vocabulary at all; the unparseable report fired on `--bar` only, so a malformed filing stayed invisible to the per-commit path until a tag; the UNKNOWN line printed to stdout while the not-met line printed to stderr; a finding at `Severity: major` matches neither the barred nor the disclosed set and is silently absent from both surfaces, true at the base ref and filed separately | | |
| BG0621 | 2026-08-26 | authoring session | 2026-08-26 | #2 -> AC5's paired control gained a lowercase TERMINAL row, `fixed` at High. The mutant `status.strip() not in TERMINAL` now fails that test - re-executed and killed as AC5 | AC7's test docstring declared a mutant that cannot fail it - deleting the `unparseable` call from the `--bar` path and making `unparseable` return empty both left the test green, because it reads the corpus in-process and never calls `main`, while the header claimed killed 9 survived 0 on that basis, BLOCKING; AC7's Test Plan row duplicated AC3's mutant and carried a title that was not its own criterion; `TERMINAL` hand-copies the shipped bug vocabulary with nothing pinning the two together; AC2's criterion named `Blocked` as a bug status when it is not in the vocabulary at all; the unparseable report fired on `--bar` only, so a malformed filing stayed invisible to the per-commit path until a tag; the UNKNOWN line printed to stdout while the not-met line printed to stderr; a finding at `Severity: major` matches neither the barred nor the disclosed set and is silently absent from both surfaces, true at the base ref and filed separately | | |
| BG0621 | 2026-08-26 | authoring session | 2026-08-26 | #3 -> the row still names the same production change as AC3, which is honest since one change breaks two different properties, but it now states WHY the mutant is a heading change rather than a bar change, and carries AC7's own title | AC7's test docstring declared a mutant that cannot fail it - deleting the `unparseable` call from the `--bar` path and making `unparseable` return empty both left the test green, because it reads the corpus in-process and never calls `main`, while the header claimed killed 9 survived 0 on that basis, BLOCKING; `\\_is\\_open`'s casefold was unpinned - dropping it left all 28 tests green, and the corpus carries no lowercase terminal status to catch it, BLOCKING; `TERMINAL` hand-copies the shipped bug vocabulary with nothing pinning the two together; AC2's criterion named `Blocked` as a bug status when it is not in the vocabulary at all; the unparseable report fired on `--bar` only, so a malformed filing stayed invisible to the per-commit path until a tag; the UNKNOWN line printed to stdout while the not-met line printed to stderr; a finding at `Severity: major` matches neither the barred nor the disclosed set and is silently absent from both surfaces, true at the base ref and filed separately | | |
| BG0621 | 2026-08-26 | authoring session | 2026-08-26 | #4 -> AC8 added, asserting `known\_issues.TERMINAL` equals the shipped bug vocabulary. Mutant: add a status the vocabulary does not carry. Executed and killed | AC7's test docstring declared a mutant that cannot fail it - deleting the `unparseable` call from the `--bar` path and making `unparseable` return empty both left the test green, because it reads the corpus in-process and never calls `main`, while the header claimed killed 9 survived 0 on that basis, BLOCKING; `\\_is\\_open`'s casefold was unpinned - dropping it left all 28 tests green, and the corpus carries no lowercase terminal status to catch it, BLOCKING; AC7's Test Plan row duplicated AC3's mutant and carried a title that was not its own criterion; AC2's criterion named `Blocked` as a bug status when it is not in the vocabulary at all; the unparseable report fired on `--bar` only, so a malformed filing stayed invisible to the per-commit path until a tag; the UNKNOWN line printed to stdout while the not-met line printed to stderr; a finding at `Severity: major` matches neither the barred nor the disclosed set and is silently absent from both surfaces, true at the base ref and filed separately | | |
| BG0621 | 2026-08-26 | authoring session | 2026-08-26 | #5 -> the criterion now says `Blocked` is NOT in the vocabulary and is present on purpose, because an out-of-vocabulary status must fall OPEN so a typo over-refuses rather than hides | AC7's test docstring declared a mutant that cannot fail it - deleting the `unparseable` call from the `--bar` path and making `unparseable` return empty both left the test green, because it reads the corpus in-process and never calls `main`, while the header claimed killed 9 survived 0 on that basis, BLOCKING; `\\_is\\_open`'s casefold was unpinned - dropping it left all 28 tests green, and the corpus carries no lowercase terminal status to catch it, BLOCKING; AC7's Test Plan row duplicated AC3's mutant and carried a title that was not its own criterion; `TERMINAL` hand-copies the shipped bug vocabulary with nothing pinning the two together; the unparseable report fired on `--bar` only, so a malformed filing stayed invisible to the per-commit path until a tag; the UNKNOWN line printed to stdout while the not-met line printed to stderr; a finding at `Severity: major` matches neither the barred nor the disclosed set and is silently absent from both surfaces, true at the base ref and filed separately | | |
| BG0621 | 2026-08-26 | authoring session | 2026-08-26 | #6 -> AC9 added: `\_warn\_unparseable` runs on the check and write path too, so the per-commit lane names it. Mutant: delete that call. Executed and killed | AC7's test docstring declared a mutant that cannot fail it - deleting the `unparseable` call from the `--bar` path and making `unparseable` return empty both left the test green, because it reads the corpus in-process and never calls `main`, while the header claimed killed 9 survived 0 on that basis, BLOCKING; `\\_is\\_open`'s casefold was unpinned - dropping it left all 28 tests green, and the corpus carries no lowercase terminal status to catch it, BLOCKING; AC7's Test Plan row duplicated AC3's mutant and carried a title that was not its own criterion; `TERMINAL` hand-copies the shipped bug vocabulary with nothing pinning the two together; AC2's criterion named `Blocked` as a bug status when it is not in the vocabulary at all; the UNKNOWN line printed to stdout while the not-met line printed to stderr; a finding at `Severity: major` matches neither the barred nor the disclosed set and is silently absent from both surfaces, true at the base ref and filed separately | | |
| BG0621 | 2026-08-26 | authoring session | 2026-08-26 | #7 -> reported and not repaired here - both lines are diagnostics on a refusing path and the exit code is unambiguous, so it is left for a stream-consistency sweep rather than widened into this unit | AC7's test docstring declared a mutant that cannot fail it - deleting the `unparseable` call from the `--bar` path and making `unparseable` return empty both left the test green, because it reads the corpus in-process and never calls `main`, while the header claimed killed 9 survived 0 on that basis, BLOCKING; `\\_is\\_open`'s casefold was unpinned - dropping it left all 28 tests green, and the corpus carries no lowercase terminal status to catch it, BLOCKING; AC7's Test Plan row duplicated AC3's mutant and carried a title that was not its own criterion; `TERMINAL` hand-copies the shipped bug vocabulary with nothing pinning the two together; AC2's criterion named `Blocked` as a bug status when it is not in the vocabulary at all; the unparseable report fired on `--bar` only, so a malformed filing stayed invisible to the per-commit path until a tag; a finding at `Severity: major` matches neither the barred nor the disclosed set and is silently absent from both surfaces, true at the base ref and filed separately | | |
| BG0621 | 2026-08-26 | authoring session | 2026-08-26 | #8 -> FILED as BG0624 - pre-existing at the base ref and outside this unit's diff | AC7's test docstring declared a mutant that cannot fail it - deleting the `unparseable` call from the `--bar` path and making `unparseable` return empty both left the test green, because it reads the corpus in-process and never calls `main`, while the header claimed killed 9 survived 0 on that basis, BLOCKING; `\\_is\\_open`'s casefold was unpinned - dropping it left all 28 tests green, and the corpus carries no lowercase terminal status to catch it, BLOCKING; AC7's Test Plan row duplicated AC3's mutant and carried a title that was not its own criterion; `TERMINAL` hand-copies the shipped bug vocabulary with nothing pinning the two together; AC2's criterion named `Blocked` as a bug status when it is not in the vocabulary at all; the unparseable report fired on `--bar` only, so a malformed filing stayed invisible to the per-commit path until a tag; the UNKNOWN line printed to stdout while the not-met line printed to stderr | | |
| BG0618 | 2026-08-26 | authoring session | 2026-08-26 | #1 -> the escapes are bound OUTSIDE the f-string, so no backslash sits in an f-string expression. Verified by compiling under a real python3.10: the working tree now compiles where it did not. AC7 added and pinned by an AST scan of every f-string expression segment | AC3's READ half was unimplemented - the criterion says an unreadable row is REPORTED and the code only skipped it, keeping the silence that made this defect dangerous, BLOCKING; the test docstring introduced a SyntaxWarning through a non-raw string; the artefact and the code comment claimed 68 chunks across 11 units where the ledger holds 67; the artefact claimed 72 characters lost where the reproduction drops 73; the escape could not express a trailing literal backslash, so evidence ending in one silently swallowed the closure after it and nothing refused the merge; the declared 3.10 floor has no guard anywhere and sprint\\_report.py already violates it at HEAD, with CI pinned to 3.12; no changelog fragment was staged | | |
| BG0618 | 2026-08-26 | authoring session | 2026-08-26 | #2 -> `parse\_closures` now PRINTS a warning naming the row it skips, on stderr, and still does not raise. AC8 added and its mutant executed and killed | a backslash inside an f-string EXPRESSION is legal only from Python 3.12 and this project's floor is 3.10 in six shipped places, so `critic.py` stopped parsing on the interpreter Ubuntu 22.04 ships - verified by compiling working tree and HEAD under a real python3.10, HEAD compiles and the tree did not - and the conformance lane, `sprint` and `transition` all import it, so the review gate died at import, BLOCKING; the test docstring introduced a SyntaxWarning through a non-raw string; the artefact and the code comment claimed 68 chunks across 11 units where the ledger holds 67; the artefact claimed 72 characters lost where the reproduction drops 73; the escape could not express a trailing literal backslash, so evidence ending in one silently swallowed the closure after it and nothing refused the merge; the declared 3.10 floor has no guard anywhere and sprint\\_report.py already violates it at HEAD, with CI pinned to 3.12; no changelog fragment was staged | | |
| BG0618 | 2026-08-26 | authoring session | 2026-08-26 | #4 -> corrected to 67 in the artefact, the code comment and the test docstring | a backslash inside an f-string EXPRESSION is legal only from Python 3.12 and this project's floor is 3.10 in six shipped places, so `critic.py` stopped parsing on the interpreter Ubuntu 22.04 ships - verified by compiling working tree and HEAD under a real python3.10, HEAD compiles and the tree did not - and the conformance lane, `sprint` and `transition` all import it, so the review gate died at import, BLOCKING; AC3's READ half was unimplemented - the criterion says an unreadable row is REPORTED and the code only skipped it, keeping the silence that made this defect dangerous, BLOCKING; the test docstring introduced a SyntaxWarning through a non-raw string; the artefact claimed 72 characters lost where the reproduction drops 73; the escape could not express a trailing literal backslash, so evidence ending in one silently swallowed the closure after it and nothing refused the merge; the declared 3.10 floor has no guard anywhere and sprint\\_report.py already violates it at HEAD, with CI pinned to 3.12; no changelog fragment was staged | | |
| BG0618 | 2026-08-26 | authoring session | 2026-08-26 | #5 -> corrected to 73 in the artefact and the code comment | a backslash inside an f-string EXPRESSION is legal only from Python 3.12 and this project's floor is 3.10 in six shipped places, so `critic.py` stopped parsing on the interpreter Ubuntu 22.04 ships - verified by compiling working tree and HEAD under a real python3.10, HEAD compiles and the tree did not - and the conformance lane, `sprint` and `transition` all import it, so the review gate died at import, BLOCKING; AC3's READ half was unimplemented - the criterion says an unreadable row is REPORTED and the code only skipped it, keeping the silence that made this defect dangerous, BLOCKING; the test docstring introduced a SyntaxWarning through a non-raw string; the artefact and the code comment claimed 68 chunks across 11 units where the ledger holds 67; the escape could not express a trailing literal backslash, so evidence ending in one silently swallowed the closure after it and nothing refused the merge; the declared 3.10 floor has no guard anywhere and sprint\\_report.py already violates it at HEAD, with CI pinned to 3.12; no changelog fragment was staged | | |
| BG0618 | 2026-08-26 | authoring session | 2026-08-26 | #6 -> `split\_items` is a SCANNER rather than a lookbehind, and a literal backslash is escapable too. AC6 added: a trailing backslash no longer swallows the next closure, mutant executed and killed | a backslash inside an f-string EXPRESSION is legal only from Python 3.12 and this project's floor is 3.10 in six shipped places, so `critic.py` stopped parsing on the interpreter Ubuntu 22.04 ships - verified by compiling working tree and HEAD under a real python3.10, HEAD compiles and the tree did not - and the conformance lane, `sprint` and `transition` all import it, so the review gate died at import, BLOCKING; AC3's READ half was unimplemented - the criterion says an unreadable row is REPORTED and the code only skipped it, keeping the silence that made this defect dangerous, BLOCKING; the test docstring introduced a SyntaxWarning through a non-raw string; the artefact and the code comment claimed 68 chunks across 11 units where the ledger holds 67; the artefact claimed 72 characters lost where the reproduction drops 73; the declared 3.10 floor has no guard anywhere and sprint\\_report.py already violates it at HEAD, with CI pinned to 3.12; no changelog fragment was staged | | |
| BG0618 | 2026-08-26 | authoring session | 2026-08-26 | #7 -> FILED as CR0561 - pre-existing at HEAD, where sprint\_report.py:420 already violates the floor, and outside this unit's diff | a backslash inside an f-string EXPRESSION is legal only from Python 3.12 and this project's floor is 3.10 in six shipped places, so `critic.py` stopped parsing on the interpreter Ubuntu 22.04 ships - verified by compiling working tree and HEAD under a real python3.10, HEAD compiles and the tree did not - and the conformance lane, `sprint` and `transition` all import it, so the review gate died at import, BLOCKING; AC3's READ half was unimplemented - the criterion says an unreadable row is REPORTED and the code only skipped it, keeping the silence that made this defect dangerous, BLOCKING; the test docstring introduced a SyntaxWarning through a non-raw string; the artefact and the code comment claimed 68 chunks across 11 units where the ledger holds 67; the artefact claimed 72 characters lost where the reproduction drops 73; the escape could not express a trailing literal backslash, so evidence ending in one silently swallowed the closure after it and nothing refused the merge; no changelog fragment was staged | | |
| BG0618 | 2026-08-26 | authoring session | 2026-08-26 | #8 -> changelog.d/BG0618.md written in this commit | a backslash inside an f-string EXPRESSION is legal only from Python 3.12 and this project's floor is 3.10 in six shipped places, so `critic.py` stopped parsing on the interpreter Ubuntu 22.04 ships - verified by compiling working tree and HEAD under a real python3.10, HEAD compiles and the tree did not - and the conformance lane, `sprint` and `transition` all import it, so the review gate died at import, BLOCKING; AC3's READ half was unimplemented - the criterion says an unreadable row is REPORTED and the code only skipped it, keeping the silence that made this defect dangerous, BLOCKING; the test docstring introduced a SyntaxWarning through a non-raw string; the artefact and the code comment claimed 68 chunks across 11 units where the ledger holds 67; the artefact claimed 72 characters lost where the reproduction drops 73; the escape could not express a trailing literal backslash, so evidence ending in one silently swallowed the closure after it and nothing refused the merge; the declared 3.10 floor has no guard anywhere and sprint\\_report.py already violates it at HEAD, with CI pinned to 3.12 | | |
| BG0618 | 2026-08-26 | authoring session | 2026-08-26 | #3 -> the docstring is a raw string\; the module compiles warning-clean | a backslash inside an f-string EXPRESSION is legal only from Python 3.12 and this project's floor is 3.10 in six shipped places, so `critic.py` stopped parsing on the interpreter Ubuntu 22.04 ships - verified by compiling working tree and HEAD under a real python3.10, HEAD compiles and the tree did not - and the conformance lane, `sprint` and `transition` all import it, so the review gate died at import, BLOCKING; AC3's READ half was unimplemented - the criterion says an unreadable row is REPORTED and the code only skipped it, keeping the silence that made this defect dangerous, BLOCKING; the artefact and the code comment claimed 68 chunks across 11 units where the ledger holds 67; the artefact claimed 72 characters lost where the reproduction drops 73; the escape could not express a trailing literal backslash, so evidence ending in one silently swallowed the closure after it and nothing refused the merge; the declared 3.10 floor has no guard anywhere and sprint\\_report.py already violates it at HEAD, with CI pinned to 3.12; no changelog fragment was staged | | |
| BG0607 | 2026-08-26 | authoring session | 2026-08-26 | #1 -> the backfill is REMOVED - all 19 rows, written minutes earlier and never committed. Confirmed on US0666, US0629 and US0591 that the cited approval's fingerprint differs from the standing rejection's in every case, which is exactly what this unit's own code refuses. The nineteen are resolved instead by a recorded WAIVER naming each unit, which is a judgement somebody made rather than evidence nobody can stand behind; #2 -> removed with the rest of the backfill. No closure now files a finding into the unit it was raised on, because no closure was written; #3 -> `repair\_state` now derives outstanding from EVERY unanswered rejection rather than the standing one. Measured: 65 previously invisible findings across the 19 came into view. Closures are matched PER rejection rather than pooled, because an ordinal is positional and pooling made a round-one closure silently answer a later round's first finding - AC4 and AC5 added, both mutants executed and killed; #4 -> AC4 in its old form is GONE. The criteria were re-authored to what actually shipped: the roll-up, the repair-state fix, and the waiver. The stale ledger row went with the old plan; #5 -> the release notes no longer credit a backfill. They state that the roll-up surfaces 19 unanswered rejections, that a backfill was rejected at review for citing the cross-seat approval this rule refuses, and that the 19 are waived by recorded decision; #6 -> reported and NOT repaired here. It is latent - 0 of 854 rows carry an empty brief - and the guard belongs with the config stand-down that makes it reachable rather than bolted onto this roll-up. Filed for the next run; #7 -> the checker is REMOVED along with the backfill it graded. Nothing hard-codes a unit list any more; #8 -> BG0618 residue, pre-existing, and the once-per-process warning this run shipped is what makes it visible at all | - | | |
| BG0615 | 2026-08-27 | authoring session | 2026-08-27 | #1 -> the supersession note is no longer a return. `\_onboarding\_hint` returns None when every pending stage is superseded, and the note is printed as an `advisory:` line beside the ladder's answer by `superseded\_marker\_advisory`. Reproduced the crash on the review's own fixture first, then confirmed exit 0 with the ladder answering; #2 -> same change - the ladder now answers because the onboarding check falls through instead of returning. AC4 added and verified THROUGH `status.main(["hint"])` rather than in-process; #3 -> the `plan` stage is deliberately NOT satisfiable from the tree. `sprint plan` writes the run state and `.local/` is gitignored, so it stays pending until the operator confirms it, which is what guided onboarding is for; #4 -> `cmd\_guided` now calls `first\_incomplete(state, root)` at both call sites, so the hint and the command it names read the same tree; #5 -> the agents oracle tests every file in AGENT\_FILES, which is AGENTS.md and CLAUDE.md. AC5 added, mutant executed and killed; #6 -> the decompose oracle tests epics AND stories, matching what the stage directs. Covered by AC5's second half; #7 -> help/hint.md states the shipped contract - advisories sit beside the next step and never replace it - and is declared in Affects; #8 -> AC4 drives `status.main(["hint"])`, the shipped command. It is the criterion that would have caught the crash, and it exists because this review found the crash instead | - | | |
| BG0622 | 2026-08-27 | engineering; session | 2026-08-27 | #1 -> AC2 rewritten: a field PRESENT but carrying no verdict (empty string, or JSON null) is REFUSED naming that field, while false and the string 'no' are both accepted at polarity no. Its mutant is now `if f not in d`. The r2 reviewer executed both halves - under the mutant, empty and null are ADMITTED so AC2 goes red\; under the correct fix both are refused and false/no both record. AC2 is the only row that dies on that mutant.; #2 -> AC3 CUT from this unit and replaced by a CLI criterion. The sweep was filed as BG0627 with all five modules declared, and CARRIED rather than added to this batch. The r2 reviewer confirmed BG0622 now stands alone with its ACs inside its declared Affects.; #3 -> BG0627 carries a per-TYPE policy rather than one blanket rule: presence-and-coerce for typed fields, presence-and-non-empty-after-coercion for prose, because storing the string False as a rationale is worse than the bug. BG0622 no longer makes any claim about prose fields.; #4 -> The new AC3 drives `sprint.py goal-review record --fields-file` as the shipped command and is red today at exit 2. The r2 reviewer confirmed the polarity clause is the load-bearing half - unwiring `\_seat\_from\_dict` from `cmd\_goal\_review` leaves both library rows green while the CLI exits 0 and stores a raw false that reads unclear.; #5 -> Closed by the AC2 rewrite above\; r2 verdict APPROVE, brief 43e68b01a06a, with the mutant matrix executed rather than read.; #6 -> Closed by filing BG0627 (5 points, 10 files, boundary check included) and carrying it. Residue named by r2 and NOT claimed fixed: sprint.py keeps its own falsey-collapse one line below the guard under repair, silently dropping a per-clause answer of false\; BG0627's Affects excludes sprint.py, so it is recorded against that unit rather than closed here. | - | | |
| BG0629 | 2026-08-27 | engineering; session | 2026-08-27 | #1 -> AC3's mutant REPLACED with the wiring mutant the r2 review named: remove the `\_test\_plan\_gate` call from the block list at transition.py:1049. The r3 review confirmed by reading 1040-1060 that the call site is separable from the function body, that the mutant kills only AC3's refusal half, and that AC1 survives it because AC1 exercises the gate directly.; #2 -> The join repair was CUT from this unit and filed as BG0631 (5 points, declaring critic.py, its test, reference-schema.md and the ledger), carrying the 109-row backfill, the writer changes, the schema contract and the US0674/US0675 conformance movement. AC5 now carries the cheap residual guard instead. The r3 review confirmed the split is honest and that BG0631 carries the whole of what was removed.; #3 -> AC4 is now exactly the cross-phase case, and AC1 states the fixture holds plan-review rows only with the call passing phase explicitly. The r3 review confirmed `repair\_state` already accepts `phase` at critic.py:1222, so the caller change needs no signature work. The residue is stated rather than claimed closed: repair rows carry no phase column, so the leak survives whenever closures resolve by ordinal, which is why AC4's fixture names its closures by text.; #4 -> AC5's uncheckable byte-identical assertion was replaced by AC6, stated as a property: a delivery rejection with a complete repair whose tier depth is not covered must still read REJECT and reach its answer through conformance.py:355. The r3 review confirmed the mutant flips it, and named the fixture condition it needs - a later independent APPROVE carrying a different fingerprint, without which the relocation returns the REJECT as latest and the mutant survives.; #5 -> Both offending criteria were rewritten into fixtures rather than the disclosure narrowed, and the section now records that the earlier draft overstated itself. The r3 review re-read all six criteria against it and confirmed every claim is true as written: none names this run's three units and none reads the live ledger. | AC5's uncheckable assertion; the missing phase scoping on repairs; the false disclosure | | |
| BG0629 | 2026-08-27 | engineering; session | 2026-08-27 | #1 -> AC3's mutant REPLACED with the wiring mutant the r2 review named: remove the `\_test\_plan\_gate` call from the block list at transition.py:1049. The r3 review confirmed by reading 1040-1060 that the call site is separable from the function body, that the mutant kills only AC3's refusal half, and that AC1 survives it because AC1 exercises the gate directly.; #2 -> The join repair was CUT from this unit and filed as BG0631 (5 points, declaring critic.py, its test, reference-schema.md and the ledger), carrying the 109-row backfill, the writer changes, the schema contract and the US0674/US0675 conformance movement. AC5 now carries the cheap residual guard instead. The r3 review confirmed the split is honest and that BG0631 carries the whole of what was removed.; #3 -> AC4 is now exactly the cross-phase case, and AC1 states the fixture holds plan-review rows only with the call passing phase explicitly. The r3 review confirmed `repair\_state` already accepts `phase` at critic.py:1222, so the caller change needs no signature work. The residue is stated rather than claimed closed: repair rows carry no phase column, so the leak survives whenever closures resolve by ordinal, which is why AC4's fixture names its closures by text.; #4 -> AC5's uncheckable byte-identical assertion was replaced by AC6, stated as a property: a delivery rejection with a complete repair whose tier depth is not covered must still read REJECT and reach its answer through conformance.py:355. The r3 review confirmed the mutant flips it, and named the fixture condition it needs - a later independent APPROVE carrying a different fingerprint, without which the relocation returns the REJECT as latest and the mutant survives.; #5 -> Both offending criteria were rewritten into fixtures rather than the disclosure narrowed, and the section now records that the earlier draft overstated itself. The r3 review re-read all six criteria against it and confirmed every claim is true as written: none names this run's three units and none reads the live ledger. | AC5's uncheckable assertion; the missing phase scoping on repairs; the false disclosure | | |
| BG0629 | 2026-08-27 | engineering; session | 2026-08-27 | #1 -> The join repair was CUT from this unit and filed as BG0631 (5 points), which declares critic.py, its test, reference-schema.md and the ledger, and carries the new column, the 109-row backfill, the writer changes and the schema contract. AC5 now holds the cheap residual guard instead - the gate refuses when a unit has more unanswered rejections on the repair's date than it has repair rows - which the r3 review confirmed is implementable with no new column.; #2 -> BG0631 carries the US0674/US0675 delivery-lane movement as its own criterion, so the movement is disclosed in the unit that causes it rather than hidden inside this one. AC6 here pins the opposite direction: the delivery lane must still answer through conformance.py:355, which is what the caller placement buys.; #3 -> Recorded as confirmation rather than a defect - the r1 and r2 reviews both reproduced the premise, and r3 confirmed it again. No change was needed.; #4 -> AC3's mutant replaced with the wiring mutant: remove the `\_test\_plan\_gate` call from the block list at transition.py:1049. The r3 review read 1040-1060 and confirmed the call site is separable from the function body, that the mutant kills AC3's refusal half, and that AC1 survives it.; #5 -> Closed by the BG0631 split above. The schema change is declared in its own unit with its own Affects, and this unit's ACs now sit entirely inside transition.py and critic.py.; #6 -> Replaced by AC6, stated as a property rather than a snapshot: a delivery rejection with a complete repair whose tier depth is not covered still reads REJECT and reaches its answer through conformance.py:355. The r3 review confirmed the relocation mutant flips it, given a fixture that also holds a later independent APPROVE with a differing fingerprint.; #7 -> AC4 is now exactly the cross-phase case, and the call passes phase explicitly - `repair\_state` already accepts it at critic.py:1222. The residue is stated rather than claimed closed: repair rows carry no phase column, so the leak survives whenever closures resolve by ordinal, which is BG0631's fingerprint key, and is why AC4's fixture names its closures by text.; #8 -> Both offending criteria were rewritten into fixtures rather than the disclosure narrowed, and the section now records that the earlier draft overstated itself. The r3 review re-read all six criteria against it and confirmed every claim is true as written: none names this run's three units and none reads the live ledger. | - | | |
| BG0629 | 2026-08-27 | engineering; session | 2026-08-27 | #1 -> The join repair was CUT from this unit and filed as BG0631 (5 points), which declares critic.py, its test, reference-schema.md and the ledger, and carries the new column, the 109-row backfill, the writer changes and the schema contract. AC5 now holds the cheap residual guard instead - the gate refuses when a unit has more unanswered rejections on the repair's date than it has repair rows - which the r3 review confirmed is implementable with no new column.; #2 -> BG0631 carries the US0674/US0675 delivery-lane movement as its own criterion, so the movement is disclosed in the unit that causes it rather than hidden inside this one. AC6 here pins the opposite direction: the delivery lane must still answer through conformance.py:355, which is what the caller placement buys.; #3 -> Recorded as confirmation rather than a defect - the r1 and r2 reviews both reproduced the premise, and r3 confirmed it again. No change was needed.; #4 -> AC3's mutant replaced with the wiring mutant: remove the `\_test\_plan\_gate` call from the block list at transition.py:1049. The r3 review read 1040-1060 and confirmed the call site is separable from the function body, that the mutant kills AC3's refusal half, and that AC1 survives it.; #5 -> Closed by the BG0631 split above. The schema change is declared in its own unit with its own Affects, and this unit's ACs now sit entirely inside transition.py and critic.py.; #6 -> Replaced by AC6, stated as a property rather than a snapshot: a delivery rejection with a complete repair whose tier depth is not covered still reads REJECT and reaches its answer through conformance.py:355. The r3 review confirmed the relocation mutant flips it, given a fixture that also holds a later independent APPROVE with a differing fingerprint.; #7 -> AC4 is now exactly the cross-phase case, and the call passes phase explicitly - `repair\_state` already accepts it at critic.py:1222. The residue is stated rather than claimed closed: repair rows carry no phase column, so the leak survives whenever closures resolve by ordinal, which is BG0631's fingerprint key, and is why AC4's fixture names its closures by text.; #8 -> Both offending criteria were rewritten into fixtures rather than the disclosure narrowed, and the section now records that the earlier draft overstated itself. The r3 review re-read all six criteria against it and confirmed every claim is true as written: none names this run's three units and none reads the live ledger. | - | | |
| BG0625 | 2026-08-27 | engineering; session | 2026-08-27 | AC3's document mutant is mis-scoped - the conformance lane is story-scoped, so stripping a review record from a Done bug -> AC3 was REPLACED. The conformance oracle is gone entirely\; the criterion now drives critic.py record and critic.py show --format json in a fixture carrying the review.require\_brief\_provenance: false stand-down.; AC3's oracle is green before AND after by the criterion's own rationale, so it discriminates nothing beyond non-regressi -> Same closure: an oracle green before and after pins nothing, so it was replaced by one that is red today and green after, measured through the shipped command.; AC2 restates coverage already at test\\\_critic.py:5427 without saying so -> AC3 (now) states in terms that it restates test\_critic.py:5427 and why it is kept beside the criteria it guards rather than folded into them.; no criterion drives critic.py's CLI, entry point 0 of 3, though the bug's own Steps to Reproduce run through critic.py -> AC4 now drives critic.py record and critic.py show --format json. The derived depth field will report the entry-point count rather than the plan asserting one.; the real tree reads 732/814, 0 not, exit 0, so that half of the finding does not stand. -> Confirmed and closed as an OVER-CLAIM by the reviewer itself at round 3: the real tree reads 732/814 with 0 non-conformant and exit 0, measured twice with the exit code read separately. The copied-tree readings were filed as BG0628.; BLOCKING: AC3 - replace it with the missing entry-point row, driving critic.py record and critic.py show in a fixtur -> Closed by the AC3/AC4 replacement above - the entry-point row exists and carries the stand-down config in its fixture. | the reviewer-identity fallback is falsified by execution - 0 of 9 real pairs match on exact string, because every APPROVE row carries a different free-text parenthetical from its REJECT row, so the rule freezes all nine including the five it exists to preserve and is behaviourally identical to the blunt fail-closed rule it replaced; the rule also reinstates one this repository already SHIPPED AND WITHDREW at 579/690, recorded in \\_unanswered\\_rejects's own docstring, and test\\_critic.py:5427 - the test AC3 restates - carries a fixture of qa-seat-round-1 and qa-seat-round-2 that is a standing counterexample; AC5's split is reproducible only under an unstated seat-token rule and its mutant cannot produce it; AC2's fixture as drafted would use identical reviewer strings and so cannot fail on the corpus shape; AC1's mutant is a plain revert that kills AC1, AC4 and AC5 together, so AC1 is not independently pinned; two consequences of the severity raise were unstated - known\\_issues.py --bar now exits 1 naming BG0625 and contradicting the shipped v5.0.1 release notes, and seven Done units change standing verdict. CLOSED from r2: the - normalisation is right and critic.py:1443 already does it, so there is a precedent to reuse; AC4's mutant now isolates.; BLOCKING: the discriminator is falsified by the corpus; AC5's mutant cannot produce the stated split; AC2's fixture shape; the unstated release-bar and Done-unit consequences | | |
| BG0625 | 2026-08-27 | engineering; session | 2026-08-27 | the Proposed Fix is falsified by execution - an absent brief is stored as the literal -, not the empty string, so if -> CONFIRMED independently: critic.read\_verdicts returns 556 rows carrying the literal - and zero carrying the empty string. The Proposed Fix was rewritten to normalise - and "" to ABSENT at read time, reusing the .strip(" -") precedent already at critic.py:1443.; independently confirmed here, 556 of 856 rows in critic-verdicts.md carry -, so the Summary's "LATENT today: 0 of 854 -> Same measurement, recorded in the Summary in place of the false LATENT claim.; nine REJECTs are already retired by a later placeholder-brief APPROVE, of which FOUR are cross-seat (US0570, US0571, US0 -> All nine are named in AC5, with the four cross-seat and the five same-seat split out. Under the shipped rule all nine stand, because none carries a repair record - which is the honest reading, not a regression.; AC4's declared mutant kills AC3, not AC4 -> AC4's mutant was reassigned\; the cmd\_show bypass now sits on the row it actually kills.; no criterion measures the corpus the change actually moves. The reviewer's own conformance figure (583/814, 149 not, exi -> AC5 is the corpus criterion, naming all nine ids and stating that seven of them are at Done.; a copied tree reports 304, 671 or 583 depending only on which directories were copied, which is a separate defect now fi -> Filed as BG0628 - conformance check counts a unit whose verifier it could not resolve as a FAILURE rather than as unknown, so the same 814 units score 304, 671 or 732 depending only on which directories were copied.; BLOCKING: the Proposed Fix predicate (- is the absent value, and same-seat retirement must survive) -> Closed by the predicate rewrite and by the operator's ruling that a rejection is answered by its REPAIR record, which is BG0629 - shipped this run, and the two units carry one rule between them.; the AC4 mutant reassignment -> Closed by the reassignment above.; a missing corpus criterion naming the four cross-seat retirements -> AC5 names US0570, US0571, US0575 and US0576 as the cross-seat four, and US0569, US0572, US0574, BG0442 and BG0452 as the same-seat five. | the reviewer-identity fallback is falsified by execution - 0 of 9 real pairs match on exact string, because every APPROVE row carries a different free-text parenthetical from its REJECT row, so the rule freezes all nine including the five it exists to preserve and is behaviourally identical to the blunt fail-closed rule it replaced; the rule also reinstates one this repository already SHIPPED AND WITHDREW at 579/690, recorded in \\_unanswered\\_rejects's own docstring, and test\\_critic.py:5427 - the test AC3 restates - carries a fixture of qa-seat-round-1 and qa-seat-round-2 that is a standing counterexample; AC5's split is reproducible only under an unstated seat-token rule and its mutant cannot produce it; AC2's fixture as drafted would use identical reviewer strings and so cannot fail on the corpus shape; AC1's mutant is a plain revert that kills AC1, AC4 and AC5 together, so AC1 is not independently pinned; two consequences of the severity raise were unstated - known\\_issues.py --bar now exits 1 naming BG0625 and contradicting the shipped v5.0.1 release notes, and seven Done units change standing verdict. CLOSED from r2: the - normalisation is right and critic.py:1443 already does it, so there is a precedent to reuse; AC4's mutant now isolates.; BLOCKING: the discriminator is falsified by the corpus; AC5's mutant cannot produce the stated split; AC2's fixture shape; the unstated release-bar and Done-unit consequences | | |
| BG0625 | 2026-08-27 | engineering; session | 2026-08-27 | the reviewer-identity fallback is falsified by execution - 0 of 9 real pairs match on exact string, because every APPROV -> CONFIRMED by execution and ACCEPTED: 0 of 9 pairs match on exact reviewer string. The identity fallback was REMOVED from the Proposed Fix, and the reason is recorded there so the rule is not proposed a third time.; the rule also reinstates one this repository already SHIPPED AND WITHDREW at 579/690, recorded in \\\_unanswered\\\_rejects -> Recorded in the Proposed Fix: \_unanswered\_rejects's own docstring says reviewer-string keying shipped and was withdrawn at 579/690, and test\_critic.py:5427's fixture is a standing counterexample. Both are now cited in the artefact.; AC5's split is reproducible only under an unstated seat-token rule and its mutant cannot produce it -> Moot - the seat-token rule was not adopted. AC5 now asserts that all nine stand, which needs no identity rule at all.; AC2's fixture as drafted would use identical reviewer strings and so cannot fail on the corpus shape -> Moot for the same reason: AC2 no longer tests identity. It asserts that a same-seat placeholder-brief approval does NOT retire, which is the behaviour the corpus supports.; AC1's mutant is a plain revert that kills AC1, AC4 and AC5 together, so AC1 is not independently pinned -> AC1's mutant is now the read-time normalisation removal, and each of the five rows carries a mutant that isolates it.; two consequences of the severity raise were unstated - known\\\_issues.py --bar now exits 1 naming BG0625 and contradict -> Both are now stated. The release bar reads NOT met while this is open, and the operator ruled it stays red until this and BG0629 are Fixed\; the Done-unit consequence is written into AC5.; AC4's mutant now isolates. -> Recorded as confirmation rather than a defect - no change was needed.; BLOCKING: the discriminator is falsified by the corpus -> Closed by removing the identity fallback entirely.; AC5's mutant cannot produce the stated split -> Moot - there is no split to produce. All nine stand.; AC2's fixture shape -> AC2's fixture asserts the same-seat placeholder case stands, which is the corpus shape rather than an invented one.; the unstated release-bar and Done-unit consequences -> Closed by the same disclosure as finding 21. | - | | |
| BG0626 | 2026-08-27 | engineering; session | 2026-08-27 | AC1 mutant is a fix-revert and the assertion is count-only, so a split-before-the-marker implementation passes all three -> AC1 rewritten to assert the EXACT clause list rather than a count, and its mutant replaced with the split-before-the-marker implementation the reviewer had measured surviving. The round-2 review re-ran that implementation against the new wording and confirmed the exact-list match fails while the count-only one passes.; AC3 reads gitignored untracked sdlc-studio/.local/goal-review.json, unrunnable in CI, and its stated population is alrea -> AC3 rewritten to require a TRACKED fixture vendored under scripts/tests/, with the path added to Affects so the new file sits inside its own unit's review scope. It asserts the property over whatever the fixture holds rather than a hard-coded total, and the fixture is asserted NON-VACUOUS in the same test.; no criterion reaches the shipped CLI, derived depth reads entry point 0 of 3, so the claimed sprint-close UNANSWERED con -> AC4 added, driving the close's goal panel over a goal-review.json whose seat keys match the splitter's output. The round-2 review confirmed the panel is live at sprint.py:9039 and that six existing tests already drive it over a tmp root, so the oracle is real rather than aspirational.; BLOCKING: AC1 (exact-text assertion plus a within-branch mutant) -> Closed by the AC1 rewrite above - exact text plus the within-branch mutant.; AC3 (tracked fixture, corrected population) -> Closed by the AC3 rewrite above - tracked fixture, and the population corrected from 41/1 to 42/2 and then stated as a property rather than a number, because recording this run's own goal review moved it again.; the missing sprint-close panel criterion -> Closed by adding AC4, which is that criterion. | AC5's named mutant cannot be written in the file it names - `goal\\_clauses` receives the goal text and nothing else, so it cannot know whether a key matched; written unconditionally it collapses every goal to one clause and kills AC1, AC3 and AC4 too, and AC5 passes on today's unfixed code under every splitter including its own mutant, which reproduces round 2's finding on the very row added to repair round 2. The Proposed Fix commits in writing to two or more markers IN ASCENDING ORDER and nothing tests the ordering half - 0 of 42 stored goals are non-ascending, so a careless `len(markers) >= 2` that drops the check passes every row. AC4 quotes a PREFIX rather than the emitted line, which is `goal panel: achieved over 6 clause(s), 1 seat(s), no seat matched the author`, so `assertEqual` on the quoted text fails on correct code - the mirror of the trap AC4 itself names. AC3's mutant uniqueness is contingent on other rows' fixtures and unenforced, and AC1's mutant still kills AC3. The tracked fixture AC3 mandates is absent from `Affects`. CLOSED: AC2 is now killable and is the only place the one-versus-two threshold can be pinned, since 0 of 42 goals carry exactly one marker; AC4's substring trap is gone, its keys are literal and `author` is supplied; six clauses is the right number, markers at offsets 367/479/582/1076/1313/1524.; BLOCKING: AC5, and the untested ascending-order clause | | |
| BG0626 | 2026-08-27 | engineering; session | 2026-08-27 | AC2's declared input carries ZERO (n) markers, so the two-markers-to-one guard mutant cannot move it and the row is unki -> AC2's input changed to a goal carrying exactly ONE stray marker, which the two-markers-to-one guard mutant does move. The round-3 review executed it: three clauses today, three under the correct fix, one under the mutant - and confirmed that 0 of the 42 stored goals carry exactly one marker, so this hand-written input is the only place the threshold can be pinned.; AC4 asserts a string the code never prints - `close\\\_goal\\\_judgement` emits `panel['verdict'] or 'UNANSWERED'` over the -> AC4 now asserts the line the code actually emits and names all three traps in the criterion text, including that ANSWERED is a substring of UNANSWERED so an assertIn passes on the exact failure the criterion exists to catch. The round-3 review corrected the wording further, to STARTS WITH, because the emitted line continues past the quoted prefix.; AC4 is silent on `state["author"]`, without which the panel returns NOT RUN and the criterion measures nothing -> AC4 now states that the fixture supplies state["author"] matching no recorded seat, without which the panel returns NOT RUN and the criterion measures nothing.; AC4's "keys equal to the splitter's own output" is a tautology if the test computes them by calling goal\\\_clauses, so th -> AC4 now requires the clause keys to be written out LITERALLY in the test. Keys computed by calling goal\_clauses match under any splitter, so the revert mutant would have survived.; AC3 has no non-vacuity guard on the vendored fixture and its mutant is also caught by AC1 -> AC3 gained the non-vacuity guard and a corpus-only mutant - detect numbering by scanning only the first 200 characters, which moves the fixture and nothing else because both hand-written goals carry their first marker within 30 characters while this run's sits at offset 367.; no criterion covers a goal amended between the plan-time review and the close, where the panel splits state["sprint\\\_goa -> AC5 added for a goal amended between the plan-time review and the close, which this run does by construction.; AC3 no longer reads gitignored .local and no longer hard-codes a total. MOVED: entry point is genuinely reached by AC4, -> Recorded as confirmation rather than a defect - no change was needed.; BLOCKING: AC2 (an input the guard mutant actually moves) and AC4 (the emitted vocabulary, hard-coded keys, and state["au -> Closed by the AC2 and AC4 rewrites above. | AC5's named mutant cannot be written in the file it names - `goal\\_clauses` receives the goal text and nothing else, so it cannot know whether a key matched; written unconditionally it collapses every goal to one clause and kills AC1, AC3 and AC4 too, and AC5 passes on today's unfixed code under every splitter including its own mutant, which reproduces round 2's finding on the very row added to repair round 2. The Proposed Fix commits in writing to two or more markers IN ASCENDING ORDER and nothing tests the ordering half - 0 of 42 stored goals are non-ascending, so a careless `len(markers) >= 2` that drops the check passes every row. AC4 quotes a PREFIX rather than the emitted line, which is `goal panel: achieved over 6 clause(s), 1 seat(s), no seat matched the author`, so `assertEqual` on the quoted text fails on correct code - the mirror of the trap AC4 itself names. AC3's mutant uniqueness is contingent on other rows' fixtures and unenforced, and AC1's mutant still kills AC3. The tracked fixture AC3 mandates is absent from `Affects`. CLOSED: AC2 is now killable and is the only place the one-versus-two threshold can be pinned, since 0 of 42 goals carry exactly one marker; AC4's substring trap is gone, its keys are literal and `author` is supplied; six clauses is the right number, markers at offsets 367/479/582/1076/1313/1524.; BLOCKING: AC5, and the untested ascending-order clause | | |
| BG0626 | 2026-08-27 | engineering; session | 2026-08-27 | AC5's named mutant cannot be written in the file it names - `goal\\\_clauses` receives the goal text and nothing else, so -> AC5 re-pointed at the file that owns the edit: sprint.py's \_recorded\_clause\_verdicts, where re-introducing the whole-goal FAN is what makes an amended goal read as answered. The criterion now says in terms that a mutant named against goal\_clauses cannot express this, because that function receives the goal text and nothing else.; written unconditionally it collapses every goal to one clause and kills AC1, AC3 and AC4 too, and AC5 passes on today's -> Same closure - the unconditional form was rejected for exactly that reason, and the mutant now names a change to a different function so it cannot kill AC1, AC3 or AC4.; AC4's substring trap is gone, its keys are literal and `author` is supplied -> Recorded as confirmation rather than a defect.; six clauses is the right number, markers at offsets 367/479/582/1076/1313/1524. -> Recorded as confirmation. Six is the number AC4 asserts, and the offsets are the reason AC3's 200-character mutant is unique to AC3.; BLOCKING: AC5, and the untested ascending-order clause -> AC5 closed above\; AC6 ADDED for the ascending-order clause, which the Proposed Fix commits to in writing and nothing tested - 0 of the 42 stored goals are non-ascending, so a careless count-only guard would have passed every other row here. | - | | |
| BG0616 | 2026-08-27 | engineering; session | 2026-08-27 | AC2 SURVIVES its own mutant - its fixture is a unit named in NO retro, and the mutant counts a unit named ANYWHERE, so a -> AC2 re-fixtured on a unit named in a retro's PROSE but in no disposition row, which the named-anywhere mutant does move. The r2 review confirmed it flips from OWED to covered under that mutant, where the old fixture could not be moved by any widening.; AC3 survives its own mutant too, and the plan states the reason in the mutant's own justification, that the two named un -> The corpus duty moved off AC3 onto AC5, with a corpus-only mutant: read only the FIRST id out of a `fixed-in:` detail. The r2 review measured it dropping BG0602 from RETRO0109's multi-id row, and confirmed AC5 asserts a non-empty residue rather than an empty owed set.; AC1 is a bare revert that also kills AC3 and does not separate the two declared coverage sources, and because the criter -> The fix now reads ONE source, the `fixed` dispositions, so there is nothing to separate. AC1's mutant narrows to `declined` rows instead. The r2 review measured that the ids inside declined details intersect the owed set in nothing, so it leaves the positives red and every still-OWED row green.; the Proposed Fix's claim that the Blocked section is gated content rather than free prose, needing no second parser, is -> CONFIRMED and the fix CHANGED: the Blocked section is no longer read at all. `Blocked / deferred` is not in REQUIRED\_SECTIONS and nothing in retro.py or close\_owed.py mentions it, so it is free prose. AC4 now pins that decision, and the Proposed Fix records the correction with RETRO0109's own bullet quoted, where BG0612 is named as future work in the same sentence as the units being accounted for.; no row pins the hole the corpus actually demonstrates, that RETRO0109's Blocked bullet names BG0612 and CR0557 as FUTURE -> Closed by the same change - the free-prose route is not read, so the hazard it carried cannot arrive.; entry point 0 of 4, while the advisory the bug is about is printed by status and named by `close\\\_owed.py detect`. Also -> AC6 added, driving `close\_owed.py detect`. The r2 review confirmed the command exists, prints ids, and that its wiring mutant is invisible to every library row so it kills AC6 alone.; BLOCKING: AC1, AC2, AC3, and the missing Blocked-section criterion -> AC5 asserts the two ids and states the residue explicitly: six owed at HEAD, four of them this run's own. The r2 review confirmed that is correct to the id, and that after the fix the owed set is exactly BG0622, BG0625, BG0626 and BG0629. | - | | |
| BG0619 | 2026-08-27 | engineering; session | 2026-08-27 | the Proposed Fix's causal account is INCOMPLETE and the plan is built on it - find\\\_by\\\_id matches files through extr -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; stem\\\_record\\\_id already exists for exactly this and its own docstring says it returns None for a handoff, a retro or -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the causal account -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC1's, AC2's and AC3's mutants -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC2's achievability -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and a missing criterion for the blast radius the honest version of AC2's mutant would expose -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the causal account is correct but still incomplete - TWO more blockers sit after find\\\_by\\\_id returns a path, both mea -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and artifact.py:1906 falls back to the WHOLE stem with extract\\\_record\\\_id(...) or path.stem, so the id handed to th -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC3's identity assertion is the only one that can see the filed state and its mutant kills it alone -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the blast radius now has a criterion. The four criteria are the right four and need no re-design. -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the two unnamed blockers and the re-pricing -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC1's mutant -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC4's mutant -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and which bug owns the link target for a handoff and a review -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the four-part account was measured against a design the plan then REJECTED, and under the design actually chosen it is s -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; with the two named halves alone, HO0001, RETRO0074 and RV0007 all still return None and artifact\\\_files('handoff') yie -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC4's mutant no longer fires a pre-existing test first -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; ownership of the link target is recorded. -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the two unnamed sites -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the retro design fork, which must be settled before code -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC2's crash-mutant standing for four assertions -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and the re-pricing that follows -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings. | - | | |
| BG0619 | 2026-08-27 | engineering; session | 2026-08-27 | the Proposed Fix's causal account is INCOMPLETE and the plan is built on it - find\\\_by\\\_id matches files through extr -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; stem\\\_record\\\_id already exists for exactly this and its own docstring says it returns None for a handoff, a retro or -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the causal account -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC1's, AC2's and AC3's mutants -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC2's achievability -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and a missing criterion for the blast radius the honest version of AC2's mutant would expose -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the causal account is correct but still incomplete - TWO more blockers sit after find\\\_by\\\_id returns a path, both mea -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and artifact.py:1906 falls back to the WHOLE stem with extract\\\_record\\\_id(...) or path.stem, so the id handed to th -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC3's identity assertion is the only one that can see the filed state and its mutant kills it alone -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the blast radius now has a criterion. The four criteria are the right four and need no re-design. -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the two unnamed blockers and the re-pricing -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC1's mutant -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC4's mutant -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and which bug owns the link target for a handoff and a review -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the four-part account was measured against a design the plan then REJECTED, and under the design actually chosen it is s -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; with the two named halves alone, HO0001, RETRO0074 and RV0007 all still return None and artifact\\\_files('handoff') yie -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC4's mutant no longer fires a pre-existing test first -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; ownership of the link target is recorded. -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the two unnamed sites -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the retro design fork, which must be settled before code -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC2's crash-mutant standing for four assertions -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and the re-pricing that follows -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings. | - | | |
| BG0619 | 2026-08-27 | engineering; session | 2026-08-27 | the Proposed Fix's causal account is INCOMPLETE and the plan is built on it - find\\\_by\\\_id matches files through extr -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; stem\\\_record\\\_id already exists for exactly this and its own docstring says it returns None for a handoff, a retro or -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the causal account -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC1's, AC2's and AC3's mutants -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC2's achievability -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and a missing criterion for the blast radius the honest version of AC2's mutant would expose -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the causal account is correct but still incomplete - TWO more blockers sit after find\\\_by\\\_id returns a path, both mea -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and artifact.py:1906 falls back to the WHOLE stem with extract\\\_record\\\_id(...) or path.stem, so the id handed to th -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC3's identity assertion is the only one that can see the filed state and its mutant kills it alone -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the blast radius now has a criterion. The four criteria are the right four and need no re-design. -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the two unnamed blockers and the re-pricing -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC1's mutant -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC4's mutant -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and which bug owns the link target for a handoff and a review -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the four-part account was measured against a design the plan then REJECTED, and under the design actually chosen it is s -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; with the two named halves alone, HO0001, RETRO0074 and RV0007 all still return None and artifact\\\_files('handoff') yie -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC4's mutant no longer fires a pre-existing test first -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; ownership of the link target is recorded. -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; BLOCKING: the two unnamed sites -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; the retro design fork, which must be settled before code -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; AC2's crash-mutant standing for four assertions -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings.; and the re-pricing that follows -> Answered by D0174 and the rewrite it produced. The design CHANGED rather than the account being patched: find\_by\_id now globs each meta directory and matches on stem\_record\_id, which sidesteps \_walk\_artifact\_files, its membership guard and conventions.is\_artifact together. The round-4 review verified it by execution - RETRO0074, HO0001 and RV0007 all resolve, 200 meta ids with no duplicates and no clash with a pipeline id, and the 17 unnumbered files in those directories are skipped. It also re-measured is\_artifact at retro 0 of 113, review 20 of 40, handoff 64 of 64, which understates rather than overstates the case. AC2 was added for the corpus-cached branch, AC3 and AC4 split the retitle from the link target so each has its own mutant, and the price was re-set to 5 on the smaller design. APPROVED at round 4 with no blocking findings. | - | | |
| BG0613 | 2026-08-27 | engineering; session | 2026-08-28 | AC1's mutant names an edit nobody can make - \\\_rung\\\_grades takes a type string and has no rung-key parameter, so "pas -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's Points mutant is off-shape for this fix and plausibly cross-kills AC1 and AC2, because their fixtures are ungroo -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; a fixture invariant is unstated and load-bearing - conformance.unit\\\_is\\\_ungroomed already scopes its no-criteria limb -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; entry point 0 of 3 against an operator-facing Impact whose own Given says "when sprint breakdown grades it", so a brea -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; D0172 names THREE surfaces and no row pins \\\_rung\\\_product\\\_blockers as a distinct call site, nor case or alias normal -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the Proposed Fix still reads "decide which of the two the project means" although D0172 is recorded and accepted, so -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; BLOCKING: AC1's unmakeable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's off-shape mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the unstated fixture invariant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the missing subprocess row -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the stale Proposed Fix -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's replacement mutant is the IDENTICAL defect round 1 rejected, reintroduced one criterion over - \\\_rung\\\_grades(typ -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC1's mutant does not say what the retained inline epic test ANSWERS, and its own rationale describes latent drift, mean -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC4 has the same wording defect - the inline copy must be stated as the PRE-FIX epic-only test or it reads as a correct -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and AC1 and AC5 both assert set equality, so on a containers-only fixture both sets are empty after the fix and "names t -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the epic fixture invariant is stated -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; \\\_rung\\\_product\\\_blockers is pinned and its inline-copy mutant is makeable -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; D0172 is stated rather than re-posed and AC5 drives the shipped command. AC2 is sound - \\\_rung\\\_grades is != "epic" -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; BLOCKING: AC3's unexecutable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC1's and AC4's unstated inline-copy answer -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the non-empty fixture statement AC1 and AC5 both need -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing. | - | | |
| BG0613 | 2026-08-27 | engineering; session | 2026-08-28 | AC1's mutant names an edit nobody can make - \\\_rung\\\_grades takes a type string and has no rung-key parameter, so "pas -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's Points mutant is off-shape for this fix and plausibly cross-kills AC1 and AC2, because their fixtures are ungroo -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; a fixture invariant is unstated and load-bearing - conformance.unit\\\_is\\\_ungroomed already scopes its no-criteria limb -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; entry point 0 of 3 against an operator-facing Impact whose own Given says "when sprint breakdown grades it", so a brea -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; D0172 names THREE surfaces and no row pins \\\_rung\\\_product\\\_blockers as a distinct call site, nor case or alias normal -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the Proposed Fix still reads "decide which of the two the project means" although D0172 is recorded and accepted, so -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; BLOCKING: AC1's unmakeable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's off-shape mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the unstated fixture invariant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the missing subprocess row -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the stale Proposed Fix -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's replacement mutant is the IDENTICAL defect round 1 rejected, reintroduced one criterion over - \\\_rung\\\_grades(typ -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC1's mutant does not say what the retained inline epic test ANSWERS, and its own rationale describes latent drift, mean -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC4 has the same wording defect - the inline copy must be stated as the PRE-FIX epic-only test or it reads as a correct -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and AC1 and AC5 both assert set equality, so on a containers-only fixture both sets are empty after the fix and "names t -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the epic fixture invariant is stated -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; \\\_rung\\\_product\\\_blockers is pinned and its inline-copy mutant is makeable -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; D0172 is stated rather than re-posed and AC5 drives the shipped command. AC2 is sound - \\\_rung\\\_grades is != "epic" -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; BLOCKING: AC3's unexecutable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC1's and AC4's unstated inline-copy answer -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the non-empty fixture statement AC1 and AC5 both need -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing. | - | | |
| BG0613 | 2026-08-27 | engineering; session | 2026-08-28 | AC1's mutant names an edit nobody can make - \\\_rung\\\_grades takes a type string and has no rung-key parameter, so "pas -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's Points mutant is off-shape for this fix and plausibly cross-kills AC1 and AC2, because their fixtures are ungroo -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; a fixture invariant is unstated and load-bearing - conformance.unit\\\_is\\\_ungroomed already scopes its no-criteria limb -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; entry point 0 of 3 against an operator-facing Impact whose own Given says "when sprint breakdown grades it", so a brea -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; D0172 names THREE surfaces and no row pins \\\_rung\\\_product\\\_blockers as a distinct call site, nor case or alias normal -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the Proposed Fix still reads "decide which of the two the project means" although D0172 is recorded and accepted, so -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; BLOCKING: AC1's unmakeable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's off-shape mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the unstated fixture invariant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the missing subprocess row -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the stale Proposed Fix -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC3's replacement mutant is the IDENTICAL defect round 1 rejected, reintroduced one criterion over - \\\_rung\\\_grades(typ -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC1's mutant does not say what the retained inline epic test ANSWERS, and its own rationale describes latent drift, mean -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC4 has the same wording defect - the inline copy must be stated as the PRE-FIX epic-only test or it reads as a correct -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and AC1 and AC5 both assert set equality, so on a containers-only fixture both sets are empty after the fix and "names t -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; the epic fixture invariant is stated -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; \\\_rung\\\_product\\\_blockers is pinned and its inline-copy mutant is makeable -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; D0172 is stated rather than re-posed and AC5 drives the shipped command. AC2 is sound - \\\_rung\\\_grades is != "epic" -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; BLOCKING: AC3's unexecutable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; AC1's and AC4's unstated inline-copy answer -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing.; and the non-empty fixture statement AC1 and AC5 both need -> Answered by the round-3 rewrite, which the panel then APPROVED. Every mutant was written from the code rather than the prose: \_rung\_grades(type\_: str) -> bool was read first, which is why AC3's mutant is now return False for every type and the row states in terms that a Points mutant cannot be written inside it. AC1 and AC4 now name the ANSWER the retained inline copy gives, and the fixture invariant requires a non-container ungroomed unit so the set-equality rows cannot be satisfied by naming nothing. | - | | |
| BG0617 | 2026-08-27 | engineering; session | 2026-08-28 | the bug's load-bearing claim is FALSE - the goal verdict is not "computed a few lines below" the title, it is a plain di -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; it is fully available at title time and nothing computes it. So AC1's ordering mutant describes swapping two adjacent re -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; BLOCKING: the false premise -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC1's unmakeable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC2's and AC3's file attributions -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC2's unfalsifiable wording -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC3 being green today -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and the uncovered slug and index row -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC4 passes on unfixed code and its mutant names an edit nobody can make. handoff.py:662 passes ONE title string to  -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and AC3's "or left one open" half is green at HEAD because \\\_pickup\\\_body already names remaining units, so only the d -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC1's replacement mutant is a one-word edit at the new title site that the neighbouring verdict == "achieved" test inv -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; the file attributions are now right -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; "DOES carry the goal" is an oracle -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and AC3's scoping to the pick-up section is confirmed necessary, since render\\\_body emits a Closed without delivery -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; BLOCKING: AC4 alone - re-site the mutant at sprint.py, keeping the goal as --title and correcting only the body pros -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows. | - | | |
| BG0617 | 2026-08-27 | engineering; session | 2026-08-28 | the bug's load-bearing claim is FALSE - the goal verdict is not "computed a few lines below" the title, it is a plain di -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; it is fully available at title time and nothing computes it. So AC1's ordering mutant describes swapping two adjacent re -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; BLOCKING: the false premise -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC1's unmakeable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC2's and AC3's file attributions -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC2's unfalsifiable wording -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC3 being green today -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and the uncovered slug and index row -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC4 passes on unfixed code and its mutant names an edit nobody can make. handoff.py:662 passes ONE title string to  -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and AC3's "or left one open" half is green at HEAD because \\\_pickup\\\_body already names remaining units, so only the d -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC1's replacement mutant is a one-word edit at the new title site that the neighbouring verdict == "achieved" test inv -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; the file attributions are now right -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; "DOES carry the goal" is an oracle -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and AC3's scoping to the pick-up section is confirmed necessary, since render\\\_body emits a Closed without delivery -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; BLOCKING: AC4 alone - re-site the mutant at sprint.py, keeping the goal as --title and correcting only the body pros -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows. | - | | |
| BG0617 | 2026-08-27 | engineering; session | 2026-08-28 | the bug's load-bearing claim is FALSE - the goal verdict is not "computed a few lines below" the title, it is a plain di -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; it is fully available at title time and nothing computes it. So AC1's ordering mutant describes swapping two adjacent re -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; BLOCKING: the false premise -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC1's unmakeable mutant -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC2's and AC3's file attributions -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC2's unfalsifiable wording -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC3 being green today -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and the uncovered slug and index row -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC4 passes on unfixed code and its mutant names an edit nobody can make. handoff.py:662 passes ONE title string to  -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and AC3's "or left one open" half is green at HEAD because \\\_pickup\\\_body already names remaining units, so only the d -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; AC1's replacement mutant is a one-word edit at the new title site that the neighbouring verdict == "achieved" test inv -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; the file attributions are now right -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; "DOES carry the goal" is an oracle -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; and AC3's scoping to the pick-up section is confirmed necessary, since render\\\_body emits a Closed without delivery -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows.; BLOCKING: AC4 alone - re-site the mutant at sprint.py, keeping the goal as --title and correcting only the body pros -> Answered by the round-3 rewrite, which the panel then APPROVED. The false premise was corrected on the artefact - the verdict is a plain dict read available at title time, not computed below - and AC4's mutant re-sited to sprint.py, keeping the goal as --title and correcting only the body prose, with an oracle asserting the slug and index row do NOT carry the goal. Verified red at HEAD against real index rows. | - | | |
| BG0623 | 2026-08-27 | engineering; session | 2026-08-28 | AC1's mutant is a bare revert - today's code plus a string change, which is "the feature was not built" rather than an e -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the mutant that matters is uncovered, namely writing the WRONG id into the repaired H1, since artifact.py:1906 reads  -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC2 is a probable SURVIVOR because \\\_H1\\\_RETITLE\\\_RE keeps its first group verbatim and substitutes only the tail, so -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC3 has no positive control, so no row asserts that the repair actually WRITES when every surface is valid -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; and no criterion reaches the shipped CLI though the bug's entire evidence is a CLI refusal and the same author applied t -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; BLOCKING: AC1's mutant and the missing wrong-id row -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC2's probable survivor -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the missing positive control -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; and reaching the shipped CLI -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC1's criterion names TWO wrongs - not the whole stem, and not the id as the caller spelled it - and only the first is m -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; a differently-formed heading DOES survive today's regex, verified by running it, so AC2 is green today and red under its -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC4 is a genuine positive control whose mutant is the right shape, and it stops AC3 being vacuously satisfied by a comma -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the dependency on BG0619 is gone and the independence is real. -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; BLOCKING: AC1 needs a second mutant row composing the heading from artifact\\\_id, and its test must invoke with a diffe -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive. | - | | |
| BG0623 | 2026-08-27 | engineering; session | 2026-08-28 | AC1's mutant is a bare revert - today's code plus a string change, which is "the feature was not built" rather than an e -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the mutant that matters is uncovered, namely writing the WRONG id into the repaired H1, since artifact.py:1906 reads  -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC2 is a probable SURVIVOR because \\\_H1\\\_RETITLE\\\_RE keeps its first group verbatim and substitutes only the tail, so -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC3 has no positive control, so no row asserts that the repair actually WRITES when every surface is valid -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; and no criterion reaches the shipped CLI though the bug's entire evidence is a CLI refusal and the same author applied t -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; BLOCKING: AC1's mutant and the missing wrong-id row -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC2's probable survivor -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the missing positive control -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; and reaching the shipped CLI -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC1's criterion names TWO wrongs - not the whole stem, and not the id as the caller spelled it - and only the first is m -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; a differently-formed heading DOES survive today's regex, verified by running it, so AC2 is green today and red under its -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC4 is a genuine positive control whose mutant is the right shape, and it stops AC3 being vacuously satisfied by a comma -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the dependency on BG0619 is gone and the independence is real. -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; BLOCKING: AC1 needs a second mutant row composing the heading from artifact\\\_id, and its test must invoke with a diffe -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive. | - | | |
| BG0623 | 2026-08-27 | engineering; session | 2026-08-28 | AC1's mutant is a bare revert - today's code plus a string change, which is "the feature was not built" rather than an e -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the mutant that matters is uncovered, namely writing the WRONG id into the repaired H1, since artifact.py:1906 reads  -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC2 is a probable SURVIVOR because \\\_H1\\\_RETITLE\\\_RE keeps its first group verbatim and substitutes only the tail, so -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC3 has no positive control, so no row asserts that the repair actually WRITES when every surface is valid -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; and no criterion reaches the shipped CLI though the bug's entire evidence is a CLI refusal and the same author applied t -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; BLOCKING: AC1's mutant and the missing wrong-id row -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC2's probable survivor -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the missing positive control -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; and reaching the shipped CLI -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC1's criterion names TWO wrongs - not the whole stem, and not the id as the caller spelled it - and only the first is m -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; a differently-formed heading DOES survive today's regex, verified by running it, so AC2 is green today and red under its -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; AC4 is a genuine positive control whose mutant is the right shape, and it stops AC3 being vacuously satisfied by a comma -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; the dependency on BG0619 is gone and the independence is real. -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive.; BLOCKING: AC1 needs a second mutant row composing the heading from artifact\\\_id, and its test must invoke with a diffe -> Answered by the round-3 rewrite, which the panel then APPROVED. AC1 now carries TWO mutant rows - one composing the heading from path.stem, one from artifact\_id, the caller's spelling in scope beside it - and the criterion requires the subprocess to be invoked non-canonically, without which the second mutant would survive. | - | | |
| BG0624 | 2026-08-27 | engineering; session | 2026-08-28 | AC2's mutant is self-refuting and survives its own control - "widen the barred set to every severity that is not explici -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's mutant is a bare revert, because file\\\_finding.py has no normalisation to remove - --severity is a free-string -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's oracle also contradicts its own Verify line, saying "NORMALISED or refused" against a test named ...\\\_is\\\_refused -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC1's oracle is too weak to guarantee its kill, since "REPORTED rather than absent from both" is satisfied by the id mer -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; entry point 0 of 3, against an Impact stated entirely in terms of --bar and the page, so a repair widening the populat -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; BLOCKING: AC2's self-refuting mutant -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's bare revert, oracle mismatch and insufficient scope -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC1's weak oracle -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; the missing CLI criterion -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; and the unstated residue population -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently. | - | | |
| BG0624 | 2026-08-27 | engineering; session | 2026-08-28 | AC2's mutant is self-refuting and survives its own control - "widen the barred set to every severity that is not explici -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's mutant is a bare revert, because file\\\_finding.py has no normalisation to remove - --severity is a free-string -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's oracle also contradicts its own Verify line, saying "NORMALISED or refused" against a test named ...\\\_is\\\_refused -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC1's oracle is too weak to guarantee its kill, since "REPORTED rather than absent from both" is satisfied by the id mer -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; entry point 0 of 3, against an Impact stated entirely in terms of --bar and the page, so a repair widening the populat -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; BLOCKING: AC2's self-refuting mutant -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's bare revert, oracle mismatch and insufficient scope -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC1's weak oracle -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; the missing CLI criterion -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; and the unstated residue population -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently. | - | | |
| BG0624 | 2026-08-27 | engineering; session | 2026-08-28 | AC2's mutant is self-refuting and survives its own control - "widen the barred set to every severity that is not explici -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's mutant is a bare revert, because file\\\_finding.py has no normalisation to remove - --severity is a free-string -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's oracle also contradicts its own Verify line, saying "NORMALISED or refused" against a test named ...\\\_is\\\_refused -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC1's oracle is too weak to guarantee its kill, since "REPORTED rather than absent from both" is satisfied by the id mer -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; entry point 0 of 3, against an Impact stated entirely in terms of --bar and the page, so a repair widening the populat -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; BLOCKING: AC2's self-refuting mutant -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC3's bare revert, oracle mismatch and insufficient scope -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; AC1's weak oracle -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; the missing CLI criterion -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently.; and the unstated residue population -> Answered by the round-2 rewrite, which the panel then APPROVED. AC2's self-refuting mutant was replaced with folding barred\_open into a filter on corpus(), the over-correction its own docstring names\; AC3 is refusal only, matching its selector\; AC4 was added for artifact.py new, the second writer\; AC5 drives both shipped commands with the exit code as the discriminator\; and the residue population and bar behaviour are settled in the artefact rather than left for either implementation to satisfy silently. | - | | |
| BG0591 | 2026-08-28 | engineering; session | 2026-08-28 | the corpus witness is EMPTY after BG0616 shipped - on the real tree owed equals unaccounted equals the same five ids -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; every fixture must therefore FORCE owed != unaccounted or AC1 passes on unfixed code and its mutant cannot kill it, an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: the missing fixture invariant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's bare revert -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's unmakeable mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the missing subprocess row -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the uncovered repair/override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's mutant SURVIVES its own test - by the row's own words it "agrees with the renderer for one of the three cases", an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's mutant is makeable but kills AC1 and AC4 as well and reds the existing close\\\_owed suite, so round 1's cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's mutant is unobservable - close\\\_owed.owed() is a pure read, both reads happen in one process against a static tr -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's criterion also states a false fact as law, that --root precedes the subcommand, which is true of close\\\_owed.py -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the fixture invariant is necessary but not sufficient - "owed and unaccounted differ" is satisfied by a fixture hold -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3 now covers the repair and override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC2 is sound and cross-kills nothing. -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC1's surviving mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's inherited cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's unobservable mutant and its false --root claim -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the per-limb fixture statement -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4 is the row nothing kills. The edit is real - delete the advisory print at status.py:415-417 and :575-577 - and it -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's partial-fix mutant empties AC1's fixture while leaving AC3's repair and override in, so it discriminates -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; both --root spellings are correct, verified against the two parsers -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the per-row fixture invariant closes the AC1/AC3 collapse. Non-blocking: AC3's phrase "blocking() subtracts three limbs" -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and no row covers the corrupt or unbaselined branches of close\\\_owed\\\_advisory, which read report["owed"] raw and ca -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC4 - state its fixture invariant (at least one genuinely unaccounted unit AND at least one accounted-for unit -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement. | - | | |
| BG0591 | 2026-08-28 | engineering; session | 2026-08-28 | the corpus witness is EMPTY after BG0616 shipped - on the real tree owed equals unaccounted equals the same five ids -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; every fixture must therefore FORCE owed != unaccounted or AC1 passes on unfixed code and its mutant cannot kill it, an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: the missing fixture invariant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's bare revert -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's unmakeable mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the missing subprocess row -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the uncovered repair/override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's mutant SURVIVES its own test - by the row's own words it "agrees with the renderer for one of the three cases", an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's mutant is makeable but kills AC1 and AC4 as well and reds the existing close\\\_owed suite, so round 1's cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's mutant is unobservable - close\\\_owed.owed() is a pure read, both reads happen in one process against a static tr -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's criterion also states a false fact as law, that --root precedes the subcommand, which is true of close\\\_owed.py -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the fixture invariant is necessary but not sufficient - "owed and unaccounted differ" is satisfied by a fixture hold -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3 now covers the repair and override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC2 is sound and cross-kills nothing. -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC1's surviving mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's inherited cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's unobservable mutant and its false --root claim -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the per-limb fixture statement -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4 is the row nothing kills. The edit is real - delete the advisory print at status.py:415-417 and :575-577 - and it -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's partial-fix mutant empties AC1's fixture while leaving AC3's repair and override in, so it discriminates -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; both --root spellings are correct, verified against the two parsers -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the per-row fixture invariant closes the AC1/AC3 collapse. Non-blocking: AC3's phrase "blocking() subtracts three limbs" -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and no row covers the corrupt or unbaselined branches of close\\\_owed\\\_advisory, which read report["owed"] raw and ca -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC4 - state its fixture invariant (at least one genuinely unaccounted unit AND at least one accounted-for unit -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement. | - | | |
| BG0591 | 2026-08-28 | engineering; session | 2026-08-28 | the corpus witness is EMPTY after BG0616 shipped - on the real tree owed equals unaccounted equals the same five ids -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; every fixture must therefore FORCE owed != unaccounted or AC1 passes on unfixed code and its mutant cannot kill it, an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: the missing fixture invariant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's bare revert -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's unmakeable mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the missing subprocess row -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the uncovered repair/override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's mutant SURVIVES its own test - by the row's own words it "agrees with the renderer for one of the three cases", an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's mutant is makeable but kills AC1 and AC4 as well and reds the existing close\\\_owed suite, so round 1's cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's mutant is unobservable - close\\\_owed.owed() is a pure read, both reads happen in one process against a static tr -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's criterion also states a false fact as law, that --root precedes the subcommand, which is true of close\\\_owed.py -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the fixture invariant is necessary but not sufficient - "owed and unaccounted differ" is satisfied by a fixture hold -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3 now covers the repair and override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC2 is sound and cross-kills nothing. -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC1's surviving mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's inherited cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's unobservable mutant and its false --root claim -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the per-limb fixture statement -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4 is the row nothing kills. The edit is real - delete the advisory print at status.py:415-417 and :575-577 - and it -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's partial-fix mutant empties AC1's fixture while leaving AC3's repair and override in, so it discriminates -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; both --root spellings are correct, verified against the two parsers -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the per-row fixture invariant closes the AC1/AC3 collapse. Non-blocking: AC3's phrase "blocking() subtracts three limbs" -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and no row covers the corrupt or unbaselined branches of close\\\_owed\\\_advisory, which read report["owed"] raw and ca -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC4 - state its fixture invariant (at least one genuinely unaccounted unit AND at least one accounted-for unit -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement. | - | | |
| BG0614 | 2026-08-28 | engineering; session | 2026-08-28 | AC2's mutant is BACKWARDS and cannot fail its own test - dropping the row index from the key makes keys COLLAPSE, so the -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC1's mutant survives unless the criterion REQUIRES the second registration to name a different test, which it does not, -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's "9 naming different tests" is wrong at HEAD - re-counted, it is 6 of 14, and membership churned today, so -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the scope of the supersede is the whole design decision and no row pins it - 12 of the 14 duplicates sit inside ONE ledg -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `retract` also decrements the entry summary, so a supersede writing only the marker leaves the join and the coverage lan -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `retract` refuses on already-withdrawn rows, so a legitimate later retraction of a superseded row fails saying it matche -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `critic.\\\_withdrawn\\\_block` puts every withdrawn row into the seat brief, so supersede noise would become indistinguisha -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; nothing stops the supersede touching MEASURED rows, which `retract` explicitly refuses -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and `mutation.py` has NO audit verb at all, so entry point is 0 of 3 and the wiring is the part a library test cannot se -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: AC2's mutant -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC1's unbound fixture -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the unpinned supersede scope -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the summary arithmetic -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and reaching the shipped CLI -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC2's criterion was rewritten and its MUTANT was NOT - the row still reads "compare keys on unit and criterion alone, dr -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC2's fixture holds two rows on ONE key, so under a coarser key they still collide, the audit still reports, and the tes -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; worse, if AC3's clean-ledger fixture holds two rows on one criterion at different row indices - the ordinary shape - tha -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the supersede scope is decided and AC4 pins the cross-entry case -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC5 and AC6 pin the summary arithmetic and the measured-row guard -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC7's premise is confirmed - there is no audit verb - and its mutant dies only on a subprocess test. -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: AC2's mutant, which was not replaced -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's stale figure -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the uncovered seat-brief consequence -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and the unpinned already-withdrawn collision -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the plan REINSTATES a supersede that the code under change records as implemented and REVERTED, and no criterion pins th -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; a supersede writes the same marker with no reason and no author, so the skip becomes free - and AC1 REQUIRES the superse -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's figure is consistent at six -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC9 earns its place. Non-blocking: AC6's fixture requirement is unstated, and the Scope section says "three collisions" -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: the reinstated escape and the missing guard -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC8's misanchored mutant -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the two uncovered readers -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3. | - | | |
| BG0614 | 2026-08-28 | engineering; session | 2026-08-28 | AC2's mutant is BACKWARDS and cannot fail its own test - dropping the row index from the key makes keys COLLAPSE, so the -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC1's mutant survives unless the criterion REQUIRES the second registration to name a different test, which it does not, -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's "9 naming different tests" is wrong at HEAD - re-counted, it is 6 of 14, and membership churned today, so -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the scope of the supersede is the whole design decision and no row pins it - 12 of the 14 duplicates sit inside ONE ledg -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `retract` also decrements the entry summary, so a supersede writing only the marker leaves the join and the coverage lan -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `retract` refuses on already-withdrawn rows, so a legitimate later retraction of a superseded row fails saying it matche -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `critic.\\\_withdrawn\\\_block` puts every withdrawn row into the seat brief, so supersede noise would become indistinguisha -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; nothing stops the supersede touching MEASURED rows, which `retract` explicitly refuses -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and `mutation.py` has NO audit verb at all, so entry point is 0 of 3 and the wiring is the part a library test cannot se -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: AC2's mutant -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC1's unbound fixture -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the unpinned supersede scope -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the summary arithmetic -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and reaching the shipped CLI -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC2's criterion was rewritten and its MUTANT was NOT - the row still reads "compare keys on unit and criterion alone, dr -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC2's fixture holds two rows on ONE key, so under a coarser key they still collide, the audit still reports, and the tes -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; worse, if AC3's clean-ledger fixture holds two rows on one criterion at different row indices - the ordinary shape - tha -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the supersede scope is decided and AC4 pins the cross-entry case -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC5 and AC6 pin the summary arithmetic and the measured-row guard -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC7's premise is confirmed - there is no audit verb - and its mutant dies only on a subprocess test. -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: AC2's mutant, which was not replaced -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's stale figure -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the uncovered seat-brief consequence -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and the unpinned already-withdrawn collision -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the plan REINSTATES a supersede that the code under change records as implemented and REVERTED, and no criterion pins th -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; a supersede writes the same marker with no reason and no author, so the skip becomes free - and AC1 REQUIRES the superse -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's figure is consistent at six -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC9 earns its place. Non-blocking: AC6's fixture requirement is unstated, and the Scope section says "three collisions" -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: the reinstated escape and the missing guard -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC8's misanchored mutant -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the two uncovered readers -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3. | - | | |
| BG0614 | 2026-08-28 | engineering; session | 2026-08-28 | AC2's mutant is BACKWARDS and cannot fail its own test - dropping the row index from the key makes keys COLLAPSE, so the -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC1's mutant survives unless the criterion REQUIRES the second registration to name a different test, which it does not, -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's "9 naming different tests" is wrong at HEAD - re-counted, it is 6 of 14, and membership churned today, so -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the scope of the supersede is the whole design decision and no row pins it - 12 of the 14 duplicates sit inside ONE ledg -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `retract` also decrements the entry summary, so a supersede writing only the marker leaves the join and the coverage lan -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `retract` refuses on already-withdrawn rows, so a legitimate later retraction of a superseded row fails saying it matche -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `critic.\\\_withdrawn\\\_block` puts every withdrawn row into the seat brief, so supersede noise would become indistinguisha -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; nothing stops the supersede touching MEASURED rows, which `retract` explicitly refuses -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and `mutation.py` has NO audit verb at all, so entry point is 0 of 3 and the wiring is the part a library test cannot se -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: AC2's mutant -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC1's unbound fixture -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the unpinned supersede scope -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the summary arithmetic -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and reaching the shipped CLI -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC2's criterion was rewritten and its MUTANT was NOT - the row still reads "compare keys on unit and criterion alone, dr -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC2's fixture holds two rows on ONE key, so under a coarser key they still collide, the audit still reports, and the tes -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; worse, if AC3's clean-ledger fixture holds two rows on one criterion at different row indices - the ordinary shape - tha -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the supersede scope is decided and AC4 pins the cross-entry case -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC5 and AC6 pin the summary arithmetic and the measured-row guard -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC7's premise is confirmed - there is no audit verb - and its mutant dies only on a subprocess test. -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: AC2's mutant, which was not replaced -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's stale figure -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the uncovered seat-brief consequence -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and the unpinned already-withdrawn collision -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the plan REINSTATES a supersede that the code under change records as implemented and REVERTED, and no criterion pins th -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; a supersede writes the same marker with no reason and no author, so the skip becomes free - and AC1 REQUIRES the superse -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's figure is consistent at six -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC9 earns its place. Non-blocking: AC6's fixture requirement is unstated, and the Scope section says "three collisions" -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: the reinstated escape and the missing guard -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC8's misanchored mutant -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the two uncovered readers -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3. | - | | |
| BG0591 | 2026-08-28 | engineering; session | 2026-08-28 | the corpus witness is EMPTY after BG0616 shipped - on the real tree owed equals unaccounted equals the same five ids -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; every fixture must therefore FORCE owed != unaccounted or AC1 passes on unfixed code and its mutant cannot kill it, an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: the missing fixture invariant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's bare revert -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's unmakeable mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the missing subprocess row -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the uncovered repair/override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's mutant SURVIVES its own test - by the row's own words it "agrees with the renderer for one of the three cases", an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's mutant is makeable but kills AC1 and AC4 as well and reds the existing close\\\_owed suite, so round 1's cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's mutant is unobservable - close\\\_owed.owed() is a pure read, both reads happen in one process against a static tr -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's criterion also states a false fact as law, that --root precedes the subcommand, which is true of close\\\_owed.py -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the fixture invariant is necessary but not sufficient - "owed and unaccounted differ" is satisfied by a fixture hold -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3 now covers the repair and override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC2 is sound and cross-kills nothing. -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC1's surviving mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's inherited cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's unobservable mutant and its false --root claim -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the per-limb fixture statement -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4 is the row nothing kills. The edit is real - delete the advisory print at status.py:415-417 and :575-577 - and it -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's partial-fix mutant empties AC1's fixture while leaving AC3's repair and override in, so it discriminates -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; both --root spellings are correct, verified against the two parsers -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the per-row fixture invariant closes the AC1/AC3 collapse. Non-blocking: AC3's phrase "blocking() subtracts three limbs" -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and no row covers the corrupt or unbaselined branches of close\\\_owed\\\_advisory, which read report["owed"] raw and ca -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC4 - state its fixture invariant (at least one genuinely unaccounted unit AND at least one accounted-for unit -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement. | - | | |
| BG0591 | 2026-08-27 | engineering; session | 2026-08-28 | the corpus witness is EMPTY after BG0616 shipped - on the real tree `owed` equals `unaccounted` equals the same five ids -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; every fixture must therefore FORCE `owed != unaccounted` or AC1 passes on unfixed code and its mutant cannot kill it, an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: the missing fixture invariant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's bare revert -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's unmakeable mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the missing subprocess row -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the uncovered repair/override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC1's mutant SURVIVES its own test - by the row's own words it "agrees with the renderer for one of the three cases", an -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's mutant is makeable but kills AC1 and AC4 as well and reds the existing close\\\_owed suite, so round 1's cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's mutant is unobservable - `close\\\_owed.owed()` is a pure read, both reads happen in one process against a static tr -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's criterion also states a false fact as law, that `--root` precedes the subcommand, which is true of `close\\\_owed.py` -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the fixture invariant is necessary but not sufficient - "owed and unaccounted differ" is satisfied by a fixture hold -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3 now covers the repair and override limbs -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC2 is sound and cross-kills nothing. -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC1's surviving mutant -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's inherited cross-kill -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC4's unobservable mutant and its false `--root` claim -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and the per-limb fixture statement -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement. | - | | |
| BG0591 | 2026-08-28 | engineering; session | 2026-08-28 | AC4 is the row nothing kills. The edit is real - delete the advisory print at `status.py`:415-417 and :575-577 - and it -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; AC3's partial-fix mutant empties AC1's fixture while leaving AC3's repair and override in, so it discriminates -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; both `--root` spellings are correct, verified against the two parsers -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; the per-row fixture invariant closes the AC1/AC3 collapse. Non-blocking: AC3's phrase "blocking() subtracts three limbs" -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; and no row covers the corrupt or unbaselined branches of `close\\\_owed\\\_advisory`, which read `report["owed"]` raw and ca -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement.; BLOCKING: AC4 - state its fixture invariant (at least one genuinely unaccounted unit AND at least one accounted-for unit -> Answered by the round-3 rewrite plus the AC4 paragraph the panel specified. AC1's mutant is now the defect itself, AC3's the partial fix that empties AC1's fixture while leaving the repair and override in, and AC4's a lane mutant on what the commands PRINT. The fixture invariant is stated per row, and AC4's now carries BOTH an unaccounted and an accounted-for unit so the two surfaces cannot agree by silence\; its oracle compares the id SET each surface claims, with no advisory line counting as disagreement. | - | | |
| BG0614 | 2026-08-27 | engineering; session | 2026-08-28 | AC2's mutant is BACKWARDS and cannot fail its own test - dropping the row index from the key makes keys COLLAPSE, so the -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC1's mutant survives unless the criterion REQUIRES the second registration to name a different test, which it does not, -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's "9 naming different tests" is wrong at HEAD - re-counted, it is 6 of 14, and membership churned today, so -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the scope of the supersede is the whole design decision and no row pins it - 12 of the 14 duplicates sit inside ONE ledg -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `retract` also decrements the entry summary, so a supersede writing only the marker leaves the join and the coverage lan -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `retract` refuses on already-withdrawn rows, so a legitimate later retraction of a superseded row fails saying it matche -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; `critic.\\\_withdrawn\\\_block` puts every withdrawn row into the seat brief, so supersede noise would become indistinguisha -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; nothing stops the supersede touching MEASURED rows, which `retract` explicitly refuses -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and `mutation.py` has NO audit verb at all, so entry point is 0 of 3 and the wiring is the part a library test cannot se -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: AC2's mutant -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC1's unbound fixture -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the unpinned supersede scope -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the summary arithmetic -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and reaching the shipped CLI -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC2's criterion was rewritten and its MUTANT was NOT - the row still reads "compare keys on unit and criterion alone, dr -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC2's fixture holds two rows on ONE key, so under a coarser key they still collide, the audit still reports, and the tes -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; worse, if AC3's clean-ledger fixture holds two rows on one criterion at different row indices - the ordinary shape - tha -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the supersede scope is decided and AC4 pins the cross-entry case -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC5 and AC6 pin the summary arithmetic and the measured-row guard -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC7's premise is confirmed - there is no audit verb - and its mutant dies only on a subprocess test. -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: AC2's mutant, which was not replaced -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's stale figure -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the uncovered seat-brief consequence -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; and the unpinned already-withdrawn collision -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3. | - | | |
| BG0614 | 2026-08-28 | engineering; session | 2026-08-28 | the plan REINSTATES a supersede that the code under change records as implemented and REVERTED, and no criterion pins th -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; a supersede writes the same marker with no reason and no author, so the skip becomes free - and AC1 REQUIRES the superse -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the Summary's figure is consistent at six -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC9 earns its place. Non-blocking: AC6's fixture requirement is unstated, and the Scope section says "three collisions" -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; BLOCKING: the reinstated escape and the missing guard -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; AC8's misanchored mutant -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3.; the two uncovered readers -> Answered by RE-SCOPING the unit rather than patching it. The supersede is dropped entirely: `register\_mutant` records that a later registration does not supersede, though a review round asked for it, because worst-verdict-wins exists precisely because a genuine correction and an author registering their way out of a survivor are byte-identical - and `retract\_mutant` records the same design implemented and reverted. The draft would have reopened it exactly, since it required the supersede to fire on a differing test name. The unit now AUDITS: nothing reports a duplicate key today, which is the whole of the harm that survives worst-verdict-wins, and correcting one stays with `retract`, which costs a reason and leaves the correction on the record. Four criteria, one through the shipped CLI, one on the corpus. Points 5 to 3. | - | | |
| BG0623 | 2026-08-28 | engineering; session | 2026-08-28 | the repair replaces LINE 1 whether or not line 1 is the heading - `\\\_H1\\\_RETITLE\\\_RE.search` scans the whole document, s -> The heading line is now FOUND rather than assumed: the first line that looks like a heading is replaced, and when nothing does the canonical H1 is INSERTED above the document. Nothing is destroyed on any shape. Two tests added for the cases no fixture covered - a leading blank line, and a document with no heading at all - and the second needed a DISTINCTIVE first line, because `\_root` appends its own Status line and the duplicate made the destruction invisible\; the mutant survived on that fixture flaw before it was fixed. Both regression mutants now KILLED.; no test covers a document whose first line is not the heading, since all four fixtures put the broken H1 on line 1, so t -> The heading line is now FOUND rather than assumed: the first line that looks like a heading is replaced, and when nothing does the canonical H1 is INSERTED above the document. Nothing is destroyed on any shape. Two tests added for the cases no fixture covered - a leading blank line, and a document with no heading at all - and the second needed a DISTINCTIVE first line, because `\_root` appends its own Status line and the duplicate made the destruction invisible\; the mutant survived on that fixture flaw before it was fixed. Both regression mutants now KILLED.; the changelog fragment's claim that the repair decides only what surface one will be is false as shipped -> The heading line is now FOUND rather than assumed: the first line that looks like a heading is replaced, and when nothing does the canonical H1 is INSERTED above the document. Nothing is destroyed on any shape. Two tests added for the cases no fixture covered - a leading blank line, and a document with no heading at all - and the second needed a DISTINCTIVE first line, because `\_root` appends its own Status line and the duplicate made the destruction invisible\; the mutant survived on that fixture flaw before it was fixed. Both regression mutants now KILLED.; the line-1 replacement, which destroys a non-heading first line and leaves the broken H1 in place -> The heading line is now FOUND rather than assumed: the first line that looks like a heading is replaced, and when nothing does the canonical H1 is INSERTED above the document. Nothing is destroyed on any shape. Two tests added for the cases no fixture covered - a leading blank line, and a document with no heading at all - and the second needed a DISTINCTIVE first line, because `\_root` appends its own Status line and the duplicate made the destruction invisible\; the mutant survived on that fixture flaw before it was fixed. Both regression mutants now KILLED. | - | | |
| BG0624 | 2026-08-28 | engineering; session | 2026-08-28 | the vocabulary is passed to argparse choices, which is CASE-SENSITIVE, while both readers casefold - census over 632 par -> The comparison CASE-FOLDS, through a shared `normalise\_severity` rather than argparse choices, so all 631 spellings the corpus actually holds are accepted and only `major` is refused - measured over every Severity value in the bug corpus. The check moved from the flag to the WRITE, which is what closes the --fields-file bypass at both writers\; verified by execution, a fields-file carrying `major` now exits 1 at the filer and `high` normalises to `High` on disk. Three mutants KILLED, including the case-sensitive compare and an unguarded write at each writer.; the guard sits on the argparse flag only, and severity reaches both writers through --fields-file as well - executed, bo -> The comparison CASE-FOLDS, through a shared `normalise\_severity` rather than argparse choices, so all 631 spellings the corpus actually holds are accepted and only `major` is refused - measured over every Severity value in the bug corpus. The check moved from the flag to the WRITE, which is what closes the --fields-file bypass at both writers\; verified by execution, a fields-file carrying `major` now exits 1 at the filer and `high` normalises to `High` on disk. Three mutants KILLED, including the case-sensitive compare and an unguarded write at each writer.; restricting unclassifiable to open findings leaves all 33 tests green, because no fixture gives an unrecognised severity -> The comparison CASE-FOLDS, through a shared `normalise\_severity` rather than argparse choices, so all 631 spellings the corpus actually holds are accepted and only `major` is refused - measured over every Severity value in the bug corpus. The check moved from the flag to the WRITE, which is what closes the --fields-file bypass at both writers\; verified by execution, a fields-file carrying `major` now exits 1 at the filer and `high` normalises to `High` on disk. Three mutants KILLED, including the case-sensitive compare and an unguarded write at each writer.; the case-sensitive refusal of 21 spellings the corpus holds -> The comparison CASE-FOLDS, through a shared `normalise\_severity` rather than argparse choices, so all 631 spellings the corpus actually holds are accepted and only `major` is refused - measured over every Severity value in the bug corpus. The check moved from the flag to the WRITE, which is what closes the --fields-file bypass at both writers\; verified by execution, a fields-file carrying `major` now exits 1 at the filer and `high` normalises to `High` on disk. Three mutants KILLED, including the case-sensitive compare and an unguarded write at each writer.; the fields-file bypass at both writers -> The comparison CASE-FOLDS, through a shared `normalise\_severity` rather than argparse choices, so all 631 spellings the corpus actually holds are accepted and only `major` is refused - measured over every Severity value in the bug corpus. The check moved from the flag to the WRITE, which is what closes the --fields-file bypass at both writers\; verified by execution, a fields-file carrying `major` now exits 1 at the filer and `high` normalises to `High` on disk. Three mutants KILLED, including the case-sensitive compare and an unguarded write at each writer. | - | | |
| BG0629 | 2026-08-28 | engineering; session | 2026-08-28 | AC7 is NOT met - it is law and says a closure naming an earlier rejection's finding is accepted AND COUNTED, yet run aga -> AC7 now holds. `record\_repair` writes ONE ROW PER REJECTION the closures actually answer, each stamped with that rejection's own date, so a closure answering an earlier rejection lands where `repair\_state` can count it. Verified on the reviewer's own two-round fixture: state complete, outstanding empty, gate clears. The test's oracle moved from the raw parsed closure list to the RESOLVED state and now asserts the unit reaches complete. Re-serialisation goes through `closures\_from\_document` so BG0618's semicolon escaping is not undone - a bare join did undo it and two existing tests caught that. The count guard was REMOVED: with per-rejection dispatch it refused a genuinely complete repair whenever one row legitimately closed two same-date rejections, and `repair\_state`'s own per-rejection outstanding computation is the real guard - probed both ways. AC5 was rewritten to that property and its test with it, disclosed here rather than folded in silently.; the test passes because its oracle is the raw parsed closure list off date-matched rows rather than a resolved set, so i -> AC7 now holds. `record\_repair` writes ONE ROW PER REJECTION the closures actually answer, each stamped with that rejection's own date, so a closure answering an earlier rejection lands where `repair\_state` can count it. Verified on the reviewer's own two-round fixture: state complete, outstanding empty, gate clears. The test's oracle moved from the raw parsed closure list to the RESOLVED state and now asserts the unit reaches complete. Re-serialisation goes through `closures\_from\_document` so BG0618's semicolon escaping is not undone - a bare join did undo it and two existing tests caught that. The count guard was REMOVED: with per-rejection dispatch it refused a genuinely complete repair whenever one row legitimately closed two same-date rejections, and `repair\_state`'s own per-rejection outstanding computation is the real guard - probed both ways. AC5 was rewritten to that property and its test with it, disclosed here rather than folded in silently.; the count guard falsely refuses a genuinely complete repair, since two same-date rejections closed by one repair row rea -> AC7 now holds. `record\_repair` writes ONE ROW PER REJECTION the closures actually answer, each stamped with that rejection's own date, so a closure answering an earlier rejection lands where `repair\_state` can count it. Verified on the reviewer's own two-round fixture: state complete, outstanding empty, gate clears. The test's oracle moved from the raw parsed closure list to the RESOLVED state and now asserts the unit reaches complete. Re-serialisation goes through `closures\_from\_document` so BG0618's semicolon escaping is not undone - a bare join did undo it and two existing tests caught that. The count guard was REMOVED: with per-rejection dispatch it refused a genuinely complete repair whenever one row legitimately closed two same-date rejections, and `repair\_state`'s own per-rejection outstanding computation is the real guard - probed both ways. AC5 was rewritten to that property and its test with it, disclosed here rather than folded in silently.; AC6's test never invokes conformance, so the criterion's second half is unexercised -> AC7 now holds. `record\_repair` writes ONE ROW PER REJECTION the closures actually answer, each stamped with that rejection's own date, so a closure answering an earlier rejection lands where `repair\_state` can count it. Verified on the reviewer's own two-round fixture: state complete, outstanding empty, gate clears. The test's oracle moved from the raw parsed closure list to the RESOLVED state and now asserts the unit reaches complete. Re-serialisation goes through `closures\_from\_document` so BG0618's semicolon escaping is not undone - a bare join did undo it and two existing tests caught that. The count guard was REMOVED: with per-rejection dispatch it refused a genuinely complete repair whenever one row legitimately closed two same-date rejections, and `repair\_state`'s own per-rejection outstanding computation is the real guard - probed both ways. AC5 was rewritten to that property and its test with it, disclosed here rather than folded in silently.; AC7 - the accepted closure must actually discharge the finding, or the criterion and the changelog must withdraw the cla -> AC7 now holds. `record\_repair` writes ONE ROW PER REJECTION the closures actually answer, each stamped with that rejection's own date, so a closure answering an earlier rejection lands where `repair\_state` can count it. Verified on the reviewer's own two-round fixture: state complete, outstanding empty, gate clears. The test's oracle moved from the raw parsed closure list to the RESOLVED state and now asserts the unit reaches complete. Re-serialisation goes through `closures\_from\_document` so BG0618's semicolon escaping is not undone - a bare join did undo it and two existing tests caught that. The count guard was REMOVED: with per-rejection dispatch it refused a genuinely complete repair whenever one row legitimately closed two same-date rejections, and `repair\_state`'s own per-rejection outstanding computation is the real guard - probed both ways. AC5 was rewritten to that property and its test with it, disclosed here rather than folded in silently. | - | | |
| BG0622 | 2026-08-28 | engineering; session | 2026-08-28 | the presence-then-coerce guard admits every falsey value except the empty string - executed against both refs, `0`, `0.0` -> The guard now accepts only a STRING or a BOOLEAN and refuses everything else, so `0`, `0.0`, `[]` and `{}` are refused as they were at the base ref, while `false` records as `no` and `true` as `yes` - which also fixes the non-blocking finding that they were stored as the Python repr. The test was widened from the two shapes the criterion named to every degenerate shape, which is the whole lesson of this review round.; `False` is stored as the literal `"False"` and `True` as `"True"`, the Python repr, rather than the documented no and ye -> The guard now accepts only a STRING or a BOOLEAN and refuses everything else, so `0`, `0.0`, `[]` and `{}` are refused as they were at the base ref, while `false` records as `no` and `true` as `yes` - which also fixes the non-blocking finding that they were stored as the Python repr. The test was widened from the two shapes the criterion named to every degenerate shape, which is the whole lesson of this review round.; the zero, float-zero, empty-list and empty-dict admission at sprint.py:10180 -> The guard now accepts only a STRING or a BOOLEAN and refuses everything else, so `0`, `0.0`, `[]` and `{}` are refused as they were at the base ref, while `false` records as `no` and `true` as `yes` - which also fixes the non-blocking finding that they were stored as the Python repr. The test was widened from the two shapes the criterion named to every degenerate shape, which is the whole lesson of this review round. | - | | |
| BG0623 | 2026-08-28 | sdlc-studio (agent) | 2026-08-28 | #1 -> replaced by `\_H1\_LINE\_RE` matching exactly one hash. The design changed rather than the pattern: the ONLY line ever overwritten is now an unambiguous level-one ATX heading outside every container, and every other shape gets an INSERT. Mutant "widen `\_H1\_LINE\_RE` to `#{1,6}`" registered killed at artifact.py.; #2 -> the pattern now captures the fence run, and a closer must match the opener's CHARACTER, be at least as long, and carry no info string. Mutants "narrow both runs to exactly three" and "replace the fence-closing test with a bare `if m:`" both registered killed.; #3 -> `\_first\_h1\_line` tracks comment state and `\_front\_matter\_end` skips front matter, with insertion placed AFTER it so an H1 cannot stop `---` being front matter. Three mutants registered killed.; #4 -> under the narrowed licence that line is correctly not a heading, so the canonical H1 is INSERTED and nothing is destroyed. Driven through the CLI over fourteen shapes with zero content loss.; #5 -> the test is now table-driven over fourteen shapes including two level-one headings, a `##`-only document, seven hashes, a tilde fence, a nested fence, an HTML comment and front matter, and asserts an unchanged marker COUNT rather than a shape list. "Return the LAST heading" is now killed.; #6 -> AC5 authored, plan re-derived to 15 rows, all 15 executed and killed, and `verify\_ac.py depth --write` regenerated the field to `criteria 5\; plan rows 15\; executed 15\; killed 15\; not-run 0`.; #7 -> a "Which line gets replaced" section now states the rule, the containers it excludes, and why the asymmetry is deliberate.; #8 -> [pre-existing] confirmed byte-identical at the base ref, reported and not repaired here. | - | | |
| BG0624 | 2026-08-28 | sdlc-studio (agent) | 2026-08-28 | #1 -> AC6 authored, and the AC1 fixture now carries BG0003 at status Fixed. The residue reader is deliberately status-blind. Mutant `add not \_is\_open(row[1]) to the residue loop's continue` registered killed.; #2 -> the test now runs `--check` as a second subprocess and asserts the id and the severity appear there too, following the `\_warn\_unparseable` precedent 166 lines above it. Mutant `remove \_warn\_unclassifiable from the check path` killed.; #3 -> AC9 authored, and both suites now assert the WRITTEN `> **Severity:**` line equals the value REQUESTED, for High/Medium/Critical. Low is exempt and stated as such, because it consolidates into a CR carrying no Severity line. Mutant `append and 'Medium' to the bug branch` killed.; #4 -> AC8 authored with a dedicated trimming test over a leading-space spelling, a trailing-space spelling, a space-padded lowercase spelling and a tab-padded form. A newline is out of scope and said so: a separate guard refuses it before the vocabulary is consulted. Both writers' mutants killed.; #5 -> AC7 authored, and the AC2 test now drives lowercase and space-padded spellings through the residue reader and requires an empty result. Mutant `replace the \_matches calls with bare membership tests` killed.; #6 -> all 15 plan rows executed and registered, and `verify\_ac.py depth --write` regenerated the field to `criteria 9, plan rows 15, executed 15, killed 15, not-run 0`.; #7 -> the claim is corrected in the artefact, and the third writer is filed as BG0633. transition.py is outside this unit's Affects, so it is reported and carried rather than fixed here. | - | | |
| BG0619 | 2026-08-28 | sdlc-studio (agent) | 2026-08-28 | #1 -> AC7 authored. The parent guard now asks the question the write will ask: a parent carrying no `> **Status:**` line is refused before anything is minted, naming the missing field. Verified through the CLI - retro parent gives rc=1 with 0 files and 0 index rows, CR parent still mints. Both mutants killed.; #2 -> it is now spliced from `ID\_SEARCH\_RE.pattern`, so the superset property holds by construction, and the test asserts the pipeline alternation appears verbatim rather than probing eight shapes. `artifact.py`'s META tuple, the fourth copy, now derives from the same map.; #3 -> AC8 authored and all three restored at the same point the walker applies them. The three phantom shapes the review demonstrated are pinned by test and all three mutants are killed.; #4 -> the comment now states what was measured: the two were equal, and find\_by\_id simply had no meta map. The unification argument is that a third equal literal is a drift nobody would notice.; #5 -> corrected, and the correction records that grep returned 1 hit which was the comment quoting itself.; #6 -> replaced with a mechanical assertion plus a per-prefix A/B over both readers. The prefix mutant is killed.; #7 -> the A/B now runs the SHIPPED `status.backlog` reader over a fixture with and without the three meta artefacts, and asserts each is resolvable first so the comparison cannot pass for the wrong reason. The review's own two-file mutant is killed.; #8 -> the test now mints EVERY type the creator accepts through the CLI, supplying the extra arguments four of them need, and resolves each back. It reads ids with `any\_record\_id`, having failed on `HO-0001` with the pipeline reader - this unit's own defect.; #9 -> [pre-existing] BG0632, filed and carried. This unit's prose wrongly claimed an exclusion no code implements and is corrected in the artefact and the changelog.; #10 -> D0174 now carries the re-measured figures for all three meta types, dated. | - | | |
| US0570 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Carried to BG0638 rather than repaired here. The finding is REAL and was never answered: \_ck\_not\_delivered iterates the retro's Batch instead of the run's planned set, `held` is read from an append-only list nothing removes from, and \_ck\_known\_issues FAILS OPEN where its sibling \_ck\_impediments reports the same blindness as UNANSWERED. Re-run against HEAD on 2026-09-02: still live. The story is Done and its code shipped a month ago, so the work moves to the bug under the operator's standing close rule - a non-stop-ship finding is opened as a bug and the story closes pointing at it. What is set aside is this unit's unanswered REJECT, not the rule: the finding itself is now tracked where it can be fixed. | - | delivery | |
| US0571 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Carried to BG0638 rather than repaired here. The finding is REAL and was never answered: \_ck\_not\_delivered iterates the retro's Batch instead of the run's planned set, `held` is read from an append-only list nothing removes from, and \_ck\_known\_issues FAILS OPEN where its sibling \_ck\_impediments reports the same blindness as UNANSWERED. Re-run against HEAD on 2026-09-02: still live. The story is Done and its code shipped a month ago, so the work moves to the bug under the operator's standing close rule - a non-stop-ship finding is opened as a bug and the story closes pointing at it. What is set aside is this unit's unanswered REJECT, not the rule: the finding itself is now tracked where it can be fixed. | - | delivery | |
| US0575 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Carried to BG0638 rather than repaired here. The finding is REAL and was never answered: \_ck\_not\_delivered iterates the retro's Batch instead of the run's planned set, `held` is read from an append-only list nothing removes from, and \_ck\_known\_issues FAILS OPEN where its sibling \_ck\_impediments reports the same blindness as UNANSWERED. Re-run against HEAD on 2026-09-02: still live. The story is Done and its code shipped a month ago, so the work moves to the bug under the operator's standing close rule - a non-stop-ship finding is opened as a bug and the story closes pointing at it. What is set aside is this unit's unanswered REJECT, not the rule: the finding itself is now tracked where it can be fixed. | - | delivery | |
| US0576 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Carried to BG0638 rather than repaired here. The finding is REAL and was never answered: \_ck\_not\_delivered iterates the retro's Batch instead of the run's planned set, `held` is read from an append-only list nothing removes from, and \_ck\_known\_issues FAILS OPEN where its sibling \_ck\_impediments reports the same blindness as UNANSWERED. Re-run against HEAD on 2026-09-02: still live. The story is Done and its code shipped a month ago, so the work moves to the bug under the operator's standing close rule - a non-stop-ship finding is opened as a bug and the story closes pointing at it. What is set aside is this unit's unanswered REJECT, not the rule: the finding itself is now tracked where it can be fixed. | - | delivery | |
| US0569 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Closed against BG0461, which fixed it. Re-run against HEAD on 2026-09-02: cycle\_drift returns unresolved 0, uncovered 0, unverifiable 0\; test\_sprint\_report.py:1789 asserts the third bucket\; and the reverse check now walks every verb the rows name - the function's own docstring records both halves as closed. The REJECT was recorded 2026-07-31 and never answered, and the work was done under another id before anyone returned to it. Re-filed as BG0639 during this triage and closed the same day as a duplicate, so the record shows what happened to the rejection rather than leaving it standing. | adding assertEqual(drift['unverifiable'], []) goes RED on the unmutated tree today, and two ceremony-verb rename mutants SURVIVED the full 91-test suite and the AC's own selector in isolation. The reverse check walks only sprint verbs though 6 of 18 rows name critic/retro/lessons/handoff verbs. US0569 AC1 requires planned POINTS, which are computed nowhere and rendered nowhere; its verifier cannot fail for the defect its own comment names - deleting the \\_planned\\_ids reconstruction SURVIVED, the fixture using one drop plus one add so raw and reconstructed are both 3. US0572 AC3 requires an authoriser, which record\\_waiver does not take and the decision schema has no column for; and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. | delivery | |
| US0572 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Closed against BG0461, which fixed it. Re-run against HEAD on 2026-09-02: cycle\_drift returns unresolved 0, uncovered 0, unverifiable 0\; test\_sprint\_report.py:1789 asserts the third bucket\; and the reverse check now walks every verb the rows name - the function's own docstring records both halves as closed. The REJECT was recorded 2026-07-31 and never answered, and the work was done under another id before anyone returned to it. Re-filed as BG0639 during this triage and closed the same day as a duplicate, so the record shows what happened to the rejection rather than leaving it standing. | adding assertEqual(drift['unverifiable'], []) goes RED on the unmutated tree today, and two ceremony-verb rename mutants SURVIVED the full 91-test suite and the AC's own selector in isolation. The reverse check walks only sprint verbs though 6 of 18 rows name critic/retro/lessons/handoff verbs. US0569 AC1 requires planned POINTS, which are computed nowhere and rendered nowhere; its verifier cannot fail for the defect its own comment names - deleting the \\_planned\\_ids reconstruction SURVIVED, the fixture using one drop plus one add so raw and reconstructed are both 3. US0572 AC3 requires an authoriser, which record\\_waiver does not take and the decision schema has no column for; and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. | delivery | |
| US0574 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Closed against BG0461, which fixed it. Re-run against HEAD on 2026-09-02: cycle\_drift returns unresolved 0, uncovered 0, unverifiable 0\; test\_sprint\_report.py:1789 asserts the third bucket\; and the reverse check now walks every verb the rows name - the function's own docstring records both halves as closed. The REJECT was recorded 2026-07-31 and never answered, and the work was done under another id before anyone returned to it. Re-filed as BG0639 during this triage and closed the same day as a duplicate, so the record shows what happened to the rejection rather than leaving it standing. | adding assertEqual(drift['unverifiable'], []) goes RED on the unmutated tree today, and two ceremony-verb rename mutants SURVIVED the full 91-test suite and the AC's own selector in isolation. The reverse check walks only sprint verbs though 6 of 18 rows name critic/retro/lessons/handoff verbs. US0569 AC1 requires planned POINTS, which are computed nowhere and rendered nowhere; its verifier cannot fail for the defect its own comment names - deleting the \\_planned\\_ids reconstruction SURVIVED, the fixture using one drop plus one add so raw and reconstructed are both 3. US0572 AC3 requires an authoriser, which record\\_waiver does not take and the decision schema has no column for; and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. | delivery | |
| US0569 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Closed by BG0461 (Fixed). Re-run against HEAD 2026-09-02: cycle\_drift returns unresolved 0, uncovered 0, unverifiable 0, and test\_sprint\_report.py:1789 asserts the third bucket. The reverse check now walks every verb the rows name\; the function's own docstring records both halves closed. Re-filed as BG0639 during triage and closed the same day as a duplicate, so the record shows what became of the rejection.; #2 -> Closed. The finding said planned POINTS are computed nowhere and rendered nowhere. Re-run against HEAD 2026-09-02 through the shipped command: `sprint.py report --id RETRO0111` prints `Planned against delivered: 10/11 unit(s), 35/36 point(s) delivered` and `Delivered: 10 unit(s), 35 points.` Both halves are now computed and rendered.; #3 -> Closed by BG0461, which the finding's own text names as the artefact it was filed to: `Filed BG0461`. BG0461 is Fixed. The unvalidated waiver scope tail was its subject, and it was fixed under that id rather than against this unit's rejection. | and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. | delivery | |
| US0572 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Closed by BG0461 (Fixed). Re-run against HEAD 2026-09-02: cycle\_drift returns unresolved 0, uncovered 0, unverifiable 0, and test\_sprint\_report.py:1789 asserts the third bucket. The reverse check now walks every verb the rows name\; the function's own docstring records both halves closed. Re-filed as BG0639 during triage and closed the same day as a duplicate, so the record shows what became of the rejection.; #2 -> Closed. The finding said planned POINTS are computed nowhere and rendered nowhere. Re-run against HEAD 2026-09-02 through the shipped command: `sprint.py report --id RETRO0111` prints `Planned against delivered: 10/11 unit(s), 35/36 point(s) delivered` and `Delivered: 10 unit(s), 35 points.` Both halves are now computed and rendered.; #3 -> Closed by BG0461, which the finding's own text names as the artefact it was filed to: `Filed BG0461`. BG0461 is Fixed. The unvalidated waiver scope tail was its subject, and it was fixed under that id rather than against this unit's rejection. | and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. | delivery | |
| US0574 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | #1 -> Closed by BG0461 (Fixed). Re-run against HEAD 2026-09-02: cycle\_drift returns unresolved 0, uncovered 0, unverifiable 0, and test\_sprint\_report.py:1789 asserts the third bucket. The reverse check now walks every verb the rows name\; the function's own docstring records both halves closed. Re-filed as BG0639 during triage and closed the same day as a duplicate, so the record shows what became of the rejection.; #2 -> Closed. The finding said planned POINTS are computed nowhere and rendered nowhere. Re-run against HEAD 2026-09-02 through the shipped command: `sprint.py report --id RETRO0111` prints `Planned against delivered: 10/11 unit(s), 35/36 point(s) delivered` and `Delivered: 10 unit(s), 35 points.` Both halves are now computed and rendered.; #3 -> Closed by BG0461, which the finding's own text names as the artefact it was filed to: `Filed BG0461`. BG0461 is Fixed. The unvalidated waiver scope tail was its subject, and it was fixed under that id rather than against this unit's rejection. | and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. | delivery | |
| US0569 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. -> Closed by BG0461, which this finding's own text names as the artefact it was filed to - `Filed BG0461` appears in the finding itself. BG0461 is Fixed. The unvalidated waiver scope tail was its subject and was repaired under that id rather than against this unit's rejection, which is why the rejection stood unanswered for a month while the defect behind it was already gone. | US0574 AC3 fails open: cycle\\_drift returns three buckets and the verifier asserts two, and the third is ALREADY non-empty on the shipped tree - retro.py builds subparsers inline in main() so 'retro validate' cannot be checked and 2 of 18 rows are unverifiable while the guard reports green; adding assertEqual(drift['unverifiable'], []) goes RED on the unmutated tree today, and two ceremony-verb rename mutants SURVIVED the full 91-test suite and the AC's own selector in isolation. The reverse check walks only sprint verbs though 6 of 18 rows name critic/retro/lessons/handoff verbs. US0569 AC1 requires planned POINTS, which are computed nowhere and rendered nowhere; its verifier cannot fail for the defect its own comment names - deleting the \\_planned\\_ids reconstruction SURVIVED, the fixture using one drop plus one add so raw and reconstructed are both 3. US0572 AC3 requires an authoriser, which record\\_waiver does not take and the decision schema has no column for | delivery | |
| US0572 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. -> Closed by BG0461, which this finding's own text names as the artefact it was filed to - `Filed BG0461` appears in the finding itself. BG0461 is Fixed. The unvalidated waiver scope tail was its subject and was repaired under that id rather than against this unit's rejection, which is why the rejection stood unanswered for a month while the defect behind it was already gone. | US0574 AC3 fails open: cycle\\_drift returns three buckets and the verifier asserts two, and the third is ALREADY non-empty on the shipped tree - retro.py builds subparsers inline in main() so 'retro validate' cannot be checked and 2 of 18 rows are unverifiable while the guard reports green; adding assertEqual(drift['unverifiable'], []) goes RED on the unmutated tree today, and two ceremony-verb rename mutants SURVIVED the full 91-test suite and the AC's own selector in isolation. The reverse check walks only sprint verbs though 6 of 18 rows name critic/retro/lessons/handoff verbs. US0569 AC1 requires planned POINTS, which are computed nowhere and rendered nowhere; its verifier cannot fail for the defect its own comment names - deleting the \\_planned\\_ids reconstruction SURVIVED, the fixture using one drop plus one add so raw and reconstructed are both 3. US0572 AC3 requires an authoriser, which record\\_waiver does not take and the decision schema has no column for | delivery | |
| US0574 | 2026-07-31 | sdlc-studio (agent) | 2026-09-02 | and the scope tail is never validated, so a waiver naming a nonexistent item records cleanly and is read by nothing - verbatim the defect decisions.py documents as having shipped once already. Filed BG0461. -> Closed by BG0461, which this finding's own text names as the artefact it was filed to - `Filed BG0461` appears in the finding itself. BG0461 is Fixed. The unvalidated waiver scope tail was its subject and was repaired under that id rather than against this unit's rejection, which is why the rejection stood unanswered for a month while the defect behind it was already gone. | US0574 AC3 fails open: cycle\\_drift returns three buckets and the verifier asserts two, and the third is ALREADY non-empty on the shipped tree - retro.py builds subparsers inline in main() so 'retro validate' cannot be checked and 2 of 18 rows are unverifiable while the guard reports green; adding assertEqual(drift['unverifiable'], []) goes RED on the unmutated tree today, and two ceremony-verb rename mutants SURVIVED the full 91-test suite and the AC's own selector in isolation. The reverse check walks only sprint verbs though 6 of 18 rows name critic/retro/lessons/handoff verbs. US0569 AC1 requires planned POINTS, which are computed nowhere and rendered nowhere; its verifier cannot fail for the defect its own comment names - deleting the \\_planned\\_ids reconstruction SURVIVED, the fixture using one drop plus one add so raw and reconstructed are both 3. US0572 AC3 requires an authoriser, which record\\_waiver does not take and the decision schema has no column for | delivery | |
| BG0628 | 2026-09-02 | sdlc-studio (agent) | 2026-09-02 | #1 -> AC4 now holds on the tree it names. `\_uncollectable\_because\_absent` treats a file that is PRESENT but cannot collect because what it imports is absent as unevaluable, and tells that apart from a file broken on its own terms, which stays a dead stamp. Measured through the shipped CLI: complete tree, a checkout without the skill directory, and a `sdlc-studio/`-only copy all report 731/814 with 1 non-conformant, where the second previously read 727/5.; #2 -> AC4's verifier keeps its in-process A/B and the CLI claim is now carried by the new pair of tests, one for the present-but-uncollectable case and one control for a file broken on its own terms.; #3 -> The changelog is rewritten. The three false claims are gone: it now says selector\_resolves is deliberately NOT widened and why, that unevaluable\_stamps is a SUBSET, and it quotes 731/814 measured after the fix rather than a figure no tree reports.; #4 -> The SUBSET claim's docstring now states the relation as a subset rather than disjointness, which is what the code and the test assert.; #5 -> The ac\_id keying is left as it is and reported: 0 of 814 story files carry a duplicate stamped AC id, so it is latent rather than live, and changing the key is a separate change with its own blast radius.; #6 -> AC3's before-any-verdict half is now pinned by position, not by presence - the test asserts the unevaluable line appears BEFORE any verdict line, which is what the criterion says and what an assertIn could not see.; #7 -> The stamped-green restriction is pinned by its own test: an unstamped criterion claims nothing, so its selector says nothing about this tree. | the stamped-green restriction in unevaluable\\_stamps was unpinned - dropping it changed behaviour invisibly | delivery | ea3c38fb94cc |
| BG0631 | 2026-09-02 | sdlc-studio (agent) | 2026-09-02 | #1 -> AC3 is now built in full. The ledger carries a `Rejection` column keyed on the brief fingerprint, which BG0607 established identifies the seat and the round together where a date cannot, and the verifier asserts both halves rather than the phase alone.; #2 -> The closure fallback is pinned by a test for the case it exists for - two rejections on one date raising DIFFERENT findings, where the closure places the row. It remains inert on today's ledger, and both the code comment and the changelog now say so.; #3 -> The false code comment is replaced. It claimed the fallback had rescued US0674\; US0674 reads `none` with or without it, which the changelog already said correctly, and the comment now records the measurement rather than the wish.; #4 -> The header-guard comment now states the real mechanism: the reader compares underscored column names against spaced markdown cells, so the header has always been returned as data, at every width.; #5 -> The five units that moved are NAMED in the changelog with what it costs - US0664, US0665, US0666, US0671 and US0674, including 72 recorded closures that stop counting.; #6 -> record\_repair's default phase is pinned by a test that supplies none.; #7 -> The zero-hit branch of unattributable\_repairs is pinned by a test for a repair dated to no rejection at all.; #8 -> THE REGRESSION: appending two columns at once broke the reader's short-by-one bound, so every six-cell legacy ledger stopped parsing and the test-plan gate began refusing units whose repairs were on record. The bound is generalised to short-by-any with a two-cell floor, and pinned by a test built on the PRE-COLUMN shape a consuming project holds the moment before it upgrades. | appending two columns at once broke the reader's short-by-one bound, so every six-cell legacy ledger stopped parsing entirely - repairs\\_for returned nothing and the test-plan gate began refusing units whose repairs were on record, which would have hit every consuming project on upgrade | delivery | 7cab12ad4a67 |
| BG0636 | 2026-09-02 | sdlc-studio (agent) | 2026-09-02 | #1 -> AC7's test now compares SETS: every id the predicate refuses for no-verifier is checked against the ids the parser reads a verifier on, and the overlap must be empty. The mutant refusing all 391 units is killed.; #2 -> AC3's test now drives `sprint plan` as a subprocess and asserts the EXIT CODE and the refusal, with a control that a verifiable unit plans. The mutant deleting the plan gate is killed.; #3 -> All three prose surfaces are aligned with D0178 - the warning, the flag help and the call-site comment now say REPORTED here and REFUSED at the planner.; #4 -> The filer/planner asymmetry is stated in the warning itself rather than glossed: the planner refuses a unit whose criteria carry NO verifier, and a unit where only some are bare is planned, so the bare ones are the criteria nobody will run.; #5 -> The changelog's hand-counted figure is replaced by what the shipped command reports.; #6 -> The vocabulary docstring no longer claims the suite pins completeness against conformance's own list, and the test class docstring records the D0178 amendment instead of contradicting it.; #7 -> A --verify supplied with no matching --ac is left as it is and reported: the pairing is positional and documented as such, and refusing a surplus selector is a separate decision from this unit's. | ac\\_miss\\_vocabulary's docstring claimed the suite pins completeness against conformance's own vocabulary where the test hard-codes a copy; the test class docstring still asserted AC2 requires a refusal, contradicting D0178 and the test forty lines below it | delivery | e83f6b3d8df3 |
| BG0635 | 2026-09-02 | sdlc-studio (agent) | 2026-09-02 | #1 -> AC3 is NARROWED by D0179 and now drives `sprint.py close` as a subprocess, asserting the close's DECISION - the branch that acts on a stopped loop - rather than the trend string it narrates. The mutant deleting that branch is killed. The converged direction is not demonstrable through the CLI without constructing an entire valid run, and that is recorded rather than quietly dropped: AC1 and AC2 pin it where it is observable.; #2 -> The config comment block is rewritten. The orphaned fragments are gone and the residue instructing the next operator to put the value back to 3 is replaced by a statement that the key is deliberately absent, why no single value is correct, and that splitting it is the open work. | - | delivery | 494e51cf0de1 |
| BG0628 | 2026-09-02 | sdlc-studio (agent) | 2026-09-02 | the stamped-green restriction in unevaluable\\\_stamps was unpinned - dropping it changed behaviour invisibly -> An unstamped criterion claims nothing, so a selector it cannot resolve says nothing about this tree. Pinned by `test\_only\_stamped\_criteria\_are\_reported`, which asserts an unstamped AC is not reported, and the mutant dropping the filter is killed. | AC4 is not met on the tree AC4 itself names - a checkout without the skill directory read 727/814 with 5 non-conformant against 731/814 with 1 complete, because unevaluable\\_stamps checked only the selector's own file and a file can be PRESENT while what it imports is not; AC4's verifier asserted a weaker claim than AC4 - detect\\_conformance in-process over a synthetic tempdir where the criterion says this repository's own tree through the shipped CLI; three changelog claims false against code and corpus - selector\\_resolves returns False not None, unevaluable\\_stamps is a SUBSET not disjoint, and no tree reports the 725/814 figure quoted; the SUBSET claim fails when pytest is off PATH, where selector\\_resolves returns None and unevaluable\\_stamps has no such guard; the subtraction is keyed on ac\\_id, which is not unique within a file, so it can suppress a different row than intended - latent at 0 of 814 today; AC2's declared mutant is not killed by AC2's declared verifier; AC3's before-any-verdict half was unpinned - the whole block moved to the end of cmd\\_check with the test green | delivery | ea3c38fb94cc |
| BG0631 | 2026-09-02 | sdlc-studio (agent) | 2026-09-02 | appending two columns at once broke the reader's short-by-one bound, so every six-cell legacy ledger stopped parsing entirely - repairs\\\_for returned nothing and the test-plan gate began refusing units whose repairs were on record, which would have hit every consuming project on upgrade -> The bound is generalised from short-by-one to short-by-any with a two-cell floor, and pinned by a test built on the PRE-COLUMN six-cell shape a consuming project holds the moment before it upgrades. The mutant restoring the old bound is killed. This was the sharpest finding of the round: it would have broken every consuming ledger on upgrade, not only this repository's. | AC3 is half-implemented and stamped complete - the row names its phase but no rejection, though the criterion says it names both and says the ledger carries NEITHER today, and the verifier asserted only the phase half; the closure fallback is inert in the suite and in production - deleting it left all tests green and moved 0 of 1388 live unit-phase pairs, about thirty lines of new code no test could fail on; the code comment justifying that fallback claimed it had rescued US0674, which reads none with or without it - the changelog stated the opposite and correct fact, so two prose surfaces in one diff contradicted each other and the code was the false one; unattributable\\_repairs and \\_row\\_answers\\_phase implement different rules, so a legacy row placed by its closure is still listed as unattributable; the header-guard comment states a false mechanism - the header has always been returned as data because the reader compares underscored column names against spaced markdown cells, at every width; five units moved to none under the code change alone - US0664, US0665, US0666, US0671 and US0674, including 72 recorded closures that stop counting - and none was named; the Affects declares reference-schema.md, which the diff never touched; record\\_repair's default phase was unpinned | delivery | 7cab12ad4a67 |
| BG0636 | 2026-09-02 | sdlc-studio (agent) | 2026-09-02 | ac\\\_miss\\\_vocabulary's docstring claimed the suite pins completeness against conformance's own vocabulary where the test hard-codes a copy -> The docstring no longer claims the suite pins completeness against conformance's own vocabulary\; it states what the test does - assert the renderings cover the reasons the predicate can return.; the test class docstring still asserted AC2 requires a refusal, contradicting D0178 and the test forty lines below it -> The test class docstring now records the D0178 amendment rather than contradicting it, and says what survives from the original criterion: the message is asserted on its CONTENT, never on an absent error string. | AC7's test performed NO set comparison though AC7 makes comparing sets the criterion - its only assertions were that the refused set was non-empty and that a string appeared in the scan output, and a mutant refusing all 391 units instead of 92 passed it; AC3's test ran breakdown, which exits 0, not the plan REFUSAL the criterion names - so deleting the plan gate left it green while plan printed a full wave at rc=0; three prose surfaces still said the filing was REFUSED after D0178 made it a report at exit 0 - the user-visible warning, the flag help and the call-site comment; the filer and the planner applied DIFFERENT rules, the filer warning per-criterion and the planner refusing only when NO criterion carries a verifier, so the warning claimed a refusal that does not happen for the mixed case; a --verify supplied beyond the number of --ac, or with none, is validated and then silently discarded where the base ref failed loudly on an unknown flag; report\\_unverifiable\\_criteria is annotated as returning a string and documented as never raising, and returns None on one path; the changelog claimed 12 of 19 where the shipped command reports 15 units and 9 ungroomed - a hand-counted figure in the unit whose AC7 forbids exactly that | delivery | e83f6b3d8df3 |
| US0674 | 2026-08-24 | sdlc-studio (agent) | 2026-09-03 | docs/release-notes-v5.0.1.md:97 still reads `The six High findings above are listed separately` while the paragraph this commit rewrote at line 77 now says four are open, and `tools/known\\\_issues.py --bar` exits 1 reporting exactly 4. At the base ref the two paragraphs AGREED at six and six, so this commit made a true sentence false in the project's own disclosure page - the one surface that exists to be read rather than trusted. The file is in no unit's Affects and carries no changelog fragment, so it rides the batch commit unowned -> Closed at HEAD. `docs/release-notes-v5.0.1.md` carries no 'six High findings' sentence\; the disclosure paragraph and the count are both re-derived from the corpus by `known\_issues.py --write` and guarded in both directions by `tools/tests/test\_known\_issues.py`, which is green. Three repair rows already recorded this in 2026-08-24\; they are re-stated here because the phase rule introduced by BG0631 cannot attribute a legacy row whose date is ambiguous, and this row can.; the lane truncates its findings to the first three with NO +N more indicator while the sibling `\\\_derived\\\_depth` in the same diff does print one, so on a batch of four or more refused units AC1's naming-each is silently not delivered -> Closed at HEAD, verified by reading both sites. `gate.py`:1137 is `"\; ".join(f["why"] for f in faults[:3]) + (f" (+{len(faults) - 3} more)" if len(faults) > 3 else "")`, and the sibling `\_first\_three` at :1206 does the same. The truncation the finding names now carries the +N indicator at both sites, so a batch of four or more refused units no longer under-reports silently.; the AC4 roster test pins the word ADVISORY and the lane name but never ties either to the lane's actual blocking flag, so a flip to blocking would leave the roster false with that test still green -> Closed at HEAD by an existing repair row from 2026-08-24: the AC4 roster test reads the lane's actual `blocking` value on both return paths, so a flip to blocking cannot leave the roster false with the test still green. Re-stated here for the attribution reason above. | AC3's law is that the LANE records its yield to a file, and nothing pins the lane to the recorder - AC3's verifier calls `\\_record\\_revert\\_yield` directly, never through `\\_revert\\_check`. EXECUTED: replacing the lane's call with `pass` leaves all 10 lane tests green including AC3's own selector, and a repo-wide grep finds no test anywhere asserting the file exists after a real lane run. BOTH declared plan rows for AC3 mutate the recorder or its path constant, so neither can reach the call site and the ledger's two killed verdicts are evidence about the recorder, not the lane; CR0552 the lane reverts files in the live tree at a real push - filed. Round-1 ruling: boundary-binding and advisory-not-blocking are CLOSED, AC3's repair is OVER-CLAIMED | delivery | b75c08c97613 |
| US0674 | 2026-08-24 | sdlc-studio (agent) | 2026-09-03 | AC3's law is that the LANE records its yield to a file, and nothing pins the lane to the recorder - AC3's verifier calls `\\\_record\\\_revert\\\_yield` directly, never through `\\\_revert\\\_check`. EXECUTED: replacing the lane's call with `pass` leaves all 10 lane tests green including AC3's own selector, and a repo-wide grep finds no test anywhere asserting the file exists after a real lane run. BOTH declared plan rows for AC3 mutate the recorder or its path constant, so neither can reach the call site and the ledger's two killed verdicts are evidence about the recorder, not the lane -> Carried to BG0640, filed 2026-09-03. Verified still live at HEAD: with nothing refused and nothing crashed the lane returns blocking False with the detail `0 unit(s) examined, none stayed green without its change` - a clause vacuously true over an empty set, in the exact shape the crashed branch immediately above it was repaired to avoid. The work moves to the bug under the operator's standing close rule.; CR0552 the lane reverts files in the live tree at a real push - filed. Round-1 ruling: boundary-binding and advisory-not-blocking are CLOSED, AC3's repair is OVER-CLAIMED -> Carried to BG0640, filed 2026-09-03. Verified still live at HEAD: with nothing refused and nothing crashed the lane returns blocking False with the detail `0 unit(s) examined, none stayed green without its change` - a clause vacuously true over an empty set, in the exact shape the crashed branch immediately above it was repaired to avoid. The work moves to the bug under the operator's standing close rule.; with every unit reported-not-examined the lane prints 0 examined and PASSES, an absence reading as a pass -> Carried to BG0640, filed 2026-09-03. Verified still live at HEAD: with nothing refused and nothing crashed the lane returns blocking False with the detail `0 unit(s) examined, none stayed green without its change` - a clause vacuously true over an empty set, in the exact shape the crashed branch immediately above it was repaired to avoid. The work moves to the bug under the operator's standing close rule.; CR0552 -> Carried, not closed. This finding names CR0552, which is still In Progress - so the work it points at is tracked and unfinished rather than done. Recording it as carried keeps the pointer live\; closing it would assert a completion CR0552's own status contradicts. | - | delivery | b75c08c97613 |
| BG0603 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1 re-worded: the linted set is every non-terminal status, fixture holds an Open AND an In Progress bug, mutant added that enumerates open and omits in progress; #2 -> AC1 re-worded: the linted set is every non-terminal status, fixture holds an Open AND an In Progress bug, mutant added that enumerates open and omits in progress; #8 -> AC1 re-worded: the linted set is every non-terminal status, fixture holds an Open AND an In Progress bug, mutant added that enumerates open and omits in progress; #3 -> AC3 states the lint\_markdown\_evidence widening as intended and measured zero-impact\; fixture carries a grep-over-.py verifier\; mutant added on lint\_markdown\_evidence; #4 -> AC3 states the lint\_markdown\_evidence widening as intended and measured zero-impact\; fixture carries a grep-over-.py verifier\; mutant added on lint\_markdown\_evidence; #9 -> AC3 states the lint\_markdown\_evidence widening as intended and measured zero-impact\; fixture carries a grep-over-.py verifier\; mutant added on lint\_markdown\_evidence; #5 -> AC1 and AC3 drive `verify\_ac.py lint --ratchet --bugs` as a subprocess over a fixture tree, the pre-commit invocation; #6 -> AC1 and AC3 drive `verify\_ac.py lint --ratchet --bugs` as a subprocess over a fixture tree, the pre-commit invocation; #7 -> no change: rows kept as approved | - | plan-review | ff4004992e0e |
| BG0640 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC3 added: zero examined with crashes leads with the crash count then no unit was examined, never 0 examined and clean\; mutant keeps the crashed branch's clause; #2 -> AC3 added: zero examined with crashes leads with the crash count then no unit was examined, never 0 examined and clean\; mutant keeps the crashed branch's clause; #3 -> AC3 added: zero examined with crashes leads with the crash count then no unit was examined, never 0 examined and clean\; mutant keeps the crashed branch's clause; #8 -> AC3 added: zero examined with crashes leads with the crash count then no unit was examined, never 0 examined and clean\; mutant keeps the crashed branch's clause; #4 -> AC1: the absence says how many reported and how many in error\; mutant added that omits the counts; #5 -> AC1: the absence says how many reported and how many in error\; mutant added that omits the counts; #6 -> no change: control and points as approved; #7 -> no change: control and points as approved | - | plan-review | 0041b467807d |
| BG0643 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1 re-worded: a typo shape outranks the not-yet-written shape, and the fixture's method name matches nothing in the file; #2 -> AC1 re-worded: a typo shape outranks the not-yet-written shape, and the fixture's method name matches nothing in the file; #12 -> AC1 re-worded: a typo shape outranks the not-yet-written shape, and the fixture's method name matches nothing in the file; #3 -> AC1 row added: consult selector\_near\_miss file-wide BEFORE the class-collected check; #13 -> AC1 row added: consult selector\_near\_miss file-wide BEFORE the class-collected check; #4 -> AC3 re-worded: close match scoped to the named class, a 0.6-or-better match in ANOTHER class must be accepted, one test asserts all three halves; #5 -> AC3 row added mutating verify\_ac.py selector\_near\_miss: gather candidates file-wide (today's code); #14 -> AC3 row added mutating verify\_ac.py selector\_near\_miss: gather candidates file-wide (today's code); #6 -> AC5 added: the four VerifySelectorWriteGuardTests tests named, re-authored under their own names\; BG0570 AC1 and US0667 AC1 re-worded with a revision row each in the same commit\; US0667 added to Affects; #7 -> AC5 added: the four VerifySelectorWriteGuardTests tests named, re-authored under their own names\; BG0570 AC1 and US0667 AC1 re-worded with a revision row each in the same commit\; US0667 added to Affects; #15 -> AC5 added: the four VerifySelectorWriteGuardTests tests named, re-authored under their own names\; BG0570 AC1 and US0667 AC1 re-worded with a revision row each in the same commit\; US0667 added to Affects; #8 -> AC1 asserts `FAIL AC1` on the subprocess output of verify\_ac.py run\; the exit-0 vacuity rendering filed into CR0511 as pre-existing; #9 -> AC1 asserts `FAIL AC1` on the subprocess output of verify\_ac.py run\; the exit-0 vacuity rendering filed into CR0511 as pre-existing; #10 -> AC2 text: the test asserts the class NAME, not only did-you-mean; #11 -> AC4 text states it passes today and is the control whose mutant sits in the node-versus-file split | - | plan-review | 267e53efd0e8 |
| BG0643 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC3 re-worded: close match scoped to the named class, a 0.6-or-better match in ANOTHER class must be accepted, one test asserts all three halves; #2 -> AC3 re-worded: close match scoped to the named class, a 0.6-or-better match in ANOTHER class must be accepted, one test asserts all three halves; #3 -> AC3 re-worded: close match scoped to the named class, a 0.6-or-better match in ANOTHER class must be accepted, one test asserts all three halves; #11 -> AC3 re-worded: close match scoped to the named class, a 0.6-or-better match in ANOTHER class must be accepted, one test asserts all three halves; #9 -> AC5 added: the four VerifySelectorWriteGuardTests tests named, re-authored under their own names\; BG0570 AC1 and US0667 AC1 re-worded with a revision row each in the same commit\; US0667 added to Affects; #10 -> AC5 added: the four VerifySelectorWriteGuardTests tests named, re-authored under their own names\; BG0570 AC1 and US0667 AC1 re-worded with a revision row each in the same commit\; US0667 added to Affects; #14 -> AC5 added: the four VerifySelectorWriteGuardTests tests named, re-authored under their own names\; BG0570 AC1 and US0667 AC1 re-worded with a revision row each in the same commit\; US0667 added to Affects; #4 -> AC1 asserts `FAIL AC1` on the subprocess output of verify\_ac.py run\; the exit-0 vacuity rendering filed into CR0511 as pre-existing; #5 -> AC1 asserts `FAIL AC1` on the subprocess output of verify\_ac.py run\; the exit-0 vacuity rendering filed into CR0511 as pre-existing; #12 -> AC1 asserts `FAIL AC1` on the subprocess output of verify\_ac.py run\; the exit-0 vacuity rendering filed into CR0511 as pre-existing; #6 -> AC1 row added: route the selector through \_md\_safe, so byte-exact has a mutant; #13 -> AC1 row added: route the selector through \_md\_safe, so byte-exact has a mutant; #7 -> AC4 text states it passes today and is the control whose mutant sits in the node-versus-file split; #8 -> AC4 text states it passes today and is the control whose mutant sits in the node-versus-file split | - | plan-review | 267e53efd0e8 |
| BG0641 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #2 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #12 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #3 -> AC1: the stub prints a lane name and the test asserts it in the push's stderr; #4 -> AC3 re-worded: tag ref runs release once, branch-only runs push, a deletion row runs no gate, remote-ref column read\; mutant added for deletions; #5 -> AC5 (was AC4): fixture clone carries a post-merge hook the script cannot know, one line per hook required, mutant is hard-code the three names; #13 -> AC5 (was AC4): fixture clone carries a post-merge hook the script cannot know, one line per hook required, mutant is hard-code the three names; #6 -> AC6 accepts both spellings, --boundary <b> and SDLC\_GATE\_BOUNDARY=<b>; #7 -> AC6 accepts both spellings, --boundary <b> and SDLC\_GATE\_BOUNDARY=<b>; #8 -> AC6: the test reads AGENTS.md in-process so relevance selection runs it; #9 -> AC6: the test reads AGENTS.md in-process so relevance selection runs it; #10 -> AC7 mutant added: hard-code the boundary list instead of reading the roster; #14 -> AC7 mutant added: hard-code the boundary list instead of reading the roster; #11 -> AC6/AC7 read the roster from AGENTS.md, never gate.py's BOUNDARIES tuple | AC4 (unforeseen hook name in the fixture); AC5 (report what was read, refuse an empty roster, AC6 fixture in the `push/release` shape). | plan-review | 41b97c1effdb |
| BG0642 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC3: a stale acknowledgement naming another run refuses\; mutant added testing -n instead of comparing the id; #9 -> AC3: a stale acknowledgement naming another run refuses\; mutant added testing -n instead of comparing the id; #2 -> AC1: the stub records argv and the test asserts --workflow Lint --branch main --event push --status completed\; fixture carries a newer green Dependabot run\; mutant added dropping the workflow filter; #10 -> AC1: the stub records argv and the test asserts --workflow Lint --branch main --event push --status completed\; fixture carries a newer green Dependabot run\; mutant added dropping the workflow filter; #3 -> AC2 added: any conclusion other than success refuses, exercised with timed\_out\; mutant compares == failure; #11 -> AC2 added: any conclusion other than success refuses, exercised with timed\_out\; mutant compares == failure; #4 -> AC1 and AC4: PATH built from the stub directory and git alone; #5 -> AC1 and AC4: PATH built from the stub directory and git alone; #12 -> AC1 and AC4: PATH built from the stub directory and git alone; #6 -> AC4: exact sentence `CI state UNREAD: could not read the latest Lint run on main`, push completes, not a substring of any refusal; #7 -> AC5: --jq prints null after removal\; whoever read the protection back is named; #8 -> Depends on: BG0641 already declared\; the planner sequences them | BLOCKING: AC1 (name the workflow or check, add the newer-green-other-workflow fixture); AC1/AC2 (the rule for non-success non-failure conclusions). | plan-review | 2c19489227b2 |
| BG0644 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1 re-worded to the SUM over the selection with the masking bound stated\; fixture is a two-module selection over the summed budget but under \_total\; mutant added holding to the largest entry; #2 -> AC1 re-worded to the SUM over the selection with the masking bound stated\; fixture is a two-module selection over the summed budget but under \_total\; mutant added holding to the largest entry; #3 -> AC1 re-worded to the SUM over the selection with the masking bound stated\; fixture is a two-module selection over the summed budget but under \_total\; mutant added holding to the largest entry; #11 -> AC1 re-worded to the SUM over the selection with the masking bound stated\; fixture is a two-module selection over the summed budget but under \_total\; mutant added holding to the largest entry; #4 -> AC1: budget file path overridable by TEST\_NOISE\_BUDGET\_FILE for the fixture; #12 -> AC1: budget file path overridable by TEST\_NOISE\_BUDGET\_FILE for the fixture; #5 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #13 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #6 -> AC4: positive controls - lowered, vanished, new-zero and uncommitted all accepted\; mutant added refusing every difference; #7 -> AC3: passes and prints the declared-debt line as today; #8 -> AC3: passes and prints the declared-debt line as today; #9 -> Fix note: the file takes precedence over TEST\_NOISE\_BASELINE\; the scalar is the fallback when no file is found; #10 -> no change: noted as fine | - | plan-review | 189430ff8f6c |
| BG0644 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1 re-worded to the SUM over the selection with the masking bound stated\; fixture is a two-module selection over the summed budget but under \_total\; mutant added holding to the largest entry; #2 -> AC1 re-worded to the SUM over the selection with the masking bound stated\; fixture is a two-module selection over the summed budget but under \_total\; mutant added holding to the largest entry; #3 -> AC1 re-worded to the SUM over the selection with the masking bound stated\; fixture is a two-module selection over the summed budget but under \_total\; mutant added holding to the largest entry; #11 -> AC1 re-worded to the SUM over the selection with the masking bound stated\; fixture is a two-module selection over the summed budget but under \_total\; mutant added holding to the largest entry; #4 -> AC1: budget file path overridable by TEST\_NOISE\_BUDGET\_FILE for the fixture; #5 -> AC1: budget file path overridable by TEST\_NOISE\_BUDGET\_FILE for the fixture; #12 -> AC1: budget file path overridable by TEST\_NOISE\_BUDGET\_FILE for the fixture; #6 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #7 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #8 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #13 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #10 -> no change: noted as fine; #9 -> AC2: \_total is a separate measured entry (106), not the sum of the modules | - | plan-review | 189430ff8f6c |
| BG0644 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #4 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #5 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #6 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #13 -> AC5 added: skill-tests.sh runs budget-check before the suite (exercised through the script on a fixture repo) and a tools test runs it over this tree at HEAD\; mutant removes the call; #1 -> AC1: the refusal names the selected modules, their summed budget and the count; #2 -> AC1: the refusal names the selected modules, their summed budget and the count; #3 -> AC1: the refusal names the selected modules, their summed budget and the count; #12 -> AC1: the refusal names the selected modules, their summed budget and the count; #7 -> AC3 states a renamed module is refused until its entry is recorded at zero or its leaks captured, and says so in the refusal; #8 -> AC3 states a renamed module is refused until its entry is recorded at zero or its leaks captured, and says so in the refusal; #9 -> Fix note corrected: tools/ ships nowhere\; the scalar remains only as the fallback; #10 -> Fix note corrected: tools/ ships nowhere\; the scalar remains only as the fallback; #11 -> Points re-sized 3 to 5 | - | plan-review | 189430ff8f6c |
| BG0641 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #2 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #12 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #3 -> AC1: the stub prints a lane name and the test asserts it in the push's stderr; #4 -> AC3 re-worded: tag ref runs release once, branch-only runs push, a deletion row runs no gate, remote-ref column read\; mutant added for deletions; #5 -> AC5 (was AC4): fixture clone carries a post-merge hook the script cannot know, one line per hook required, mutant is hard-code the three names; #13 -> AC5 (was AC4): fixture clone carries a post-merge hook the script cannot know, one line per hook required, mutant is hard-code the three names; #6 -> AC6 accepts both spellings, --boundary <b> and SDLC\_GATE\_BOUNDARY=<b>; #7 -> AC6 accepts both spellings, --boundary <b> and SDLC\_GATE\_BOUNDARY=<b>; #8 -> AC6: the test reads AGENTS.md in-process so relevance selection runs it; #9 -> AC6: the test reads AGENTS.md in-process so relevance selection runs it; #10 -> AC7 mutant added: hard-code the boundary list instead of reading the roster; #14 -> AC7 mutant added: hard-code the boundary list instead of reading the roster; #11 -> AC6/AC7 read the roster from AGENTS.md, never gate.py's BOUNDARIES tuple | AC4 (unforeseen hook name in the fixture); AC5 (report what was read, refuse an empty roster, AC6 fixture in the `push/release` shape). | plan-review | 41b97c1effdb |
| BG0642 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC3: a stale acknowledgement naming another run refuses\; mutant added testing -n instead of comparing the id; #9 -> AC3: a stale acknowledgement naming another run refuses\; mutant added testing -n instead of comparing the id; #2 -> AC1: the stub records argv and the test asserts --workflow Lint --branch main --event push --status completed\; fixture carries a newer green Dependabot run\; mutant added dropping the workflow filter; #10 -> AC1: the stub records argv and the test asserts --workflow Lint --branch main --event push --status completed\; fixture carries a newer green Dependabot run\; mutant added dropping the workflow filter; #3 -> AC2 added: any conclusion other than success refuses, exercised with timed\_out\; mutant compares == failure; #11 -> AC2 added: any conclusion other than success refuses, exercised with timed\_out\; mutant compares == failure; #4 -> AC1 and AC4: PATH built from the stub directory and git alone; #5 -> AC1 and AC4: PATH built from the stub directory and git alone; #12 -> AC1 and AC4: PATH built from the stub directory and git alone; #6 -> AC4: exact sentence `CI state UNREAD: could not read the latest Lint run on main`, push completes, not a substring of any refusal; #7 -> AC5: --jq prints null after removal\; whoever read the protection back is named; #8 -> Depends on: BG0641 already declared\; the planner sequences them | BLOCKING: AC1 (name the workflow or check, add the newer-green-other-workflow fixture); AC1/AC2 (the rule for non-success non-failure conclusions). | plan-review | 2c19489227b2 |
| BG0641 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #2 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #12 -> AC1-AC3 re-worded: the stub replaces the clone's own gate.py at the invoked path, the test\_commit\_msg\_hook fixture shape; #3 -> AC1: the stub prints a lane name and the test asserts it in the push's stderr; #4 -> AC3 re-worded: tag ref runs release once, branch-only runs push, a deletion row runs no gate, remote-ref column read\; mutant added for deletions; #5 -> AC5 (was AC4): fixture clone carries a post-merge hook the script cannot know, one line per hook required, mutant is hard-code the three names; #13 -> AC5 (was AC4): fixture clone carries a post-merge hook the script cannot know, one line per hook required, mutant is hard-code the three names; #6 -> AC6 accepts both spellings, --boundary <b> and SDLC\_GATE\_BOUNDARY=<b>; #7 -> AC6 accepts both spellings, --boundary <b> and SDLC\_GATE\_BOUNDARY=<b>; #8 -> AC6: the test reads AGENTS.md in-process so relevance selection runs it; #9 -> AC6: the test reads AGENTS.md in-process so relevance selection runs it; #10 -> AC7 mutant added: hard-code the boundary list instead of reading the roster; #14 -> AC7 mutant added: hard-code the boundary list instead of reading the roster; #11 -> AC6/AC7 read the roster from AGENTS.md, never gate.py's BOUNDARIES tuple | AC4 (unforeseen hook name in the fixture); AC5 (report what was read, refuse an empty roster, AC6 fixture in the `push/release` shape). | plan-review | 41b97c1effdb |
| BG0642 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC3: a stale acknowledgement naming another run refuses\; mutant added testing -n instead of comparing the id; #9 -> AC3: a stale acknowledgement naming another run refuses\; mutant added testing -n instead of comparing the id; #2 -> AC1: the stub records argv and the test asserts --workflow Lint --branch main --event push --status completed\; fixture carries a newer green Dependabot run\; mutant added dropping the workflow filter; #10 -> AC1: the stub records argv and the test asserts --workflow Lint --branch main --event push --status completed\; fixture carries a newer green Dependabot run\; mutant added dropping the workflow filter; #3 -> AC2 added: any conclusion other than success refuses, exercised with timed\_out\; mutant compares == failure; #11 -> AC2 added: any conclusion other than success refuses, exercised with timed\_out\; mutant compares == failure; #4 -> AC1 and AC4: PATH built from the stub directory and git alone; #5 -> AC1 and AC4: PATH built from the stub directory and git alone; #12 -> AC1 and AC4: PATH built from the stub directory and git alone; #6 -> AC4: exact sentence `CI state UNREAD: could not read the latest Lint run on main`, push completes, not a substring of any refusal; #7 -> AC5: --jq prints null after removal\; whoever read the protection back is named; #8 -> Depends on: BG0641 already declared\; the planner sequences them | BLOCKING: AC1 (name the workflow or check, add the newer-green-other-workflow fixture); AC1/AC2 (the rule for non-success non-failure conclusions). | plan-review | 2c19489227b2 |
| BG0641 | 2026-09-04 | planning session (Claude) | 2026-09-04 | AC4 (unforeseen hook name in the fixture) -> AC5 (was AC4): the fixture clone carries a post-merge hook the script cannot know and the mutant hard-codes the three names; AC5 (report what was read, refuse an empty roster -> AC6 requires the checker to report both boundaries read from the push/release shape\; AC7 refuses an empty roster and its fixture uses that shape; points re-sized to 5 -> Points re-sized 3 to 5 | AC1/AC2/AC3 - "the gate stubbed on PATH": every hook invokes `python3 "$skill/gate.py"` by path (pre-commit:348, commit-msg:426) and the established fixture (test\\_commit\\_msg\\_hook.py `\\_fixture`) places stubs AT the invoked path inside the fixture repo; a PATH stub only works if pre-push does PATH lookup for `gate.py`, which would make the real hook run whatever `gate.py` is first on PATH; reword to a stub at the invoked path in the fixture clone or an explicit env override the hook honours; AC4 - the mutant "keep the two-hook sentence" is not the careless edit; the careless edit is a hard-coded THREE-hook sentence, which passes a test checking only the three real names; run enable-hooks.sh in a clone whose `.githooks/` carries a hook name the script cannot know (`post-merge`) and assert it is named; AC5 - vacuous-roster hole (L-0390): AGENTS.md's shape is one code span `gate.py --boundary push/release`; a reader matching `--boundary (\w+)` finds `push` only and one finding nothing exits 0 with nothing checked; AC5 must assert the checker REPORTS the boundaries it read (both push and release) and refuses a roster it read none from, and AC6's fixture must use the `push/release` shape; AC5 - missing row: a boundary named in a hook COMMENT but not an invocation must not count (hookutil.py already reads invocation lines only for tools); AC3 - pre-push stdin rows with a zero local sha are deletions; `git push origin :refs/tags/x` should not run the release gate; name the rule.; BLOCKING: AC1 wording (stub mechanism matches how the hook invokes gate.py) | plan-review | 41b97c1effdb |
| BG0642 | 2026-09-04 | planning session (Claude) | 2026-09-04 | BLOCKING: AC1 (name the workflow or check, add the newer-green -> AC1 names the push-triggered Lint workflow and the fixture carries a newer green Dependabot run; AC1/AC2 (the rule for non-success non-failure conclusions) -> AC2 added: any conclusion other than success refuses, exercised with timed\_out; AC3 - approved: reporting UNREAD by name and never green -> no change: kept as approved, now AC4; POINTS - 2 is tight for a `gh`-reading hook branch -> Points re-sized 2 to 3; 3 is honest -> Points re-sized 2 to 3; BLOCKING: AC1 oracle narrowed to the push-triggered -> AC1 reads the push-triggered Lint run on main only, asserted through the stub's recorded argv; AC1/AC2 define the repeated-push and in-progress-newer-run -> AC3: the acknowledged id is remembered in .local/push-ack.json so later pushes over the same red run proceed\; an in-progress newer run is not read and the criterion says the remembered acknowledgement is the route; AC1 refusal prints the acknowledgement route -> AC1: the refusal prints the acknowledgement command verbatim; AC4 restated as the operator's ruling with the PR-flow -> AC5 restated as the operator's ruling at sign-off with the PR-flow alternative named and the edit after the ruling | AC1 - "the most recent completed CI run for main" is underdetermined and the executed evidence shows it matters: on 2026-09-01 two Dependabot workflow runs (33467277040, 33467277114, `success`, headSha 4ae0333e, branch main) sat between two red Lint runs, so a hook reading the latest completed run on main read green over a red Lint; name the workflow (`Lint`) or check (`ci`) the hook reads, and the stub `gh` must answer a newer green non-Lint run above the red Lint one so the mutant "drop the workflow filter" dies; AC1/AC2 - the enumerated conclusions `failure` and `success` exempt `cancelled`, `timed\\_out`, `startup\\_failure`; `== "failure"` and `!= "success"` disagree on every one and both pass the plan; state the rule (anything not `success` refuses) and add one row with `timed\\_out`; AC3 - "UNREAD" must not be a substring of the refusal text (the summary already uses "unread" for a red run); assert the exact unread sentence, exit 0, and the absence of the refusal's run-id line; AC2 - "the only route past a red main" is false by construction (`git push --no-verify`); say "the only route the hook offers"; ordering - read CI before `gate.py --boundary push` and have the AC1 test assert the stub gate was NOT invoked when the push is refused for red CI; AC4 - manual is acceptable for a forge setting and `decisions.py add` exists; record who read the protection back. | plan-review | 2c19489227b2 |
| BG0642 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1 names the stub directory's contents: the gh stub plus symlinks to git, bash and python3, and nothing else; #7 -> AC1 names the stub directory's contents: the gh stub plus symlinks to git, bash and python3, and nothing else; #8 -> AC1 names the stub directory's contents: the gh stub plus symlinks to git, bash and python3, and nothing else; #2 -> AC3 exercises the file with the variable unset for the remembered and the stale case, and adds the blanket-file mutant; #9 -> AC3 exercises the file with the variable unset for the remembered and the stale case, and adds the blanket-file mutant; #3 -> AC3: the hook creates .local if absent; #4 -> AC3: the hook creates .local if absent; #5 -> AC1 names the --json fields databaseId,conclusion,url; #6 -> AC1 names the --json fields databaseId,conclusion,url | ordering - read CI before `gate.py --boundary push` and have the AC1 test assert the stub gate was NOT invoked when the push is refused for red CI; AC4 - manual is acceptable for a forge setting and `decisions.py add` exists; record who read the protection back.; BLOCKING: AC1 (name the workflow or check, add the newer-green-other-workflow fixture); AC1/AC2 (the rule for non-success non-failure conclusions). | plan-review | 2c19489227b2 |
| BG0644 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC3 re-worded to contributes zero, exercised with the module selected alone and fixture numbers stated; #8 -> AC3 re-worded to contributes zero, exercised with the module selected alone and fixture numbers stated; #2 -> AC5 row added moving the invocation after the suite\; asserted by no Ran-N-tests line before the refusal; #3 -> AC5 row added moving the invocation after the suite\; asserted by no Ran-N-tests line before the refusal; #4 -> AC5: a named but missing or unreadable budget file refuses loudly\; the script no longer passes the scalar\; fallback mutant added; #5 -> AC5: a named but missing or unreadable budget file refuses loudly\; the script no longer passes the scalar\; fallback mutant added; #6 -> no change: AC2 kept; #7 -> sdlc-studio/tsd.md added to Affects | AC2 - state how 106 is apportioned: modules measured alone print import-time leaks once each while discovery prints them once, so the JSON may not sum to what the full run prints and AC2 refuses on day one; cross-unit - nothing else pins `TEST\\_NOISE\\_BASELINE` (grep), fine.; BLOCKING: AC1 (per-module versus sum-over-selection decided, with the masked-module fixture if per-module); AC1 (budget-file override for the fixture); AC4 (a caller in the commit path, pinned over the real tree). | plan-review | 189430ff8f6c |
| BG0640 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1 names test\_a\_reported\_unit\_is\_not\_counted\_as\_examined as the test the fix reddens and its re-authoring under its own name; #2 -> AC1 names test\_a\_reported\_unit\_is\_not\_counted\_as\_examined as the test the fix reddens and its re-authoring under its own name; #6 -> AC1 names test\_a\_reported\_unit\_is\_not\_counted\_as\_examined as the test the fix reddens and its re-authoring under its own name; #3 -> AC1 says test\_a\_unit\_the\_lane\_cannot\_examine\_is\_not\_reported\_as\_a\_clean\_pass survives; #4 -> AC1 says test\_a\_unit\_the\_lane\_cannot\_examine\_is\_not\_reported\_as\_a\_clean\_pass survives; #5 -> AC1 requires the test's own fixture root | points 2 - fair; BLOCKING: AC1 extended to the crashed-with-zero-examined rendering | plan-review | 0041b467807d |
| BG0643 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1 files a second selector whose method resembles one in another class and states that shape's verdict, so the file-wide-first row has a fixture that reaches it; #2 -> AC1 files a second selector whose method resembles one in another class and states that shape's verdict, so the file-wide-first row has a fixture that reaches it; #8 -> AC1 files a second selector whose method resembles one in another class and states that shape's verdict, so the file-wide-first row has a fixture that reaches it; #3 -> AC2 row added: check\_verify\_selectors refuses but skips the near-miss hint; #4 -> AC2 row added: check\_verify\_selectors refuses but skips the near-miss hint; #5 -> AC3 states the same-leaf branch is unchanged and test\_the\_refusal\_names\_the\_near\_miss survives; #6 -> AC3 states the same-leaf branch is unchanged and test\_the\_refusal\_names\_the\_near\_miss survives; #7 -> AC1 names --root <fixture> for both commands | BG0570 - AC1's Given ("a node absent from a file that EXISTS ... REFUSE") becomes false while its `Verified: yes (2026-08-11)` stamp stands; the revision row must re-word AC1, not only note the narrowing.; BLOCKING: AC3 (cross-class close-match fixture and the unscoped-`selector\\_near\\_miss` mutant); AC1 (name the report fields asserted; add a byte-exact mutant); BG0570 AC1 text revised in the same commit. | plan-review | 267e53efd0e8 |
| BG0603 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> the fixture seeds sdlc-studio/.verify-lint-baseline.json as an empty groups object, stated in AC1 and AC3; #2 -> the fixture seeds sdlc-studio/.verify-lint-baseline.json as an empty groups object, stated in AC1 and AC3; #6 -> the fixture seeds sdlc-studio/.verify-lint-baseline.json as an empty groups object, stated in AC1 and AC3; #7 -> the fixture seeds sdlc-studio/.verify-lint-baseline.json as an empty groups object, stated in AC1 and AC3; #3 -> AC1 requires every fixture selector distinct; #4 -> AC1 asserts the REFUSED line naming each criterion, not the exit code; #5 -> AC1 names is\_terminal\_status over TERMINAL\_STATUS with the type from the id prefix, and says stories are covered too | BLOCKING: AC1 (name In Progress in or out of the set; state the markdown-evidence widening). | plan-review | ff4004992e0e |
| BG0603 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> the fixture seeds sdlc-studio/.verify-lint-baseline.json as an empty groups object, stated in AC1 and AC3; #2 -> the fixture seeds sdlc-studio/.verify-lint-baseline.json as an empty groups object, stated in AC1 and AC3; #3 -> the fixture seeds sdlc-studio/.verify-lint-baseline.json as an empty groups object, stated in AC1 and AC3; #6 -> the fixture seeds sdlc-studio/.verify-lint-baseline.json as an empty groups object, stated in AC1 and AC3; #4 -> AC1 names is\_terminal\_status over TERMINAL\_STATUS with the type from the id prefix, and says stories are covered too; #5 -> AC1 names is\_terminal\_status over TERMINAL\_STATUS with the type from the id prefix, and says stories are covered too | AC2/AC3 rows are sound.; BLOCKING: AC1 (name In Progress in or out of the set; state the markdown-evidence widening). | plan-review | ff4004992e0e |
| BG0641 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC3 states one literal invocation per boundary so AC6 finds a literal line for each; #2 -> AC3 states one literal invocation per boundary so AC6 finds a literal line for each; #13 -> AC3 states one literal invocation per boundary so AC6 finds a literal line for each; #3 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #4 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #14 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #5 -> AC3 row added: run the gate once per ref; #6 -> AC3 row added: run the gate once per ref; #7 -> AC5 names the second comment line as the source, gives the post-merge fixture one, prints (no description) otherwise, and adds a headerless mutant; #8 -> AC5 names the second comment line as the source, gives the post-merge fixture one, prints (no description) otherwise, and adds a headerless mutant; #9 -> AC6 adds an env-spelled fixture hook and a mutant recognising only --boundary; #10 -> roster tests moved to tools/tests/test\_boundary\_roster.py, attributed by name\; the delivery runs test\_census before committing; #11 -> AC4 names tools/gate\_timing.py; #12 -> AC4 names tools/gate\_timing.py | AC4 (unforeseen hook name in the fixture); AC5 (report what was read, refuse an empty roster, AC6 fixture in the `push/release` shape). | plan-review | 41b97c1effdb |
| BG0641 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #2 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #3 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #9 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #6 -> AC5 names the second comment line as the source, gives the post-merge fixture one, prints (no description) otherwise, and adds a headerless mutant; #5 -> AC6 adds an env-spelled fixture hook and a mutant recognising only --boundary; #1 -> AC1 requires a real directory holding the stub\; the commit-msg citation is gone; #7 -> AC1 requires a real directory holding the stub\; the commit-msg citation is gone; #8 -> AC1 requires a real directory holding the stub\; the commit-msg citation is gone; #4 -> AC7 adds the comment-only fixture and the read-comments row; #10 -> AC7 adds the comment-only fixture and the read-comments row | AC3 - pre-push stdin rows with a zero local sha are deletions; `git push origin :refs/tags/x` should not run the release gate; name the rule.; BLOCKING: AC1 wording (stub mechanism matches how the hook invokes gate.py); AC4 (unforeseen hook name in the fixture); AC5 (report what was read, refuse an empty roster, AC6 fixture in the `push/release` shape). | plan-review | 41b97c1effdb |
| BG0641 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #2 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #13 -> AC4 names tools/gate\_timing.py, the boundary-<push/release> suite key, the no-history floor, the hook recording its own series, both fixtures, and the order assertion; #8 -> AC5 names the second comment line as the source, gives the post-merge fixture one, prints (no description) otherwise, and adds a headerless mutant; #9 -> AC5 names the second comment line as the source, gives the post-merge fixture one, prints (no description) otherwise, and adds a headerless mutant; #3 -> AC8 says the cost is the floor until a boundary run is recorded, then the recorded figure; #4 -> AC8 says the cost is the floor until a boundary run is recorded, then the recorded figure; #5 -> AC1 names --no-verify only in the gate refusal\; BG0642 AC3 never names it\; AC8 updates the bypass sentence; #6 -> AC1 names --no-verify only in the gate refusal\; BG0642 AC3 never names it\; AC8 updates the bypass sentence; #7 -> AC1 names --no-verify only in the gate refusal\; BG0642 AC3 never names it\; AC8 updates the bypass sentence; #10 -> AC3 says release binds the same two lanes so once is enough; #11 -> AC3 says release binds the same two lanes so once is enough; #12 -> points kept at 5; #14 -> AC9 added: the operator's ruling recorded as a decision at plan sign-off\; decisions.md in Affects\; the goal names it | AC4 (unforeseen hook name in the fixture); AC5 (report what was read, refuse an empty roster, AC6 fixture in the `push/release` shape). | plan-review | 41b97c1effdb |
| BG0643 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC1 files a second selector whose method resembles one in another class and states that shape's verdict, so the file-wide-first row has a fixture that reaches it; #7 -> AC1 files a second selector whose method resembles one in another class and states that shape's verdict, so the file-wide-first row has a fixture that reaches it; #2 -> AC5 drops test\_one\_reader\_answers\_which\_file\_a\_selector\_targets\; three tests re-authored; #3 -> AC5 drops test\_one\_reader\_answers\_which\_file\_a\_selector\_targets\; three tests re-authored; #8 -> AC5 drops test\_one\_reader\_answers\_which\_file\_a\_selector\_targets\; three tests re-authored; #4 -> AC5 says the revision rows are checked by the delivery review reading them back; #5 -> AC5 says the revision rows are checked by the delivery review reading them back; #6 -> no change: AC2 kept as approved | BG0570 - AC1's Given ("a node absent from a file that EXISTS ... REFUSE") becomes false while its `Verified: yes (2026-08-11)` stamp stands; the revision row must re-word AC1, not only note the narrowing.; BLOCKING: AC3 (cross-class close-match fixture and the unscoped-`selector\\_near\\_miss` mutant); AC1 (name the report fields asserted; add a byte-exact mutant); BG0570 AC1 text revised in the same commit. | plan-review | 267e53efd0e8 |
| BG0642 | 2026-09-04 | planning session (Claude) | 2026-09-04 | #1 -> AC3 adds the step: variable set to another run's id with no push-ack.json present, asserted refused with nothing recorded, so the -n mutant dies; #5 -> same step added under AC3; #2 -> AC1 states the hook is written against bash builtins, git, python3 and gh only, .local created through python3, no coreutils; #3 -> AC1 names the literal SDLC\_PUSH\_ACK\_RED=<run id> git push; #4 -> AC4: the gate stub exits 0 in the two completing steps and the test asserts the remote advanced | AC3 - "UNREAD" must not be a substring of the refusal text (the summary already uses "unread" for a red run); assert the exact unread sentence, exit 0, and the absence of the refusal's run-id line; AC2 - "the only route past a red main" is false by construction (`git push --no-verify`); say "the only route the hook offers"; ordering - read CI before `gate.py --boundary push` and have the AC1 test assert the stub gate was NOT invoked when the push is refused for red CI; AC4 - manual is acceptable for a forge setting and `decisions.py add` exists; record who read the protection back.; BLOCKING: AC1 (name the workflow or check, add the newer-green-other-workflow fixture); AC1/AC2 (the rule for non-success non-failure conclusions). | plan-review | 2c19489227b2 |
| BG0643 | 2026-09-04 | delivery session (Claude) | 2026-09-04 | #1 -> AC6 added: \_classify\_selector reads the node address through the one target parser and refuses a target without a node\; pinned by test\_a\_pattern\_a\_bare\_file\_and\_a\_node\_below\_a\_leaf\_are\_still\_refused, mutant registered killed; #2 -> selector\_near\_miss names the test a below-a-leaf node hangs off, so it refuses with a hint\; AC6 covers it with its own mutant, killed; #3 -> comment added on the second half: the refusal comes from the unpatched near-miss reader and the helper claim is carried by the first half; #4 -> comment in verify\_ac.py and the fragment corrected to all three; #5 -> two blank lines before the class; #6 -> pre-existing: CR0511 carries it, no change in this unit; #7 -> pre-existing: filed into CR0511 (the .pytest\_cache row); #8 -> same repair as #1: the base verdict is restored for targets without a node, with AC6 and its test | - | delivery | 7732c59c3250 |
| BG0643 | 2026-09-04 | delivery session (Claude) | 2026-09-04 | #1 -> AC6 added with the -k, bare-file and below-a-leaf shapes refused and a matching -k control accepted\; mutants registered killed; #2 -> both tests assert the backticked hint did you mean `test\_alpha\_thing`, which the echoed selector cannot satisfy\; NearMissScopingTests also pins the below-a-leaf hint; #3 -> the artifact.py new --verify accept control added under AC1 against the real tree with --dry-run; #4 -> AC1 asserts verify-report.json under the fixture root, beside the repo-report mtime check; #5 -> pre-existing: CR0511; #6 -> corrected to three in the comment and the fragment; #7 -> decided: the base verdict is restored for targets without a node\; AC6 licenses nothing new and pins the refusal | - | delivery | 7732c59c3250 |
| BG0643 | 2026-09-04 | delivery session (Claude) | 2026-09-04 | #1 -> AC6 moved into the Acceptance Criteria section with a revision row\; critic.py brief now renders six criteria and verify\_ac run reports ac=6; #5 -> same repair: AC6 is in the section every reader consumes; #2 -> filed as BG0648: two readers disagree about a criterion outside its section and nothing refuses the shape; #3 -> pre-existing and unchanged from the base ref: neither shape had a hint before this unit\; carried, not this unit's; #4 -> filed into CR0511: the filer's markdown-safing splits a Class::method token in prose | plan-note count: the file-wide reader refuses all three of BG0603's selectors at HEAD, not two - documentary only; the `-k` / bare-file widening (decide it: restore the base verdict for targets without `::` in `\\_classify\\_selector`, or add a criterion and a test that licenses it), [new] the AC3 and NearMissScopingTests near-miss-name assertions that cannot fail (assert the backticked hint) | delivery | 7732c59c3250 |
| BG0603 | 2026-09-04 | delivery session (Claude) | 2026-09-04 | #1 -> AC1's parenthetical and the fragment now say the criteria are linted and refused regardless and the ratchet returns 1 afterwards, so only the exit-0 control needs the seeded baseline; #6 -> same correction, both places; #2 -> AC1's rationale, the plan rows and the test docstrings re-worded to the new code: an enumerated set reintroduced in \_still\_authoring\; the registered mutants were applied in that shape; #3 -> pre-existing: filed into CR0511 (summary line counts stacked refusals as markdown-only on a story); #4 -> pre-existing: filed into CR0511 (pre-commit lane label and fix hint); #5 -> pre-existing: filed into CR0511 (help and reference do not document the refusals) | - | delivery | 34840d2dcca0 |
| BG0644 | 2026-09-04 | delivery session (Claude) | 2026-09-04 | #1 -> tools/test-noise-baseline.json records test\_config at 1, its state-independent figure\; the file carries a \_note naming BG0647 and the \_total-versus-sum relationship; #2 -> the fragment, the script comment, AC2 and the budget\_for docstring now say a module measured alone prints an import-time leak once (a couple of lines) and that test\_config is held at its state-independent figure because it prints this clone's state (BG0647)\; BG0647 is in the batch and lands first | - | delivery | a77aa5ff11d0 |
| BG0644 | 2026-09-04 | delivery session (Claude) | 2026-09-04 | #1 -> same: entry recorded at 1 with the reason in the file's note; #2 -> all four surfaces corrected to what was measured\; the remaining import-time share is stated as at most a couple of lines | - | delivery | a77aa5ff11d0 |
| BG0647 | 2026-09-04 | delivery session (Claude) | 2026-09-04 | #1 -> AC1 row 2 replaced by a status.py mutant (schema\_version None), executed killed and registered\; the equivalent row retracted with its reason; #2 -> same: the premise was false and the row is gone; #3 -> AC2 row replaced by a status.py mutant that prints during gather, executed killed; #4 -> AC3 added: a fixture whose .config.yaml cannot be honoured makes the gather warn, the warning is asserted in the captured text, with the remove-the-redirect row killed; #5 -> AC1 asserts data['run'] is None (this clone has a run open) and reads an override of 3 back, both timing-independent; #6 -> same pin; #7 -> the fragment says the module's own entry stays at one line, a pre-existing warning from another class, so its count is state-independent rather than zero; #8 -> advisory noted: the other real-tree readers gather no status\; no change; #9 -> AC1 row 2 replaced, the two-directory wording corrected (the directories are inert); #10 -> AC2 row replaced by the print mutant and AC3 carries the noisy-fixture capture test | - | plan-review | 459cee528e3d |
| BG0647 | 2026-09-04 | delivery session (Claude) | 2026-09-04 | #1 -> AC1 row 2 replaced by the schema\_version-None status.py mutant, executed killed; #2 -> AC2 row replaced by the print mutant, executed killed; #3 -> AC2 now has the status.py print row\; AC3 adds the capture control; #4 -> AC1 reads an override of 3 back from a fixture that carries one, so the branch is distinct; #5 -> AC1 wording corrected: the directories are inert; #6 -> pre-existing: LoadTests reading this repository through mod.get('.') filed into CR0511\; the budget note and script comment now speak of BG0647 as fixed; #7 -> AC1 row 2 replaced as asked; #8 -> AC2 row replaced as asked, the capture row kept under AC3 with an assertion it can redden | - | plan-review | 459cee528e3d |
| BG0647 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #1 -> test\_a\_noisy\_fixture\_is\_captured\_and\_never\_reaches\_the\_console asserts "could not load" and "was not applied" separately in the captured text - the seat mutant (config.py loader warning routed to sys.\_\_stderr\_\_) is now an AC3 plan row, executed and killed alongside the other four re-measured in one pass, and the module alone still prints its one pre-existing line; #2 -> the test pops sys.modules["config"] before \_status() so the loader module is fresh and warns whatever ran earlier, both warnings captured in every order | AC1 names test\\_status\\_reads\\_config as the "when" but that test's own gather target is unpinned - reverting test\\_config.py:93 to status.gather(Path(".")) keeps all three Verify selectors plus that test green while the class takes 71s over this corpus (mutant M5), the sub-two-second and no-run pins live only in the sibling test at :97-:118; the one line test\\_config.py still leaks under the noise counter comes from GracefulDegradeTests.test\\_get\\_returns\\_default\\_when\\_yaml\\_unavailable (test\\_config.py:168) printing config.py:85 outside any redirect - present at base ref 1d3a4d95, held at tools/test-noise-baseline.json test\\_config: 1, no open Bug records it (BG0160 is Fixed); AC3's test does not pin the silent half of its criterion - config.py:85's warning routed past the redirect leaks to the console with the selector green (mutant M3 survived under the unittest runner with 1 counted line) - fix: in test\\_a\\_noisy\\_fixture\\_is\\_captured\\_and\\_never\\_reaches\\_the\\_console pop sys.modules["config"] before \\_status() so the fixture emits both warnings deterministically, then assert "was not applied" AND "could not load" are both in the captured text (one assertion per warning site), which kills M3 and removes the order dependence | delivery | 527719e0d988 |
| BG0641 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #1 -> both gate invocations send stdout to stderr (1>&2) with the reason commented, the AC1 test reads r.stderr alone and asserts the lane name, the re-run and the refusal block own bypass line after the lane, plan row added and the leave-stdout mutant killed; #2 -> AC3 pushes main:refs/tags/v0.0.3 and asserts release, plan row added and the local-ref mutant killed; #3 -> \_Clone.hook\_direct hands the hook a tag row followed by a branch row and asserts one release call, plan row added and the demote mutant killed; #4 -> a fixture with the suite series and no boundary series asserts "expect at least the full suite (~62" before the lane name with the bypass, plan row added and the disabled-branch mutant killed; #5 -> the no-history fixture asserts "release-rehearsal and revert-check", plan row added and the drop-names mutant killed; #6 -> AC1 asserts "bypass once (emergency only): git push --no-verify" in the text after the lane name, plan row added and the drop-line mutant killed; #7 -> the AC6 env-spelling fixture carries "echo --boundary deploy" and asserts it binds nothing, plan row added and the drop-anchor mutant killed; #8 -> AGENTS.md row now says the floor is the full suite own recorded figure until a boundary run is recorded, else about ten minutes; #9 -> AC4 now names gate\_timing.py estimate --suite skill-tests as the reader | - | delivery | ead6293ce8b1 |
| BG0641 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #1 -> the enable-hooks fixture runs the script under an environment with every REPO\_LOCATING variable removed, the same scrub as the module \_git helper; #2 -> a fixture with the suite series and no boundary series asserts the "expect at least the full suite" floor before the lane name, plan row added and mutant X3 killed; #3 -> AGENTS.md row states the floor as the full suite own recorded figure until a boundary run is recorded, else about ten minutes; #4 -> AC3 pushes main:refs/tags/v0.0.3 and asserts the release boundary, plan row added and mutant X1 killed | the enable-hooks fixture runs `bash tools/enable-hooks.sh` with the caller's environment inherited unscrubbed (tools/tests/test\\_pre\\_push\\_hook.py:212) and under a polluted GIT\\_DIR/GIT\\_WORK\\_TREE/GIT\\_INDEX\\_FILE writes `core.hooksPath` into a foreign repository's config | delivery | ead6293ce8b1 |
| BG0641 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #1 -> AC3 pushes a tag with the stub gate red and asserts the refusal, the lane name and the release re-run on stderr, and that the remote gained no tag, plan row added and the rc=0 mutant killed; #2 -> the same fixture reads stderr alone, so dropping the release invocation 1>&2 fails it, plan row added and the mutant killed | AGENTS.md:44 says "the floor is about ten minutes until a run is recorded" but the hook's floor with no boundary run is the recorded skill-tests median (probed: `expect at least the full suite (~624s ...)`), the literal being reached only with no timings file at all - the number is right by coincidence and the stated mechanism is not; AC3's "reads the REMOTE ref column" clause is unpinned - mutant X1 (`case "$\\_local\\_ref"`, .githooks/pre-push:24) survives `test\\_a\\_tag\\_push\\_runs\\_the\\_release\\_boundary\\_and\\_a\\_deletion\\_runs\\_none` because every fixture pushes a ref whose local and remote names agree, and probed `git push origin main:refs/tags/v2` runs `--boundary push` under X1 against `release` under the shipped hook; the enable-hooks fixture runs `bash tools/enable-hooks.sh` with the caller's environment inherited unscrubbed (tools/tests/test\\_pre\\_push\\_hook.py:212) and under a polluted GIT\\_DIR/GIT\\_WORK\\_TREE/GIT\\_INDEX\\_FILE writes `core.hooksPath` into a foreign repository's config | delivery | ead6293ce8b1 |
| BG0642 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #1 -> AGENTS.md now says the required check is the operator to remove under the ruling and stays owed until the read-back shows none, and the sign-off names it as owed with the live read (ci still required); #2 -> hook\_direct builds its PATH from stub\_path like every push, so no fixture can reach the live forge; #3 -> the parser prints UNREAD for a run with no databaseId, pinned by a noid stub answer in AC3 and AC4; #4 -> the record write is tested and a failure refuses with "could not be written", pinned by a directory at the ack path; #5 -> filed Low into CR0511 (no timeout binary on the four-tool PATH, a python3-bounded read is the fix); #6 -> AC2 exercises timed\_out AND cancelled, plan row added and the enumerated-list mutant killed; #7 -> AC3 starts from a clone with no .local so the hook creates it, plan row added and the makedirs mutant killed; #8 -> AC1 pushes a tag from a topic checkout and asserts --branch main, plan row added and the pushed-ref mutant killed; #9 -> AC4 answers non-JSON at rc 0 and asserts UNREAD, plan row re-pointed at the parser fallback and killed; #10 -> covered by AC3 starting with no .local, where an added mkdir -p exits 127 on the four-tool PATH; #11 -> the header now says every GATE refusal names the bypass and the red-CI refusal deliberately does not | - | delivery | ad0e6806b702 |
| BG0642 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #1 -> hook\_direct runs under the stub PATH; #2 -> AC1 and AC3 assert --no-verify is absent from every red-CI refusal, plan rows added for the appended bypass line and the moved CI read, both killed; #3 -> \_git drops SDLC\_PUSH\_ACK\_RED from the inherited environment, so only a test that sets it can; #4 -> the wrong ids are 1, a prefix and a superstring, plan row added and the honour-1 mutant killed; #5 -> AC3 starts from a clone with no .local, plan row added and the makedirs mutant killed; #6 -> AC1 pushes a tag from a topic checkout and asserts --branch main, plan row added and mutant killed; #7 -> the header now distinguishes gate refusals from the red-CI refusal; #8 -> the sign-off names the forge edit as owed to the operator with the live read-back | hook\\_direct runs BG0641's older fixture under the caller's PATH and reaches whatever gh it holds (tools/tests/test\\_pre\\_push\\_hook.py:149-152); AC3's never-names-no-verify clause has no assertion and mutants M7 and M8 survive (tools/tests/test\\_pre\\_push\\_hook.py:305-314 and :336-354) | delivery | ad0e6806b702 |
| BG0641 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #3 -> filed as BG0650 and landed at 835c48f9: the census reads the deriver total; #4 -> the disclosure page and the v5.0.1 count are regenerated once after the last transition, green at 14a9d4d6; #5 -> reported, not this unit to close: CR0552 owns the lane and stays In Progress, named at sign-off; #6 -> closed by BG0642 landing at 14a9d4d6, every push fixture and the direct-stdin helper run under the stub PATH; #7 -> 30 rows registered after the last edit, re-registered after BG0642 re-wrote the hook, 29 killed and AC9 ruled equivalent; #8 -> the bare dunder on BG0647 line 53 is backticked at 652e451b | the enable-hooks fixture runs `bash tools/enable-hooks.sh` with the caller's environment inherited unscrubbed (tools/tests/test\\_pre\\_push\\_hook.py:212) while every other call in the module goes through the scrubbed `\\_git` helper - under GIT\\_DIR/GIT\\_WORK\\_TREE/GIT\\_INDEX\\_FILE pointed at a decoy repository the test fails AND writes `core.hooksPath=.githooks` into the decoy's config (probed: decoy `unset` before, `.githooks` after), the BG0222 shape in a module that declares `\\_GIT\\_ENV\\_VARS` for exactly this reason; AC4's middle floor is unpinned - mutant X3 (skill-tests fallback branch disabled, .githooks/pre-push:37) survives `test\\_the\\_hook\\_announces\\_its\\_cost\\_and\\_the\\_bypass\\_before\\_running` because no fixture holds a `skill-tests` series without a boundary series and asserts the "expect at least the full suite" text, and this is the branch the FIRST real push takes since the live gate-timings.json carries a ten-sample skill-tests series (tools/tests/test\\_pre\\_push\\_hook.py:189 only covers the no-file literal) | delivery | ead6293ce8b1 |
| BG0642 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #12 -> AGENTS.md now states the forge edit as the operator to make under D0181, owed until the read-back shows none, and the sign-off names it with the live read (ci still required); #13 -> the direct-stdin helper runs under the stub PATH, landed at 14a9d4d6, a tracer gh on the outer PATH saw zero calls in round two | hook\\_direct runs the hook under the caller's PATH so BG0641's older fixture now invokes whatever gh the outer PATH holds - executed with a recorder gh prefixed to PATH, one `run list --workflow Lint --branch main --event push --status completed --limit 1 --json databaseId,conclusion,url` call was logged from test\\_a\\_tag\\_push\\_runs\\_the\\_release\\_boundary\\_and\\_a\\_deletion\\_runs\\_none, so in the real suite that is the real gh and AC1's "the live forge is unreachable" is false for this fixture (tools/tests/test\\_pre\\_push\\_hook.py:149-152 and :232); AC3's clause "this refusal never names git push --no-verify" has no assertion - mutant M8 (append a bypass line to the refusal) and mutant M7 (move the whole CI read below the cost announcement, whose line prints "Bypass once with: git push --no-verify") both survive the full class 4 passed, and under M7 the pusher's refusal output contains "git push --no-verify" (tools/tests/test\\_pre\\_push\\_hook.py:305-314 and :336-354, .githooks/pre-push:99); an outer SDLC\\_PUSH\\_ACK\\_RED leaks into the fixtures because \\_git builds env from os.environ and scrubs only \\_GIT\\_ENV\\_VARS while push sets the variable only when ack is given - executed with SDLC\\_PUSH\\_ACK\\_RED=33731466380 in the outer environment, 3 of 4 tests fail (tools/tests/test\\_pre\\_push\\_hook.py:67-73 and :130-133); AC3's "a profile-exported SDLC\\_PUSH\\_ACK\\_RED=1 cannot bypass every red main" is exercised with 99999 not 1, so a substring match, a prefix match, a substring match on the file's id and an explicit `or = "1"` all survive - the real hook refuses 1 and the prefix 3373146638, the test cannot tell (tools/tests/test\\_pre\\_push\\_hook.py:336); AC3's "the hook creates .local/ if absent" is unpinned - dropping os.makedirs survives because \\_Clone pre-creates sdlc-studio/.local with gate-timings.json, the positive control with no\\_timings\\_file=True shows the real hook does create it (tools/tests/test\\_pre\\_push\\_hook.py:98-105 and :334, .githooks/pre-push:72); AC1's "the read is of main whatever ref is pushed" is unpinned - `--branch "$(git rev-parse --abbrev-ref HEAD)"` survives because every test pushes main from main, a tag push on the real hook does read main (tools/tests/test\\_pre\\_push\\_hook.py:305, .githooks/pre-push:39); the hook's header still claims the bypass "is named in every refusal this hook makes" while the CI refusal deliberately never names it (.githooks/pre-push:13-14); AC5's manual row is only partly met at delivery - D0181 is committed and names the PR-flow alternative and why it was declined but names nobody who read the protection back, and I did not run gh api so required\\_status\\_checks null is unverified by this seat (sdlc-studio/decisions.md:195); hook\\_direct runs BG0641's older fixture under the caller's PATH and reaches whatever gh it holds (tools/tests/test\\_pre\\_push\\_hook.py:149-152); AC3's never-names-no-verify clause has no assertion and mutants M7 and M8 survive (tools/tests/test\\_pre\\_push\\_hook.py:305-314 and :336-354) | delivery | ad0e6806b702 |
| BG0647 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | #3 -> declined as a repair: the fixture target is pinned by AC1 own test (run is None, under two seconds) over the same helper, and the US0015 test asserts config consumption only; #4 -> pre-existing at the base ref, held at 1 in tools/test-noise-baseline.json and disclosed in the fragment; #5 -> closed at 652e451b: the loader is re-imported and both warning sites are asserted separately, the seat mutant is a plan row and killed, engineering round two APPROVE | AC3's test pins "captured" for only one of the two warnings the noisy fixture makes the gather emit and pins "silent" for neither - the assertIn at test\\_config.py:142 matches sdlc\\_md.py:1509 ("exists but was not applied") while config.py:85 ("could not load .config.yaml") is unasserted, so routing that second warning past the redirect (mutant M3, file=sys.\\_\\_stderr\\_\\_) leaks one line to the unittest runner's console with all four IntegrationTests green - the criterion's "nothing reaches the runner's console" half has no assertion behind it; which warnings AC3's gather emits depends on test order - config.\\_DEGRADE\\_WARNED (config.py:84) is a per-module-instance flag and IntegrationTests reuses whatever sys.modules["config"] the earlier GracefulDegradeTests left behind, so in the whole-module run the captured text holds one warning and in a selector-only run two - harmless today because captured, but it is why the test cannot assert the config.py warning without popping the cached module first | delivery | 527719e0d988 |
| BG0641 | 2026-09-04 | delivery session (Claude, author of the diff) | 2026-09-04 | CR0552 (In Progress) revert-check mutates the live working tree, and this hook now -> reported, not this unit to close: CR0552 owns the lane and stays In Progress, named at sign-off; BG0642 (Open) AC1's "every push fixture also runs under BG0642's stub PATH once its CI read lands" is a forward reference - no fixture in this -> closed by BG0642 landing at 14a9d4d6, every push fixture and the direct-stdin helper run under the stub PATH; the artefact sdlc-studio/bugs/BG0641-there-is-no-pre-push-hook-so-the.md carries no -> the field was added (functional) and derived: 30 plan rows, 29 killed, 1 ruled equivalent; a concurrent unit's edit to -> BG0647 own follow-up commit 652e451b, outside this unit, no action here; AC1's stderr claim is false by execution -> both invocations send the gate stdout to stderr and the AC1 test reads stderr alone, landed at 0da43495 | the enable-hooks fixture runs `bash tools/enable-hooks.sh` with the caller's environment inherited unscrubbed (tools/tests/test\\_pre\\_push\\_hook.py:212) while every other call in the module goes through the scrubbed `\\_git` helper - under GIT\\_DIR/GIT\\_WORK\\_TREE/GIT\\_INDEX\\_FILE pointed at a decoy repository the test fails AND writes `core.hooksPath=.githooks` into the decoy's config (probed: decoy `unset` before, `.githooks` after), the BG0222 shape in a module that declares `\\_GIT\\_ENV\\_VARS` for exactly this reason; AC4's middle floor is unpinned - mutant X3 (skill-tests fallback branch disabled, .githooks/pre-push:37) survives `test\\_the\\_hook\\_announces\\_its\\_cost\\_and\\_the\\_bypass\\_before\\_running` because no fixture holds a `skill-tests` series without a boundary series and asserts the "expect at least the full suite" text, and this is the branch the FIRST real push takes since the live gate-timings.json carries a ten-sample skill-tests series (tools/tests/test\\_pre\\_push\\_hook.py:189 only covers the no-file literal); AGENTS.md:44 says "the floor is about ten minutes until a run is recorded" but the hook's floor with no boundary run is the recorded skill-tests median (probed: `expect at least the full suite (~624s ...)`), the literal being reached only with no timings file at all - the number is right by coincidence and the stated mechanism is not; AC3's "reads the REMOTE ref column" clause is unpinned - mutant X1 (`case "$\\_local\\_ref"`, .githooks/pre-push:24) survives `test\\_a\\_tag\\_push\\_runs\\_the\\_release\\_boundary\\_and\\_a\\_deletion\\_runs\\_none` because every fixture pushes a ref whose local and remote names agree, and probed `git push origin main:refs/tags/v2` runs `--boundary push` under X1 against `release` under the shipped hook; the enable-hooks fixture runs `bash tools/enable-hooks.sh` with the caller's environment inherited unscrubbed (tools/tests/test\\_pre\\_push\\_hook.py:212) and under a polluted GIT\\_DIR/GIT\\_WORK\\_TREE/GIT\\_INDEX\\_FILE writes `core.hooksPath` into a foreign repository's config | delivery | ead6293ce8b1 |
| BG0651 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC1 now names re-applying each row against the staged bytes and mutation.py register as the remedy, with --from-plan named only as the check afterwards; #2 -> AC2 now reads rows whose recorded hash differs from the staged blob, never dropped; #3 -> AC2 carries the row refuse an In Progress unit differing rows as if it were terminal; #4 -> AC1 carries rows for the working-tree compare, the missing remedy, refuse-on-any-row and terminal-by-literal, so a measured entry with a differing hash has a row that fails on each wrong reading; #5 -> the AC1 row name the unit but not the rows or the re-register remedy pins the file, rows and remedy together; #6 -> AC3 states the lane blocking status in the roster as the revert-check row does, pinned by test\_check\_spec\_claims; #7 -> the named remedy is re-apply and register, from-plan is the check; #8 -> re-worded to rows whose recorded hash differs from the staged blob | the status set the lane guards is undefined and `Fixed` alone never guards a story | plan-review | 882a5c6e5f00 |
| BG0645 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC1 requires the fingerprint footer on stderr of the shipped command so record --brief consumes it; #2 -> AC1 requires the unit current Test Plan table in the rejoinder; #3 -> --tier on a rejoinder is unchanged by this unit and reported, not repaired: the rejoinder computes the tier as today; #4 -> the verbatim quote of the prior verdict is pinned by the existing test\_rejoinder\_quotes\_prior\_verdict\_verbatim, unchanged; #5 -> AC2 keeps the drop-the-block-everywhere row as the control; #6 -> AC2 now requires the footer on the delivery rejoinder too, closing the CR0329 gap in the same unit; #7 -> the footer is required by AC1; #8 -> the current plan rows are required by AC1 with a row that renders the rejoinder without the table | - | plan-review | f9f7b43c413e |
| BG0648 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC1 fixtures both shapes, the bullet under Impact and the heading under another section, with a bullet-only detector as a planned mutant; #2 -> US0629 is removed from AC4 and named as a hand-authored plan the grammar never matched; #3 -> the Verified stamp on AC4 is removed and its selector is validate.py check with no invented flag; #4 -> AC2 gives the misplaced block a failing verifier and asserts the run stays green and the report names no such criterion, so a count equality cannot pass it; #5 -> AC3 carries a positive control over a clean artefact that names nothing; #6 -> the widen-the-brief row stays under AC2 as the mutant that makes the readers agree the wrong way; #7 -> fixtured in both shapes; #8 -> the offender list is US0597 (no section, one is created) and BG0270 (AC4 moved), by verify\_ac own parser | - | plan-review | fe7343afbd7b |
| BG0646 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> the fixture is sized to this corpus shape, 2,340 artefacts with 190 uncovered terminal epics with children and 45 discovery items, and AC1 requires the test own control to record today code above the bound; #2 -> the headline is pinned structurally: the Run: line is the first line in the captured output, flushed, before any census-derived line, with the clock as a second bound only; #3 -> the second-walk row now sits under AC2 where the read spy kills it; #4 -> AC2 spans one spy over gather and advisory in one process and requires the advisory to reuse the census, with the children map placed in the census and a read-layer memo refused as a planned mutant; #5 -> AC3 cross-call cache is dropped, no shipped verb makes two gathers; #6 -> the history is named: retros/evidence/actuals-<date>.jsonl and retros/VELOCITY.md, the files close\_owed.owed reads; #7 -> resized as in #1; #8 -> the 3 s bound is no longer the only kill: the structural order pin and the flush row carry the headline rows | - | plan-review | 699921540e76 |
| BG0651 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | the status set the lane guards is undefined -> AC1 defines terminal by sdlc\_md.terminal\_statuses(type), a bug at Fixed, Verified or Closed and a story at Done, never a literal, with a planned mutant that decides by the literal Fixed alone so a Done story goes unguarded | AC1 names `mutation.py run --story <id> --from-plan` as the re-measure command but `cmd\\_from\\_plan` is a ledger READER that writes nothing and read `killed` on stale bytes in the executed Given, so the refusal names a remedy that cannot clear it - the remedy is `mutation.py register` per row against the new bytes (then from-plan to confirm); AC1 and AC2 never define the status set - `Fixed` alone never guards a story (stories reach Done, never Fixed), so name the set as delivered-terminal-for-type via the shared vocabulary; Steps to Reproduce step 3 does not reach its branch - after editing the target, both uncommitted and committed, from-plan still prints `every one executed and killed` (rows are dropped only by the NEXT register on that file, BG0550's path), so AC2's wording `whose rows are dropped` describes an event the commit never causes and the fixture must be built by hashing OLD bytes (ledger hash 64545701caa5 vs staged blob 655c94e0c9a2 in the scratch repo); from-plan's hash-blind join (a registered entry whose stored hash no longer matches the file is still read as killed) is the defect underneath this bug and is unfiled - BG0550 covers only the register-time drop; no row covers the fresh clone - the ledger lives in gitignored `sdlc-studio/.local/mutation-runs.json`, so the lane must PASS reporting `no ledger` when it is absent and must never write under `.local/` (the repo-writes lane refuses a hook that does); AC1 does not say whether MEASURED entries (provenance run, which also carry a target hash) count beside registered ones, nor what a staged deletion or rename of a registered target does; Points 3 are honest for the lane and the roster but become 5 if the remedy path and the status set both change; AC1's re-measure command is a reader that cannot clear the refusal - name the register-per-row remedy instead | plan-review | 882a5c6e5f00 |
| BG0651 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC1 now names is\_delivered\_terminal, the done-gate own predicate, and says why terminal\_statuses is wrong, with a mutant row that decides by terminal\_statuses; #2 -> the fixture holds both a Fixed bug and a Done story, so the literal-Fixed row dies on the Done story; #3 -> AC1 states that register hashes the working file and the remedy registers with the working copy equal to the staged bytes, the lane naming a difference; #4 -> AC2 carries the row skip every non-terminal unit differing rows silently; #5 -> AC3 carries the row name the lane in the roster without its blocking status; #6 -> replaced with is\_delivered\_terminal; #7 -> the fixture composition is stated, a Fixed bug and a Done story | AC1's re-measure command is a reader that cannot clear the refusal - name the register-per-row remedy instead; the status set the lane guards is undefined and `Fixed` alone never guards a story | plan-review | 882a5c6e5f00 |
| BG0649 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC1 names test\_critic alone with the executed cause, the two other modules are red only from the tests directory because each passes dot as its root, a working-directory dependence filed into CR0511 and out of scope, and the Verify runs from the repository root as the hook does; #2 -> the PARALLEL clause is pinned by a fixture of four one-second modules finishing in under three, with a serial row; #3 -> the head-import row stays as the one production change AC1 fails on under the unittest runner; #4 -> AC3 carries the row name the lane without saying it binds at the boundaries only; #5 -> stated per module in AC1 | two other skill test modules are red alone on HEAD so the module-alone lane refuses the push that ships it - either scope them in or state how the lane lands green | plan-review | 1cd7f46f42c3 |
| BG0645 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC1 requires the footer to be the plan-review return contract, record --phase plan-review, with a row that copies the delivery footer unchanged; #2 -> the record command with its phase is named in AC1; #3 -> named as the section the reviewer reads back | AC1's Given says a plan-review REJECT is `recorded` but `rejoinder\\_brief` reads the verdict from the file or stdin and never consults the ledger - say `given a prior verdict file` so the fixture is not built to record a row the code never reads; the rejoinder path prints no `brief fingerprint` or `record` hint on stderr for either phase (cmd\\_brief 3768-3772), so a round-two verdict can only carry provenance through `--brief-file` - nearest filed neighbour BG0625, no exact id found, worth folding into this unit since it rewrites that path; AC1 must require the test-plan table in the plan-review rejoinder and carry a row for it | plan-review | f9f7b43c413e |
| BG0648 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC4 greps the lane error code criterion-outside-section, which no filename carries; #2 -> AC2 states the fallback changes the brief, which renders zero criteria today, with a row that keeps the brief at zero; #3 -> the figure now reads fifteen tests in test\_verify\_ac alone; #4 -> replaced with the error code | Points 5 hold for the strict design (three consumers, one lane, two corpus moves, ~22 fixture edits); AC4's offender list is wrong under the reader it is meant to share - derive it from `parse\\_story` versus `criteria\\_section` (two artefacts today) and state the recognition set; the no-section rule and the test modules it reaches must be decided before code, with test\\_transition.py in Affects | plan-review | fe7343afbd7b |
| BG0646 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC1 pins the headline in-process: a test patches gather to assert the flushed Run: line is already in the stdout buffer when gather is entered, so rows 1 and 4 die whatever the clock; #2 -> the plan-time figure is recorded in AC1, 45 s at plan review with 45 gather and 189 advisory walks, and the test control re-records it at delivery; #3 -> AC2 spans gather and advisory in one process and names the ledger files the advisory reads; #4 -> the count reads about 190 walks, 189 measured on this corpus; #5 -> the in-process kill is in AC1 | `children\\_of` has seven other callers (rfc, reconcile x2, transition, sprint, status, close\\_owed) so its signature must keep the walking default, and `find\\_by\\_id` inside `\\_breakdown\\_child\\_ids` is a second full-tree scan per epic that the census must also answer; Points 5 are light for a fixture generator, a subprocess timing harness, a census refactor across three files with compatible defaults and a two-phase read spy - 8 is honest; two of AC1's four rows are today's-code mutants that survive on the criterion's own fixture (HEAD answers it in 12.0 s) - re-home them under AC2's read count or re-size the fixture and state the margin | plan-review | 699921540e76 |
| BG0649 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | two other skill test modules are red alone on HEAD -> AC1 and AC2 state the lane runs each module from the repository root, the hook working directory, where both modules pass, and their tests-directory failure is a working-directory dependence filed into CR0511 and named in AC1 as out of scope, so the lane does not refuse its own first push; AC2's lane must name its working directory -> AC2 names the working directory, the repository root given by --root, with a planned mutant that runs from the tests directory instead | AC2's lane refuses its own first push - on HEAD two OTHER modules are red when run alone (`test\\_file\\_finding.VerifySelectorWriteGuardTests.test\\_one\\_reader\\_answers\\_which\\_file\\_a\\_selector\\_targets` fails `ValueError not raised` and `test\\_sprint.RungTerminalAndProductTests.test\\_the\\_build\\_rung\\_still\\_reports\\_its\\_own\\_terminal` fails `'Review' != 'Done'`), reproduced under unittest and pytest from both the tests dir and the scripts dir, and the plan names only test\\_critic; AC2 names no cost - the serial module-alone sweep measured 519 s over 133 modules with test\\_gate capped at 90 s (it alone exceeds that), so the push boundary grows by roughly ten minutes on top of D0180's ten-to-fifteen, and the ruling should be re-affirmed or the lane parallelised before it binds; AC2 row 3 (bind per commit as well) survives the named test unless that test also asserts the lane is ABSENT from a plain `gate.py` run, as `test\\_gate.py` 6590 does for revert-check - name that assertion in the row; no row covers a root that is not the skill repo - a consuming project runs `gate.py --boundary push` with no `scripts/tests` directory, so the lane needs an `N/A (not the skill repo)` positive control like `\\_doc\\_coverage`; Points 2 hold for the import and the lane but not for whatever the two other red-alone modules need - re-point once their causes are known | plan-review | 1cd7f46f42c3 |
| BG0651 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> the fixture now holds a Won't Fix bug with rows on the file whose rows are reported and never refused, the outcome that separates is\_delivered\_terminal from terminal\_statuses, so the row dies; #2 -> AC1 carries a row that names the unit and rows but not the file; #3 -> added as in #1 | from-plan's hash-blind join (a registered entry whose stored hash no longer matches the file is still read as killed) is the defect underneath this bug and is unfiled - BG0550 covers only the register-time drop; no row covers the fresh clone - the ledger lives in gitignored `sdlc-studio/.local/mutation-runs.json`, so the lane must PASS reporting `no ledger` when it is absent and must never write under `.local/` (the repo-writes lane refuses a hook that does); AC1 does not say whether MEASURED entries (provenance run, which also carry a target hash) count beside registered ones, nor what a staged deletion or rename of a registered target does; Points 3 are honest for the lane and the roster but become 5 if the remedy path and the status set both change; AC1's re-measure command is a reader that cannot clear the refusal - name the register-per-row remedy instead; the status set the lane guards is undefined and `Fixed` alone never guards a story | plan-review | 882a5c6e5f00 |
| BG0649 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC2 states that modules carrying the suite serial\_only marker run in a serial phase after the parallel one, as the suite runner partitions them, with the phase recorded and a fixture module carrying the marker asserted to run last, and a row that ignores the marker; #2 -> the AC1 head-import row stands in the table as the one production change the unittest runner fails on, the earlier record wording was loose; #3 -> AC1 requires the import at the module head so every patch site resolves through it, which is what the row drops; #4 -> the CR0511 filing landed on 2026-09-07 and AC1 dates it; #5 -> stated as in #1 | two other skill test modules are red alone on HEAD so the module-alone lane refuses the push that ships it - either scope them in or state how the lane lands green | plan-review | 1cd7f46f42c3 |
| BG0645 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC1 now requires the stdout Return EXACTLY block to be the plan-review contract, never the delivery block, with a row that keeps appending the delivery block; #2 -> the earlier record wording is superseded by this closure, the contract is the stdout block; #3 -> --tier on a rejoinder is accepted as today and out of this unit scope, reported not repaired; #4 -> the closing-contract clause is in AC1 | the rejoinder path prints no `brief fingerprint` or `record` hint on stderr for either phase (cmd\\_brief 3768-3772), so a round-two verdict can only carry provenance through `--brief-file` - nearest filed neighbour BG0625, no exact id found, worth folding into this unit since it rewrites that path; AC1 must require the test-plan table in the plan-review rejoinder and carry a row for it | plan-review | f9f7b43c413e |
| BG0651 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | #1 -> AC4 now says what a stale row means to every reader: --from-plan and the done-gate read it as not-run, never killed, so a unit with any stale row cannot reach Fixed until it re-registers, whether or not the commit lane ran, with a row that keeps the join hash-blind; #2 -> stale has one meaning in AC1 and AC4: the recorded hash matches neither HEAD blob nor the working file; #3 -> the attribution reads BG0640 on gate.py and BG0596 and BG0597 on mutation.py by the ledger, three Fixed units re-measured by this unit | from-plan's hash-blind join (a registered entry whose stored hash no longer matches the file is still read as killed) is the defect underneath this bug and is unfiled - BG0550 covers only the register-time drop; no row covers the fresh clone - the ledger lives in gitignored `sdlc-studio/.local/mutation-runs.json`, so the lane must PASS reporting `no ledger` when it is absent and must never write under `.local/` (the repo-writes lane refuses a hook that does); AC1 does not say whether MEASURED entries (provenance run, which also carry a target hash) count beside registered ones, nor what a staged deletion or rename of a registered target does; Points 3 are honest for the lane and the roster but become 5 if the remedy path and the status set both change; AC1's re-measure command is a reader that cannot clear the refusal - name the register-per-row remedy instead; the status set the lane guards is undefined and `Fixed` alone never guards a story | plan-review | 882a5c6e5f00 |
| BG0651 | 2026-09-07 | planning session (Claude, author of the plan) | 2026-09-07 | AC4 must state what a stale-marked row means to every reader -> AC4 now states that --from-plan and the done-gate read a stale row as not-run, never killed, so a unit with any stale row cannot reach Fixed until it re-registers whether or not the commit lane ran, with a row that keeps the join hash-blind; AC1 must attribute the 14 live rows as the ledger does -> AC1 now reads BG0640 six on gate.py and BG0596 and BG0597 eight on mutation.py, three Fixed units, BG0644 dropped | AC1 names `mutation.py run --story <id> --from-plan` as the re-measure command but `cmd\\_from\\_plan` is a ledger READER that writes nothing and read `killed` on stale bytes in the executed Given, so the refusal names a remedy that cannot clear it - the remedy is `mutation.py register` per row against the new bytes (then from-plan to confirm); AC1 and AC2 never define the status set - `Fixed` alone never guards a story (stories reach Done, never Fixed), so name the set as delivered-terminal-for-type via the shared vocabulary; Steps to Reproduce step 3 does not reach its branch - after editing the target, both uncommitted and committed, from-plan still prints `every one executed and killed` (rows are dropped only by the NEXT register on that file, BG0550's path), so AC2's wording `whose rows are dropped` describes an event the commit never causes and the fixture must be built by hashing OLD bytes (ledger hash 64545701caa5 vs staged blob 655c94e0c9a2 in the scratch repo); from-plan's hash-blind join (a registered entry whose stored hash no longer matches the file is still read as killed) is the defect underneath this bug and is unfiled - BG0550 covers only the register-time drop; no row covers the fresh clone - the ledger lives in gitignored `sdlc-studio/.local/mutation-runs.json`, so the lane must PASS reporting `no ledger` when it is absent and must never write under `.local/` (the repo-writes lane refuses a hook that does); AC1 does not say whether MEASURED entries (provenance run, which also carry a target hash) count beside registered ones, nor what a staged deletion or rename of a registered target does; Points 3 are honest for the lane and the roster but become 5 if the remedy path and the status set both change; AC1's re-measure command is a reader that cannot clear the refusal - name the register-per-row remedy instead; the status set the lane guards is undefined and `Fixed` alone never guards a story | plan-review | 882a5c6e5f00 |
| BG0651 | 2026-09-07 | delivery session (Claude, author of the diff) | 2026-09-07 | #1 -> rows registered against a working copy that differs from the staged blob now REFUSE for a delivered unit with that reason and are reported for any other, pinned in both lane tests and as AC2 row 3; #2 -> the AC1 selector gained a step that re-registers BG0001 against a working copy differing from the stage and asserts the refusal names it, so mutant P1b dies there; #3 -> the join reads a HEAD-matching, working-differing row as executed, pinned in the register test with the git fixture, AC4 row 5; #4 -> the stale mark is a record of who and when and never a rule, the join uses the computed comparison only, pinned by the restored-bytes step, AC4 row 4; #5 -> a withdrawn row is never named, pinned by a retract step in the AC2 test, AC2 row 4; #6 -> a shared entry gives up only the registering unit own rows, pinned by a shared-entry step in the register test; #7 -> the mark no longer contradicts the rule, one meaning everywhere; #8 -> the inventory now reads BG0640 six on gate.py, BG0596 four and BG0597 one on mutation.py; #9 -> the lane no longer reads the mark at all, both branches removed; #10 -> as #1; #11 -> as #2 | BG0596 and BG0597 read not-ok at plan\\_execution on rows targeting verify\\_ac.py, transition.py and critic.py that have no ledger entry at all - files this unit never touched; tools/tests/test\\_trd\\_surface\\_derivation.py default-sweep pin fails - trd.md:170 omits evidence-drift; tools/tests/test\\_skill\\_tests\\_env.py scrub-site pin fails - test\\_gate.py:7024 `\\_git` scrub list unregistered; AC1 "says so when the two differ" unimplemented and a re-register on non-staged bytes turns the lane green (gate.py:1196-1210); sticky `stale` marker that the named remedy cannot clear (mutation.py:2103-2131 and 2655-2657) | delivery | 49b3a3261d71 |
| BG0651 | 2026-09-07 | delivery session (Claude, author of the diff) | 2026-09-07 | BG0596 and BG0597 read not-ok at plan -> pre-existing at the base ref and outside this unit Affects: rows on verify\_ac.py, transition.py and critic.py with no ledger entry were dropped by earlier register calls before this unit existed, reported here and to be re-measured by the units that touch those files (BG0645 and BG0648 in this batch), never by this one; tools/tests/test\_trd\_surface\_derivation.py default-sweep pin fails -> trd.md line 170 names evidence-drift in the default sweep; tools/tests/test\_skill\_tests\_env.py scrub-site pin fails -> the test helper scrubs through gitutil.git\_env and declares no list of its own, so it is no scrub site; AC1 "says so when the two differ" unimplemented -> rows registered against a working copy that differs from the staged blob now refuse for a delivered unit with that reason and are reported otherwise, pinned in both lane tests and as AC2 row 3; sticky `stale` marker that the named remedy cannot clear -> the mark is a record of who and when and never a rule, the join uses the computed comparison only, so the named remedy clears it and a restored file reads live, pinned in the register test | tools/tests/test\\_trd\\_surface\\_derivation.py::ShippedSurfaceIsDerived::test\\_the\\_default\\_sweep\\_lane\\_list\\_equals\\_gate\\_default\\_checks FAILS with the diff and passes at 9ff66875 - sdlc-studio/trd.md:170 enumerates the default sweep without `evidence-drift` (the assertion names it: "omits ['evidence-drift']"), so the tools suite goes red on main at this commit; tools/tests/test\\_skill\\_tests\\_env.py::ScrubSiteSweepTests::test\\_every\\_scrub\\_site\\_is\\_registered FAILS with the diff and passes at 9ff66875 - the new `EvidenceDriftTests.\\_git` helper (test\\_gate.py:7024-7035) hand-scrubs GIT\\_DIR/GIT\\_WORK\\_TREE/... and is not registered in SCRUB\\_SITES, the message says "Add a test holding it to REPO\\_LOCATING, then register it in SCRUB\\_SITES"; AC1's clause "the lane says so when the two differ" is unimplemented AND the trap it warns about clears the lane - gate.py:1196-1210 `\\_evidence\\_drift` never hashes the working copy against the staged blob, prints one constant remedy, and because `commit\\_drifts` requires `entry.hash == head`, re-registering against a working copy that differs from the staged bytes (fixture: x=3 registered while x=2 staged, HEAD x=1) turns the lane GREEN ("1 registered target(s) examined, none drifted") and the commit ships bytes the evidence is not about; the `stale` marker is sticky and the named remedy cannot clear it - mutation.py:2103-2131 `register\\_mutant` never removes `entry["stale"]` when the matching-hash entry is found again, and `plan\\_execution` (mutation.py:2655-2657) tests `entry.get("stale")` before the hash rule, so after a later unit marks rows stale and the bytes revert to the recorded hash, the earlier unit's own re-register leaves marker=True with `entry\\_staleness`=live and its rows still read not-run (fixture fy: plan\\_execution(BG0001) ok=False), a second definition of stale beside AC4's one; missing regression cover for the half of AC4 that the fix exists for - my mutant reducing the plan\\_execution skip to the marker alone (mutation.py:2656 drop the `entry\\_staleness` call) SURVIVES both Verify selectors, so a commit drift with hooks off or `--no-verify` reading killed again would pass the suite green (RegisterKeepsOtherUnitsRowsTests exercises only the register-marker path), and four more of my mutants survive: a missing target reading killed in plan\\_execution (mutation.py:2657), a staged deletion no longer refusing (gate.py:1204), withdrawn rows named in refusals (gate.py:1180), and a unit with no artefact refusing (gate.py:1209); gate.py:1191-1194 `\\_delivered` returns None for a unit whose artefact is absent from disk and gate.py:1211 labels its drifted rows "not yet delivered - reported, not refused" (fixture BG0999), so a renamed or deleted artefact's evidence is drifted under a false label; the lane filters to PROVENANCE\\_REGISTERED (gate.py:1157-1158) while `plan\\_execution` now reads a MEASURED entry's rows as not-run on the same drift (fixture fw: measured x.py entry, `head` before the commit, stale after), so a commit drifting `mutation.py run` evidence is unrefused and surfaces at the close dry-run, the original symptom on the measured path; `entry\\_staleness`'s docstring "one meaning of STALE for every reader" and AC4's "the same thing to every reader" are over-claimed - transition.py:2413-2446 `repair\\_mutation\\_gate` keeps its own rule (working-file hash only, refuses only when EVERY entry is stale, ignores the marker) and counts killed rows on stale entries as `applied`, and verify\\_ac.py:4040-4063 `\\_unit\\_ledger\\_rows` lists stale rows as live for `ledger\\_absent`, though `\\_planned\\_mutant\\_gate` blocks the same transition so the outcome holds; AC1 prose miscounts the re-measured rows - it says BG0597's five on mutation.py and BG0596's three withdrawn, the ledger holds BG0597 one and BG0596 four live there (6+4+1 = the eleven the fragment names); sprint.py:5964 `claimed\\_proof\\_gaps` iterates the entries LIST so `mutated\\_files` is a set of dict reprs and every unit reports as a gap (executed: ['BG0597', 'BG0640', 'BG0651'] on a ledger with live gate.py rows for BG0651), present since e16fc332, no open id found in bugs/\\_index.md - file it; a same-unit same-bytes re-register appends a duplicate row (2 rows at base and with the diff, mutation.py register\\_mutant), no open id found in bugs/\\_index.md | delivery | 49b3a3261d71 |
| BG0646 | 2026-09-07 | sdlc-studio session | 2026-09-07 | #1 -> CLOSED: the in-process pin asserts `remaining=4)` on the closed token and assertEqual(4, build's remaining) over a batch exercising every branch (test\_status.py GatherPerformanceTests) - a digit-growth mutant is killed; #2 -> CLOSED: handoff.\_classify is the ONE predicate read by \_unit and remaining\_count, and the fixture's run holds a delivered, red-evidence, open, dropped, missing and quarantined-outside-batch unit - the two surviving mutants are AC1 rows 5 and 6, both killed; #3 -> CLOSED: open\_run's docstring names \_classify and remaining\_count as the shared predicate and its cheap entry point; #4 -> CLOSED: cmd\_pillars reads open\_run once and passes it as gather(root, run=run); #5 -> FILED: pre-existing handoff.build KeyError on a loop-state entry with no attempts key is a Low in CR0511 | the substring assertion behind the fragment's equality claim (test\\_status.py:1228); the unpinned dropped/missing/quarantined branches of the new `remaining\\_count` predicate (handoff.py:420-451) | delivery | 85ff2e4efff9 |
| BG0646 | 2026-09-07 | sdlc-studio session | 2026-09-07 | #1 -> CLOSED: one predicate (handoff.\_classify) and a fixture run exercising every branch - the two surviving mutants are AC1 rows 5 and 6 in the ledger, both killed by the AC1 selector; #2 -> CLOSED: the pin asserts `remaining=4)` on the closed token beside assertEqual against build; #3 -> CLOSED: the fixture stamps .close-owed-baseline.json so owed passes its unbaselined return, and AC2's test asserts VELOCITY.md was read; #4 -> CLOSED: AC2's test builds the same full-scale fixture AC1 defines; #5 -> CLOSED: the fixture writes 26 RV files and the total still sums to 2,340; #6 -> CLOSED: the clause is replaced - AC1 points at the ledger's sweep-absence row, re-measured at delivery above the bound (23 s); #7 -> CLOSED: the fragment now says never a cache that outlives the sweep; #8 -> CLOSED: sys.path and sys.modules are restored by addCleanup; #9 -> FILED: the re-reads by advisories outside gather and close-owed are a Low in CR0511 | - | delivery | 85ff2e4efff9 |
| BG0646 | 2026-09-07 | sdlc-studio session | 2026-09-07 | the handoff.remaining\_count equality pin is vacuous on its fixture -> CLOSED: handoff.\_classify is the one predicate, the fixture run holds a quarantined-outside-batch unit and a batch id with no file, and both mutants are AC1 rows 5 and 6 in the ledger, killed by the AC1 selector | the fragment's claim that the in-process pin "asserts it equals what `build` derives" is FALSE - it is a substring `assertIn`, and a `remaining\\_count` returning 11 where build says 1 passes it (test\\_status.py:1228); `handoff.remaining\\_count` re-implements `\\_unit`'s predicate instead of sharing it and three of its four branches have NO cover - dropping the quarantined-outside-batch term or the missing-file term diverges from `build` (3 vs 2, 3 vs 1) and survives all 219 tests in test\\_status + test\\_close\\_owed + test\\_handoff (handoff.py:420-451); `open\\_run`'s docstring still says "`handoff.build` owns that predicate ... and this asks it, so the dashboard and the handoff cannot answer the same question differently" 35 lines above the call that no longer asks it (status.py:262-265); `open\\_run` now runs twice per text-mode invocation - once for the headline and again inside `gather` - so `remaining\\_count` is paid twice, the first time outside the sweep (status.py:380 and status.py:338); `handoff.build` raises KeyError on a loop-state unit with no `attempts` key, so a half-written loop-state takes the whole run line down (loop\\_guard.py:66) - not this diff, and `remaining\\_count` is immune to it; the substring assertion behind the fragment's equality claim (test\\_status.py:1228); the unpinned dropped/missing/quarantined branches of the new `remaining\\_count` predicate (handoff.py:420-451) | delivery | 85ff2e4efff9 |
| BG0646 | 2026-09-07 | sdlc-studio session | 2026-09-07 | the handoff.remaining\_count equality pin is vacuous on its fixture -> CLOSED: handoff.\_classify is the one predicate, the fixture run holds a quarantined-outside-batch unit and a batch id with no file, and both mutants are AC1 rows 5 and 6 in the ledger, killed by the AC1 selector | the fragment's claim that the in-process pin "asserts it equals what `build` derives" is FALSE - it is a substring `assertIn`, and a `remaining\\_count` returning 11 where build says 1 passes it (test\\_status.py:1228); `handoff.remaining\\_count` re-implements `\\_unit`'s predicate instead of sharing it and three of its four branches have NO cover - dropping the quarantined-outside-batch term or the missing-file term diverges from `build` (3 vs 2, 3 vs 1) and survives all 219 tests in test\\_status + test\\_close\\_owed + test\\_handoff (handoff.py:420-451); `open\\_run`'s docstring still says "`handoff.build` owns that predicate ... and this asks it, so the dashboard and the handoff cannot answer the same question differently" 35 lines above the call that no longer asks it (status.py:262-265); `open\\_run` now runs twice per text-mode invocation - once for the headline and again inside `gather` - so `remaining\\_count` is paid twice, the first time outside the sweep (status.py:380 and status.py:338); `handoff.build` raises KeyError on a loop-state unit with no `attempts` key, so a half-written loop-state takes the whole run line down (loop\\_guard.py:66) - not this diff, and `remaining\\_count` is immune to it; the substring assertion behind the fragment's equality claim (test\\_status.py:1228); the unpinned dropped/missing/quarantined branches of the new `remaining\\_count` predicate (handoff.py:420-451) | delivery | 85ff2e4efff9 |
| BG0649 | 2026-09-07 | sdlc-studio session | 2026-09-07 | the module-alone lane is RED on this very repository -> CLOSED: the lane exports the hook's RELATIVE tests path and runs green on this repository (274.7 s, 133 modules, 16 workers, 1 serial); the roster's cost claim is false by 5x -> CLOSED: AGENTS.md states the slowest module, test\_gate at about four minutes, against a serial sum of about seventeen; the "519 s serial sum" in AC2 -> CLOSED: AC2, the fragment and the docstring state 519 s at filing and about 1,000 s measured at delivery; a failed `pytest --collect-only` is silently read -> CLOSED: a collect that exits outside 0 and 5 is refused naming the partition read, pinned by a broken-conftest fixture (AC2 row 10); AC2's "never a source scan" clause is unpinned -> CLOSED: a decoy module carrying the marker's name in a comment runs in the parallel phase (AC2 row 7); a module that exits non-zero with no -> CLOSED: a fixture module that dies inside a test with no output is named by the fallback (AC2 row 8); AC2's "first failure line" is unpinned -> CLOSED: the borrower carries two failing tests and the detail names the first with its exception line (AC2 rows 9 and 12); the shipped module-alone lane is RED on this repository, twice -> CLOSED: relative PYTHONPATH, green on this repository; AGENTS.md states the lane's cost as "about 45 s on this machine" -> CLOSED: four minutes, measured; AGENTS.md:44, the pre-push row a pusher actually reads -> CLOSED: the row says three boundary lanes; `.githooks/pre-push` header comment and its announced-cost -> CLOSED: both name module-alone; a red module's detail line carries unittest's header line only -> CLOSED: the exception line follows the header (AC2 row 12); the lane exports an ABSOLUTE PYTHONPATH that every module's grandchild -> CLOSED: relative, pinned by test\_c\_cwd (AC2 row 11); AGENTS.md:103 states the lane's cost as "about 45 s on this machine" -> CLOSED: four minutes, and the revision row records the hand measurement AC3's test cannot see; the 3.0s parallelism bound does not discriminate -> CLOSED: eight sleepers and a detail line naming the machine's core count (AC2 row 13); the `pytest --collect-only` returncode is unchecked -> CLOSED: refused (AC2 row 10); no per-module timeout on the unittest subprocess -> CLOSED: SDLC\_MODULE\_ALONE\_TIMEOUT, half an hour by default, pinned by a hanging fixture (AC2 row 14); the module glob is `test\_*.py` -> CLOSED: the glob is unittest discover's own test*.py, the pattern the hook runs; .githooks/pre-push:2 still says "the two boundary lanes -> CLOSED: names the three lanes | CR0511 `test\\_file\\_finding` and `test\\_sprint` remain working-directory dependent - both green from the root, out of scope here; the lane is red on this repository and blocks every push (`test\\_repo\\_hygiene` under the lane's absolute PYTHONPATH); AGENTS.md's "about 45 s" cost claim and the "519 s serial sum" claim are both refuted by measurement (241-257 s wall, 1009 s sum, slowest module 239 s) | delivery | af1805d85aad |
| BG0649 | 2026-09-07 | sdlc-studio session | 2026-09-07 | the module-alone lane is deterministically red on this repository -> CLOSED: relative PYTHONPATH, the lane is green on this repository at 274.7 s; the lane's cost is stated as 45 s and measured at 247-271 s -> CLOSED: AGENTS.md states four minutes for the slowest module against a serial sum of about seventeen; the absolute PYTHONPATH makes test -> CLOSED: relative, pinned by test\_c\_cwd (AC2 row 11); AGENTS.md's stated lane cost of "about 45 s" is false by 6x -> CLOSED: four minutes, hand-measured and recorded in the revision row; the lane is red on this repository and blocks every push -> CLOSED: green on this repository with the hook's relative tests path; AGENTS.md's "about 45 s" cost claim and the "519 s serial sum" claim -> CLOSED: both figures replaced by the delivery measurements; CR0511 holds test\\\_file\\\_finding and test\\\_sprint red only -> FILED: CR0511; CR0511 test\\\_file\\\_finding and test\\\_sprint working-directory dependence -> FILED: CR0511; CR0511 `test\\\_file\\\_finding` and `test\\\_sprint` remain working-directory dependent -> FILED: CR0511 | the module-alone lane is RED on this very repository and BLOCKS the push boundary - `test\\_repo\\_hygiene` fails under the lane's ABSOLUTE PYTHONPATH while the hook's relative one keeps it green (gate.py:1176-1177); the roster's cost claim is false by 5x - AGENTS.md says "about 45 s on this machine", measured 241.4 s and 256.7 s over two runs, and the gate prints "OVER the 45s budget by 196.4s" at every push (AGENTS.md:98-105); the "519 s serial sum" in AC2, the fragment and the gate docstring is false - the measured sum of the 133 modules run alone is 1009 s, and the slowest module is `test\\_gate` at 239 s not 45 s (gate.py:1151-1156); a failed `pytest --collect-only` is silently read as "no serial modules" and the lane still PASSES while printing "none marked serial\\_only" (gate.py:1170-1174); AC2's "never a source scan" clause is unpinned - a source-scan mutant SURVIVES the AC2 test; a module that exits non-zero with no `FAIL:`/`ERROR:` line is unpinned - a mutant that drops it from `failed` SURVIVES, so the lane's own crash/timeout fallback line is dead cover; AC2's "first failure line" is unpinned - a last-failure-line mutant SURVIVES | delivery | af1805d85aad |
| BG0649 | 2026-09-07 | sdlc-studio session | 2026-09-07 | AC3's cost clause has no failing test -> CLOSED: the pin demands the cost in minutes beside D0180 and the slowest module named, and AC3 row 2 deletes the sentence and dies; the glob repair does not close the gap it names -> CLOSED: a testextra\_ok module the narrower glob would miss is in the fixture and counted, AC2 row 15; the parallel pin is a string decoupled from the pool -> CLOSED: the figure is read from pool.\_max\_workers, AC2 row 16; AGENTS.md:98-105 states the lane's cost as the slowest module -> CLOSED: the cost is stated as the lane's wall clock, four to five minutes, test\_gate near four; the Revision History records only "Filed" -> CLOSED: rows for delivery, r1 and r2; the pre-push hook's no-history cost literal now names three lanes -> CLOSED: the pin in tools/tests/test\_pre\_push\_hook.py names the three lanes and the file is in Affects; .githooks/pre-push is edited but is absent from BG0649's declared Affects -> CLOSED: Affects names the hook and its test; a missing pytest refuses the whole push boundary and the reason is truncated -> CLOSED: the reason is clipped at a word at 300 characters, never mid-path; weak cover - the serial phase LABEL is unpinned -> CLOSED: the marked fixture module is red and reported under [serial], AC2 row 17; weak cover - the summary's parallel count is unpinned -> CLOSED: the detail's module and parallel counts are asserted exactly, AC2 row 18; no open id - test\_gate under the unittest runner prints 24 lines -> FILED: CR0511; tools/tests/test\_test\_census.py::ImportabilityTests::test\_every\_module\_here\_imports\_under\_pytest is red -> FILED: CR0511; the gate prints "OVER the 45s budget" on every boundary run -> FILED: CR0511 | the module-alone lane is RED on this very repository and BLOCKS the push boundary - `test\\_repo\\_hygiene` fails under the lane's ABSOLUTE PYTHONPATH while the hook's relative one keeps it green (gate.py:1176-1177); the roster's cost claim is false by 5x - AGENTS.md says "about 45 s on this machine", measured 241.4 s and 256.7 s over two runs, and the gate prints "OVER the 45s budget by 196.4s" at every push (AGENTS.md:98-105); the "519 s serial sum" in AC2, the fragment and the gate docstring is false - the measured sum of the 133 modules run alone is 1009 s, and the slowest module is `test\\_gate` at 239 s not 45 s (gate.py:1151-1156); a failed `pytest --collect-only` is silently read as "no serial modules" and the lane still PASSES while printing "none marked serial\\_only" (gate.py:1170-1174); AC2's "never a source scan" clause is unpinned - a source-scan mutant SURVIVES the AC2 test; a module that exits non-zero with no `FAIL:`/`ERROR:` line is unpinned - a mutant that drops it from `failed` SURVIVES, so the lane's own crash/timeout fallback line is dead cover; AC2's "first failure line" is unpinned - a last-failure-line mutant SURVIVES; CR0511 `test\\_file\\_finding` and `test\\_sprint` remain working-directory dependent - both green from the root, out of scope here; the lane is red on this repository and blocks every push (`test\\_repo\\_hygiene` under the lane's absolute PYTHONPATH); AGENTS.md's "about 45 s" cost claim and the "519 s serial sum" claim are both refuted by measurement (241-257 s wall, 1009 s sum, slowest module 239 s) | delivery | af1805d85aad |
| BG0649 | 2026-09-07 | sdlc-studio session | 2026-09-07 | the pre-push hook's lane literal was changed without its test -> CLOSED: tools/tests/test\_pre\_push\_hook.py pins the three-lane literal and passes (10 passed), the hook and its test are in Affects; AC3's "with its cost stated beside D0180's" clause has no test -> CLOSED: the pin demands the cost in minutes beside D0180 with the slowest module named, and AC3 row 2 deletes the sentence and dies | the module-alone lane is RED on this very repository and BLOCKS the push boundary - `test\\_repo\\_hygiene` fails under the lane's ABSOLUTE PYTHONPATH while the hook's relative one keeps it green (gate.py:1176-1177); the roster's cost claim is false by 5x - AGENTS.md says "about 45 s on this machine", measured 241.4 s and 256.7 s over two runs, and the gate prints "OVER the 45s budget by 196.4s" at every push (AGENTS.md:98-105); the "519 s serial sum" in AC2, the fragment and the gate docstring is false - the measured sum of the 133 modules run alone is 1009 s, and the slowest module is `test\\_gate` at 239 s not 45 s (gate.py:1151-1156); a failed `pytest --collect-only` is silently read as "no serial modules" and the lane still PASSES while printing "none marked serial\\_only" (gate.py:1170-1174); AC2's "never a source scan" clause is unpinned - a source-scan mutant SURVIVES the AC2 test; a module that exits non-zero with no `FAIL:`/`ERROR:` line is unpinned - a mutant that drops it from `failed` SURVIVES, so the lane's own crash/timeout fallback line is dead cover; AC2's "first failure line" is unpinned - a last-failure-line mutant SURVIVES; CR0511 `test\\_file\\_finding` and `test\\_sprint` remain working-directory dependent - both green from the root, out of scope here; the lane is red on this repository and blocks every push (`test\\_repo\\_hygiene` under the lane's absolute PYTHONPATH); AGENTS.md's "about 45 s" cost claim and the "519 s serial sum" claim are both refuted by measurement (241-257 s wall, 1009 s sum, slowest module 239 s) | delivery | af1805d85aad |
| BG0645 | 2026-09-07 | sdlc-studio session | 2026-09-07 | the shipped `--rejoinder` help string -> CLOSED: the help describes both phases' shapes, the tier refusal and the footer, pinned by the AC1 test reading brief --help; the shipped consumer reference reference-scripts-review.md:149 -> CLOSED: the sentence describes both shapes and the footer; `--tier` on a plan-review rejoinder now exits 2 -> CLOSED: AC1 names the refusal as the plain plan brief's, with a revision row; the Revision History still carries only -> CLOSED: rows for the four plan rounds, the delivery and this round; the `record` command the new footer prints does not run as printed -> CLOSED: both plan footers name --reviewer and --author, pinned; the fingerprint the new footer prints always records with the NOTE -> CLOSED: the fingerprint identifies the base brief and phase and the matcher reproduces it, pinned in both phases; CR0329 is Status Complete and Decomposed-into -> CLOSED: the citation is dropped from AC2 and the fragment; the rejoinder fingerprint footer can never answer its own provenance check -> CLOSED: rejoinder\_fingerprint over the base brief and phase, matched by \_seats\_whose\_brief\_matches, no note on an honest re-review in either phase, a constant fingerprint dies; AC1's "the prior verdict verbatim" is not pinned -> CLOSED: all three lines asserted verbatim; the plan-review re-examination paragraph is unpinned -> CLOSED: the ruling instruction is asserted and the RE-EXECUTE demand refused; `--tier-explicit` on the new delivery-rejoinder footer is unpinned -> CLOSED: asserted under --tier full; the "Given a unit with a REJECT recorded" precondition of both criteria is decorative -> CLOSED: both Givens say the prior verdict FILE, and the record step feeds the footer's own record assertion; the `--rejoinder` help string at critic.py:4457-4460 asserts -> CLOSED: the help describes both shapes, pinned by the AC1 test reading brief --help; the rejoinder footer prints a fingerprint that `record --brief` always reports -> CLOSED: rejoinder\_fingerprint over the base brief and phase, reproduced by the matcher, no note in either phase, a constant fingerprint dies | - | delivery | ccb1ea5b9c2b |
| BG0648 | 2026-09-07 | sdlc-studio session | 2026-09-07 | the Done gate no longer refuses a story whose misplaced criterion has a FAILING verifier -> CLOSED: transition's AC gate refuses a misplaced criterion by name and reads the section's blocks, pinned by MisplacedCriterionGateTests with the in-section red verifier as the control (AC2 row 4); BG0270's correction is incomplete -> CLOSED: the revision row is back in its table, the depth field is restored, and BG0270 and US0597 record the edit; three of the twelve rewritten call sites are unpinned -> CLOSED: the depth deriver is pinned (AC2 row 7), revert-check and the matrix scaffold stay unpinned and are named in the revision row; the brief keeps its own case-sensitive section regex -> CLOSED: one heading rule everywhere, case-insensitive with a suffix allowed, pinned by a variant-heading fixture (AC1 row 4, AC2 row 5); `has\_criteria\_section` anchors the whole heading -> CLOSED: substring heading as count\_acs reads it, pinned; the changelog fragment's "Three corpus artefacts already held that shape" -> CLOSED: two bugs or stories, an epic and a retro out of scope; `ac\_fingerprint` still hashes every block including misplaced ones -> CLOSED: the fingerprint hashes the section's blocks, pinned (AC2 row 6); the Done gate silently stops refusing a misplaced criterion whose verifier fails -> CLOSED: refused by name, pinned; BG0270's Revision History row is left inside -> CLOSED: returned to its table | - | delivery | 54f4a76bc0fe |
| US0818 | 2026-09-07 | Claude Fable 5.1 | 2026-09-08 | #1 -> restated at plan time as a different target PATH for the same row: control two in test\_mutation.py::RegisterReplacesTests::test\_a\_same\_key\_registration\_replaces\_the\_live\_row\_and\_a\_new\_row\_appends registers the same row against u.py and asserts BG0614's audit names the pair, and BG0614 AC1's matching sentence was reworded the same day\; engineering rejoinder APPROVE e07cf868d945; #2 -> the plan row names the two makeable edits, both executed at delivery and killed by test\_the\_cli\_path\_replaces\_and\_reports\_through\_the\_shipped\_command: AC3 r0 delete the replaced line from cmd\_register's stdout, AC3 r1 append the record a second time after register\_mutant returns; #3 -> the row names AC1's same-key replace as its control, and test\_a\_disagreeing\_verdict\_or\_test\_is\_refused\_naming\_retract registers the survived row first so the refusal is judged against a live row; #4 -> the BLOCKING line restates #1: closed by the same restatement (different target PATH, control two of the AC1 test), engineering rejoinder APPROVE e07cf868d945 | - | plan-review | 86662d7541ae |
| US0815 | 2026-09-07 | Claude Fable 5.1 | 2026-09-08 | #1 -> the Test Plan row for AC1 gained the subject-alone edit, executed at delivery as AC1 r4 (narrow the match to the subject alone) and killed by test\_uncovered\_added\_lines\_are\_named\_by\_number\_and\_covered\_ones\_are\_not, whose fixture commit names the unit only on a Refs: line; #2 -> \_unit\_commit\_shas reads every Refs: line of a message and the comma form on one line\; the fixture commits carry both shapes (one Refs: per id with the unit on the later line, and the comma line), and AC1 r5 (read only the first Refs line) is killed by the same test; #3 -> the AC3 fixture presents a stub coverage package whose \_\_version\_\_ is 6.5.0 on the wrapper interpreter, so the floor refusal is asserted\; AC3 r1 (drop the version-floor check) killed by test\_a\_missing\_coverage\_module\_is\_refused\_not\_read\_as\_covered; #4 -> the no-base fixture seeds a run-state whose batch names a different unit, stated in the test\; AC1 r2 (delete the batch membership check so a foreign run's ref is read) killed; #5 -> test\_lint\_workflow\_coverage.py::CoverageBeforeSuiteTests asserts the coverage>=7.10 install precedes the skill-tests step by position in lint.yml, and README.md names the dependency in the same patch\; AC7 r0 (drop the floor from the install line) killed; #6 -> the BLOCKING line restates #1, #2 and #3, closed as above\; qa rejoinder APPROVE ea6a61241251 | AC7 no row for the AGENTS.md soft-dependency half - delete `coverage` from the table; BLOCKING: AC1 names four attribution mutants and none for the `Refs:` half - a subject-only matcher, or one using git's trailer parser that the criterion says cannot see the hook-shaped line, survives every row, so add a fixture commit that names the unit ONLY on a hook-shaped `Refs:` line with an unexecuted added line and a row (match the subject alone) | plan-review | d9dc8dde9ef3 |
| US0815 | 2026-09-07 | Claude Fable 5.1 | 2026-09-08 | #1 -> on the first verdict: the Refs: half has rows AC1 r3 (widen to the whole body), r4 (subject alone) and r5 (first Refs line only), each killed by the fixture commit that names the unit only on a hook-shaped Refs: line with an unexecuted added line / on the second verdict: the Test Plan row for AC1 gained the subject-alone edit, executed at delivery as AC1 r4 (narrow the match to the subject alone) and killed by test\_uncovered\_added\_lines\_are\_named\_by\_number\_and\_covered\_ones\_are\_not, whose fixture commit names the unit only on a Refs: line; #2 -> on the first verdict: the fixture states the outside test sits in the same module as the unit's own selector\; AC2 r0 (collect over the whole test module the selector names) killed by test\_only\_the\_units\_own\_verifiers\_count\_as\_coverage / on the second verdict: \_unit\_commit\_shas reads every Refs: line of a message and the comma form on one line\; the fixture commits carry both shapes (one Refs: per id with the unit on the later line, and the comma line), and AC1 r5 (read only the first Refs line) is killed by the same test; #3 -> on the first verdict: AC2 r1 (change the untraced-only branch to count the file as uncovered) killed by the exit-decided-by-measured-files assertion / on the second verdict: the AC3 fixture presents a stub coverage package whose \_\_version\_\_ is 6.5.0 on the wrapper interpreter, so the floor refusal is asserted\; AC3 r1 (drop the version-floor check) killed by test\_a\_missing\_coverage\_module\_is\_refused\_not\_read\_as\_covered; #4 -> on the first verdict: AC3 asserts the whole sentence coverage: not measured - the coverage module is absent together with the non-zero exit, in test\_a\_missing\_coverage\_module\_is\_refused\_not\_read\_as\_covered / on the second verdict: the no-base fixture seeds a run-state whose batch names a different unit, stated in the test\; AC1 r2 (delete the batch membership check so a foreign run's ref is read) killed; #5 -> on the first verdict: AC4 r2 (omit the Affects hash from the report's coverage key) killed by a key assertion in test\_the\_cli\_exit\_code\_and\_report\_carry\_the\_verdict / on the second verdict: test\_lint\_workflow\_coverage.py::CoverageBeforeSuiteTests asserts the coverage>=7.10 install precedes the skill-tests step by position in lint.yml, and README.md names the dependency in the same patch\; AC7 r0 (drop the floor from the install line) killed; #6 -> on the first verdict: the 7.10 floor is pinned in the lint.yml install line (AC7 r0) and in the absence refusal (AC3 r1), both killed / on the second verdict: the BLOCKING line restates #1, #2 and #3, closed as above\; qa rejoinder APPROVE ea6a61241251; #7 -> on the first verdict: AC7 r1 (delete coverage from AGENTS.md's soft-dependency table) killed by test\_coverage\_is\_installed\_before\_the\_suite\_runs\_and\_named\_as\_a\_dependency; #8 -> on the first verdict: the BLOCKING line restates #1 and #2, closed as above\; qa rejoinder APPROVE ea6a61241251 | - | plan-review | d9dc8dde9ef3 |
| US0816 | 2026-09-07 | Claude Fable 5.1 | 2026-09-08 | #1 -> on the engineering verdict: AC7 gained a second row: AC7 r0 flips the fallback literal for the mode in transition.py to block, killed by test\_the\_shipped\_default\_is\_report, beside the template row AC7 r1 killed by a text assertion\; both executed at delivery / on the product verdict: AC2 states the reason floor: add\_coverage\_ruling refuses a reason shorter than mutation.\_RETRACT\_REASON\_MIN naming the floor, with the control that a sufficient reason writes the row\; AC2 r1 (drop the reason-floor check in coverage rule) killed by test\_a\_ruled\_line\_passes\_and\_the\_ruling\_is\_in\_the\_depth\_field / on the qa verdict: AC7 r0 flips the fallback literal in transition.py to block, killed by the behavioural test test\_the\_shipped\_default\_is\_report\; AC7 r1 flips the template's line\_coverage value, killed by a text assertion on the value and the adoption sentence; #2 -> on the engineering verdict: the cutoff is read by line\_coverage\_cutoff as a date string compared against the Created field, never through parse\_cutoff\; AC5 r3 (compare as a string prefix so a truncated cutoff exempts the dates it prefixes) killed by test\_units\_created\_before\_the\_cutoff\_are\_exempt / on the product verdict: the report-mode absence line names the route (pip install coverage, or review.line\_coverage: off) and report stays silent when no run state exists at all, so an upgrading project with no run sees nothing\; AC6 r0 killed by test\_a\_missing\_coverage\_module\_never\_reads\_as\_covered / on the qa verdict: AC4 gained rows for the unknown value (AC4 r1, treat an unknown value as off, refused by name) and for collection under off (AC4 r2, call the collector under off and print nothing, killed by a spy on the collector) in test\_the\_mode\_decides\_refusal\_report\_or\_silence; #3 -> on the engineering verdict: the ruled-line count is written through verify\_ac's derive-and-seal path (depth\_facts lines\_ruled, render\_depth 'lines ruled N')\; AC2 r2 (omit the ruled-line count from the derived half) killed by test\_a\_ruled\_line\_passes\_and\_the\_ruling\_is\_in\_the\_depth\_field / on the product verdict: README.md left Affects (US0815 carries the README dependency sentence), and help/verify.md gained the run --coverage and coverage rule entries, named in Affects / on the qa verdict: the fixture writes the bare off, the spelling YAML 1.1 parses to False and line\_coverage\_mode special-cases, so the documented spelling is accepted; #4 -> on the engineering verdict: AC1 states the fixture plants a prior report claiming uncovered: 0 while the tree holds an uncovered line\; AC1 r0 (read the last report on disk) and r1 (drop the hash comparison) killed by test\_an\_uncovered\_added\_line\_refuses\_the\_terminal\_transition\_and\_a\_covered\_unit\_passes / on the product verdict: the BLOCKING line restates #1, closed by the reason floor\; product rejoinder APPROVE 1e5d7c9f4ac1 / on the qa verdict: AC1 split into two rows, r0 (read the last report on disk) and r1 (drop the hash comparison), with a seeded stale report reading uncovered: 0 for the uncovered unit\; both killed; #5 -> on the engineering verdict: confirmed, no change: unit\_run\_base\_ref reads the run whose batch names the unit open or closed, and AC8 r0 (read the base ref only while the run is open) is killed by test\_no\_base\_ref\_refuses\_under\_block\_and\_is\_reported\_under\_report / on the qa verdict: AC5 fixtures a unit created on the cutoff date and AC5 r2 (compare with <= so the on-the-date unit is exempt) is killed by test\_units\_created\_before\_the\_cutoff\_are\_exempt; #6 -> on the engineering verdict: the BLOCKING line restates #1, closed by the transition.py fallback row\; engineering rejoinder APPROVE 3d51f96302e8 / on the qa verdict: AC8 r1 (drop the batch-membership check so a run whose batch does not name the unit is read) killed by test\_no\_base\_ref\_refuses\_under\_block\_and\_is\_reported\_under\_report; #7 -> on the qa verdict: AC2 r2 (omit the ruled-line count from the derived half) killed by test\_a\_ruled\_line\_passes\_and\_the\_ruling\_is\_in\_the\_depth\_field; #8 -> on the qa verdict: AC3's refusal names the ruling's file and line beside the word stale, asserted in test\_a\_ruling\_on\_stale\_bytes\_does\_not\_satisfy\_the\_gate; #9 -> on the qa verdict: the BLOCKING line restates #1 and #2, closed as above\; qa rejoinder APPROVE b06819f4f53d | - | plan-review | 504ad122ddf0 |
| BG0614 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | #1 -> the two catalogue bullets were restored to coherent prose in 82b57ae8, with the `stamps --staged` and `audit` sentences moved to each bullet's end\; markdownlint and the verb census both green; #2 -> `cmd\_audit` now reads through `ledger\_entries`, which refuses an unparseable ledger with its path named, and the verb reports that at exit 1 - pinned by AuditRemedyTests and by mutant AC2 r1, killed; #3 -> the summary figures are pinned on a second shape by test\_the\_summary\_figures\_follow\_the\_ledger\_shape, and mutants AC6 r1 and AC6 r2 (hard-coding 5 and 0) are both killed; #4 -> the fixture README now states that the three stale-entry keys pair a None row with a 0 row, that `register` writes 0 and None marks a pre-row-column row, and that a literal-value walk counts 16 where the audit counts 19; #5 -> the fragment carries one dated measurement by the shipped verb: 21 duplicated keys on 2026-09-08, 18 once this run's own three were withdrawn; #6 -> a missing target is tagged like a stale one and pinned by test\_a\_row\_whose\_target\_is\_missing\_is\_tagged\_like\_a\_stale\_one, with mutant AC5 r1 (tag only `stale`) killed; #7 -> closed by the same repair as finding 2: the unreadable ledger is refused at exit 1 with the path named, never read as clean | - | delivery | 69ca917b4282 |
| BG0653 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | #1 -> the fragment was rewritten in the round-two commit to name all four selector shapes and the HEAD comparison that reports rather than refuses a stamp already dead; #2 -> the module-level async arm is pinned by AC1's async case, which removes both isinstance tuples in one mutant and dies; #3 -> the no-readable-HEAD case is the reported-not-refused branch AC8 pins, and its mutant (delete the HEAD comparison) is killed\; a first commit reports every dead selector rather than refusing, which is the intended reading; #4 -> the revision row's handler count was corrected against the code when the D0183 figure was re-measured and the lines re-ruled on the current bytes; #5 -> the corpus re-measure is disclosed in the revision row and carried: the scheduled lane is named in the v5.1 bar, which the close records rather than claiming green; #6 -> pre-existing and disclosed: BG0569's surviving row is recorded in CR0511 and ruled not-stop-ship in RETRO0115; #7 -> pre-existing and report-only: the `stamps` catalogue entry is filed in CR0511 with the other two catalogue omissions and ruled not-stop-ship | - | delivery | 39dba21259e1 |
| US0815 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | #1 -> the ruling was WITHDRAWN on the record in f399dd0b rather than restated, because two seats reached the line with every tool intact - the withdrawal names that as its reason; #2 -> the fixture now spawns a traced child that finishes before the verifier hangs, so a real data file exists when the handler runs\; deleting the purge fails the test, which was verified by applying and restoring that exact mutant; #3 -> the comment and the commit message were both narrowed to what the line does: the pre-run purge covers the next run, and this one covers the run cut short; #4 -> the figure was corrected in the same commit against a re-measurement at the current bytes; #5 -> carried and disclosed: the four fixture gaps are non-blocking, none is a registered ledger row, and they are filed in CR0511 and ruled not-stop-ship in RETRO0115 | the Test Plan names 24 edits (story US0815 rows at :47-:53) but the ledger holds 22 standing rows plus one withdrawn - AC5's "replace it with uncovered" and AC7's "move the coverage install step below the suite step" have no ledger row (both die when applied, so the gap is paperwork, not a survivor); AC1's uncommitted-tracked-file clause unpinned (verify\\_ac.py:2208, test\\_verify\\_ac.py:6640); the pre-run stale-data purge unpinned (verify\\_ac.py:2287-2288, test\\_verify\\_ac.py:6816) | delivery | 26dcdaa892d2 |
| US0818 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | #1 -> the remedy is now printed with every join field shell-quoted and `--root` included, and AC2's test splits the printed line with `shlex` and RUNS it, asserting the row it names is withdrawn; #2 -> AC2's fixture now replaces with a drifted line and description first, so the printed values are the LIVE row's and not the caller's - two mutants swapping them are killed; #3 -> carried and disclosed: the `replaced the identical live row` wording when only the verdict and test match is filed in CR0511 and ruled not-stop-ship, with the replace key left as AC1 declares it; #4 -> carried and disclosed: the unpinned `return 0` after the printed line is filed in CR0511 with the wording finding and ruled not-stop-ship; #5 -> carried and disclosed: AC3's `plan\_execution` clause against its test's ledger assertion is filed in CR0511 and ruled not-stop-ship; #6 -> pre-existing: BG0614's duplicated keys are the corpus this batch's audit exists to name, disclosed in the retro and re-measured at each commit; #7 -> closed by the same repair as finding 2: the live-row-versus-caller behaviour is pinned by the drifted-row fixture, and both swapping mutants die | - | delivery | d40544912b93 |
| US0816 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | #1 -> `coverage withdraw` now rewrites the row matched on its own file, line and hash rather than on the reason text, pinned by a fixture with two rows sharing one reason where only the row named is withdrawn; #2 -> the same rewrite reads the escaped cell back, so a reason carrying a pipe is withdrawn rather than silently left live\; the fixture rules with a pipe in the reason and withdraws it; #3 -> AC3 was amended in f399dd0b to say what shipped: a stale ruling satisfies nothing, blocks nothing, and is named beside the withdrawal command; #4 -> the two AC10 rows carry the AC10 selector as their test, which is the verifier that kills them\; the criterion column is corrected by the same re-registration that measured them; #5 -> the config comment now states that only BG0614 predates the cutoff, and that BG0652 and BG0653 were created on it and are judged; #6 -> the transition docstring was corrected with AC3, so the code, its test, the help page and the criterion all say staleness blocks nothing; #7 -> carried and disclosed: the sweep reads uses inside a function, no live instance exists in the corpus, and the gap is filed in CR0511 and ruled not-stop-ship; #8 -> carried and disclosed: both round-one non-blockers are filed in CR0511 and ruled not-stop-ship in RETRO0115; #9 -> the revision row's two module figures were replaced with the verdict they support, because a per-module count moves as later commits add to the file; #10 -> the AC3 plan row's file reference was corrected to verify\_ac.py with the criterion amendment; #11 -> closed by the same repair as findings 1 and 2: the pipe case round-trips through the CLI and the blank-error path is gone with the assertion it came from; #12 -> closed by the same amendment as finding 3: AC3 now states what the code and its test do | - | delivery | 3a9c1a33964e |
| BG0614 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | mutation.py:2905 audit exits 0 silently on an unreadable led -> closed in 82b57ae8: `cmd\_audit` reads through `ledger\_entries`, which refuses an unparseable ledger with its path named, and the verb reports that at exit 1 - pinned by test\_an\_unparseable\_ledger\_is\_refused\_rather\_than\_read\_as\_clean and mutant AC2 r1, killed | reference-scripts.md:178 the `audit` sentence is spliced into the middle of the mutation.py entry ("verify\\_ac `audit` names ... ledger." then "confirms an AC's tests pass") so the catalogue entry no longer parses as prose; reference-scripts.md:177 the `stamps --staged` sentence is appended onto the verify\\_ac.py entry's dangling "updates each AC's" fragment; reference-scripts.md:177 that verify\\_ac.py entry was already truncated at 2c6ffef2; mutation.py:2895 docstring says a single-row criterion registers without a row number but register\\_mutant writes 0 (mutation.py:2083) and the None rows are pre-row-era (49 live on this ledger) - behaviour correct, prose wrong; changelog.d/BG0614.md says nineteen keys while the shipped verb read 21 then 18 on delivery - ledger gitignored so UNVERIFIABLE which walk produced 19; reference-scripts.md:177-178 both catalogue entries spliced mid-sentence | delivery | 69ca917b4282 |
| BG0653 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | a fresh checkout of HEAD reads dead-stamps 9 not 3 (US0357/U -> disclosed and carried: the corpus reading is a property of the developer's tree, recorded in the revision row with the command that produced it, and the scheduled corpus lane is named in the v5.1 bar rather than claimed green; AC1 `-k` shape blind with a false `every one still resolves` -> closed: AC1 exercises the `-k` shape as its own case, and the mutant deleting the `kpat` branch of `\_selector\_live` is killed; AC1/AC7 pre-existing dead stamps refuse unrelated commits as -> closed in 79f306b3: `staged\_stamps` compares against HEAD and reports a stamp already dead at HEAD rather than refusing it, pinned by AC8 whose mutant deleting the comparison is killed; changelog.d/BG0653.md omits the `-k` shape, the already-dead -> closed in the round-two commit: the fragment names all four selector shapes, the already-dead-at-HEAD report and the rename wording | AC1 staged\\_stamps (verify\\_ac.py:3494-3496) never reads a `path -k name` selector - the corpus holds 324 stamped ones (a fourth shape beside the three AC1 names) and the corpus sweep resolves them by \\_k\\_selects (verify\\_ac.py:875-878), but the staged lane passes any of them while the file is in the index and prints "every one still resolves": renaming the friendly\\_alias nodes in test\\_github\\_sync.py and staging gives `stamps --staged` exit 0 over 10 selectors while `stamps --story US0057-*.md` reports 2 dead, so the bug's own failure mode (a rename passing every per-commit lane) survives for 13% of the stamped corpus and the shipped helper already exists to close it; staged\\_stamps carries no tolerated baseline and words every dead stamp as "orphaned by this commit" (verify\\_ac.py:3497-3499) - staging test\\_mutation.py for any reason is refused on BG0357 AC4 and AC5, dead before this commit and inside the corpus lane's baseline of 3 (touched once in the last 60 commits, so low frequency, remedy is to repoint or unstamp those two or consult the baseline); the delivery revision row's "0.32 s with three staged test files and 119 stamped selectors" names no files so the count cannot be reproduced - the three test files the unit touched give 0.36 s and 129 selectors, the order of magnitude and the 0.07 s no-test figure confirmed; CR0511 BG0569 AC4's row on .githooks/pre-commit SURVIVES its test, disclosed in the commit message and the ledger; the `stamps` subcommand has no reference-scripts.md entry so `--staged` is catalogued only in AGENTS.md and the hook (no id, report only); AC1 staged\\_stamps never reads a `path -k name` selector (verify\\_ac.py:3494-3496) - 324 stamped selectors pass unjudged under a lane that prints "every one still resolves" | delivery | 39dba21259e1 |
| US0815 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | the Test Plan names 24 edits (story US0815 rows at :47-:53) -> closed: the Test Plan and the ledger were reconciled at delivery - 30 plan edits, 30 live rows, with the one equivalent mutant withdrawn on the record and its reason stated; AC1's uncommitted-tracked-file clause unpinned (verify\\\_ac.p -> closed in a49a5244: AC1's fixture leaves an uncommitted dead function in the TRACKED module and expects it listed\; the mutant deleting the zero-sha branch fails the test; the pre-run stale-data purge unpinned (verify\\\_ac.py:2287-22 -> closed in a49a5244: AC6 seeds a REAL coverage data file from an earlier run in which a sibling executed the dead line\; deleting the pre-run purge fails the test; fixture gaps where the wrong implementation survives and HEA -> carried and disclosed: the four fixture gaps are non-blocking, none is a registered ledger row, and they are filed in CR0511 and ruled not-stop-ship in RETRO0115; silent `uncovered: 0` exit 0 when the present coverage modul -> closed in a49a5244: coverage's own `combine` and `json` verbs must succeed, an unreadable JSON report is refused, and the mutant ignoring their exit codes is killed; `affects\\\_hash` asserted by length only (test\\\_verify\\\_ac.py -> closed in a49a5244: AC4 changes a file's bytes and expects the hash to move, and the mutant hashing the names alone is killed; no fixture for uncommitted lines in a tracked Affects file ( -> closed by the same fixture as finding 2: an uncommitted edit to the tracked module is listed, and the zero-sha mutant dies; the timeout handler's purge unpinned (verify\\\_ac.py:2311-231 -> closed in f399dd0b: the fixture spawns a traced child that finishes before the verifier hangs, so a real data file exists when the handler runs - deleting the purge now fails the test; the Coverage Rulings reason for verify\\\_ac.py:2312 refuted b -> closed in f399dd0b: the ruling was WITHDRAWN on the record rather than restated, naming the seats' refutation as its reason; the false Coverage Rulings row waiving verify\\\_ac.py:2312 (s -> closed by the same withdrawal as finding 9: the row is retracted on the record and the line is covered rather than waived | AC1's clause "plus the working tree's uncommitted changes" is unpinned for a TRACKED file - deleting the zero-sha branch at .claude/skills/sdlc-studio/scripts/verify\\_ac.py:2208 survives all six LineCoverageTests selectors while an uncommitted edit to prod/mod.py then reads "0 added statement(s), uncovered 0, exit 0" against the shipped code's "2 added, 1 uncovered, exit 1" (test\\_verify\\_ac.py:6640 exercises uncommitted lines only through an UNTRACKED file, which the blame-failure branch at verify\\_ac.py:2194-2201 handles instead); the pre-run purge of stale data files at verify\\_ac.py:2287-2288 is unpinned - deleting it survives AC6 (test\\_verify\\_ac.py:6816 seeds a text file that `coverage combine` cannot read) while a real data file left by an interrupted earlier run is combined into the current one and a line no current selector executes reads covered (isolated: exit 0 with the mutant, exit 1 shipped); `subprocess.TimeoutExpired` is uncaught on the coverage path - verify\\_ac.py:2291, :2296, :2299 (and :2169, :2192 for the git calls) let a selector exceeding the 900 s timeout escape `coverage\\_report` as a traceback rather than the CoverageUnavailable refusal cmd\\_run catches at :2501, and the escape skips the post-read purge at :2307, leaving the stale data file the second finding guards against (demonstrated in-process with timeout=1); help/verify.md:130-131 says the whole sentence `coverage: not measured - the coverage module is absent` is printed when the module is below 7.10, but the lane prints `coverage: not measured - coverage 6.5.0 on <python> is below the floor 7.10 that subprocess patching needs` (executed against a stub 6.5.0); AGENTS.md:256 describes a `review.line\\_coverage` gate with `block` and `report` modes, and verify\\_ac.py:2251 tells the user to set `review.line\\_coverage: off`, while nothing at HEAD reads that key (`grep -rn line\\_coverage` finds only US0816's story) - a claim ahead of the code that ships it | delivery | 26dcdaa892d2 |
| US0816 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | AC3 still says the gate refuses on a stale ruling while the -> closed in f399dd0b: AC3 now says what shipped - a stale ruling satisfies nothing, blocks nothing, and is named beside the withdrawal command - and the transition docstring and help page say the same | a unit with ZERO uncovered added lines is refused at Fixed once earlier rulings go stale on a later edit to the same file, and the only printed remedy - "rule again on the current bytes" (transition.py:978-979) - tells the consumer to re-assert a waiver the tool's own measurement contradicts, writing false rows into the tracked artefact and inflating `lines ruled`. Executed: rule src/thing.py 6,7,8, rename inside that file, add a test reaching every arm, `run --coverage` reads "0 uncovered" and `set BG0001 Fixed` still blocks on three stale rulings. No shipped verb withdraws a ruling - `coverage` takes `rule` alone (verify\\_ac.py:4998) and help/verify.md documents no removal - so the honest exits are hand-editing the artefact or `--force`, which switches off every other pre-write gate too. `mutation.py retract`, whose reason floor this borrows, exists for exactly this case; sdlc-studio/.config.yaml:106 claims "The four units Fixed before the gate landed carry a hand-collected `verify\\_ac run --coverage` figure in their revision rows" - three units reached Fixed in the run window (BG0614, BG0652, BG0653, by status diff 2c6ffef2..da7c3920), the unit's own AC5 says three bugs, and all six batch units carry a figure, so "four" matches no reading. The same comment says that batch "is judged by it rather than exempted" while BG0614 (Created 2026-08-25) is exempt under the 2026-09-07 cutoff the comment itself sets; the `report` line stutters its own count - executed output reads `coverage: 3 uncovered added line(s) - 3 uncovered added line(s) its own verifiers never executed - src/thing.py: 6, 7, 8` because transition.py:984 prefixes the figure to a `problems` string that already carries it; `coverage rule` accepts a line that was never uncovered and depth\\_facts counts every live row (verify\\_ac.py:4780), so a fully covered fixture seals `lines ruled 2` after two rulings on always-executed lines, which reads as two waived lines that waived nothing; help/verify.md:147-150 attributes the depth write to the transition ("The terminal transition subtracts live rulings ... and counts them in the derived half of `Verification depth`") - executed, the transition never writes the derived half, the field stayed plain `functional` after a passing Fixed and a passing Done, and AC2 correctly names verify\\_ac's derive-and-seal path instead; the stale-ruling refusal's only printed remedy instructs a false waiver and no shipped verb withdraws a ruling (transition.py:978-979, verify\\_ac.py:4998); sdlc-studio/.config.yaml:106 claims four units Fixed where three reached Fixed and AC5 says three | delivery | 3a9c1a33964e |
| US0818 | 2026-09-08 | Claude Fable 5.1 | 2026-09-08 | the withdrawn-skip at mutation.py:2157 that keeps the retrac -> closed in 9d468790: AC2 follows the retract-then-re-register route to its end, and the mutant dropping the withdrawn skip is killed; the live-row-versus-caller behaviour the repair adds has no -> closed in 9d468790: AC2 replaces with a drifted line and description first, so the printed values are the live row's\; both swapping mutants are killed | the refusal's printed remedy `mutation.py retract --reason <why>` fails as printed (argparse: --unit, --criterion, --target, --line, --mutant, --verdict required) and the refusal omits the live row's `line` and `mutant` description, two of retract's six join fields that BG0614's audit remedy prints - after a same-verdict registration with a drifted mutant text or line (which REPLACES silently) the reader's own values no longer match and `retract` refuses "no ... matches" (.claude/skills/sdlc-studio/scripts/mutation.py:2166); the retract-then-re-register route the refusal names is unpinned - dropping `not m.get("withdrawn")` at mutation.py:2156 closes it (verified alone via the CLI: retract exit 0, then register exit 2) while all 253 test\\_mutation.py tests and 13 retract-related tests in six other modules stay green (test\\_mutation.py:5413 never completes the correction); the `replaced the identical live row` line prints when the mutant text or line differs from the live row - per CR0568's key that replace is correct, the word "identical" is not (mutation.py:3043); claim drift: "six suite fixtures" take the legacy-append path in the revision row and commit message, execution counts four (five call twice statically) (sdlc-studio/stories/US0818-mutation-py-register-replaces-the-live-row-when.md:51); AC3 names `plan\\_execution` and its Verify selector asserts the ledger row count instead - the claim is TRUE by my execution (from-plan reads 1 row after 3 identical registrations) but the test never calls it (test\\_mutation.py:5442); the refusal's remedy fails as printed and omits the `line` and `mutant` join fields (mutation.py:2166); the retract-then-re-register route is unpinned (mutation.py:2156) | delivery | d40544912b93 |
| BG0490 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC2 split. The invocation limb is now AC3 and its mutant deletes the `\_INVOCATION` branch's script-token extraction, which the new node dies on because the cell's script no longer resolves.; #2 -> The two sites are now separate criteria: AC2 owns the `\_PATH\_CELL` extension allowlist, AC3 owns the invocation branch, each with its own mutant.; #3 -> New AC4 is the positive control - a templated cell and a prose cell naming no path stay exempt\; its mutant deletes the `\_TEMPLATED` branch, so a classifier reporting every cell fails it.; #4 -> AC2, AC3 and AC4 moved to `LoadingGuideExemptionTests`\; AC1 stays in `AuditProfilePathsTests`, the class for the surface it is about.; #5 -> AC3 renumbered AC5 and its mutant now reinserts the absolute phrasing into the docstring while `root.rglob` stays in the resolver, so the assertion is on the disagreement rather than on the sentence.; #6 -> AC1 now names the rule rather than a count: the signature row that NAMES A SCRIPT, and its mutant renames that script to one not on disk.; #7 -> Closed by the AC2 split above - the surviving mutant is now the allowlist narrowing, which the new node dies on.; #8 -> Added as AC2's own row: narrow the `\_PATH\_CELL` extension alternation back to the six shipped suffixes.; #9 -> Added as AC4, and AC3's criterion text states in terms that the cell KEEPS its `invocation` kind, so the shipped exemption test's contract is unchanged and the conflict is ruled rather than ignored. | - | plan-review | da852e7d77e2 |
| BG0493 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC4's mutant now edits `.githooks/pre-commit` and deletes the block that invokes the practice-rules module, which is where the wiring lives.; #2 -> New AC5: on a tree the checker refuses the lane refuses too, and on a tree it accepts the lane passes. Its mutant swaps the lane body for a bare echo naming the module path.; #3 -> AC4's node renamed `test\_the\_checker\_is\_named\_by\_a\_lane`, and the execution claim moved to AC5's own node, so the class name matches what each node asserts.; #4 -> AC2's mutant now moves the lane-check block back above the comment header, so the slice lands in commentary whatever window bound is in force\; the criterion states the property rather than the number.; #5 -> Accepted and stated: AC1's criterion now says the CALL is deleted, distinguishing it from the docstring mention at line 8 that satisfies the assertion today.; #6 -> Accepted. AC1's mutant is unchanged because it is the one that reproduces - reading the file's text rather than the imported module is exactly the shape that passes with the call gone.; #7 -> Accepted, not acted on: the existing positive control is left where it is rather than duplicated into this unit's plan. Recorded here so the reader knows AC3 leans on it.; #8 -> Closed by the AC4 mutant move above.; #9 -> Closed by AC5 above. | - | plan-review | bea2b94362a3 |
| BG0567 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC1 now names a PINNED pre-epic commit held as a constant in the test, and says in terms why the run's base ref cannot serve: the epic landed in August, so this run's base ref already contains it.; #2 -> The pin replaces the code-marker anchor rather than discarding it, and AC1's mutant is reverting to the copy-and-rewrite baseline, so the old form is what the test must die on.; #3 -> New AC5: an unresolvable pin SKIPS naming the reason. Its mutant deletes the resolvability check so an unresolvable pin raises.; #4 -> Covered by AC5, which names the skip and its reason as the required outcome.; #5 -> New AC4: an unchanged tree compares equal. Its mutant deletes the run-id substitution from `\_norm`, so a permanently red comparison fails it.; #6 -> AC2's mutant now names the edit: change the `retros` argument the baseline side passes to `\_proj`.; #7 -> AC2's criterion text now names both callers, including the dormant-gate one that wants no retros on either side.; #8 -> Accepted and not designed away: the checkout is per test class rather than per assertion, and the cost is carried knowingly. Recorded rather than hidden.; #9 -> Closed by the AC1 and AC5 changes above.; #10 -> Closed by AC4 above, whose criterion text also states that the normalisation was calibrated for a same-tree copy and must be re-judged for a cross-commit checkout.; #11 -> Closed by AC2 above. | - | plan-review | 9b9606a46c00 |
| BG0578 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC5's mutant is now removing the marker branch from `tools/test\_census.py`, an edit that exists\; the criterion pins the regression on `test\_cli\_grammar.py` rather than a phantom August edit.; #2 -> Accepted. The Evidence's attribution of the edit is left as recorded and the criterion no longer depends on it.; #3 -> `tools/tests/test\_cli\_grammar.py` added to `Affects`, and AC5 edits a COPY of it so the repo-writes lane is not tripped.; #4 -> AC2 now states both limbs: a single-script declaration decides, a multi-script one defers to counting, and the result says which route answered.; #5 -> AC1's criterion states the precedence explicitly - the marker decides whatever the mention counts say.; #6 -> AC4 rewritten: it now asserts the file is ATTRIBUTED by the marker or declaration route, which is false at HEAD, rather than asserting wording that already passes.; #7 -> Closed by the AC4 rewrite above.; #8 -> AC4's mutant now returns the unattributed result whenever the counts tie, before the declared routes are consulted - an unambiguous edit.; #9 -> AC6 rewritten onto the OWNER MAP rather than the unattributed count, so an owner flip is observable.; #10 -> Covered by AC6, whose claim is that NO file's owner changes under a prose-only edit - the non-tie flip included.; #11 -> Accepted: AC6 runs through the shipped entry point and supplies the junit report it requires.; #12 -> AC6 edits a COPY of the corpus, so no tracked file is modified and the repo-writes lane is not involved.; #13 -> Closed by the AC5 changes above.; #14 -> Closed by the AC2 rewrite above.; #15 -> Closed by the AC4 rewrite above.; #16 -> Closed by the AC6 rewrite above. | - | plan-review | fe2466776501 |
| BG0591 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> New AC5 states the extraction: `detect` must NAME its blocking set, and the criterion says in terms that it enumerates only the raw owed list today, so the comparison has to be made possible before it can be made.; #2 -> Closed by AC5 - the id set comes from the command's own output rather than from the test re-implementing `blocking()`.; #3 -> New AC4: a velocity-only fixture still announces a close, because `is\_owed` returns true on that limb alone. Its mutant narrows the advisory to the units key.; #4 -> AC3's text corrected to two subtractions with the override as a label on a repair, and it now demands two fixtures built through the repairs path.; #5 -> Accepted and removed. The claim about the `--root` spelling is gone from AC4\; the reviewer's execution refutes it.; #6 -> Closed by AC5 above.; #7 -> Closed by AC4 above.; #8 -> Closed by the AC3 correction above. | - | plan-review | 1ca9761a9ebf |
| BG0601 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC2's mutant now swaps the sweep's preview side for the BLIND scratch, so the sweep fails on a correct tree and AC2's own node dies on it.; #2 -> AC3's ordering premise dropped. The criterion is now about the sensitivity control comparing the same fields the sweep compares.; #3 -> Closed by the AC3 rewrite - the third field is `detail`, and AC1 is now explicitly about a divergence in it.; #4 -> AC3's mutant is now a code edit: narrow the sensitivity control to the state field while the sweep compares the whole answer.; #5 -> AC1's criterion states the comparison as the swept tuple itself, and its mutant reverts that tuple to `[:2]`, so the killability does not depend on how the node is wired.; #6 -> Closed by the AC1 restatement above.; #7 -> AC1's Given now names the shape that must exist: agreement on state and value with a difference in detail, which is the case the fixture must construct.; #8 -> The literal 22 is gone from AC2\; the criterion says every `\_ck\_` probe.; #9 -> Closed by the AC2 mutant change above.; #10 -> Closed by the AC3 rewrite above.; #11 -> Closed by the AC1 restatement above. | - | plan-review | e2108d52597a |
| BG0608 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC3 rewritten onto the half that IS false at HEAD: the drift clause appended to a selected run with no width on either figure. Its mutant hoists the baseline block above the selected test.; #2 -> AC4 rewritten: when no test count is recorded, the total that is still printed must say the width is unrecorded. The Given can no longer be deleted away.; #3 -> Closed by the AC4 rewrite above.; #4 -> Closed by the AC4 rewrite - the uncovered branch is now the criterion's subject.; #5 -> `sdlc-studio/.config.yaml` removed from `Affects`\; no criterion touches it.; #6 -> AC2 rewritten to be about the same leading rate clause AC1 defines, which does not exist at HEAD, so both halves are false there.; #7 -> AC1's mutant now names a construct that exists: move the rate-verdict concatenation back below the drift block.; #8 -> Closed by the AC3 rewrite above.; #9 -> Closed by the AC4 rewrite above. | - | plan-review | 0a903ac9ab5e |
| BG0612 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC2's node moved to `test\_sprint\_report.py::ChecklistRosterTests` and its mutant is now the production edit - drop the `known-issues` entry from the CHECKLIST literal.; #2 -> Closed by the AC2 mutant change above.; #3 -> AC2, AC3 and AC4 all moved to `test\_sprint\_report.py`\; only AC1, which is about `\_EDIT\_VERBS`, stays in `test\_verify\_ac.py`. `Affects` updated to match.; #4 -> AC1 names exactly two verbs, `restore` and `keep`, and its mutant deletes those two entries.; #5 -> AC3's criterion now states the real gap: `\_resolve\_item` catches broadly and degrades a missing resolver to UNANSWERED at close time.; #6 -> AC3 now covers both - an absent resolver and an attribute that exists but is not callable - with a second row narrowing the validation to a `hasattr` test.; #7 -> Accepted and stated: AC4 is a control and discriminates only through its mutant, which raises unconditionally from the import-time validation.; #8 -> Closed by the AC2 changes above.; #9 -> Closed by the module move above.; #10 -> Closed by the AC1 change above. | - | plan-review | d790b025f3d5 |
| BG0627 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> Premise replaced. The criteria are re-framed onto the defect that reproduces: a non-string in a PROSE field is not refused by name, it crashes. A note at the head of the section records why the typed-field framing was withdrawn.; #2 -> Accepted. `points` is correctly presence-tested and is out of scope\; no criterion depends on it.; #3 -> AC3 now declares four rows, one per module, each bypassing the shared loader at that command's own call site, so a regression on any command path is measured.; #4 -> Closed by the re-frame: the criteria no longer assert behaviour on a required key that is already refused.; #5 -> That crash is now AC1's subject, and AC1's mutant deletes the type check so a bool reaches the string method.; #6 -> AC4's mutant no longer deletes its own node\; it narrows the sweep's predicate so a guard on a fields-file path is not matched.; #7 -> New AC5 is that control: an `or ""` that is not on a fields-file path still passes, with a mutant dropping the fields-file scoping.; #8 -> Accepted. The raw-grep counts are withdrawn from the criteria\; nothing is pinned to them.; #9 -> AC1 names `5` alongside `false` and `0`, so the non-falsey non-string takes the same route.; #10 -> Closed by the re-frame above.; #11 -> Closed by the four per-module rows above.; #12 -> Closed by the AC4 mutant change above.; #13 -> Closed by AC5 above.; #14 -> Closed by the re-frame above\; the AttributeError is ruled on as AC1. | - | plan-review | f6687a102cf6 |
| BG0630 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC4 is now AC5 and states a decided behaviour: the change does not reopen the 12 bugs already at Fixed, and a reopened-and-refixed bug is REFUSED until its rejection is answered.; #2 -> Closed by the AC5 rewrite - the assertion is now defined, and its mutant skips the gate whenever the current status is already terminal.; #3 -> Re-measured on the plan-review ledger at kind test-plan and corrected in the criteria: 12 bugs at Fixed with a standing rejection, listed by id.; #4 -> The 167/155 figures are withdrawn. A note at the head of the criteria records that they came from the delivery ledger, which this gate never reads, and gives the plan-review figures: 77 ever rejected, 63 terminal.; #5 -> New AC2: a DIRECT route to a terminal status is refused too, with the measurement that both `In Progress -> Fixed` and `Open -> Fixed` exit 0 today.; #6 -> AC1 states the terminal statuses in both vocabularies - Done for a story, Fixed for a bug - so the target set question is answered on the page.; #7 -> The Summary's claim is superseded by the measurement recorded in the criteria note\; no criterion rests on it.; #8 -> AC1 and AC4 now name both vocabularies rather than only Done.; #9 -> Stated: AC3 says both routes are green at HEAD and each discriminates only through its mutant.; #10 -> AC4's mutant now names the call site - the terminal-route one - rather than 'the cutoff test'.; #11 -> Closed by the AC5 rewrite above.; #12 -> Closed by the re-measurement above.; #13 -> Closed by AC2 and AC1 above. | - | plan-review | 90953fefbb25 |
| BG0633 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> Both spliced selectors deleted from the criterion prose. Each criterion now carries one runnable node, on its `Verify:` line, and the Test Plan Title column was re-derived from the cleaned text.; #2 -> Closed by the deletion above - no backticked node paths remain.; #3 -> Accepted, and it is a real gap in `verify\_ac lint`: it reads only the `Verify:` sub-bullet. Recorded here rather than fixed inside this unit's scope.; #4 -> New AC3: a differently-cased `--field severity` is guarded too, and the file gains no second severity line. Its mutant keys the guard on the raw field argument rather than the lowercased key.; #5 -> Closed by AC3 above.; #6 -> AC2's mutant now names the value the criterion names, replacing the normalisation with an exact-match test so `high` is refused rather than written as `High`.; #7 -> Accepted, not acted on: the duplicated `normalise\_severity` in `file\_finding.py` and `artifact.py` is out of this unit's scope and is left as a finding on the record.; #8 -> Accepted, not acted on: the unchecked vocabulary on other fields is a wider defect than this unit, recorded rather than folded in.; #9 -> Noted - the premise stands and no change was needed.; #10 -> Closed by the deletion above.; #11 -> Closed by AC3 above. | - | plan-review | 78e7d1b73c37 |
| BG0637 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> New AC2 is the outside-a-span half, with its own mutant deleting the underscore escape from `\_clean` altogether, so the one-token implementation is measured.; #2 -> Closed by AC2 above.; #3 -> New AC3: cleaning an already-cleaned value changes nothing, with a mutant removing the already-escaped test.; #4 -> New AC4: a pipe or newline inside a code span is still neutralised, with a mutant hoisting those replacements into the outside-a-span branch.; #5 -> Closed by AC4 above.; #6 -> New AC6: an even backtick count is written through unchanged, with a mutant stripping every backtick.; #7 -> Decided in favour of REFUSING at the write, which is what the Proposed Fix argues for. AC5 now says the write is refused naming the value, and its mutant is the silent auto-balance.; #8 -> Closed by the AC5 decision above.; #9 -> Noted - the figures stand and no change was needed.; #10 -> Closed by AC2 above.; #11 -> Closed by AC3 above.; #12 -> Closed by AC4 above.; #13 -> Closed by AC5 and AC6 above. | - | plan-review | bd2a981fbf7a |
| BG0654 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC2 is now AC3 and its mutant edits `.githooks/pre-push`, stripping the keepalive sentence from the cost line, which is the one production line the criterion adds.; #2 -> AC3's criterion text states that the fixture copies the hook alone and never runs the enabling script, so the claim rests on the hook's own text.; #3 -> AC1 split. AC1 keeps the sets-when-absent claim\; new AC2 is leaves-an-existing-one-alone, with its own mutant dropping the read-first guard to make the write unconditional.; #4 -> AC1 and AC2 now require an environment scrubbed of GIT\_CONFIG\_GLOBAL and GIT\_CONFIG\_SYSTEM, so the developer's own config cannot answer for the clone.; #5 -> Both criteria now say LOCAL config, read through `git config --local`.; #6 -> Accepted, not acted on: no criterion pins the AGENTS.md paragraph. It stays in `Affects` as a documentation obligation carried by the delivery rather than by a criterion.; #7 -> Closed by the AC3 mutant move above.; #8 -> Closed by the AC1 split above.; #9 -> Closed by the scope and scrubbing changes above. | - | plan-review | 12d8f780393e |
| BG0657 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC1 rewritten. There is no baseline writer, so the criterion is now about the identity list SURVIVING the elision on the way out of `gate.py`, with a mutant wrapping that payload in `\_elide`.; #2 -> `gate.py` added to `Affects`, and AC1 makes the full list obtainable before anything downstream records it.; #3 -> AC6 requires the lane to run on an unchanged tree and exit 0 with the committed baseline still parsing, so a hand-written fixture alone cannot satisfy the set.; #4 -> Closed by AC6 above.; #5 -> New AC4: an equal-sized swap exits NON-ZERO and names both sides, with a mutant inserting an early exit 0 as soon as the counts agree.; #6 -> AC2 now requires each metric's count to agree with the length of its identity list, with a mutant removing that comparison.; #7 -> AC2 names both metrics, `red-criteria` and `dead-stamps`.; #8 -> AC6 is the compatibility control: the committed baseline must still name both metrics in the shape the existing parse reads, with a mutant renaming the metric keys.; #9 -> AC2's criterion text names the discriminator: the red clause alone, not the 67-row exclusion ledger printed after it.; #10 -> AC6 serves as the positive control for the set - an unchanged tree exits 0 - so a detector flagging every absent identity fails it.; #11 -> Closed by the AC1 rewrite above.; #12 -> Closed by bringing `gate.py` into `Affects` and by AC1.; #13 -> Closed by AC4 above.; #14 -> Closed by AC2 above. | - | plan-review | 081a64a10ac7 |
| BG0490 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC3 no longer claims the shipped contract is unchanged. It now rules the conflict: that fixture's command names a script not on disk, so it is amended in the same change to name one that exists, and its kind assertion is kept.; #2 -> AC4 replaced. The templated cell cannot move the observable, measured, so the control is now an invocation whose script IS on disk, with the mutant emitting a finding for every invocation without testing the script.; #3 -> AC1 now asserts AT LEAST ONE script-naming row exists as well as that every named script resolves, and its mutant replaces that row with a manual note, so the vacuous case fails.; #4 -> Closed by the AC3 ruling above.; #5 -> Closed by the AC4 replacement above. | AC1 - the "one real row" is not identified by a rule: templates/audit-profiles/code.md carries two command-shaped signature cells (line 17 `rg -ni ... .` and line 19 the verify\\_ac.py path), and nothing in tools/ reads audit-profiles at all, so a pin that hard-picks one row reads the wrong row after any row is added or reordered; BLOCKING: AC2 - the mutant that cannot kill; AC2 - the missing `\\_PATH\\_CELL` extension-allowlist row; AC2 - the missing positive control and the unacknowledged conflict with test\\_templated\\_and\\_invocation\\_cells\\_are\\_classified\\_OUT\\_explicitly | plan-review | da852e7d77e2 |
| BG0493 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC2's mutant is now on the test side: revert the assertion to slicing a fixed prefix after the keyword. The hook-relocation row is dropped, because it kills nothing once the anchor is the fix.; #2 -> Closed by the same change - the anchor is the fix target, stated in the criterion.; #3 -> The fix target is decided: the assertion's anchor, not the hook's layout. The criterion says so and the hook row is gone.; #4 -> AC4 and AC5 moved to `tools/tests/test\_precommit\_lane\_order.py::PracticeRulesLaneTests`, the module that hosts lane-wiring claims.; #5 -> AC1 now states it: the existing verifier hard-codes the tracked path, so the new node drives a COPY with the call removed.; #6 -> AC3's mutant now names the real edit - return 0 from `main()` on the missing-file path.; #7 -> Closed by the AC2 change above. | BLOCKING: AC4 - the mutant that mutates tools/best\\_practice\\_rules.py when the wiring under test lives in .githooks/pre-commit and package.json, so it cannot redden the named node; AC4 - the missing execution case, an enumeration-only criterion that a dead lane satisfies | plan-review | bea2b94362a3 |
| BG0567 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC4's mutant replaced: move the pinned constant forward to a commit that already contains the softening epic, so the control reddens on a baseline that is no longer pre-epic.; #2 -> AC4 now records the measurement rather than gesturing at it: no compared string carries a run id or a temporary path, so the existing normalisation is dead here and the pin is what the control turns on.; #3 -> AC2 now says the observable is the CALL SHAPE, with the measurement that the pinned tree's output is byte-identical either way.; #4 -> Adopted. AC1 now specifies `git archive` piped through `tar` and says why a worktree is not used.; #5 -> Closed by the AC4 mutant replacement above.; #6 -> Closed by the AC4 text above. | AC2 - missing case: the helper has TWO callers, and only one is described - test\\_a\\_dormant\\_gate\\_is\\_unchanged\\_by\\_the\\_softening (:4603) legitimately wants retros=0 on both sides, so parameterising the helper to satisfy AC2 silently changes what the dormant-gate test compares, and no row or criterion protects it; AC1/AC3 - cost is unaccounted: a base-ref worktree checkout per assertion, three assertions plus two existing callers, on a suite already over its per-commit budget, and the plan does not say whether this binds per commit or at the boundary; BLOCKING: AC1 - name the ref (the pre-softening commit, not "the run's base ref") and say what happens when it cannot be resolved, or the test compares the tree with itself and cannot fail; AC3 - add the positive control that no unrelated change yields no reported difference, and state the normalisation that makes a cross-ref comparison stable; AC2 - the missing row for the second caller of \\_capture\\_with\\_softening\\_disabled | plan-review | 9b9606a46c00 |
| BG0578 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> Path corrected to `.claude/skills/sdlc-studio/scripts/tests/test\_cli\_grammar.py` in AC5 and in `Affects`\; the phantom entry is gone.; #2 -> AC3's mutant restored to the killing one: delete the counting fallback so a file with no declared subject is dropped.; #3 -> AC1 now states precedence against the by-name route explicitly, with the measurement that 95 of 204 files are placed by it.; #4 -> AC5 and AC6 now say the copy is made into a temp tree carrying the sibling module directory, and give both reasons the other readings fail.; #5 -> AC6 now says the junit report is generated over the copied tree.; #6 -> Closed by the path correction above.; #7 -> Closed by the AC3 mutant restoration above. | AC4 - the mutant is ambiguous: `max(counts.values())` is present today and is not the defect - the defect is `if len(tops) > 1: return None` at test\\_census.py:164 - so "revert to max(counts.values()) as the decisive rule" describes something already in the tree and may change no behaviour at all; AC6 - wrong observable, so the bug's own title is unpinned: the ratchet counts unattributed FILES (tools/tests/test\\_test\\_census.py:272, UNATTRIBUTED\\_BASELINE = 38), and an owner FLIP from A to B leaves that count unchanged, so AC6 cannot see "mentioning one more module silently changes a file's owner"; no criterion covers the non-tie flip (mention B three more times and B overtakes A), which AC3 explicitly preserves by keeping counting as the fallback - AC4 covers only the tie; AC6 - unimplementable as stated: every shipped subcommand of tools/test\\_census.py requires --junit (build\\_parser: report and candidates both required=True), so "driven through its shipped entry point over the real corpus" needs a junit report of the whole suite inside a unit test, or a new verb the plan does not name; and "a prose-only edit is made to a test file" over the real corpus either modifies a tracked file, which the repo-writes lane refuses at commit time, or is made on a copy - which the row's own mutant ("swap the ratchet's target to a fixture tree") forbids without distinguishing a synthetic fixture from a relocated real corpus; BLOCKING: AC5 - the mutant names a non-existent August edit to tools/test\\_census.py, and the criterion cannot be delivered inside the unit's declared Affects; AC2 - the missing multi-script and multi-declarer branch, which is the actual state of the file AC5 names; AC4 - the criterion is already satisfied at HEAD as worded, and its mutant does not name a behaviour-changing edit; AC6 - the ratchet count cannot observe an owner flip, and no criterion pins the non-tie flip the bug's title describes | plan-review | fe2466776501 |
| BG0591 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> The Fixture invariant section is rewritten. AC4 is now the stated exception to the difference rule, with an empty units limb and an outstanding velocity row, which is the only shape where the narrowed predicate goes silent.; #2 -> Closed by the same rewrite - AC4's fixture is no longer built to the difference rule.; #3 -> The three-limbs claim is gone from the invariant section too: it now says two subtractions with the override as a label on a repair, and AC3's two fixtures are both built through the repairs path.; #4 -> Noted, no change needed.; #5 -> Noted, no change needed.; #6 -> Noted, no change needed.; #7 -> Closed by the section rewrite above. | AC3's three-limbs premise (restate as two subtractions with the override as a label on a repair, so the fixture is constructible) | plan-review | 1ca9761a9ebf |
| BG0601 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC3 replaced. The sensitivity control cannot serve, measured, so AC3 is now about the roster being resolved off the module at run time, with the mutant hard-coding it as a literal tuple.; #2 -> AC1 now says the probe is SYNTHETIC and registered for the test, and records the measurement that no shipped probe diverges that way.; #3 -> Noted, no change needed.; #4 -> Accepted: the Proposed Fix's set-comparison wording is superseded by the criteria, which are the law. Recorded here rather than silently rewritten.; #5 -> Closed by the AC3 replacement above. | unless it is extracted to a shared helper or the new node drives that method directly, the natural implementation re-implements the comparison and reverting `[:2]` SURVIVES - the exact anti-pattern the class's own comment at test\\_sprint.py:16406-16410 forbids; AC1 - no row establishes that any real probe can diverge at the third element while agreeing on the first two, so the strengthened sweep may have no live witness and the fix be a no-op on the corpus; AC2 - "22" is a literal the sweep does not currently hold (it asserts > 15), so pinning it makes the node brittle to the next probe added while leaving the declared mutant alive if it is not pinned; BLOCKING: AC2 (the mutant must be one the named node can die on - assert the probe ROSTER or exact count as the outcome, or replace the mutant); AC3 (the Given does not reproduce against a 3-tuple return - re-state the criterion against the actual third field, and add a mutant for the DECISION, not only for the docstring); AC1 (state whether the comparison is extracted to a shared helper or the new node drives the existing method, so the mutant is killable) | plan-review | e2108d52597a |
| BG0608 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC4's Given narrowed to the measured shape: no test count recorded AND no rate ceiling declared, which is the only combination false at HEAD.; #2 -> Noted, no change needed.; #3 -> Noted, no change needed.; #4 -> Noted. AC2 is about the leading rate clause the fix introduces, not the existing prefix, and the criterion says both halves are false at HEAD.; #5 -> Accepted: the Proposed Fix's config demand is superseded by the criteria and the file is out of `Affects`.; #6 -> Closed by the AC4 narrowing above.; #7 -> Closed by the AC4 narrowing above. | BLOCKING: AC3 (add a mutant for the percentage-since-baseline clause, which is the half that is false at HEAD, and drop or re-scope the totals-comparison mutant that BG0594 already killed); AC4 (state whether the seconds total survives - a criterion that can be satisfied by deleting its own Given measures nothing - and add the no-recorded-test-count branch) | plan-review | 0a903ac9ab5e |
| BG0627 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC1's mutant moved to `resolve\_prose\_fields`, where the prose keys are known, so the check lands where the split exists.; #2 -> AC2 now names the typed fields that must survive - a `points` integer, an `acs` list, a `verify` list, a `line` number - and its mutant hoists the refusal up to the loader where it would see them.; #3 -> A fifth row added for `file\_finding.cmd\_file`, the call site that reaches the loader without the prose resolution.; #4 -> Closed by the fifth row above.; #5 -> Evidence line numbers corrected to the measured 825 and 1086.; #6 -> Closed by the AC1 and AC2 changes above. | AC3 has no positive control - nothing pins that the check PASSES on a legitimate `or ""` that is not a fields-file consumer (sprint.py alone carries 35 `.get(...) or ""`, and decisions.py:355/374/389 read ledger records), so a check refusing every `or ""` in the tree satisfies it; the Evidence enumeration is a raw grep of `.get(...) or ""` (measured today 1/4/2/1/18) rather than fields-file consumers, two of its four line numbers are stale at HEAD (handoff.py:825 not :764, validate.py:1086 not :1063), and decisions.py:355/374/389, handoff.py:786 and file\\_finding.py:2078 are not fields-file consumers, so the title's "eleven other fields-file consumers" over-counts; missing case - no row covers a non-string that is not falsey ({"rationale": 5}), which takes the same crash path; BLOCKING: AC1 (premise dead - name a typed field that exists, or restate the criterion as the prose-field rule the code actually has); AC4 (same dead premise, and one mutant for a five-command criterion); AC3 (mutant deletes the named test node; no positive control for a legitimate non-fields-file `or ""`); AC2 (already true at HEAD on `decision` - name the field where the behaviour changes, and rule on the AttributeError crash) | plan-review | f6687a102cf6 |
| BG0630 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC5's mutant replaced: skip the gate for any unit whose artefact already records a terminal status in its revision history. That is inert for AC1 and AC2 and fatal for AC5.; #2 -> Population re-measured under the gate's own reading - standing rejection AND no repair clearing it - and it is 16, not 12. The sixteen are listed, and the note says the first count was wrong in both directions.; #3 -> AC3's mutant re-sited to the branch that exists.; #4 -> The Revision History's withdrawn figures are superseded by the note at the head of the criteria, which names the ledger each came from. The history row is left as written rather than edited, and the criteria are the law.; #5 -> Closed by the AC5 mutant replacement above.; #6 -> Closed by the re-measurement above. | the Summary's claim that the gate fires "on a direct Ready -> Done" and the Evidence's correction of BG0629 ("a direct Ready-to-Done escapes the gate - it does not") are both false for the bug family, measured above; AC1 and AC3 are written in story vocabulary ("Done", "any unit reaches Done") while the population and AC4 are bugs at Fixed; AC2 and AC3 are both green at HEAD (verified: In Progress -> Fixed with an independent APPROVE exits 0 today, and with `review.test\\_plan\\_after` unset the gate already stands down), so neither can fail until the fix lands and each discriminates only via its mutant - acceptable for controls but state it; AC3's mutant "delete the cutoff test" does not name which of the two \\_plan\\_gate\\_active call sites it edits (transition.py:1096 is the planned-mutant gate, :1238 is this one); BLOCKING: AC4 (no stated behaviour - unfalsifiable, and its test can only mirror the implementation); AC4's "16" (measures 12 on the ledger the gate reads); the uncovered direct `Open -> Fixed` route and the undecided question of whether `Fixed` joins \\_IMPL\\_TARGETS - without it the fix closes one of two holes and the corpus AC4 cites is untouched | plan-review | 90953fefbb25 |
| BG0637 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> The row is re-authored without any pipe: the mutant now reads as hoisting the pipe and newline substitutions into the outside-a-span branch. The criterion's example command was also reworded, since it carried the pipe that split the Title cell. markdownlint is clean on the file.; #2 -> The Proposed Fix now reads check backtick PARITY and never rewrite the value, so the artefact no longer argues for balancing anywhere.; #3 -> Accepted. The Summary's earlier figures are superseded by the revision history's re-measurement, and no criterion rests on them.; #4 -> Noted, no change needed.; #5 -> Closed by the row re-authoring above.; #6 -> Closed by the row re-authoring above.; #7 -> Closed by the row re-authoring above. | these are different behaviours with different consequences (auto-balancing silently rewrites a reviewer's text and swallows the row's tail into a code span, refusing does not) and the criterion is law, so it currently mandates balance while every stated rationale argues for refusal; non-blocking - the revision history's figures reproduce EXACTLY with the greps the Steps name (494 / 512 / 571 spans and 943 doubles), \\_clean is a single function serving the verdict, evidence and repair writers (critic.py:254, :971, :1231) so AC1's three-writer claim is sound, and AC1's own mutant is a properly named production edit that the named test would die on; BLOCKING: AC1 (missing mutant for the outside-a-span half - the obvious "delete the escape" implementation survives); the absent idempotency criterion (943 measured double escapes, named in the fix and pinned by nothing); the absent criterion that a pipe or newline INSIDE a code span is still neutralised (the "verbatim" instruction regresses the row's only column defence); AC2 (no positive control for an even backtick count, and balance-vs-refuse must be decided before code) | plan-review | bd2a981fbf7a |
| BG0654 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC1 and AC2 now point `GIT\_CONFIG\_GLOBAL` and `GIT\_CONFIG\_SYSTEM` at `/dev/null` rather than unsetting them, and say in terms that unsetting is what lets the developer's own config answer.; #2 -> Closed by the same change, which adopts the mechanism the module's own git helper uses.; #3 -> Closed by the same change - the clause is no longer a no-op on a shell carrying neither variable.; #4 -> Closed by the same change.; #5 -> Accepted, not acted on: the criteria settle on local scope and do not require the message to name it. Recorded so the choice is visible.; #6 -> Noted, no change needed.; #7 -> Carried and accepted: AGENTS.md stays in `Affects` as a documentation obligation of the delivery, pinned by no criterion.; #8 -> Noted, no change needed.; #9 -> Closed by the AC1 and AC2 change above.; #10 -> Closed by the AC1 and AC2 change above. | - | plan-review | 12d8f780393e |
| BG0657 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> AC1 now says its node drives the emission DIRECTLY rather than through the lane, and states why: every existing node runs the lane against a stub, so a mutant inside the gate would be reported as survived.; #2 -> Closed by the same change.; #3 -> AC6 rewritten as the real-tool anchor: the real emitter is run once over this tree and its output parsed by the lane's own parser.; #4 -> A second AC2 row added, dropping the discriminator that stops the capture at the clause boundary.; #5 -> Closed by the AC1 change above. | AC1 - nothing pins consistency between the recorded count and the recorded identity list: "the identities behind each count" is only falsifiable if len(identities) == count, and without that pin a stale ten-name list sits beside a count of twenty and the lane still passes; AC1 - "each count" is two metrics and no row covers dead-stamps identities, though verify\\_ac.py stamps prints them and rehearse\\_stamps compares that count too; AC1 - the format change collides with test\\_the\\_committed\\_baseline\\_names\\_both\\_metrics (tools/tests/test\\_verify\\_corpus.py:204), which asserts the baseline's non-comment rows are exactly {red-criteria, dead-stamps}: the plan does not say whether identities become a new row type, a fourth pipe field or comments, and that existing pin must not be loosened into vacuity; AC2/AC3 - the identity parse carries the hazard the count parse already needed a discriminator for, since the unbuilt exclusion clause is printed IN FULL after the red clause, so a naive comma-list read picks up 67 excluded ids - test\\_the\\_exclusion\\_count\\_is\\_never\\_mistaken\\_for\\_the\\_red\\_count exists for the count and no row covers the identity equivalent; AC3 - no positive control of its own: a detector that flags every absent identity as vanished passes AC3's node, and the discriminating green case lives in AC2's separate test.; BLOCKING: AC1's mutant names a baseline write that does not exist; the identity source is unnamed and the obvious one caps at ten (gate.py:2011/2145) - either bring gate.py into scope or the criteria are not deliverable; AC2 must state the exit status for an equal-count swap and declare its mutant; AC1 must pin count-to-identity consistency, or the list can rot beside a passing gate. | plan-review | 081a64a10ac7 |
| BG0633 | 2026-09-08 | Claude Fable 5.1; session | 2026-09-08 | #1 -> Fixed. `cmd\_annotate` now prints the record `annotate` returns, so the text line and the JSON output state the same write. New AC4 pins it with its own mutant\; nothing in the corpus asserted that line before.; #2 -> Origin accepted: the divergence is this diff's, because canonicalisation happens inside the function the command wraps.; #3 -> Accepted, not acted on: the consumer-facing reference sits outside this unit's declared surface. The changelog fragment carries the new refusal class, and the doc line is recorded here rather than smuggled in.; #4 -> Accepted as a wording mismatch, not a hole: the reviewer re-ran all three cases as a genuine subprocess against the delivered file and the behaviour is identical. The criteria's `SUBPROCESS` wording is about what is being exercised, the shipped verb, which the driver does.; #5 -> Noted. The looseness is the tree's across 32 artefacts and is not repaired here.; #6 -> Closed by the fix in #1.; #7 -> Fixed. AC3's test docstring now names the mutant this artefact already recorded as correct, and says in terms why the earlier one survives its own node. | - | delivery | 3da560432bf8 |
| US0819 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | #1 -> Closed by US0821, which ships the ruling writer the first cut assumed. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #2 -> Closed by splitting the class. AC2 is now resolves-to-nothing ALONE, and an all-skipped node is new AC4's `never-fails`, a finding in its own right with its own mutant. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #3 -> Closed by new AC7: `manual` and unspecified verifiers are each named, counted SEPARATELY, and neither is a finding - the requirement the CR stated and no criterion carried. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #4 -> Closed by new AC5, which carries the timeout and absent-runner shapes in their own row, separate from the invalid and blocked kinds. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #5 -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes, and AC4's proceed-set names the rest explicitly, so no class is unpinned by omission. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #6 -> Accepted: the CR carries no Options section under that heading\; the reasoning the brief referred to is in its Recommendation, and the split is judged against that. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #7 -> Closed: US0819 declares verify\_ac.py and its test module only, US0820 declares sprint.py and its test module only, and US0821 the same pair as US0819. The ledger collision the finding names is real and is handled by registering both verify\_ac units after the last edit to that file. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows. | a criterion with no Verify line at all is UNSPECIFIED and never reaches run\\_verifier. verify\\_ac counts manual and unspecified SEPARATELY on purpose ("conflating them is how a deleted (rather than declared) verifier reaches a green release gate", verify\\_ac.py:1805-1817), the corpus holds 58 manual and ~15 unparseable Verify lines, and CR0569's own Recommendation says "Manual and unspecified verifiers are named and skipped rather than guessed at" - a requirement that is in the CR and in NO acceptance criterion of this story; AC4 - the single row pins the timeout half only. "drop the untrusted-answer branch so a timeout falls through to the failing class" is survived by a mutant that keeps the timeout branch and drops the absent-runner one. Worse, the two are not one branch: `\\_build\\_command` returns an argv list for pytest/jest/go/grep (a missing binary raises FileNotFoundError -> 127) and a STRING for `shell` (executed via shell=True, where 127 is the script's own exit and indistinguishable from the criterion failing for its own reasons - verified: `shell nosuchbinary\\_xyz --go` -> rc 127 -> unknown, `shell sleep 3` at timeout 2 -> rc 124 -> unknown). One row per shape; AC4 - it reverses a documented decision in the same file with no criterion acknowledging it and no paired control. `lint\\_runner\\_available` (verify\\_ac.py:1318-1338) already answers "is this runner absent" and is deliberately advisory: "the author machine's PATH may legitimately differ from the machine that verifies (CI, a teammate), so the wording owns that ambiguity and the check never blocks or fails anything on its own"; AGENTS.md lists pytest/jest/vitest/go/curl/jq/rg as SOFT dependencies. AC4 makes the same fact fail the command and US0820 makes it refuse the plan, so on a machine missing one runner every criterion using it is `unknown` and the batch is refused - the "a check that refuses every plan is switched off rather than satisfied" outcome AC2 and AC3 exist to prevent, arriving through AC4's door. No criterion separates a runner absent from THIS MACHINE from a runner the plan named wrongly; AC5 - the ruling hash is specified over the criterion's TEXT, so re-pointing the `Verify:` selector under an unchanged title leaves the pin live, which is precisely "a ruling outliving the criterion it excused". `ac\\_fingerprint` - the nearest existing thing - hashes (ac\\_id, title, verifier) TOGETHER because the verifier is what actually gets executed, and the pattern AC5 cites keys on the whole FILE's content hash (`\\_file\\_hash`/`live\\_rulings`), so any byte change staleness it. Neither AC5 row reaches this: a mutant that hashes the title and drops the Verify line survives, because the named test makes a ruling stale by changing the criterion's text. Missing row: "in verify\\_ac.py, exclude the criterion's Verify selector from the ruling hash"; AC5 - it copies the coverage-ruling SHAPE and not the properties that stop it being a free bypass. `add\\_coverage\\_ruling` refuses a reason below `mutation.\\_RETRACT\\_REASON\\_MIN`, refuses a reason opening with "withdrawn ", refuses a second live ruling on one line, has a `withdraw` verb that marks the row in place so a retraction is on the record, and `apply\\_rulings` NAMES stale rulings in the report rather than dropping them. AC5 requires none of these and no row pins any: a one-character reason, or a stale ruling that vanishes silently, satisfies every row here; AC1-AC5 - nothing says the probe leaves the artefact unmodified. The obvious implementation reuses `verify\\_story`, whose non-dry path writes `**Verified:** yes` back into the story for every criterion that PASSED - i.e. exactly the GREEN ones the probe exists to report - which then feeds the `transition -> Done` gate and the AC fingerprint. Wired into `sprint plan` (US0820) that mutates tracked artefacts from a read-only census command, the failure `tools/repo\\_writes.py` exists for. Missing row: "in verify\\_ac.py, call the runner from the probe with dry\\_run=False"; header - Affects is identical to US0820's (verify\\_ac.py, sprint.py, test\\_verify\\_ac.py, test\\_sprint.py) while every plan row names only verify\\_ac.py. Over-declaring widens this unit's review scope onto US0820's files, and two batch units declaring the same production files collide in the mutation ledger, which drops registered rows on any byte change to a shared file (LL0053); `revert-check` would also revert sprint.py for US0819. Declare verify\\_ac.py + test\\_verify\\_ac.py only; AC1 - no class for green-because-already-delivered (measured: 5 of 52 open units, 5 of 5 false positives) and two missing rows for the naming and always-exit-0 mutants; AC2 - vacuous conflates deleted/renamed/skipped with not-yet-written, passing the very shape the story exists to catch; AC2/AC4 - manual, unspecified and invalid verifiers have no class and read as `red`, dropping a requirement CR0569 states explicitly; AC4 - one row for two branches, the absent-runner shape differs per verb, and there is no control for a runner absent from the machine rather than from the plan; AC5 - the ruling hash must cover the Verify selector, with a row that pins it; AC1-AC5 - add a criterion and a row that the probe writes nothing back to the artefact | plan-review | 1d99baf0e2a3 |
| US0820 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | #1 -> Answered by moving the consequence: the probe classifies an absent runner `unreadable`, and the GATE that acts on it (US0820) ships in `report` mode by default, so a machine missing a runner reports rather than refuses. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #2 -> Closed: US0819 declares verify\_ac.py and its test module only, US0820 declares sprint.py and its test module only, and US0821 the same pair as US0819. The ledger collision the finding names is real and is handled by registering both verify\_ac units after the last edit to that file. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #3 -> Closed by US0820 AC1, which now requires the refusal to name the unit, the criterion, the class the probe returned, why that class is a finding, and the command that rules it. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #4 -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, which is also what keeps the greenfield rehearsal's own criterion out of the finding set. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #5 -> Answered by the re-design recorded at the head of the criteria: the classifier now carries nine classes rather than four, shell verifiers are never executed, the ruling is a unit of its own (US0821), the gate ships in `report` mode by default, and each unit declares only the files it changes. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #6 -> Closed: US0819 declares verify\_ac.py and its test module only, US0820 declares sprint.py and its test module only, and US0821 the same pair as US0819. The ledger collision the finding names is real and is handled by registering both verify\_ac units after the last edit to that file. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows. | the wiring into the command people actually run is what makes it a gate", which is US0577's defect verbatim (`brief\\_fingerprint` green in-process while `critic.py brief` printed nothing), and verify\\_ac's `entry\\_point\\_never\\_entered` lane exists for it. All three selectors are bare pytest nodes in test\\_sprint.py; nothing refuses a test that calls the library gate directly instead of `sprint.main(["plan", ...])`. Missing row: "in sprint.py, leave the gate callable as a library function and never call it from cmd\\_plan"; AC1 - the trust boundary is crossed and no criterion carries it. `run\\_verifier` gates shell-backed verbs on allow\\_shell AND `shell\\_allowed\\_for(text)`, which refuses a story stamped `Provenance: external` - "the technical control behind the documented trust boundary... externally ingested content must not reach a shell just because a workflow copied it into an artefact", and verify\\_ac.py records that a lane runner re-deriving this once executed a shell verifier the authoritative runner refused. `sprint plan` today reads artefacts and executes nothing from them. Nothing says which of allow\\_shell/allow\\_external the plan path passes, and BOTH answers are defects with no row: pass it True and planning a batch runs arbitrary artefact-authored shell (351 `shell` verifiers in this corpus, including `npm run lint:links` and a loop over `mutation.py run --from-plan`); pass it False and every shell and eval verifier returns kind `blocked` with ok=False, classifies `red`, and the gate is dead on 351 criteria while reporting itself green; AC1/AC2/AC3 - the cost of putting execution on the plan path is not stated and there is no degrade. Measured here it is modest and I say so: 200 criteria in 39.8s, ~0.2s each, because this repo's open backlog is 100% pytest and 183 of 200 selectors are unresolved nodes. But nothing bounds it: the per-verifier default is `--timeout 120`, so one hanging shell verifier costs two minutes and then, under US0819 AC4, comes back `unknown` and REFUSES the plan; verify\\_ac.py's own record of the release gate is "694 of the 1,223 Verify lines are pytest, and a bare pytest start costs ~1.26s here... the spawns alone were ~15 minutes", which is why `pytest\\_batch\\_cache` exists - and neither story mentions that cache. This repo has ruled three times that a minutes-long lane belongs at a boundary and not on the common path (release-rehearsal, revert-check, module-alone), and `claim-drift` and `revert-check` both ship ADVISORY "while its yield is measured, because a new blocking check on a gate already over its ceiling earns its place on a number rather than on assertion". US0820 has no criterion for a report mode, a config key on the `review.*` pattern, a skip flag or a timeout budget; header - Affects is identical to US0819's (verify\\_ac.py, sprint.py, test\\_verify\\_ac.py, test\\_sprint.py) while every plan row names only sprint.py. Two batch units declaring the same production files collide in the mutation ledger, which drops registered rows on any byte change to a shared file (LL0053), and `revert-check` would revert verify\\_ac.py for US0820. Declare sprint.py + test\\_sprint.py only; AC1 - the greenfield rehearsal fixture's `shell true` classifies GREEN, so this refusal fails release-rehearsal and reddens the push and release boundaries; settle it before code; AC1/AC2 - 5 of 52 open units on this repo would be refused today and all five are false positives (delivered, not transitioned); AC2's control must carry that case; AC3 - the declared mutant survives on the batch AC3 names; name the discriminating class or the row pins nothing; AC1 - second row for the naming half of the refusal; AC1/AC3 - a row pinning that the criterion is exercised through `sprint.py plan` and not the library gate; AC1 - a criterion deciding whether the plan path may execute shell verifiers, since both answers are currently defects | plan-review | 7f4d8c01b3a8 |
| US0819 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | #1 -> Closed by US0821, which ships the ruling writer the first cut assumed. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #2 -> Closed by splitting the class. AC2 is now resolves-to-nothing ALONE, and an all-skipped node is new AC4's `never-fails`, a finding in its own right with its own mutant. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #3 -> Closed by new AC7: `manual` and unspecified verifiers are each named, counted SEPARATELY, and neither is a finding - the requirement the CR stated and no criterion carried. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #4 -> Closed by new AC5, which carries the timeout and absent-runner shapes in their own row, separate from the invalid and blocked kinds. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #5 -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes, and AC4's proceed-set names the rest explicitly, so no class is unpinned by omission. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #6 -> Accepted: the CR carries no Options section under that heading\; the reasoning the brief referred to is in its Recommendation, and the split is judged against that. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows.; #7 -> Closed: US0819 declares verify\_ac.py and its test module only, US0820 declares sprint.py and its test module only, and US0821 the same pair as US0819. The ledger collision the finding names is real and is handled by registering both verify\_ac units after the last edit to that file. The same re-design answers the qa and engineering seats' findings on this point, which are on the record as their own verdict rows. | a criterion with no Verify line at all is UNSPECIFIED and never reaches run\\_verifier. verify\\_ac counts manual and unspecified SEPARATELY on purpose ("conflating them is how a deleted (rather than declared) verifier reaches a green release gate", verify\\_ac.py:1805-1817), the corpus holds 58 manual and ~15 unparseable Verify lines, and CR0569's own Recommendation says "Manual and unspecified verifiers are named and skipped rather than guessed at" - a requirement that is in the CR and in NO acceptance criterion of this story; AC4 - the single row pins the timeout half only. "drop the untrusted-answer branch so a timeout falls through to the failing class" is survived by a mutant that keeps the timeout branch and drops the absent-runner one. Worse, the two are not one branch: `\\_build\\_command` returns an argv list for pytest/jest/go/grep (a missing binary raises FileNotFoundError -> 127) and a STRING for `shell` (executed via shell=True, where 127 is the script's own exit and indistinguishable from the criterion failing for its own reasons - verified: `shell nosuchbinary\\_xyz --go` -> rc 127 -> unknown, `shell sleep 3` at timeout 2 -> rc 124 -> unknown). One row per shape; AC4 - it reverses a documented decision in the same file with no criterion acknowledging it and no paired control. `lint\\_runner\\_available` (verify\\_ac.py:1318-1338) already answers "is this runner absent" and is deliberately advisory: "the author machine's PATH may legitimately differ from the machine that verifies (CI, a teammate), so the wording owns that ambiguity and the check never blocks or fails anything on its own"; AGENTS.md lists pytest/jest/vitest/go/curl/jq/rg as SOFT dependencies. AC4 makes the same fact fail the command and US0820 makes it refuse the plan, so on a machine missing one runner every criterion using it is `unknown` and the batch is refused - the "a check that refuses every plan is switched off rather than satisfied" outcome AC2 and AC3 exist to prevent, arriving through AC4's door. No criterion separates a runner absent from THIS MACHINE from a runner the plan named wrongly; AC5 - the ruling hash is specified over the criterion's TEXT, so re-pointing the `Verify:` selector under an unchanged title leaves the pin live, which is precisely "a ruling outliving the criterion it excused". `ac\\_fingerprint` - the nearest existing thing - hashes (ac\\_id, title, verifier) TOGETHER because the verifier is what actually gets executed, and the pattern AC5 cites keys on the whole FILE's content hash (`\\_file\\_hash`/`live\\_rulings`), so any byte change staleness it. Neither AC5 row reaches this: a mutant that hashes the title and drops the Verify line survives, because the named test makes a ruling stale by changing the criterion's text. Missing row: "in verify\\_ac.py, exclude the criterion's Verify selector from the ruling hash"; AC5 - it copies the coverage-ruling SHAPE and not the properties that stop it being a free bypass. `add\\_coverage\\_ruling` refuses a reason below `mutation.\\_RETRACT\\_REASON\\_MIN`, refuses a reason opening with "withdrawn ", refuses a second live ruling on one line, has a `withdraw` verb that marks the row in place so a retraction is on the record, and `apply\\_rulings` NAMES stale rulings in the report rather than dropping them. AC5 requires none of these and no row pins any: a one-character reason, or a stale ruling that vanishes silently, satisfies every row here; AC1-AC5 - nothing says the probe leaves the artefact unmodified. The obvious implementation reuses `verify\\_story`, whose non-dry path writes `**Verified:** yes` back into the story for every criterion that PASSED - i.e. exactly the GREEN ones the probe exists to report - which then feeds the `transition -> Done` gate and the AC fingerprint. Wired into `sprint plan` (US0820) that mutates tracked artefacts from a read-only census command, the failure `tools/repo\\_writes.py` exists for. Missing row: "in verify\\_ac.py, call the runner from the probe with dry\\_run=False"; header - Affects is identical to US0820's (verify\\_ac.py, sprint.py, test\\_verify\\_ac.py, test\\_sprint.py) while every plan row names only verify\\_ac.py. Over-declaring widens this unit's review scope onto US0820's files, and two batch units declaring the same production files collide in the mutation ledger, which drops registered rows on any byte change to a shared file (LL0053); `revert-check` would also revert sprint.py for US0819. Declare verify\\_ac.py + test\\_verify\\_ac.py only; AC1 - no class for green-because-already-delivered (measured: 5 of 52 open units, 5 of 5 false positives) and two missing rows for the naming and always-exit-0 mutants; AC2 - vacuous conflates deleted/renamed/skipped with not-yet-written, passing the very shape the story exists to catch; AC2/AC4 - manual, unspecified and invalid verifiers have no class and read as `red`, dropping a requirement CR0569 states explicitly; AC4 - one row for two branches, the absent-runner shape differs per verb, and there is no control for a runner absent from the machine rather than from the plan; AC5 - the ruling hash must cover the Verify selector, with a row that pins it; AC1-AC5 - add a criterion and a row that the probe writes nothing back to the artefact | plan-review | 1d99baf0e2a3 |
| US0819 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | AC5 presumes "a recorded ruling" but no criterion in either -> Closed by minting US0821, five points, in this batch: `testplan rule` and `testplan withdraw` with a reason floor, a duplicate-live refusal, a sentinel-reason refusal, a stale ruling NAMED rather than dropped, and a withdrawal that marks the row in place.; AC1/AC3 - the classifier is specified only against selectors -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; CR0569's Recommendation promises "Manual and unspecified ver -> Closed by new AC7: `manual` and unspecified verifiers are each named, counted SEPARATELY, and neither is a finding - the requirement the CR stated and no criterion carried.; AC4 makes an absent runner a hard non-zero exit, and AGENTS. -> Closed: AC5 now carries two rows, one for the invalid and blocked kinds and one for the timeout and absent-runner exit shapes, which differ per verb.; CR0569's Recommendation names three classes (red, not-yet-wr -> Accepted: the CR carries no Options section under that heading and its Recommendation named three classes where the criteria now carry nine, which is the outcome space the runner actually produces.; the review brief asks whether the split recreates the report -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; Affects on US0819 and US0820 declares the same four files wh -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC5 has no shipped writer for the ruling it requires -> Closed by minting US0821, five points, in this batch: `testplan rule` and `testplan withdraw` with a reason floor, a duplicate-live refusal, a sentinel-reason refusal, a stale ruling NAMED rather than dropped, and a withdrawal that marks the row in place.; AC1/AC3 do not say what the classifier does with a verb that -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; the manual and unspecified classes CR0569's Recommendation p -> Closed by new AC7: `manual` and unspecified verifiers are each named, counted SEPARATELY, and neither is a finding - the requirement the CR stated and no criterion carried.; AC1 - one row for three claims: "merge the passing branch in -> Closed: AC1 now carries three rows - merging the classes, printing a bare count, and returning zero whatever was classified - so the naming and always-zero mutants are both measured.; AC1 - no class for GREEN-because-already-delivered. Measured -> Closed by new AC8: a green criterion on a unit already carrying attributing commits is `delivered` - reported, not a finding. The measured 17 criteria across 5 of 52 open units, none of them the defect the CR is about, is recorded in the criterion itself.; US0553 is Won't Implement). Zero of the 17 are the defect CR -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC2 - `vacuous` is three states, not one, and AC2 sends all -> Closed by splitting the class: AC2 is resolves-to-nothing ALONE, and an all-skipped node is new AC4's `never-fails`, a finding in its own right with its own mutant.; AC2/AC4 - three verifier states have no class at all, and un -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; `python3 tools/check\\\_links.py` -> kind=invalid rc=2 -> Closed by new AC5: an invalid Verify line, a verb the trust boundary blocked, a timeout and an absent runner are all `unreadable` - a finding, never green and never red. The criterion cites this module's own record of having made the count-it-red mistake once.; a criterion with no Verify line at all is UNSPECIFIED and ne -> Closed by new AC7: `manual` and unspecified verifiers are each named, counted SEPARATELY, and neither is a finding - the requirement the CR stated and no criterion carried.; AC4 - the single row pins the timeout half only. "drop the u -> Closed: AC5 now carries two rows, one for the invalid and blocked kinds and one for the timeout and absent-runner exit shapes, which differ per verb.; AC4 - it reverses a documented decision in the same file wit -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AGENTS.md lists pytest/jest/vitest/go/curl/jq/rg as SOFT dep -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC5 - the ruling hash is specified over the criterion's TEXT -> Closed by US0821 AC3: the ruling hash covers the criterion's title AND its Verify selector, with a mutant that narrows the digest to the title alone.; AC5 - it copies the coverage-ruling SHAPE and not the proper -> Closed by minting US0821, five points, in this batch: `testplan rule` and `testplan withdraw` with a reason floor, a duplicate-live refusal, a sentinel-reason refusal, a stale ruling NAMED rather than dropped, and a withdrawal that marks the row in place.; AC1-AC5 - nothing says the probe leaves the artefact unmodif -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; header - Affects is identical to US0820's (verify\\\_ac.py, sp -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; `revert-check` would also revert sprint.py for US0819. Decla -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 - no class for green-because-already-delivered (measured -> Closed by new AC8: a green criterion on a unit already carrying attributing commits is `delivered` - reported, not a finding. The measured 17 criteria across 5 of 52 open units, none of them the defect the CR is about, is recorded in the criterion itself.; AC2 - vacuous conflates deleted/renamed/skipped with not-yet -> Closed by splitting the class: AC2 is resolves-to-nothing ALONE, and an all-skipped node is new AC4's `never-fails`, a finding in its own right with its own mutant.; AC2/AC4 - manual, unspecified and invalid verifiers have no -> Closed by new AC5: an invalid Verify line, a verb the trust boundary blocked, a timeout and an absent runner are all `unreadable` - a finding, never green and never red. The criterion cites this module's own record of having made the count-it-red mistake once.; AC4 - one row for two branches, the absent-runner shape diff -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC5 - the ruling hash must cover the Verify selector, with a -> Closed by US0821 AC3: the ruling hash covers the criterion's title AND its Verify selector, with a mutant that narrows the digest to the title alone.; AC1-AC5 - add a criterion and a row that the probe writes no -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC3/AC4 - the state set has no home for `kind="invalid"` or -> Closed by new AC5: an invalid Verify line, a verb the trust boundary blocked, a timeout and an absent runner are all `unreadable` - a finding, never green and never red. The criterion cites this module's own record of having made the count-it-red mistake once.; measured against `run\\\_verifier` on a throwaway fixture, a m -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; verify\\\_ac.py:4588 (`revert\\\_check`) already carries the fix -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC2 - `vacuous` is the sole discriminator for not-yet-written -> Closed by splitting the class: AC2 is resolves-to-nothing ALONE, and an all-skipped node is new AC4's `never-fails`, a finding in its own right with its own mutant.; a permanently skipped test exists, was collected and can nev -> Closed by splitting the class: AC2 is resolves-to-nothing ALONE, and an all-skipped node is new AC4's `never-fails`, a finding in its own right with its own mutant.; AC1-AC5 - `manual` and unspecified verifiers have NO class a -> Closed by new AC7: `manual` and unspecified verifiers are each named, counted SEPARATELY, and neither is a finding - the requirement the CR stated and no criterion carried.; the corpus holds 56 manual and 72 unspecified criteria, and -> Closed by new AC7: `manual` and unspecified verifiers are each named, counted SEPARATELY, and neither is a finding - the requirement the CR stated and no criterion carried.; AC5 - the plan reproduces `coverage rule`'s SHAPE and not th -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; as written a one-character reason is a valid pin -> Closed by minting US0821, five points, in this batch: `testplan rule` and `testplan withdraw` with a reason floor, a duplicate-live refusal, a sentinel-reason refusal, a stale ruling NAMED rather than dropped, and a withdrawal that marks the row in place.; AC5 - no criterion ships a WRITER for the ruling, so the onl -> Closed by minting US0821, five points, in this batch: `testplan rule` and `testplan withdraw` with a reason floor, a duplicate-live refusal, a sentinel-reason refusal, a stale ruling NAMED rather than dropped, and a withdrawal that marks the row in place.; `coverage rule` is a validated CLI verb with a --fields-file -> Closed by minting US0821, five points, in this batch: `testplan rule` and `testplan withdraw` with a reason floor, a duplicate-live refusal, a sentinel-reason refusal, a stale ruling NAMED rather than dropped, and a withdrawal that marks the row in place.; AC4 - two distinct claims (the verifier times out, and it na -> Closed: AC5 now carries two rows, one for the invalid and blocked kinds and one for the timeout and absent-runner exit shapes, which differ per verb.; AC5's two claims each got a row and AC4's did not -> Closed: AC1 now carries three rows - merging the classes, printing a bare count, and returning zero whatever was classified - so the naming and always-zero mutants are both measured.; Affects overstates the unit - US0819 declares sprint.py and -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC2/AC3 - `resolve\\\_pytest\\\_from\\\_cache` states that "a red -> Reported and recorded\; not a regression from this plan and not acted on in these units.; reported, not a regression from this plan -> Reported and recorded\; not a regression from this plan and not acted on in these units.; AC3/AC4 invalid and blocked classify as `red`, the healthy c -> Closed by new AC5: an invalid Verify line, a verb the trust boundary blocked, a timeout and an absent runner are all `unreadable` - a finding, never green and never red. The criterion cites this module's own record of having made the count-it-red mistake once.; AC2 `vacuous` conflates an all-skipped run with not-yet-writ -> Closed by splitting the class: AC2 is resolves-to-nothing ALONE, and an all-skipped node is new AC4's `never-fails`, a finding in its own right with its own mutant.; AC1-AC5 manual and unspecified criteria have no class though -> Closed by new AC7: `manual` and unspecified verifiers are each named, counted SEPARATELY, and neither is a finding - the requirement the CR stated and no criterion carried.; AC5 the ruling carries the hash and none of `coverage rule`' -> Closed by minting US0821, five points, in this batch: `testplan rule` and `testplan withdraw` with a reason floor, a duplicate-live refusal, a sentinel-reason refusal, a stale ruling NAMED rather than dropped, and a withdrawal that marks the row in place.; Affects must name only the files this unit changes before an -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file. | two more rows needed | plan-review | 1d99baf0e2a3 |
| US0820 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | AC1 pins only the GREEN case while the story's own title and -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 proposes an unconditional refusal with no mode and no da -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1 requires the refusal to name only the unit and the crite -> Closed by US0820 AC1, which requires the refusal to name the unit, the criterion, the class the probe returned, why that class is a finding, and the command that rules it.; AC2's control set does not include the greenfield shape and -> Closed by US0820 AC4, whose control fixture carries a delivered-but-not-transitioned unit and whose proceed-set includes `not-probed`, the greenfield shape.; AC3 requiring the plan to quote the probe's class rather tha -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; Affects on US0819 and US0820 declares the same four files wh -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 and AC2 leave the `unknown` class unpinned although the -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1 has no mode and no cutoff, so a consuming project has no -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1's refusal is specified to name the unit and criterion on -> Closed by US0820 AC1, which requires the refusal to name the unit, the criterion, the class the probe returned, why that class is a finding, and the command that rules it.; AC2's control set omits the greenfield rehearsal shape, whic -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1 - this refusal turns the push and release boundaries red -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 - the gate's false-positive rate on this repo's own -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; US0553 is Won't Implement. None is a criterion that proves n -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC3 - the declared mutant SURVIVES on the batch AC3 names. R -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC1 - one row for two claims. "delete the probe call from th -> Closed: AC1 now carries three rows - merging the classes, printing a bare count, and returning zero whatever was classified - so the naming and always-zero mutants are both measured.; AC1/AC3 - no row pins that the test enters the shipped comma -> Closed by US0820 AC5: the criterion is exercised through `sprint.py plan` as the shipped command, with a row that leaves the gate callable and never calls it from the plan command.; the wiring into the command people actually run is what make -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; nothing refuses a test that calls the library gate directly -> Closed by US0820 AC5: the criterion is exercised through `sprint.py plan` as the shipped command, with a row that leaves the gate callable and never calls it from the plan command.; AC1 - the trust boundary is crossed and no criterion carries -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; pass it False and every shell and eval verifier returns kind -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2/AC3 - the cost of putting execution on the plan path -> Answered with the measurement rather than an assertion: the last real batch is 16 units and 72 criteria at 14.5 s, shell verifiers are never executed at all under AC6, and the gate reports rather than refuses by default while its yield is measured - this repository's own pattern for a new check.; verify\\\_ac.py's own record of the release gate is "694 of th -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; header - Affects is identical to US0819's (verify\\\_ac.py, sp -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 - the greenfield rehearsal fixture's `shell true` classi -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 - 5 of 52 open units on this repo would be refused t -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC2's control must carry that case -> Closed by US0820 AC4, whose control fixture carries a delivered-but-not-transitioned unit and whose proceed-set includes `not-probed`, the greenfield shape.; AC3 - the declared mutant survives on the batch AC3 names -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; name the discriminating class or the row pins nothing -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC1 - second row for the naming half of the refusal -> Closed: AC1 now carries three rows - merging the classes, printing a bare count, and returning zero whatever was classified - so the naming and always-zero mutants are both measured.; AC1/AC3 - a row pinning that the criterion is exercised thro -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 - a criterion deciding whether the plan path may execute -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1/AC2 - the refusal reddens the push and release boundarie -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; rehearse-release.sh appears in neither unit's Affects, so re -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1/AC2 - the plan does not decide whether the probe execute -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; with shell OFF every one of those returns kind=blocked and c -> Closed by new AC5: an invalid Verify line, a verb the trust boundary blocked, a timeout and an absent runner are all `unreadable` - a finding, never green and never red. The criterion cites this module's own record of having made the count-it-red mistake once.; AC3 - the mutant survives its own criterion: recomputing eac -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC3 - quoting is not the anti-drift property -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; sprint.py's own precedent is `\\\_affects\\\_blocking`, written -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 - the unit's own title and CR0569's criterion both say " -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC2's proceed-list excludes unknown only by omission -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1/AC2 - no config mode and no recorded waiver, against the -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; an unconditional new refusal in the most-run planning comman -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1/AC2 - no criterion bounds the cost, and `sprint plan` do -> Answered with the measurement rather than an assertion: the last real batch is 16 units and 72 criteria at 14.5 s, shell verifiers are never executed at all under AC6, and the gate reports rather than refuses by default while its yield is measured - this repository's own pattern for a new check.; measured on the last real batch (16 units, 72 criteria, ever -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; Affects overstates the unit - US0820 declares verify\\\_ac.py -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; ordering - US0819 can land alone as an inert verb nobody cal -> Answered: all three units ship in this batch, in the order US0819, US0821, US0820, so the verb, its ruling writer and the wiring land together.; AC1/AC2 the greenfield rehearsal's `Verify: shell true` crit -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 the shell question is undecided and both branches ar -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC3 the named test does not have to die on its mutant unless -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 the unknown half of the unit's own title has no criterio -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1/AC2 the refusal needs a config mode or a recorded waiver -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; Affects must name only the files this unit changes, and US08 -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file. | settle it before code | plan-review | 7f4d8c01b3a8 |
| US0820 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | AC1 pins only the GREEN case while the story's own title and -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 proposes an unconditional refusal with no mode and no da -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1 requires the refusal to name only the unit and the crite -> Closed by US0820 AC1, which requires the refusal to name the unit, the criterion, the class the probe returned, why that class is a finding, and the command that rules it.; AC2's control set does not include the greenfield shape and -> Closed by US0820 AC4, whose control fixture carries a delivered-but-not-transitioned unit and whose proceed-set includes `not-probed`, the greenfield shape.; AC3 requiring the plan to quote the probe's class rather tha -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; Affects on US0819 and US0820 declares the same four files wh -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 and AC2 leave the `unknown` class unpinned although the -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1 has no mode and no cutoff, so a consuming project has no -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1's refusal is specified to name the unit and criterion on -> Closed by US0820 AC1, which requires the refusal to name the unit, the criterion, the class the probe returned, why that class is a finding, and the command that rules it.; AC2's control set omits the greenfield rehearsal shape, whic -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1 - this refusal turns the push and release boundaries red -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 - the gate's false-positive rate on this repo's own -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; US0553 is Won't Implement. None is a criterion that proves n -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC3 - the declared mutant SURVIVES on the batch AC3 names. R -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC1 - one row for two claims. "delete the probe call from th -> Closed: AC1 now carries three rows - merging the classes, printing a bare count, and returning zero whatever was classified - so the naming and always-zero mutants are both measured.; AC1/AC3 - no row pins that the test enters the shipped comma -> Closed by US0820 AC5: the criterion is exercised through `sprint.py plan` as the shipped command, with a row that leaves the gate callable and never calls it from the plan command.; the wiring into the command people actually run is what make -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; nothing refuses a test that calls the library gate directly -> Closed by US0820 AC5: the criterion is exercised through `sprint.py plan` as the shipped command, with a row that leaves the gate callable and never calls it from the plan command.; AC1 - the trust boundary is crossed and no criterion carries -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; pass it False and every shell and eval verifier returns kind -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2/AC3 - the cost of putting execution on the plan path -> Answered with the measurement rather than an assertion: the last real batch is 16 units and 72 criteria at 14.5 s, shell verifiers are never executed at all under AC6, and the gate reports rather than refuses by default while its yield is measured - this repository's own pattern for a new check.; verify\\\_ac.py's own record of the release gate is "694 of th -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; header - Affects is identical to US0819's (verify\\\_ac.py, sp -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 - the greenfield rehearsal fixture's `shell true` classi -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 - 5 of 52 open units on this repo would be refused t -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC2's control must carry that case -> Closed by US0820 AC4, whose control fixture carries a delivered-but-not-transitioned unit and whose proceed-set includes `not-probed`, the greenfield shape.; AC3 - the declared mutant survives on the batch AC3 names -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; name the discriminating class or the row pins nothing -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC1 - second row for the naming half of the refusal -> Closed: AC1 now carries three rows - merging the classes, printing a bare count, and returning zero whatever was classified - so the naming and always-zero mutants are both measured.; AC1/AC3 - a row pinning that the criterion is exercised thro -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 - a criterion deciding whether the plan path may execute -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1/AC2 - the refusal reddens the push and release boundarie -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; rehearse-release.sh appears in neither unit's Affects, so re -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1/AC2 - the plan does not decide whether the probe execute -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; with shell OFF every one of those returns kind=blocked and c -> Closed by new AC5: an invalid Verify line, a verb the trust boundary blocked, a timeout and an absent runner are all `unreadable` - a finding, never green and never red. The criterion cites this module's own record of having made the count-it-red mistake once.; AC3 - the mutant survives its own criterion: recomputing eac -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC3 - quoting is not the anti-drift property -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; sprint.py's own precedent is `\\\_affects\\\_blocking`, written -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 - the unit's own title and CR0569's criterion both say " -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC2's proceed-list excludes unknown only by omission -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1/AC2 - no config mode and no recorded waiver, against the -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; an unconditional new refusal in the most-run planning comman -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1/AC2 - no criterion bounds the cost, and `sprint plan` do -> Answered with the measurement rather than an assertion: the last real batch is 16 units and 72 criteria at 14.5 s, shell verifiers are never executed at all under AC6, and the gate reports rather than refuses by default while its yield is measured - this repository's own pattern for a new check.; measured on the last real batch (16 units, 72 criteria, ever -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; Affects overstates the unit - US0820 declares verify\\\_ac.py -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; ordering - US0819 can land alone as an inert verb nobody cal -> Answered: all three units ship in this batch, in the order US0819, US0821, US0820, so the verb, its ruling writer and the wiring land together.; AC1/AC2 the greenfield rehearsal's `Verify: shell true` crit -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 the shell question is undecided and both branches ar -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC3 the named test does not have to die on its mutant unless -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 the unknown half of the unit's own title has no criterio -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1/AC2 the refusal needs a config mode or a recorded waiver -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; Affects must name only the files this unit changes, and US08 -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file. | settle it before code | plan-review | 7f4d8c01b3a8 |
| US0820 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | AC1 pins only the GREEN case while the story's own title and -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 proposes an unconditional refusal with no mode and no da -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1 requires the refusal to name only the unit and the crite -> Closed by US0820 AC1, which requires the refusal to name the unit, the criterion, the class the probe returned, why that class is a finding, and the command that rules it.; AC2's control set does not include the greenfield shape and -> Closed by US0820 AC4, whose control fixture carries a delivered-but-not-transitioned unit and whose proceed-set includes `not-probed`, the greenfield shape.; AC3 requiring the plan to quote the probe's class rather tha -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; Affects on US0819 and US0820 declares the same four files wh -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 and AC2 leave the `unknown` class unpinned although the -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1 has no mode and no cutoff, so a consuming project has no -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1's refusal is specified to name the unit and criterion on -> Closed by US0820 AC1, which requires the refusal to name the unit, the criterion, the class the probe returned, why that class is a finding, and the command that rules it.; AC2's control set omits the greenfield rehearsal shape, whic -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1 - this refusal turns the push and release boundaries red -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 - the gate's false-positive rate on this repo's own -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; US0553 is Won't Implement. None is a criterion that proves n -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC3 - the declared mutant SURVIVES on the batch AC3 names. R -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC1 - one row for two claims. "delete the probe call from th -> Closed: AC1 now carries three rows - merging the classes, printing a bare count, and returning zero whatever was classified - so the naming and always-zero mutants are both measured.; AC1/AC3 - no row pins that the test enters the shipped comma -> Closed by US0820 AC5: the criterion is exercised through `sprint.py plan` as the shipped command, with a row that leaves the gate callable and never calls it from the plan command.; the wiring into the command people actually run is what make -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; nothing refuses a test that calls the library gate directly -> Closed by US0820 AC5: the criterion is exercised through `sprint.py plan` as the shipped command, with a row that leaves the gate callable and never calls it from the plan command.; AC1 - the trust boundary is crossed and no criterion carries -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; pass it False and every shell and eval verifier returns kind -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2/AC3 - the cost of putting execution on the plan path -> Answered with the measurement rather than an assertion: the last real batch is 16 units and 72 criteria at 14.5 s, shell verifiers are never executed at all under AC6, and the gate reports rather than refuses by default while its yield is measured - this repository's own pattern for a new check.; verify\\\_ac.py's own record of the release gate is "694 of th -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; header - Affects is identical to US0819's (verify\\\_ac.py, sp -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 - the greenfield rehearsal fixture's `shell true` classi -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 - 5 of 52 open units on this repo would be refused t -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC2's control must carry that case -> Closed by US0820 AC4, whose control fixture carries a delivered-but-not-transitioned unit and whose proceed-set includes `not-probed`, the greenfield shape.; AC3 - the declared mutant survives on the batch AC3 names -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; name the discriminating class or the row pins nothing -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC1 - second row for the naming half of the refusal -> Closed: AC1 now carries three rows - merging the classes, printing a bare count, and returning zero whatever was classified - so the naming and always-zero mutants are both measured.; AC1/AC3 - a row pinning that the criterion is exercised thro -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 - a criterion deciding whether the plan path may execute -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1/AC2 - the refusal reddens the push and release boundarie -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; rehearse-release.sh appears in neither unit's Affects, so re -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1/AC2 - the plan does not decide whether the probe execute -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; with shell OFF every one of those returns kind=blocked and c -> Closed by new AC5: an invalid Verify line, a verb the trust boundary blocked, a timeout and an absent runner are all `unreadable` - a finding, never green and never red. The criterion cites this module's own record of having made the count-it-red mistake once.; AC3 - the mutant survives its own criterion: recomputing eac -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; AC3 - quoting is not the anti-drift property -> Closed by US0820 AC6, which names the discriminating class - the fixture holds an `unreadable` criterion, the one class that appears in a refusal AND disagrees with its own exit code - and requires BOTH the label and the decision to come from the probe.; sprint.py's own precedent is `\\\_affects\\\_blocking`, written -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; AC1 - the unit's own title and CR0569's criterion both say " -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC2's proceed-list excludes unknown only by omission -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1/AC2 - no config mode and no recorded waiver, against the -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; an unconditional new refusal in the most-run planning comman -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; AC1/AC2 - no criterion bounds the cost, and `sprint plan` do -> Answered with the measurement rather than an assertion: the last real batch is 16 units and 72 criteria at 14.5 s, shell verifiers are never executed at all under AC6, and the gate reports rather than refuses by default while its yield is measured - this repository's own pattern for a new check.; measured on the last real batch (16 units, 72 criteria, ever -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; Affects overstates the unit - US0820 declares verify\\\_ac.py -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; ordering - US0819 can land alone as an inert verb nobody cal -> Answered: all three units ship in this batch, in the order US0819, US0821, US0820, so the verb, its ruling writer and the wiring land together.; AC1/AC2 the greenfield rehearsal's `Verify: shell true` crit -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC1/AC2 the shell question is undecided and both branches ar -> Closed by new AC6: a `shell` or `eval` verifier is `not-probed`, named, and NOTHING is executed for it. The plan path never runs artefact-authored shell, and that is also what keeps the greenfield rehearsal's own criterion out of the finding set, so the push and release boundaries are unaffected. This was the finding that changed the design.; AC3 the named test does not have to die on its mutant unless -> Answered by the re-design recorded at the head of the criteria: nine classes rather than four, shell verifiers never executed, the ruling split into US0821, the gate shipped in `report` mode by default, and each unit declaring only the files it changes.; AC1 the unknown half of the unit's own title has no criterio -> Closed: US0820 AC1 names `green`, `never-fails` AND `unreadable` as the blocking classes and AC4's proceed-set names the rest explicitly, so no class is left unpinned by omission.; AC1/AC2 the refusal needs a config mode or a recorded waiver -> Answered by moving the consequence to the gate: the probe classifies, and US0820 ships behind `review.plan\_falsifiability: report/block/off` with `report` as the shipped DEFAULT, so a machine missing a runner reports rather than refuses and the sibling helper's advisory stance is preserved where it matters.; Affects must name only the files this unit changes, and US08 -> Closed: US0819 and US0821 declare verify\_ac.py and its test module, US0820 declares sprint.py and its test module. The ledger collision between the two verify\_ac units is real and is handled by registering both after the last edit to that file.; settle it before code -> Settled before code, as asked. New AC6 on US0819 classifies a shell or eval verifier `not-probed` and executes nothing for it, so the greenfield rehearsal's only criterion is named rather than called green, the plan proceeds and the push and release boundaries are untouched. | - | plan-review | 7f4d8c01b3a8 |
| US0819 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | AC6 names only shell and eval, but http is the third v -> Closed: AC6 now names every verb the runner executes through a shell - `shell`, `eval` AND `http`, whose builder returns a piped curl-and-jq string - and a second row narrows the short-circuit to the first two so the http case is measured.; AC6's mutant row says "shell-backed verbs", which is broader -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC7 makes three claims - each is named, the two are counted -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; the consequential mutant, counting manual as a finding, su -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified.; AC8 got its not-a-finding mutant and AC7 did not -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC5 names FOUR untrustworthy shapes and pins them with TWO r -> Closed: AC5's four shapes now carry five rows, one per shape - the invalid kind, the blocked kind, the timeout status, the absent-runner status and the runner-error status.; AC8's figures do not reproduce on this tree: running every c -> Closed by re-measuring rather than by argument: I ran all 195 pytest selectors carried by this repository's 50 non-Draft non-terminal units and got 14 green criteria across 4 units, which reproduces the reviewer's figure exactly and refutes my own. Both US0819 AC8 and US0820 AC4 now carry it.; 216 over all 177 non-terminal units), 181 selectors resolve -> Closed by re-measuring rather than by argument: I ran all 195 pytest selectors carried by this repository's 50 non-Draft non-terminal units and got 14 green criteria across 4 units, which reproduces the reviewer's figure exactly and refutes my own. Both US0819 AC8 and US0820 AC4 now carry it.; the SHAPE claim is confirmed (all four sit at Open and each -> Closed: AC5's four shapes now carry five rows, one per shape - the invalid kind, the blocked kind, the timeout status, the absent-runner status and the runner-error status.; AC3/AC5 - one point of the outcome space is still unclassifi -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; it is not ok, not vacuous, not 124, not 127, not invalid and -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; grep is excluded from vacuity by construction (\\\_TEST\\\_KI -> Closed: AC5 now names a runner-level error whose exit status cannot attribute it - a malformed grep pattern, a missing path, a glob matching nothing - as `unreadable`, with its own row, and records that grep is excluded from the vacuity test by construction.; exposure on this repo is currently nil (every criterion on a -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC6 leaves http unassigned between "probed" and "not-probe -> Closed: AC6 now names every verb the runner executes through a shell - `shell`, `eval` AND `http`, whose builder returns a piped curl-and-jq string - and a second row narrows the short-circuit to the first two so the http case is measured.; AC7 needs a row naming the mutant that counts manual (or u -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified.; AC1 - one row for three claims: "merge the passing branch in -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - no class for GREEN-because-already-delivered. Measured -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; US0553 is Won't Implement). Zero of the 17 are the defect CR -> Closed by re-measuring rather than by argument: I ran all 195 pytest selectors carried by this repository's 50 non-Draft non-terminal units and got 14 green criteria across 4 units, which reproduces the reviewer's figure exactly and refutes my own. Both US0819 AC8 and US0820 AC4 now carry it.; AC2 - vacuous is three states, not one, and AC2 sends all -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2/AC4 - three verifier states have no class at all, and un -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; python3 tools/check\\\_links.py -> kind=invalid rc=2 -> Closed: AC5 now names a runner-level error whose exit status cannot attribute it - a malformed grep pattern, a missing path, a glob matching nothing - as `unreadable`, with its own row, and records that grep is excluded from the vacuity test by construction.; a criterion with no Verify line at all is UNSPECIFIED and ne -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified.; AC4 - the single row pins the timeout half only. "drop the u -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC4 - it reverses a documented decision in the same file wit -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AGENTS.md lists pytest/jest/vitest/go/curl/jq/rg as SOFT dep -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC5 - the ruling hash is specified over the criterion's TEXT -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC5 - it copies the coverage-ruling SHAPE and not the proper -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1-AC5 - nothing says the probe leaves the artefact unmodif -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; header - Affects is identical to US0820's (verify\\\_ac.py, sp -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; revert-check would also revert sprint.py for US0819. Decla -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - no class for green-because-already-delivered (measured -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2 - vacuous conflates deleted/renamed/skipped with not-yet -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2/AC4 - manual, unspecified and invalid verifiers have no -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified.; AC4 - one row for two branches, the absent-runner shape diff -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC5 - the ruling hash must cover the Verify selector, with a -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1-AC5 - add a criterion and a row that the probe writes no -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC3/AC4 - the state set has no home for kind="invalid" or -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; measured against run\\\_verifier on a throwaway fixture, a m -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; verify\\\_ac.py:4588 (revert\\\_check) already carries the fix -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2 - vacuous is the sole discriminator for not-yet-writt -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; a permanently skipped test exists, was collected and can nev -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1-AC5 - manual and unspecified verifiers have NO class a -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified.; the corpus holds 56 manual and 72 unspecified criteria, and -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified.; AC5 - the plan reproduces coverage rule's SHAPE and not th -> Closed by re-measuring rather than by argument: I ran all 195 pytest selectors carried by this repository's 50 non-Draft non-terminal units and got 14 green criteria across 4 units, which reproduces the reviewer's figure exactly and refutes my own. Both US0819 AC8 and US0820 AC4 now carry it.; as written a one-character reason is a valid pin -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC5 - no criterion ships a WRITER for the ruling, so the onl -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; coverage rule is a validated CLI verb with a --fields-file -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC4 - two distinct claims (the verifier times out, and it na -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC5's two claims each got a row and AC4's did not -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; Affects overstates the unit - US0819 declares sprint.py and -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2/AC3 - resolve\\\_pytest\\\_from\\\_cache states that "a red -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; reported, not a regression from this plan -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC3/AC4 invalid and blocked classify as red, the healthy c -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2 vacuous conflates an all-skipped run with not-yet-writ -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1-AC5 manual and unspecified criteria have no class though -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified.; AC5 the ruling carries the hash and none of coverage rule' -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; Affects must name only the files this unit changes before an -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC5 presumes "a recorded ruling" but no criterion in either -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC3 - the classifier is specified only against selectors -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; CR0569's Recommendation promises "Manual and unspecified ver -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified.; AC4 makes an absent runner a hard non-zero exit, and AGENTS. -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; CR0569's Recommendation names three classes (red, not-yet-wr -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; the review brief asks whether the split recreates the report -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; Affects on US0819 and US0820 declares the same four files wh -> Closed: AC5's four shapes now carry five rows, one per shape - the invalid kind, the blocked kind, the timeout status, the absent-runner status and the runner-error status.; AC5 has no shipped writer for the ruling it requires -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC3 do not say what the classifier does with a verb that -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; the manual and unspecified classes CR0569's Recommendation p -> Closed: AC7 gains a second row that adds a manual criterion to the finding tally deciding the exit status, and the criterion now says in terms that the command still exits 0 on a batch whose only classified criteria are manual or unspecified. | two more rows needed | plan-review | 1d99baf0e2a3 |
| US0820 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | AC3 pins "a value that is none of the three is REFUSED namin -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; both siblings carry an explicit raw is False -> "off" bran -> Closed: AC3 now says the bare spelling counts, which YAML reads as the boolean false rather than as the string, and a second row compares the configured value as a string only so the boolean falls through to the refusal.; AC1 and AC2 pin what the gate does with each criterion CLASS -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; the natural implementation is a broad except that proceeds s -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC6's parenthetical "the one class that appears in a refusal -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC3 must say that off written unquoted - which YAML reads -> Closed: AC3 now says the bare spelling counts, which YAML reads as the boolean false rather than as the string, and a second row compares the configured value as a string only so the boolean falls through to the refusal.; AC1 - this refusal turns the push and release boundaries red -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 - the gate's false-positive rate on this repo's own -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; US0553 is Won't Implement. None is a criterion that proves n -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC3 - the declared mutant SURVIVES on the batch AC3 names. R -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - one row for two claims. "delete the probe call from th -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC3 - no row pins that the test enters the shipped comma -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; the wiring into the command people actually run is what make -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; nothing refuses a test that calls the library gate directly -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - the trust boundary is crossed and no criterion carries -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; pass it False and every shell and eval verifier returns kind -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2/AC3 - the cost of putting execution on the plan path -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; verify\\\_ac.py's own record of the release gate is "694 of th -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; header - Affects is identical to US0819's (verify\\\_ac.py, sp -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - the greenfield rehearsal fixture's shell true classi -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 - 5 of 52 open units on this repo would be refused t -> Closed by re-measuring rather than by argument: I ran all 195 pytest selectors carried by this repository's 50 non-Draft non-terminal units and got 14 green criteria across 4 units, which reproduces the reviewer's figure exactly and refutes my own. Both US0819 AC8 and US0820 AC4 now carry it.; AC2's control must carry that case -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC3 - the declared mutant survives on the batch AC3 names -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; name the discriminating class or the row pins nothing -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - second row for the naming half of the refusal -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC3 - a row pinning that the criterion is exercised thro -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - a criterion deciding whether the plan path may execute -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 - the refusal reddens the push and release boundarie -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; rehearse-release.sh appears in neither unit's Affects, so re -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 - the plan does not decide whether the probe execute -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; with shell OFF every one of those returns kind=blocked and c -> Closed: AC3 now says the bare spelling counts, which YAML reads as the boolean false rather than as the string, and a second row compares the configured value as a string only so the boolean falls through to the refusal.; AC3 - the mutant survives its own criterion: recomputing eac -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC3 - quoting is not the anti-drift property -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; sprint.py's own precedent is \\\_affects\\\_blocking, written -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - the unit's own title and CR0569's criterion both say " -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2's proceed-list excludes unknown only by omission -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 - no config mode and no recorded waiver, against the -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; an unconditional new refusal in the most-run planning comman -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 - no criterion bounds the cost, and sprint plan do -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; measured on the last real batch (16 units, 72 criteria, ever -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; Affects overstates the unit - US0820 declares verify\\\_ac.py -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; ordering - US0819 can land alone as an inert verb nobody cal -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 the greenfield rehearsal's Verify: shell true crit -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 the shell question is undecided and both branches ar -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC3 the named test does not have to die on its mutant unless -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 the unknown half of the unit's own title has no criterio -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC2 the refusal needs a config mode or a recorded waiver -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; Affects must name only the files this unit changes, and US08 -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 pins only the GREEN case while the story's own title and -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 proposes an unconditional refusal with no mode and no da -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 requires the refusal to name only the unit and the crite -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2's control set does not include the greenfield shape and -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC3 requiring the plan to quote the probe's class rather tha -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; Affects on US0819 and US0820 declares the same four files wh -> Closed: AC5's four shapes now carry five rows, one per shape - the invalid kind, the blocked kind, the timeout status, the absent-runner status and the runner-error status.; AC1 and AC2 leave the unknown class unpinned although the -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 has no mode and no cutoff, so a consuming project has no -> Closed: AC3 now says the bare spelling counts, which YAML reads as the boolean false rather than as the string, and a second row compares the configured value as a string only so the boolean falls through to the refusal.; AC1's refusal is specified to name the unit and criterion on -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC2's control set omits the greenfield rehearsal shape, whic -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control. | settle it before code | plan-review | 7f4d8c01b3a8 |
| US0821 | 2026-09-09 | Claude Fable 5.1; session | 2026-09-09 | AC1/AC5 - no row covers a reason (or, if the criterion cell -> Closed by new AC7 on US0821: a criterion title or a reason carrying a pipe, a backtick or a newline round-trips and the table still parses, with two rows deleting the pipe substitution and the whitespace fold. The measurement - one raw newline carrying a heading marker drops every live row - is recorded in the criterion.; measured against the sibling parser, an unescaped pipe leave -> Closed by new AC7 on US0821: a criterion title or a reason carrying a pipe, a backtick or a newline round-trips and the table still parses, with two rows deleting the pipe substitution and the whitespace fold. The measurement - one raw newline carrying a heading marker drops every live row - is recorded in the criterion.; the criterion does not even fix whether the criterion cell h -> Closed by new AC7 on US0821: a criterion title or a reason carrying a pipe, a backtick or a newline round-trips and the table still parses, with two rows deleting the pipe substitution and the whitespace fold. The measurement - one raw newline carrying a heading marker drops every live row - is recorded in the criterion.; AC3 - only the selector half of "the hash covers the title A -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; the plan's own rationale ("the selector is what actually run -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC5 - the withdrawal path carries none of the refusals its s -> Closed: AC5 now holds the withdrawal's reason to the same floor, requires the row NAMED rather than one matched on reason text, and refuses a withdrawal naming no live row rather than exiting 0. Three rows added.; AC2 - the duplicate refusal has no positive control: nothing -> Closed: AC2 now requires that a criterion whose only rulings are withdrawn or stale accepts a new one, with a row dropping the liveness filter from the collision lookup.; AC1 - "the floor the sibling writers share" is not distingui -> Closed: AC5 now holds the withdrawal's reason to the same floor, requires the row NAMED rather than one matched on reason text, and refuses a withdrawal naming no live row rather than exiting 0. Three rows added.; only a row that patches the constant and asserts the boundar -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1/AC5 - nothing asserts the artefact's other sections surv -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; AC1 - no --fields-file parity, though the reason is the sa -> Answered by the round-two repair recorded on the artefacts: the shell-backed verbs are named in full, the four untrustworthy shapes each carry a row, the figures are re-measured, the mode reads YAML's boolean spelling, and the ruling gains cell escaping, the withdrawal refusals and a re-ruling control.; testplan derive and \\\_testplan\\\_rows truncate a mutant a -> Filed as BG0658 rather than folded in: `testplan derive` writes its rows unescaped and reads them back by splitting on a raw pipe, so a mutant naming a piped command is silently truncated today. It is the live instance of the class and it sits in the function this story extends.; AC1/AC5 cell escaping and newline folding -> Closed by new AC7 on US0821: a criterion title or a reason carrying a pipe, a backtick or a newline round-trips and the table still parses, with two rows deleting the pipe substitution and the whitespace fold. The measurement - one raw newline carrying a heading marker drops every live row - is recorded in the criterion.; AC3 missing the re-titled-criterion half of the hash -> Closed: AC3 gains the row that narrows the digest input to the selector alone, so the re-titled half is measured too.; AC5 missing the withdrawal floor, the withdrawal-matches-not -> Closed: AC5 now holds the withdrawal's reason to the same floor, requires the row NAMED rather than one matched on reason text, and refuses a withdrawal naming no live row rather than exiting 0. Three rows added.; AC2 missing the positive control that a withdrawn or stale r -> Closed: AC5 now holds the withdrawal's reason to the same floor, requires the row NAMED rather than one matched on reason text, and refuses a withdrawal naming no live row rather than exiting 0. Three rows added. | - | plan-review | 74a61fd2eda7 |
| BG0608 | 2026-09-09 | Claude Opus 5 | 2026-09-09 | AC3 - the new baseline-width read is unguarded: `float(base_tests)` at tools/gate\\_timing.py:370 raises ValueError on a non-numeric `gate_budget.baseline_tests` / `full_baseline_tests`, so `python3 tools/gate_timing.py budget` exits 1 with a traceback (measured, both keys) and .githooks/commit-msg:386 swallows it under `2>/dev/null` and prints no gate-budget line at all - the whole lane goes silent with no diagnostic. Both keys are read for the FIRST time in this diff (`git log -S baseline_tests -- tools/gate_timing.py` returns 0418d963 only\; absent at 98685d9f, where the same config reports normally), every sibling config float in this function is try/except-guarded (tools/gate\\_timing.py:258, 306, 310, 362), budget\\_config states "a bad config is advisory here, never a commit failure" (tools/gate\\_timing.py:239) and the module docstring promises it "degrades to silence rather than to a wrong number" (tools/gate\\_timing.py:11). No criterion reaches the branch, and nothing else in the repo validates the key -> Fixed. The read is wrapped in the try/except its four siblings use, so a non-numeric width key degrades to the withheld clause. AC9 pins it: the mutant that restores the bare float() is killed.; AC3 - the working tree's key-naming repair is UNPINNED: the clause added at tools/gate\\_timing.py:374-380 ("Declare `gate_budget.baseline_tests` to restore it") is promised to the reader by changelog.d/BG0608.md:7-8 ("the clause names both sides and the key that restores it"), but deleting it leaves the module at 57 passed / 2 environmental failures - identical to pristine (measured on an isolated copy), so a later edit reverts the promise with the suite green -> Fixed. AC6 pins the clause that names the key, and the mutant deleting that sentence is killed.; AC3 - the justification for exempting the full series is false for one of its two shapes: tools/gate\\_timing.py:358-360 and changelog.d/BG0608.md:8-9 both say "two whole-suite totals are like-for-like even with the count unrecorded", but `elif base_tests or series != "selected"` (tools/gate\\_timing.py:368) also admits the FALLBACK path, where own\\_series is False (tools/gate\\_timing.py:293-295) and a whole-suite total is compared against the per-commit `baseline_seconds` - measured `(baseline 317s on 2026-07-26, +184% since)` for a 7,000-test full run against a ~1,400-test per-commit baseline, which is the bare cross-width percentage BG0608 exists to remove, now positively asserted in prose to be safe -> Fixed in the code rather than the prose. The carve-out is now the own-series full run alone, so the fallback limb withholds like a selected run. AC10 pins the withholding and AC11 the own-series control\; both mutants killed.; AC1/AC2 - the shipped line stutters its verdict: cmd\\_budget prefixes `OVER` followed by a space and a dash (tools/gate\\_timing.py:449) and the new leading clause ends with its own `OVER` (tools/gate\\_timing.py:318-319), so this repo's live line reads `gate-budget: OVER - rate 0.247s/test vs a 0.152 ceiling - OVER - REGRESSION: ...` where the base ref read the word once -> Fixed. The command prefixes its verdict word only when the detail does not already carry one, so the rate clause reads once. Verified on this repo: gate-budget: rate 0.247s/test vs a 0.152 ceiling - OVER - REGRESSION.; AC3 - no criterion reaches the full-series drift branch at all: AC3's Given is scoped to a SELECTED run, the full and fallback percentage is unchanged from 98685d9f and is pinned by tools/tests/test\\_gate\\_timing.py:193-196 -> Answered by AC10 and AC11, which reach the full-series drift branch from both sides - withheld on the fallback limb, stated on the own-series one.; AC3 - CLOSED in flight, recorded because it was true of the delivered commit 0418d963: changelog.d/BG0608.md then claimed the percentage was "stated only when both widths are on record" and told the reader to declare `full_baseline_tests` "to get the percentage back", both measured false for the full series\; the fragment has been rewritten in the working tree since and the claim is now correct as far as issue three above -> Accepted as recorded: the fragment was rewritten in flight and is now correct on every shape, including the fallback limb this round changed.; AC3 - guard `float(base_tests)` at tools/gate\\_timing.py:370 with the try/except its four siblings use, and pin the branch with a node -> Fixed as stated: guarded, and pinned by AC9.; AC3 - pin the key-naming clause at tools/gate\\_timing.py:374-380 with an assertion in BudgetLineTests::test\\_a\\_cross\\_width\\_drift\\_clause\\_names\\_both\\_widths\\_or\\_is\\_withheld, or drop the promise from changelog.d/BG0608.md:7-8 -> Fixed as stated: the clause is pinned by AC6 in BudgetLineTests, and the promise in the fragment now matches the code.; AC4 has no paired control - appending the "did not record" note unconditionally (tools/gate\\_timing.py:336, hoisted above the `if series == "selected"` block) passes all 59 tests in tools/tests/test\\_gate\\_timing.py, so nothing asserts the note is WITHHELD when a count IS recorded\; AC1 got exactly this control in AC2 and AC4 got none (tools/tests/test\\_gate\\_timing.py:917-929) -> Fixed. AC5 is the paired control: with a count recorded the line must not say the width is unrecorded, and it names the width. The unconditional-note mutant is killed.; the changelog fragment's `baseline_tests` claim is refuted by execution - as committed in 0418d963 changelog.d/BG0608.md:10-11 said declaring `full_baseline_tests` gets the percentage back, and a 64-combination sweep of the config/record space finds it changes whether a percentage is stated in 0 cases (`base_tests` is read from `full_baseline_tests` only when `own_series` is true, which forces `series != "selected"` and already satisfies the disjunction at tools/gate\\_timing.py:368), against 16 for `baseline_tests`\; the working tree has since rewritten the fragment to claim the clause "names ... the key that restores it" and added that sentence to tools/gate\\_timing.py:376-377, and that repair is UNPINNED - deleting the "Declare `gate_budget.baseline_tests` to restore it" sentence passes all 59 tests -> Fixed. The fragment no longer claims either key restores anything it does not. With the fallback limb now withholding, baseline\_tests restores the percentage there, and full\_baseline\_tests names the baseline width on the own-series limb. Both keys are documented in the project config.; tools/gate\\_timing.py:373 the `"an unrecorded number of"` arm of `here` survives replacement by a literal "ZZZ" against the whole module - a new branch, reachable (selected run + baseline declared + no `total.selected.tests` recorded), reached by no criterion and asserted by no test -> Fixed. AC8 reaches that arm - a selected run with a baseline declared and no count recorded - and the literal-replacement mutant is killed.; tools/gate\\_timing.py:322 `over {tests:.0f} tests` in the leading clause survives deletion against the whole module, so the Proposed Fix's "state the width beside any total that is kept" ships unpinned -> Fixed. AC7 pins the width in the leading clause and the deletion mutant is killed.; the FALLBACK full-run path still prints a bare cross-width percentage - a 7,400-test full run against `baseline_seconds: 317` (a selected-series baseline at ~1,300-1,800 tests) reads "+184% since" identically at base ref 98685d9f and at 0418d963, so the behaviour is not this diff's\; the new comment at tools/gate\\_timing.py:358 justifies the carve-out as "a full run against a full-series baseline", which is [new] and false for that path, where `own_series` is False and the baseline read is the per-commit one -> Fixed rather than carried: the fallback limb withholds now, so the bare cross-width percentage is gone from the one path that still printed it. AC10 pins it.; tools/gate\\_timing.py:364-367 (non-numeric baseline) is uncovered by the whole module - measured uncovered at base ref 98685d9f too (base lines 321-322), so not a regression -> Fixed. AC9 covers the non-numeric read, which is now guarded.; changelog.d/BG0608.md "the width here moves from about 1,400 to about 5,600 tests" matches only the stale window in sdlc-studio/.config.yaml (1,418-5,573) and not the artefact's own Evidence (1,292-6,041) or the live series in sdlc-studio/.local/gate-timings.json (2,197-6,101) -> Fixed. The fragment cites the live series: 2,197 to 6,101 tests over the ten recorded selected runs, measured from the timings file rather than a stale config comment.; AC4 has no paired control - the unconditional "did not record" note at tools/gate\\_timing.py:336 passes all 59 tests -> Fixed as stated, by AC5.; the `baseline_tests` claim in changelog.d/BG0608.md is refuted by execution as committed (0 of 64 combinations for `full_baseline_tests`) and its in-flight repair at tools/gate\\_timing.py:376-377 ships with no test - deleting the sentence passes all 59 tests -> Fixed as stated, by correcting the fragment and by giving the fallback limb the withholding the fragment described.; AC3 - changelog.d/BG0608.md:9-11 states two things the shipped code does not do. (a) "The drift percentage is stated only when both widths are on record" is false for the entire full series: executed against a fixture declaring full\\_seconds/full\\_baseline\\_seconds/full\\_baseline\\_date with no full\\_baseline\\_tests and no total.tests, gate\\_timing.py prints `900s of a 1080s budget over a selection width this run did not record (baseline 899s on 2026-08-19, +0% since)` - neither width on record, percentage stated, in the same sentence that admits the width is unknown\; with only the current width it prints `(baseline 899s on 2026-08-19, +0% since)`. (b) "Declare `gate_budget.baseline_tests` (or `full_baseline_tests`) to get the percentage back" is false for full\\_baseline\\_tests: the full series never withholds a percentage (tools/gate\\_timing.py:363 `elif base_tests or series != "selected"`), so that key restores nothing - it only appends a width to a clause that already carried the figure. A consumer following the fragment declares a key for an effect they already have, and believes a guarantee that holds only on the selected series -> Fixed. Both claims are now true of the code: the percentage is withheld wherever the two totals are not like-for-like, and the fragment states which key restores it on which series.; AC3 - the withheld-percentage clause never names the key that restores it (tools/gate\\_timing.py:369-373). It spends 131 characters explaining the withholding and none on the remedy, while both sibling disclosures in the same function name theirs - `gate_budget.full_seconds` at :348 and `gate_budget.rate_seconds_per_test` at :331 - and naming the key was made law for the neighbouring clause by BG0594 AC5. The changelog knows the remedy\; the line a person meets on every commit does not, so the reader who just lost the trend has no route back from the line -> Fixed. The clause names the key for the series it is on, and AC6 pins it.; AC3 - scope creep: `full_baseline_tests` is a new public config key that no criterion requires, no test covers (zero hits across tools/tests/ and .claude/), no config comment documents (sdlc-studio/.config.yaml:241-272 comments every other gate\\_budget key), and whose only description is the false sentence above -> Answered. The key stays, with a behaviour and a document: it names the baseline width on the own-series limb, and the project config now comments both width keys and says why neither is declared here.; AC1/AC2 - the verdict word is now printed twice in the first sixty characters: cmd\\_budget prefixes `OVER` followed by a space and a dash (gate\\_timing.py:447) and the new leading clause repeats it, so this repo prints `gate-budget: OVER - rate 0.247s/test vs a 0.152 ceiling - OVER - REGRESSION: ...`. Not misleading, but it is the first thing a reader meets and it stutters -> Fixed. The command prefixes its own verdict word only when the detail carries none.; AC1 - every branch of the line got materially longer, on a line the bug itself says "has now twice been the thing that cost operator attention during a close". Measured base 98685d9f vs HEAD on identical inputs: this repo's live line 234 -> 349 chars (+49%)\; selected run with an unrecorded width and a rate ceiling 231 -> 427 (+85%)\; no-count no-ceiling 49 -> 96 (+96%). The ordering fix was asked for\; the growth was not, and most of it is the un-actionable explanation in the finding above -> Accepted and not reversed. The length is the disclosure: every branch that grew, grew by saying what it could not compare. The stutter is gone, which is the one growth that carried no information.; AC4 - a full run still states a drift percentage against a baseline of unrecorded width, including when neither side's width is known\; base 98685d9f printed `900s of a 1080s budget (baseline 899s on 2026-08-19, +0% since)` for the same fixture, so the behaviour is unchanged. What is new is that AC4's clause now makes it self-contradictory in one line, and the fallback limb (gate\\_timing.py:346-349) still compares a FULL total to `baseline_seconds`, the selected-series baseline - executed: `900s of a 380s budget over 7400 tests [FULL run judged against the per-commit ceiling ...] (baseline 317s on 2026-07-26, +184% since)`, which is exactly the cross-population percentage AC3 removes one limb above. The in-code comment at gate\\_timing.py:353-361 defends this as "like-for-like", but the label it points at is about the ceiling, not the baseline -> Fixed. That is the fallback limb, which now withholds - AC10 - so the line no longer contradicts itself.; AC1-AC4 - the artefact's paperwork does not record the delivery. sdlc-studio/bugs/BG0608-...md Revision History carries only "2026-08-24 / sdlc-studio / Filed", where BG0633 and BG0655 in this same commit and the predecessor BG0594 each carry delivery and review rows\; and the Proposed Fix still demands "`seconds: 380` should either be re-declared as a full-suite figure or removed for the selected series" with nothing in the artefact saying it was superseded. Q5 is answered: the plan review's round-2 finding #5 WAS ruled - "Accepted: the Proposed Fix's config demand is superseded by the criteria and the file is out of `Affects`" - but the ruling lives only in sdlc-studio/reviews/repair-record.md:291, so a later reader of the bug sees an undelivered Fix clause and no ruling -> Fixed. The Revision History carries the delivery, the plan-review ruling that superseded the Proposed Fix's config demand, and this round.; AC3 - the changelog fragment's two false claims (changelog.d/BG0608.md:9-11), both refuted by execution: the percentage is stated with neither width on record on the full series, and `full_baseline_tests` restores nothing. Either the fragment states what the code does, or the code withholds on the full series as the fragment already claims it does. Fixing the fragment alone leaves `full_baseline_tests` a key that no criterion, no test and no document justifies, so withdraw it or give it the behaviour the fragment promises -> Fixed as stated: the code withholds on the fallback limb, the fragment describes what the code does, and full\_baseline\_tests keeps a documented behaviour. | - | delivery | 8eed61578bfc |
| BG0637 | 2026-09-09 | Claude Opus 5 | 2026-09-09 | AC5 - the refusal fires INSIDE record\\_repair's per-rejection write loop (critic.py:1265-1292 calls \\_clean on each group\; the raise is critic.py:233-238), so a repair whose closures span two rejection dates with one odd-backtick value appends the FIRST row and then refuses: `critic.py repair` prints "repair refused" and exits 2 with a row already committed to the append-only repair record, and the corrected re-run duplicates it - repair\\_state then reported "3 fixed" for 2 findings. Proven at the shipped CLI in a throwaway fixture, and proven [new] by running the identical case against 98685d9f's critic.py, which wrote both rows and refused nothing. Fix: check parity for every value before the first \\_append\\_row, beside unreadable\\_closures at critic.py:1194 -> Fixed. Every row's cells are cleaned before the first append: record\_repair builds the whole list, then writes it. Proven at the CLI on a two-rejection fixture - the delivered code leaves one orphan row, the repaired code leaves no file at all - and the clean control still writes both rows. AC8 pins it.; AC3's "943 doubled escapes" measures 1134 in repair-record.md today, and the artefact title's "655 identifiers" measures 2186 escaped underscores inside code spans across the three ledgers - neither figure was re-measured at delivery and the fix backfills none of them -> Re-measured and recorded. Across the three ledgers today: 1,136 doubled escapes, 1,134 of them in repair-record.md, and 2,987 escaped underscores inside code spans by a single-pair span walk. The fix backfills none, which is what the Proposed Fix says\; the figures are dated in the Revision History.; 12 rows already in sdlc-studio/reviews/critic-evidence.md carry odd backticks and are now unrecordable text - measured refusal rate over 2635 real free-text values from the corpus and the ledgers is 12 (0.46%), every one genuinely unbalanced, so AC5 behaves as designed -> Accepted as designed and recorded. Twelve of 12,422 live ledger values are refused by this rule, every one genuinely unbalanced. The new-rule and old-rule refusal sets are identical on the corpus.; AC5 - the torn write in record\\_repair (critic.py:1265-1292): a refused repair must not leave a row in the append-only ledger -> Fixed as stated: a refused repair writes nothing, pinned by AC8.; AC5 - the new refusal reaches one caller UNCAUGHT: plan\\_review.py record with an odd-backtick --notes raises ValueError out of critic.py:235 through record\\_review (plan\\_review.py:179) to cmd\\_record (plan\\_review.py:328), which has no try/except, so the user meets a Python traceback and exit 1 where every sibling path prints a named refusal and exits 2. Executed at base ref 98685d9f, \\_clean returns a string and cannot raise, so the crash is new\; reference-config.md:438-444 tells the user to PREFER this exact command over critic.py record --phase plan-review -> Fixed. plan\_review.py record catches the refusal and prints a named one with exit 2, the way its siblings do. AC10 drives the shipped command and pins it.; AC5 - the "REFUSED naming the value" clause is unpinned: test\\_an\\_odd\\_backtick\\_count\\_is\\_refused\\_at\\_the\\_write asserts only that the message contains the word "backtick", which still passes if the value is deleted from the message, so the half of AC5 that carries its whole product value can be reverted with the suite green -> Fixed. The test asserts the refused value appears in the message, so deleting it from the message reddens the row.; AC5 - the refusal names only text[:120] (critic.py:239) while 71 percent of this repo's 1,918 review-ledger findings cells exceed 120 characters (median 418, p90 1,766), and in ALL 12 of the corpus rows this rule would have refused the 120-character excerpt contains ZERO backticks: the message says "drop the stray backtick" over an excerpt holding none, which is precisely the complaint this bug's own Impact section makes about the old failure, reporting a position hundreds of characters from the real stray -> Fixed. The message quotes sixty characters either side of the stray and gives its offset and the value's length. AC9 pins it with a value whose stray falls past the first 120 characters.; four of the SEVEN reproductions the artefact's own Revision History records are the trailing-space MD038 class (2026-09-08 "a bare word inside a code span with a trailing space", and the 2026-09-09 withdrawal sentinel, http verifier and level-two heading marker rows) and this fix neither addresses nor rules them - executed, \\_clean returns a span with a trailing space verbatim, and .markdownlint.json sets default true so MD038 still blocks those commits\; no criterion covers the class and BG0634 is Won't Fix on a different premise, so the unit closes with four of its seven recorded reproductions live and unrecorded -> Ruled and filed. The trailing-space class is not covered by any criterion here and is BG0659, which measures the corpus and decides between padding to the CommonMark form and refusing at the write.; AC5 the uncaught ValueError out of plan\\_review.py record -> Fixed as stated, by AC10.; AC5 the untested "naming the value" clause -> Fixed as stated: the value is now asserted in the message.; AC5 the 120-character excerpt that omits the offending backtick in every measured real case -> Fixed as stated: the excerpt is centred on the stray.; \\_SPAN\\_RE (critic.py:194, a pattern matching ONE backtick, any run of non-backticks, one backtick) reads a DOUBLE-backtick span as an empty span followed by outside text, so `_clean('``_read_rows``')` still returns ``\\_read\\_rows`` - the artefact's own Steps to Reproduce 1, run verbatim at HEAD - while AC1 says "INSIDE a code span" with no qualification of the delimiter\; the base ref behaved identically (so the corruption is not a regression) and the three ledgers hold ZERO double-backtick spans today, but the new span definition this diff introduces is incomplete and AC1 is unmet for that shape -> Fixed. The delimiter is a RUN of backticks paired with the next run of the same width, so a double-backtick span's interior is left alone. AC7 pins it and the single-backtick mutant is killed.; a backslash-escaped backtick is now REFUSED - `_clean(r"a literal \\` backtick and a _name")` raises at critic.py:235 where the base ref wrote it through, yet `\\`` is valid markdown for a literal backtick and needs no closing partner, so the parity count blocks a value the file can hold and the message's remedy ("Close the span or drop the stray backtick") is the wrong instruction -> Fixed. An escaped backtick is not a delimiter and does not count toward parity. AC11 pins it and the lookbehind-deletion mutant is killed.; prose drift - test\\_critic.py:6225 still names the superseded AC4 mutant ("hoist the pipe and newline substitutions into the outside-a-span branch") while the Test Plan row was corrected at delivery to "delete the pipe substitution"\; I executed BOTH and both redden AC4, so no coverage is lost -> Fixed. The docstring names the shipped mutant, deleting the pipe substitution, matching the Test Plan row.; the commit message's "Two of this bug's seven recorded reproductions were exactly that [a closure key cut at sixty characters, through a code span]" is UNVERIFIABLE as written - the revision history records ONE such row (the FOURTH) -> Accepted. The claim is withdrawn: the Revision History records one such row, not two.; the fix repairs no existing corruption - repair-record.md still carries 1134 doubled escapes (943 at base ref 98685d9f, the figure AC3 cites, verified) - correctly out of scope per the Proposed Fix, recorded so the close does not read the ledgers as clean -> Accepted and recorded with today's figures. Backfilling the corpus is out of scope by the Proposed Fix and is not smuggled in here.; \\_SPAN\\_RE (critic.py:194) does not treat a double-backtick span as a code span, so AC1 is unmet for that shape and the artefact's own Steps to Reproduce 1 still reproduces at HEAD -> Fixed as stated, by AC7. | - | delivery | efb9a5f789e4 |
| BG0654 | 2026-09-09 | Claude Opus 5 | 2026-09-09 | AC1/AC2 - as committed, tools/enable-hooks.sh:44 read the existing value with `git config --local --get`, so a developer whose core.sshCommand is set GLOBALLY had it shadowed by the local keepalive write: with `ssh -i ~/.ssh/id_ai_ops -p 2222` in force, the effective command after the script was `ssh -o ServerAliveInterval=60 -o ServerAliveCountMax=10` - identity and port gone, and the next push fails to authenticate - while AGENTS.md:56 and changelog.d/BG0654.md both claimed unconditionally that an existing value is left as the developer set it. Neither named node can see it: AC1 and AC2 both pin GIT\\_CONFIG\\_GLOBAL at /dev/null, and my extra mutant widening the read to `git config --get` SURVIVED. ANSWERED IN THE WORKING TREE but UNCOMMITTED - the precondition now reads the effective value and names the scope, AC4 was added, and I re-ran both the probe and KeepaliveTests (4 passed) against it - AC4 still reads `Verified: no` -> Fixed and now committed. The precondition reads the effective value and names the scope it found it in, AC4 pins it, and the mutant narrowing it back to local scope is killed. AGENTS.md and the changelog fragment both say what the script does.; AC3 - tools/tests/test\\_pre\\_push\\_hook.py:536-546 asserts on the hook's SOURCE TEXT, while the criterion says the hook is "driven as a subprocess in the existing hook fixture" and "that output names the keepalive ... on stderr". Wrapping the echo in `if false\; then ...\; fi` leaves every assertion true and the node PASSES while the hook prints nothing (mutant SURVIVED). The behaviour is right - I drove the shipped hook through the module's own \\_Clone fixture and the line appears on stderr immediately after the cost line - so the selector, not the hook, is what must change -> Fixed. The node drives the shipped hook through the module's own push fixture and reads stderr. Both mutants are killed: stripping the sentence, and wrapping it in a false branch so it is present and unreachable.; the keepalive line sits after the red-CI acknowledgement block (.githooks/pre-push:60-93), so a push refused for an unacknowledged red main exits before it is ever printed -> Fixed. The line is printed before the red-CI read, so a push refused for an unacknowledged red main sees it. AC5 pins it and the move-it-back mutant is killed.; AC1/AC2 - the global core.sshCommand shadow, until the working-tree repair is committed and AC4 is verified -> Fixed as stated: committed, with AC4 pinning it.; AC3 - the selector asserts source text where the criterion demands the driven hook's stderr, and a reachability mutant survives it -> Fixed as stated: the selector drives the hook, and the reachability mutant is killed.; AC3's test asserts the hook's SOURCE TEXT, not its output - tools/tests/test\\_pre\\_push\\_hook.py:567 does `(REPO / ".githooks" / "pre-push").read_text()` and greps it, while the criterion says "driven as a subprocess in the existing hook fixture, when it prints its opening cost estimate, then that output names the keepalive ... on stderr where the pusher reads it"\; a mutant that KEEPS the sentence but wraps it `if false\; then ... fi` (so no pusher ever sees it) PASSES the shipped node and is KILLED by the criterion's own fixture (`_Clone().push()`, tools/tests/test\\_pre\\_push\\_hook.py:135) - I ran both, the shipped node stayed green and the subprocess check failed with the hook's full stderr -> Fixed as stated: the node drives the hook and reads stderr, and the false-branch mutant is killed.; CLOSED (verified by execution) the product seat's global-shadow finding: the delivered commit 6da143b9 read `git config --local --get` and my /dev/null-scrubbed probe turned an effective `ssh -i ~/.ssh/id_ai_ops -o User=ops` into a bare `ssh -o ServerAliveInterval=60 ...`\; the working-tree tools/enable-hooks.sh:49 now reads the effective value, my probe leaves the global value in force and names its scope, AC4's own mutant (narrow the precondition to `--local`) kills AC4 and nothing else, and both false prose claims (AGENTS.md, changelog.d/BG0654.md) are corrected -> Recorded as closed by the reviewer's own execution, and the repair is in this commit with AC4 pinning it.; non-blocking consequence of that repair, disclosed rather than defective: a developer whose GLOBAL core.sshCommand carries no keepalive now gets NO keepalive at all, only a printed hint (tools/enable-hooks.sh:50-52), so this bug's headline failure - the connection dying across the gate - is unsolved for exactly the developers who set their own ssh command\; AC4 legislates it, so it needs a ruling rather than a code change -> Ruled, not coded. A developer who chose their own ssh command is not overwritten - AC4 legislates that deliberately - and AGENTS.md now says in the same sentence to add a keepalive if yours has none.; AGENTS.md:56 (working tree) reads "An `core.sshCommand` already in force at ANY scope" - "An" should be "A", in a file this unit declares in its Affects -> Fixed. The sentence reads A rather than An, and now also names what to do when your own command carries no keepalive.; AC3's test greps the hook's source text instead of driving it as a subprocess and reading stderr, as its criterion specifies (tools/tests/test\\_pre\\_push\\_hook.py:567)\; a present-but-unreachable keepalive line passes it -> Fixed as stated, by driving the hook in AC3's node.; AC2 - enable-hooks.sh tests only the LOCAL core.sshCommand (git config --local --get), so a developer's GLOBAL one is silently SHADOWED by the write. Executed in a throwaway clone with GIT\\_CONFIG\\_GLOBAL holding "ssh -o User=ops -i ~/.ssh/id\\_ai\\_ops -p 2222": before, the effective value is that command\; after, the script prints "Set core.sshCommand: ssh -o ServerAliveInterval=60 -o ServerAliveCountMax=10" and the effective value is the bare ssh, with the user, identity file and port gone and nothing naming what was replaced - the next push then fails to authenticate for a reason no output gives. AGENTS.md's new sentence "An existing core.sshCommand is left as you set it" and changelog.d/BG0654.md's "An existing value is left exactly as the developer set it" are both FALSE for that case, which is the common one since a per-clone sshCommand is rare and a global one is not, and the script's own comment states the intent ("a developer who has chosen their own ssh command has chosen it deliberately") that the code does not meet. Reading the EFFECTIVE value rather than the local one satisfies AC1 unchanged (its fixture points GIT\\_CONFIG\\_GLOBAL and GIT\\_CONFIG\\_SYSTEM at /dev/null, so effective equals local equals unset) and AC2 unchanged, so the criteria do not require this behaviour and the two tests cannot tell the safe implementation from the harmful one -> Fixed. The precondition reads the effective value, AC4 pins it against exactly this shape, and both prose claims are corrected.; AC1 - the shipped keepalive is ServerAliveInterval=60 with ServerAliveCountMax=10, while the pair the artefact's own Evidence field records as the retry that landed the push is 20 and 60, changed with no note. It should still hold, since 60 seconds is well inside the roughly 480-second idle timeout the Summary records, but the delivered value is not the measured one and nothing says why -> Ruled and recorded. Sixty seconds is well inside the roughly 480-second idle timeout the Summary measures, and ten missed replies is ten minutes of silence before the connection is judged dead, which outlasts the gate. The Revision History says so.; AC3 - the pre-push keepalive line is unconditional, so it prints on every push forever including after the developer has run enable-hooks.sh and the keepalive is set, although the hook could read core.sshCommand and suppress it. AC3 as written mandates the unconditional line, so this is reported against the criterion rather than against the code -> Ruled and recorded. The hook cannot read the pusher's effective config cheaply, and one line on a push that already prints three is not worth a config read. AC3 mandates the unconditional line.; AC2 the local-only precondition check that silently shadows a global core.sshCommand while AGENTS.md and the changelog fragment both state the opposite -> Fixed as stated, by AC4 and by correcting both prose claims. | - | delivery | 26dc8c64b4f9 |
| BG0658 | 2026-09-09 | Claude Opus 5 | 2026-09-09 | AC2 - a legacy row whose TITLE cell carries a raw pipe now reads back a CORRUPTED mutant, where the base ref read it correctly. Proven end to end through the shipped entry point on one fixture artefact whose criterion title quotes gate.py --boundary push then a pipe then release: at base ref 98685d9f, verify\\_ac.py testplan derive exits 0 with "unchanged - 1 row(s) already match its 1 criteria, and 1 authored mutant(s) were kept"\; at HEAD the same command REFUSES with "BG0002 AC1: restates its own criterion - 67% of its substance is the Then clause, over the 60% ceiling", because \\_split\\_cells re-joins cells 1 to -1 (verify\\_ac.py:3198) and fuses the title into the mutant, truncating at the title's own pipe. The re-join heuristic cannot distinguish a pipe in the mutant from one in the title, and the failure it produces blames the author for a defect the reader created - the exact "sent to the wrong problem" outcome this bug was filed on. AC2's three tests only ever use the title "a title", so the family where the two implementations agree is the only one exercised -> Fixed. The legacy re-join is removed: the mutant is the second cell, always. AC2 now pins the title-pipe shape, and the re-join mutant is killed. Measured: 0 of 1,032 corpus Test Plan rows split to anything but three cells, so the re-join was dead for every real row.; AC1 - the WRITE half of AC1 is never executed by any of its verifiers, and the repo's own gate says so: transition.py set --id BG0658 --status Fixed --dry-run reports "1 uncovered added line(s) its own verifiers never executed - verify\\_ac.py: 3342", which is the \\_cell(m) and \\_cell(t) writer line. All three tests compose rows by calling \\_cell directly and testplan\\_derive is never driven, so AC1's "when the row is written" is asserted of a hand-built string, not of the writer -> Fixed. AC4 drives the shipped testplan derive and asserts the written row holds three columns and reads back unchanged. The mutant reverting the writer to a bare f-string is killed.; the commit message and the shipped code comment at verify\\_ac.py:2994 both state that half this corpus's shell verifiers are pipelines. Measured on the corpus: 36 of 370 shell verifiers carry a pipe (10 percent), and 41 of 600 across every non-pytest verifier (7 percent) -> Fixed. Both the comment and the changelog now say 36 of 370 shell verifiers carry a pipe.; BG0658 carries no Verification depth field and no Test Plan section, so transition to Fixed is refused on three requirements, the third being the uncovered writer line above -> Fixed. The artefact carries a Verification depth field and a Test Plan with a row per criterion.; AC2 the legacy re-join corrupting a mutant when the title carries a pipe, proven against 98685d9f through testplan derive -> Fixed as stated, by removing the re-join.; AC1 the writer line no verifier executes, reported by the repo's own coverage gate -> Fixed as stated, by AC4.; AC1's node never exercises the writer - reverting testplan\\_derive to the pre-fix f-string (verify\\_ac.py:3342, `table += [f"/ {i} / {m} / {t} /" for i, t, m in rows]`) passes ALL 383 tests in test\\_verify\\_ac.py including all three of this unit's nodes, so the escape at the write site is unpinned\; AC1's Given says "when the row is written and read back" but the test builds the row itself from `_cell` and never calls testplan\\_derive -> Fixed as stated, by AC4 driving the shipped command.; testplan\\_unnameable (verify\\_ac.py:3239) is a second parser of the SAME table in the same file and was not migrated - it still does `line.strip().strip("/").split("/")` with no `_uncell`, so a row this diff's writer produces (a row whose unnameable reason quotes an escaped pipe inside a code span) is read as reason 'a rg x backslash' with malformed=True, and sprint.py:2154 then refuses the batch with "`unnameable` with no reason recorded" for a row that has one - the truncation itself is pre-existing (base split raw too) but the stray backslash and the disagreement with \\_testplan\\_rows on the same row are new, and testplan\\_rows\\_by\\_criterion's own docstring at verify\\_ac.py:3203 says "two parsers of one artefact are how the row count and the criterion count came to disagree in the first place" -> Fixed. testplan\_unnameable uses the same splitter as the row reader, so an escaped pipe no longer truncates a reason. AC5 pins it and the raw-split mutant is killed.; AC1's node never exercises the writer (verify\\_ac.py:3342 revert survives all 383 tests) -> Fixed as stated, by AC4.; testplan\\_unnameable (verify\\_ac.py:3239) not migrated to \\_split\\_cells -> Fixed as stated, by migrating it and pinning it with AC5. | - | delivery | 73694491a854 |
| BG0601 | 2026-09-10 | Claude Opus 5 | 2026-09-10 | the repair this bug is named for is pinned by nothing. All three criteria drive a new private helper, \\_parity, which is a SECOND copy of the comparison, while the shipped sweep inside test\\_the\\_dry\\_run\\_gives\\_a\\_read\\_only\\_probe\\_the\\_real\\_root keeps its own inline copy. Proven by execution: restoring the sweep's slice, x equals fn(real) sliced to two fields against y equals fn(copy) sliced to two fields, leaves ALL FOUR nodes of DryRunScratchParityTests green, the sweep's own node included. The one line the title is about can be reverted with the suite still green, which is the failure mode the repo's standing practice names word for word. Worse, \\_parity is inserted directly beneath this class's pre-existing comment "NO PRIVATE COPY OF THE PRODUCTION BUILD ... A helper that rebuilds the thing under test is not a fixture, it is a second implementation, and the tests then pin the copy", which records that this class already shipped this exact defect once and that an independent review found it by deleting the code and running the suite. \\_parity's own docstring gives "two copies of the comparison is how the sweep and its own tests came to disagree" as the reason for it, and the delivery leaves two copies in the file -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the artefact records no delivery in its Revision History. The last row is "2026-08-21 sdlc-studio Filed" while all three ACs were flipped to Verified yes (2026-09-09) in this commit. artifact.py revision is the shipped command for it -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the changelog bullet "The roster is walked off the module rather than listed" reads as a change that this unit made. Measured, the walk over vars(sprint\\_report) already stood at the base ref (681021cc~1, line 16485) and AC3 is a regression pin on behaviour that was already there, not a fix. The criterion says so plainly and the changelog does not -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the sweep resolves 22 probes and actually compares only 19 - \\_ck\\_planned\\_vs\\_delivered, \\_ck\\_scope\\_creep and \\_ck\\_known\\_issues raise KeyError on both sides and are dropped silently by the except-and-continue. AC2's floor asserts more than 15 probes were RESOLVED, not that any were compared, so a sweep that skipped every one of them would still satisfy the control -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the artefact is still Status Open with every AC box unchecked while all three read Verified yes. MEASURED AND TRUE: both declared mutants die when applied to \\_parity, the eleven nodes of the class are green, and every factual number in the criteria checks out - 22 probe classes as the Summary says, zero differing across the full-width sweep on an unmodified tree, and the blind scratch does differ from the real tree at the state field, ran against not-run, so the sensitivity control genuinely could not have served as AC3's third row. -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the sweep's own two-field slice can be restored with the whole DryRunScratchParityTests class still green, because all three criteria drive a duplicated \\_parity helper rather than the sweep, in direct contradiction of the NO PRIVATE COPY comment the helper is written under -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; no Revision History row records the delivery -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; all three criteria pin a COPY of the sweep, not the sweep - the new \\_parity helper at test\\_sprint.py:16414 re-implements the comparison, and the sweep the bug is about (the probes walk and differing loop inside test\\_every\\_read\\_only\\_probe\\_honours\\_the\\_read\\_root, test\\_sprint.py:16585) was left carrying its own second copy\; measured, reverting the WHOLE production change of 681021cc - the sweep's two comparisons back to a two-field slice and the sensitivity control's three - leaves every one of the 11 tests in DryRunScratchParityTests green, AC1, AC2 and AC3 included, so the entire delivery can be reverted with its own criteria still passing -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; this is the exact failure the class's own comment three lines above \\_parity records: a helper that rebuilds the thing under test is not a fixture, it is a second implementation, and the tests then pin the copy - that comment was written after an independent review found the same defect in this same class -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the commit message claims the three criteria share one comparison helper rather than each carrying a copy, because two copies of this comparison is how the sweep and its own tests came to disagree - execution refutes it: after this commit there are exactly two copies, \\_parity and the untouched inline loop, and the criteria are attached to the wrong one -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; AC1's declared mutant (narrow the swept comparison back to the first two fields) is killed only inside \\_parity - applied to the sweep it SURVIVES all three verifiers, so three mutants three killed is true of the copy and false of the subject -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the three criteria verify \\_parity rather than the sweep, so the whole production change of 681021cc reverts with all 11 tests in the class still green -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the whole BG0601 production change reverts with the suite GREEN - the three verify nodes drive a NEW private helper \\_parity (test\\_sprint.py:16414-16436) that is a second copy of the sweep, while the sweep the bug is about is the inline loop in test\\_every\\_read\\_only\\_probe\\_honours\\_the\\_read\\_root\; restoring the two-field slices at test\\_sprint.py:16556, 16557, 16561, 16585 and 16586 leaves all 954 tests in test\\_sprint.py passing (measured, full module run), so no criterion can fail on the defect it names -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the class comment 20 lines above the new helper (test\\_sprint.py:16406-16409) forbids exactly this shape - a helper that rebuilds the thing under test is a second implementation and the tests then pin the copy - and an earlier independent review installed that comment after finding the same defect in this class -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the comment added at test\\_sprint.py:16573-16576 claims the synthetic probe BELOW it proves the walk is what selects, but no probe sits below it - the synthetic probes are above, inside the helper's callers, and they exercise the helper's walk rather than this one -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the three mutants the docstrings name do die, but only against \\_parity (measured: two-field slice kills AC1, blind scratch kills AC2, enumerated roster kills AC1 and AC3), so the criteria are self-consistent and measure the copy -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green.; the sweep's own comparison and roster are unpinned - reverting the delivered fix leaves all 954 tests green\; the three criteria must drive the sweep under test rather than a private re-implementation of it, or the sweep must be made to call the shared helper -> Fixed. The comparison's WIDTH is decided in one place both the sweep and its criteria call, and the sweep calls the shared helper rather than walking the roster a second time. Reverting the width now reddens AC1\; re-inlining the sweep's copy reddens the new AC4, which pins the single-implementation property behaviourally. Verified by hand before acting: the pre-repair tree did revert green. | - | delivery | cd62be2c466c |
| BG0591 | 2026-09-10 | Claude Opus 5 | 2026-09-10 | AC5 is recorded Verified yes and its verifier cannot fail on the disagreement it exists to pin. AC5 is "Given one fixture root, when status and close\\_owed.py detect are BOTH run as SUBPROCESSES, then the id set each NAMES as holding the close is the same set". The delivered node test\\_the\\_two\\_commands\\_name\\_the\\_same\\_blocking\\_set starts no subprocess, never invokes status at all, and asserts that the ids in close\\_owed.render's BLOCKING line are the ids in close\\_owed.blocking - one reader compared with itself, which is true by construction. Proven by execution on a throwaway fixture carrying one genuinely owed unit and one run-attributed unit: with status's advisory reverted to the raw owed list, which is exactly the defect this bug is about, the two shipped commands name DIFFERENT sets - status prints "2 delivery unit(s) reached terminal with no retro (US0005, US0009)" while close\\_owed detect prints "BLOCKING (1): US0005" - and the AC5 node still passes -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; the artefact records no delivery in its Revision History. The last row is "2026-08-17 sdlc-studio Filed" while all five ACs were flipped to Verified yes (2026-09-09) in this commit. BG0490, BG0493 and BG0578 in the same batch each carry a delivery row, and artifact.py revision is the shipped command that writes one -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC1's node carries a skipTest escape - if report run\\_attributed is ever empty the criterion goes GREEN by skipping rather than failing, so the one condition that makes the fixture meaningless also makes it silent. It does not skip today, verified by running the class with skip reporting on -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; the artefact is still Status Open with every AC box unchecked while all five read Verified yes. MEASURED AND TRUE: all five declared mutants killed by execution, the five verifiers green, and the fix itself is correct where it counts - driven as subprocesses on my fixture, status names US0005 and close\\_owed detect names BLOCKING US0005, the same set, and the run-attributed US0009 is correctly excluded from both. The velocity limb is genuinely covered, the AC4 mutant narrowing the advisory to the units limb dies, and the new BLOCKING line gives a second reader something to compare against for the first time. -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC5's verifier runs no subprocess, never invokes status, and compares close\\_owed with itself, so it passes under the exact two-surfaces-disagree mutant the criterion names -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; no Revision History row records the delivery -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC5 requires status and close\\_owed.py detect to be run AS SUBPROCESSES and their named id sets compared, and test\\_status.py:1472 does neither - CloseOwedAgreementTests begins at test\\_status.py:1316 and contains no subprocess call at all, and the test never calls status\; what it actually compares is close\\_owed.render's own BLOCKING line against close\\_owed.blocking, the function render built that line from, so the comparison is tautological and one implementation is being checked against itself -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC5's outcome clause is FALSE of the shipped code above five blocking units - measured on a seven-unit fixture through both surfaces, status names US0005 to US0009 and truncates with an ellipsis while detect names all seven, so the two commands do NOT name the same set\; the ids truncation at status.py:659 is pre-existing but the criterion asserting agreement is new and unmet, and its one-unit fixture cannot reach the branch -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; wiring blindness - replacing BOTH close\\_owed\\_advisory call sites in status.py (lines 434 and 602) with None, so the advisory is gone from the shipped CLI entirely, leaves all five verifiers green\; a pre-existing test in HintPerformanceTests catches it so the tree is not blind, but the unit's own criteria are, which is precisely what AC5 existed to close -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; status truncates its named set to five ids while close\\_owed enumerates all of them (status.py:659), so the two surfaces still disagree on membership past that boundary -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC5's verifier runs neither command as a subprocess and never reads status's named set, so it does not implement its criterion -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC5's claim that the two commands name the same set is false above five blocking units, measured -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC5 demands that status and close\\_owed detect be run AS SUBPROCESSES over one fixture root and that the id set each NAMES be the same set, but the verifier (test\\_status.py:1475-1490) runs neither as a subprocess, never invokes status at all, and only checks that close\\_owed.render names the ids close\\_owed.blocking computes - both sides of that comparison come from one function, so it cannot observe disagreement\; hard-wiring status.close\\_owed\\_advisory to return None leaves the node green (measured) -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC5 is FALSE of the delivered code - status truncates its named set at five ids (status.py:665, five-element slice plus an ellipsis) while close\\_owed names all of them, so on a fixture with eight blocking units the two subprocesses name 5 and 8 respectively (measured end to end: close\\_owed detect prints BLOCKING (8) US0002-US0009, status prints US0002-US0006 and an ellipsis) -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC1's verifier degrades to SKIPPED, which is green, when the run-attributed limb yields nothing (test\\_status.py:1365) - disabling run attribution in close\\_owed.owed turns the criterion into a silent pass rather than a failure -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC1 to AC4 are otherwise soundly pinned - the raw-owed-list mutant kills three nodes, the repairs-subtraction mutant kills two, the units-limb-only mutant kills AC4 and the missing-BLOCKING-line mutant kills AC5 (all measured) -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; AC5's verifier does not run either command as a subprocess and never touches status, so the agreement the criterion is entirely about is unmeasured -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it.; the two surfaces do NOT name the same id set once more than five units block, so AC5 is unsatisfied by the delivered code -> Fixed, and the code was wrong rather than merely unpinned. `status`'s advisory named only the first five blocking ids\; it names them all now, so the two surfaces agree on the set. AC5's verifier drives BOTH shipped commands as subprocesses over an eight-unit fixture and asserts detect's owed exit, so the fixture is known to reach the state. Restoring the truncation reddens it. | - | delivery | 8194ce18d971 |
| BG0493 | 2026-09-10 | Claude Opus 5 | 2026-09-10 | AC5 is recorded Verified yes and its verifier does not do what AC5 says. AC5 is "Given a tree the checker REFUSES, when that lane is driven as a SUBPROCESS, then the gate refuses too, and given a tree it accepts, the lane passes". The delivered node test\\_the\\_lane\\_runs\\_the\\_checker\\_and\\_carries\\_its\\_exit (tools/tests/test\\_precommit\\_lane\\_order.py:466-490) starts no subprocess, builds neither a refusing tree nor an accepting one, and instead parses .githooks/pre-commit as text and asserts argv[0] is python3 and argv[1] names the module. Proven by execution: wrapping the whole practice-rules lane in a never-true condition, so the checker never runs on any commit, leaves BOTH PracticeRulesLaneTests nodes green. That is the dead-lane shape AC5 was written to refuse, one step along from the echo shape AC4 already covers. The only thing that caught it was test\\_message\\_first\\_gate.py LaneInventoryTests test\\_every\\_lane\\_runs\\_exactly\\_once\\_across\\_the\\_hook\\_pair, which drives the hook for real - a file outside this unit's declared Affects and named by no criterion here, so the protection exists by luck rather than by the contract -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; the shipped changelog fragment changelog.d/BG0493.md claims "the criterion pins that the lane RUNS it rather than merely naming it". Measured false on the same evidence - the pin is on the hook's text, and a lane that can never execute satisfies it -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; the commit edits AGENTS.md and tools/tests/test\\_message\\_first\\_gate.py, both outside the unit's declared Affects, while tools/tests/conftest.py and tools/tests/test\\_conftest\\_guard.py are declared and untouched. The AGENTS.md edit is forced by the hook-derived roster guard in tools/tests/test\\_check\\_spec\\_claims.py so it is defensible, but the Affects line no longer describes the diff -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; the artefact is still Status Open with every AC box unchecked while all five read Verified yes. MEASURED AND TRUE: AC2's plan-time number is exact - the old assertion's window, taken the way the old code took it as split on the keyword then sliced to 600, finds its or-true at relative offset 556 and never reaches the guard's own call, which sits at 626 (executed at d2e9fede~1). All five declared mutants killed by execution, including the AC5 echo mutant and the AC1 text-versus-AST mutant. The AC3 refusal runs as printed - exit 2 with "the practice ships with the skill, so its absence is a broken tree rather than a project without one. Restore it, or point --root at the tree that has it" - and the remedy it names works. The Revision History records the delivery and the two tests that failed to kill their own mutant first time. -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC5's verifier drives no subprocess and cannot fail on a lane that never executes, so the criterion recorded Verified yes is unmet, and with it the changelog's claim that the criterion pins the lane RUNS the checker -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC2's declared mutant SURVIVES - reverting LaneCheckLaneTests in test\\_gate.py to the fixed 600-character slice from the first mention leaves LaneCheckAnchorTests (test\\_gate.py:7337) green, and the reverted assertion passes too\; the new test reads .githooks/pre-commit and re-implements the anchoring, it never reads the assertion the criterion is about, so the one thing AC2 asks about is the one thing it cannot see (measured offsets in the hook: first mention 14628, guard call 15254, delta 626, and the 600-character window ends inside the id-gathering pipeline's or-true fallback) -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC5 requires the lane to be DRIVEN AS A SUBPROCESS against a tree the checker refuses and a tree it accepts, and test\\_precommit\\_lane\\_order.py:470 executes nothing - it parses the hook text for argv position and the enclosing run name\; measured, changing the lane command to add a --help flag, so it names the checker, runs the checker, and can never refuse anything, passes both PracticeRulesLaneTests and the whole of test\\_precommit\\_lane\\_order.py and test\\_message\\_first\\_gate.py (21 passed, 2 skipped), so the dead lane this criterion exists to refuse satisfies it -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; the commit message's five mutants five killed is refuted by execution for AC2, whose named mutant survives -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; tools/tests/conftest.py and tools/tests/test\\_conftest\\_guard.py are in the declared Affects and neither was modified by d2e9fede -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC4 is satisfied by a bare substring search for the checker's filename anywhere in the hook, which AC5 was written to compensate for and does not -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC2's verifier asserts on the hook rather than on the assertion, and its own declared mutant survives -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC5's verifier never drives the lane as a subprocess and a lane that runs the checker with --help satisfies it -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC5 demands the lane be DRIVEN AS A SUBPROCESS against a tree the checker refuses and a tree it accepts, but the verifier (test\\_precommit\\_lane\\_order.py:472-490) only parses the hook's source text - it runs no bash at all\; two behavioural mutants survive it: discarding the lane's exit inside the hook's run helper while leaving the lane text untouched, and pointing the lane at a root the checker refuses so it would fail EVERY commit (both measured green) -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC2 is about US0606's assertion, but the verifier (test\\_gate.py:7338-7353) never reads that assertion - it re-derives the anchoring from the hook text\; reverting US0606's assertion to the 600-character slice, the exact mutant AC2's own docstring names, leaves BOTH LaneCheckAnchorTests and LaneCheckLaneTests green (measured), so the anchoring repair is unpinned -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC4's verifier is a plain substring search over the whole hook file rather than an enumeration of lanes, so a mention inside a comment satisfies it - partly mitigated by AC5's line-position check -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AGENTS.md and tools/tests/test\\_message\\_first\\_gate.py were changed by this commit and appear in no declared Affects, so they sit outside the review scope and outside mutation registration, while two declared paths - tools/tests/conftest.py and tools/tests/test\\_conftest\\_guard.py - were not touched\; AC1's SUBSTANCE holds by execution - deleting the sys.path.insert call from conftest.py reddens BG0476's AC1 verifier, test\\_a\\_conftest\\_puts\\_this\\_directory\\_on\\_the\\_path - though the named selector exercises the shared AST helper rather than that verifier, so reverting the verifier alone to a text search leaves AC1's node green\; AC3 is a genuine subprocess test and its zero-return mutant dies, and AC4's lane-deletion mutant dies -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC5's verifier never drives the lane, so a lane whose exit is discarded and a lane that refuses every tree both pass it -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies.; AC2's verifier never reads US0606's assertion, so reverting the anchoring to the fixed window leaves the suite green -> Fixed. AC5's verifier takes the lane's own argv from the hook and EXECUTES it against two trees the checker judges differently, and a new AC6 executes the hook's own `run` definition against a failing and a passing command. Four mutants, four killed. AC2's verifier RUNS US0606's assertion against a fixture hook built so the anchored and sliced readings disagree, and the declared mutant dies. | - | delivery | 8ff40c8bbd0a |
| BG0578 | 2026-09-10 | Claude Opus 5 | 2026-09-10 | AC4 is recorded Verified yes and its fixture never builds the tie the criterion requires, so its declared mutant SURVIVES. The fixture body is a subject-marker line naming pkg/alpha.py followed by "alpha beta". Measured, the marker line itself supplies a second alpha, so the counts are alpha 2 against beta 1 - no tie. Strip the marker word and the counting route returns pkg/alpha.py by reference with no tie in sight. Executed: the declared mutant, returning the unattributed result whenever the counts tie before the declared routes are consulted, leaves the node GREEN, while the same mutant on a genuine tie changes the answer from the marker to None - so the mutant is a real behaviour change and the fixture is the fault. The commit message, the changelog and the delivery record all state "Eleven mutants, eleven killed". Measured: ten killed, one survives -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC6 is not met on its own terms. It demands the attribution map "taken over the real corpus THROUGH THE SHIPPED ENTRY POINT", and says the entry point requires a junit report so one is generated over the copied tree. The delivered node calls tc.attribute directly in a dict comprehension, starts no command, generates no junit report at all, and copies only .claude/skills/sdlc-studio/scripts, leaving the tools/ tree - half the corpus - out -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the shipped count is wrong. "The unattributed ratchet drops 38 to 33. Five files gained a home through a declaration" appears in the commit message, in changelog.d/BG0578.md, in the ratchet's own comment at tools/tests/test\\_test\\_census.py:290-292 and in the bug's Revision History. Measured with the ratchet's own computation: 37 unattributed at the base ref, and 37 at 681021cc~1, 681021cc and c3e58d78 too, against 33 after this commit. FOUR files gained a home, not five - test\\_cli\\_grammar.py by marker, and test\\_noise.py, test\\_boundary\\_marker.py and test\\_precommit\\_budget\\_recording.py by affects. The fifth unit of the drop was slack already sitting in the constant, and the ratchet comment now records an arithmetic a later auditor cannot reconcile -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the shipped help contradicts the tool. python3 tools/test\\_census.py --help prints the module docstring verbatim, and it still says "Attribution is by convention, in two passes" and lists only name and reference, closing with "Anything else is unattributed". Four routes ship now, and the author-facing convention the fix asks people to adopt appears nowhere outside the code and the changelog fragment - a grep for test-census-subject across the tree finds the regex, two fixtures, the changelog line and the one live marker -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the two Test Plan rows corrected at delivery were not carried into the tests, whose MUTANT docstrings still name the superseded mutants - "remove the marker branch so mention frequency decides the file again" and "drop the per-file owner from the entry point's payload". Artefact and test now disagree about what each node must fail on, and the docstrings name the very mutants the commit message says were wrong because four criteria shared one edit -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; six files silently changed OWNER, disclosed nowhere. Measured base against delivered: test\\_audit\\_profiles.py lessons.py to readiness.py, test\\_batch\\_selection.py status.py to sprint.py, test\\_detector\\_owed.py provenance.py to readiness.py, test\\_docs\\_single\\_writer.py sprint.py to mutation.py, test\\_help\\_structure.py sprint.py to doc\\_coverage.py, test\\_claim\\_drift\\_replay.py gate\\_timing.py to check\\_spec\\_claims.py. The changelog says only that five files gained a home and none by loosening the count, which reads as though nothing else moved - and an owner flip is the defect in this bug's own title -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC1's "95 of 204 files are placed by that route and only 73 by counting" is stale. Measured at the base ref: 206 files, 95 by name, 74 by counting. The 95 is right, the other two are not, and the text is unchanged by this diff -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the artefact is still Status Open with every AC box unchecked while all six read Verified yes. MEASURED AND TRUE: five of the six declared mutants killed by execution, the six verifiers are green, "none through a looser count" holds since the counting rule is byte-identical, and the Revision History records the delivery. -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC4's fixture builds no tie and its declared mutant survives, so the criterion recorded Verified yes is unmet and eleven-of-eleven mutants killed is false -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC6 uses no shipped entry point, no junit report and only half the corpus -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the shipped 38-to-33 and five-files-gained-a-home count is wrong in four places, measured four against a base of 37 -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; tools/test\\_census.py --help still describes two routes and documents the new test-census-subject convention nowhere a user reads -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC6 demands the owner map be taken THROUGH THE SHIPPED ENTRY POINT with a junit report generated over the copied tree, and test\\_test\\_census.py:944 uses neither - it calls tc.attribute per file, never census, never a junit report, never the CLI\; measured, AC6's own declared mutant (drop the per-file owner from the entry point's payload, emitting only the tally - implemented by popping module and test\\_files from every row census returns) leaves all six AttributionTests GREEN, so the criterion cannot kill the mutant it names -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; test\\_census.py:136 states the marker is checked at the head of the file only, so a mention of the marker inside a fixture's string cannot claim a subject - execution refutes it: \\_SUBJECT\\_RE at test\\_census.py:138 is re.M over the whole file, and a test-census-subject line placed inside a triple-quoted fixture string returns marker with the declared module, beating the by-name route\; nothing pins the head-only behaviour, so this diff re-opens its own bug class (an owner changing because somebody wrote a line of prose) -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the five-files number is FOUR - measured over the ratchet's own file set (bench fixtures excluded, 206 files both sides) parent unattributed is 37 and HEAD is 33, and the files that gained a home are test\\_cli\\_grammar.py, tools/test\\_noise.py, test\\_boundary\\_marker.py and test\\_precommit\\_budget\\_recording.py\; the wrong figure is repeated in the commit message, in changelog.d/BG0578.md and in the baseline comment at test\\_test\\_census.py:275 -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; undisclosed scope - 16 files moved from the counting route to a declared one and SIX of them changed owner outright (test\\_batch\\_selection.py from status.py to sprint.py, test\\_help\\_structure.py from sprint.py to doc\\_coverage.py, test\\_audit\\_profiles.py, test\\_detector\\_owed.py, test\\_docs\\_single\\_writer.py, test\\_claim\\_drift\\_replay.py), which the ratchet count cannot see and the fragment does not mention -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; a marker naming a path that is not on disk is silently ignored at test\\_census.py:200, so a renamed module turns a declaration back into counting with no signal -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC6's verifier does not enter the shipped entry point and its own declared mutant survives -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the head-of-the-file-only guarantee at test\\_census.py:136 is false of \\_SUBJECT\\_RE and is unpinned -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the five-files-gained-a-home figure is four, in all three prose surfaces -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC6 demands the attribution map be taken over the real corpus THROUGH THE SHIPPED ENTRY POINT, with a junit report generated over the copied tree, but the verifier (test\\_test\\_census.py:970-985) loops test\\_census.attribute directly, generates no junit report and never calls census, cmd\\_report or the CLI\; its own named mutant - drop the per-file owner from the entry point's payload - SURVIVES: deleting the test\\_files accumulation from census (test\\_census.py:297-299) leaves all six AttributionTests green (measured) -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC5's assertion compares only the BASENAME of the owner rather than its path (test\\_test\\_census.py:955), so two modules of the same name in different directories would compare equal -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; the durable comment recording the ratchet move (test\\_test\\_census.py:292-294) says five files gained a home\; measured base against head over the real corpus, FOUR did - test\\_cli\\_grammar.py by marker, tools/test\\_noise.py, test\\_boundary\\_marker.py and test\\_precommit\\_budget\\_recording.py by affects - and the base tree measured 37 unattributed against a declared baseline of 38, so the fifth step of the 38 to 33 move was pre-existing slack rather than a file gaining a home -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; six further files' owners MOVED between base and head as the affects route overtook counting (measured), which the changelog and comment do not record\; AC1 to AC5 are soundly pinned - deleting the marker lookup kills four nodes, first-of-many widening kills AC2, deleting the counting fallback kills AC2 and AC3, and deleting the affects route kills AC2 -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head.; AC6's verifier does not use the shipped entry point and cannot kill the mutant its own docstring names, so the criterion that the OWNER MAP is stable through the command is unmeasured -> Fixed. AC6 takes the owner map from the shipped entry point and asserts it is not empty, so the mutant that empties the payload cannot pass by making both sides of the comparison vacuous. AC4's fixture now ties, proven by a control with the same counting profile and no marker. AC7 added: the head-of-file guarantee the comment claimed is implemented and pinned. The count is corrected to four in every live surface, measured at the parent commit and at head. | - | delivery | 55fbfa1a39cc |
| BG0490 | 2026-09-10 | Claude Opus 5 | 2026-09-10 | the replacement check\\_versions.py module docstring ships a fresh false absolute in the very sentence AC5 exists to make honest - "Every version is extracted BY STRUCTURE - a declared field or a heading, never a mention in prose - so an incidental version in a sentence is ignored", closed by "by structure, never by prose is the guarantee". Refuted by execution against from\\_readme (tools/check\\_versions.py:80-88), which has two prose fallbacks after the structured one: a README head reading "back in the version 9.9.9 era" returns 9.9.9 and one reading "the v8.8.8 wheel" returns 8.8.8. Home 4 of the docstring's own list states that fallback, so the sentence is refuted four lines below itself. AC5's verifier only asserts two literal phrases are ABSENT, so it cannot see a replacement overstatement, and BG0490's own defect class is re-shipped in the fix for it -> Fixed, and the claim made TRUE rather than softened. Both prose fallbacks are gone from `from_readme` and the structured pattern covers the second bolded shape, which is the one this repository's own README uses - so the checker still resolves 5.0.1 here while a version mentioned only in prose yields nothing. Both fallback mutants killed. The vacuity guard beside it now EXECUTES discovery against a file nested three directories down, rather than testing that the word appears in the module.; the widened \\_PATH\\_CELL (tools/check\\_links.py:365) treats any dotted token as a path claim, so a guide cell whose whole content is a version now reports "5.0.1 names 5.0.1, which is not on disk - a reader told to load it gets nothing" (executed on a throwaway SKILL.md fixture). No such cell exists today, so this is a stated risk rather than a live break -> Fixed, and the claim made TRUE rather than softened. Both prose fallbacks are gone from `from_readme` and the structured pattern covers the second bolded shape, which is the one this repository's own README uses - so the checker still resolves 5.0.1 here while a version mentioned only in prose yields nothing. Both fallback mutants killed. The vacuity guard beside it now EXECUTES discovery against a file nested three directories down, rather than testing that the word appears in the module.; measured over the real skill the classification is byte-identical before and after the diff - 113 bare, 29 anchored, 16 prose, 11 templated, and ZERO invocation cells at both refs - so the new invocation-script resolution and the widened extension list have no observable effect on this tree and their value is entirely prospective -> Fixed, and the claim made TRUE rather than softened. Both prose fallbacks are gone from `from_readme` and the structured pattern covers the second bolded shape, which is the one this repository's own README uses - so the checker still resolves 5.0.1 here while a version mentioned only in prose yields nothing. Both fallback mutants killed. The vacuity guard beside it now EXECUTES discovery against a file nested three directories down, rather than testing that the word appears in the module.; BG0434, BG0435, BG0462 and BG0437 are all still Status Fixed with no revision row and no cross-reference to BG0490, so the four false Fixed records the Impact section names remain uncorrected in the ledger. The ACs are explicitly reframed as the contract for the reproducing halves only, so this does not hold the gate -> Fixed, and the claim made TRUE rather than softened. Both prose fallbacks are gone from `from_readme` and the structured pattern covers the second bolded shape, which is the one this repository's own README uses - so the checker still resolves 5.0.1 here while a version mentioned only in prose yields nothing. Both fallback mutants killed. The vacuity guard beside it now EXECUTES discovery against a file nested three directories down, rather than testing that the word appears in the module.; the artefact is still Status Open with every AC box unchecked while all five read Verified yes - uniform across all five units in this batch. MEASURED AND TRUE: all five declared mutants killed by execution (AC1 audit-profile row to a manual note, AC2 extension alternation narrowed, AC3 invocation script-token extraction deleted, AC4 report every invocation cell, AC5 absolute phrasing reinserted), all five verifiers green, the Revision History records the delivery and its two at-delivery corrections, and the audit profile's one script-naming signature row resolves. -> Fixed, and the claim made TRUE rather than softened. Both prose fallbacks are gone from `from_readme` and the structured pattern covers the second bolded shape, which is the one this repository's own README uses - so the checker still resolves 5.0.1 here while a version mentioned only in prose yields nothing. Both fallback mutants killed. The vacuity guard beside it now EXECUTES discovery against a file nested three directories down, rather than testing that the word appears in the module.; the check\\_versions.py docstring's new "never a mention in prose / by structure, never by prose is the guarantee" absolute, refuted by executing from\\_readme -> Fixed, and the claim made TRUE rather than softened. Both prose fallbacks are gone from `from_readme` and the structured pattern covers the second bolded shape, which is the one this repository's own README uses - so the checker still resolves 5.0.1 here while a version mentioned only in prose yields nothing. Both fallback mutants killed. The vacuity guard beside it now EXECUTES discovery against a file nested three directories down, rather than testing that the word appears in the module. | - | delivery | 7eadce7efe77 |
| BG0630 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> OVER-CLAIMED, and answered by execution. The ENTRY call site at transition.py:1256 carries no `force` guard either, so this gate has never been waivable at any call site: what the change removes is a route that reached no test-plan gate at all, which is the unit's whole point. `not force` was applied, broke the unit's own AC2 node, and was reverted with the reason recorded in the code. Making one firing forceable while its twin is not would let the same fact be waived or refused depending on the route taken; #2 -> FILED, not repaired. The remedy the refusal names does run - `testplan derive --dry-run` exits 0 on both machine-filed bugs checked - so the population is walled rather than trapped. Deciding what a machine-filed finding owes at its terminal transition is a design question and not a patch; #3 -> REPAIRED. Deduplicated on the FACT (`_TESTPLAN_FACT`) rather than the message string. Measured on one fixture through the shipped command: 3 requirements before, 1 after, and the absence is stated once. New AC6 pins it, with two mutants registered and killed; #4 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | - | delivery | 116c5569e05a |
| BG0656 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #4 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | - | delivery | 98ad4287c12a |
| BG0657 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #4 -> OVER-CLAIMED, refuted by execution on this tree. `verify_ac.py stamps --root . --bugs` reports `3 stamped AC(s)` and names exactly US0063::AC2, BG0357::AC4, BG0357::AC5 - the three the baseline records - and the shipped lane exits 0 with `dead-stamps: 3 (baseline 3) OK - identities match`. The seat measured 9 in an isolated clone | - | delivery | 9feea14ae247 |
| BG0612 | 2026-09-10 | x | 2026-09-10 | #1 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | the AC1 verifier gap | delivery | b322153d233a |
| BG0660 | 2026-09-10 | x | 2026-09-10 | #1 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #2 -> REPAIRED. The fixture is seven files - four offending shapes and three innocent ones - and the assertion is on the SET of files named, not a count; #3 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #4 -> REPAIRED. The fragment says what the sibling actually drives and what the scan now reaches, with the measurement; #5 -> CONFIRMED. The AC4 row is re-measured against the widened fixture and registered killed; #6 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #7 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | - | delivery | ac4a0e3f4ec9 |
| US0819 | 2026-09-10 | x | 2026-09-10 | #1 -> REPAIRED. `_PROBE_RUNNER_ERROR` carries the runner-error exit per runner family (`grep`: 2), and the branch reads it. AC5's fixture set gains the three shapes the criterion names by name - a malformed pattern, a missing path and a glob matching nothing - and all six subtests pass. Four declared rows re-measured and registered killed, one per limb; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> REPAIRED. The comment now states what the hook requires and what each wrong reading costs, with the measurement; #4 -> REPAIRED. The criterion is law: the constant now carries `not-probed` and the test asserts it. This also makes US0820's control fixture, which was written on the criterion's word, a real class - the mutant that seat proved survives is now killed; #5 -> REPAIRED. It reads the subject of every commit plus the `Refs:` trailers of DELIVERY commits only, keyed on the conventional-commit type, and it scans the message BODY rather than asking git for trailers - measured, git reads trailers from the last paragraph and this repository's `Refs:` block sits above the sign-off block, so the trailer lookup returns empty. All eight units delivered by f763a89a now read delivered | - | delivery | b6847d945342 |
| US0820 | 2026-09-10 | x | 2026-09-10 | #1 -> REPAIRED. `templates/config-defaults.yaml` gains the key with its three modes, the YAML-boolean caveat, the cost of probing and the shell-verb exemption; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> REPAIRED in the documentation rather than the behaviour: the config entry states that planning costs what those verifiers cost, with the measured per-unit figure. The behaviour is what the unit was built to do and both remaining seats confirmed the greenfield path is safe; #4 -> REPAIRED by US0819's delivered-detection fix; #5 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | - | delivery | fc8ee2e4735d |
| US0821 | 2026-09-10 | x | 2026-09-10 | #1 -> REPAIRED. `_ruling_cell` delegates to `_cell`, the escaping writer BG0658 added in the same run, and `read_rulings` reads with `_split_cells`. The node asserts the reason and the author character for character. The declared AC7 r1 mutant is restated to the change that now matters - substituting rather than escaping - and it is killed; #2 -> RULED. Non-blocking on the seat's own reading. A second digest means the criterion was rewritten, and the older row is reported STALE rather than live, which is the state AC4 names; #3 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #4 -> REPAIRED. `_ruling_cell` delegates to `_cell`, the escaping writer BG0658 added in the same run, and `read_rulings` reads with `_split_cells`. The node asserts the reason and the author character for character. The declared AC7 r1 mutant is restated to the change that now matters - substituting rather than escaping - and it is killed | - | delivery | fcfae6352534 |
| US0822 | 2026-09-10 | x | 2026-09-10 | #1 -> REPAIRED. Re-pointed at `EvidenceDriftTests::test_the_lane_names_only_the_rows_whose_anchors_moved`, the class the test was delivered in; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> REPAIRED. Each remedy found in the source is now parsed flag by flag, its placeholders filled from a throwaway fixture, and RUN - a fragment naming one flag is completed from the fixture, a full remedy is run exactly as printed. Registered mutant: dropping --anchor from the dirty-tree remedy, killed; #4 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #5 -> REPAIRED as a consequence of the plan\_execution fix: both readers now apply the same per-row rule, so the sentence is true again; #6 -> RULED. Non-blocking on the seat's own reading; #7 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #8 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | - | delivery | 8c24eebee7a5 |
| BG0612 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | the AC1 verifier gap | delivery | b322153d233a |
| BG0660 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #2 -> REPAIRED. The fixture is seven files - four offending shapes and three innocent ones - and the assertion is on the SET of files named, not a count; #3 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #4 -> REPAIRED. The fragment says what the sibling actually drives and what the scan now reaches, with the measurement; #5 -> CONFIRMED. The AC4 row is re-measured against the widened fixture and registered killed; #6 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #7 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | - | delivery | ac4a0e3f4ec9 |
| US0819 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> REPAIRED. `_PROBE_RUNNER_ERROR` carries the runner-error exit per runner family (`grep`: 2), and the branch reads it. AC5's fixture set gains the three shapes the criterion names by name - a malformed pattern, a missing path and a glob matching nothing - and all six subtests pass. Four declared rows re-measured and registered killed, one per limb; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> REPAIRED. The comment now states what the hook requires and what each wrong reading costs, with the measurement; #4 -> REPAIRED. The criterion is law: the constant now carries `not-probed` and the test asserts it. This also makes US0820's control fixture, which was written on the criterion's word, a real class - the mutant that seat proved survives is now killed; #5 -> REPAIRED. It reads the subject of every commit plus the `Refs:` trailers of DELIVERY commits only, keyed on the conventional-commit type, and it scans the message BODY rather than asking git for trailers - measured, git reads trailers from the last paragraph and this repository's `Refs:` block sits above the sign-off block, so the trailer lookup returns empty. All eight units delivered by f763a89a now read delivered | - | delivery | b6847d945342 |
| US0820 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> REPAIRED. `templates/config-defaults.yaml` gains the key with its three modes, the YAML-boolean caveat, the cost of probing and the shell-verb exemption; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> REPAIRED in the documentation rather than the behaviour: the config entry states that planning costs what those verifiers cost, with the measured per-unit figure. The behaviour is what the unit was built to do and both remaining seats confirmed the greenfield path is safe; #4 -> REPAIRED by US0819's delivered-detection fix; #5 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | - | delivery | fc8ee2e4735d |
| US0821 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> REPAIRED. `_ruling_cell` delegates to `_cell`, the escaping writer BG0658 added in the same run, and `read_rulings` reads with `_split_cells`. The node asserts the reason and the author character for character. The declared AC7 r1 mutant is restated to the change that now matters - substituting rather than escaping - and it is killed; #2 -> RULED. Non-blocking on the seat's own reading. A second digest means the criterion was rewritten, and the older row is reported STALE rather than live, which is the state AC4 names; #3 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #4 -> REPAIRED. `_ruling_cell` delegates to `_cell`, the escaping writer BG0658 added in the same run, and `read_rulings` reads with `_split_cells`. The node asserts the reason and the author character for character. The declared AC7 r1 mutant is restated to the change that now matters - substituting rather than escaping - and it is killed | - | delivery | fcfae6352534 |
| US0822 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> REPAIRED. Re-pointed at `EvidenceDriftTests::test_the_lane_names_only_the_rows_whose_anchors_moved`, the class the test was delivered in; #2 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #3 -> REPAIRED. Each remedy found in the source is now parsed flag by flag, its placeholders filled from a throwaway fixture, and RUN - a fragment naming one flag is completed from the fixture, a full remedy is run exactly as printed. Registered mutant: dropping --anchor from the dirty-tree remedy, killed; #4 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #5 -> REPAIRED as a consequence of the plan\_execution fix: both readers now apply the same per-row rule, so the sentence is true again; #6 -> RULED. Non-blocking on the seat's own reading; #7 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect; #8 -> ANSWERED by the repairs recorded against the findings above - this line is the seat's summary of them rather than a separate defect | - | delivery | 8c24eebee7a5 |
| BG0612 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> REPAIRED. The node now calls `verify_ac.testplan_row_faults` - the reader `testplan derive` calls - over each restorative phrase and each outcome phrase, and asserts on the named `edit verb` fault. Re-measured: deleting the limb at verify\_ac.py:3165 is now KILLED, and so is deleting the four restorative verbs, both registered as BG0612 AC1 r1 and r0; #2 -> REPAIRED. The docstring names what the node now does and records what the first cut asserted instead, with the measurement both seats took; #3 -> ANSWERED by the two closures above - it is the seat's own summary of the same defect | the AC1 verifier gap | delivery | b322153d233a |
| BG0612 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> REPAIRED. The verifier drives `verify_ac.testplan_row_faults`, the reader `testplan derive` calls, and asserts on the named `edit verb` fault rather than on the vocabulary constant. Both mutants the seats proved survive are now KILLED and registered: deleting the edit-verb limb at verify\_ac.py:3165 (AC1 r1) and deleting the four restorative verbs (AC1 r0); #2 -> REPAIRED. The docstring states what the node now does and what the first cut asserted instead, with the measurement; #3 -> ACKNOWLEDGED, no change. That is the seat's own reading of AC2 to AC4, and both nodes were re-run green after the AC1 repair | the AC1 verifier gap | delivery | b322153d233a |
| BG0612 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | the AC1 verifier gap -> REPAIRED and re-measured. AC1's verifier now drives `verify_ac.testplan_row_faults`, the reader `testplan derive` calls, and asserts on the named `edit verb` fault. The mutant both seats proved survives - deleting the edit-verb limb at verify\_ac.py:3165 - is KILLED, and is registered as BG0612 AC1 r1 beside the vocabulary mutant at r0 | test\_verify\_ac.py:4738 - AC1's verifier cannot observe what AC1 claims: it re-implements the predicate as any(v in phrase for v in verify\_ac.\_EDIT\_VERBS) instead of running testplan derive or testplan\_row\_faults, and never asserts a refusal. Proven by execution - deleting the whole edit-verb limb from verify\_ac.py:3165 leaves EditVerbVocabularyTests green (3 passed, 13 subtests); test\_verify\_ac.py:4739 - the docstring claims the node is driven through the cell reader testplan derive uses, which is false; AC2/AC3/AC4 are well built - AC3 execs a real modified copy of sprint\_report.py and AC4 execs the shipped module | delivery | b322153d233a |
| BG0660 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> REPAIRED. AC4's declared mutant is replaced by the two the reviewer verified, one per limb of the criterion: deleting the list and tuple flattening from `_boundary_calls` (the scan no longer sees BOTH invocations), and narrowing the scope test to the bare flags (a scoped call is named an offender). Both applied and KILLED, and the second reddens AC4's node alone with AC3 still green - the discrimination the criterion asks for and the old mutant never gave; #2 -> ACCEPTED. AC3 is not blind to a scanner that reports nothing, so AC4's value is classification rather than silence, and both replacement mutants are classification mutants; #3 -> ACKNOWLEDGED, no change. The assert-prefix guard is redundant given the `gate.py` filter, and removing a guard that costs nothing to keep, on a scan just widened three ways, is a change with more risk than value at a close; #4 -> REPAIRED. The joined command line is read now, for a RUNNER callee only - a runner's arguments are an argv, while the same words in a hook body written to a fixture or a Test Plan row quoting a command are text. Measured before that filter: four false positives, three of them fixture hook bodies and one a quoted row. Two offending fixture rows and one innocent one were added, and eleven shapes were probed against the final scan | AC4's declared mutant measures nothing | plan-review | bbd9774197c5 |
| BG0660 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | AC4's declared mutant measures nothing -> REPAIRED. The row is replaced by the two mutants the reviewer verified, one per limb of the criterion, and both are applied and KILLED: deleting the list and tuple flattening (the scan stops seeing BOTH invocations) and narrowing the scope test to the bare flags (a scoped call is named an offender). The second reddens AC4's node with AC3 still green, which is the discrimination the criterion asks for and the old mutant never gave. | AC4's declared mutant does not do what it says on the current tree - removing the only `yield` stops `_boundary_calls` being a generator, so `_scan` dies on a TypeError before either assertion is reached, and the same edit also reddens AC3, which refutes AC4's stated reason for existing; AC4's premise is wrong independently of the wording - the faithful version, a bare `return` before the walk, kills BOTH nodes, so AC3 is not blind to a scanner that reports nothing and AC4's distinct value is classification rather than silence; the `assert`-prefix guard in `_boundary_calls` is unkillable by any row, because the `gate.py` filter already excludes every assert-shaped call in both suites; a fifth call shape no row covers - a joined command line, `run("gate.py --boundary push", shell=True)` or the same string through `shlex.split`, gives seen=0, and this file already writes the first shape | plan-review | bbd9774197c5 |
| US0822 | 2026-09-10 | Claude Opus 5; session | 2026-09-10 | #1 -> REPAIRED. AC2's row now reads: widen `row_staleness`'s occurrence test from `== 1` to `<= 1`, so an anchor that has VANISHED from the target reads live. Applied and KILLED on AC2's own node, with AC1's and AC3's green; #2 -> REPAIRED. AC4's row now reads: return `live` from `row_staleness` when the row carries no anchor, instead of falling back to `entry_staleness`. That is the reading the reviewer proved reddens the test, applied and KILLED, and it names what the fall-through returns rather than leaving it to the reader; #3 -> ANSWERED by the two closures above, being the reviewer's summary of the same pair | - | plan-review | a55b768c599d |
| BG0663 | 2026-09-11 | Claude Opus 5; session | 2026-09-11 | #1 -> REPAIRED, every finding in the block. The four mutants are authored into the table and pass the derive guard at exit 0, with no placeholder left. The wordings are the reviewer's own, verified through a derive dry-run before they proposed them, and re-measured here as four applied and four killed. AC1 is measured under the precondition its mutant now NAMES - the runner empties the fragment directory, holding the files aside and restoring them in a finally with the set asserted byte-for-byte, confirms the control green in that state, then applies the mutant and records KILLED. AC1 is restated to the property its own node can establish, that the control passes in every fragment state because the verb the directory decides is no longer listed. AC2 r0 now says replace the initialiser with an empty dict literal keeping the binding, the reading that fires the criterion's assertion rather than crashing on a NameError. AC2 gains a row for the thin-reason half and AC3 gains one that extends the inventory to the whole surface. The Verification depth no longer claims each mutant reddens only its own node. The two findings that outlive this unit are FILED with the reviewer's measurements: BG0666 for the placeholder exemption that hid the first finding, BG0667 for the id-range-bound marker that makes a verdict oscillate with the release cycle; #2 -> ANSWERED by the closure above - it is the reviewer's summary of the same three blocking items | - | plan-review | a3a0fc582dba |
