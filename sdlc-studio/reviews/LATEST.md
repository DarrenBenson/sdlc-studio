<!-- close-status:begin -->
> **RUN-01M1WPNV closed goal-reached.** 5 unit(s) in the batch. **Sign-off is RECORDED** - nothing is owed on this run.
> Stamped by `sprint close` - edit the prose below, not this block.
<!-- close-status:end -->

> **Run of record:** RUN-01M1WPNV - five instruments answer for what they measured, not for what
> happened to be in front of them (D0182). Five units, 23 points, 84 mutants registered killed
> and two ruled equivalent; the rows of twelve earlier units re-measured after every shared-file
> edit. Goal **achieved**, every clause measured on main at 72e7ab71. Sign-off recorded on
> 2026-09-07 with the operator as principal, delegated in session; the push stays the operator's.

## THE HEADLINE: THE SEATS FOUND WHAT THE AUTHOR HAD NOT RUN

Thirty seat verdicts over five units, seventeen approvals and thirteen rejections, every rejection
earned by execution. A new boundary lane was red on this very repository because it exported an
absolute PYTHONPATH where the hook uses a relative one (BG0649); a run line's count was pinned by a
prefix match over a fixture that reached two of five branches (BG0646); a rejoinder printed a
fingerprint no matcher could reproduce (BG0645); a Done gate stopped refusing a misplaced red
criterion once the runner learned to skip it (BG0648). None was found by the author's own checks,
and each was a five-to-fifteen-minute check the author had not done: run the shipped command on
this repository, measure the number before writing it, write a mutant for every branch the
fixture never reached. The operator asked why reviews keep failing; the analysis is in RETRO0114
and the gates that would have caught each are filed as CR0564 to CR0568.

## WHAT LANDED

- BG0651 (0b9f4e12) - `mutation.py register` keeps another unit's rows and marks them stale; the
  pre-commit `evidence-drift` lane refuses a commit that drifts a delivered unit's rows, naming
  the unit, the file, the rows and the remedy. It refused four commits in this run, correctly.
- BG0646 (f6f34865) - `status.py` answers this corpus in under a second, headline first from the
  run record, census and advisories inside one corpus sweep reading each file once; the run
  line's count comes from `handoff._classify`, the one predicate `build` reads too.
- BG0649 (ea8a99a0) - `test_critic` imports what it uses; the `module-alone` boundary lane runs
  every skill test module alone under unittest from the repository root, in parallel with the
  `serial_only` partition after, refusing a partition it cannot read - four to five minutes on
  this machine, measured green on this repository.
- BG0645 (7d58b1ce) - a plan-review rejoinder keeps the plan-review shape; both rejoinders print a
  fingerprint the matcher reproduces, so `record --brief` recognises a re-review.
- BG0648 (72e7ab71) - a criterion outside `## Acceptance Criteria` is an error naming the heading,
  is neither run nor briefed, is named by lint and refused by the Done gate; US0597 and BG0270
  moved into their sections.

## WHAT IS OWED

- **The push.** Nine commits ahead of origin since e61b98fb; the pre-push hook pays the full
  boundary gate, now three lanes, about fifteen minutes. The operator pushes.
- **The gates the rounds asked for** (CR0564 to CR0568): coverage of new lines by a unit's own
  verifiers, a recorded self-run for gate-lane changes, numeric claims with no measurement, a
  generated mutation run at the done-gate, register replacing a duplicate row. Groom CR0564 and
  CR0565 first; each was one rejection this run.
- **BG0652** - `status.py hint` still costs 56 s outside any sweep. Eleven Lows in CR0511.

## READ NEXT

`sprint plan` with CR0564 and CR0565 groomed; before any brief, run the shipped command on this
repository, put the measuring command beside every number, and derive one mutant per new branch
from the diff. The memory of this run is RETRO0114's Lessons.
