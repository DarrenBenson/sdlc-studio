### BG0626

A Sprint Goal's own `(n)` numbering is discarded and the goal is split on every prose comma, so the close's clause panel judges fragments nobody committed to. Measured over all 47 recorded rounds: round 46's goal, which numbers five commitments, yields 15 fragments including the bare string `BG0618`, and 0 of 3 seats have ever carried a per-clause answer.
