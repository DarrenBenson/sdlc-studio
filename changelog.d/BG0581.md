- The reachable end state a plan reports is derived from the run's RUNG and the batch's own types. It reported the build rung's terminal for every rung, so a `design` rung - finished when its units are groomed - was described as reaching `Done`, work it never set out to do. It also applied the two-role cap to every unit, though that gate is guarded by `type == "story" and target == "Done"`, so a batch of bugs was reported as reaching `Review`, a state that is not in a bug's vocabulary at all.
