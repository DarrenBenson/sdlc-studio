<!-- section: Fixed -->
- **A retro's demonstration table is no longer invisible to the close (BG0459, partial).** The three worked-example rows under `## Actions raised` in the shipped retro template carried no `<!-- example -->` marker, so the leftover detector could not see them: a retro with every MARKED line replaced and that table untouched validated as `ok - 3 finding(s) all dispositioned`, which is a fabricated sprint record - a bug id nobody filed, a commit hash, a declined finding nobody raised - passing as a clean close. The rows are now marked, and the guard asserts the template's worked-example count EXACTLY rather than as a floor. A floor is why this survived: it tolerates a marker going missing, and stripping one from a carried bullet passed all 215 tests of the module. Both mutations now redden it. The close still discards the validator's report on a zero exit, which is tracked separately under the same bug.
