<!-- section: Fixed -->
- **Four of the eight guards that could not fail now bite (BG0401, partial - the bug stays open for the other four).** The closing review of the previous run applied mutants to its own repairs and eight survived, which means four delivered fixes could be reverted with no test going red.

  The index guard stated `<root>/sdlc-studio/sdlc-studio/epics`, because the type's directory already carries the `sdlc-studio/` prefix - so the predicate was False for every type, the missing-index list was unconditionally empty, and skipping an index entirely survived. It now stats the real path and carries a positive control, so a path typo reads as a broken guard rather than a clean tree.

  The close's goal judgement had no test asserting the CALL: every test called the function, so replacing `for line in close_goal_judgement(...)` with `for line in []` unwired the goal panel, the defect judgement, the prediction miss and the batch caller-check in silence. A sentinel is now patched into the mechanism and the close is RUN, which cannot pass if the call site is deleted and - unlike a source grep - proves the line runs rather than that it is present.

  The waiver double-report guard kept its only assertion behind `if carried:`, which was always False because its fixture's story was `Draft` and the lane judges delivered units. It iterated to nothing and passed however the code behaved. The fixture is now `Done`, the assertion runs, and the behaviour it asserts turns out to be correct - which nothing had established before.

  The untimed-lane guard re-implemented the gate's own formatting expression inline and asserted it against itself, so no change to `gate.py` could redden it. `lane_stamp` is extracted and asserted through, with a MEASURED zero pinned as distinct from an unmeasured lane.
