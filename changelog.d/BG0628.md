### BG0628

`conformance check` counts a unit whose verifier it could not resolve as a FAILURE rather than as unknown, so the same corpus scores 304, 671 or 732 conformant depending only on which directories were copied. Two independent reviewers reported the wrong figure as fact in one session.
