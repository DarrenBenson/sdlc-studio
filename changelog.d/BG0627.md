### BG0627

Eleven other `--fields-file` consumers across five modules carry the same `or ""` guard, so a falsey value is reported as a missing field there too. Split out of BG0622 because a two-point unit cannot carry a twenty-guard cross-module sweep, and because prose fields need the opposite rule from typed ones.
