# CLAUDE.md

This project uses the `sdlc-studio` skill for this task. Use it to drive the work:
create/derive the minimal relevant specification, plan, implement, verify, and reconcile,
scaled to the size of the ticket in `TICKET.md` (a small ticket does not need a full PRD -
use your judgement on how much of the pipeline a task this size actually warrants, the way
the skill's own doctrine intends). Run `/sdlc-studio help` if unsure how to invoke it.

Do not skip verification: run the resulting tests before declaring the task done.
